# THE LOOP — ledger (subconscious)

Running record of every finding across rounds. Re-fed into each mason launch so no
finding is re-surfaced. Entry shape:

    [R<round>] <perf|bug> <FIXED sha|REJECTED|ESCALATED> — <file:line> — <what>

---

[R1] bug   FIXED 0165714b (fix 02bb7ee3) — forwarding.rs:1594 — successor-erasure on fast module reconnect: remove_module_connection_locked deleted modules_by_id[M] unconditionally, erasing a live successor connection that reused module_id M after a fast disconnect/reconnect. Endpoint-fence fix (mirror abandoned_route_target:1577) + non-vacuous fail-first regression test. VERIFIED at source by Alfonso, gate green. Likely a contributor to today's auth-herd aft outage.
[R1] perf  ESCALATED — forwarding.rs:846 (lookup_data_route) — one process-wide std::sync::RwLock + Arc-clone per data frame, both directions; write-side cleanup holds it across whole-store scans. VERIFIED real, but it is the CONSCIOUSLY-ACCEPTED tradeoff from the forwarding refactor (2859b8b5), and the fix (sharded / snapshot-publish route index) is a concurrency-critical hot-path REDESIGN needing design-doc + Oracle before any worker touches it. NOT auto-fixed. Alfonso lean: readers get lock-free Arc<Snapshot>, writers rebuild-and-swap, control metadata stays behind the lock. Measure before/after with the new drop/route counters. → review with Ufuk tomorrow.
[R2] perf  FIXED 9517bfef (fix 9821b7e2) — clients/subc-client/src/socket.ts:233 — TS outbound write path did Buffer.from(bytes) copying the already-contiguous encodeFrame output a SECOND time before every net.Socket.write (bodies up to 64 MiB → double copy + GC pressure per frame). Fix: zero-copy Buffer.from(bytes.buffer, offset, len) view via toWriteBuffer helper; safe structurally because encodeFrame always allocates fresh per-call storage no caller retains (Alfonso confirmed the copy boundary + auth writes). Fail-first aliasing test with non-zero-offset subarray. VERIFIED at source, TS gate green.
[R2] bug   FIXED b1cdcd41 (fix 2ac2ebf8, merge 33ba90ba, fmt b1cdcd41) — supervise.rs:2192 — spawn failure during set_child_enabled stranded module in unretryable Starting: state=Starting written pre-spawn (2185), spawn ? propagated without rollback, retry gate no-op'd (revive_terminal only heals Failed/Stopped). GAP in Alfonso's OWN same-day fix 996c6bfe. Fix: roll Starting→Failed + untrack liveness on spawn error so the existing revive arm retries; non-vacuous fail-first test. VERIFIED at source, gate green.
[R3] perf  FIXED fe51f154 (fix 7d47d2b7) — subc-client-rs/src/consumer.rs:2985 (write_one_and_flush in writer_loop) — Rust consumer egress writer batched via try_recv drain but flushed per-frame, so F frames = F flushes/syscalls; correct batch-then-flush already existed in the same crate (lib.rs:1198 drain_writer). Fix mirrors it (write_frame no-flush first+drain, one flush after) preserving pending/accepted + NotSent/OutcomeUnknown classification exactly. Non-vacuous flush-count test (8→1 fail-first) + flush-failure classification test. VERIFIED at source, full gate green.
[R3] bug   REVERTED → ESCALATED (net master change: NONE for R3; revert ed03d03e undoes c7f32773). Timeline: (1) merged two-window cancel fix c7f32773; (2) backstop Oracle bg_12575e9b = BREAKS (zero-terminal host-hang: task.abort() kills waiter mid send_reverse_response under full egress); (3) fix-forward 0d795ceb detach-not-abort, gate-B + my trace clean, MERGE HELD on gating Oracle; (4) gating Oracle bg_16ea1e67 = NEW DEFECTS (detached waiter NOT ttl-bounded once host-arm wins the select!; two-terminal under duplicate-corr ABA reinsertion during blocked-send). ROOT CAUSE: reverse-request settlement state machine under-designed for backpressure×duplicate×teardown. Proper fix = settling-tombstone-until-terminal-enqueued + deadline-bounded enqueue + connection-terminal on undrainable egress = 1-2 day concurrency-critical REDESIGN. DECISION: reverted to production-dogfooded baseline (ed03d03e); the ORIGINAL spurious-cancel bug (fast host completion → stray notifications/cancelled → aft denies) is back but is the shipped/proven behavior + degrades to safe deny. All three variants (baseline stray-cancel / merged-fix drop / fix-fwd two-terminal) degrade to "deny bounded by aft ask-timeout", none hangs a human. → ESCALATED for design gate w/ Ufuk. Mason rejected (built spec faithfully; spec was insufficient). Oracle found: post-spawn "entry-gone -> task.abort()" branch can abort the waiter AFTER it removed the pending entry (settle) but BEFORE its forward completes when the relay egress channel is FULL (send_reverse_frame blocks) -> reverse response DROPPED with NO cancel (RelayCancelHandle has no Drop cancel) = ZERO terminal -> host hangs. My two-window fix removed the spurious cancel but converted drop-with-terminal into drop-with-zero-terminal under backpressure (host hang is arguably worse). SEPARATE flag: drop_route/GOODBYE never cancels an installed host cancel handle (zombie prompt) -- but design doc subc-reverse-request.md:136-143 says teardown is drop-only, so that may be CONTRACTUAL (escalate, not auto-fix). Gate-A verifying at source before fix. — reverse-request (elicitation/sampling/roots) spurious cancellation: on fast host completion the spawned waiter's settle_host_answer removes the pending entry + forwards the success response BEFORE the parent reacquires the lock (574), so the parent hits cancel_if_gone (579) and sends notifications/cancelled for an already-answered host request → double-signal on one JSON-RPC id. VERIFIED at source by Alfonso. Mason's 1-line sketch was INCOMPLETE (would regress case (a): teardown-during-send must still cancel the host). Authorized with a two-window split: pre-spawn install cancel (entry-gone-here = case a, cancel host + return); post-spawn install only the JoinHandle (entry-gone = settled or teardown-already-cancelled, NEVER re-cancel). Teardown paths unchanged.
[R4] bug   FIXED 6be116d1 (fix 87715455) — clients/subc-client-swift/Sources/SubcClient/Transport.swift:22,49 — Swift client socket created without SO_NOSIGPIPE and send(...,0) has no MSG_NOSIGNAL (Darwin has no such flag anyway), so a write to a peer-closed daemon socket raises SIGPIPE → default disposition KILLS the host process before the error path at :50 can throw. POSIXTransport is the ONLY transport (Client.swift:112), no global SIGPIPE ignore anywhere → shipped Swift client + SubcChat app hard-crash on any daemon bounce/GOODBYE mid-write. Mason repro exited 141 (SIGPIPE). Fix: setsockopt SO_NOSIGPIPE=1 right after socket() (fail-closed on setsockopt err), per-socket not global (library hygiene); send then returns EPIPE through existing throw. VERIFIED at source + scope-confirmed by Alfonso. NOT Oracle'd (deterministic repro-proven POSIX mechanism, not an interleaving/judgment call).
[R4] perf  FIXED a19bc508 (fix 2210df42) [Oracle bg_d7345f08 = SAFE, cleared the merge hold] — subc-core/src/server.rs connection_loop reader (frame_io.rs:128,157) — daemon reads every frame in 2-3 syscalls (5B prefix / 16B header-remainder / body, each a distinct AsyncRead::read) on a RAW unbuffered split TcpStream read half, on the hot per-frame routing loop. Fix: persistent tokio BufReader wrapping the post-split read half (created once, lives across loop iters). Cancel-safety: SAFE here because read_frame is only cancelled by the select close branch which RETURNS/tears down (no read-after-cancel path); auth completes before split so no handshake-byte swallow. Scoped DAEMON-ONLY; consumer.rs:2761 second site DEFERRED (needs own cancel-safety analysis). VERIFIED at source by Alfonso; merge gated on independent Oracle backstop per residual-doubt rule (BufReader-in-select is a textbook trap even though safe here).
[R3] esc   ESCALATED (contract call, from R3 backstop Oracle bg_12575e9b) — subc-mcp drop_route (main.rs:677-698) vs docs/subc-reverse-request.md settlement enum: route GOODBYE/teardown case (c) is DROP-ONLY by design (module side gone, late host answer matches nothing → dropped), but the explicit-Cancel case (d) DOES cancel the host prompt ("the ask's reason is gone — don't leave a zombie prompt up"). Inconsistency: a provider module dying mid-elicitation (GOODBYE) orphans the host's dialog (e.g. AFT bash-permission prompt), while an explicit cancel cleans it. NOT a correctness hang (host isn't protocol-blocked, just a stale UI dialog); low-severity UX. Alfonso lean: make (c) also cancel the installed host handle for consistency with (d) — cheap, but it's a documented-contract behavior change → Ufuk's call, not a loop auto-fix. Verified reachable + contractual at source. → morning ledger walk.
[R5] perf  ESCALATED (HIGH severity — arguably the night's most important find) — subc-core connection_loop (server.rs:357-375) + router.rs:452-465 handle_bound + forwarding.rs:1692-1698 — CONNECTION-WIDE HEAD-OF-LINE BLOCKING. The read loop routes each frame to completion before reading the next; routing a Request awaits route.flow.acquire() which blocks until a module terminal frees the per-route credit. So a saturated route (serial window=1, or ANY window fully in-flight) blocks the ENTIRE client connection read loop — independent channels AND the CANCEL frame that could free it. CONFIRMED AT SOURCE incl. the severity-critical consequence: CANCELLATION IS DEFEATED — a client that pipelined B behind long-running A cannot cancel A (CANCEL(A) sits unread behind B's blocked acquire at router.rs:465; A's credit frees only on A's own completion) → whole connection wedged until A self-completes. Reachable: TS client pipelines managed requests with no route-credit gate (client.ts:758-800). acquired_credit=Request-only (router.rs:463) means CANCEL is cheap to route but can't be REACHED. FIX = concurrency-critical redesign of the core dispatch loop (acquire off the read loop / per-route drain queue) — changes documented serial-dispatch intent (server.rs:177), backpressure-by-serialization (mem 7022), and the cancel-safety invariant the R4 BufReader Oracle relied on. PAIRS WITH R1 forwarding-lock escalation (same core-dispatch-path theme) — likely ONE design session. VERIFIED at source by Alfonso. Confidence 0.99. → morning ledger walk / design session.
[R5] bug   FIXED 37a6fc4c (fix 343a91df) — clients/subc-client-swift/Sources/SubcClient/Client.swift:640-641 — Swift session-event decoder does TRAPPING signed→unsigned conversions on module-provided wire data: (cursor?["wal_seq"] as? Int).map { UInt64($0) } and UInt32($0) for sub_index. A negative value on the wire (wal_seq:-1) → uncatchable Swift fatal error ("Negative value is not representable") → crashes the ENTIRE host process, bypassing decodeStreamEvent's throws contract. Daemon forwards StreamData bodies opaquely (router.rs:281-285, zero-deser by design) so the Swift client is the SOLE validator. Same class as R4 SIGPIPE (malformed wire → host crash). Reachable from any matching StreamData while runSessionTurn subscribed (Client.swift:372-375). Repro: wal_seq=-1 → exit 133. Fix: UInt64(exactly:)/UInt32(exactly:), throw SubcError on present-but-invalid, PRESERVE absent→0 default. VERIFIED at source by Alfonso. Localized, Swift-only, auto-flows. NOT Oracle'd (deterministic trap, not interleaving).
[R6] bug   FIXED fb5d9972 (fix dd32709c) — clients/subc-client-swift Transport.swift (no deinit, close() at :84 non-idempotent) + Client.swift:112-114 — Swift transport LEAKS its socket fd when auth throws: connect() opens POSIXTransport (112), then try authenticateClient (113) can throw (EOF/malformed/wrong-key) BEFORE ownership transfers to SubcClient (114); the dropped transport has no deinit so the raw fd is never closed → every failed/retried handshake leaks 1 fd → eventual EMFILE (breaks all subsequent connects AND file opens). Accelerated by a reconnect storm (tonight's auth-herd class). 3rd defect in the Swift transport surface (after SIGPIPE + cursor-trap) — all lifecycle/robustness gaps around the raw fd. Fix: RAII — add deinit (close-if-open), make close() idempotent via private var fd + closed sentinel (fd=-1) to also prevent double-close of a recycled descriptor. VERIFIED at source by Alfonso. Localized, Swift-only, auto-flows. NOT Oracle'd (deterministic resource-lifecycle, not interleaving).
[R6] perf  FIXED 0326c5dc (fix c90a2bae) — subc-client-rs/src/consumer.rs:1985-1993 route_state() — O(R) linear scan `routes.values().find(|r| r.handle == handle)` under the global mutex on EVERY explicit-handle request()/subscribe_route(), despite validate_current_handle() already doing an O(1) route_epochs.get(channel) check one line up. Doc (685-687) EXPLICITLY designs for "long-lived consumer that opens unbounded distinct routes, one per session" = the alfonso-core worker-substrate usage, so R grows → contended O(R) on the hot path. Fix: add O(1) channel→RouteKey secondary index (route_by_channel) maintained in LOCKSTEP with routes at every mutation site (mirror the existing route_epochs template 1:1), resolve route_state via index instead of scan. Localized index-maintenance, behavior-preserving, NO concurrency-model change → auto-flows (unlike R1/R5 dispatch escalations). Index-consistency is the risk (stale index if a mutation site missed) → lockstep + debug_assert 3-map agreement + lifecycle test. VERIFIED at source by Alfonso.
[R7] bug   FIXED 6092c269 (fix 0167c8c6) [class-elim: also fixed silent UInt8(truncatingIfNeeded:) normalization of key/daemon_id bytes → UInt8(exactly:)+throw, + JSON-decode error wrapping; full ConnectionFile.swift parser audit clean, no force-unwrap/as!/index] — ConnectionFile.swift:60 UInt16(port) TRAPPED on out-of-range port (65536/negative) from a malformed connection file → crashed the process instead of throwing ConnectionFileError (repro: 65536 → exit 133). Reachable via SubcClient.connect. 4TH Swift trap-on-untrusted-input this session (→ note #703). Fix: UInt16(exactly:)/UInt8(exactly:)+throw, JSON decode wrapped. Non-vacuous tests (port 65536/-1, out-of-range byte, truncated JSON all throw; well-formed unchanged). VERIFIED at source, swift gate green.
[R7] perf  ESCALATED (subc-protocol Frame payload-type design) — subc-client-rs/src/consumer.rs:552 SubcConsumer::call does body.clone() per send inside the retry loop to retain the caller body for rare unknown_channel/not_sent retry while the clone is moved into Frame::build (Frame owns Vec<u8>). Success-path retained copy unused → redundant (up to 64MiB cap; payload-size-dependent — modest for small tool-call JSON, real for MB file payloads). The clone is the PRICE OF TRANSPARENT RETRY (unknown_channel round-trips → frame's copy gone to wire → needs pre-send retained copy), NOT gratuitous — request() (no retry) correctly moves. Clean fix = bytes::Bytes/Arc<[u8]> shared payload for O(1) retain, but that changes the Frame body type in the PUBLISHED subc-protocol wire crate → fleet-wide (every module+client builds Frames, byte-identical wire) = architecture decision. Local alternatives just relocate the single necessary frame copy. VERIFIED at source by Alfonso. → morning walk (adopt bytes::Bytes in wire Frame fleet-wide).
[R8] perf  FIXED 0787b56a (fix 12b37d31) [follow-up noted: client.ts:1204 encode() send-side copy remains] — clients/subc-client/src/client.ts:1208-1209 parseJson — Buffer.from(frame.body).toString("utf8") COPIES every JSON response body before UTF-8 decode (Buffer.from(uint8array) allocates+copies). Single decode point for data-plane replies (392-400) + managed-call replies (726-732) → cost scales with large tool results. Same class as R2 (write-path copy), read-path side. Mason benchmarked 58.9ms→28.5ms (1MiB×1000). Fix: zero-copy view Buffer.from(b.buffer, b.byteOffset, b.byteLength) — safe because byteOffset/byteLength exactly window b even if pooled, and .toString() is synchronous (no retention). Crux test: body as non-zero-offset view into a larger buffer with garbage outside the window → decodes exactly (fails if offset arithmetic wrong). VERIFIED at source by Alfonso. Localized TS-only, auto-flows.
[R8] bug   ESCALATED (auth DoS-bound vs herd-liveness contract call — code Alfonso wrote TONIGHT) — server.rs:195-212 + auth.rs:157 — the pre-auth capacity wait (up to auth.deadline for a permit) does NOT charge against the handshake budget: authenticate_server does Deadline::starting_now(auth.deadline) fresh, so total pre-auth occupancy is up to 2× deadline, contradicting the server.rs:194 comment. Trigger: hold all unauth permits, queue a peer near-deadline, release a permit, slow-roll handshake → ~2× bound (~4s vs 2s default). VERIFIED at source. NOT auto-fixed: the obvious single-shared-deadline fix would give a near-full-queued supervised module ~0 handshake budget under CPU saturation → auth fail → reintroduces the fatal-auth-failure→restart-budget-burn the herd fix (this same code, tonight's outage) prevents. Genuine DoS-tightness vs herd-liveness tension on the auth security contract; low severity (loopback-only trusted-local). 3 options: A single shared deadline (tight/herd-risky), B formalize 2 explicit budgets + honest comment (herd-safe/2× bound), C explicit smaller configurable queue budget. Alfonso lean B or C. → morning walk.
[R9] perf  FIXED cf00d0cd (fix b8d561d8) — clients/subc-client-swift/Sources/SubcClient/Transport.swift:69-81 readExact — allocates [UInt8] buffer, recvs into it, then `return Data(buf)` COPIES the whole array into fresh Data (double alloc + full-body copy per frame body). readFrame routes every frame body through readExact (Client.swift ingress). Fix: recv directly into var data = Data(count: n) via withUnsafeMutableBytes, return it (behavior-identical, elides the copy). Mason benchmarked ~1.7x construction throughput. Safe: Data(count:) is contiguous/zero-filled/uniquely-owned, no CoW surprise; same partial-read loop + EOF/errno messages preserved. Test: chunked partial-read equivalence + EOF. VERIFIED at source by Alfonso. Localized Swift-only, auto-flows (localized vein not dry).
[R9] bug   FIXED 5b57dd66 (fix 1816357a) [uniform fix both comment arms; subc-core/daemon_config verified unaffected] — crates/subc-jsonc/src/lib.rs:38-55 (block-comment arm) — block comments removed WITHOUT replacement whitespace (only embedded newlines re-emitted), so a zero-newline block comment MERGES adjacent tokens: `8/* x */123` → `8123` instead of the correct `8 123` (which serde should reject). JSONC comments are lexical whitespace. Consequence: daemon_config.rs deserializes the normalized output directly → a mid-token block comment in subc.jsonc silently accepts malformed config (e.g. wrong port 8123) and can start the daemon on an unintended port. Line-comment arm (29-37) is SAFE by construction (newline/EOF-terminated). Fix: replace removed comment with ≥1 space, preserve embedded newlines (uniform across both arms for defense-in-depth). VERIFIED at source by Alfonso. Localized (subc-jsonc utility crate), behavior-preserving for well-formed input, auto-flows. NOT Oracle'd (deterministic lexing).
[R10] bug  FIXED 779ca514 (fix 98553695) — crates/subc-core/src/watchdog.rs:304-350 verify_connection_file — the self-watchdog's connection-file integrity check compares port/key/wire_version but NOT daemon_id, though daemon_id is an authenticated handshake input (server proves possession; clients read it from the file and reject with DaemonIdMismatch on mismatch — confirmed via passing wrong_daemon_id_aborts_without_client_auth transport test). A corrupted/stale daemon_id in the published file makes EVERY real client reject the daemon (unusable connection file) while the watchdog stays GREEN — the watchdog authenticates its own loopback via in-memory live_connection_info so it never hits the mismatch itself, which is exactly why it needs the explicit field comparison. Detection-gap in the watchdog's whole purpose (catch unusable connection file). Fix: add daemon_id divergence check symmetric with the existing key check (332-334). VERIFIED at source by Alfonso. Localized, behavior-preserving, auto-flows. NOT Oracle'd (deterministic field-comparison gap).
[R10] perf  FIXED a5601bc8 (fix c4f91b45) [EQUIVALENCE-GATED: differential test vs round-trip oracle across ~60 cases green; Alfonso line-by-line source-verified validator==decodeHeader] — clients/subc-client/src/envelope.ts:213 buildFrameWithVersion validates via decodeHeader(encodeHeader(header)) — full round-trip + throwaway 21-byte alloc — then encodeFrame:225 re-encodes the same header (encodeHeader ×2 + decodeHeader ×1 + alloc per outbound frame). Warm path both TS roles; mason benched ~2× on small-frame build+encode. Fix: extract non-allocating validateHeaderFields reproducing the round-trip's UNION behavior (encode-side range/coercion + decode-side version/enum/bits + DecodeError code+order) EXACTLY, call it in build, encodeFrame stays sole serializer. RISK: a missed/divergent check lets a malformed header reach the wire → SAFE ONLY via a differential-equivalence test (new path vs old round-trip oracle, boundary+out-of-range every field, same throw-code / same resulting header). Gate-B on that test's rigor. VERIFIED at source by Alfonso. Localized TS-only. Mason also swept Rust transport/daemon/clients + Swift envelope: no stronger non-off-limits finding (diminishing-returns signal).
[R11] bug  ESCALATED (flow-control hardening vs thin-core trusted-module contract — documented design tradeoff) — router.rs:307-308 + forwarding.rs:1702-1731 ChannelFlow::release — non-serial flow-control credits double-released by a DUPLICATE TERMINAL. Router releases credit purely on is_terminal_frame(ty), no corr; release() decrements any positive in_flight + adds a permit. The in_flight==0 guard catches a duplicate ONLY when nothing else is outstanding → gap = duplicate-terminal-WHILE-CONCURRENT: 32-window, terminal for A then duplicate-A while 31 outstanding → in_flight 32→31→30, 2 permits for 1 completion → window inflates past cap + in_flight() undercounts (reload drain can declare quiescence while requests remain / teardown-while-active). VERIFIED mechanism at source. NOT auto-fixed: (1) DOCUMENTED design decision — release() comment says modules trusted to emit one terminal/request, guard is 'best-effort... not a security boundary against malicious peers'; aggregate-count is deliberate thin-core (mem 7022). (2) correct fix = per-corr outstanding-set in ChannelFlow (release(corr) removes-once) → reverses thin-core + adds lock+set to per-frame hot path (R1/R5 contention path) = flow-control-model change w/ hot-path cost. (3) low severity: loopback, first-party spawn-attested modules, duplicate terminal = module bug, simple non-concurrent case already caught. → morning walk; pairs with R1/R5 core-flow-path escalations.
[R11] perf  FIXED 56a7d00c (fix fbc744c3) [completes TS copy trilogy R2/R8/R11; 2048-frame pool-interleave test proves no cross-contamination] — clients/subc-client/src/client.ts:1204-1205 encode() — `new Uint8Array(Buffer.from(JSON.stringify(value),"utf8"))` COPIES the Buffer bytes into a fresh Uint8Array; runs for every object-valued request (343/355/395/462/521/727) before encodeFrame copies the body into the frame = one avoidable full-body alloc+memcpy per JSON request. The send-side completion of R2 (write copy) + R8 (read copy). Fix: return Buffer.from(...) directly (Buffer IS Uint8Array subclass; downstream uses .length + .set() which are view-correct). Safe: encodeFrame .set() respects logical view so pool byteOffset transparent; Node pool never re-hands live region + synchronous memcpy = no overwrite. Crux test: multi-frame pool-interleaving (collect N encoded bodies THEN encodeFrame each, assert each matches its own JSON) proves no pool cross-contamination + byte-identity vs old oracle. Mason benched 16.9% (1KiB) / 3.9% (64KiB) encode time. VERIFIED at source by Alfonso. Localized TS-only, auto-flows.
[R12] bug  FIXED 6b8f0c83 (fix 616c7c87) — clients/subc-client-swift/Sources/SubcClient/Envelope.swift:234-254 encodeFrame — omits the 64 MiB outbound body-limit check that Rust (frame.rs:43-55), TS (envelope.ts:252-264), AND the daemon (frame_io.rs:73-78) all enforce; also UInt32(body.count) TRAPS for body > UInt32.max. Swift client is asymmetric: the READ path (Transport.swift:106) enforces MAX_FRAME_BODY_LEN but encode does not. Two failure modes: (a) 64MiB<size<=UInt32.max → emits a frame the daemon rejects + closes the connection (self-inflicted disconnect); (b) size>UInt32.max → process trap (6th Swift-client robustness gap, wire-parity divergence). All 4 callers (Client.swift:243/477/498/598) already in throws contexts (try writeAll). Fix: make encodeFrame throwing, guard body.count <= MAX_FRAME_BODY_LEN with a typed FrameEncodeError(len:Int untruncated), UInt32(exactly:) defense, add try at call sites. VERIFIED at source by Alfonso. Localized Swift-only, auto-flows, makes client wire-parity-correct. NOT Oracle'd (deterministic cap-omission).
[R12] perf  NONE FOUND (honest stop-signal — perf lane exhausted) — mason swept daemon framing/router/egress, Rust module+consumer loops, TS socket/client/provider dispatch, Swift transport/client streaming, subc-mcp reader/writer; reported no localized high-confidence perf finding remains (remaining candidates were off-limits repeat themes or cold/control-path micro-costs). Matches Alfonso's read: after 11 rounds the localized-perf vein is mined out (TS copy trilogy complete, Swift readExact done, Rust flush/index done, daemon BufReader done). PERF LANE TERMINATED. Remaining perf wins are all ESCALATED architecture items (forwarding-lock, HOL-blocking, Frame-bytes, flow-control) for the morning design walk.
[R13] bug  FIXED c9ee0724 (fix f7ccb2f7) — crates/subc-core/src/supervise.rs:1672-1688 health_restart_child — SIBLING of R2/996c6bfe (Starting-strand). Health-triggered respawn failure strands module in Restarting: drains to ModuleState::Restarting (1680) + tracks liveness (1685), then spawn_and_mark_running(...)? (1686) propagates spawn error WITHOUT rollback; caller (1601-1603) only logs. Trigger: health.on_failing=restart, module reports failing, executable non-spawnable at respawn (unlink/chmod mid-deploy or OS spawn fail) → child=None, enabled=true, state=Restarting forever → command-only mode never retries + set_enabled(true)/start no-ops (revive_terminal heals only Failed/Stopped) → needs manual supervisor.restart. This is the HEALTH-PATH analog of tonight's opening outage class. Crash-restart path (1811-1822) already does it RIGHT (fail_snapshot->Failed + untrack_if_current + child=None). Fix: mirror that in health_restart_child's spawn-Err arm so revive_terminal can heal it. VERIFIED at source by Alfonso. Localized, mirrors existing correct pattern, auto-flows. NOT Oracle'd (deterministic state-transition gap).
[R14] bug  ESCALATED (TS/Rust client parity + published-API decision) — clients/subc-client/src/client.ts:126-133 ManagedCallOptions has no consumerCapabilities field (CachedRoute doesn't retain it; initial 809-862 + reconnect 974-980 pass only consumer_identity), while direct routeOpen HAS it (354) AND the Rust client's managed path HAS it (consumer.rs:114, canonicalized into RouteKey, reconnect-preserved). Gap: a TS consumer using call() can't declare reverse-request capabilities → provider can't issue elicitation/sampling/roots mid-call. NOT auto-fixed: (1) adds PUBLIC API to published @cortexkit/subc-client 0.5.0 + changes managed-route cache-KEY identity (capabilities must canonicalize into key or wrong cached route reused) = API/product decision; (2) NO confirmed live-broken consumer (ALF on Rust client; AFT prod elicitation via subc-mcp Rust gateway, not TS managed call) — contract hole not live break; (3) parity/missing-capability gap, not crash/race/leak/strand. Fix settled-by-Rust-precedent (mirror consumer.rs). VERIFIED at source by Alfonso. → morning walk (fill to match Rust + next release sign-off). ALSO = BUG-LANE WIND-DOWN SIGNAL: clean defects exhausted, remaining findings are product/API parity gaps (bug-lane analog of perf-lane exhaustion).
[R15] bug  FIXED 17cc475c (fix ab0cc5e3) [bonus: defer placement also plugs commandRoute-orphaned-if-subscribe-open-throws leak] — clients/subc-client-swift/Sources/SubcClient/Client.swift:278-386 runSessionTurn — ROUTE LEAK: opens 2 fresh routes/turn (command 280 + subscribe 287, routeOpenManagementSurface mints a new channel/epoch each call) but NO exit path (run_finished return 377, streamEnd 380, error throws 355/366/382, defer 341-344 only cleans local inFlight keys) ever GOODBYEs either route. Daemon removes routes only on GOODBYE (not terminal frames) → each completed turn leaks 2 daemon-side slots → repeated runSessionTurn on one persistent client (the normal SubcChat multi-turn pattern) exhausts channel space → ClientRouteChannelExhausted; run_finished exit also leaves the subscription request holding flow credit (no CANCEL). (Finding's "no close path exists" framing was imprecise — closeRoute/writeGoodbye DO exist at 254/588, just aren't called.) 7th Swift-client robustness gap (note #703). Fix: defer-based best-effort cleanup on EVERY exit — CANCEL subscription if not StreamEnded + try? GOODBYE both routes (swallow errors, NO transport-close fallback: must preserve the persistent connection for the next turn). VERIFIED at source by Alfonso. Localized to runSessionTurn, happy-path-preserving, auto-flows. NOT Oracle'd (deterministic resource leak). Bug lane still productive (confirmation probe R15 found a real leak, not the expected none/escalation).
[R16] bug  ESCALATED (Rust client close-under-backpressure contract) — crates/subc-client-rs/src/consumer.rs:2099,2137 close_handle/close_route call send_route_goodbye(handle,false); send_route_goodbye (2184-2224) try_sends the GOODBYE and on Full returns false WITHOUT dropping the connection; callers ignore false, uninstall handle, report success → daemon keeps both forwarding halves (control.rs:439-456 releases only on received GOODBYE) until connection dies. Queue bounded 128 vs route window 1024 → request burst fills it. Rust sibling of R15 Swift leak class. NOT auto-fixed: 3 fix options each fork the published client's close-under-backpressure contract (ALF worker-substrate is primary consumer): (A) close_on_failure=true → no leak but drops connection = kills co-tenant routes (anti-multiplexing); (B) block-with-timeout → no collateral but close can hang + sync→async; (C) orphan-GOODBYE retry queue → cleanest but real state. Low severity (narrow: full-128 egress at close moment, self-heals on next connection drop). VERIFIED at source by Alfonso. → morning walk (pick semantics w/ ALF); pairs w/ R15 route-cleanup-under-backpressure theme.
[R17] bug  FIXED dc29b17b (fix c53c98f6) [sibling-path: health path had the guard, data path didn't; test also proves finally runs on early-return via inflight.size===0] — clients/subc-client/src/provider.ts:859-891 handleDataRequest — runs the handler for a request CANCELLED WHILE QUEUED: registers AbortController (860), awaits requestGate.acquire() (888, blocks on handler-capacity saturation), then UNCONDITIONALLY calls opts.handler (891); a CANCEL/route-teardown aborting the controller DURING the acquire wait is ignored → handler runs + can do mutating side effects (post-handler check at 892 is too late). At-most-once/cancel-safety gap. The sibling handleHealthRequest (946-948) does it RIGHT: post-acquire `if (controller.signal.aborted) return;` BEFORE calling health(). Fix: add the same pre-handler guard as the first statement in the try (finally still releases permit + cleans inflight), KEEP the post-handler check (guards abort-during-handler). Mirror line 948. LOCALIZED auto-fixable (the clean fix the r17 localized-only probe was looking for — loop NOT done). VERIFIED at source by Alfonso. Behavior-preserving except the cancelled-while-queued case (which should not run). NOT Oracle'd (deterministic missing-guard, proven in-file pattern).
[R18] bug  FIXED f49d0db3 (fix 2575c88a) [used canonical settle() helper = byte-identical to StreamEnd settle w/ built-in identity guard; isLiveHandle-gated cancel for 2nd stale-handle-throw issue] — clients/subc-client/src/client.ts:496-500 Subscription.unsubscribe — SIBLING-PATH GAP (the pattern this round targeted): TS unsubscribe only sends Cancel; never settles the `closed` promise nor removes the pending entry. Provider suppresses terminal output after cancel-abort → no terminal frame → closed HANGS FOREVER + 1 pending entry leaks until route/connection teardown. Rust sibling (consumer.rs:1798-1810 unsubscribe_subscription) does it right: remove pending → settle_subscription_result(Ok()) → send_cancel. Live test (live-streaming.test.ts:88-92) already asserts unsubscribe settles closed = documented contract TS violates. 2ND latent issue same closure: this.cancel→assertLiveHandle (506) can THROW out of a void unsubscribe if handle stale post-reconnect. Fix: mirror Rust — identity-guarded local remove+resolve(closed) FIRST, then best-effort cancel (swallow stale-handle throw); late frames match nothing (identity-safe), onEvent can't fire after unsubscribe. VERIFIED at source by Alfonso. Localized TS-only, auto-fixable, behavior-preserving except the hang/leak/throw. NOT Oracle'd (deterministic sibling-gap w/ Rust reference + live test spec).
[R19] bug  FIXED 307c23bd (fix 581998ec) [R17+R19 = same cancel-while-queued gap closed across BOTH TS provider + Rust client; Swift provider still to check] — crates/subc-client-rs/src/lib.rs:884-896 spawn_data_request — RUST-CLIENT SIBLING of R17 (same cancel-while-queued gap, higher severity: this is ALF's alfonso-core worker substrate w/ real mutating handlers). After permits.acquire_owned().await (885) calls handler.handle (891) UNCONDITIONALLY; a CANCEL/teardown firing the CancellationToken while queued for a permit is ignored → handler runs + mutating side effects for an already-cancelled request. Sibling spawn_health_request (921-928) does it right: `if !cancellation.is_cancelled()` (928) before health(). Fix: mirror 928 — post-acquire pre-handler is_cancelled() check that removes the in-flight entry (mirror permit-closed arm 886-888) + returns. VERIFIED at source by Alfonso. 5th sibling-path fix (R2/R13/R17/R18/R19). Localized Rust-only, auto-fixable, behavior-preserving except cancelled-while-queued. NOT Oracle'd (deterministic missing-guard, proven in-file pattern).
[R20] bug  FIXED a8584d75 (fix f11d0f86) [Alfonso scoped to Push arm only after verifying Response/Error arms leak-free via reader terminal-removal] — crates/subc-mcp/src/main.rs:3547-3549 tool_call_over_route Push arm (FORWARD path, distinct from off-limits ReverseRelay) — malformed provider progress PUSH propagates via forward_progress(...).await? WITHOUT send_route_cancel/abandon_request → pending entry + held route request LEAK. Verified full mechanism: reader loop (3824-3830) removes pending on TERMINAL frames but only CLONES for non-terminal Push, so Response(3550)/Error(3558) arms are already leak-free (terminal auto-removed) but the Push arm's ?-return drops reply_rx WITHOUT removing pending; provider that sends malformed progress + NO terminal (reachable, phase1_integration.rs:3947-3959) leaks until connection teardown. Sibling cancellation branch (3525-3533) demonstrates the fix: best-effort send_route_cancel + unconditional abandon_request before returning. Fix scoped to the Push arm ONLY (Response/Error correct as-is). VERIFIED at source by Alfonso. 6th sibling-path fix (R2/R13/R17/R18/R19/R20). Localized, auto-fixable. NOT Oracle'd (deterministic missing-cleanup, sibling-demonstrated pattern).
[R21] bug  FIXED 51fc315b (fix 0d9b9063) — clients/subc-client-swift/Sources/SubcClient/Auth.swift:46-50 randomNonce — SECURITY: discards SecRandomCopyBytes' OSStatus (`_ = SecRandomCopyBytes(...)`), so on RNG failure the client nonce stays ALL-ZERO/partial and authenticateClient (85) sends it → predictable nonce weakens the HMAC challenge-response handshake. File header (5-8) mandates byte-for-byte parity with Rust; Rust sibling propagates getrandom's Result (fails loud), TS sibling's randomBytes throws — Swift silently proceeds. SIBLING-PATH GAP. randomNonce called only at 85 inside authenticateClient (already throws). Fix: make randomNonce throwing, guard status==errSecSuccess else throw AuthError, `try randomNonce` at call site; success path + wire byte-identical. VERIFIED at source by Alfonso. 7th sibling-path fix (R2/R13/R17/R18/R19/R20/R21). Localized Swift-only, auto-fixable. NOT Oracle'd (deterministic dropped-status). NOTE: RNG-failure path can't be deterministically unit-tested without RNG injection (out of scope for a 1-line security fix) — positive non-zero-nonce regression test only.
[R22] bug  FIXED 6c3be6a6 (fix 98a45267) [Alfonso EXPANDED scope: 2 sites not 1] — clients/subc-client/src/provider.ts:548 SubcProvider.cancel AND :559 SubcProvider.closeRoute — both do `void this.sendOn(...)` (Cancel / Goodbye) with NO rejection handler; sendOn rejects on socket-close/write-fail/timeout → UNHANDLED promise rejection can terminate the Node host process. Discarded-error-status + sibling-path gap: the sibling SubcClient.cancel (client.ts:521) correctly does `.catch(() => undefined)`. Finding named only cancel; I verified every void-sendOn site — closeRoute (559) has the identical defect (missed by mason); every OTHER sendOn is safe (await or explicit .catch). Fix: `.catch(() => undefined)` at BOTH 548 + 559, mirroring the sibling; keep `: void` best-effort semantics. VERIFIED at source by Alfonso. 8th sibling-path fix. Localized TS-only, auto-fixable. NOT Oracle'd (deterministic unhandled-rejection).
[R23] bug  FIXED 805709ad (fix 10bb3e4a) [LOW severity — robustness/diagnostics, NOT security; 3 sites] — JSON byte-array values silently coerced instead of rejected, completing the R7 class (R7 fixed only Swift ConnectionFile.swift; class persisted in 3 more byte-parse sites): (1) clients/subc-client/src/connection-file.ts:33-38 toBytes (Uint8Array.from) for key+daemon_id (111-112); (2) clients/subc-client/src/auth.ts:106-108 (Uint8Array.from) server_nonce/daemon_id/server_proof; (3) clients/subc-client-swift/Auth.swift:80-83 jsonBytes (UInt8(truncatingIfNeeded:)). Out-of-range value 427→171 (verified). Rust serde [u8;N] + Swift ConnectionFile siblings reject. HONEST SEVERITY: I traced downstream — coerced bytes feed computeProof+constantTimeEq which FAIL CLOSED, so coercion CANNOT cause a false-accept; consequence is a corrupt connection file → confusing "proof mismatch" instead of precise "invalid byte 427" (diagnostics + spec-compliance + house fail-loud style), NOT an exploit. Fix: strict finite-integer 0..255 validation before conversion (Number.isInteger guards / UInt8(exactly:)), throw existing error type, all 3 sites. VERIFIED at source by Alfonso. Localized, auto-fixable. NOT Oracle'd.
[R24] bug  FIXED 9b995560 (fix 8855ee42) [LOW severity — robustness/spec-compliance, NOT security] — clients/subc-client-swift/Sources/SubcClient/Client.swift:572-589 installRouteOpen — parses route.open route_channel/route_epoch as NSNumber then int64Value/uint64Value → TRUNCATES fractional JSON (1.5→1) and COERCES JSON booleans (true→1) into valid-looking route handles. Swift wire-decode-strictness sibling of R5 (cursor int trap) + R23 (byte coercion). HONEST SEVERITY: route.open response is DAEMON-sourced (trusted, u16/u32 ints on the wire past HMAC handshake) → fractional/boolean not reachable against the real daemon; robustness + wire-parity with Rust u16/u32 + TS Number.isInteger, NOT a live misroute/exploit. Fix: exactWireUInt helper rejecting CFBoolean-backed NSNumber + non-finite + non-integral (d==d.rounded(.towardZero)) + range, before conversion; valid ints byte-identical. VERIFIED at source by Alfonso. Localized Swift-only. Mason's worktree lacked XCTest → swift test to be run by Alfonso at merge. THIS IS THE TAIL SIGNAL (2nd consecutive LOW-severity/hygiene fix after R23) → after shipping, declare loop productive-phase complete.

========================================================================
THE LOOP — CLOSING SUMMARY (2026-07-15, productive phase complete after R24)
========================================================================

RAN: 24 rounds (perf lane R1-R12 then terminated on exhaustion R12; bug lane R1-R24).
FINDINGS: 35 total, 35 verified-real at source by Alfonso, ZERO false positives across 24 rounds.
  - 27 SHIPPED + gated to master (full local gate + 3-OS CI; prod daemon UNTOUCHED, stays v0.3.0).
  - 8 ESCALATED (design/contract forks — the morning-walk agenda, listed below).
  - 1 REVERTED-to-baseline (R3 reverse-request cancel — see below).
  - 1 honest NONE-FOUND (R12 perf, the exhaustion signal).

WHY STOPPED: severity curve declined to hygiene tier. R15-R22 were genuine crash/leak/security
fixes on live paths; R23-R24 were both LOW-severity wire-decode-strictness (fail-closed or
trusted-daemon-source downstream — not live-exploitable). 2 consecutive hygiene-tier rounds +
12-29min hunts = diminishing returns vs 27 real fixes banked. Bug lane not fully dry, but the
remaining vein is hygiene + escalation, not shippable crash/leak/security defects.

TOP PATTERNS (for future LOOP runs — these were the engines):
  1. SIBLING-PATH GAP (8 fixes: R2,R13,R17,R18,R19,R20,R21,R22) — a guard/check/cleanup present
     in one path but MISSING in a parallel one. Highest-yield instruction by far. Cross-client
     (TS/Rust/Swift do the same op) and cross-arm (data vs health, one frame-type vs another).
  2. DISCARDED ERROR/STATUS (R21 SecRandomCopyBytes, R22 void-rejecting-promise) — ignored
     Result/OSStatus/bool/reject on a correctness path where a sibling handles it right.
  3. WIRE-DECODE STRICTNESS (R5,R23,R24) — trap/truncate/coerce on untrusted-or-spec input;
     one language stricter than another (Rust serde is the strict reference).
  VERIFICATION-GATE VALUE PROVEN: caught under-scoped fixes TWICE (R20 3-arm, R22 2-site — mason
  reported 1, real blast radius was more), corrected an incomplete fix spec (R3 case-a), and
  the residual-doubt Oracle rule caught a merged zero-terminal hang (R3 backstop) that both my
  trace AND the mason's test missed. NEVER rubber-stamped.

SWIFT CLIENT = the session's dominant hotspot (note #703): 8 of the fixes are in subc-client-swift
  (SIGPIPE, cursor trap, fd-leak, connection-file traps, readExact, encodeFrame cap, runSessionTurn
  route leak, auth nonce, route.open strictness). It was written as a "spike" (its own header says so)
  and never hardened, yet it's the CK app's transport. RECOMMENDATION: one dedicated Swift-client
  production-hardening pass (NWConnection transport + systematic wire-input audit) rather than the
  loop's piecemeal discovery. Note #703 has the full case.

============ THE 8 ESCALATIONS (morning design-walk agenda) ============
Two THEMES dominate:
  A. CORE PER-CONNECTION DISPATCH (likely ONE design session — Oracle-gate before any worker):
     - R1  forwarding.rs:846 lookup_data_route: one process-wide RwLock + Arc-clone PER DATA FRAME,
       both directions; write-cleanup holds it across whole-store scans. (Consciously-accepted
       tradeoff from refactor 2859b8b5.) Alfonso lean: readers get lock-free Arc<Snapshot>,
       writers rebuild-and-swap, control metadata stays behind the lock. Measure w/ the new counters.
     - R5  connection_loop HOL-blocking: read loop routes each frame to completion before reading
       the next; routing a Request awaits route.flow.acquire() → a saturated route blocks the ENTIRE
       connection incl. independent channels AND the CANCEL that would free it (CANCELLATION DEFEATED).
       HIGH severity. Pairs with R1 (same core-dispatch path).
  B. FLOW-CONTROL / BACKPRESSURE CONTRACT:
     - R11 flow-control credit double-release on a DUPLICATE TERMINAL while concurrent (window
       inflates past cap + in_flight() undercounts). Documented thin-core trusted-module tradeoff
       (release() comment). Fix = per-corr outstanding-set (reverses thin-core + hot-path cost).
     - R16 Rust consumer close-under-backpressure: close_handle/close_route try_send GOODBYE on a
       full egress queue, drop it, uninstall handle, report success → daemon leaks both forwarding
       halves until connection dies. 3 fix options each fork the close contract (ALF is prime consumer).
     - R8  auth queue-wait resets the absolute deadline → up to 2× pre-auth occupancy (code Alfonso
       wrote TONIGHT for the herd fix). DoS-tightness vs herd-liveness tension. Low sev (loopback).
  C. CLIENT API PARITY:
     - R14 TS managed call() can't declare consumer_capabilities (Rust client can) → a TS managed
       consumer can't receive reverse-requests. Adds published-API + cache-key semantics. No live break.
  D. REVERSE-REQUEST SETTLEMENT (the one revert):
     - R3  subc-mcp ReverseRelay settlement state machine is under-designed for the
       backpressure×duplicate×teardown cross-product. Shipped fix had a zero-terminal hang (Oracle
       backstop caught it); fix-forward had 2 more defects (gating Oracle caught them); REVERTED to
       the production-dogfooded baseline (ed03d03e). Correct fix = tombstone-held-until-terminal-
       enqueued + deadline-bounded enqueue + connection-terminal on undrainable egress = 1-2 day
       concurrency-critical REDESIGN. Orphaned fix-forward commit 0d795ceb banked if the detach
       approach helps. ALSO a contract inconsistency logged: drop_route (GOODBYE) leaves the host
       dialog orphaned while explicit-Cancel cleans it (docs/subc-reverse-request.md case c vs d).

DEPLOY POSTURE: all 27 fixes are on master, CI-green, but prod daemon runs v0.3.0 (subc-core-v0.3.0)
and was deliberately NOT hot-swapped. A batched release (new subc-core + republished subc-protocol/
transport per memory 7233 + @cortexkit/subc-client bump for the TS client fixes) is a SEPARATE
explicit-go action — several of the fixes (TS unhandled-rejection, Rust cancel-while-queued, Swift
hardening) are worth shipping to consumers, but that's a release decision for the walk, not the loop.
========================================================================

========================================================================
ESCALATION WALK — DECISIONS (2026-07-15, with Ufuk, post-reverification)
========================================================================
REVERIFICATION OF 27 SHIPPED: blind Oracle (gpt-5.6-sol-pro xhigh, bg_015aa00b) + Alfonso
source re-trace over 5f80cb03..9b995560. Result: 25/27 fully clean; 2 regressions found
(both UNRELEASED, master-only):
  - REG1 (critical): R19 cancelled-while-queued emits no terminal → daemon route-credit leak
    (Serial route wedges). TS R17 has the same hole PRE-EXISTING. Fix = emit Error{cancelled}
    terminal on cancel arms, both SDKs. → REPAIRED 74f61592 (fix eeacf1fd, mason bg_0ce97c89):
    both SDKs emit Error{cancelled} on live-route abort arms via the guarded egress (exactly-once
    + dead-route no-op proven fail-first, both languages); TS sendError gained liveness guard.
    REG2 same commit: public SubcSocket copies defensively, internal writes stay zero-copy.
  - REG2 (minor): R2 zero-copy toWriteBuffer aliases the PUBLIC SubcSocket API (internal
    callers safe/fresh). Fix = copy on public entrypoints, keep internal zero-copy. Same mason.

THEME A (core dispatch, R1+R5+R11) — Ufuk: "design it now, Athena pass." DONE:
  docs/subc-dispatch-redesign.md (commit f3185c89). Non-blocking read loop + per-route dispatch
  queues + CANCEL-inspects-queue w/ daemon-synth Error{cancelled} + channel-0 FIFO offload
  (found a 3rd blocker: route.open 12s bind-await also blocks the whole connection today) +
  fail-loud route_backpressure + R11 exactly-once corr-set rider + ArcSwap snapshot forwarding.
  Zero wire changes, 2-merge rollout. COUNCIL VERDICT (bg_7f9827dc, 8 models): UNANIMOUS NO-GO —
  architecture SOUND (per-route drain tasks, snapshot table, synth cancelled terminal, R11 rider
  all endorsed), but load-bearing MECHANISM is unspecified/wrong. 10 blockers (B1-B10) added to
  docs/subc-dispatch-redesign.md as a v2 punch list (commit be293a4c). Highest-value catches
  (Alfonso source-verified): B1 — "zero SDK changes" is FALSE, route_backpressure classifies as
  outcome_unknown→RECONNECT in TS / CallError::Module in Rust, needs a prerequisite SDK merge-0;
  B2 — CANCEL vs queued→delivered limbo re-introduces the R5 defect transposed + double-terminal;
  B7 — channel-0 FIFO breaks the shipped bind-ACK-before-reverse-request barrier (test
  router.rs:1078-1102), Q3 lean WRONG; B8 — R11 framing imprecise: shipped release() ALREADY has
  a CAS over-release guard (forwarding.rs:1702-1731), R11 is specifically the CONCURRENT-duplicate
  case. NEXT: write mechanism-complete v2 (state machine + specified queue primitive + normative
  drain error arms + RAII credit + inline bind barrier + merge-0 SDK retryable class) → re-gate
  → then decompose. This is now a multi-day design workstream, NOT a tonight build. R11 rider
  survives but must be spec'd as an intentional I3 delta, not "untouched".
  athena() tool BROKEN (config.members regression) — used athena-junior; flagged to ALF.

THEME B — Ufuk agreed to all leans (2026-07-15):
  - R8 (auth 2x pre-auth deadline) = OPTION B: document the two explicit budgets (queue-wait
    bounded by auth deadline, handshake gets its own fresh deadline) as the intentional
    herd-safe contract. Comment + one named constant, NO behavior change. Fix any time (cheap).
  - R16 (Rust close-under-backpressure GOODBYE drop) = PARK until dispatch redesign lands,
    then OPTION C (bounded orphan-GOODBYE retry on next writer-capacity) w/ ALF sign-off. The
    redesign makes the daemon tolerant of late GOODBYEs → shrinks the client fix.
  - R11 = rides Theme A (3.7 rider).

THEME C — R14 TS consumerCapabilities parity: next-release fill-to-match-Rust (mirror
  consumer.rs canonicalization into cache key). Small, no live break. → PENDING Ufuk timing.
THEME D — R3 reverse-request settlement tombstone REDESIGN + the drop_route-vs-Cancel host-
  dialog contract inconsistency (docs/subc-reverse-request.md case c vs d). SEQUENCE AFTER
  the dispatch redesign ships (adjacent machinery; Theme A changes the backpressure landscape
  under it). → PENDING, own design session later.
========================================================================
