You are fixing TWO regressions introduced during last night's LOOP in the CortexKit `subconscious` repo, confirmed by a blind Oracle audit + Alfonso source-verification. Work off master (currently 9b995560). REPORT + FIX + TEST + COMMIT (you have local commit authority in your worktree; do NOT push). Do NOT spawn or use any subagents.

BACKGROUND — the daemon flow-control credit contract (verified at source, DO NOT change the daemon):
- crates/subc-core/src/router.rs handle_bound: when the daemon forwards a REQUEST frame to a module it ACQUIRES one per-route flow-control credit (`route.flow.acquire()`), and it RELEASES that credit ONLY when a TERMINAL frame flows back for that route. Terminal = Response | Error | StreamEnd (router.rs is_terminal_frame ~line 501). CANCEL is NOT terminal and explicitly bypasses credit.
- CONSEQUENCE: once a module has RECEIVED a Request frame, SOME terminal frame (Response/Error/StreamEnd) MUST eventually flow back for that (channel,epoch,corr), or the daemon's credit for that route leaks until route teardown. On a Serial route (window=1) a single leaked credit PERMANENTLY WEDGES the route.

=========================================================================
REGRESSION 1 (CRITICAL) — cancelled-while-queued request emits NO terminal → daemon credit leak.
=========================================================================
Two SDKs have handler-dispatch paths that, when a request is CANCELLED (via a CANCEL frame firing the abort token/CancellationToken) while QUEUED for handler capacity — or cancelled mid-handler — now RETURN WITHOUT emitting any terminal frame. That leaks the daemon route credit.

SITE A — Rust: crates/subc-client-rs/src/lib.rs spawn_data_request (~line 884-902).
  After `permits.acquire_owned().await`, the guard `if ctx.cancelled.is_cancelled() { ...remove in_flight...; return; }` (~891-896) returns with NO terminal. The normal path calls `send_handler_outcome` (~898) which DOES emit a terminal.
  FIX: on the cancellation arm (the `is_cancelled()` branch AFTER a permit was acquired, i.e. the module is alive and this specific request was cancelled), emit an ERROR terminal frame `Error { code: "cancelled", message: "request cancelled" }` back on (channel,epoch,corr) BEFORE removing in_flight and returning — reuse the SAME egress/send path the normal terminal uses (send_handler_outcome's HandlerOutcome::Error shape, or ctx.send_frame(FrameType::Error, ...) with a serialized ErrorBody). Then remove in_flight, return.
  DO NOT change the PERMIT-CLOSED arm (`let Ok(_permit) = ... else { ...; return }` ~885-889): that fires only when the dispatcher semaphore is CLOSED = module/connection shutting down = connection teardown frees all credit anyway. Leave it bare (add a one-line comment why). If emitting there is trivially safe you may, but it is NOT required and MUST NOT hang.

SITE B — TypeScript: clients/subc-client/src/provider.ts handleDataRequest (~line 888-939).
  THREE abort arms currently suppress the terminal by bare `return`:
    (1) ~891 pre-handler: `if (controller.signal.aborted) return;` (added last night, R17)
    (2) ~893 post-handler: `if (controller.signal.aborted) return;` (PRE-EXISTING)
    (3) ~927 catch: `if (error instanceof StaleRouteHandleError || controller.signal.aborted) return;` (PRE-EXISTING)
  FIX: for arms (1) and (2) — a request that was cancelled (aborted) but whose ROUTE IS STILL LIVE must emit an Error terminal so the daemon frees credit. Emit `sendError(frame, "cancelled", "request cancelled", dataFlags, sock, generation)` (the existing guarded sendError helper — it already respects generation/stale-route so it NO-OPS when the route is genuinely gone) then return. For arm (3): if the abort cause is a genuine cancel with a live route, likewise emit the cancelled Error; BUT if it's a StaleRouteHandleError (route already torn down) keep the bare return (no live route to credit). Distinguish: StaleRouteHandleError branch → bare return; plain `controller.signal.aborted` with a non-stale route → emit cancelled Error.

CRITICAL SAFETY PROPERTY (both SDKs) — why emitting through the GUARDED send path is mandatory:
  An abort has TWO possible causes: (a) an explicit CANCEL frame while the route is STILL LIVE (client still there) — here the Error terminal MUST be delivered to free credit; (b) route TEARDOWN / GOODBYE / generation bump (route GONE) — here writing must be a NO-OP (writing to a dead route/socket is the exact hazard the abort checks were originally added to prevent). The fix is safe ONLY because the existing send path is generation/live-guarded (TS: sendError→sendOn checks generation + assertLiveHandle; Rust: ctx.send_frame goes through the egress channel which drops if the route/connection is gone). VERIFY this guarding holds for the path you use; if the Error emit is NOT generation-guarded, route it through the guarded path. NEVER emit an unguarded write on the abort arm.
  EXACTLY-ONCE: never emit two terminals. The success path already emits one; only the cancel/abort arms (which currently emit ZERO) get the new emit. A request that reached send_handler_outcome / the success sendResponse must NOT also emit cancelled.
  The late Error is HARMLESS to the client: the canceller settled locally when it sent CANCEL, so a late Error on that corr matches no pending entry and drops via the channel-gone backstop (this mirrors the R18 late-frame reasoning).

NON-VACUOUS TESTS (fail-first REQUIRED for regression 1, both SDKs):
- Rust (crates/subc-client-rs): saturate the handler permits (capacity-1 semaphore, first request's handler blocks holding the permit), enqueue a second data request (queues for the permit), fire a CANCEL for the second (its CancellationToken cancels while queued), release the first → the second's guard runs → ASSERT an Error frame with code "cancelled" is emitted on the second's (channel,epoch,corr) (capture via the egress mpsc / a scripted egress). Fail-first: against current code the cancelled-while-queued arm emits NOTHING → the assertion that an Error terminal was emitted FAILS. Also keep a positive: a normal (non-cancelled) request still emits its Response exactly once (no spurious cancelled).
- TS (clients/subc-client): mirror in tests/provider.test.ts — capacity-1 requestGate, first handler blocks, second data request queues, abort the second while queued (simulate the CANCEL → controller.abort()), release → ASSERT an Error frame code "cancelled" is written on the second's route (via the fake socket write capture). Fail-first: current bare `return` writes nothing. ALSO add a test for the post-handler abort arm (2): handler completes but controller aborted during handler → assert cancelled Error emitted (this is the pre-existing arm you're also fixing). AND a stale-route case: abort with a stale/torn route (generation bumped) → assert NO write (guarded no-op) — proves the safety property.

=========================================================================
REGRESSION 2 (MINOR) — public SubcSocket API buffer aliasing.
=========================================================================
clients/subc-client/src/socket.ts toWriteBuffer (~47-52) now returns a zero-copy Buffer VIEW of the caller's Uint8Array (`Buffer.from(bytes.buffer, bytes.byteOffset, bytes.byteLength)`). Internal frame/auth callers pass fresh single-use encodeFrame storage (safe, that's the R2 perf win — KEEP it for them). But `SubcSocket` is a PUBLIC export (index.ts:61) with public `write`/`writeTracked`; an external caller who mutates/reuses their buffer under socket backpressure before the write completes would corrupt queued wire bytes.
  FIX (choose the cleaner, state which): EITHER (a) copy defensively on the PUBLIC entrypoints (`write`/`writeTracked` public methods) while keeping the internal frame-write path zero-copy — e.g. the public methods copy, an internal writeTracked variant used by client.ts frame writes stays zero-copy; OR (b) if cleanly separating public vs internal is awkward, document a hard no-mutate-until-completed contract in the public method JSDoc AND keep zero-copy. PREFER (a) if it's a small refactor (the perf win matters only for the high-volume internal frame path, not the rarely-used public API). Keep the internal client.ts frame writes (client.ts:616,806) zero-copy either way.
  Test: a public-API caller that mutates its source buffer after write() returns-but-before-completed does NOT see corrupted queued bytes (if you pick (a)); or a doc-only change (if (b)) needs no test but state the rationale.

CONSTRAINTS:
- subc-protocol/subc-transport wire crates: DO NOT touch. subc-core daemon: DO NOT touch (the credit contract is correct; the SDKs must honor it).
- Do NOT spawn or use any subagents.
- GREEN BAR before you hand back:
  Rust: `cargo test -p subc-client-rs` green; `cargo clippy -p subc-client-rs --all-targets` clean AND `cargo clippy -p subc-client-rs --target x86_64-pc-windows-gnu --all-targets` clean; `cargo fmt -p subc-client-rs -- --check`.
  TS: from clients/subc-client — `bun install --frozen-lockfile` if deps missing, then `npm run typecheck` clean AND `npm test` green. Do NOT skip typecheck.
- Report: per-regression, the fail-first evidence, the fix (files+lines), the passing runs, the commit SHA(s), and explicitly CONFIRM the exactly-once + guarded-no-op-on-dead-route properties with the test that proves each.