---
task_id: bg_2c3f3554
agent: council-member
session_id: ses_09b2a7017ffefpPex0bnwH6Y7J
parent_session_id: ses_09b2b56e1ffeYwM1Vc5SuTRFuZ
status: completed
completed_at: 2026-07-15T08:14:04Z
---

[user] 2026-07-15T08:12:25Z
## Solo Analysis Mode
You MUST do ALL exploration yourself using your available read/search tools.
- Do NOT use task or any delegation tool under any circumstances
- Do NOT delegate to explore, librarian, or any other subagent
- Do NOT spawn background tasks
- Search the codebase directly — you have full read-only access to every file
- This mode produces the most thorough analysis because you see every result firsthand


## Analysis Intent: AUDIT

You are conducting an **audit** — your goal is to find discrete issues, risks, or violations.

**Focus:**
- Search for problems, anti-patterns, security risks, correctness issues, or violations of stated requirements
- Each finding must be a distinct, actionable item with concrete evidence
- Severity determines priority: critical (blocks/breaks), high (significant risk), medium (should fix), low (nice to fix)
- For each finding, provide the specific location (reference, section, or component where it occurs)
- State your confidence: high (clear evidence), medium (likely but needs verification), low (suspicion, investigate further)
- **This is a broad sweep, not a targeted trace.**

**Analytical standards:** Support claims with concrete evidence. State confidence (high/medium/low) for key assertions. Note caveats and limitations.

**Structure your response as:**
```
<COUNCIL_MEMBER_RESPONSE>
## Finding 1: [Title]
- **Severity**: critical/high/medium/low
- **Location**: [specific reference — e.g. component, section, endpoint, rule]
- **Confidence**: high/medium/low
- **Issue**: [what is wrong and why it matters]
- **Evidence**: [concrete reference, snippet, or observation that proves the issue]
- **Suggested Fix**: [actionable recommendation]

## Finding 2: [Title]
...

## Summary
[Total findings by severity. Overall risk assessment with confidence levels.]
</COUNCIL_MEMBER_RESPONSE>
```

## Analysis Question

ADVERSARIAL RE-GATE — subc-core dispatch redesign v2

You are one of several independent expert reviewers performing a concurrency-critical adversarial re-gate. Repo: subconscious at ~/Work/Projects/CortexKit/subconscious. Design under review: docs/subc-dispatch-redesign-v2.md (committed 72891b31). Shipped daemon source: crates/subc-core/ at master. This is v2 of a dispatch redesign; v1 (docs/subc-dispatch-redesign.md) received a UNANIMOUS 8/8 NO-GO with 10 blockers B1-B10. v2 claims to close all of them.

THE V1 ARCHIVE (READ IT FIRST for exact blocker statements + source citations):
.cortexkit/alfonso/athena/council-subc-dispatch-redesign-e26112ccb9170497/synthesis.md
The B1-B10 blockers map to findings #1-#10 in that synthesis (in order). Each v2 section tags which blocker(s) it claims to resolve.

YOUR TWO JOBS:
(A) VERIFY each v1 blocker B1-B10 is ACTUALLY closed by the v2 mechanism — not merely claimed. Read the v2 mechanism, then read the shipped source it cites, and confirm the mechanism genuinely closes the defect. A plausible-sounding paragraph is not a closure; trace the concurrency.
(B) HUNT for NEW defects the v2 mechanisms introduce.

GROUND TRUTH (already verified at source — you may re-verify):
- forwarding.rs:1692-1700 ChannelFlow::acquire does permit.forget() then in_flight.fetch_add — credit is NOT RAII-held by the task.
- forwarding.rs:1702-1731 release() has a CAS in_flight!=0 guard that ignores over-release (best-effort, not a security boundary per its own comment).
- router.rs:465-496 shipped handle_bound: acquire().await → mutate header → module_sink.send().await → release() on send error.
- router.rs:281-309 module->client terminal path: try_send to client, then flow.release() if terminal.

V2 KEY MECHANISMS TO SCRUTINIZE (map to doc sections):
1. §1 RouteDispatcher: route-local Mutex<RouteInbox> (VecDeque<corr> + HashMap<corr,Slot> + admission enum + outstanding count) as SINGLE serialization point; every op holds the lock O(1) and NEVER across an await; drain task's blocking awaits (flow.acquire, module_sink.send) happen OUTSIDE the lock. VERIFY: is every claimed-O(1)-under-lock op actually O(1) and await-free? Can the read loop and drain task deadlock/livelock on this lock? Is Notify usage correct (lost-wakeup between pop-returns-None and wait)?
2. §2 Per-corr state machine Queued→Claimed→Delivered with CANCEL decided under the same lock. VERIFY B2 limbo is TRULY gone: enumerate CANCEL arriving in EACH state incl. exactly at the Claimed/acquire boundary and the Delivered/send boundary; can a terminal still double-fire (synthetic cancelled + module terminal) or vanish (zero terminal)? The rollback path (Claimed{cancelled:true} after acquire → release + synthesize cancelled): does it race the module→client terminal path?
3. §2 outstanding/Delivered recorded UNDER LOCK BEFORE module_sink.send (B5). VERIFY a fast module terminal cannot arrive before corr is Delivered: trace exact happens-before. The frame isn't sent until after the lock releases with state=Delivered — but is the terminal-release path's slots.remove correctly ordered against a Delivered set before send?
4. §4 Credit RAII (AcquiredCredit guard) + drain error arms: ChannelFlowClosed disambiguation via per-route teardown:TeardownKind field. VERIFY the teardown field is set/read under the right lock ordering and the None→backend_error arm actually preserves the shipped test blocked_flow_control_acquire_wakes_when_module_tears_down (forwarding.rs ~3811). Can the teardown kind be stale/racing when the drain reads it?
5. §3 R11 exactly-once release: terminal path does slots.remove under lock, releases only if was-Delivered. VERIFY against shipped release() which ALREADY has a CAS in_flight!=0 guard (forwarding.rs:1702-1731) — does v2's gate + existing CAS double-guard CLEANLY or CONFLICT? Is the concurrent-duplicate case actually closed, or does the double-guard mask/hide a real leak?
6. §5 Teardown 3-phase Open/Closing/Closed: admission=Closing before snapshot removal; cancel_token select; bounded-join then abort. VERIFY: the try_push-after-flush hole (B9) — is admission=Closing genuinely happens-before any stale-snapshot reader's push? Can a drain task blocked on module_sink.send with a FrameSink clone still hang connection close (server.rs:241-267)? Is the JoinHandle ownership (teardown path, not binding) actually acyclic?
7. §7 Snapshot publish-under-lock + closed:AtomicBool recheck (B10): VERIFY the closed flag set-under-write-lock-before-publish + reader recheck restores today's unknown_channel observable (not backend_error) on stale-Bound. Is there any ordering where closed=false is read but the route is already released?
8. §6 Client-side-only control offload (B7): module connections NOT split, bind-ack stays inline. VERIFY the shipped bind barrier test accepted_route_publishes_route_open_before_immediate_reverse_request (router.rs:1078-1102) stays green AND that client-side route.open FIFO doesn't itself reorder anything observable.
9. §8 merge-0 SDK prerequisite (B1): the retryable data-plane class. VERIFY the classification claim against SDK source (client.ts classifyFailure ~781-792, consumer.rs ~570-579) — is a distinct retryable class actually needed and does the proposed in-place-retry (not reconnect) fit the existing managed-call retry loop? NOTE: broca/aft/alfonso-core are NOT in this checkout — their contract impact is UNVERIFIABLE; flag this.
10. §9/§11 The 3-merge rollout ordering + invariant deltas I3/I4/I7 — are they now HONEST against source, and is merge-1 truly standalone-landable under the publish-under-lock constraint?

ALSO CHECK §12 Q1'-Q5' open-question leans for wrong leans. AND HUNT ANYTHING NEW:
- lock-ordering inversions between the RouteInbox mutex and the global forwarding write lock (specify the hierarchy; is it respected everywhere?);
- the AcquiredCredit RAII guard interacting with the outstanding count → double-counting or double-release;
- Notify lost-wakeups (the classic pop-returns-None → miss notify_one → sleep-forever gap);
- the corr-uniqueness assumption (Q4') as a credit-leak vector if not enforced at enqueue.

DELIVERABLE (your COUNCIL_MEMBER_RESPONSE):
- For EACH v1 blocker B1-B10: state CLOSED / NOT-CLOSED / NEWLY-BROKEN, with a one-to-three sentence justification and source citation (file:line). "CLOSED" requires you traced the mechanism, not just read the claim.
- A section of NEW defects introduced by v2 mechanisms, each with severity (BLOCKER/MAJOR/MINOR), a concrete interleaving or source contradiction, and file:line citations.
- Rulings on Q1'-Q5' leans (RIGHT / WRONG / RIGHT-BUT-UNSAFE).
- Your bottom-line verdict: GO / GO-WITH-CHANGES (enumerate each change, specific + file:line-cited) / NO-GO (enumerate blockers).

Be adversarial and precise. Cite shipped source for every claim. A single un-refuted concurrency or contract defect is a NO-GO. Do not flatten disagreement with the v1 findings — if v2 genuinely closed a blocker, say CLOSED and prove it; if it only papers over one, say NOT-CLOSED and show the hole.

[assistant] 2026-07-15T08:12:26Z
The user wants an adversarial audit of the subc-core dispatch redesign v2. I need to:

1. Read the v2 design doc
2. Read the v1 synthesis with B1-B10 blockers
3. Read shipped source in crates/subc-core/
4. Verify each blocker closure and hunt for new defects

Let me start by reading multiple files in parallel.
Conducting an adversarial re-gate: reading the v2 design, v1 synthesis, and key shipped sources.

[assistant] 2026-07-15T08:12:29Z
I have the v2 design doc and v1 synthesis. Now I need to verify each blocker against shipped source in more depth. Let me read:
- forwarding.rs release, lookup, write lock, blocked_flow_control test
- server.rs read loop, connection close
- control.rs route bind
- SDK client.ts classifyFailure, consumer.rs
- router test accepted_route_publishes
- Any ArcSwap usage (might not exist yet in master)

I'll search for relevant patterns and read more sections.

[assistant] 2026-07-15T08:12:36Z
I need more detailed reads of client.ts classifyFailure, consumer.rs call retry logic, router test 1078-1102, blocked_flow_control test, CANCEL handling in v2 vs shipped, and whether RouteBinding has closed flag yet (it's design only).

Also search for AcquiredCredit, RouteDispatcher - likely not in code yet.

For B1 I need to verify SDK classification at the cited lines.

For v2 mechanisms I need adversarial tracing - some may NOT-CLOSED.

Key concerns to trace:
1. Drain loop: pop returns None, wait notify - lost wakeup if notify happens between pop and wait
2. Claimed boundary: read sets Claimed before acquire outside lock - wait, v2 says:
   - corr = lock pop_front
   - lock set Claimed
   - acquire outside
   So between pop and Claimed, could CANCEL see Queued still? Actually after pop, corr is removed from queue but still in slots as Queued until second lock sets Claimed. CANCEL on Queued removes from queue+slots - if already popped from queue, slot might still be Queued!

This is a CRITICAL bug in v2 pseudocode:
```
corr = { lock inbox; pop_front queue or None }
{ lock inbox; slots[corr].state = Claimed{cancelled:false} }
```
Between pop and Claimed update, slot is still Queued but NOT in queue. CANCEL match Some(Queued) tries "remove from queue+slots" - corr not in queue anymore! So CANCEL might hit wrong branch.

Let me read v2 CANCEL again for Queued case: "remove from queue+slots"

If frame was popped but not yet Claimed, state is still Queued but not in queue. CANCEL would find Some(Queued) and try remove from queue - might fail to find in queue, but removes slot? The pseudocode says remove from queue+slots.

If only in slots as Queued without being in queue - that's an inconsistent state during drain's gap between pop and Claimed.

Actually after pop_front, the corr is removed from queue but slot remains Queued until Claimed is set. CANCEL with Some(Queued) would remove from queue (no-op?) and slots - synthesize cancelled. But drain might have already popped it and be about to acquire - race:
- Drain pops corr (not in queue, slot Queued)
- CANCEL: Some(Queued) -> remove slot, synthesize cancelled
- Drain: set Claimed - but slot gone? or drain uses corr from local variable

Drain continues with corr variable even if slot removed - then set Claimed on missing slot?

This is a NEW defect or B2 NOT-CLOSED.

Let me read the doc more carefully for pop ordering.

```
corr = { lock inbox; pop_front queue or None }
{ lock inbox; slots[corr].state = Claimed{cancelled:false} }
```

The gap: after first lock, slot is Queued, not in queue. CANCEL Queued branch removes from queue+slots - good, cancels. Drain second lock tries slots[corr].state = Claimed - if slot removed, panic or skip?

If CANCEL removed slot, drain's Claimed update fails - doc doesn't say. Drain might still flow.acquire() - credit leak!

B2 claimed closed by single lock - but pop and Claimed are TWO lock sections with Queued-not-in-queue limbo.

That's adversarial finding.

Also: set Claimed BEFORE acquire is correct for CANCEL Claimed branch, but the pop should atomically transition Queued -> Claimed in ONE lock hold, not pop then later Claimed.

v2 says:
```
corr = { lock inbox; pop_front queue or None }
{ lock inbox; slots[corr].state = Claimed{cancelled:false} }
```

Should be one critical section: pop and set Claimed together.

For Delivered before send: lock sets Delivered and outstanding+=1 before unlock then send. Module terminal path needs slots.remove. Fast terminal: can module respond before send completes? Module can't see frame until send delivers - so terminal can't arrive before frame at module. But terminal can arrive on daemon module->client path before drain finishes send.await on client side - the Delivered is set before send, so terminal path should find Delivered in slots. Good for B5.

Concurrent duplicate terminals: v2 slots.remove gate + existing CAS - should work cleanly if first removes and releases, second finds no slot release=false. CAS on second release when in_flight already decremented - if release=false, no double decrement. Good B8.

Rollback Claimed cancelled:true after acquire: release credit, synthesize cancelled. Module path if Delivered already - CANCEL on Delivered forwards to module. Race: acquire done, before post-acquire lock, CANCEL sets Claimed cancelled? CANCEL on Claimed sets cancelled=true. Post-acquire lock sees Claimed{cancelled:true}, rollback. Good.

CANCEL on Delivered forwards to module - could module still process request? If Delivered but send not complete - state is Delivered before send, so CANCEL sees Delivered, forwards cancel to module. Request might not be at module yet - module cancel no-op, request arrives later - cancel loss?

Sequence:
- Drain: set Delivered, outstanding+=1, unlock
- Drain: module_sink.send.await (slow)
- CANCEL: Delivered -> forward to module (module doesn't have request yet)
- Send completes, module processes uncancelled

That's B2 NOT-CLOSED - limbo at Delivered-before-send-complete but state is Delivered so CANCEL doesn't synthesize, forwards to module too early.

v2 explicitly sets Delivered BEFORE send specifically for B5 fast terminal - but that opens cancel-loss window before module receives request.

This is a fundamental tension - v2 chooses B5 over cancel-before-module-sees-request.

For saturated serial route, CANCEL forwarded early while send pending - module gets cancel before request = no-op, then request arrives. **NOT-CLOSED for B2** from one perspective - the v1 limbo is different but cancel can still lose.

Actually read loop CANCEL on Delivered: "forward CANCEL to module_sink (module has it or will)"

The design accepts forwarding before module has it relying on module queue ordering? If cancel arrives at module before request, module no-ops. **Known issue** - v2 assumes module buffers cancel after request? Wire order: request sent first from daemon before cancel forwarded... but cancel can be forwarded while send.await pending - so cancel frame might reach module BEFORE request frame if send is slow and cancel forwarded on separate path?

Actually both go through module_sink - cancel is from read loop direct forward, request from drain send. **Two producers to module_sink** - ordering not guaranteed between read loop cancel forward and drain send!

CRITICAL NEW: read loop forwards CANCEL to module_sink without holding ordering relative to drain's pending send for same corr.

Interleaving:
- Drain: Delivered set, awaiting send(request)
- Read loop: CANCEL for corr -> Delivered -> forward cancel to module_sink immediately
- Cancel might be delivered to module before request
- Drain: send(request) completes after

B2 NOT-CLOSED, NEW BLOCKER.

Need verify shipped - today cancel and request same serial path so ordered.

Read client.ts for classifyFailure, router test, blocked test.
Tracing CANCEL/ordering gaps and verifying SDK + test sources.

[assistant] 2026-07-15T08:12:49Z
Let me read more: the blocked_flow_control test, router test, handle_cancel, how Error terminals are classified in TS when received as frames, channel 0 routing, and grep for ArcSwap in v2 implementation (there may be none - it's design only).

Also need to analyze the drain loop's race:
- Line 87: lock, set Claimed
- unlock
- acquire.await OUTSIDE lock
- Between 87-92 another thread could CANCEL and set cancelled=true on Claimed
- But wait: CANCEL on Claimed sets cancelled=true - good
- CANCEL on Queued removes - but if we already popped from queue and set Claimed, CANCEL sees Claimed - good

Race: pop corr from queue, NOT yet set Claimed:
```
drain: lock; pop_front -> corr
drain: unlock (before set Claimed?)
```
Looking at pseudocode:
```
corr = { lock inbox; pop_front queue or None }
{ lock inbox; slots[corr].state = Claimed{cancelled:false} }
```
Two separate lock regions between pop and Claimed. In between, could read loop insert? No, same corr already removed from queue at pop? Actually pop removes from queue but slot still Queued until second lock.

Between pop and Claimed:
- Read loop CANCEL: slots.get_mut - state is still Queued! So CANCEL would remove from queue+slots - but corr already popped from queue! **BUG**: CANCEL on Queued does "remove from queue+slots" - if corr already popped from queue but still Queued in slots, CANCEL removes slot and synthesizes cancelled, while drain will still process corr from pop_front result.

That's a NEW defect - the two-step pop then Claimed leaves Queued state with corr not in queue.

Interleaving:
1. Drain: pop_front removes corr from queue, returns corr, unlock
2. Read: CANCEL for corr, sees Queued in slots, tries remove from queue (already gone) + remove slots, synthesize cancelled
3. Drain: sets Claimed on... slot gone? Or if CANCEL removed slot, drain's second lock would panic on slots[corr]

Or if CANCEL runs after Claimed set:
- Between pop and Claimed: CANCEL sees Queued, removes from slots - drain then fails on slots[corr]

This is a NOT-CLOSED or NEW defect for B2.

Actually re-read Queued cancel:
```
Some(Queued) => remove from queue+slots; ...
```
If already popped from queue, remove from queue is no-op?, still removes slot - good for cancel side
Drain still has corr from local variable - proceeds to acquire, then lock - slot missing?

Need to check if v2 says pop also transitions state. It doesn't - pop only removes from deque, slot stays Queued until Claimed.

**Definite hole**: pop_front without atomically transitioning Queued→Claimed allows CANCEL to see Queued and synthesize cancelled while drain holds popped corr and will acquire+send.

Interleaving:
1. Drain pop corr (out of queue, slot Queued)
2. CANCEL: Queued branch → remove slot, synthetic cancelled
3. Drain: Claimed on missing slot OR if not removed yet, set Claimed
4. If slot removed at 2, drain panics or errors
5. If CANCEL after Claimed at 3: Claimed branch sets cancelled
6. Worst: CANCEL at Queued between pop and Claimed → cancelled terminal + drain continues with corr not in slots? remove slot at 2, drain at step `{ lock; slots[corr].state = Claimed}` - **uses slots[corr] on missing entry - UB/panic**

Or CANCEL removes slot, drain acquires credit, lock take Slot fails?

Also double terminal: CANCEL Queued after pop - synthetic cancelled, drain still sends to module → module terminal too.

This is B2 NOT-CLOSED unless they merge pop+Claimed in one critical section. v2 pseudocode explicitly has them separate (lines 86-87).

Let me also verify Delivered boundary:
- After acquire, before Delivered set: state is Claimed not Delivered
- CANCEL on Claimed sets cancelled flag - good
- CANCEL on Delivered forwards to module - good
- At acquire boundary: read sets cancelled on Claimed, drain checks after acquire - good IF Claimed was set before acquire. But Claimed is set BEFORE acquire (line 87 before 88). So during acquire.await, state is Claimed - CANCEL works.

The hole is ONLY between pop and Claimed (still Queued).

Also: duplicate corr insert - Q4' lean enforce but not in section 2 pseudocode - only open question.

Read blocked test and router test.

[assistant] 2026-07-15T08:13:01Z
Need more on: TS how error terminal becomes SubcCallError, module cancel no-op, v2 gaps around double terminal on Delivered+CANCEL forward, duplicate corr insert, synthetic egress blocking, control offload details, write_inner lock and snapshot publish, release path on module side

Also check: Delivered + CANCEL forwards cancel to module - if module already processing could get double terminal - v2 says module owns terminal - good. But can read loop synthesize cancelled AND module send terminal? For Delivered, CANCEL forwards - module may emit cancelled or response - only one from module. Good.

Claimed race: drain pops, sets Claimed, then await acquire. CANCEL sets cancelled=true on Claimed. After acquire, rollback releases + synthesize cancelled. Good.

What if CANCEL on Queued removes and synth cancelled, but drain already popped from queue? Pop happens before Claimed set in v2 pseudocode:
```
corr = { lock inbox; pop_front queue or None }
{ lock inbox; slots[corr].state = Claimed{cancelled:false} }
```
If CANCEL removes from queue+slots between pop and Claimed set - drain could pop corr that was removed? Pop only from queue - if CANCEL removed corr from queue AND slots, pop might get different corr. But if drain already popped corr from queue before CANCEL removes - CANCEL might still find Slot Queued if not yet Claimed? Order: drain pops corr from queue (corr not in queue), slot still Queued until second lock sets Claimed. CANCEL could find Some(Queued) and remove from slots+queue - but corr already popped from queue! CANCEL's remove from queue might be wrong. Need to read v2 again - "remove from queue+slots" for Queued. If drain already pop_front'd, slot still Queued - CANCEL finds Queued, removes from slots (and queue.remove corr - noop if not in queue), synthesizes cancelled. Drain still has corr variable and will set Claimed on missing slot? BUG!

This is a NEW defect: between pop_front and Claimed transition, CANCEL can remove slot as Queued while drain holds corr id.

Interleaving:
1. Drain lock, pop_front -> corr=5, unlock
2. CANCEL lock, slots[5] is Queued -> remove queue+slots, synth cancelled, unlock
3. Drain lock, slots[5] missing -> panic or error?

v2 line 86-87 separates pop and Claimed into two lock sections - classic race.

Severity BLOCKER.

Similarly double terminal: 
1. Drain sets Delivered, unlock, await send
2. CANCEL Delivered -> forward cancel to module
3. Module sends response + maybe error on cancel?

For Queued case handled if atomic under one lock for pop+claim.

Notify lost wakeup: if push does notify_one after drain went to wait - standard pattern if wait is notified before wait - need notify before wait check. v2 says "if None -> wait notify" - must use loop { lock; pop; if Some break; drop lock; notified().await } 

Claimed during acquire: CANCEL sets flag - good.

Delivered boundary: recorded before send - terminal path needs dispatcher's slots.remove - module→client path must lock same inbox - v2 says it does. Shipped router.rs:281-309 doesn't have inbox yet - future. Order: Delivered set in drain before send completes; module receives after send completes typically; terminal could arrive on module connection before client's send().await returns on drain? Module processes on another thread - terminal can reach module→client handler while frame still in flight on send - but Delivered already set under lock before send.await - good for B5.

Rollback path vs terminal: If Delivered set then send fails, outstanding decremented - good. If Delivered set, send succeeds, CANCEL forwarded (Delivered state), module terminal arrives - slots.remove Delivered, release - good.

If cancelled rollback synthesizes cancelled while module also got frame? Rollback happens when cancelled flag OR admission Closed BEFORE send - if cancelled at Claimed, no send. Good.

Race: set Delivered, unlock, before send.await, admission=Closing - send still happens? Drain doesn't recheck admission between unlock and send except at Claimed check. If teardown sets Closing after Delivered committed but before send - frame could still send to module during close - v1 issue partial.

Teardown: step 1 admission=Closing - read loop rejects new. Drain in send await - select cancel_token - good if implemented.

stale teardown kind: set when flow.close() step 2 without lock on inbox - race?

module_reloading vs backend_error: shipped uses endpoint_is_draining at acquire error in handle_bound router.rs:465-484 - v2 TeardownKind must mirror.

B1 merge-0: v2 correctly identifies need - CLOSED as design prerequisite NOT as shipped - verdict NOT-CLOSED until merge-0 ships? The question is whether v2 CLOSES the blocker in the design. B1 was false SDK claim - v2 fixes by merge-0 + gating - CLOSED at design level if merge-0 lands first. But broca unverifiable - still CLOSED with caveat.

B7: client-only offload - preserves module inline - CLOSED for design.

B10: publish under lock + closed flag - need verify if closed set before publish in spec - section 7 says set true at release BEFORE snapshot removes - reader recheck unknown_channel - need module path too router.rs 246-310 - for stale Bound on module→client without closed check today clones Arc - v2 adds recheck - CLOSED if implemented as spec.

R11: slots.remove gate + existing CAS - duplicate terminal first release decrements in_flight, second slots.remove false so release=false - good. If duplicate both try slots.remove only one succeeds - second release=false even if in_flight>0 from other corrs - good. CONFLICT? No - complementary.

AcquiredCredit + outstanding: on rollback release() and guard commit - need disarm on commit - if both release double? Spec says commit() on Delivered - guard disarmed - good. On send error path explicit release AND outstanding-=1 - guard should disarm too - spec must say commit/disarm.

Double release on send error: flow.release in error arm + guard drop?

Lock hierarchy: not specified in v2 - NEW defect if dispatcher mutex taken while holding forwarding write lock?

Synthetic errors: read loop "synthesize to client" - blocking or try? Not specified - v1 #13 persists - NEW MAJOR.

Q4 lean enforce duplicate corr - good but not in main mechanism only Q section - if not enforced BLOCKER from v1 #11.

Depth cap queue.len()+outstanding - duplicate corr overwrite if slots.insert without check - leaks credit - NEW ties Q4'.

Control_backpressure on module responses - v2 still says control offload with backpressure - MAJOR from v1 #14 may persist.

Read loop handle_bound path replaced by dispatcher - CANCEL path today?

Check fake-aft cancel no-op unknown corr

[assistant] 2026-07-15T08:13:16Z
For errorFromFrame - it returns SubcError with code from frame, not SubcCallError. When settling on Error frame, it rejects with SubcError. managed call catches SubcCallError only - so SubcError from error frame might be wrapped elsewhere. Let me check how error frames become SubcCallError in managed path.

Actually line 1060: pending.reject(this.errorFromFrame(frame)) - that's SubcError not SubcCallError. So call() would throw SubcError not SubcCallError? Line 423 checks SubcCallError - so SubcError might propagate as non-SubcCallError and get terminalCallError wrapper... need trace managedRequest.

Actually the point for B1 is clear: route_backpressure Error would be SubcError with code route_backpressure, not in retry set, not not_sent - hard failure or needs separate handling.

v2 merge-0 proposes adding classifier - spec is honest that current SDK doesn't support it.

B1 closure in v2 doc: acknowledges merge-0 prerequisite - CLOSED as design doc fixes the false claim and specifies prerequisite. NOT closed for shipping without merge-0.

For adversarial gate: B1 asks verify SDK classification claim. v2 correctly says FALSE and needs merge-0 - CLOSED for design honesty, but deployment gate still requires merge-0 - I'll say CLOSED (mechanism/documentation) with caveat NOT deployable until merge-0 lands.

Actually re-read B1 in v1: blocker is emitting route_backpressure without SDK fix. v2 section 8 fixes that with merge-0 gate + don't emit until deployed. CLOSED.

Now write full response with new defects:

1. **Claimed race - two drains** (MAJOR/BLOCKER): After pop, corr removed from queue but state still Queued until second lock. Second request can be popped, two concurrent acquires on serial route - breaks I1 FIFO credit ordering.

2. **CANCEL Queued vs pop race** (BLOCKER): pop_front doesn't remove from slots until Claimed lock; CANCEL can remove Queued while drain holds popped corr - double terminal or send after cancel.

3. **Delivered + CANCEL forwards to module** (potential double terminal): If client CANCEL after Delivered, module may emit cancelled while request in flight - client gets module terminal + possibly duplicate. Spec says forward CANCEL - module fake-aft emits cancelled if in flight. After deliver, module has in_flight - CANCEL works. Client might not have cancelled locally if they sent CANCEL - they'd get module cancelled terminal. OK if only one.

4. **AcquiredCredit vs rollback double release**: commit() on Delivered; rollback calls release(); drop also releases - need commit() to disarm. Spec says commit() - if implemented correctly CLOSED.

5. **teardown:None without write lock**: If TeardownKind not set atomically with flow.close() under same ordering as inbox - drain reads stale None → backend_error. Module death: shipped uses endpoint_is_draining for module_reloading vs backend. v2 TeardownKind must be set when closing flow in release paths - not fully specified WHERE set in forwarding.rs:1424 route.flow.close() - no teardown kind in shipped code. v2 adds field - must be set before close() visible to drain. If set outside inbox lock but close wakes drain, race on kind. MAJOR if not specified under write lock + route dispatcher field.

6. **Notify lost wakeup**: Standard pattern: after pop returns None, must check queue again before wait OR use notified guard. v2 says "if None -> wait notify" without re-check loop - CLASSIC bug unless while-loop with check after wake.

Drain pseudocode:
```
corr = { lock; pop_front or None }
```
Then if corr is None, wait - but between unlock after empty pop and wait, push could happen - if notify was before wait registration, lost wakeup UNLESS Notify::notified() used.

Tokio Notify: recommended pattern is loop { lock; if let Some = pop; drop lock; process; } or wait while empty with notified().

7. **Global write lock vs RouteInbox**: v2 doesn't specify lock hierarchy. If teardown holds write lock and tries to join drain which needs inbox lock, while read loop holds inbox and looks up snapshot... need to check if dispatcher ever takes forwarding write lock. Not specified - MINOR/MAJOR inversion risk.

8. **module_sink.send not cancelled in pseudocode**: §5 claims select! against cancel_token but §2 drain only shows plain send.await - NOT-CLOSED partial for B9 unless implementation mandate is clear.

9. **Synthetic egress**: v2 read loop synthesizes errors but doesn't specify try_send vs await - v1 #13 still open. read loop must not await - if try_send fails, terminal vanishes. NEW MINOR.

10. **Q4 corr duplicate**: v2 lean enforce at enqueue - NOT in required mechanism, only open question. slots.insert overwrites - NOT-CLOSED for Q4 until enforced.

11. **R11 + CAS**: v2 slots.remove gate prevents duplicate release at call site; first terminal removes slot releases; second terminal release=false so release() not called - CLEAN, no conflict with CAS. CLOSED.

12. **B5 insert before send**: Under lock state=Delivered before send - terminal path needs slots.remove - happens after Delivered set. Fast terminal: module receives after send completes on wire, but Delivered is before send - so when send returns, already Delivered. Terminal arrives on module connection after network - daemon module→client path runs slots.remove - CLOSED.

13. **B2 Claimed during acquire**: CANCEL sets cancelled=true while drain in acquire.await. Post-acquire lock sees flag, rollback - CLOSED.

14. **B2 double terminal Queued**: Race with pop - NOT-CLOSED (finding 2).

15. **B10 closed flag**: Published under write lock + AtomicBool on binding - module→client must check closed - v2 says router path checks - if only dispatcher checks admission Closing but snapshot still has Bound without closed check on module path - section 7 says both. Shipped router doesn't have closed - v2 adds. Ordering: closed=true before snapshot publish removes route - stale Arc still has closed=true - CLOSED if implemented on ALL paths including forward_backend before dispatcher.

Stale Bound: lookup returns Arc clone - binding still in snapshot until publish - closed set on binding before remove from snapshot - reader gets old Arc with closed=true - unknown_channel - CLOSED.

16. **B7 client-only offload**: Module inline - test is module sends request immediately after bind ack on module connection - stays inline - CLOSED.

17. **control_backpressure** for module responses on client ch0 - v2 still generic overflow - v1 #14 partially unresolved - NEW MAJOR?

18. **Byte cap DoS** - v2 Q3 lean only, not in mechanism - NEW from v1 #12 - still open MINOR for re-gate unless blocker.

Verdict: NOT-CLOSED on B2 due to pop/CANCEL race and possible dual-drain; B9 partial (select not in drain spec); Q4 not enforced. → NO-GO or GO-WITH-CHANGES with specific fixes.

Also check: Delivered CANCEL forwards to module - client already waiting - if read loop synthesized nothing, module handles cancel - if request already complete module no-op - one terminal from module maybe none - OK.

Claimed boundary: CANCEL Some(Claimed{cancelled}) sets flag - but if state is Claimed without cancelled false and corr not in queue - what if drain hasn't set Claimed yet? Race: popped, first Claimed lock not taken - still Queued in slots - CANCEL removes as Queued - GOOD. Race: popped, between pop and Claimed lock still Queued - CANCEL removes - drain then fails slots[corr] - need to handle missing slot - NOT SPECIFIED - drain would panic or error - MAJOR.

If CANCEL wins Queued removal while drain has corr locally:
```
{ lock; slots[corr].state = Claimed }
```
fails if slot removed - spec doesn't handle - BLOCKER.

I'll include this in B2 NOT-CLOSED.

For dual drain: after pop corr not in queue, second pop gets next - first still processing - two acquires - SERIAL concurrency still two in flight credits - violates serial semantics and I1 - BLOCKER.
Checking drain-loop structure and whether the design addresses the pop/CANCEL interleaving.

[assistant] 2026-07-15T08:13:31Z
I have enough for a comprehensive adversarial re-gate. Let me also quickly check if errorFromFrame becomes terminal - yes settle with errorFromFrame returns SubcError not SubcCallError, then managed call throws SubcCallError terminal from line 423.

For TS error terminals: kind would be from reject - need to see how SubcError from errorFromFrame maps in call() - it's not SubcCallError unless wrapped. Actually reject uses errorFromFrame -> SubcError, not SubcCallError. So managed call catch: `if (!(err instanceof SubcCallError)) throw terminal` - so SubcError from daemon Error frame would become terminal via wrap... need to check.

Actually at 1059-1060: pending.reject(this.errorFromFrame(frame)) - SubcError not SubcCallError. In call() 423: if not SubcCallError, throw terminalCallError - so data plane Error becomes terminal kind, not retried.

Confirmed B1 mechanism in v2 is merge-0 prerequisite - CLOSED as design intent but NOT CLOSED until merge-0 ships - the blocker is addressed in design but deployment gate remains.

For adversarial rating:
- B1: NOT-CLOSED operationally until merge-0; v2 correctly identifies and specifies merge-0 - design closure is YES for mechanism, NO for deployability without merge-0. Prompt asks if blocker is ACTUALLY closed - merge-0 is not in repo, broca unverifiable → NOT-CLOSED for gate until merge-0 lands.

Actually re-read: "VERIFY each v1 blocker B1-B10 is ACTUALLY closed by the v2 mechanism"
- B1: v2 mechanism is merge-0 prerequisite + fail-loud only after merge-0. The mechanism closes the false claim but the backpressure path still breaks consumers without merge-0. → CLOSED in design honesty, NOT-CLOSED for GO without merge-0 deployed.

I'll say: **NOT-CLOSED** (prerequisite merge-0 is specified but not implemented in this checkout; emitting route_backpressure before fleet deploy is still a production breaker per client.ts:1059-1060, 423-452).

B2: Mostly CLOSED but with race at pop/Claimed boundary - I'll say NOT-CLOSED or CLOSED with caveat on N1.

Let me be precise on pop race:
- T1: drain pop removes corr from queue (implicit in pop_front - the corr is removed from queue deque)
- Slot still Queued until next lock sets Claimed
- Cancel Queued: remove from queue+slots - but if already popped from queue, slot is Queued, cancel removes slot - good
- If cancel between pop and Claimed set: slot Queued, cancel removes - good  
- If cancel after Claimed false set: Claimed cancelled true - good
- Double terminal: cancel Queued removes before drain claims - if drain already popped, state Queued, cancel removes - synthetic cancelled. Drain then tries slots[corr] for Claimed - **slot gone** → panic or skip?

Drain pseudocode:
```
corr = pop_front queue
{ lock; slots[corr].state = Claimed }
```

If cancel removed slot between pop and second lock, `slots[corr]` is missing → **UB/panic** not handled in spec.

That's a NEW BLOCKER.

Also: cancel Delivered|None forwards to module; if cancel was for Queued that drain just popped but not yet Claimed - cancel sees Queued in slots, removes - OK.

Cancel None: if corr was popped and slot removed... cancel None forwards to module - **cancel loss** if request still in flight on drain side without slot!

Interleaving:
1. Drain pop_front corr X from queue (X not in queue)
2. Cancel X: slots.get_mut(X) = Some(Queued) still! → removes, synthetic cancelled
3. Drain: slots[X].state = Claimed → **missing** - spec doesn't handle

OR:
1. Drain pop X
2. Cancel: Queued → remove slot + queue (X already not in queue) → synthetic cancelled  
3. Drain: acquire credit, lock, take slot - **missing** - handle_closed? leak credit if acquired?

If slot missing after pop, drain still has corr from local variable, acquire already happened? Order is pop, set Claimed, then acquire. So:
- pop
- set Claimed (fails if no slot)
- acquire

If cancel between pop and Claimed: slot removed, Claimed fails
If cancel between Claimed and acquire: Claimed{cancelled:false}, cancel would need Claimed branch - if cancel sees Queued only in match, slot is Claimed - **CANCEL matches Claimed{cancelled}** only for Some(Claimed{cancelled}) => set true. Good.

If cancel between pop and Claimed: Queued removed - drain sets Claimed on missing entry - **BUG**

**NOT-CLOSED for B2** due to pop-without-slot atomicity gap.

B3: VecDeque+HashMap+mutex - CLOSED

B4: TeardownKind + error arms + AcquiredCredit - partially CLOSED, teardown None vs module death - v2 says None→backend_error; shipped uses endpoint_is_draining for module_reloading else backend (router.rs:465-484). Teardown field must be set on flow.close() in release paths (forwarding.rs:1424). v2 doesn't specify WHO sets TeardownKind on ConnClose vs Reloading vs Goodbye vs module death - **MAJOR gap**

Read v2 §4 again - teardown set by mutator that closed flow. release_client_route_locked only flow.close() - no TeardownKind in shipped code. **NOT-CLOSED** until every close() site sets kind atomically with close.

B5: Delivered before send under lock - CLOSED for insert-order if B2 pop race fixed

B6: O(1) cancel - CLOSED

B7: client-side only - CLOSED for bind barrier

B8: slots.remove gate + CAS - CLOSED cleanly (complementary guards)

B9: admission Closing, cancel, join - NOT CLOSED fully:
- select! on module_sink.send not in pseudocode (only mentioned in prose)
- FrameSink clones on RouteBinding (release_client_route_locked clones module_sink)
- drain may hold clone after binding released from map
- v2 doesn't close bounded channel on all sender drops explicitly
- synthetic egress still awaited on read loop today (server.rs:395-399) - v2 read loop try_push synthesize not specified await vs try_send for errors

B10: publish under lock + closed AtomicBool - CLOSED in design IF implemented; race closed flag vs snapshot - reader must check closed after load; ordering: set closed under write lock before publish remove - **CLOSED** if discipline followed

B11 from v1 #11 corr uniqueness - v2 Q4' lean enforce at enqueue - NOT CLOSED until enforced in §2 pseudocode (insert overwrites silently)

I'll write the full response.
<COUNCIL_MEMBER_RESPONSE>

## V1 blocker verdicts (B1–B10)

| Blocker | Verdict | Justification |
|---------|---------|---------------|
| **B1** (SDK `route_backpressure` / zero SDK changes) | **NOT-CLOSED** | v2 correctly retracts v1 and mandates merge-0 (`docs/subc-dispatch-redesign-v2.md` , but **no SDK change exists in this repo**: TS `classifyFailure` only distinguishes `not_sent` vs `outcome_unknown` when `handedToSocket` (`clients/subc-client/src/client.ts:781-793`); daemon `Error` terminals settle via `errorFromFrame` → `SubcError` (`1059-1060`), and `call()` retries only `unknown_channel` / `not_sent`, treating other errors as terminal or `outcome_unknown` + reconnect (`423-452`). Rust mirrors: `CallError::Module` with no `route_backpressure` retry (`crates/subc-client-rs/src/consumer.rs:570-579`, `3130-3134`). Until merge-0 is deployed fleet-wide (broca/aft/alfonso-core **unverifiable here**), merge-2 fail-loud admission is still a production contract break. |
| **B2** (CANCEL limbo / double terminal) | **NOT-CLOSED** | The single-lock state machine fixes v1’s acquire/send limbo **only if** queue pop and slot mutation are one atomic step. v2 splits them: `pop_front` then a **second** lock to set `Claimed` (`docs/subc-dispatch-redesign-v2.md`  lines 86-87). A CANCEL on `Queued` can `remove` slot+queue while the drain already holds `corr` from `pop_front` → next step does `slots[corr].state = Claimed` on a **missing** entry (unspecified → panic or skip → credit/request orphan). That revives limbo/double-terminal at the pop boundary, not only at acquire. |
| **B3** (queue primitive / mpsc scan race) | **CLOSED** | `Mutex<RouteInbox>` with `VecDeque` + `HashMap` corr→`Slot`, O(1) CANCEL and drain (`, `). No concurrent mpsc recv + read-loop scan. |
| **B4** (drain error arms / RAII / `ChannelFlowClosed`) | **NOT-CLOSED** | Send-failure `release` + `AcquiredCredit` + `outstanding` rollback are specified (` lines 97-101, 109-111). **`TeardownKind` is not wired in shipped code**: `release_client_route_locked` only calls `route.flow.close()` (`crates/subc-core/src/forwarding.rs:1424`) with no reason field; `handle_closed` reads `self.teardown` (`) but the spec does not require atomic set-on-close at every `close()` site (release, reload drain, conn close, module death). Shipped disambiguation uses `endpoint_is_draining` on acquire failure in the read path (`router.rs:465-477`), not a per-route teardown flag—mapping module death to `None`→`backend_error` (`) is plausible for `blocked_flow_control_acquire_wakes_when_module_tears_down` (`crates/subc-core/tests/forwarding.rs:3811-3877`) **only if** teardown is set correctly on every close; that ordering is not specified. |
| **B5** (`outstanding` / Delivered before send) | **CLOSED** (conditional on B2) | `state=Delivered; outstanding+=1` under inbox lock **before** `module_sink.send().await` (` lines 92-97). Module→client terminal `slots.remove` + `release` (`) can then observe Delivered. Ordering matches the fast-terminal race v1 flagged (`router.rs:281-309` today releases unconditionally). **Invalid if B2 pop/slot race fires.** |
| **B6** (O(queue) CANCEL DoS) | **CLOSED** | `slots.get_mut(corr)` is O(1) (` lines 72-76); no read-loop scan. |
| **B7** (whole channel-0 FIFO / bind-ACK barrier) | **CLOSED** | Module connections stay inline (`); only **client** channel-0 is offloaded. Shipped barrier test `accepted_route_publishes_route_open_before_immediate_reverse_request` (`router.rs:1078-1102`) depends on module inline commit—v2 explicitly preserves that. Reserved/Absent drops remain on module path (`router.rs:227-245`). |
| **B8** (I3 “release untouched” / R11 framing) | **CLOSED** | v2 corrects framing: concurrent duplicate while `in_flight>0` (` lines 115-118). Call-site gate `slots.remove` was Delivered → `release` (` lines 123-128) **complements** shipped CAS `in_flight!=0` guard (`forwarding.rs:1702-1731`)—first terminal decrements; duplicate finds slot gone → `release=false` → second `release()` hits `observed==0` and is ignored. No conflict; redundant safety. |
| **B9** (GOODBYE flush vs enqueue; teardown hang) | **NOT-CLOSED** | `admission=Closing` before snapshot removal (` steps 1, 5-6) closes the stale-snapshot **push** hole in principle. Gaps: (1) drain pseudocode shows bare `module_sink.send().await` (` line 97)—`select!` against `cancel_token` is prose-only (` line 171), not normative in the loop; (2) `RouteBinding` clones `module_sink` on release (`forwarding.rs:1432`); a drain task blocked on `send` with a retained `FrameSink` can still hold the bounded channel open—same class as v1 #9 (`server.rs:262-272` writer grace/abort does not unblock module sink); (3) read-loop synthetic errors still **await** egress today (`server.rs:388-400`); v2 read path says “synthesize” without mandating non-blocking egress—v1 #13 survives. |
| **B10** (merge-1 snapshot not invariant-neutral) | **CLOSED** (design-level) | Publish-under-write-lock + `closed: AtomicBool` set before publish-remove + recheck → `unknown_channel` (`, `router.rs:350-358` pattern). Restores stale-`Bound` observable vs `backend_error` from blocked acquire on dead flow (`router.rs:465-484`). **Implementation not present**; mechanism is coherent if every consumer checks `closed` after snapshot load. |

---

## New defects introduced by v2

### Finding N1: Pop-then-Claimed is not atomic with CANCEL (reopens B2)
- **Severity**: BLOCKER  
- **Location**: `docs/subc-dispatch-redesign-v2.md`  drain loop (lines 86-87)  
- **Confidence**: high  
- **Issue**: After `pop_front`, the corr is out of the FIFO but may still be `Queued` in `slots`. CANCEL can remove the slot and synthesize `cancelled` while the drain still proceeds with that `corr`, leading to missing-slot access, duplicate terminals (synthetic + module), or an acquired credit with no slot.  
- **Evidence**: Two separate critical sections; CANCEL only matches `Some(Queued)` etc. (` 72-76), not “popped but not yet Claimed.”  
- **Suggested fix**: Single lock: `pop_front` + transition `Queued→Claimed{cancelled:false}` atomically; or keep corr in queue until post-acquire claim; drain must handle absent slot after pop as rollback without acquiring.

### Finding N2: Duplicate-corr `slots.insert` can leak credit (Q4′ not enforced)
- **Severity**: BLOCKER  
- **Location**: ` read loop line 66; ` Q4′ lean only  
- **Confidence**: high  
- **Issue**: `slots.insert(corr, …)` silently overwrites; v1 #11 remains. First in-flight corr loses its slot; terminal/release accounting attaches to one corr; the other leaks window capacity (`router.rs:452-497` admits without corr uniqueness).  
- **Evidence**: No reject-on-duplicate in pseudocode; wire uniqueness not enforced in daemon.  
- **Suggested fix**: Under lock, reject duplicate corr in `queue`/`slots`/non-terminal `outstanding` with protocol error close; normative in  not only Q4′.

### Finding N3: `TeardownKind` lifecycle unspecified vs all `flow.close()` sites
- **Severity**: MAJOR  
- **Location**: `, ` step 2; `forwarding.rs:1424`, `1455`  
- **Confidence**: high  
- **Issue**: `handle_closed` branches on `teardown` (` 141-148) but shipped `close()` has no kind. Without a happens-before rule (set kind under inbox or forwarding write lock, then `close()`), drain can read stale `None` and emit wrong terminal (module reload vs `backend_error` vs silent drop).  
- **Suggested fix**: Extend close path: `set_teardown(kind); flow.close()` under defined lock order; table every caller (release, reload, conn drop).

### Finding N4: `AcquiredCredit` + explicit `release` on rollback/send-error may double-release
- **Severity**: MAJOR  
- **Location**: ` lines 94-101, 109-111  
- **Confidence**: medium  
- **Issue**: Rollback and send-error paths call `flow.release()` explicitly while `AcquiredCredit` may still be in scope until `commit()`. Double `release()` is masked by CAS (`forwarding.rs:1705-1714`) but can hide a logic bug (outstanding decremented once, credit released twice attempt).  
- **Suggested fix**: Normative: `commit()` disarms guard before terminal-path release; rollback uses guard drop only, or explicit release + `mem::forget(guard)`.

### Finding N5: Notify lost-wakeup / spurious-wakeup pattern unspecified
- **Severity**: MAJOR  
- **Location**: ` `notify`; ` line 86 (“if None -> wait notify”)  
- **Confidence**: medium  
- **Issue**: Classic pattern requires `notify_one` after push (` 67) and a loop: recheck queue under lock after `notified().await` to avoid lost wakeup if notify fires between pop-empty and wait. Spec does not show the wait loop or `Closing` exit predicate.  
- **Suggested fix**: Document `while admission==Open { lock; pop or break; unlock; work; }` / `notified()` with lock-held recheck.

### Finding N6: Global forwarding write lock vs `RouteInbox` mutex hierarchy absent
- **Severity**: MAJOR  
- **Location**: `, `; `forwarding.rs` `RwLock` + future per-route `Mutex`  
- **Confidence**: medium  
- **Issue**: Teardown step 6 uses epoch-fenced release under forwarding write lock (`forwarding.rs:1409-1470`) while dispatcher holds `inbox`. If any path takes `write_inner` then awaits or blocks on `inbox` (or reverse), deadlock. v2 does not state lock order (e.g. **never** hold `inbox` across forwarding write).  
- **Suggested fix**: Explicit hierarchy: forwarding write > route inbox; teardown sets `admission`/`teardown` without nesting; snapshot publish only under write lock with inbox released.

### Finding N7: Synthetic terminal egress still blocking / lossy
- **Severity**: MAJOR  
- **Location**: ` synthesize paths; shipped `server.rs:388-400`  
- **Confidence**: high  
- **Issue**: v2 promises `cancelled` / `route_backpressure` / `route_closing` from read loop “non-blocking O(1)” (`, I6) but does not require reserved try_send lane or connection close on full egress. Full client egress (`CONNECTION_EGRESS_BUFFER` 64, `server.rs:243`) can drop or force await—zero-terminal / renewed HOL.  
- **Suggested fix**: Reserved synthetic egress or actor; on `Full`, escalate close; forbid `.await` on read hand-off except documented exceptions.

### Finding N8: Client control-queue offload can reorder module relay responses (v1 #14 partial carryover)
- **Severity**: MAJOR  
- **Location**: `  
- **Confidence**: medium  
- **Issue**: Client channel-0 FIFO can queue client `route.open` behind other control while module **Responses** on channel 0 still go through control handler inline on whichever connection (`router.rs:207-217`). If client control task reorders client-originated control vs data, OK; but `control_backpressure` on overflow (`) applied to responses that settle relay RPCs is still hazardous—v2 does not exempt module-originated channel-0 responses.  
- **Suggested fix**: Inline module control responses / relay completions; cap only client-issued control commands.

### Finding N9: Byte/memory DoS (v1 #12) still open
- **Severity**: MAJOR  
- **Location**: ` Q3′ only; depth_cap frame-count (` line 65)  
- **Confidence**: high  
- **Issue**: `depth_cap` bounds frame count, not bytes; bodies allocated on read before admission (`frame_io` path per v1 synthesis). v2 does not charge byte budget at enqueue.  
- **Suggested fix**: Per-route/per-connection byte cap at push, RAII on dequeue/teardown.

---

## Q1′–Q5′ lean rulings

| Q | Lean in v2 | Ruling |
|---|------------|--------|
| **Q1′** | Hard-gate merge-0 | **RIGHT** — fail-loud without SDK is proven fatal (`client.ts:781-793`, `consumer.rs:570-579`). Interim blocking admission is **RIGHT-BUT-UNSAFE** if used to skip merge-0 indefinitely (reintroduces wait semantics, must cap wait). |
| **Q2′** | 2s bounded-join; abort OK on close | **RIGHT** — matches `server.rs:262-272` writer pattern; acceptable to drop in-flight terminal when connection is closing **if** credit/outstanding drained in step 5 and abort does not leak `in_flight` (`forwarding.rs:1697-1698`). |
| **Q3′** | Frame cap; byte cap “lean measure” | **WRONG** as gate lean — frame-only cap does not close DoS; must be **RIGHT-BUT-UNSAFE** to ship merge-2 without byte budget. |
| **Q4′** | Enforce corr uniqueness at enqueue | **RIGHT** — but **WRONG** until  mandates it (currently **NOT-CLOSED**, N2). |
| **Q5′** | Whole-table snapshot | **RIGHT, conditional** on publish-under-lock + `closed` recheck (`); shard later per T9. |

---

## Summary

| Severity | New findings |
|----------|----------------|
| BLOCKER | N1 (pop/Claimed vs CANCEL), N2 (duplicate corr) |
| MAJOR | N3–N9 |

**V1 blockers fully CLOSED:** B3, B6, B7, B8.  
**Conditionally CLOSED:** B5 (needs B2), B10 (design only).  
**NOT-CLOSED:** B1 (merge-0 not in tree / fleet), B2 (pop atomicity), B4 (TeardownKind wiring), B9 (select!/sink clones/synthetic egress).

---

## Bottom-line verdict: **NO-GO**

Un-refuted concurrency/contract defects remain:

1. **B2 / N1** — split `pop_front` and `Claimed` leaves a CANCEL vs drain race (`docs/subc-dispatch-redesign-v2.md:86-87`).  
2. **B1** — merge-0 prerequisite not implemented; current SDKs treat data-plane Errors as terminal/outcome_unknown (`client.ts:1059-1060`, `423-452`; `consumer.rs:570-579`).  
3. **N2 / Q4′** — duplicate corr not enforced at enqueue (`66`).  
4. **B4 / N3** — `TeardownKind` not tied to all `flow.close()` call sites (`forwarding.rs:1424`).  
5. **B9 / N7** — teardown `select!` and non-blocking synthetic egress not normative; `FrameSink` clone hang class persists (`forwarding.rs:1432`, `server.rs:388-400`).

### Minimum changes for **GO-WITH-CHANGES** (re-gate after spec edit + tests T10–T13)

1. Atomic **pop+Claim** (or equivalent) under one inbox lock (`86-87`).  
2. **Reject duplicate corr** at enqueue (`66`).  
3. **TeardownKind** set-before-close table for every `flow.close()` (`forwarding.rs:1409-1470`).  
4. Normative drain **`select!`** (acquire, send, cancel_token) + **lock hierarchy** doc (`, `).  
5. **Synthetic error egress** policy (try_send / reserved buffer / close on full) (`server.rs:243`, `388-400`).  
6. **merge-0 landed and verified** in consumer repos before merge-2 emit (`).  
7. **AcquiredCredit** commit/disarm rules (`94-101`).  
8. Notify **wait loop** with lock-held recheck (`86`).  
9. Add **byte budget** to admission or accept as explicit re-gate blocker (` Q3′`).

Until those are in the spec and covered by T10–T13 (and merge-0 is deploy-proven), the gate should stay **NO-GO**.

</COUNCIL_MEMBER_RESPONSE>

