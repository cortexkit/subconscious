---
task_id: bg_40ece100
agent: council-member
session_id: ses_09b2a701effe8ov5pxZ32D60oF
parent_session_id: ses_09b2b56e1ffeYwM1Vc5SuTRFuZ
status: completed
completed_at: 2026-07-15T08:27:30Z
---

[assistant] 2026-07-15T08:13:15Z

[assistant] 2026-07-15T08:13:17Z

[assistant] 2026-07-15T08:13:20Z

[assistant] 2026-07-15T08:13:22Z
Now I have the v1 findings. Let me look at the v1 redesign to understand what was claimed:

[assistant] 2026-07-15T08:13:25Z

[assistant] 2026-07-15T08:13:29Z
Confirmed: forwarding.rs:1692-1700 acquire does permit.forget() then in_flight.fetch_add — credit is NOT RAII-held by the task. forwarding.rs:1702-1731 release has CAS in_flight!=0 guard that ignores over-release (best-effort, not security).

Let me look at the full forwarding.rs for related context:

[assistant] 2026-07-15T08:13:31Z

[assistant] 2026-07-15T08:13:33Z

[assistant] 2026-07-15T08:13:39Z

[assistant] 2026-07-15T08:13:41Z
Confirmed shipped: server.rs:381-386 route_result is select!'d on close_receiver, and on Err the error_frame is sent via `ctx.egress.send(error_frame).await` at server.rs:399 — that is the shipped Error-frame recovery via awaited egress.

Let me look at the test that v2 must preserve:

[assistant] 2026-07-15T08:13:47Z

[assistant] 2026-07-15T08:13:50Z
Confirmed: the test asserts the client sees a `backend_error` for the blocked corr when the module is torn down. The v2 doc claims the `None`/backend_error arm preserves this test.

Let me look at the existing handle_control_frame and the route.bind/handle_route_goodbye for v2  mechanism:

[assistant] 2026-07-15T08:13:56Z

[assistant] 2026-07-15T08:13:58Z

[assistant] 2026-07-15T08:14:00Z

[assistant] 2026-07-15T08:14:03Z

[assistant] 2026-07-15T08:14:04Z
Now let me look at the relevant `bind-relay` mechanism and `commit_route_locked` to understand how routes are bound and what's the inline commit-before-data ordering:

[assistant] 2026-07-15T08:14:09Z

[assistant] 2026-07-15T08:14:11Z

[assistant] 2026-07-15T08:14:17Z

[assistant] 2026-07-15T08:14:20Z
Confirmed shipped `commit_route_locked` at forwarding.rs:1472-1564 publishes the binding + sends route_open_frame all under the same write lock. This is what v2 wants to preserve (in the form of publish-under-write-lock).

Let me see what `route_open_frame` and route.open does in `control.rs`:

[assistant] 2026-07-15T08:14:25Z

[assistant] 2026-07-15T08:14:27Z

[assistant] 2026-07-15T08:14:29Z

[assistant] 2026-07-15T08:14:32Z

[assistant] 2026-07-15T08:14:35Z

[assistant] 2026-07-15T08:14:37Z
Now let me look at the route.open control flow to confirm how the inline commit works today:

[assistant] 2026-07-15T08:14:41Z

[assistant] 2026-07-15T08:14:44Z

[assistant] 2026-07-15T08:14:49Z

[assistant] 2026-07-15T08:14:52Z
Now let me search for any existing route.open implementation to understand how the module ack is awaited:

[assistant] 2026-07-15T08:14:55Z
Good, I see the shipped route.open path. The route.open has its own `module_sink.send(relay_frame).await` and `timeout_at(relay_deadline, receiver).await` — those are the existing inline awaits. The route.open happens inline today. v2 says only this `route.open` op (the bind-wait) should be offloaded to a per-client control task.

Now let me check existing dispatcher / RouteDispatcher code in the source (maybe v2 is partially already implemented):

[assistant] 2026-07-15T08:15:02Z

[assistant] 2026-07-15T08:15:04Z
Confirmed: v2 mechanisms are NOT in the shipped source. The doc is design-only and untested.

Now let me check the read_inner / write_inner pattern in forwarding.rs to verify lock ordering claims:

[assistant] 2026-07-15T08:15:08Z

[assistant] 2026-07-15T08:15:10Z

[assistant] 2026-07-15T08:15:13Z
Confirmed: shipped uses a std::sync::RwLock. v2 wants to convert to ArcSwap<Snapshot>. Lock ordering question: in v2, the RouteInbox Mutex would be NEW; the global forwarding write lock is the EXISTING lock. We need to check if any v2 path holds both.

Now let me look at supervisor/registry / module reload/GOODBYE flow:

[assistant] 2026-07-15T08:15:17Z

[assistant] 2026-07-15T08:15:19Z
Good. Now I see `flow.close()` is what triggers `ChannelFlowClosed` errors on `flow.acquire()`. This is set by `begin_module_drain`. The teardown kind (`Reloading` vs `None`) in v2 has to be set in coordination with this.

Now let me look at the connection close / GOODBYE teardown to understand the current path:

[assistant] 2026-07-15T08:15:24Z

[assistant] 2026-07-15T08:15:26Z
Now let me look at `cleanup_connection` in forwarding.rs:

[assistant] 2026-07-15T08:15:30Z

[assistant] 2026-07-15T08:15:32Z
Now let me check what `endpoint_by_connection` returning Some means in `lookup_data_route` and the `closing_connections` interaction:

[assistant] 2026-07-15T08:15:36Z

[assistant] 2026-07-15T08:15:38Z
Important: `ForwardingTable` has a `RwLock` (`inner`) and a `close_registry: Mutex`. The v2's `RouteInbox` is a per-route Mutex. Let me also see what the existing `lookup_data_route` returns to confirm `Bound` for module→client path and what `Arc<RouteBinding>` contains:

[assistant] 2026-07-15T08:15:42Z

[assistant] 2026-07-15T08:15:44Z
Confirmed `RouteBinding` has `flow: Arc<ChannelFlow>`. No `closed: AtomicBool` exists in the shipped code — v2 proposes to add it.

Let me look at the tests for v2 to understand what new tests would need to validate:

[assistant] 2026-07-15T08:15:47Z

[assistant] 2026-07-15T08:15:50Z
Now let me look at the SDK classifyFailure claim for B1 verification:

[assistant] 2026-07-15T08:15:55Z

[assistant] 2026-07-15T08:15:57Z

[assistant] 2026-07-15T08:16:00Z

[assistant] 2026-07-15T08:16:02Z

[assistant] 2026-07-15T08:16:04Z
Confirmed: managed `call()` retries ONLY `unknown_channel` (in-place, with one retry) and `not_sent` (triggers reconnect); everything else (terminal, outcome_unknown) re-throws or schedules reconnect. So the v1 B1 finding is real: a data-plane `Error{route_backpressure}` is a "terminal" kind and the SDK will re-throw it. The v2 doc correctly identifies this and proposes merge-0 (B1) as a prerequisite. 

Let me now look at the consumer.rs to verify the Rust SDK claim:

[assistant] 2026-07-15T08:16:11Z

[assistant] 2026-07-15T08:16:13Z

[assistant] 2026-07-15T08:16:16Z
Confirmed: Rust consumer only retries `unknown_channel` and not_sent; everything else becomes `CallError::Module(body)`. The v1 B1 finding stands.

Now let me look at the v1 Gate archive's other findings to understand the context. I need to look at the protocol/body spec to see if `body` is parsed:

[assistant] 2026-07-15T08:16:20Z

[assistant] 2026-07-15T08:16:23Z
Now let me look at the v1 B2 cite about limbo: claim that under the v1 design CANCEL inspects queue and gets "not in queue" → forwards to module, which is a no-op for unknown corr. Let me also check the B5 ordering:

[assistant] 2026-07-15T08:16:27Z
Now let me look at the supervision/drain flow to understand if a "module_reloading" terminal is produced for an in-flight blocked acquire:

[assistant] 2026-07-15T08:16:30Z
Now, the key thing v2 says about  / `teardown: TeardownKind`:

The shipped `flow.acquire()` returns `ChannelFlowClosed` (per forwarding.rs:1692-1700). Currently shipped handler at router.rs:465-485 produces:
- if endpoint is draining → `module_reloading`
- else → `backend_error`

That's exactly what the v2 doc claims the test `blocked_flow_control_acquire_wakes_when_module_tears_down` demands.

Now let me check the v2 mechanism question: is `teardown` set under the right lock ordering, and can it be stale when drain reads it?

The v2 design:
- `teardown: TeardownKind` is a per-route field on `RouteDispatcher`
- The mutator that closes the flow also sets teardown
- In v2 the only thing that closes a flow is: (a) the route's GOODBYE path (Closing/Closed admission), (b) module drain (Reloading), (c) module death (None → backend_error)
- The drain reads `teardown` after the flow.acquire() returns ChannelFlowClosed

But the v2 design has the per-route RouteInbox mutex. The flow.close() must be done under the same lock, or at least in a deterministic order. Let me look at the v2 mechanism more carefully.

V2 says: "lock inbox: take slot; kind = self.teardown". So the drain reads teardown under the inbox lock. That means teardown must also be set under that lock.

But the v2 flow.close() is done by the mutator. If the mutator (e.g. module drain) is operating on a global forwarding table lock, and the drain task reads the per-route inbox lock, lock ordering matters: drain can't hold the inbox lock and try to acquire the global forwarding lock. Let me check the v2 spec more carefully:

Looking at 
```
1. lock inbox: admission = Closing
2. flow.close() with teardown=<reason>  // wakes any blocked drain acquire into handle_closed
3. cancel_token.cancel()
4. bounded-join the drain task
5. lock inbox: admission = Closed
6. epoch-fenced release of the binding
```

So step 2 (flow.close + set teardown) happens while the inbox lock is HELD. Then the lock is released, then the drain task runs, then step 4 joins it.

What about module drain? That's done outside the route's own teardown. Module drain is initiated by the supervisor, calls `begin_module_drain` (forwarding.rs:1023) which iterates routes' flows and calls `flow.close()` on each. It does NOT set a per-route teardown. So v2 says "the mutator that closed the flow" should also set teardown — but the v2 doc does not specify the pathway. This is a significant new mechanism with insufficient detail.

Let me also verify: the v2 doc's flow.close() with teardown (step 2) calls `self.flow.close()` directly. The actual `ChannelFlow::close()` (forwarding.rs:1737) just calls `self.sem.close()` — it doesn't take a teardown kind. So v2 is asking us to invent a new mechanism where the `RouteDispatcher` itself owns the teardown kind, and the mutator that closes the flow writes teardown under the inbox lock first, then closes. This is not specified in the doc, just implied.

Now let me check: is there any current mechanism for "module death" (endpoint gone, not graceful reload/goodbye)?

[assistant] 2026-07-15T08:16:46Z

[assistant] 2026-07-15T08:16:50Z
Now let me look at the v2 doc's claim about per-client-connection FIFO for channel-0 (Q3 client-side-only). The v1 whole-channel-0 FIFO was rejected. V2 says offload ONLY the `route.open` to a per-client-connection control task, not the entire channel-0 FIFO. Let me see the issue with this — a "per-client-connection FIFO control task" — what does it do?

Reading v2 
"the ONLY blocking channel-0 op is `route.open`... Offload is scoped to the CLIENT connection's channel-0: a per-client-connection FIFO control task drains channel-0 frames so a client's `route.open` bind-wait no longer stalls that client's data frames + CANCELs."

So a per-client FIFO control task drains channel-0 frames. If "drains channel-0 frames in FIFO order" then if module re-originated ACK for a route.open from a prior route bind sits at the head of the channel-0 queue, and an immediate data frame on the new route comes from a different connection, there's no ordering issue (they're separate connections).

The concern is what happens on a SINGLE client connection when a client's route.open (which awaits) is in the FIFO, and a subsequent Ping or other control frame arrives. The v2 design says "control commands are FIFO on a single client connection" — that preserves route.open→route.close order. Ping in the FIFO behind route.open would be delayed. That's actually fine for control semantics; data frames go to the dispatcher.

The "good" property: the data path is decoupled from the control path. Data frames on this client connection go to dispatchers. CANCEL frames go to the relevant dispatcher's inbox. The route.open FIFO is the only thing in the control task. 

But wait: the read loop needs to classify a frame as channel-0 or data. That's fine: per-frame dispatch. CANCEL is a data-frame type. So far so good.

Now here's the gotcha: HELLO, route.open response, route.open ERROR — these are responses to daemon-initiated operations or to client-initiated ones. The doc says "Offload is scoped to the CLIENT connection's channel-0". So the client FIFO control task handles HELLO, route.open response, route.open errors, supervisor.list, etc.

What about route.bind responses? That's a channel-0 frame ON the MODULE connection. Per the v2 doc, module connections are NOT split. So route.bind response stays inline on the module connection's read path. Good.

But: the client's route.open request — it does `module_sink.send(relay_frame).await` to the MODULE, then `timeout_at(deadline, receiver).await` to wait for the module to ack. v2 says: "the client's route.open task awaits the module's ack, and the module's ack + route publication commit happen inline on the module connection's own read path (unchanged), so publication still happens-before any module data frame on the new route."

But wait: the route.bind commit happens via `commit_route_locked` (forwarding.rs:1472-1564) when the module's route.bind RESPONSE is processed by `handle_module_relay_response` (control.rs:2029-2031). That commit is on the module connection's read path. The route.open response is then sent to the client via the existing client egress.

The issue is timing. v2 says: "the module's ack + route publication commit happen inline on the module connection's own read path (unchanged), so publication still happens-before any module data frame on the new route."

This is the key claim. Let me trace:

Module connection read loop:
1. Read module frame F1 (route.bind RESPONSE → commit_route_locked publishes binding + sends route_open to client via OwnedPermit::send)
2. Read module frame F2 (immediate reverse request — Request on the new route)

These are processed serially in the same task. So F1's commit happens-before F2 is even read. F2's lookup_data_route sees the binding. So the bind barrier is preserved. ✓

But wait, the v2 doc says the module connection's read path is unchanged. It does NOT mention control offload for module connections. So module connections are still serial. Good.

Now there's a subtle issue: v2's client-side control FIFO — when a client's `route.open` task waits for the module ack, the module-side read loop does the commit. But if the client FIFO has Ping behind route.open, and the module's bind ack arrives, the route.open is settled, then the Ping is processed. No issue.

The actual concern is: what about the module-initiated catalog.update? The v2 says HELLO stays inline. But what about `catalog.update` (a module-originated channel-0 request, control.rs:807-818)? That's a control frame on the module connection. The shipped code processes it in the same control-handler that also handles `route.bind` responses. They share the read path. Fine.

But what if a module-originated channel-0 `catalog.update` (a control REQUEST) is followed by an immediate reverse request (data on a new route)? The reverse request hits the data lookup. The binding must already be published. Since the catalog.update runs INLINE on the module's read path, and the data frame runs INLINE on the same path, the serial ordering is preserved. Good.

OK, B7 seems plausibly closed. Let me also check B10 — `closed: AtomicBool` recheck:

[assistant] 2026-07-15T08:17:12Z

[assistant] 2026-07-15T08:17:15Z
Confirmed: no `closed: AtomicBool` exists on `RouteBinding` in the shipped code. v2 introduces this.

Now let me check the existing test referenced in the doc, particularly the `accepted_route_publishes_route_open_before_immediate_reverse_request` test to understand what it asserts:

[assistant] 2026-07-15T08:17:19Z
The test asserts the client sees: frame 1 = route.open response (channel 0, corr 700), frame 2 = data on the new route (corr 800). The fixture already commits the route (via `complete_pending_relay` with `Accepted`). The commit happens before any frame is read. So when the module sends its immediate reverse request, the route is already published.

This is a unit test of the data-plane router; it doesn't go through a full connection loop. So the "bind-ACK → immediate data" barrier holds because the route is already committed before the test runs. The real-world scenario (B7's "broken FIFO") is not exercised by this test — it's a per-call router test.

Let me look at the v1 read loop / loop in server.rs to confirm the v2 design correctly identifies the B7 hazard:

Now let me look at the test infrastructure to check what `attached` does:

[assistant] 2026-07-15T08:17:26Z

[assistant] 2026-07-15T08:17:28Z

[assistant] 2026-07-15T08:17:30Z
Now let me check existing channel-0 data frame types — does control.rs handle route.open such that it is "channel-0 only"?

[assistant] 2026-07-15T08:17:34Z
Now let me look at the protocol::wire spec to understand what's channel-0 vs data plane:

[assistant] 2026-07-15T08:17:36Z

[assistant] 2026-07-15T08:17:40Z

[assistant] 2026-07-15T08:17:43Z

[assistant] 2026-07-15T08:17:45Z
Confirmed: "Corr values are never reused within a connection's lifetime". v2's Q4' explicit says "enforce at enqueue" — this is a new behavior not present in the shipped code. v2 doc itself acknowledges this is an open question with "Lean: enforce at enqueue". 

Now let me check the routes bind step for memory DoS (B12) to see if v2's mechanism specifies byte budgets:

[assistant] 2026-07-15T08:17:50Z

[assistant] 2026-07-15T08:17:53Z
Confirmed: only egress buffer cap exists. No byte-based admission budget in shipped. v2's Q3' asks about byte-based secondary cap, with no commitment in the spec. 

Let me check the v2  mechanisms to see what has not been addressed about depth_cap:

[assistant] 2026-07-15T08:17:57Z
Confirmed: v2 Q3' explicitly says "Byte-based secondary cap" is OPEN, "Lean: keep max(4,2×window)". So v2 does NOT close the B12 byte-budget issue from v1 finding #12; it's flagged as an open question.

Let me check Q3's actual spec — what about the snapshot mechanism:

[assistant] 2026-07-15T08:18:01Z

[assistant] 2026-07-15T08:18:04Z
Now let me look at the dispatcher's v2 ordering claim more carefully. The doc says:
- Read loop enqueues a Request under inbox lock, sets state=Queued, pushes to queue
- Then lock+pop front of queue, set state=Claimed{cancelled:false}
- Then `flow.acquire().await` (B4: the ONE credit acquire)
- Then lock inbox, take Slot, if cancelled OR closed: release+drop; else set state=Delivered
- Then unlock
- Then `module_sink.send(frame).await`

Now: **the drain task is a single task per route**. It pops one at a time. The lock is held briefly for pop+claim, then released. The flow.acquire().await happens outside the lock. The second lock+decision+delivered-set+unlock happens after acquire. Then module_sink.send outside the lock.

**CAN a CANCEL that arrives during `flow.acquire().await` get applied correctly?**

Yes: when CANCEL arrives, the read loop takes inbox lock, sees state=Claimed, sets `cancelled=true`, releases lock. Then when the drain task wakes from acquire and re-locks, it sees `cancelled=true`, releases the credit, synthesizes cancelled, drops. Good. No limbo for the cancelled case.

**CAN a CANCEL arriving between `flow.acquire().await` and the second `lock inbox` get applied?**

Yes: same as above. The `cancelled` flag is set on the slot. When the drain re-locks, it sees it. Order is fine.

**CAN a CANCEL arriving AFTER state=Delivered (and before module_sink.send completes) cause a double terminal?**

Looking at the read-loop CANCEL logic in v2:
```
match slots.get_mut(corr):
    Some(Queued)              => remove from queue+slots; unlock; synthesize Error{cancelled}   // never acquired credit
    Some(Claimed{cancelled})  => set cancelled=true; unlock; return    // drain rolls back post-acquire
    Some(Delivered) | None    => unlock; forward CANCEL to module_sink
```

If state is `Delivered`, the read loop forwards CANCEL to the module_sink. The drain task, meanwhile, has set state=Delivered and is about to do `module_sink.send(frame).await`. There's a race:
- Drain sets state=Delivered, releases lock
- CANCEL arrives, read loop locks, sees Delivered, releases lock, forwards CANCEL to module_sink
- Drain's `module_sink.send(frame).await` happens after CANCEL has been sent (or before, FIFO ordering)

Either way, the module sees Request THEN CANCEL. Module's handle_cancel no-ops for unknown or sends cancel terminal. Module's terminal arrives, releases credit. Drain task's "module terminal" path takes the slot from `slots`, decrements outstanding, releases credit.

But: what if the module's terminal is processed BEFORE the drain's `module_sink.send` completes? That shouldn't happen because the module only sees the Request once it's sent. So if `module_sink.send` hasn't returned Ok, the module hasn't received it. The race is impossible: the module-side handle_cancel can't have run because the module hasn't received the Request.

So the `Delivered` state transition + `module_sink.send().await` ordering prevents the double-terminal race for a CANCEL that arrives between Delivered and send. The cancel forwarding happens AFTER Delivered is set. The drain has already committed to delivering. The module will see (Request, CANCEL) and emit one terminal.

Now what if the module's terminal arrives (router.rs:281-309) for a corr in the Delivered state, and the drain's `module_sink.send` is still in flight?

Looking at the v2  drain task:
```
match module_sink.send(frame).await:
    Ok(()) => {}                                          // terminal will release on module->client path
    Err(_) => { lock inbox: outstanding-=1; remove slot; unlock;
                flow.release();
                synthesize Error{backend_error} to client if route-alive }
```

If `module_sink.send` Ok: nothing happens locally. The terminal will release on the module->client path (router.rs:281-309). But the slot remains in `slots` and `outstanding` includes this corr. The module terminal will, on the module->client path, take the lock, `slots.remove(corr)`, decrement outstanding, release credit.

But what if the module terminal arrives BEFORE the drain's send completes? The module can't emit a terminal for a frame it hasn't seen. So module terminal can't arrive first. 

What about a SECOND terminal for the same corr? It arrives at router.rs:281-309 with the corr. The path takes inbox lock, `slots.remove(corr) was Delivered?` No — first terminal already removed. So release=false, forwarded but credit-inert. That's the v2 R11 fix.

**However**, there's a subtle issue I want to check carefully: the v2 doc says "remove slot" only happens on terminal. The drain task sets state=Delivered, increments outstanding. The module terminal path (router.rs:281-309) does `slots.remove(corr)`. What if the drain's send errors? Then the drain's error arm does `outstanding-=1; remove slot; release credit`. So slot is removed in both paths. Good.

What about the case where the drain is **between** `flow.acquire().await Ok` and the second `lock inbox`? CANCEL arrives, read loop tries to lock, but the drain has the lock? No, the drain is NOT holding the lock during acquire.await. The lock is released, then acquire awaits, then lock taken again. So a CANCEL arriving during acquire.await takes the lock, sees state=Claimed, sets cancelled=true, releases. Drain re-locks, sees cancelled, releases credit. Good.

But wait — there's a potential issue. Let me re-read:

```
loop:
  corr = { lock inbox; pop_front queue or None }        // if None -> wait notify (or Closing->drain-then-exit)
  { lock inbox; slots[corr].state = Claimed{cancelled:false} }
  match flow.acquire().await:                            // B4: the ONE credit acquire
```

The drain first does `pop_front queue or None` under one lock acquisition. Then does `slots[corr].state = Claimed{cancelled:false}` under another lock acquisition. **Between these two lock acquisitions**, a CANCEL could arrive! The read loop takes the lock, sees state=Queued, removes the corr from queue+slots, synthesizes cancelled terminal. Then the drain re-locks and... wait, the queue.pop_front already returned the corr. The slots no longer has the corr. But the drain's next step is `slots[corr].state = Claimed{cancelled:false}`.

This is a problem! After the CANCEL removed the slot, the drain's `slots[corr].state = Claimed{cancelled:false}` will... well, the slot is gone. The drain needs to handle "slot was removed by CANCEL" — but it does `slots[corr].state = Claimed{...}` which would re-insert the slot. Or it would panic on missing entry.

Wait, looking again: `pop_front` returns Some(corr), then drain has the corr. Then lock again and `slots[corr].state = Claimed{cancelled:false}`. The v2 doc says `slots[corr].state = Claimed{cancelled:false}` — the syntax `slots[corr].state` requires that `slots` has `corr`. If the CANCEL removed the slot, `slots[corr]` would be None and the assignment would fail.

Actually in Rust, `slots[corr].state = ...` would panic on missing entry. So this is a bug in the v2 mechanism. Or the doc is glossing over it. Let me re-read.

Actually, the v2 doc says:
```
{ lock inbox; slots[corr].state = Claimed{cancelled:false} }
```
This uses syntax that's shorthand. The intent is to find the slot by corr and update its state. If the slot is gone (cancelled), the drain task must check and skip.

But the v2 doc's pseudocode doesn't say that. It implies a direct update. This is a design gap.

Wait, actually, if the CANCEL removed the slot from the queue AND from the slots map, the corr is GONE from the dispatcher's state. The drain has the corr in hand (from pop_front). It doesn't need the slot to proceed with flow.acquire() because... no, it does need to know the frame to send. The slot holds the frame.

So the v2 mechanism is broken: if CANCEL arrives between pop_front and the state-Claimed write, the slot is gone, the frame is gone, and the drain has a corr it can't use.

OR the v2 doc means: the read-loop CANCEL, on seeing the slot, can only remove it if state is Queued. If state is Claimed, it just sets cancelled=true. So between pop_front and state=Claimed, the state is... still Queued? No, the drain just popped it from the queue. Is the queue.remove and the state=Claimed the same lock acquisition?

Let me re-read very carefully:
```
loop:
  corr = { lock inbox; pop_front queue or None }        // if None -> wait notify (or Closing->drain-then-exit)
  { lock inbox; slots[corr].state = Claimed{cancelled:false} }
```

Two separate `lock inbox` blocks. The first pops from the queue (and presumably the slot is still Queued). The second sets state=Claimed.

**This IS a race window.** Between the two locks, the state is Queued (the slot is in slots, NOT in queue, state=Queued). A CANCEL arriving in this window would find state=Queued, remove the slot, and synthesize cancelled. The drain then has a corr with no slot, and panics on the second lock.

This is a real defect in the v2 spec. It should be:
```
{ lock inbox; 
   match slots.get_mut(corr) {
     Some(slot) => slot.state = Claimed{cancelled:false},
     None => { pop_front was raced by a CANCEL; synthesize cancelled ourselves; continue }
   }
}
```

OR the pop and the state-Claimed must be under the SAME lock.

The v2 doc is incorrect on this concurrency. Let me note this as a v2-introduced defect.

Actually, hmm. The v2 doc says: "the read loop and the drain task both make their cancel/deliver decision under the SAME lock". The claim is that there's no limbo. But this specific two-lock-dance within the drain task reintroduces a window.

Let me also look at Notify lost-wakeup:

[assistant] 2026-07-15T08:18:47Z
The v2 doc has:
```
corr = { lock inbox; pop_front queue or None }        // if None -> wait notify (or Closing->drain-then-exit)
```

If pop_front returns None, the drain "waits notify". But the v2 doc doesn't say "lock is released first, then wait". This is pseudocode shorthand. The intent is clear: release lock, then `notify.notified().await`. Classic pattern.

Now: can a Notify wake-up be missed? Standard pattern:
- Read loop: lock, push, unlock, notify.notify_one()
- Drain: lock, pop, unlock (or pop returns None), [if None:] notify.notified().await

The drain checks "is queue empty?" under lock. If yes, releases lock, awaits notify. The read loop does push under lock, then notifies. Between the drain's "is empty" check and `notify.notified().await`, the read loop might:
- push (lock), unlock, notify.notify_one()
- The drain's "is empty" check: queue is empty, return None
- The drain's notify.notified().await
- The notify_one happens — does the drain see it?

Tokio's `Notify::notify_one` only stores a permit IF there are no current waiters. The drain has not yet called `notified().await` when the notify happens. So the permit is stored. Then drain calls `notified().await` and gets the permit immediately. Standard pattern, no lost wakeup. ✓

But what if the drain's lock+pop returns None twice? First time: drain sees empty, releases lock, calls `notified().await`. Second iteration starts: takes lock, pops (empty), releases lock, `notified().await`. Between these two iterations, the read loop pushes and notifies. The first notify is consumed. The drain is in the second `notified().await`. The notify permits in tokio's Notify are FIFO; the first `notified()` call gets the first permit. The second `notified()` call gets the second permit (if any). 

Actually, let me re-read the v2 spec:
```
corr = { lock inbox; pop_front queue or None }
```

This is one iteration of the loop. The loop is:
```
loop:
  corr = { lock inbox; pop_front queue or None }        // if None -> wait notify (or Closing->drain-then-exit)
  { lock inbox; slots[corr].state = Claimed{cancelled:false} }
  ...
```

So if `pop_front` returns None, the drain waits on notify. After being notified, the loop iterates: pop_front again. If queue is non-empty, return Some(corr). Otherwise None, wait again. Standard.

Now: is there a lost-wakeup between `pop_front returns None` (releases lock) and `notified().await`? If read loop:
- T0: read loop acquires lock
- T1: read loop pushes corr
- T2: read loop releases lock  
- T3: read loop calls notify_one
- T4: drain acquires lock
- T5: drain pop_front returns Some(corr) (or None, if pushed but not yet visible — but it IS visible since read loop released lock)
- T6: drain releases lock
- T7: drain processes corr

OK, no lost wakeup if drain is in lock-acquisition mode. The issue arises if drain is in `notified().await` mode:
- T0: drain in notified().await (queue was empty)
- T1: read loop pushes, calls notify_one
- T2: drain wakes, acquires lock, pops, processes

If the drain is in `notified().await` and the read loop pushes, the drain wakes. ✓

Now: can read loop push and drain miss the wakeup? Only if the drain is in a state where it can't be woken. Tokio's Notify stores a permit if no waiter, otherwise directly wakes a waiter. So no.

**However**, the v2 doc says:
```
corr = { lock inbox; pop_front queue or None }        // if None -> wait notify (or Closing->drain-then-exit)
```

The wait notify happens after the lock is released. But where exactly? The pseudocode says "if None -> wait notify" but doesn't specify the ordering. If `wait notify` happens BEFORE the lock is released, that's a deadlock. If AFTER, it's the standard pattern. The pseudocode is ambiguous.

A subtle issue: if the drain is waiting on notify, and the read loop pushes and notifies, the drain wakes. But if the read loop pushes and the drain's `notified().await` is called LATER, the permit is stored. Drain consumes the permit immediately on entering `notified().await`. ✓

OK, Notify usage is plausible. Let me also think about the inbox lock holding during `flow.acquire().await`. The v2 says the lock is NOT held during the await. So the inbox lock is free for the read loop to enqueue and CANCEL to inspect. Good.

Now let me look at the v2 Q5' (snapshot publish granularity):

[assistant] 2026-07-15T08:19:09Z
Now let me check what test exists in the shipped code for "stale-Bound observable":

[assistant] 2026-07-15T08:19:13Z

[assistant] 2026-07-15T08:19:15Z
Confirmed: today's behavior for a route that's been GOODBYE'd is "unknown_channel" error. The v2 doc claims the `closed: AtomicBool` recheck preserves this.

Now let me see what happens to the shipped  (`DataRoute::Client(DataRouteState::Absent)`) when a data frame comes in for a route that was released:

Looking at `route_for_connection` (router.rs:200-361), `DataRoute::Client(DataRouteState::Absent)` at line 344 falls through to line 347: `if let Some(backend) = self.backends.get(&channel)` — only EchoBackend for now. Otherwise at line 350: synthesize `Error{code:"unknown_channel"}`. So today's post-release observable is `unknown_channel`. v2's `closed` recheck must preserve this.

If v2's snapshot.load() yields `Bound(route)` with `route.closed=true`, the consumer checks `closed` and treats as `unknown_channel` (per v2 . ✓

But what if the `closed` recheck happens after the route has been removed from the snapshot entirely? Then the consumer gets `Absent` and synthesizes `unknown_channel` via the existing path. ✓

What if v2 introduces a new error code? No, v2  I8 says "Wire: no new frame types/header/bump. New CODES only: route_backpressure, control_backpressure (+ reuse cancelled, module_reloading, backend_error). Consumed by merge-0." So v2 does NOT introduce a new error code for stale-Bound. Good. The "closed" recheck must map to "treat as unknown_channel", which means using the existing RouterError::UnknownChannel. The doc says "treats closed-but-still-visible as `unknown_channel` — restoring today's post-release observable exactly". ✓ (assuming the implementation does this)

Now let me look at B5's insert-order claim more carefully. The v2 doc says:

```
lock inbox:
  take Slot; if state==Claimed{cancelled:true} OR admission==Closed:
      unlock; flow.release(); synthesize Error{cancelled} if route-alive; drop; continue   // rollback (B2)
  else: state=Delivered; outstanding+=1                 // INSERT-before-send (B5): corr is now "delivered" pre-send
unlock
match module_sink.send(frame).await:
```

So the order is:
1. flow.acquire() — increments in_flight, NO RAII
2. Lock inbox, set state=Delivered, increment outstanding
3. Unlock
4. module_sink.send(frame).await

Now: between step 2 (state=Delivered) and step 4 (send completes), can the module terminal arrive? The module can't see the frame until step 4 completes. So the module's terminal can't arrive before step 4. ✓

Can the module's terminal arrive between step 4 (send Ok) and the next request? The module sees the frame, processes it, emits terminal. The terminal goes to `router.rs:281-309` (module→client path). That path does `route.client_sink.try_send(frame)`. It does NOT do `slots.remove(corr)` — that gate is in v2.

Wait, the v2 says:
```
on terminal frame (Response|Error|StreamEnd) for corr, module->client path (router.rs:281-309):
  lock inbox:
    if slots.remove(corr) was Delivered: outstanding-=1; release=true else release=false
  unlock
  forward terminal to client (unchanged)
  if release: flow.release()
```

But the shipped `route_for_connection` at router.rs:281-309 does NOT know about the route's dispatcher. It's calling `route.client_sink.try_send(frame)` and `route.flow.release()`. The v2 mechanism requires the module→client path to find the dispatcher and lock its inbox.

**This is a non-trivial change to the shipped code.** The v2 doc describes the mechanism in pseudocode but doesn't show the wire-up. How does the module→client path find the route's RouteDispatcher? It's not in the shipped RouteBinding. The v2 doc doesn't add it to RouteBinding either.

Let me check the v2 doc to see if it adds dispatcher reference to RouteBinding:

[assistant] 2026-07-15T08:19:31Z
Confirmed: v2 only adds `closed: AtomicBool` to `RouteBinding`, NOT a reference to the dispatcher. So how does the module→client path at `router.rs:281-309` find the dispatcher's `slots` to do the `slots.remove(corr)` exactly-once gate?

**This is a design gap.** The v2 mechanism as written is not implementable as-is.

Possible fixes the v2 doc would need:
1. Add `Arc<RouteDispatcher>` to `RouteBinding`
2. Add a side map keyed by (connection_id, channel, corr) — but that's O(1) lookup, needs update on every release
3. Maintain a per-connection dispatcher map in ForwardingTable

The v2 doc glosses over this. The mechanism as written is unworkable.

Let me now verify the v2's B5 claim with my earlier analysis — the rollback in the drain task. The v2 says:
```
match flow.acquire().await:
    Err(ChannelFlowClosed) => handle_closed(corr); continue
    Ok(()) => {}
```

If `Ok(())`, the credit is held (well, in_flight incremented, permit forgotten). The next step:
```
lock inbox:
  take Slot; if state==Claimed{cancelled:true} OR admission==Closed:
      unlock; flow.release(); synthesize Error{cancelled} if route-alive; drop; continue   // rollback (B2)
  else: state=Delivered; outstanding+=1
unlock
```

If state is Claimed{cancelled:true}, release the credit. If state is Claimed{cancelled:false} and admission is Closed, release. Otherwise mark Delivered and proceed.

But what if state is `Queued` here? The drain already did `slots[corr].state = Claimed{cancelled:false}` in the previous step. So it should be Claimed, not Queued. ✓ (assuming the two-lock-dance issue I found earlier is fixed)

**Note**: the v2 mechanism has the `acquire-credit-then-rollback` window. If `state==Claimed{cancelled:true}`, it calls `flow.release()`. The shipped `flow.release()` decrements in_flight and adds a permit. Good.

But: the rollback path in the drain task **races with the module→client terminal path**. If the module→client terminal arrives for a corr in `outstanding` (Delivered state) AND the drain task is about to release+drop on rollback, both paths might fire. Let me think about this.

Actually no. The drain task only rolls back if state is Claimed{cancelled:true}. The terminal path only fires if state is Delivered (because the module can't see the frame before send). So no overlap.

But: what if state is `Claimed{cancelled:true}` (so drain rolls back) AND the module had already received the frame somehow? That's impossible: the drain hasn't called `module_sink.send` yet. So the module has NOT seen the frame. So no terminal can arrive.

**But wait** — what if state is `Claimed{cancelled:false}` and admission is `Closed`? The drain releases the credit and drops. This is the "we never delivered" case. The module has not seen the frame. Good.

Now, here's a subtle issue: the v2 doc says "if state==Claimed{cancelled:true} OR admission==Closed". But what if the state is `Claimed{cancelled:false}` and admission is `Open`? It proceeds to mark Delivered. But what if a CANCEL arrives between the previous step (set state=Claimed{cancelled:false}) and this step? That would have set `cancelled=true` already. So state would be `Claimed{cancelled:true}` here. ✓

What if the CANCEL arrives between this step and the next (module_sink.send)? The state is now Delivered. The CANCEL arrives, sees Delivered, forwards to module_sink. Drain's send completes. Module sees Request then CANCEL. Module's handle_cancel no-ops (or sends cancel terminal). Module's terminal arrives on module→client path. Slot is removed, credit released.

But what if the module doesn't honor CANCEL and the Request completes normally? Then a normal Response arrives, slots.remove(corr), credit released. The CANCEL was a no-op (the module didn't cancel). This is correct behavior per the v2 spec — "Delivered | None => unlock; forward CANCEL to module_sink (module has it or will; its terminal releases)".

OK, the v2 design is mostly sound, modulo the implementation details.

Let me now look at the B4 (drain error arms) more carefully:

The v2  says:
```
handle_closed(corr):
  lock inbox: take slot; kind = self.teardown
  match kind:
    Reloading  => synthesize Error{module_reloading} to client for corr
    Goodbye    => drop silently
    ConnClose  => drop silently
    None       => synthesize Error{backend_error} to client for corr
```

The shipped behavior at `router.rs:465-485`:
```
if let Err(err) = route.flow.acquire().await {
    if self.forwarding.endpoint_is_draining(route.module_endpoint)... {
        return Err(RouterError::route_error_with_epoch(channel, ..., "module_reloading", ...));
    }
    return Err(RouterError::backend_with_epoch(channel, ..., format!("{err}...")));
}
```

So shipped produces:
- module_reloading if endpoint is draining
- backend_error otherwise (any other ChannelFlowClosed)

The v2 maps:
- Reloading → module_reloading ✓
- Goodbye → silent drop — DIFFERENT from shipped (shipped produces backend_error)
- ConnClose → silent drop — DIFFERENT from shipped (shipped produces backend_error)
- None → backend_error ✓

**The v2 introduces different observable behavior for "route GOODBYE with blocked acquire" and "connection close with blocked acquire".**

Shipped: GOODBYE → flow.close() (release_client_route_locked:1424) → all in-flight acquires wake with ChannelFlowClosed. The dispatched handler at router.rs:465-485 does NOT check why the flow was closed; it just checks if the endpoint is draining. If endpoint is not draining, it returns `backend_error`.

Wait, let me re-read. The endpoint check is on `module_endpoint`, which is the module this route was bound to. After a client GOODBYE on a route, the route is released (release_client_route_locked) and the module_endpoint is still in the module map (unless module was also reloading). So `endpoint_is_draining` returns false. Result: `backend_error`.

V2 says: GOODBYE → silent drop. **Different observable.** The shipped test would need to be updated, but the doc claims "T8 Existing suites green UNMODIFIED".

But wait — the v2's "Goodbye" silent drop would silently drop the blocked acquire. The test `blocked_flow_control_acquire_wakes_when_module_tears_down` involves `module.stop()` (test:3869) which forces the module to die, NOT a GOODBYE. So the test would see "module death" which maps to `None` → `backend_error`. The test stays green for that test.

But the test's expectation is: when the module is torn down, the blocked acquire gets `backend_error`. In the v2 mechanism, this works as long as the teardown kind is `None`. But the actual teardown reason is... what? The test calls `module.stop()` which kills the module's process. The daemon sees the connection close. Does the v2 teardown get set to `ConnClose` or `None`?

Looking at v2  teardown:
```
1. lock inbox: admission = Closing
2. flow.close() with teardown=<reason>
3. cancel_token.cancel()
4. bounded-join the drain task
5. lock inbox: admission = Closed
6. epoch-fenced release
```

The teardown reason is set in step 2. For a client→module route, a module death (connection close on the module side) would NOT be handled by this teardown. The shipped code at `cleanup_connection` removes the module endpoint and closes routes. The v2 mechanism has to deal with this case.

But the v2 doc says: "module death mid-block → backend_error (preserves the shipped blocked_flow_control_acquire test)". So the v2 mechanism must distinguish:
- Module reload (drain): Reloading → module_reloading
- Module GOODBYE: Goodbye → silent
- Module connection close: ConnClose → silent
- Other (sem closed without a teardown reason): None → backend_error

But in the test, `module.stop()` causes a connection close. The v2 teardown would be `ConnClose` → silent drop. That would NOT match the test's `backend_error` expectation.

**This is a v2-introduced regression.** The test expects `backend_error`, v2 says `ConnClose` → silent, but the test's setup is `module.stop()` which is connection close.

Wait, let me re-read v2 
```
None       => // sem closed without a teardown reason: treat as backend_error (defensive)
              synthesize Error{backend_error} to client for corr
```

The "without a teardown reason" case is when the sem was closed but no teardown was set. For a module stop (connection close on module side), the v2 mechanism must set teardown. If the teardown path on the route runs (cleanup_connection), it sets teardown=ConnClose (presumably). Then the drain reads ConnClose and silently drops. **This breaks the test.**

Actually, I need to think more carefully. The test:
- Writes inflight_corr Request (corr 932) on route
- Writes blocked_corr Request (corr 933) on route
- Module dies (stop)
- Reads frame on client within 2s
- Asserts: either a GOODBYE for the route, OR an Error{backend_error} for corr 933

So the test allows either GOODBYE OR backend_error. If the v2 mechanism drops silently on ConnClose, the client sees a GOODBYE eventually (from the connection close), but the blocked corr 933 might never see a frame.

Wait, the test reads ONE frame within 2 seconds. After GOODBYE, no more frames. So the test could fail with a hang if the v2 mechanism drops the blocked corr silently.

Actually, GOODBYE arrives at the client (from the route GOODBYE). The test accepts that:
```rust
if frame.header.ty == FrameType::Goodbye {
    assert_eq!(frame.header.channel, ack.route_channel);
}
```

So the test just reads ONE frame and accepts GOODBYE. It does NOT require an Error frame for corr 933 before the GOODBYE. So the v2 mechanism's silent drop is OK for THIS test.

But the doc claims "preserves shipped Error-frame recovery" and the test specifically allows EITHER outcome. So the v2 mechanism's silent drop would still keep this test green (the client gets GOODBYE eventually, which the test accepts).

But: a "synthesize Error{backend_error}" for the blocked corr would be a more informative signal. The test allows either. So v2 is OK on this specific test.

However, v2 introduces different observable for module-death (was: backend_error, now: silent on ConnClose). This is a wire-observable change for clients that distinguish backend_error from absence-of-terminal. Per I7 "module→client wire behavior unchanged", this is a violation.

Actually wait — I7 says "module→client wire behavior unchanged". Module→client is the direction from module back to client. The error for a blocked acquire is on the client→module direction, going to the client. So this is a CLIENT→module error, not module→client. Different.

But still, the test was specifically designed to assert a `backend_error` on this path. The v2 mechanism changes that observable to silent.

OK, let me now check the `closed: AtomicBool` recheck claim more carefully. v2 claims this restores today's `unknown_channel` observable. Let me look at what happens if the lookup_data_route returns Bound (route) with route.closed=true:

In v2, the data-plane consumer checks `closed` and synthesizes `unknown_channel`. The shipped `RouterError::UnknownChannel` (router.rs:642) is the error to use. The error code is `"unknown_channel"`. The test at forwarding.rs:2285-2292 asserts this. So v2's recheck must use RouterError::UnknownChannel. ✓

But wait — what about the `closed` set vs the snapshot published? v2 says "closed: AtomicBool, set true under the write lock at release BEFORE the snapshot that removes it is published." So:
1. Lock write lock
2. Set route.closed.store(true, ...)
3. Remove route from maps
4. Publish new snapshot (without the route)
5. Release write lock

After step 4, any reader that loaded the snapshot BEFORE step 4 sees Bound with closed=false. Reader checks closed, sees false, proceeds. This is the "stale-Bound" window.

Reader that loaded snapshot AFTER step 4 sees Absent. Synthesizes unknown_channel via existing path.

Reader that loaded snapshot BEFORE step 4 AND checks closed AFTER step 4: sees closed=true, synthesizes unknown_channel. ✓

Reader that loaded snapshot BEFORE step 4 AND checks closed BEFORE step 4: sees closed=false, proceeds. → routes into a released module.

**This is the race!** The check has to happen AFTER the route is closed. The v2 doc says "Every data-plane consumer of a `Bound` route ... checks `closed` after loading". The "after loading" is fine, but the consumer might process the frame BEFORE the writer sees closed=true. Specifically:

- T0: Reader loads snapshot with Bound(route), closed=false
- T1: Writer sets closed=true, removes from snapshot
- T2: Reader checks closed, sees false (because the load was at T0)
- T3: Reader proceeds with route

This is a TOCTOU. The v2's "closed flag" must be re-checked AFTER the snapshot load, but the load and the check are at different times. If the reader holds the Arc<RouteBinding> between load and check, the check could see stale closed=false.

Actually, with `Arc<RouteBinding>` and `closed: AtomicBool`, the reader can do `route.closed.load(Ordering::Acquire)` AFTER acquiring the Arc. If the writer sets `closed=true` AFTER the reader's load, the reader's load sees true. So the reader's check is correct as long as it's after the load.

But what if the writer's set+publish happens between the reader's load and check? Then the reader's check sees true. ✓

What if the writer's set+publish happens AFTER the reader's check? Then the reader sees false, proceeds, and the writer subsequently closes the route. The reader's frame is processed by the (now closed) route. The drain's flow.acquire might fail (ChannelFlowClosed) and disambiguate via teardown. Or the module_sink is dead and try_send fails.

In the data-plane consumer (router.rs:281-309 module→client, or dispatcher push), the reader has an Arc<RouteBinding>. The reader calls `route.client_sink.try_send(frame)` (module→client) or `route.module_sink.send(frame)` (dispatcher push).

For module→client, `try_send` is non-blocking. If the sink is closed, the channel is full or closed, the send fails. The handler escalates. ✓ (kind of)

For dispatcher push, the dispatcher does `slots.insert(corr, Slot{frame, Queued})`. The dispatcher doesn't reference the route's `module_sink` directly. The drain task uses its own `module_sink: FrameSink` (per the v2 struct:30). So the dispatcher's flow is its own, not the binding's.

Hmm, actually let me re-read the v2 struct:
```
struct RouteDispatcher {
    inbox: Mutex<RouteInbox>,
    notify: Notify,
    flow: Arc<ChannelFlow>,
    module_sink: FrameSink,
    // identity for synthesized frames (client channel/epoch/ver) + module channel/epoch for forwarding
}
```

The dispatcher has its own `flow` and `module_sink`. Where do these come from? They must be cloned from the binding's `flow` and `module_sink` at bind time. So the dispatcher's `flow` IS the same `Arc<ChannelFlow>` as the binding's. The dispatcher's `module_sink` IS the same `FrameSink` as the binding's `module_sink` (via Arc clone or mpsc sender clone).

The binding has `closed: AtomicBool`. The dispatcher doesn't. The dispatcher's `flow` is closed when the binding is released (via the v2 teardown step 2 or the shipped release path). When the dispatcher reads `flow.acquire().await` and gets `ChannelFlowClosed`, it checks teardown.

If the dispatcher's flow was closed AFTER the dispatcher loaded a frame (i.e., the read-loop enqueued a frame, then the route was released), the drain task will:
- Pop the frame from the queue
- Try flow.acquire() → ChannelFlowClosed
- handle_closed: read teardown

The teardown kind is set by the mutator that closed the flow. If the route was GOODBYE'd, teardown=Goodbye. If the route's module was reloading, teardown=Reloading. If the connection closed, teardown=ConnClose. Otherwise, teardown=None → backend_error.

But for the dispatcher push, the enqueue happens BEFORE the teardown check. The read loop does `slots.insert` and `queue.push_back` under the inbox lock. The admission check (`admission != Open`) is in the read-loop enqueue path. So if admission is Closing or Closed, the enqueue is rejected.

The race: between `admission != Open` check and the actual push, admission could change. But the check and push are under the same lock, so no race. ✓

**OK, the v2 mechanism for the dispatcher side is plausible.** But the module→client side has the unworkable mechanism: the v2 says "the module→client terminal path removes the corr from the route's `slots`/`outstanding` and releases credit ONLY if the corr was still Delivered". This path is at `router.rs:281-309` and operates on `Arc<RouteBinding>`, not on the route's dispatcher. Without a `dispatcher` reference on the binding, this is unworkable.

Let me also verify: v2's snapshot claim of "the snapshot is rebuilt-and-published INSIDE the existing write-lock critical section for every mutation". This means a fresh `Arc<Snapshot>` is allocated and `ArcSwap::store` is called under the write lock. The read path does `ArcSwap::load()`. The issue: `ArcSwap::store` doesn't require a write lock. But v2 says it does, which is fine.

But: the read path uses `ArcSwap::load()`. If a reader does `load()` and a writer is concurrently doing `store()`, the reader gets the old or new snapshot. The reader's lookup runs on whichever snapshot it loaded. ✓

The concern is: the reader's lookup returns `Bound(route)` with a `closed` flag. The reader holds the `Arc<RouteBinding>`. The reader's check on `closed` could see false (stale). The reader proceeds. The frame is sent (via dispatcher push or module→client).

For the module→client case at `router.rs:281-309`, the frame is sent via `route.client_sink.try_send(frame)`. If the client connection is closing or the route is stale, the send fails and escalates. The credit is then released via `route.flow.release()`. So credit is released even if the route is stale. This is the CURRENT behavior.

In v2, the module→client path also does the `slots.remove(corr)` gate. But the `slots` belong to the dispatcher's `inbox`, not the binding. So the binding must have a reference to the dispatcher.

**This is the unworkable gap.**

Let me also check the v2 claim about "The snapshot is rebuilt-and-published INSIDE the existing write-lock critical section for every mutation (bind/release/register/cleanup/drain), so publication is serialized with the canonical mutation (no publish-after-unlock window)."

The shipped `release_client_route_locked` (forwarding.rs:1409-1438) modifies `inner.client_to_module`, `inner.module_to_client`, `inner.status` under the write lock. It does `route.flow.close()`. In v2, this would also need to:
1. Set `route.closed = true`
2. Set `teardown = Goodbye` (or whatever reason)
3. Publish a new snapshot

But `route` is `Arc<RouteBinding>`. Setting `route.closed.store(true, ...)` from inside the write lock is fine.

But: the `teardown` field is on the `RouteDispatcher`, not the `RouteBinding`. The release path needs to find the dispatcher. Without a `dispatcher` reference on the binding, the release path can't set the teardown.

This is the same unworkable gap as before.

Let me also check: the v2 says the write lock is held during the publish. The shipped code holds the write lock for the mutation but NOT for the publish (no publish exists today). v2 introduces a new publish that happens UNDER the write lock. The write lock is the global forwarding table lock. v2 says no publish-after-unlock. ✓

But: the write lock is the same lock the read path uses. Today, the read path uses `read_inner()` (RwLock read). v2's read path uses `ArcSwap::load()` (no lock). So the read path doesn't contend with the write lock. The write lock is only contended with itself (other write paths). ✓

But: the dispatcher's inbox mutex is a NEW lock. The dispatcher lock is per-route, not global. No contention with the global forwarding write lock. The dispatcher lock is held only briefly (O(1) ops, no await). The drain task is the only long-running task that holds... wait, the drain task does NOT hold the dispatcher lock during await. It releases the lock, then awaits, then re-locks. ✓

But: the dispatcher's inbox mutex is acquired by:
- Read loop (enqueue, CANCEL inspect)
- Drain task (pop, claim, decide)

The read loop and the drain task contend on the same mutex. The read loop holds it briefly for enqueue. The drain task holds it briefly for pop+claim. Brief holds, no await, no deadlock. ✓

But: if a CANCEL arrives while the drain is in `flow.acquire().await`, the read loop takes the lock, mutates, releases. The drain doesn't hold the lock. ✓

OK, the v2 dispatcher primitive is plausible. The main gap is the missing `dispatcher` reference on the binding for the module→client terminal path's `slots.remove(corr)` gate.

Now let me look at the supervisor/connection close / cleanup_connection path to see how route cleanup interacts with the v2 dispatcher:

[assistant] 2026-07-15T08:21:03Z
Let me also check the v1 B9 claim about the teardown hang — what about the existing teardown 5-step mechanism in v2:

The v2 teardown step 4 is "bounded-join the drain task". The v2 doc says the JoinHandle is owned by the teardown path (connection/forwarding table), NOT by the binding. This avoids the binding→handle→task→binding strong cycle.

But: if the teardown is initiated by the connection close (server.rs:241-267 area), the connection has a `close_receiver`. The teardown needs to:
1. Find all live dispatchers for routes on this connection
2. For each: set admission=Closing, close flow with teardown, cancel token, bounded-join

The v2 doc doesn't specify the dispatcher map. The ForwardingTable must have a way to enumerate live dispatchers per connection.

Today's code: `client_to_module` and `module_to_client` maps are the canonical state. The v2 dispatcher is a per-route entity. Where are they stored?

The v2 doc doesn't specify this. It just says "Per live client-side route binding, one `RouteDispatcher`". The binding doesn't have a dispatcher reference (per the v2 doc's struct). The ForwardingTable doesn't have a dispatcher map (per the v2 doc's struct).

**This is another design gap.** The v2 mechanism needs a way to:
1. Find all live dispatchers when a connection closes
2. Trigger teardown for each
3. Join each drain task
4. Then do the epoch-fenced release

Without a dispatcher map, this is unworkable.

OK, so the v2 mechanism has at least two major design gaps:
1. The module→client terminal path (router.rs:281-309) needs to find the route's `slots` to do the exactly-once gate. The dispatcher reference is missing from RouteBinding.
2. The connection close path needs to find all live dispatchers for routes on the connection. The dispatcher map is missing.

Let me also look at the v2's claim about I7 (module→client wire behavior unchanged):

V2 section 3 says:
```
on terminal frame (Response|Error|StreamEnd) for corr, module->client path (router.rs:281-309):
  lock inbox:
    if slots.remove(corr) was Delivered: outstanding-=1; release=true else release=false
  unlock
  forward terminal to client (unchanged)
  if release: flow.release()
```

The wire to the client is unchanged (forward terminal to client unchanged). The credit release is gated. So the wire behavior is unchanged but the credit accounting is fixed. ✓ I7's "wire behavior unchanged" is correct, but the mechanism (slots.remove) is on the wrong side.

Now let me look at the v1 finding #4 about the rollback: "panic between acquire and send releases the credit" — v2 claims an `AcquiredCredit` RAII guard. The shipped `flow.acquire` does `permit.forget()` which BREAKS RAII. The v2 mechanism has to add a NEW RAII guard on top of the existing forget'd permit. The v2 guard calls `flow.release()` on drop unless `commit()`ed. This is plausible.

But: the v2 guard's `commit()` must be called EXACTLY at the point where the credit is correctly transferred to the module (i.e., after the module emits a terminal, the credit is released by the terminal path, not the drain's release guard). The drain's release guard is the FALLBACK for send-failure and cancel-rollback.

Let me trace the v2:
1. flow.acquire() Ok — credit held by `AcquiredCredit` guard
2. Lock inbox, decide, unlock
3. module_sink.send().await:
   - Ok: terminal will release on module→client path → guard.commit() (disarm release)
   - Err: guard NOT commit()ed, guard's drop releases credit → `outstanding-=1; remove slot`

But wait, after Ok, when does the guard disarm? Looking at v2:
```
match module_sink.send(frame).await:
    Ok(()) => {}
    Err(_) => { lock inbox: outstanding-=1; remove slot; unlock;
                flow.release();
                synthesize Error{backend_error} to client if route-alive }
```

There's no explicit `guard.commit()`. The guard's drop would release the credit on both Ok and Err paths. But on Ok, the module→client terminal path will also release. **That's a double release.**

Wait, let me re-read. The v2  says "Credit release stays on the module→client terminal path (unchanged direction), now gated by an exactly-once check". So the Ok path of module_sink.send does NOT release — the module's terminal does. So the guard's drop on the Ok path must NOT release.

The v2 doc says "scope guard (AcquiredCredit that calls flow.release() on drop unless commit()ed)". So after `module_sink.send().await` returns Ok, the guard's commit() must be called.

But the v2 pseudocode doesn't show `commit()`. It's an implementation detail. The intent is clear: after Ok, the credit is the module's responsibility (will be released on terminal). After Err, the credit is released immediately.

But here's the issue: between `flow.acquire Ok` and `module_sink.send Ok`, the guard is held. If the drain task panics here, the guard's drop releases the credit. ✓ (B4's panic arm)

If the module→client terminal path sees the corr and tries to release (with the slots.remove gate), but the corr is not in slots because the drain's send hasn't completed yet... wait, the corr is in slots (state=Delivered) BEFORE the send completes. So the module→client terminal can find the slot and release.

So the timeline:
- Drain: set state=Delivered, send to module
- Module receives, processes, emits terminal
- Module→client terminal path: slots.remove(corr) → was Delivered, so release=true, release credit
- Drain's send Ok returns; guard.commit() is called (no release)

If the module→client terminal arrives BEFORE the drain's send Ok, there's a race. But that's impossible: the module can't emit a terminal for a frame it hasn't seen. So the module→client terminal can't arrive first.

OK so the guard's commit() is called after `module_sink.send Ok`, and the module→client terminal releases the credit. No double release.

**But the v2 pseudocode doesn't show `commit()`.** This is an implementation gap. The pseudocode glosses over it. If the implementer forgets to `commit()` after Ok, the guard's drop releases the credit, and the module→client terminal also releases (or finds slots.remove returns false because the slot was removed... wait, no, the slot is still there in the Ok case). 

Wait, the v2 Ok arm of `module_sink.send` doesn't remove the slot. The module→client terminal path does. So after Ok, the slot is in slots, state=Delivered, outstanding includes it. The module→client terminal removes it. The drain's guard.drop releases the credit (no commit). The module→client terminal's release is also called. **That's a double release.**

The v2 RAII guard's commit() is critical here. The pseudocode doesn't show it. This is a hazard.

Let me also check: the v2 RAII guard interacts with the rollback path. If the drain rolls back (state=Claimed{cancelled:true} or admission==Closed), the v2 says:
```
if state==Claimed{cancelled:true} OR admission==Closed:
    unlock; flow.release(); synthesize Error{cancelled} if route-alive; drop; continue
```

It calls `flow.release()` directly. The guard's drop would also call `flow.release()`. **That's a double release** (well, the shipped release's CAS guard would ignore the second).

The shipped `flow.release()` has a CAS guard that ignores over-release when in_flight==0. So the second release would be a no-op (log a warning). Not a credit leak, just a log.

But: the second release might fire BEFORE the first release's CAS commits. In that case, in_flight is still 1, and the second release decrements to 0 and adds a permit. **That's a real double release.** The CAS loop would not save it.

So the v2 mechanism has a subtle issue: if both the explicit `flow.release()` and the guard's drop fire, the second might double-release. The implementation must ensure only one fires.

**This is a v2-introduced hazard.** The pseudocode doesn't address it. The implementation must be careful to disarm the guard before calling `flow.release()` explicitly.

Let me check the v1 B2 (limbo) — the v2 mechanism should close it. Let me re-trace:

V2 says: 
- Read loop: state=Queued
- Read loop CANCEL: state=Queued → remove from queue+slots, synthesize cancelled
- Read loop CANCEL: state=Claimed{cancelled} → set cancelled=true
- Read loop CANCEL: state=Delivered | None → forward CANCEL to module

- Drain: pop, state=Claimed{cancelled:false}, flow.acquire().await
- Drain: lock, decide (rollback if cancelled or closed, else Delivered), unlock
- Drain: module_sink.send

The limbo: between pop and state=Claimed, the state is still Queued (slot exists, not in queue). A CANCEL arriving here would find Queued and remove the slot. Drain's next step is `slots[corr].state = Claimed{...}` which would fail.

**This is the v2-introduced defect I found earlier.** The two-lock-dance in the drain task reintroduces a window.

Fix: combine pop + state-Claimed into one lock acquisition. The v2 doc doesn't do this. The pseudocode says:
```
corr = { lock inbox; pop_front queue or None }
{ lock inbox; slots[corr].state = Claimed{cancelled:false} }
```

This is two separate lock acquisitions. The race window is between them.

Actually, wait. Let me re-read the v2:
```
loop:
  corr = { lock inbox; pop_front queue or None }        // if None -> wait notify (or Closing->drain-then-exit)
  { lock inbox; slots[corr].state = Claimed{cancelled:false} }
```

Maybe the `{...}` is meant to be the same lock acquisition? In Rust pseudocode, `{}` is a block. Two blocks = two lock acquisitions. But the v2 doc's prose says "every op holds the lock O(1) and NEVER across an await". The pop and the state-Claimed are two ops, each holding the lock briefly. The "between" them is a release.

**This is a design defect.** The v2 should be:
```
corr = { lock inbox; pop_front queue or None }
if corr.is_none(): wait notify; continue
{ lock inbox; 
   match slots.get_mut(corr) {
     Some(slot) => slot.state = Claimed{cancelled:false},
     None => { /* CANCEL raced; drain synthesizes cancelled itself */ }
   }
}
```

Or:
```
corr = { lock inbox; 
  let corr = queue.pop_front();
  if let Some(c) = corr { slots.get_mut(c).map(|s| s.state = Claimed{cancelled:false}); }
  corr
}
```

The v2 doc's pseudocode is ambiguous on whether the two `lock inbox` are one or two acquisitions. The prose says "every op holds the lock O(1) and NEVER across an await", which is consistent with two separate acquisitions (each O(1), neither holding across an await). But the prose also says "the read loop and the drain task both make their cancel/deliver decision under the SAME lock" — this is true for the read loop's CANCEL and the drain's claim, but the drain's own pop-then-claim is a window.

This is a subtle but real defect. The v2 doc glosses over it.

Let me check the v2 B4 (drain error arms) — the v2 mechanism is:
- flow.acquire() Err ChannelFlowClosed → handle_closed
- module_sink.send() Err → backend_error + release

But what about the panic case? The v2 says RAII guard releases the credit. But the guard's drop also fires on the rollback path (which explicitly calls `flow.release()`). As I noted, this is a double-release hazard unless the guard is disarmed.

Let me also check the v2 Q1' (merge-0 hard-gate vs blocking-admission interim). The v2 doc says:
- Lean: hard-gate (cleaner; consumers are our own and fast to bump)
- interim only if a consumer bump stalls

This is a reasonable lean. Hard-gating is safer. The doc acknowledges the risk (interim if stalls). OK.

Let me check the v2 Q2' (teardown bounded-join timeout). The doc says 2s and "abort-after-timeout can drop an in-flight terminal (acceptable: connection is closing)". The 2s is a guess. The "acceptable" is contingent on the connection actually closing. If the connection is NOT closing (e.g., partial teardown), dropping an in-flight terminal is a real loss. But the doc says "connection is closing", so the scope is correct.

But: 2s is a specific number with no justification. The shipped `CLOSE_DRAIN_GRACE` is:

[assistant] 2026-07-15T08:21:54Z

[assistant] 2026-07-15T08:23:29Z

[assistant] 2026-07-15T08:23:32Z
Confirmed: TS `SubcCallError.kind` is "not_sent" | "outcome_unknown" | "terminal". A `route_backpressure` Error frame would be a "terminal" kind (Error from `errorFromFrame`, which returns a plain `SubcError`, wrapped as "terminal" via `terminalCallError`). Managed call() does NOT retry "terminal". So the v1 B1 finding stands.

Now let me check the existing test in forwarding.rs that v2 claims is preserved — the bind barrier:

[assistant] 2026-07-15T08:23:37Z

[assistant] 2026-07-15T08:23:39Z
This is a unit test in router.rs (not a forwarding.rs integration test). The v2 doc says `router.rs:1078-1102`. The test uses `route_for_connection` directly (not a real connection). It commits the route via `complete_pending_relay` and then sends a Request from the module side. The test asserts the client sees route.open first, then the data on the new route.

The test does NOT exercise the channel-0 FIFO offload. It's a unit test of the data-plane router. So whether the v2 channel-0 offload breaks this test depends on whether the offload changes the data-plane router's behavior. The data-plane router (router.rs:200-361) is unchanged in v2 (the dispatcher is on the read-loop side, not the data-plane router). So this test should still pass.

But the v2 doc claims the test "MUST stay green with control offload active". The offload is on the read-loop side. The test doesn't go through the read loop. So the test is N/A for the offload. The v2 doc's claim is correct in that the test stays green, but the test doesn't actually validate the offload.

The real bind barrier test would be: a real connection, a module that sends route.bind response then immediate reverse request, the client sees the route.open THEN the reverse request data. This is a higher-level integration test. The v2 T14 says "add a module that sends bind-ack then an immediate reverse request under load → reverse frame delivered, not dropped". T14 is the new test. T8 says existing suites green. The shipped test (router.rs:1078-1102) is a unit test that doesn't exercise the offload.

**Verdict on B7**: v2's mechanism (client-side-only offload, module-side stays inline) is plausibly correct. The shipped test stays green (it's a unit test that doesn't exercise the offload). The real bind barrier is preserved by the module-side inline commit-before-next-frame. ✓

But: the v2 doc says "MODULE connections are NOT split — their frames (HELLO, bind acks, catalog.update, data) stay on the single inline read path". This is the key claim. The module's read loop processes all frames inline, in order. The module's bind ack + immediate reverse request are processed in order. The bind commit (forwarding.rs:1472-1564) happens under the write lock and includes `pending.client_permit.send(pending.route_open_frame)` which sends the route.open to the client. So the client's egress receives the route.open BEFORE the next frame is read on the module side. ✓

But wait — the module's next frame is a reverse request (data). The module's read loop processes it after the bind ack. The data plane lookup sees the binding (already published). The data is forwarded to the client's route channel. The client's egress receives it. ✓

So the bind barrier is preserved by the module-side inline processing. ✓

Now let me check the Q5' (snapshot publish granularity):

V2 says "lean whole-table (mutations rare vs lookups)". The current RwLock read provides per-mutation read-after-write. The ArcSwap whole-table would require rebuilding the entire table on every mutation. For a system with many routes, this could be expensive. But mutations are rare vs lookups (data plane is hot). The lean is reasonable. ✓

Now let me also look at the v1 B4 (drain-task panic guard):

V2 says "RAII: the credit between flow.acquire Ok and either Delivered-commit or rollback-release is held by a scope guard (AcquiredCredit that calls flow.release() on drop unless commit()ed), so a panic between acquire and send releases the credit."

The shipped `flow.acquire` does `permit.forget()` which BREAKS the Semaphore's RAII. The v2 mechanism adds a new RAII guard on top. The guard's drop calls `flow.release()`. On panic, the guard's drop fires. The credit is released. ✓

But: between `flow.acquire Ok` and the module_sink.send, the slot is in slots, state=Claimed. If the drain panics, the guard releases the credit, but the slot remains in slots. The next time the route is accessed (e.g., a CANCEL arrives), the read loop sees state=Claimed and sets cancelled=true. The drain is dead, so the cancelled flag is never observed. The slot leaks.

**This is a v2-introduced defect.** The panic guard releases the credit but doesn't clean up the slot. The slot remains in slots, state=Claimed{cancelled:true}, and the slot's frame is held forever (memory leak).

Actually, the v2 doc says: "make the panic guard release the route (abort-guard mirroring the coordinator-actor drop-guard pattern from broca) — a dead drain task with a live binding would recreate the silent wedge this design kills."

But v2 doesn't actually specify the panic guard's behavior beyond "release the credit". The slot cleanup is not mentioned. The "exactly-once release" via slots.remove is on the terminal path, not the panic path. So a panicked drain leaks slots.

The v2 doc's T10 test plan doesn't include a "drain task panics" test. T10 tests CANCEL in each state. T12 tests drain error arms. No panic test. This is a gap.

Now let me look at B6 (O(queue) CANCEL scan):

V2 says CANCEL is O(1) via `slots.get_mut(corr)`. The `slots` is a HashMap. O(1) lookup. ✓ (assuming HashMap is appropriate). No linear scan on the read loop. ✓

But: the v2 Q4' says "`slots` HashMap vs a slab/index — corr-uniqueness assumption: a client reusing an in-flight corr on one route is a protocol violation; enforce (reject duplicate-corr enqueue as a protocol error)". This is OPEN. If a duplicate corr is enqueued, the HashMap `insert` would overwrite the old slot, losing the old frame and the old credit. The v2 doc acknowledges this as an open question. The "lean: enforce at enqueue" is not in the v2 mechanism section.

**This is a v2-introduced gap.** The v2 mechanism doesn't specify corr-uniqueness enforcement. The HashMap insert is a silent overwrite (in Rust, `slots.insert(corr, Slot{...})` replaces the old value). If duplicate corrs are allowed, the old slot is lost, the old frame is dropped (or leaked if the Arc prevents deallocation), and the old credit is leaked (because the terminal path's `slots.remove(corr)` will find the NEW slot, not the old one).

Wait, the old slot is replaced. The new slot has the new frame. The terminal for the new frame removes the new slot. The terminal for the old frame finds no slot (the new one replaced it), so release=false. The old credit is never released. **Permanent credit leak on duplicate-corr enqueue.**

This is a v2-introduced defect that the v2 doc acknowledges as an open question. The mechanism doesn't address it.

Let me also check the v2 Q3' (depth caps). The v2 doc says "keep max(4,2×window)? Byte-based secondary cap per route + per connection to close the frame-count-only memory-DoS the v1 gate flagged". This is OPEN. The v2 mechanism uses `if queue.len()+outstanding >= depth_cap: synthesize Error{route_backpressure}`. No byte cap. The v1 B12 finding (256 GiB per connection memory DoS) is NOT closed by v2.

**This is a v2-introduced gap.** The v2 mechanism doesn't address the byte-based memory budget that v1 B12 flagged.

Now let me look at the v2 mechanism's claim about the AcquiredCredit RAII guard and the outstanding count:

V2 section 2 says:
```
outstanding: usize,              // delivered-not-terminated count (mirrors flow.in_flight; for drain-side assertions)
```

The v2 doc says outstanding MIRRORS flow.in_flight. But the drain task increments outstanding when state=Delivered, and decrements on terminal removal. The flow.in_flight is incremented by flow.acquire() and decremented by flow.release(). 

The two should match: when state=Delivered, flow.in_flight is 1 and outstanding is 1. When the terminal releases, flow.in_flight is 0 and outstanding is 0. But the timing is different: flow.in_flight is decremented by `flow.release()` on the module→client terminal path, AFTER the slot is removed. The outstanding is decremented BEFORE flow.release() (in the same critical section). So the order is: outstanding--, then flow.release(). The in_flight-- happens at flow.release().

But: between the slot removal and the flow.release(), another path might observe outstanding=0 and in_flight=1. This is a brief inconsistency but not a correctness issue (both are per-corr counters, not externally visible).

**However**, the v2 doc says outstanding "mirrors flow.in_flight; for drain-side assertions". The drain uses outstanding to assert "no in-flight when admission check happens". If outstanding != in_flight (briefly), the assertion is wrong. This is a v2-introduced hazard.

Actually, the v2 doc doesn't show any assertion using outstanding. It's just a comment. The drain uses slots to check "is this corr already in flight". Outstanding is just a count. OK.

Now let me look at the v2's claim about the `dispatcher push`:

V2 says the read loop pushes to the dispatcher. The dispatcher's inbox has `slots.insert(corr, Slot{frame, Queued})`. The v2 doesn't show the lookup_data_route that finds the dispatcher. The read loop needs to find the dispatcher for the route. Where is the dispatcher stored?

The v2 doc's `RouteDispatcher` struct doesn't say where it's stored. The `RouteBinding` doesn't have a dispatcher reference. The `ForwardingTable` doesn't have a dispatcher map.

**This is a design gap.** The read loop needs to find the dispatcher to enqueue. Without a map, this is unworkable.

Possible fix: ForwardingTable has a `dispatchers: HashMap<ClientRouteKey, Arc<RouteDispatcher>>` (per v2 merge-2). But the v2 doc doesn't add this.

OK, let me now check the v2's claim about the v1 B3 (queue data structure). The v2 specifies the primitive explicitly: `VecDeque<u64>` + `HashMap<u64, Slot>` behind a Mutex. No mpsc. ✓

V1 B3 (data race on mpsc scan): CLOSED. The v2 specifies `VecDeque + HashMap` behind a per-route Mutex, with O(1) operations. No mpsc. ✓

V1 B6 (O(queue) CANCEL scan DoS): CLOSED. CANCEL is O(1) via `slots.get_mut(corr)`. No linear scan. ✓

Now let me consolidate and produce the verdict. Let me also look at the bin/ck.rs and the protocol::wire for any other v1/v2 relevance:

[assistant] 2026-07-15T08:24:21Z

[assistant] 2026-07-15T08:24:23Z
Let me check if there's an `Arc<RwLock<...>>` vs the use of `RwLock<...>`:

The shipped uses `std::sync::RwLock<ForwardingInner>` inside `ForwardingTable` (line 284: `inner: RwLock<ForwardingInner>`). The v2 wants to replace this with `ArcSwap<Snapshot>`. This is a significant refactor.

Now let me look at the protocol type for Frame and is_terminal_frame:

[assistant] 2026-07-15T08:24:27Z

[assistant] 2026-07-15T08:24:29Z

[assistant] 2026-07-15T08:24:31Z

[assistant] 2026-07-15T08:24:34Z

[assistant] 2026-07-15T08:24:36Z
Confirmed: the fake-aft-stub's `handle_cancel` looks up the in-flight entry by `(channel, epoch, corr)`. If the key is not in `in_flight`, the cancel is a no-op (no terminal emitted). The v1 B2 finding cited this: "module inserts its own in-flight entry only AFTER receiving the Request (fake-aft-stub.rs:339-344)". The v2's CANCEL-handling must ensure the module sees the Request after the cancel-wins case, OR the daemon synthesizes the cancelled terminal itself (which is what v2 does for the Queued case).

For the Delivered case, the cancel is forwarded to the module. The module's handle_cancel looks up the in-flight entry. If the Request was delivered (state=Delivered), the module has the entry. handle_cancel finds it, emits cancelled terminal. ✓

For the Claimed{cancelled:true} case, the drain rolls back, releases the credit, drops. The module never sees the Request. The cancel is a no-op (no terminal from module). The daemon synthesizes the cancelled terminal. ✓

For the Queued case, the read loop removes the slot, synthesizes cancelled terminal. The module never sees the Request. The cancel is a no-op (no terminal from module). ✓

So the v2 mechanism correctly handles the B2 limbo. The v2 mechanism is sound for B2. ✓

But the v2-introduced defects remain:
1. Two-lock-dance in drain task (pop + state-Claimed) is a race window
2. Module→client terminal path (router.rs:281-309) needs dispatcher reference
3. Connection close path needs dispatcher map
4. Panic guard doesn't clean up slots (memory leak)
5. AcquiredCredit RAII guard's commit() is implicit (double-release hazard)
6. Corr-uniqueness not enforced (open Q4', but mechanism section doesn't address)
7. Byte-based memory budget not specified (open Q3', but mechanism section doesn't address)

Now let me verify B10 (snapshot stale-read) one more time. The v2 says `closed: AtomicBool` is set under the write lock at release BEFORE the snapshot is published. Let me trace:

```
1. Lock write lock
2. Set route.closed.store(true, Release)  // or SeqCst
3. Remove route from inner.client_to_module and inner.module_to_client
4. Publish new snapshot (without the route)
5. Unlock write lock
```

Reader on data plane:
```
1. snapshot = ArcSwap.load()  // gets old or new snapshot
2. lookup_data_route(snapshot)  // returns Bound(route) or Absent
3. If Bound, check route.closed.load(Acquire)
4. If closed, treat as unknown_channel
5. If not closed, proceed
```

The race: reader loads snapshot at T0, gets Bound(route) with closed=false. Writer sets closed=true at T1, publishes new snapshot at T2, unlocks at T3. Reader checks closed at T4. If T4 > T1, reader sees true. If T4 < T1, reader sees false.

But the reader's check is AFTER the load. The load is at T0. The check is at T4. If the writer's set is at T1, the reader's check at T4 (after T1) sees true. ✓

But what if the reader's check is BEFORE the load's effects propagate? The ArcSwap::load() returns an Arc<Snapshot>. The snapshot contains the route's Arc<RouteBinding>. The route's `closed: AtomicBool` is on the RouteBinding. The reader's load of `route.closed` is a separate atomic load, which sees the LATEST value (subject to memory ordering). If the writer uses SeqCst or Release, and the reader uses Acquire, the reader sees the writer's set if the check is after the load.

But: the reader's check is AFTER the load. The load gets an Arc. The Arc points to a Snapshot. The Snapshot contains an Arc to the RouteBinding. The RouteBinding has the `closed` AtomicBool. The reader's check is `route.closed.load(Acquire)`. This is a fresh atomic load. It sees the latest value.

So if the writer sets closed=true at T1, and the reader checks at T4 > T1, the reader sees true. ✓

But: what if the reader holds the Arc<RouteBinding> across the snapshot load and the check? The reader's check is on the Arc's data. The data is mutable (AtomicBool). The check sees the latest value. ✓

So the closed flag works for the data-plane consumer. ✓

But: the read loop's dispatcher push doesn't check `closed`. The v2 doc says the read loop enqueues to the dispatcher. The read loop doesn't check the binding's `closed` flag. It just enqueues to the dispatcher. The dispatcher's `admission != Open` check catches Closing/Closed routes.

The v2 doc says "The dispatcher admission=Closing (section 5) is the client→module equivalent" of the closed recheck. So the dispatcher side uses admission, the module→client side uses closed. ✓

But: the admission check is at enqueue time. If a frame is enqueued before admission=Closing, the frame is in the queue. The drain processes it. If the route's flow is closed (e.g., module stopped), the drain sees ChannelFlowClosed. The teardown disambiguates. ✓

OK, B10 is plausibly closed. ✓

Let me also check the v2 I3/I7 corrections:

V2 I3 (DELTA): "Epoch-fence + escalation predicates preserved; release call site gains the exactly-once gate. NOT 'untouched.'" — Correct, this acknowledges the change. ✓

V2 I7 (DELTA): "module→client wire behavior unchanged; duplicate/late-terminal credit fixed; a `closed` recheck added to the stale-Bound path." — Correct, acknowledges the credit fix and the closed recheck. ✓

V2 I4 (DELTA): "GOODBYE now flushes queued client→module frames (a raw-wire semantic change; SDK clients settle locally first so unaffected)." — Hmm, the v2 doc claims GOODBYE flushes queued frames. But the v2 mechanism's teardown step 5 says: "lock inbox: admission = Closed; drain queued corrs -> synthesize cancelled/drop per reason". So queued corrs are dropped (with cancelled terminal synthesized). This is a semantic change for raw-wire clients.

The v2 doc says "SDK clients settle locally first so unaffected". This is plausible for SDK clients, but raw-wire clients (testing, custom) would see this change. The v1 B8 finding (#8) said "I4 is FALSE" — the v2 acknowledges this and marks it as a DELTA. ✓

Now let me also check the v1 B4 (drain error arms) — the v2 mechanism specifies:
- flow.acquire() Err → handle_closed with teardown disambiguation
- module_sink.send() Err → backend_error + release credit
- panic → RAII guard releases credit

The v2 doc claims "B4 resolved; T8 preserved rather than edited." But:
- The test `blocked_flow_control_acquire_wakes_when_module_tears_down` expects `backend_error` for the blocked corr when the module is torn down. The v2 maps this to `ConnClose` → silent drop. The test allows EITHER GOODBYE OR `backend_error`. So the test passes if GOODBYE arrives (from the connection close) and the silent drop doesn't fire. **But if the v2's silent drop fires, the client sees GOODBYE eventually, and the test reads one frame and accepts GOODBYE.** ✓ (test passes)
- But the client experience is different: the client sees a GOODBYE without a prior `backend_error` for the blocked corr. This is a wire-observable change.

OK, the test passes. The v2 doc's claim of T8 preservation is correct for THIS test. But the wire observable for "module stopped with blocked acquire" is different (was: backend_error, now: silent drop + GOODBYE).

Now let me check the v2's Q1' (hard-gate vs interim):

The v2 doc says: "the daemon MUST NOT emit these codes [route_backpressure, control_backpressure] until merge-0 is deployed fleet-wide." This is the hard-gate. The interim is "per-route bounded BLOCKING admission (drain-side, not read-loop)".

But: if the daemon hard-gates, then between merge-2 (daemon deploy) and merge-0 (SDK deploy), the daemon CANNOT emit route_backpressure. So the daemon must use the interim (blocking admission) until merge-0 is live. But the v2 doc says hard-gate is the lean.

This is a contradiction in the v2 doc. The hard-gate means "wait until merge-0 is live before deploying merge-2". The interim means "ship merge-2 with blocking admission, then flip to fail-loud after merge-0". The v2 doc says "Decide at re-gate whether to ship the blocking-admission interim or hard-gate on merge-0."

So the doc is presenting the decision, not deciding. The lean is hard-gate, but the interim is a fallback.

**This is a process question, not a mechanism question.** The v2 mechanism is the dispatcher + state machine + drain error arms + teardown gate + client-side control offload + R11 gate. The merge-0 is a prerequisite for the `route_backpressure` / `control_backpressure` codes. The mechanism is the same either way; the difference is whether the codes are emitted.

OK, I think I have enough to produce the verdict. Let me also quickly check the v1's "I3 'release paths untouched'" — the v2 I3 says "release call site gains the exactly-once gate". The shipped `release_client_route_locked` is the path that closes the flow. The v2 mechanism adds a `teardown` field setting and a `closed: AtomicBool` setting. The actual `flow.release()` is still called from `router.rs:309` (module→client) and `router.rs:495` (handle_bound send error). The v2 gates the module→client path's `flow.release()` with `slots.remove(corr)`. So the v2 changes the module→client terminal path's release call site. ✓

Let me also check the v1 B11 (corr-uniqueness) — the v2 mechanism uses `slots.insert(corr, Slot{...})`. The v2 doc says "a client reusing an in-flight corr on one route is a protocol violation; enforce at enqueue". The mechanism section (section 1, 2) doesn't show this check. The mechanism's `slots.insert` is unconditional. **So the v2 mechanism has a v1-B11 gap.**

OK, I have enough. Let me write the final response.

Let me also confirm that v2 doc's "outstanding" comment is the issue I noted:

[assistant] 2026-07-15T08:25:16Z
Confirmed: v2 uses `outstanding` for admission check (`queue.len()+outstanding >= depth_cap`). The v2 doc says "outstanding mirrors flow.in_flight". The drain increments outstanding when state=Delivered, decrements on terminal removal. The terminal removal happens on the module→client path (router.rs:281-309 in v2) or on the drain's send-failure arm.

But: the module→client path's slots.remove is GATED by the v2 mechanism. The drain's send-failure arm removes the slot. The drain's rollback (cancelled or closed) does NOT remove the slot from `slots` — it calls `flow.release()` and drops. Wait, let me re-read:

```
if state==Claimed{cancelled:true} OR admission==Closed:
    unlock; flow.release(); synthesize Error{cancelled} if route-alive; drop; continue
```

The "drop" here means drop the frame. But what about the slot? The slot was `slots[corr].state = Claimed{cancelled:false}` (set earlier). The rollback doesn't remove the slot. The slot remains in slots with state=Claimed{cancelled:true} (or false, but cancelled was set).

If a subsequent CANCEL arrives, the read loop sees state=Claimed{cancelled} and sets cancelled=true (no-op if already true). The slot remains.

If the module→client terminal arrives for this corr... but the corr was rolled back, so the module never saw the Request. No terminal arrives.

So the slot is orphaned in slots with state=Claimed{cancelled:true}. **This is a memory leak.**

The v2 mechanism's admission check `queue.len()+outstanding >= depth_cap` uses `outstanding`, not `slots.len()`. The `outstanding` is incremented on Delivered and decremented on terminal removal. The rollback does NOT increment outstanding (because the slot was Claimed, not Delivered). So `outstanding` is correct. But `slots` accumulates orphaned Claimed slots.

**This is a v2-introduced memory leak.** The rollback path drops the frame but leaves the slot in `slots`. Over time, slots grows unbounded for routes that receive many cancellations.

Wait, let me re-read the v2 rollback:
```
if state==Claimed{cancelled:true} OR admission==Closed:
    unlock; flow.release(); synthesize Error{cancelled} if route-alive; drop; continue
```

The "drop" is ambiguous. It could mean "drop the frame" or "drop the slot". The v2 doc doesn't say. If it means "drop the frame but keep the slot", the slot leaks. If it means "drop both", the slot is removed.

Looking at the v2 doc's intent: the rollback should release the credit and synthesize a cancelled terminal. The frame is consumed. The slot... the v2 doc doesn't show a `slots.remove(corr)` in the rollback path. So the slot leaks.

Actually, looking at the v2 doc more carefully:
```
lock inbox:
  take Slot; if state==Claimed{cancelled:true} OR admission==Closed:
      unlock; flow.release(); synthesize Error{cancelled} if route-alive; drop; continue
```

"take Slot" — in Rust, this could be `slots.remove(corr).unwrap()` (takes ownership, removes from map) or `slots.get_mut(corr).unwrap()` (borrows, doesn't remove). The v2 doc is ambiguous.

If "take" means `slots.remove(corr)`, then the slot is removed in the rollback. The memory leak is avoided. But the doc doesn't explicitly say "remove".

If "take" means `slots.get(corr)`, the slot remains. The leak is real.

The v2 doc's pseudocode is ambiguous. The implementation must choose. The v2 doc should specify "remove the slot on rollback" to avoid the ambiguity.

OK, I have a comprehensive view. Let me write the verdict.

Let me now consolidate my findings into the COUNCIL_MEMBER_RESPONSE:

Findings summary:
- B1 (SDK prerequisite): CLOSED via merge-0 acknowledgment
- B2 (CANCEL limbo): CLOSED via per-corr state machine
- B3 (queue data structure): CLOSED via explicit VecDeque + HashMap + Mutex
- B4 (drain error arms): PARTIALLY CLOSED — teardown disambiguation specified but introduces wire-observable change for "module stop with blocked acquire"
- B5 (outstanding insert ordering): CLOSED via state=Delivered BEFORE send
- B6 (O(queue) CANCEL scan DoS): CLOSED via slots.get_mut O(1)
- B7 (channel-0 FIFO breaks bind-ACK): CLOSED via client-side-only offload
- B8 (false invariants I3/I7/I4): CLOSED via reworded DELTA I3/I4/I7
- B9 (GOODBYE flush non-atomic + teardown hang): PARTIALLY CLOSED — admission=Closing barrier is correct, but rollback slot removal is ambiguous
- B10 (merge-1 not invariant-neutral): CLOSED via publish-under-lock + closed flag

New defects introduced by v2:
1. BLOCKER: Module→client terminal path (router.rs:281-309) needs dispatcher reference to do `slots.remove(corr)` gate — v2 mechanism is unworkable as-specified
2. BLOCKER: Connection close path needs dispatcher map to trigger teardown — v2 mechanism is unworkable as-specified
3. MAJOR: Two-lock-dance in drain task (pop + state-Claimed) is a race window — CANCEL arriving in this window can remove the slot before the drain's claim
4. MAJOR: AcquiredCredit RAII guard's commit() is implicit in pseudocode — double-release hazard on rollback path
5. MAJOR: Panic guard releases credit but doesn't clean up slots (memory leak)
6. MAJOR: Module-death-on-blocked-acquire observable changes from `backend_error` to silent drop (B4 T8 preservation claim is wrong for wire-observable)
7. MEDIUM: HashMap insert in slots silently overwrites on duplicate corr (Q4' is open but mechanism doesn't enforce)
8. MEDIUM: No byte-based memory budget (Q3' is open but mechanism doesn't address B12)
9. MEDIUM: Outstanding count "mirrors" in_flight but timing is different — assertion hazard if used
10. MEDIUM: "drop" in rollback path is ambiguous — could leak slots if not explicitly removed

Q1' lean: RIGHT (hard-gate is cleaner)
Q2' lean: RIGHT (bounded-join + abort is correct)
Q3' lean: RIGHT-BUT-UNSAFE (depth cap is specified, but byte-based budget is open)
Q4' lean: RIGHT (enforce at enqueue, but mechanism doesn't specify how)
Q5' lean: RIGHT (whole-table publish is correct for read-mostly)

Bottom line: NO-GO. The v2 mechanism is sound in spirit but has at least two BLOCKER design gaps (missing dispatcher reference on RouteBinding for module→client terminal path's slots.remove gate, and missing dispatcher map for connection close teardown) and several MAJOR hazards (two-lock-dance race, RAII commit() implicit, panic slot leak, module-death observable change). The mechanism as written is unworkable and must be redesigned with explicit dispatcher↔binding wiring and explicit commit()/slot-removal at every release point.

Now let me write the final response. Let me also drop some context to keep things lean:

[assistant] 2026-07-15T08:25:46Z

[assistant] 2026-07-15T08:25:50Z
Now let me write the final council member response:

<COUNCIL_MEMBER_RESPONSE>
# subc-core dispatch redesign v2 — adversarial re-gate verdict

I read the v2 doc, the v1 synthesis, and the shipped source (forwarding.rs, router.rs, control.rs, server.rs, supervise.rs, fake-aft-stub.rs, subc-client-rs/consumer.rs, subc-client/client.ts). The v2 mechanism is sound in spirit, but as specified it is **not implementable** and re-introduces at least two race windows the v1 gate explicitly closed. I find the v2 spec to be in the same "redesign the mechanism, re-gate" state as v1.

## B1–B10: closure verdict

### B1 (SDK prerequisite — `route_backpressure` → NotSent false claim) — **CLOSED**
The v2 doc correctly identifies that the v1 "zero SDK changes" claim is false. Verified at source: `clients/subc-client/src/client.ts:427-450` retries only `code === "unknown_channel"` or `kind === "not_sent"`; `errorFromFrame` (client.ts:1118-1127) produces a plain `SubcError` wrapped as `terminalCallError` ("terminal" kind) for an Error frame, and `call()` does NOT retry terminal. `crates/subc-client-rs/src/consumer.rs:570-579` mirrors this (returns `CallError::Module(body)` for non-unknown_channel errors). v2's `merge-0` prerequisite is the right fix; Q1' presents the hard-gate vs interim decision honestly.

### B2 (CANCEL queued→delivered limbo) — **CLOSED** (with one new race below)
The per-corr state machine `Queued→Claimed→Delivered` with CANCEL decided under the same lock removes the v1 limbo. Verified by tracing each CANCEL arrival point: Queued → daemon-synthesized cancelled (no credit held); Claimed{cancelled} → set flag, drain rolls back; Delivered | None → forward to module, its terminal releases. The state machine is sound.

### B3 (queue data structure unspecified / mpsc-scan race) — **CLOSED**
v2 specifies `VecDeque<u64>` + `HashMap<u64, Slot>` behind a per-route `Mutex<RouteInbox>`, with O(1) push/pop/get/remove-by-corr and no mpsc. No data race. Concrete enough to implement.

### B4 (drain-task error arms unspecified) — **PARTIALLY CLOSED (one wire-observable regression)**
v2 specifies the three error arms: `ChannelFlowClosed` disambiguation (Reloading/Goodbye/ConnClose/None), `module_sink.send` failure (backend_error + release), RAII panic guard. The shipped test `blocked_flow_control_acquire_wakes_when_module_tears_down` (forwarding.rs:3811-3877) accepts EITHER a route GOODBYE OR an `Error{backend_error}` for the blocked corr. The v2 mechanism maps `module.stop()` → connection-close path → teardown=ConnClose → silent drop. The test stays green because the client eventually sees a GOODBYE, which the test accepts. **But the wire-observable for "module dies with blocked acquire" changes from `backend_error` to silent drop + later GOODBYE**, contradicting v2's own I7 "module→client wire behavior unchanged" (the error is on the client→module terminal path, not strictly module→client, but the client observable is different). The T8 preservation claim is technically true for the assertion but materially wrong for the observable.

### B5 (outstanding insert-ordering) — **CLOSED**
v2 sets `state=Delivered` and increments `outstanding` UNDER the inbox lock BEFORE `module_sink.send().await`. The module cannot emit a terminal for a frame it hasn't seen, so a fast module terminal cannot race the insert. Sound.

### B6 (O(queue) CANCEL scan DoS) — **CLOSED**
CANCEL is O(1) via `slots.get_mut(corr)`. No linear scan on the read loop. ✓

### B7 (channel-0 FIFO breaks bind-ACK→data barrier) — **CLOSED**
v2 explicitly scopes offload to the CLIENT connection's channel-0 only. Module connections stay serial: HELLO, bind-ack, catalog.update, and data all run on the module connection's single inline read path, so the bind-commit-before-next-frame barrier is preserved by construction. The shipped test `accepted_route_publishes_route_open_before_immediate_reverse_request` (router.rs:1078-1102) is a unit test that doesn't go through the read-loop offload, so it stays green; v2's T14 plans the real integration test.

### B8 (false invariants I3/I7/I4) — **CLOSED**
v2 rewrites I3 (release call site gains exactly-once gate), I4 (GOODBYE flushes queued client→module frames, raw-wire semantic change), I7 (closed recheck added, duplicate/late-terminal credit fixed) as honest DELTAs. The "trusted vs enforced" R11 framing is corrected to the concurrent-duplicate case. ✓

### B9 (GOODBYE flush non-atomic + teardown hang) — **PARTIALLY CLOSED (one missing piece)**
The 3-phase `Open/Closing/Closed` admission gate is the right primitive: `admission=Closing` happens-before the snapshot removal that drops the binding, so a stale-snapshot reader hits `admission != Open` and synthesizes `route_closing` rather than delivering to a released module. The bounded-join + abort after 2s (Q2' guess, no justification but in line with the shipped `CLOSE_DRAIN_GRACE=2s` at server.rs:28) prevents connection-close hang. The JoinHandle is owned by the teardown path, not the binding, avoiding the binding→handle→task→binding strong cycle. **However, the v2 mechanism does not specify how the teardown path finds all live dispatchers for routes on a closing connection** — see New Defect 2 below.

### B10 (merge-1 not invariant-neutral) — **CLOSED**
v2 publishes the snapshot INSIDE the write-lock critical section for every mutation, and sets `RouteBinding.closed: AtomicBool` to true at release BEFORE the snapshot that removes it is published. Data-plane consumers check `closed` after loading the snapshot and treat closed-but-still-visible as `unknown_channel` (using the existing `RouterError::UnknownChannel` at router.rs:642) — restoring today's `unknown_channel` observable exactly. The TOCTOU between snapshot load and `closed.load` is safe because `closed` is an `AtomicBool` with a fresh Acquire load at the check, not a value captured at snapshot-load time. The v1 finding's specific claim (new `backend_error` state) is avoided.

---

## New defects introduced by v2

### New Defect 1 (BLOCKER): Module→client terminal path cannot reach the dispatcher's `slots` for the R11 gate
**File**: `crates/subc-core/src/router.rs:281-309`; v2 `docs/subc-dispatch-redesign-v2.md:122-128`.
**Issue**: v2 specifies that the module→client terminal path must do `slots.remove(corr)` on the route's `RouteInbox` to enforce the exactly-once release gate:
```
on terminal frame for corr, module->client path:
  lock inbox:
    if slots.remove(corr) was Delivered: outstanding-=1; release=true else release=false
```
But the shipped `RouteBinding` (`crates/subc-core/src/forwarding.rs:52-65`) has no reference to any `RouteDispatcher`. The v2 doc's `RouteBinding` change is *only* `closed: AtomicBool` (v2  lines 207-211). The module→client path at router.rs:285-310 holds an `Arc<RouteBinding>` cloned from `lookup_data_route` (forwarding.rs:840-890) and has no path to the route's per-dispatcher inbox. **The v2 mechanism as written is unworkable** — the module→client path literally cannot acquire the route's `RouteInbox` mutex. The implementation would need to add `dispatcher: Weak<RouteDispatcher>` (or equivalent) to `RouteBinding`, and the v2 doc is silent on this. Same gap applies to the rollback-on-admission=Closing path in the drain: who tells the binding's "the route is Closing"? It needs the dispatcher↔binding wiring both directions.

### New Defect 2 (BLOCKER): Connection-close teardown cannot enumerate live dispatchers
**Files**: `crates/subc-core/src/server.rs:241-267` (writer wait), `crates/subc-core/src/forwarding.rs:1168-1239` (`cleanup_connection`), v2  lines 156-174.
**Issue**: v2's teardown step 1 says "lock inbox: admission = Closing" for each live dispatcher on the closing connection. The teardown step 4 says "bounded-join the drain task". But the v2 doc specifies NO data structure for "the set of live dispatchers". The `ForwardingTable` (`forwarding.rs:283-287`) holds `inner: RwLock<ForwardingInner>` with `client_to_module: HashMap<ClientRouteKey, Arc<RouteBinding>>` and `module_to_client: HashMap<ModuleRouteKey, Arc<RouteBinding>>` — but no dispatcher map. `cleanup_connection` (forwarding.rs:1168-1239) iterates `client_to_module` to release routes, and never touches a dispatcher. Without a `dispatchers: HashMap<ClientRouteKey, Arc<RouteDispatcher>>` (or equivalent) in `ForwardingInner`, the teardown path has no way to find the dispatchers whose `admission` must be set to `Closing`, whose `flow` must be closed, whose `cancel_token` must be cancelled, and whose `JoinHandle` must be bounded-joined. **The v2 mechanism as written is unworkable** — the teardown path literally cannot find the dispatchers.

### New Defect 3 (MAJOR): Two-lock-dance in the drain task reintroduces a CANCEL window
**File**: v2  lines 86-87.
**Issue**: The drain task does:
```
corr = { lock inbox; pop_front queue or None }        // lock #1
{ lock inbox; slots[corr].state = Claimed{cancelled:false} }   // lock #2
```
Two separate `lock inbox` acquisitions. Between them, the state is "slot exists, not in queue, state=Queued". A CANCEL arriving in this window finds state=Queued, removes the slot from `slots`, and synthesizes a cancelled terminal. Then the drain re-locks and tries `slots[corr].state = Claimed{...}` — but `slots` no longer has the corr. In Rust this is a panic (`IndexMut` on missing key) or a silent no-op depending on the implementer's choice. The v2 doc's prose claims "the read loop and the drain task both make their cancel/deliver decision under the SAME lock" — this is true for the read loop's CANCEL vs the drain's claim, but the drain's own pop-then-claim is a window the prose doesn't cover. The fix is to combine pop+state-Claimed into one lock acquisition, or to handle the "slot missing because CANCEL raced" case explicitly. This is a v1-B2-style race the v2 claims to have closed.

### New Defect 4 (MAJOR): `AcquiredCredit` RAII guard's `commit()` is implicit in the pseudocode
**File**: v2  lines 88-101.
**Issue**: v2 says "scope guard (`AcquiredCredit` that calls `flow.release()` on drop unless `commit()`ed)" but the pseudocode never shows a `commit()` call. The Ok arm of `module_sink.send().await` is:
```
Ok(()) => {}                                          // terminal will release on module->client path
```
Without an explicit `guard.commit()` here, the guard's drop fires on function return, calling `flow.release()` — and the module→client terminal path also calls `flow.release()` after `slots.remove(corr)` returns true. The shipped `flow.release()` has a CAS `in_flight!=0` guard (forwarding.rs:1702-1731) that ignores over-release only when `in_flight==0`; if both releases race while `in_flight==1`, the second CAS decrements to 0 and adds a permit — a real double release. The v2 mechanism's "exactly-once" guarantee depends on an implementation detail (`commit()`) that the spec doesn't mandate. The same hazard exists on the rollback path (v2 line 94: explicit `flow.release()` plus guard's implicit drop release).

### New Defect 5 (MAJOR): Rollback path may orphan slots in `slots` (memory leak)
**File**: v2  lines 92-94.
**Issue**: The rollback on `Claimed{cancelled:true}` or `admission==Closed` is:
```
unlock; flow.release(); synthesize Error{cancelled} if route-alive; drop; continue
```
"drop" is ambiguous — it could mean "drop the frame" (slot remains) or "remove the slot and drop the frame". The pseudocode does not say `slots.remove(corr)`. If the implementation interprets "drop" as frame-only, the slot remains in `slots` with state=Claimed{cancelled:true} forever (no future terminal will arrive for a Request the module never saw). Over time, for a route that receives many cancellations, `slots` grows unboundedly. This is a slow memory leak that would only surface under long-running high-cancellation workloads. The doc should specify `slots.remove(corr)` explicitly.

### New Defect 6 (MAJOR): Panic guard releases credit but does not clean up slot (memory leak + inconsistent state)
**File**: v2  line 111.
**Issue**: v2 says the RAII guard "releases the credit" on panic between acquire and send. But the slot is in `slots` with state=Claimed, and the `outstanding` count is unaffected (the drain hadn't reached the Delivered increment yet). After the panic, the route is alive, the binding is live, but the slot is orphaned. The next CANCEL for that corr would find state=Claimed and set `cancelled=true` — but the drain task is dead, so the flag is never observed. The frame is held forever in the slot. Worse, the v2 doc's T10 test plan tests CANCEL in each state but does NOT test a drain-task panic. This is a v1-B4 gap the v2 claims to have closed.

### New Defect 7 (MEDIUM): `slots` HashMap insert silently overwrites on duplicate corr
**File**: v2  line 41 (`slots: HashMap<u64, Slot>`),  line 66 (`slots.insert(corr, Slot{frame, Queued})`).
**Issue**: v2 Q4' explicitly says corr-uniqueness is a protocol requirement but the mechanism section does NOT enforce it. In Rust, `HashMap::insert` is a silent overwrite: the old slot is dropped, the old frame is leaked (or freed), and the old credit is permanently leaked (no future terminal will find the old slot under `slots.remove(corr)` — the new slot is there, and when the new terminal fires it removes the new slot, never the old one). The v1 B11 finding (3/8 raised it as a BLOCKER for two) is acknowledged as Q4' but not closed by the mechanism. v2 should add `if slots.contains_key(&corr) { synthesize Error{protocol_violation}; return }` to the read-loop enqueue path.

### New Defect 8 (MEDIUM): Module-death-on-blocked-acquire observable changes
**File**: v2  lines 140-148.
**Issue**: v2's `handle_closed` maps:
- `ConnClose` (module connection close) → silent drop
- `None` (no teardown reason) → `backend_error`
For the shipped test `blocked_flow_control_acquire_wakes_when_module_tears_down` (forwarding.rs:3811-3877), the test calls `module.stop()` (line 3869), which is a module connection close. v2 sets teardown=ConnClose → silent drop. The test passes because it accepts either GOODBYE or `backend_error`, and the connection close path emits a route GOODBYE. But for a module that dies (process crash, supervisor kill) WITHOUT a route GOODBYE being emitted (the module is gone; no goodbye), the client sees neither a cancelled/closed terminal for the blocked corr nor an error. The pre-v2 observable was `backend_error`. v2 changes this to "no terminal, wait for connection close". This is a wire-observable change for the client and a regression in error-reporting latency.

### New Defect 9 (MEDIUM): `outstanding` "mirrors" `in_flight` but timing differs
**File**: v2  line 42 (comment "mirrors flow.in_flight; for drain-side assertions").
**Issue**: `outstanding` is incremented on `state=Delivered` and decremented on terminal removal (slot removed by module→client path or drain send-failure arm). `flow.in_flight` is incremented by `flow.acquire().await` and decremented by `flow.release()`. The two are NOT synchronized: `flow.in_flight` is incremented at acquire (before Delivered), `outstanding` is incremented at Delivered (after acquire); `outstanding` is decremented at slot removal, `flow.in_flight` is decremented at `flow.release()` (after slot removal). The drain's admission check `queue.len()+outstanding >= depth_cap` (v2 line 65) uses `outstanding`. If any code path uses `outstanding` to assert "matches flow.in_flight", the assertion can fail transiently. The v2 doc's "mirrors" comment invites such use without specifying the invariant.

### New Defect 10 (MEDIUM): No byte-based memory budget (v1 B12 not closed)
**File**: v2  Q3' line 314-315.
**Issue**: v1 finding #12 (3/8 raised it as a BLOCKER) flagged that frame-count caps are not memory bounds: max body is 64 MiB (`crates/subc-protocol/src/lib.rs:114-119`), so a 2048-deep queue with 64 MiB bodies is 128 GiB per route. v2's mechanism section (  uses frame-count admission only. Q3' asks about byte-based secondary cap but the doc itself says "Lean: keep max(4,2×window)" without committing to a byte cap. v2 does not close v1 B12; it merely flags it as an open question. The memory DoS surface remains.

### New Defect 11 (MEDIUM): Read loop's enqueue path uses `FrameSink` clone, but the dispatcher's `module_sink` is not specified to be the binding's
**File**: v2  line 33-35 (struct fields `flow: Arc<ChannelFlow>`, `module_sink: FrameSink`).
**Issue**: v2's `RouteDispatcher` has its own `flow` and `module_sink` fields. The doc says "the EXISTING per-route credit sem (unchanged: acquire/forget + CAS release)" — so the dispatcher's `flow` is the same `Arc<ChannelFlow>` as the binding's (cloned at bind time). But the doc doesn't say where the dispatcher's `module_sink` comes from. If it's a separate clone of the binding's `module_sink` (an mpsc sender), then closing one closes the other (good). But the v2 teardown step 2 says `flow.close() with teardown=<reason>` — it does NOT say `module_sink.close()`. If the module connection closes, the `module_sink` is dropped by the connection's teardown, and the drain's `module_sink.send().await` returns Err. That's fine. But the v2 doc doesn't address what happens if the `module_sink` is dropped while the dispatcher's `flow` is still open — the drain's send fails, the error arm fires (backend_error + release), but the route is still "bound" from the snapshot's perspective. The v2 mechanism assumes the binding→module_sink lifetime and the dispatcher→module_sink lifetime are synchronized, but doesn't specify how.

### New Defect 12 (LOW): AcquiredCredit RAII guard interacting with the outstanding count
**File**: v2  lines 91-101.
**Issue**: The rollback path increments `outstanding`? No — the rollback is on `state=Claimed{cancelled:true}`, not on `state=Delivered`, so `outstanding` is NOT incremented. The rollback decrements the credit but not outstanding. The `module_sink.send` Ok path doesn't touch outstanding. The module→client terminal path does `outstanding -= 1`. The `outstanding` count tracks "Delivered but not yet Settled" corrs. But the admission check `queue.len()+outstanding >= depth_cap` (v2 line 65) treats both queue and outstanding as "in flight". If a corr is in `outstanding` (Delivered, module processing) and a CANCEL arrives, the CANCEL is forwarded to the module (state=Delivered), the module emits a terminal, the terminal removes the slot, decrements outstanding, releases credit. The slot was in outstanding, and the admission check counted it. During the time the module is processing, the corr is in outstanding. If the depth cap is hit, new enqueues are rejected. This is the intended behavior. No defect here, but the interaction is subtle and the v2 doc doesn't explain it.

---

## Q1'–Q5' open-question rulings

| Question | Lean | Ruling | Rationale |
|---|---|---|---|
| Q1' merge-0 hard-gate vs blocking-admission interim | hard-gate | **RIGHT** | Hard-gating is cleaner; the doc honestly presents the interim fallback. The contradiction is process-level (deploy sequencing), not mechanism. |
| Q2' teardown bounded-join timeout (2s?) | 2s + abort | **RIGHT-BUT-UNSAFE** | The 2s matches the shipped `CLOSE_DRAIN_GRACE` (server.rs:28), so it's a defensible default. The "abort-after-timeout can drop an in-flight terminal (acceptable: connection is closing)" is correct for full connection close but not for partial teardowns (e.g., supervisor reload of a single module). The doc should narrow the scope. |
| Q3' depth caps: keep max(4,2×window)? Byte-based secondary cap? | keep max(4,2×window) | **RIGHT-BUT-UNSAFE** | The frame-count cap is specified; the byte-based cap is open. v1 B12 (256 GiB per connection memory DoS) is not closed. The mechanism admits on frame count only, which is the exact attack surface v1 flagged. |
| Q4' `slots` HashMap vs slab/index; corr-uniqueness enforcement | enforce at enqueue | **RIGHT-BUT-UNSAFE** | The lean is correct (enforce at enqueue), but the mechanism section (  does not implement the enforcement. `slots.insert(corr, ...)` is a silent overwrite. The doc acknowledges the gap but doesn't close it. |
| Q5' snapshot publish granularity: whole-table vs per-shard | whole-table | **RIGHT** | Whole-table publish is correct for read-mostly workloads (mutations rare vs lookups). The T9 perf evidence is the right gate. ✓ |

---

## Bottom-line verdict: **NO-GO**

The v2 mechanism is sound in spirit and correctly identifies the v1 gaps. The closure arguments for B1, B2, B3, B5, B6, B7, B8, B10 are convincing and source-cited. But the v2 doc is a **design spec, not an implementable spec** — it has at least two BLOCKER design gaps (New Defects 1 and 2: the module→client terminal path cannot find the route's `RouteInbox` because `RouteBinding` has no dispatcher reference, and the connection-close teardown cannot enumerate live dispatchers because `ForwardingInner` has no dispatcher map), plus four MAJOR race/leak/observable hazards (New Defects 3-6: two-lock-dance CANCEL window, implicit `commit()`, rollback slot leak, panic slot leak), plus wire-observable changes (New Defect 8) and a still-open memory DoS (New Defect 10).

**Required changes before re-gate** (all must be addressed; each is file:line-cited):

1. **BLOCKER**: Add `dispatcher: Weak<RouteDispatcher>` (or `Arc<RouteDispatcher>`) to `RouteBinding` (forwarding.rs:52-65). The module→client terminal path at `router.rs:281-309` uses this to lock the route's `RouteInbox` and do `slots.remove(corr)` for the R11 gate.
2. **BLOCKER**: Add `dispatchers: HashMap<ClientRouteKey, Arc<RouteDispatcher>>` (or equivalent) to `ForwardingInner` (forwarding.rs:236-256). The connection-close teardown (`cleanup_connection` at forwarding.rs:1168-1239 and `RouterConnection::drop` at router.rs:391-398) uses this to enumerate live dispatchers, set `admission=Closing`, close `flow` with `teardown`, cancel the token, and bounded-join each drain task before the epoch-fenced release.
3. **MAJOR**: Combine the drain task's `pop_front` and `state=Claimed` into a single lock acquisition (v2  lines 86-87). Or add an explicit "slot was removed by CANCEL between pop and claim — synthesize cancelled, continue" branch.
4. **MAJOR**: Make the `AcquiredCredit` guard's `commit()` explicit in the pseudocode (v2  line 98). Show `guard.commit()` after the Ok arm of `module_sink.send().await`, and `guard.disarm()` (or equivalent) on the rollback path before the explicit `flow.release()`.
5. **MAJOR**: Specify `slots.remove(corr)` explicitly on the rollback path (v2  line 94). The "drop" is ambiguous.
6. **MAJOR**: Specify panic guard behavior beyond "release the credit" (v2  line 111). Must also `slots.remove(corr)` and `outstanding -= 1` (or equivalent) to clean up the slot. Add a T-panic test to T10.
7. **MAJOR**: Revert the silent-drop-on-ConnClose mapping (v2  line 146). The shipped test expects a `backend_error` observable; v2 should preserve it. Map `ConnClose` → `backend_error` to match the shipped behavior, not silent drop.
8. **MEDIUM**: Add corr-uniqueness enforcement to the read-loop enqueue path (v2  line 66). Reject duplicate-corr enqueue as a protocol violation with a typed error frame, not a silent overwrite.
9. **MEDIUM**: Add a byte-based secondary cap (v2 Q3' line 314-315) to close the v1 B12 memory DoS. The frame-count cap is not a memory bound.
10. **MEDIUM**: Remove the "mirrors flow.in_flight" comment on `outstanding` (v2  line 42) or replace with the actual invariant: "delivered-not-settled count; briefly diverges from `flow.in_flight` by the slot-removal-to-release window".

**One single un-refuted concurrency or contract defect is a NO-GO.** I find at least six (New Defects 1-6), two of which are BLOCKERs that make the mechanism un-implementable. The v2 spec must be revised and re-gated.

**External consumers (broca/aft/alfonso-core) are not in this checkout**, as the v1 synthesis noted and v2 acknowledges (Q1'). The `route_backpressure` / `control_backpressure` codes' impact on those consumers cannot be verified here and must be checked in their own repos.
</COUNCIL_MEMBER_RESPONSE>

