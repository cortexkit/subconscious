---
task_id: bg_53104f31
agent: council-member
session_id: ses_0c38fdc9affeJUx2RDvVNjK6BW
parent_session_id: ses_0c390bb4bffeG3XXZm7M7xt8JI
status: completed
completed_at: 2026-07-07T12:08:08Z
---

[user] 2026-07-07T11:56:50Z
## Solo Analysis Mode
You MUST do ALL exploration yourself using your available read/search tools.
- Do NOT use task or any delegation tool under any circumstances
- Do NOT delegate to explore, librarian, or any other subagent
- Do NOT spawn background tasks
- Search the codebase directly — you have full read-only access to every file
- This mode produces the most thorough analysis because you see every result firsthand


## Analysis Intent: AUDIT

You are conducting an **audit** — your goal is to find discrete issues, risks, or violations.

**Focus:**
- Search for problems, anti-patterns, security risks, correctness issues, or violations of stated requirements
- Each finding must be a distinct, actionable item with concrete evidence
- Severity determines priority: critical (blocks/breaks), high (significant risk), medium (should fix), low (nice to fix)
- For each finding, provide the specific location (reference, section, or component where it occurs)
- State your confidence: high (clear evidence), medium (likely but needs verification), low (suspicion, investigate further)
- **This is a broad sweep, not a targeted trace.**

**Analytical standards:** Support claims with concrete evidence. State confidence (high/medium/low) for key assertions. Note caveats and limitations.

**Structure your response as:**
```
<COUNCIL_MEMBER_RESPONSE>
## Finding 1: [Title]
- **Severity**: critical/high/medium/low
- **Location**: [specific reference — e.g. component, section, endpoint, rule]
- **Confidence**: high/medium/low
- **Issue**: [what is wrong and why it matters]
- **Evidence**: [concrete reference, snippet, or observation that proves the issue]
- **Suggested Fix**: [actionable recommendation]

## Finding 2: [Title]
...

## Summary
[Total findings by severity. Overall risk assessment with confidence levels.]
</COUNCIL_MEMBER_RESPONSE>
```

## Analysis Question

ADVERSARIAL DESIGN REVIEW — subc Federation PHASE 3 design.

You are a hostile, senior security+distributed-systems reviewer. Your job is to FIND GAPS AND BLOCKERS in a phase-3 design that builds on a shipped, verified phase 0-2 architecture. Be adversarial: assume attackers, assume races, assume the cloud is compromised. Do NOT be agreeable. Reward finding real holes; do not manufacture noise.

## DOCUMENTS TO READ (both in the `subconscious` repo)
1. PRIMARY (under review): `docs/subc-federation-phase3-design.md` — DRAFT v1.
2. LOAD-BEARING PRIOR CONTEXT (the shipped phase 0-2 architecture this builds on): `docs/subc-federation-design.md` — v4.1.
   NOTE: `docs/specs/subc-health.md` is NOT relevant — skip it.

Read BOTH fully before analyzing. Where phase-3 asserts INHERITANCE from phase-2 (e.g. "carrier-blind", "reaping unchanged", "verified-gate makes discovered peers non-routable", "relay-path partitions classify exactly like TCP partitions", "Noise session resumption = ordinary re-handshake"), VERIFY the claim against the phase-2 design doc. If the phase-2 doc does not actually support the inherited property, that is a finding.

## SHIPPED & VERIFIED (phase 0-2 — do NOT re-litigate these; treat as ground truth)
Noise IK E2E between device static keys; verify-code pairing ceremony (SHA-256 safety number, order-independent); default-deny exposure with an enforced verified-gate; exactly-once effect ledger (incarnation epoch + seq high-water + recovery reconciliation); WAN-proven.

## WHAT PHASE 3 ADDS
(3a) A CortexKit account: managed auth provider (WorkOS) at the edge behind an `AccountVerifier` seam; we mint our own account IDs + device tokens. A Cloudflare Worker + per-account Durable Object (AccountDO) rendezvous: device registry, candidate endpoints, signaling. Candidate-priority dialing lan→public→relay.
(3b) A Noise-over-WebSocket carrier (WsCarrier) and a zero-knowledge ciphertext relay DO (RelayDO) for double-NAT.

## LOCKED DECISIONS — do NOT relitigate (out of scope, will be ignored):
- Managed auth provider at the edge (WorkOS), NOT self-hosted auth.
- account = discovery-only (trust stays key-based via the verify-code ceremony).
- Cloudflare hosting.
- relay-NOT-hole-punching for phase 3.
- one phase gate covering 3a+3b.
Findings that merely re-argue these are noise. Findings about how these decisions are IMPLEMENTED (races, lifecycle, enforcement gaps) are in scope.

## REQUIRED ANALYSIS AXES — hunt in each:

(1) SECURITY
- Device-token lifecycle: theft, rotation, revocation races (revoke vs in-flight signaling; token rotation while a peer holds a stale token).
- Enrollment races: two devices enrolling the same pubkey; pubkey re-enrollment after removal (the `revoked_by_account` → re-pair path); account-id resolution races.
- Signaling abuse: connect_request floods; candidate poisoning by a sibling device OR by the cloud; replay of signaling messages (no nonce/timestamp specified?).
- Relay pipe-token properties: single-use enforcement (where/how?), TTL, who can consume whose grant (can device X redeem device Y's pipe_token?), grant issued to "both sides" — symmetric token or per-side?
- Cross from metadata to routability: anything that lets a compromised cloud OR an account-thief escalate from "sees the device graph / can offer a rogue device" to actual routability or plaintext. Test the claim that verify-code makes this impossible in EVERY window (including the pairing UX window where offer-to-unverified is allowed).

(2) CORRECTNESS
- Candidate racing: both sides dialing simultaneously — how does this interact with the phase-2 pubkey tie-break? Can two carriers win simultaneously (TCP session AND WS session both establishing)? What resolves a double-win?
- WS carrier framing: "one fed record per WS message + retained 4-byte length prefix (redundant but kept)." Any ambiguity — e.g. a WS message carrying !=1 record, or a length prefix disagreeing with the WS message length? Is the receiver's parser fed exactly one record per message, and what happens on mismatch?
- Keepalive/reap over a relay pipe with idle teardown: does RelayDO idle-teardown look like a peer PARTITION to the reaper (false-positive partition → spurious GOODBYE / OutcomeUnknown)? Does the effect ledger's recovery reconciliation interact badly with relay reconnects (a reconnect mid-effect: does recovery re-query correctly across a NEW pipe)? Is "Noise session resumption = ordinary re-handshake, cheap" actually true given phase-2 session/incarnation semantics?
- Registry staleness: candidates changing mid-dial; device online flaps; a stale candidate list causing dial to a dead/wrong endpoint.

(3) OPERATIONAL
- DO-per-account hot spots (a busy account funnels all signaling+registry through one DO).
- WS hibernation wake latency vs signaling timeouts (open question 5): can a cold-wake round-trip blow a ~2s candidate timeout or a connect_request deadline?
- Rendezvous DOWN: degradation to static profiles must be AIRTIGHT. Verify that `[rendezvous]`-absent = exact phase-2 behavior, and that a rendezvous that is present-but-down cleanly falls back rather than hanging. Any path where rendezvous-down bricks an otherwise-static-reachable peer?
- Device clock assumptions: TTLs, last_seen, token expiry — any reliance on device wall-clock that a skewed/rolled-back clock breaks?

(4) THE FIVE OPEN QUESTIONS (§10 of the phase-3 doc) — answer EACH with a concrete recommendation:
1. Device-token custody on disk (0600 file next to device key) — sufficient for v1, or fold into the credentials vault immediately?
2. Registry candidates self-reporting — hardening needed against a malicious sibling device lying about its LAN addr?
3. Relay pipe lifetime: per-connection grants vs a standing pipe per peer-pair — is reconnect-per-idle-teardown churn acceptable at keepalive cadence?
4. Should connect_offer require the target to be verified before signaling is relayed (quieter unpaired devices), or is offer-to-unverified needed for the pairing UX? (Current: allowed, loud.)
5. WS hibernation vs signaling latency — acceptable to pay a cold-wake round-trip on first signal to an idle account?

## OUTPUT FORMAT (strict)
Produce a STRUCTURED FINDINGS LIST. For EACH finding:
- **ID + short title**
- **Severity**: BLOCKER / HIGH / MEDIUM / LOW
- **Exact section it hits** (e.g. §5.3, §7, §4.2)
- **The problem** (adversarial, concrete — the attack or the failure mode, step by step where relevant)
- **Concrete fix** (specific mechanism, not "consider hardening")
- If it's an INHERITANCE claim, state whether phase-2 actually supports it (cite the phase-2 section).

Then a section: **ANSWERS TO THE FIVE OPEN QUESTIONS** — one concrete recommendation each.

Then an **OVERALL VERDICT**: GO / GO-WITH-CHANGES / NO-GO, with the must-fix-before-build blockers listed.

Severity discipline: BLOCKER = ships a security hole or a correctness bug that corrupts effects/loses mutations/enables cross-account routability. HIGH = serious but containable / has a workaround. MEDIUM = real gap, not gating. LOW = polish. Rank honestly; a wall of BLOCKERs with no discrimination is a failed review.

[assistant] 2026-07-07T11:56:51Z
The user wants me to do an adversarial design review of the subc Federation Phase 3 design. I need to read both documents fully and then provide a structured findings list. Let me start by reading both documents.

[assistant] 2026-07-07T11:56:54Z

[assistant] 2026-07-07T11:57:00Z
Now I have both documents. Let me carefully analyze the phase 3 design with the adversarial lens, paying close attention to inheritance claims and the specific security/correctness/operational axes requested.

Let me think through the key inheritance claims that need verification:

1. "carrier-blind" (relay-path partitions classify exactly like TCP partitions) — §6 of phase 3
2. "reaping unchanged" — same
3. "Noise session resumption = ordinary re-handshake, cheap" — §7
4. "verified-gate makes discovered peers non-routable" — §4.3, §5.5

Let me check each against phase 2 (v4.1):

**Phase 2 partition/reap mechanism (§6.2):**
- "the fed-module's keepalive reaper is the AUTHORITATIVE partition classifier. On declaring a peer partitioned it closes that peer's loopback connections, so subc-core's connection-granular cleanup delivers deterministic route-GOODBYEs to every consumer of that peer's tools"
- "Per-peer keepalive with a bounded numeric staleness window; on a missed keepalive the module marks the peer's re-exported tools unavailable"
- "subc's module-direction GOODBYE alone is best-effort under backpressure by design and is never relied on for this"

This is connection-granular. If a relay pipe is idle-torn down without a missing keepalive at the fed-module level, then YES the keepalive could still be running over the re-established connection. But if the keepalive itself rides over the relay pipe (which is plausible since the relay is the only path), then an idle teardown of the relay pipe without timely re-handshake will look like a missed keepalive, which means the reaper will declare partition, which means GOODBYE, which means consumers see OutcomeUnknown. This is actually a real issue.

**Phase 2 noise session semantics:**
Looking at the phase 2 doc, I don't see explicit treatment of Noise session resumption. Phase 2 doc just says "Noise IK" (the IK pattern is a 2-message interactive handshake with static key knowledge, but session resumption is not in the IK pattern). The phase 3 claim that "Noise session resumption = ordinary re-handshake" needs to be examined.

Actually, Noise IK does NOT have built-in session resumption — that's Noise XXpsk or XXfallback. IK requires an interactive handshake each time. The "resumption" claim in phase 3 must be carefully read: it says "ordinary re-handshake, cheap" — meaning the cost of a re-handshake is bounded/acceptable, not that there's a session resumption token. This is a claim about performance/cost, not about the protocol. Let me re-read...

§7 says: "idle timeout tears the pipe down (peers reconnect via a fresh grant — Noise session resumption = ordinary re-handshake, cheap)."

This is a bit ambiguous. The "Noise session resumption" might refer to whether the fed-module reuses a Noise session across pipe reconnects (i.e., the new pipe carries the SAME Noise session, resumed). Or it could mean they re-handshake. The phrase "ordinary re-handshake, cheap" suggests they re-handshake. But the "= " sign suggests it's an equivalence claim.

Looking at the phase 2 doc carefully: phase 2 doesn't appear to specify whether the Noise session is a single long-lived session, or per-connection. The phase 3 doc says "the Noise session, fed-frame codec, forwarder, ledger, and profile model are unchanged" (§3). So the Noise session is presumably a long-lived end-to-end object that survives carrier changes.

Wait, that's important. If the Noise session is end-to-end and survives carrier changes, then when the relay pipe idle-tears-down, the Noise session might be terminated. If the carrier re-handshake cost is "ordinary" and the connection teardown at subc-core triggers GOODBYE — that's the issue. The phase 2 partition classifier doesn't know that "carrier changed" vs "peer dead", and so a relay idle teardown will look like a partition.

But if the Noise session survives the carrier change (just a new WSS connection within the same Noise session — that IS a thing in Noise XX patterns, but not IK), then keepalive would continue. But the phase 3 doc says "one fed record per WS message" and doesn't mention session resumption tokens.

**This is a significant finding.** The phase 3 doc claims:
- "carrier-blind" → the noise session survives carrier changes (claim)
- "reaping unchanged" → keepalive is carrier-agnostic (claim)
- "relay-path partitions classify exactly like TCP partitions" → a relay-pipe disconnect triggers the same partition behavior (claim)

But a relay pipe idle-teardown isn't a partition — it's an expected carrier event. If the fed-module's reaper sees no keepalive (because the relay pipe is down), it will declare partition. The fix is: keep the Noise session across pipe teardowns, so the fed-module just re-dials the relay and resumes within the same Noise session.

The phase 2 doc §6.1 talks about "incarnation_uuid" — and §6.4 talks about device identity revocation. If the Noise session is tied to a specific incarnation... wait, that's about effect_ids not Noise sessions.

Actually I need to think about this more carefully. In phase 2:
- The fed-module establishes a Noise IK session to peer B
- That session is the data plane
- Reconnect scenarios: re-handshake on disconnect

The phase 3 doc adds WsCarrier as a SECOND transport. It says "the Noise session ... [is] unchanged" (§3). The Noise session is end-to-end between the two fed-modules. When carried over TCP, that's a TCP connection. When carried over WS, that's a WS connection. But the Noise session is ONE session — when the carrier changes, does the Noise session get re-handshaked?

Looking again at the phase 3 doc §6: "Everything above the carrier (Noise, strict JSON, effect ledger, forwarder, keepalive/reaping) is carrier-blind." This implies one Noise session that spans carriers. And: "the 4-byte length prefix is redundant inside a message framing but kept identical so record parsing is carrier-agnostic and golden vectors hold." So the fed frames are exactly the same on both carriers.

This is plausible IF the Noise protocol supports binding to neither the carrier nor the IP. Noise IK by itself is over a transport; in practice a fed-module implementation might keep the Noise state alive across TCP/WS reconnects. But this requires explicit code, and the phase 3 doc doesn't detail how.

**Inheritance claim audit:**

(a) "carrier-blind" — phase 2 says nothing about transport adapters. Phase 2 specifies "Noise IK" over a network connection. The phase 3 doc introduces a second carrier. There's no phase 2 statement that says "carriers are interchangeable" or that "a noise session spans multiple transport connections of different kinds." The phase 3 doc is INTRODUCING this property; it's not inherited.

(b) "reaping unchanged" — phase 2 specifies the reaper. The reaper works at fed-module level. The phase 3 doc claims the reaper is unchanged. That's likely true at the fed-module level — but the question is: what event triggers a reap? In phase 2, it's TCP keepalive miss. In phase 3, it's Noise-level keepalive miss (which is carrier-agnostic). If the relay pipe is down, the Noise-level keepalive is missed, and the reaper declares partition. THIS IS A BUG.

The fix: the reaper must distinguish between (a) the Noise session being down and (b) the carrier being down. Or, more simply: if the fed-module can re-establish a fresh carrier (including a new pipe via fresh grant), it should do so without declaring partition. But this means a re-handshake is needed (a fresh Noise IK), which contradicts "session resumption = ordinary re-handshake, cheap" if the handshake is per-pipe rather than per-session.

This is a significant correctness finding. Let me think about it more carefully.

Actually — re-reading phase 2 §6.2:
"On declaring a peer partitioned it closes that peer's loopback connections, so subc-core's connection-granular cleanup delivers deterministic route-GOODBYEs to every consumer of that peer's tools (OutcomeUnknown for in-flight)."

This is severe. If the reaper declares partition because the relay pipe idle-tore, all in-flight calls get OutcomeUnknown. Even though the peer is reachable and the operator can re-establish in seconds, the ledger's recovery reconciliation kicks in (§6.1) and tries to settle via dedup ledger or marks as unknown. If the peer is actually reachable but a fresh handshake takes 1-2 seconds, the entire partition/recovery dance is triggered for what is really a transient carrier event.

This is HIGH severity. The phase 3 doc says "carriers classify exactly like TCP partitions" — but that's BACKWARDS. The desired behavior is: a carrier disconnect should NOT trigger a partition if the peer is reachable via a different carrier. A relay pipe idle-teardown should be a CARRIER event, not a PEER event.

Let me also look at the recovery reconciliation claim. The phase 2 doc §6.1 says:
"recovery queries the SERVING ledger for the effect_id: ledger hit → settle with the recorded outcome; miss + peer reachable → provably not executed → not_sent tombstone; miss + peer unreachable → stays unknown until reachable"

If the relay pipe is re-established within seconds, the fed-module needs to query the serving ledger. But the serving fed-module is reachable (it's the same peer), so the query should succeed. So recovery should work. But it adds latency and bookkeeping to a transient event.

OK let me also think about: "the effect ledger's recovery reconciliation interact[s] badly with relay reconnects (a reconnect mid-effect: does recovery re-query correctly across a NEW pipe?)." The phase 3 doc is silent on this. If the effect was sent but no outcome received, the origin needs to re-query after reconnect. Does the recovery protocol use the same effect_id? Yes (§6.1). Does the recovery work over a new pipe? Phase 2 says recovery queries the serving ledger via the served fed-module. The fed-module-to-fed-module protocol is the Noise session. If the Noise session is re-handshaked (the cheaper path), then the recovery query goes over the new session. If the Noise session is "kept alive" across pipes, then the recovery query goes over the same session with a new pipe. Either way, it should work — IF the reaper doesn't close the loopback connections first.

The crux: the reaper closing loopback connections is what triggers GOODBYE. If the reaper DOESN'T close them (because the carrier event is "soft"), the loopback connections stay alive, the routes stay, and the operator just gets a few seconds of latency. But "soft carrier event" isn't what phase 2 partition semantics do.

**OK so this is a real BLOCKER-or-HIGH finding for the phase 3 design.**

(c) "verified-gate makes discovered peers non-routable" — phase 2 says (§5.3): "TOFU pinning: a peer's pubkey, once learned, is pinned locally; the cloud can introduce a peer but never silently REPLACE a pinned key. First contact is gated, not just detectable: a cloud-introduced pair is non-routable (deny-all exposure) until the out-of-band verification code is compared and confirmed in the CK app on both ends."

So yes, phase 2 supports this. Verified-gate makes discovered peers non-routable until ceremony. Inherited correctly.

But there's a subtle issue: the phase 3 doc §5.5 says "Registry-discovered peers materialize as unverified profile peers (name from registry, verified:false) — the existing enforced gate makes them non-routable until the ceremony." This is correct.

However: §4.3 says "the CK app will render this as compare-and-tap; fed-cli renders it as today." So the pairing UX DOES depend on offer-to-unverified. A discovered peer is unverified, the operator needs to verify it via the ceremony. But what about offer-to-unverified? §10 Q4 asks this question.

Let me look at the cross-from-metadata-to-routability concern: a compromised cloud offers a rogue device. Phase 2 says the verify-code ceremony blocks this because the rogue device's verify-code won't match. So routability requires the operator to confirm the code on a real device. Good.

But what if the operator auto-confirms? The phase 2 doc explicitly notes (§5.3 last paragraph): "a cloud-tier user who skips the code comparison is trusting the cloud at introduction time — the accepted cost of the convenience tier. Manual pairing IS the out-of-band channel (the pairing token carries the key), so the self-hosted tier is structurally immune."

So the inheritance is correct, but the phase 3 doc doesn't push back on the convenience-tier risk. It just says "loud device-join events." The loud events don't help if the user just auto-confirms.

Now let me look at the relay pipe-token properties in detail:

§7 says: "RelayDO: created per pipe grant; bridges exactly two authenticated WebSockets (pipe_id + per-side pipe_token, single-use, short TTL)"

OK so:
- pipe_id is the pipe identifier
- per-side pipe_token: each side gets its own token
- single-use: each token is single-use
- short TTL

But there's an issue: "issued to both sides" (per §5.3: `relay_open {to: pubkey}` → `relay_grant {relay_url, pipe_id, pipe_token}` issued to both sides). So both sides get pipe_token. The question (the prompt asks): "is the token symmetric (both sides get the same) or per-side?"

The phase 3 doc says "per-side pipe_token" — but in the same sentence says "single-use". If both sides need to use the same pipe_id and pipe_token, then the token is effectively symmetric. If per-side, each side has a different token.

This is ambiguous in the doc. The phase 3 doc says in §7: "bridges exactly two authenticated WebSockets (pipe_id + per-side pipe_token, single-use, short TTL)". This suggests per-side tokens. But the doc also says in §5.3: "relay_grant {relay_url, pipe_id, pipe_token} issued to both sides" — singular pipe_token, suggesting symmetric.

The prompt asks: "is the grant issued to 'both sides' — symmetric token or per-side?" — the doc is genuinely ambiguous. This is a finding.

If per-side: device X cannot redeem device Y's token (good, prevents cross-device-grant escalation).
If symmetric: device X can redeem device Y's token (bad). The pipe_id is shared, so even with per-side tokens, the RelayDO needs to track which side connected.

The doc needs to be precise here. This is MEDIUM severity — not a security hole per se, but an ambiguity that could lead to a hole.

**Another relay concern:** "single-use enforcement (where/how?)" — the doc says single-use but doesn't say where the check is. Presumably the RelayDO checks "this token has not been redeemed". If the RelayDO is a Cloudflare DO, the check is in DO state. But the doc doesn't say "the RelayDO stores redeemed tokens" or similar. Also: what's the trust model? The relay is part of the cloud. The grant is also created by the cloud. So a malicious cloud can mint arbitrary grants to itself. This is documented and accepted in §8. The question is whether the design has the right primitive for non-malicious cloud + stolen-token.

If device-token-theft + relay-grant-theft, the attacker can connect to the relay as the device. But the relay only forwards ciphertext — so the attacker can only get DoS or wrong addresses. The actual tool traffic is Noise-encrypted and requires the device's private key. So a stolen device-token gives rendezvous access, not data plane access. Good.

But: device-token-theft allows the attacker to enroll a rogue device via the control plane? Let me check. §4.2 says: "fed-cli login runs the device-authorization flow → provider JWT. CLI calls POST /v1/device/enroll with the JWT + {device_pubkey, device_name, platform}."

So enrollment uses the provider JWT, not the device token. The device token is for the rendezvous control plane. So a stolen device token does NOT let the attacker enroll a rogue device — they need the provider JWT (i.e., they need to authenticate to WorkOS).

What if the attacker has the provider JWT (i.e., has stolen the WorkOS session)? They can enroll a rogue device — but per §8: "Ceremony: rogue device never becomes routable without operator confirming codes on an existing device; join events loud." So the ceremony blocks this.

OK so the device-token threat is well-handled. But:

**Token rotation race:** The doc says "long-lived with rotation." It doesn't specify when/how. If a device is mid-signal (e.g., connect_request in flight) and the device token is rotated, what happens? The rendezvous might reject the in-flight signal because the new token hasn't been presented yet. Or it might accept the old token until expiry.

The doc says "revocable, long-lived with rotation" but doesn't specify the rotation mechanism or the in-flight behavior. This is a finding.

**Revocation race:** "Device removal: fed-cli device remove <name-or-fp> or CK app → DO deletes the record + revokes the token + broadcasts a registry delta." But what if the device is currently connected via the control WS? The DO needs to kill the WS too. The doc says "DO deletes the record + revokes the token + broadcasts a registry delta; peers unpair it." But the doc doesn't say "and the device's open control WS is closed." If the WS isn't closed, the rogue device can still send registry updates and signaling even after revocation. This is a finding.

Actually: revoking the token means subsequent requests fail. But the open WS might not check the token mid-stream — it might be checked at hello time only. The doc is silent on this. If the WS is per-message authenticated (e.g., every frame includes the token), then revocation is immediate. If it's hello-only authenticated, there's a window. This is a finding.

**Registry candidates self-reporting:** §5.2: "self-reported, refreshed on connect + change." The doc says "candidates are self-reported." §10 Q2 asks about hardening. A malicious sibling device on the same account can lie about its LAN addr. The current answer: "it can only redirect its own inbound dials; Noise pins identity regardless."

But wait: if device B is the malicious sibling, it can report a fake LAN address. When device A dials device B, device A will try the fake LAN addr. This is a denial of service or, worse, a connection to an attacker's machine. But Noise IK pins the identity: when device A dials, the Noise handshake authenticates device B's pubkey. If the fake LAN addr actually points to a third machine C, the handshake will fail (no matching static key) — so it's DoS only, not impersonation.

OK so the "self-report" claim is actually safe because of Noise IK. But the doc could be more precise about the trust model.

**Connect offer to unverified:** §5.3 says "connect_request {to: pubkey} → relayed to the target as connect_offer {from: pubkey, candidates}". This is per-§10 Q4, "current: allowed, loud." The threat: a malicious device enrolls, then floods every online device on its account with connect_offers. Each target wakes up, sees a "compare-and-tap" prompt, and if the user auto-confirms... but the verify-code won't match (per §5.3). So it's just a denial-of-service / annoyance, not a security hole.

But wait — if the target is OFFLINE and a connect_offer arrives, does the target get woken up? The phase 3 doc says "pushes registry deltas and signaling messages" via the control WS. If the target is offline (no open WS), the signaling message is queued or dropped. If dropped, the initiator retries; if queued indefinitely, it's a memory leak. The doc is silent.

**Candidate racing:** §5.4: "On connect_offer/accept, the initiator (pubkey tie-break as phase 2) tries candidates in order: lan → public → relay, with a short per-candidate timeout (~2s) and first-success-wins."

The phase 2 pubkey tie-break: "the initiator (pubkey tie-break as phase 2) tries candidates in order" — but I don't see explicit pubkey tie-break in the phase 2 doc. Let me re-read.

Phase 2 says "the initiator (pubkey tie-break as phase 2)" but the phase 2 doc doesn't specify what the pubkey tie-break IS. The phase 2 doc's §5.4 talks about caller-identity vs execution-identity. The phase 2 doc's partition section uses "the fed-module's keepalive reaper" — no tie-break. The phase 2 doc doesn't actually have a "pubkey tie-break" mechanism. So the phase 3 doc is referring to something that isn't in phase 2.

Actually wait, the phase 2 doc's design does say "first-success-wins" implicitly via "the winning transport is remembered per peer and retried first next time, with periodic re-probes for direct upgrade while on relay" — no, that's phase 3.

Let me search the phase 2 doc for "tie-break"... I don't see it. This is an INHERITANCE claim that doesn't check out. The phase 3 doc says "initiator (pubkey tie-break as phase 2)" but phase 2 doesn't have a documented pubkey tie-break.

This is a finding.

Wait, maybe the "pubkey tie-break" refers to "the side with the lexicographically smaller pubkey is the initiator" — that's a common Tailscale pattern. But it's not in the phase 2 doc. So phase 3 is asserting inheritance of a non-existent mechanism.

Actually re-reading: §5.4 says "the initiator (pubkey tie-break as phase 2)". The "as phase 2" might be referring to a convention in the original fed-module code (not the design doc). Let me search for it in the actual code... but I don't have access to the actual fed-module code. The design doc is what I'm reviewing. The doc says "as phase 2" but phase 2 doesn't document this. So at minimum the phase 3 doc is unclear about what mechanism is inherited.

**Candidate racing simultaneous wins:** §5.4 says "first-success-wins" but what if LAN and relay both succeed in close succession? The Noise session is one; the carrier is the abstraction. If two carriers "win" simultaneously — does the noise session get bound to one? The phase 3 doc says "Everything above the carrier (Noise, strict JSON, effect ledger, forwarder, keepalive/reaping) is carrier-blind." This means the fed-module picks ONE carrier at a time. If a second carrier establishes a successful Noise session while the first is already running, the second is closed and its connection is dropped.

But the doc doesn't say this explicitly. There's ambiguity about double-win resolution.

Also: when a carrier establishes a Noise session, what's the cost? "Ordinary re-handshake, cheap" — the phase 3 doc claims it's cheap. But "cheap" relative to what? Each candidate dial that gets a TCP SYN-ACK or a WS upgrade then runs a full Noise IK handshake (~2 roundtrips). If the fed-module is racing 3 candidates and 2 succeed, the fed-module has done 2-3 handshakes before realizing. If the keepalive timer fires during this, the reaper might get confused.

**WS carrier framing:** §6: "Noise handshake + fed-frames over WebSocket binary messages (one fed record per WS message; the 4-byte length prefix is redundant inside a message framing but kept identical so record parsing is carrier-agnostic and golden vectors hold)."

The doc says "one fed record per WS message" — but it doesn't say what happens if a WS message contains 0 records (control message? padding?) or 2+ records. The phase 2 doc has fed-frames as a stream of length-prefixed records. A WS message is a separate framing layer. If the WS message length and the length prefix disagree, what's the policy?

Also: who does the WS message→fed-record mapping? The fed-module? The WsCarrier? The doc says "the WsCarrier" but doesn't specify. If the WsCarrier emits one fed record per message, and the receiver also expects one, then any mismatch is a parse error. The doc should specify:
- Is the WS message size bounded (e.g., 16 MiB)?
- What if a WS message has 0 bytes? Close? Error?
- What if the length prefix says 1 MB but the WS message has 1 MB+1?

The doc says (§7) "enforces the 16 MiB frame cap" — so messages >16 MiB are dropped. But that's the relay's cap, not the carrier's. The WsCarrier itself might have a different cap.

**Keepalive/reap over a relay pipe with idle teardown:** as discussed above, this is a significant issue.

**Recovery reconciliation across a new pipe:** also discussed.

**Noise session resumption:** the phase 3 doc says "Noise session resumption = ordinary re-handshake, cheap." This is at §7. Reading carefully: "idle timeout tears the pipe down (peers reconnect via a fresh grant — Noise session resumption = ordinary re-handshake, cheap)."

I think this is saying: when the pipe is re-established, the Noise session is resumed (i.e., re-handshaked). The "cheap" is a claim that re-handshaking is acceptable cost. But it does mean: every pipe teardown = full Noise re-handshake = new Noise session = new ephemeral keys. The previous session's state (e.g., sequence numbers, anti-replay window) is lost. This means:
- If the reaper uses the Noise session as the keepalive carrier, every pipe teardown is a session change. Anti-replay state resets. Not a security issue (ephemeral keys are fresh), but a state-management issue.
- The effect_id is per-session-agnostic (it's keyed on origin_device_pubkey + incarnation_uuid + seq), so effects are durable across sessions. Good.
- The dedup ledger on the serving side is keyed on effect_id, so it's durable across sessions. Good.

So this is OK. But the "cheap" claim is unverified. A full Noise IK handshake is ~2 RTTs over the relay (which is ~1 RTT to the relay + the relay forwards). If the relay is 100ms away, that's 200-400ms. Over a slow connection, 1-2s. The keepalive cadence (let's say 30s) and idle timeout (let's say 60s) might not have time to re-establish before the next idle timeout.

The doc is silent on:
- Keepalive cadence
- Idle timeout
- Re-handshake timeout
- Max re-handshake retries

**Registry staleness:** the doc says "candidates [are] self-reported, refreshed on connect + change." If device A's candidate list goes stale (e.g., device A's IP changed but the registry hasn't been updated), device B might dial a dead address. The doc says the dial will fail that candidate and move to the next. But what if the relay candidate is ALWAYS available? Then the dial succeeds via relay even though device A is actually reachable on a direct path. That's a perf issue (latency through relay), not a correctness issue.

But there's a security angle: a malicious cloud could give device A a wrong public candidate (poisoned candidate), causing device B to dial a third machine C. C can't impersonate A (Noise IK fails), but C can launch a DoS or scan. This is acknowledged in §5.3: "A malicious rendezvous can deny service or hand out wrong addresses."

**DO-per-account hot spot:** §5.1 says "one Durable Object per account." This is a hot spot. For an account with N devices, all signaling goes through one DO. Cloudflare DOs have throughput limits. If a busy account has lots of signaling, it could hit limits. The doc says "WS hibernation keeps idle accounts at ~zero cost" — but active accounts are not hibernating.

The doc doesn't quantify:
- DO request rate limits
- Per-account signaling rate limit
- WS hibernation wake latency (mentioned in §10 Q5)

**WS hibernation vs signaling latency:** §10 Q5: "WS hibernation vs signaling latency — acceptable to pay a cold-wake round-trip on first signal to an idle account?" The doc doesn't answer this. If a device enrolls and the AccountDO is hibernating, the first connect_request might trigger a cold wake. Cloudflare DO hibernation wake is typically 200-500ms (or more). If the signaling timeout is ~2s, this fits, but it's tight.

The doc doesn't specify the signaling timeout.

**Device clock assumptions:** §4.2 says "long-lived with rotation." §5.2 says "last_seen_ms." §7 says "short TTL" for pipe_token. All of these use time. If a device's clock is skewed or rolled back:
- last_seen_ms could be wrong (mostly fine, just stale display)
- TTL on pipe_token could be misjudged by the cloud (if device clock vs server clock disagrees). But the TTL is enforced by the cloud, not the device. So the device's clock doesn't matter for the cloud's TTL.
- Token rotation: if the device uses time-based rotation, a rolled-back clock could cause issues. The doc says "long-lived with rotation" but doesn't specify time-based vs counter-based.

**Open questions:**

1. **Device-token custody on disk (0600 file next to device key today) — sufficient for v1, or fold into the credentials vault immediately?**
   - The phase 2 doc doesn't talk about credentials vault for device keys. The phase 2 doc §5.2 talks about the federation module being reserved + nonce-bound. The device token is a new artifact.
   - If the device key is in a credentials vault (e.g., Keychain on macOS, libsecret on Linux), the device token should be there too. Putting it in 0600 is a downgrade.
   - Recommendation: fold into the credentials vault immediately. The credentials vault exists; using it costs little and is more secure.
   - The phase 3 doc has a Bug here: §10 Q1 frames the choice as 0600 vs vault. The right answer is vault (it's the same cost).

2. **Registry candidates self-reporting — hardening needed against a malicious sibling device lying about its LAN addr?**
   - The Noise IK pins the identity, so a sibling can lie about its OWN addr, but can't make another device's Noise handshake succeed against the sibling's pubkey.
   - But: a sibling COULD make OTHER devices dial a fake LAN addr that points to a third party. The third party can't decrypt, but they can DoS / fingerprint.
   - Hardening: don't allow self-reported LAN candidates from a sibling that hasn't been verified by the operator. But the operator trusts the sibling by virtue of having added it. The verify-code ceremony doesn't prevent the sibling from lying about its own IP.
   - Recommendation: at minimum, log + alert when a peer fails to handshake at a "self-reported" candidate. A peer that reports candidates that consistently fail handshake is suspicious.
   - Or: drop the self-reported LAN candidate unless the device is verified (post-ceremony). But that breaks the pairing UX (you can't discover a peer via LAN without verifying first).
   - The real answer: it's an accepted risk, document it. The Noise IK pinning means it's at worst DoS, not impersonation.

3. **Relay pipe lifetime: per-connection grants vs a standing pipe per peer-pair — is reconnect-per-idle-teardown churn acceptable at keepalive cadence?**
   - Per-connection grants (the current design) means: every pipe teardown = new grant roundtrip = new Noise handshake. If the keepalive cadence is 30s and idle teardown is 60s, the re-handshake must complete in 30s.
   - A Noise IK handshake is ~2 RTTs. Over a relay, ~400ms-1s typically. 30s is plenty.
   - But: if the user is on a slow connection or the relay is cold, this could blow the keepalive window.
   - Recommendation: use a standing pipe (with idle teardown only after long inactivity, e.g., 5 minutes) for active peer-pairs. Per-connection grants are wasteful.
   - Alternative: keep the Noise session state across pipe teardowns, only re-handshake on peer-level disconnect.
   - The doc is silent on keepalive cadence and idle timeout. This is a finding.

4. **Should connect_offer require the target to be verified before signaling is relayed (quieter unpaired devices), or is offer-to-unverified needed for the pairing UX?**
   - If the target is unverified, the operator can still pair (the verify-code is generated). But "quieter unpaired devices" means: don't even offer to them.
   - The current design: "allowed, loud" — offer is relayed, the target sees a loud "incoming connection from <pubkey>".
   - The pairing UX: when device A pairs with device B, B is currently unverified. A connects to B; B receives an offer; B verifies A's code; A verifies B's code. So offer-to-unverified is REQUIRED for the pairing UX.
   - Recommendation: keep offer-to-unverified, but with strict rate limiting. A compromised cloud can flood connect_offers, but per-account rate limit caps the damage.

5. **WS hibernation vs signaling latency — acceptable to pay a cold-wake round-trip on first signal to an idle account?**
   - Cloudflare DO cold wake is typically 200-500ms (sometimes longer). The signaling timeout (~2s for connect_request) is tight but workable.
   - If the cold wake pushes past the timeout, the request is dropped and the operator retries.
   - Recommendation: yes, acceptable. The cost is on the first signal only. Subsequent signals within the same "warm window" are fast. If it becomes a UX issue, pre-warm the DO via a periodic heartbeat.
   - The doc should specify the cold-wake latency target and the signaling timeout.

**Now let me organize my findings.**

I'll structure them as a STRUCTURED FINDINGS LIST with severities. Let me also look at the actual code if any exists to verify some claims.

Let me first check if there's any related code.

[assistant] 2026-07-07T11:58:35Z
Now I have a clear picture of both docs. Let me read them carefully and analyze for the audit.

Let me start by going through the phase-3 doc and verifying each inheritance claim against the phase-2 doc. The key claims are:
1. "carrier-blind" (above the carrier is unchanged) - phase 2 §2.2 Noise IK E2E
2. "reaping unchanged" - phase 2 §6.2 keepalive reaper
3. "verified-gate makes discovered peers non-routable" - phase 2 §5.3 TOFU, §2.6 P1, §2.3 federation_exposure
4. "relay-path partitions classify exactly like TCP partitions" - phase 2 §6.2 keepalive
5. "Noise session resumption = ordinary re-handshake, cheap" - phase 2 §6.1 effect_id/incarnation
6. "loud device-join events"
7. "4-byte length prefix (redundant but kept)"
8. "one fed record per WS message"

Let me now think hard about each axis:

## Axis 1: SECURITY

### Device-token lifecycle
- Device token is opaque bearer bound to (account_id, device_pubkey), stored hashed in DO
- "Long-lived with rotation" - how? When?
- Stolen token → can impersonate signaling (registry, send connect_request, accept offers) but cannot decrypt (no private key)
- BUT: a stolen token can offer a rogue device for pairing! Let's check §4.3: "A cloud/account compromise therefore yields metadata + the ability to *offer* a rogue device for pairing — it cannot make any existing device trust the rogue one." So a stolen token doesn't make the rogue device routable without the ceremony.
- BUT — the rogue device needs a stolen token to be IN the registry, and then offers the operator a pubkey. The operator confirms the code. The rogue device is now routable. So the impact is that the ceremony is the line. OK.
- Token rotation: "Day-to-day rendezvous auth uses this token; the provider is out of the loop until the operator re-logins." - rotation means re-login? Or periodic? Unclear.
- Revoke vs in-flight: when you `device remove`, peers "unpair it" — but a peer mid-connect_request is in the registry. Race: token is revoked while a connect_request is in flight to the device being removed. The connect_request has already been relayed, target is going to accept... what happens?
- A bigger problem: token theft + the legacy of "device_token on disk" (Q1) — if a sibling-device on the same account reads the file (note: they are in the SAME account, so the device_token doesn't grant cross-account power, but they can issue a connect_offer for the legit device). Wait — but the device is in the registry already with its real pubkey. A sibling can't impersonate the device's pubkey. So a stolen token gives the attacker only signaling-plane access, not Noise plane.

### Enrollment races
- Two devices enrolling the same pubkey: but the private key is unique to the device. If a rogue device has the same pubkey, that means it has the private key — so the legit device's private key was copied. That's a real device compromise.
- Pubkey re-enrollment after removal: §4.2 says "device removal: DO deletes the record + revokes the token + broadcasts a registry delta; peers unpair it (profile entry flips to `revoked_by_account`, non-routable, requires re-pair if re-enrolled)." So the new enrollment with the SAME pubkey (which requires the private key) needs re-pair. OK.
- Account-id resolution races: federated ID minting keyed by provider subject — if two devices enroll concurrently with the same WorkOS JWT, both will try to mint account_id. Race: the DO may not exist yet, both requests try to create it. Cloudflare DOs are created by `idFromName` from a Worker, but the namespace setup is global. The race is whether two requests can both create the DO without conflict. With Cloudflare, the first one to call `get()` will create it; the second sees the existing one. But the create-or-lookup race in Worker code can lead to: first request mints account_id, second request gets the same DO via routing, but the second request's POST handler may still be in the "create account_id" branch.

### Signaling abuse
- connect_request floods: there's no rate limit specified. A compromised account can flood. Mitigation: per-account quotas are mentioned in §5.5 of phase 2 ("per-account/per-device quotas + rate limits") but NOT in phase 3 §5. This is a gap.
- Candidate poisoning: the candidates list is self-reported + public is server-observed. A sibling device can lie about its LAN address to redirect its own inbound dials — but a malicious sibling cannot redirect a legitimate device's dials. The issue: if the sibling lies about its LAN address, an INITIATOR will dial the wrong address. The legitimate device is the one that placed the lie. So the impact is limited to that device's own dials — but it can be used to MITM the data plane by having the initiator dial the attacker's LAN address. Wait, but Noise IK pins keys. If attacker has different key, the handshake fails. If attacker has the same key (private key compromise), the device is already compromised. So the LAN spoofing is bounded.
- BUT: even with no key compromise, a LAN-spoofed candidate can be used for DoS: legitimate initiators fail to connect to the device.
- Replay: "Signaling carries no secrets and no trust decisions: candidates and pubkeys only" — so replay of signaling messages would have to be defended by the underlying transport (TLS to Worker) + a non-replayable property. The phase-3 doc says nothing about nonces, timestamps, or message IDs in signaling. Without a nonce, a replayed connect_request can be re-delivered. The connect_offer doesn't carry state, but a replayed registry_delta is at most redundant. The replay of a relay_grant is the most dangerous: see below.

### Relay pipe-token properties
- "single-use, short TTL" — good. But:
  - Where is single-use enforced? If A consumes the grant, can B consume it? "issued to both sides" — this is the key issue. If both sides receive the SAME pipe_token, then either side can redeem it, and the other side's redemption could be a replay. If the pipe_tokens are per-side, that's a different model.
  - The doc says "pipe_id + per-side pipe_token, single-use, short TTL" — so it IS per-side. Good. But where is the per-side assertion? "per-side pipe_token" in §7. OK.
  - "single-use" — meaning one use per side? Or one use period? In the relay DO, each side presents the token to establish their pipe leg. If the same side's token is reused, that's a problem. The phrase "single-use" needs definition: do you mean one TCP/WS connection to the relay can be established per token?
  - TTL: short, but what's the value? The signaling round-trip plus one retry. Not specified.
  - Who can consume whose grant? Per-side tokens are bound to (pipe_id, side), so device X can't redeem device Y's token. OK.
  - Symmetric vs per-side: explicitly per-side. OK.

### Cross from metadata to routability
- "test the claim that verify-code makes this impossible in EVERY window" - the pairing UX window is the dangerous one. The phase-3 doc says offer-to-unverified is allowed (§10 Q4). So in the pairing window, a rogue device can offer. The ceremony is supposed to stop it. The user can refuse. OK.
- BUT: in the pairing UX window, the INITIATOR is going to dial. The candidates include relay. The initiator dials — to a pubkey that may not match. The user is told "do these numbers match?" If the user types "yes" without checking, the rogue is trusted. This is the documented residual in phase-2 §5.3: "a user who dismisses the code prompt on the cloud tier is vulnerable." So that's an accepted residual.
- HOWEVER: there's a more subtle attack. If a compromised account on the rendezvous can rewrite the registry (say, replace device B's pubkey with attacker's pubkey), then device A dials "device B" but the candidates are now for the attacker's pubkey. Wait — but A and B were already paired; A's local profile has B's pubkey pinned. The registry is for discovery, not for routing. Per phase-2 §5.3: "a peer's pubkey, once learned, is pinned locally; the cloud can introduce a peer but never silently REPLACE a pinned key." Good. So this is defended.
- BUT in the FIRST CONTACT (not yet pinned), the registry IS the source of identity. So a compromised rendezvous can introduce a rogue device for FIRST CONTACT and the user might type "yes" to the code without checking. This is the residual. Phase-3 doc says "loud device-join events" — so the user gets a notification, but the door is still open.
- The doc claims in §4.3: "the rogue device's verify-code will not match anything the operator's real devices display" — this assumes the user is comparing. The pairing UX shows the code on BOTH sides, so the operator checks both. The malicious rendezvous can offer a rogue for A, but when A displays the code, A says "compare with device B"; device B is the rogue. So the operator must compare with the REAL B, not the rogue. If the operator is told "the code is X on A" and "the code is Y on the rogue B", the operator must say "no match" and refuse.
- The question: does the pairing UX work for first-contact initiated by the cloud (no in-person meeting)? If device A's "compare with device B" requires the operator to walk over to B... then cloud-first-contact is a UX hole. This is the F4/F5 decision: "compare-and-tap" — how is this done in a cloud-first-contact scenario where the user is at device A and device B is miles away?
- The phase-2 doc says the code is on BOTH devices. For a cloud-introduced pair, the user has both devices (say, laptop and home server). They're not in the same room. So how does the user compare? The CK app shows the code on A; the user must read it on B (or vice versa) and confirm. This is the cloud-friction-residual.
- So: the verify-code ceremony is the line. The pairing UX is loud. But the verify-code itself is the user-confirmation step. If the user confirms, they're trusting.
- The question: can a compromised rendezvous MITM the verify-code itself? If the rendezvous controls the code on each side, it could show the same code on both sides, with the attacker's pubkey. Then the user compares and they match, and trusts the attacker. Wait — does the verify-code depend on pubkey? Phase-2 §5.3: "The code is derived from both endpoints' long-term device static keys (never session/ephemeral material), so it binds the pair, not the conversation." So the code is derived from the pubkey. The rendezvous doesn't have the pubkey of the rogue — the rogue enrolled with its own pubkey. So the code on A is a function of A's pubkey + rogue's pubkey. The code on B (the rogue) is a function of A's pubkey + rogue's pubkey. They match — because they're both for the SAME pair. So the user compares and the codes MATCH, but the rogue IS the device being paired. This is the residual.
- In other words: the verify-code ceremony only catches MITM (where the rendezvous substitutes a DIFFERENT key in the middle). It does NOT catch a rogue device that is who it claims to be. The protection is that the rogue has to be physically present or the user has to be tricked into enrolling it. The "loud device-join events" is the only protection.
- Actually wait: the verify-code on A is shown when A is being asked to pair with B. The code includes A's pubkey + B's pubkey. The user looks at A and sees code X. The user goes to B and sees code X. If the rogue is pretending to be B, the user will compare A's "X" with the rogue's "X" — and they match. The user is supposed to verify that B is the device they expect to be pairing with. The UX needs to also show B's identity (e.g., name, fingerprint) and the user must verify that identity outside the code. The phase-2 §5.3 says "rotation requires a chain signed by the old key OR re-verification identical to first contact" and "the code binds the pair" — so the code is a strong proof of "you are who I think you are", not a proof of "you are the device I expected."
- The "loud device-join events" is the only first-contact signal. If the user ignores it, they're owned.
- The phase-3 doc §4.3 says: "the rogue device's verify-code will not match anything the operator's real devices display" — this is the claim. Is it true? If the rogue is being introduced as a new device, the operator's REAL devices won't have a pairing relationship with it. So the operator's real devices won't display a code at all (the ceremony only happens on the two devices being paired: the existing device and the new one). The phase-3 doc is wrong here: the operator's OTHER devices (not in the pairing pair) don't display the code. Only the two devices in the pair display it. So the comparison is between the new device and ONE existing device, not all existing devices.
- This is actually a problem in the cloud-first-contact scenario. The doc claims safety in §4.3 by saying "the rogue device's verify-code will not match anything the operator's real devices display" — but the ceremony only runs between the initiator and the target. Other devices don't display.
- This is a phase-3 claim that doesn't survive careful reading.

### Let me move on to other axes.

## Axis 2: CORRECTNESS

### Candidate racing
- Both sides dialing simultaneously. Phase-3 §5.4 says "the initiator (pubkey tie-break as phase 2) tries candidates in order: lan → public → relay." The pubkey tie-break determines which side is the initiator. Both sides dial simultaneously, but only the initiator drives the connect. The acceptor waits.
- BUT: what if both sides start dialing before the tie-break is resolved? The phase-3 doc says the initiator tries candidates, but doesn't specify what the responder does. If the responder is also dialing, then both sides may establish two Noise sessions — one over TCP, one over WS. The phase-2 doc says: "subc-core is always loopback-only on every machine" and "one loopback connection per (peer, remote module)." The fed-module has ONE Noise session per peer pair. Two carriers means the Noise session is over one carrier. The other carrier's WS would be torn down or used as failover.
- The phase-3 doc says the "winning transport is remembered per peer and retried first next time, with periodic re-probes for direct upgrade while on relay." OK, but what about double-wins in the same moment? If both sides establish simultaneously — TCP session completes on side A, WS session completes on side B. Both sides' state machines see a connection. This is a real race.
- The phase-3 doc needs to specify: only one carrier "wins." The tie-break is the pubkey. The pubkey-high side is the initiator; the pubkey-low side is the responder. The responder does NOT dial; it accepts on the listening port. So there's no double-dial. The phase-3 doc says "the initiator (pubkey tie-break as phase 2) tries candidates" — so only the initiator dials. The responder just listens. The same candidates are pre-arranged. So the design avoids the double-dial. OK.
- But wait: the WS carrier is the relay. The responder needs to dial the relay too (the relay is outbound). So the responder dials the relay URL. But the responder doesn't dial the TCP candidates. So: initiator dials TCP+relay, responder dials relay only. If TCP connects, the responder sees it on the listening port. If TCP fails, both go to relay.
- Race: initiator's TCP dials succeed, responder's relay dial succeeds. Initiator sends Noise handshake over TCP. Responder is busy with relay. Now the responder has two "connections" incoming. The phase-2 fed-module logic: "one loopback connection per (peer, remote module)" — so the fed-module can only have ONE Noise session per peer. If the responder's relay dial has already established a Noise session, the TCP handshake on the responder will fail (the responder's fed-module has a live session; TCP-side dials are rejected?). Or the responder accepts the new session and tears down the relay? The phase-3 doc is silent on this.
- This is a correctness issue.

### WS carrier framing
- "one fed record per WS message + retained 4-byte length prefix (redundant but kept)." 
- The WS message boundary is one record. The 4-byte length prefix is redundant because WS messages are already length-delimited. The receiver's parser is fed exactly one record per message. Mismatch: what if a WS message has !=1 record (e.g., 0 records, or 2+ concatenated)? The parser would error.
- The doc says: "any ambiguity — e.g. a WS message carrying !=1 record, or a length prefix disagreeing with the WS message length?" — this is what the audit is asking.
- The doc says nothing about mismatch handling. The receiver would need to enforce "exactly one record per message" and on mismatch, kill the connection. This is not specified.
- Also: the 4-byte length prefix. If the prefix disagrees with the message length, the parser would read the wrong number of bytes. The doc says "redundant but kept" — so the design accepts the redundancy and relies on the WS message boundary for framing. But the 4-byte prefix is parsed, so if it's wrong, parsing fails. The behavior on mismatch is unspecified.

### Keepalive/reap over relay with idle teardown
- RelayDO idle timeout tears the pipe down. From the fed-module's perspective, this looks like the peer's transport went away. The reaper sees missing keepalives, declares partition, closes the per-peer loopback connection. The phase-2 doc §6.2: "the fed-module's keepalive reaper is the AUTHORITATIVE partition classifier. On declaring a peer partitioned it closes that peer's loopback connections." So the reaper sees the relay teardown, declares partition, closes loopback.
- Effect ledger interaction: the in-flight effect on the disconnected session is `intent durable, send unconfirmed` per phase-2 §6.1. Recovery reconciliation: on fed-module restart, queries the serving ledger. Wait — fed-module doesn't restart. The fed-module's session is reaped. The recovery reconciliation is for crash recovery, not for partition. Partition: in-flight effects get `OutcomeUnknown` at the call site (the connection is closed).
- The relay re-establishes via a fresh grant (phase-3 §7). The Noise session is re-handshaken. The effect_id has the incarnation epoch. So a re-handshake gets a new incarnation. The effect_ids in flight had the OLD incarnation. They are not at-most-once-safe across the new incarnation.
- The phase-2 doc §6.1: "effect_id = (origin_device_pubkey, incarnation_uuid, seq). The incarnation UUID is minted whenever the origin's send-log db is created (fresh install, db loss, restore-from-backup) and stored IN that db." — so the incarnation UUID is per-DB-creation, not per-session. A re-handshake does NOT mint a new incarnation. So the same effect_id can be safely re-sent after a partition/recovery.
- But wait: phase-3 §7 says "Noise session resumption = ordinary re-handshake, cheap." Phase-2 §6.1 says the session incarnation is per-DB-creation. So the "resumption" inherits the same incarnation, and dedup is safe. OK.
- The phase-3 doc's claim is correct on this point.
- BUT: there's a subtle issue. The recovery reconciliation (§6.1) handles fed-module RESTART, not partition. A partition doesn't restart the fed-module. So in-flight effects are `OutcomeUnknown` at the call site. The caller can re-invoke with the same effect_id (since the incarnation is unchanged). The serving side's dedup ledger has no record (effect never delivered). So the re-invoke would be a duplicate. WAIT: the origin hasn't marked the effect as sent-confirmed, so the recovery reconciliation will fire on next startup? No — recovery only fires on startup. A partition doesn't restart the fed-module. So the in-flight effect is `intent durable, send unconfirmed`. The serving side has NO record (the effect was sent but the relay dropped it before the serving side received it — or the serving side received it but the ack was lost). On re-invoke with the same effect_id, the origin re-sends. The serving side's ledger has no record. The serving side re-executes. Duplicate.
- This is a subtle interaction: the phase-2 doc §6.1 says "A re-arriving effect_id with a ledger row returns the RECORDED outcome without re-dispatch — what makes a legitimate origin re-send safe even when the first send actually arrived (lost-ack)." So if the serving side DID receive the first send and ack was lost, the ledger has the outcome. If the serving side did NOT receive the first send (network drop), the ledger has nothing. The re-invoke creates a duplicate.
- This is the at-most-once tradeoff: at-least-once on the network, exactly-once at the serving module. The phase-2 doc is honest about this: "Phase-0 test vectors must cover each row." The phase-3 doc doesn't address whether relay reconnects preserve this.
- Specifically: the relay idle timeout tears the pipe. The fed-module sees disconnect. The reaper fires. The loopback connection closes. The origin's effect is `OutcomeUnknown`. The caller can re-invoke. The new Noise session is established via the relay. The same effect_id is sent. If the serving side received the first send (but the ack was lost in the relay teardown), the dedup ledger returns the recorded outcome. If the serving side did NOT receive the first send, the re-invoke is a duplicate.
- The phase-3 doc claims "relay-path partitions classify exactly like TCP partitions." Is this true? Yes, the reaper sees the disconnect, declares partition, and the effect is `OutcomeUnknown`. The caller can re-invoke. The dedup ledger on the serving side handles it. So functionally, yes, it classifies the same.
- But the doc says "Noise session resumption = ordinary re-handshake, cheap." Phase-2 doesn't have "session resumption" — it has effect_id-based dedup. The "session" in Noise terms is the cryptographic session; the "incarnation" in fed terms is the DB. A re-handshake is a new Noise session with the same fed incarnation. The doc is using "resumption" loosely. The dedup is the same. OK, the claim is correct.

### Registry staleness
- Candidates change mid-dial. If the target updates its public IP, the initiator has a stale candidate. The dial fails for that candidate. Falls to the next. OK.
- Device online flaps. The reaper keeps the device "offline" but the registry says "online." The dialer sees "online" and tries; the candidates are stale. Fails. The fed-module would need to handle the registry-vs-reality gap.
- A stale candidate list causing dial to a dead/wrong endpoint: this is the DoS-by-sibling attack from earlier. The sibling lies about its candidates; the dialer dials a dead endpoint. Bounded by key-pinning, but causes availability issues.

## Axis 3: OPERATIONAL

### DO-per-account hot spots
- One DO per account funnels all signaling + registry. A busy account (many devices, many peers) is one DO. Cloudflare DOs have per-DO CPU/memory limits. This is a real operational concern. The phase-3 doc doesn't address it.

### WS hibernation wake latency
- WS hibernation: when a DO has no active connections, it hibernates. The first WS upgrade wakes the DO. Cold-wake latency is ~hundreds of ms in Cloudflare. The phase-3 doc §10 Q5 asks about this.
- The connect_request deadline: the doc says "short per-candidate timeout (~2s)." 2s is plenty for a cold wake. But if there are multiple peers and the DO is hibernated, the cold-wake for the first request takes the hit; subsequent ones are warm. OK.
- More important: the registry_snapshot and the connect_offer delivery. If device B is hibernated, the Worker upgrades its WS, the DO wakes, delivers the offer. The offer's TTL is short. The first connect_request from device A wakes the DO, delivers the offer to B. B processes it, returns connect_accept. If B is hibernated and slow to wake, the offer may time out.
- This is a real latency concern, especially for the "single login makes subcs connect" UX where the user expects <1s.

### Rendezvous DOWN: degradation
- The phase-3 doc §5.5: "New `[rendezvous]` profile section: `{account: true, control_url, device_token_path}`. Absent → phase-2 behavior exactly (static profiles keep working forever; the WAN test rig never needs an account)." OK.
- BUT: what if the profile HAS `[rendezvous]` and the rendezvous is down? The doc doesn't specify the fallback. If the fed-module's control WS is down, can the device still reach a peer via static profile? The phase-3 doc says "Static `addr` in a profile acts as one more candidate (highest priority) for hybrid setups." So static is a candidate in the dial. But the dial is initiated by a connect_offer, which comes from the rendezvous. If the rendezvous is down, there's no connect_offer. The static candidate is used by the manual-pairing flow (phase-2). The rendezvous-dependent flow is broken.
- So: a device with only rendezvous-discovered peers has no connectivity if the rendezvous is down. The static profile is independent. The doc needs to say: if the rendezvous is down, the device continues to use the static profile exactly as phase-2. The static profile is independent of the rendezvous. The doc should make this explicit.
- Actually, re-reading: "Absent → phase-2 behavior exactly." So if the section is absent, phase-2. If present and down, the doc doesn't say. This is a gap.

### Device clock assumptions
- TTLs, last_seen, token expiry. If the device clock is skewed or rolled back, TTLs could be bypassed. The doc doesn't specify whether TTLs are device-clock or server-clock.
- "last_seen_ms" in the registry is stamped by the server (on hello), so server-clock. OK.
- "short TTL" on the pipe token is server-side (RelayDO), so server-clock. OK.
- The device-clock issue is more subtle: if the device's wall clock is rolled back, the device's connect_request's notion of "offer not received yet" could be wrong. But the connect_request is not TTL-bound on the device side; it's a fire-and-forget.
- The real device-clock issue: if the device's keepalive cadence is clock-based and the clock is wrong, the reaper could falsely declare partition. But keepalive is typically timer-based, not wall-clock.
- This is a low concern, but worth noting.

## Now let me check the inheritance claims explicitly:

### "carrier-blind"
- Phase-2 §2.2: "subc-transport is symmetric-HMAC with plaintext frames" — the existing transport is symmetric-HMAC. The phase-3 doc says "fed-transport grows a second carrier under the same Noise session code." So the fed-transport in phase 3 is a NEW layer (Noise IK), not the phase-2 subc-transport. The phase-2 doc says the WAN data plane is Noise IK. So the "fed-transport" in phase-3 IS the phase-2 WAN data plane, just with a new carrier. The "carrier-blind" claim is that everything above the carrier (Noise, strict JSON, effect ledger, forwarder, keepalive/reaping) is unchanged. This is a design claim; if true, the fed-module's state machine is carrier-agnostic. The phase-2 doc doesn't say the state machine is carrier-agnostic; it says the WAN is Noise IK. The phase-3 doc extends this by adding a WS carrier. The claim is plausible but not directly supported by phase-2.

### "reaping unchanged"
- Phase-2 §6.2: "the fed-module's keepalive reaper is the AUTHORITATIVE partition classifier." The reaper is the same; the carrier is different. The reaper's timer + check is carrier-agnostic. So the claim holds. The phase-2 doc doesn't explicitly say "carrier-agnostic," but the design is such that the reaper operates on the carrier's liveness signal, not on the carrier's internals. So the reaping is unchanged in behavior.

### "verified-gate makes discovered peers non-routable"
- Phase-2 §2.3 default-deny `federation_exposure`. §3.3: profiles have peers with public keys. §5.3: TOFU ceremony. A discovered peer (from the registry) is added to the profile as an unverified peer. The phase-2 verified-gate means it's non-routable until the ceremony. The phase-3 doc §5.5: "Registry-discovered peers materialize as unverified profile peers (name from registry, `verified:false`) — the existing enforced gate makes them non-routable until the ceremony." This is consistent with phase-2.

### "relay-path partitions classify exactly like TCP partitions"
- Phase-2 §6.2: the reaper is authoritative. The reaper doesn't care about the carrier. So this claim holds. But the phase-3 doc's relay idle teardown adds a NEW cause of disconnect (idle timeout) that the phase-2 doc didn't consider. The reaper would fire on this. The classification is the same (partition → close loopback → OutcomeUnknown). But the ROOT CAUSE is different: relay idle vs network partition. The reaper doesn't distinguish. The doc's claim is that the classification is the same, which is true; but the COUNTER (the reason for the disconnect) is lost. This matters for telemetry/diagnostics.

### "Noise session resumption = ordinary re-handshake, cheap"
- Phase-2 §6.1: effect_id has incarnation epoch. The session is per-DB-creation, not per-Noise-handshake. A re-handshake with the same incarnation is safe for dedup. So "session resumption" is the fed-module re-establishing the carrier; the fed-module's effect_id space is preserved. The "cheap" part: a full Noise IK handshake is ~2 round trips, so a "cheap" re-handshake claim is plausible but should be qualified. The phase-2 doc doesn't claim Noise has 0-RTT resumption. The phase-3 doc says "ordinary re-handshake" — a full handshake. So "cheap" is relative. OK.

### "4-byte length prefix (redundant but kept)"
- Phase-2: the fed record has a 4-byte length prefix. The WS message is also length-delimited. Keeping the prefix is for "record parsing is carrier-agnostic and golden vectors hold." This is a design choice. The risk: if the prefix disagrees with the WS message length, the receiver doesn't know which to trust. The doc is silent.

## Now let me also check the phase-2 doc for specifics on:

- "verified-gate" — the phase-2 doc says "default-deny `federation_exposure`" and "per-peer allow-list in the profile, authored via the CK app." The "verified-gate" specifically: §5.3 says "a cloud-introduced pair is non-routable (deny-all exposure) until the out-of-band verification code is compared and confirmed in the CK app on both ends." So the gate is on the profile: a peer in the profile without the ceremony completed is non-routable. The phase-3 doc says "verified-gate makes discovered peers non-routable" — consistent.

- "reaping unchanged" — the phase-2 doc says "Per-peer keepalive with a bounded numeric staleness window; on a missed keepalive the module marks the peer's re-exported tools unavailable rather than letting calls hang." So the reaper marks tools unavailable, not closes loopback. Wait, the phase-2 doc §6.2 also says: "the fed-module's keepalive reaper is the AUTHORITATIVE partition classifier. On declaring a peer partitioned it closes that peer's loopback connections." So the reaper does both: marks tools unavailable AND closes loopback. OK.

- "rotated device key" — the phase-2 doc §5.3: "a legitimate key rotation must be signed by the old key (tombstone chain) or confirmed via an already-verified device / manual re-pairing; any other key change presents as first contact (non-routable + code comparison)." So a rotated key is re-paired via the ceremony. The phase-3 doc's "revoked_by_account → re-pair" path: a removed device, if re-enrolled with the SAME pubkey (which means the private key was preserved), must re-pair with existing peers. This is consistent with phase-2's key rotation ceremony.

Let me now think about findings. I want to be specific and concrete, with section references and severity.

## Findings list

### F1: Account-ID resolution race during concurrent enrollment (CRITICAL/HIGH)
- §4.2: "Worker verifies via JWKS, resolves/creates account_id, and hands enrollment to that account's AccountDO"
- If two devices enroll concurrently with the same WorkOS subject (e.g., two browser tabs, or the user logging in on two devices at once), both requests reach the Worker. The Worker resolves account_id keyed by provider subject. The first request creates the DO; the second sees the existing DO. But the resolution itself is a race: if the account_id is derived from the subject, both workers compute the same id and route to the same DO. The DO handles the enrollment.
- But what if the first request is still in "create account_id" branch when the second arrives? The DO doesn't exist yet; the second request tries to create it. Cloudflare's `idFromName` is idempotent — both get the same id. The first to call `get()` creates the DO; the second sees it. But the "hand to AccountDO" step happens after `get()`. If both are in the create branch, both proceed.
- More important: the DO's storage is global. Two concurrent enrollments would both call `enroll_device`, which would add two records. The race is on the (device_pubkey) uniqueness — if the same device enrolls concurrently from two places (e.g., the user clicks "enroll" twice), the DO would add two records. The UNIQUE constraint on (account_id, device_pubkey) would prevent this if enforced.
- The phase-3 doc doesn't mention the UNIQUE constraint or the race resolution.

### F2: Device-token lifecycle (HIGH)
- §4.2: "long-lived with rotation." Rotation mechanism not specified. When? Triggered by what? If rotation is time-based, a stolen token is valid for the rotation window. If rotation is event-based (e.g., on device remove), the in-flight signaling is at risk.
- Revoke vs in-flight: §4.2 says "device removal: DO deletes the record + revokes the token + broadcasts a registry delta; peers unpair it." But what about a connect_request in flight to the device being removed? The relay_grant is issued before the device is removed. The target device receives the grant, opens the relay. Now the device is removed. The relay pipe is open. The relay DO checks the token on each side; if the token is revoked, the next message from the device fails. But the pipe is already open. The device is "online" in the relay's view but "removed" in the account's view.
- This is a gap. The fix: the relay DO must check token validity on every message, not just at pipe open. Or the device removal must propagate to the relay DO to tear down the pipe.
- The doc says "rejects the token" — but the token check is at connect time. After that, the pipe is open. A revoked device can still use the relay for the duration of the pipe TTL.

### F3: Stolen token + rogue device enrollment (HIGH)
- §4.2: "Stolen device token" row in §8 says: "Cannot speak Noise as the device (no private key); revocable server-side."
- The threat: a stolen token allows the attacker to control the signaling plane: register candidates, accept offers, etc. The attacker can pair as the device. But the attacker doesn't have the private key, so the Noise handshake fails.
- BUT: a stolen token allows the attacker to accept a connect_request ON BEHALF of the device. The attacker can open a relay pipe using the stolen token. The attacker doesn't have the private key, so the Noise handshake fails. The caller sees a connection failure.
- The threat is bounded: the attacker can't decrypt or impersonate the device's tool calls. But the attacker can DoS the device's connectivity (by accepting and then dropping) and can phish the operator (by sending connect_offers).
- This is bounded but worth flagging. The fix: tighten the token's scope (e.g., bind to the device's pubkey, which the doc already does) and revoke aggressively.

### F4: Replay of signaling messages (HIGH)
- §5.3: "connect_request {to: pubkey} → relayed to the target as connect_offer" — no nonce or timestamp. A replayed connect_request would re-deliver the offer. The target would re-accept (or not, if it has state). If the target is stateful and rejects duplicates, OK. If not, the replay causes a redundant connect.
- More dangerous: replay of relay_grant. If the relay_grant is replayed, the attacker can open a relay pipe using the stolen grant. The doc says the grant is "single-use, short TTL." If "single-use" means the grant is invalidated after first use, replay is detected. But the doc doesn't specify WHERE single-use is enforced. If it's enforced in the AccountDO (which issued the grant), and the grant is sent to both sides, then the relay DO needs to check with the AccountDO. Or the grant includes a one-time token that the relay DO validates locally.
- The doc is silent on the enforcement mechanism. This is a gap.

### F5: Candidate poisoning (MEDIUM)
- §5.2: "The public candidate is stamped server-side by the Worker from the connection's observed source IP." So public is server-observed. Good.
- §5.2: "lan is self-reported." A malicious sibling can lie about its LAN address. An initiator dialing the sibling's LAN address would dial the attacker's machine. The attacker doesn't have the sibling's private key, so the Noise handshake fails. But the attacker can be on the LAN and respond with its own key... wait, the initiator is dialing for the SIBLING'S pubkey. The attacker's machine would receive the TCP connection and try to do a Noise handshake with the sibling's pubkey. The attacker doesn't have the sibling's private key, so the handshake fails. The attacker can't MITM because Noise IK pins the key.
- So the attack is: the attacker causes the initiator to fail to connect (DoS). Bounded.
- BUT: a more subtle attack. If the sibling is also dialing the initiator, the sibling's fed-module knows its own LAN address. If the sibling lies about its LAN address, the sibling's own outbound dials would go to the wrong place. The sibling's dials would fail. So the attack only affects the sibling's own dials.
- The doc says in Q2: "any hardening needed against a malicious sibling device on the same account lying about its LAN addr (current answer: it can only redirect its own inbound dials; Noise pins identity regardless)?" This is the doc's own Q, and the answer is correct: bounded.
- The fix: validate the LAN address is reachable before publishing it (e.g., a "self-test" the device runs). Or: don't publish LAN candidates from the registry; have the dialer discover LAN via mDNS or local broadcast. But this is phase 4+.
- For phase 3, the current answer is acceptable.

### F6: Double-win race on simultaneous dial (HIGH)
- §5.4: "the initiator (pubkey tie-break as phase 2) tries candidates in order: lan → public → relay, with a short per-candidate timeout (~2s) and first-success-wins."
- The pubkey tie-break determines the initiator. The initiator dials; the responder waits. But the responder also needs to dial the relay (outbound). The phase-3 doc doesn't say what the responder does for the TCP candidates.
- The phase-2 doc §4.1: "A learns B's exposed catalog and re-registers it into A's local subc." This is one-way. The phase-2 doc doesn't specify the symmetric case (B also learning A). But the fed-module is symmetric; both sides establish sessions.
- The phase-3 doc needs to specify: the responder does NOT dial TCP candidates; it only dials the relay. The initiator dials all candidates. The first to succeed is the carrier.
- If the initiator's TCP dials succeed AND the responder's relay dial succeeds, the responder has a relay session, and the initiator has a TCP session. The initiator sends Noise IK over TCP. The responder has already completed Noise IK over the relay. The responder's fed-module sees a new TCP connection. The fed-module can only have ONE Noise session per peer. It needs to reject the new TCP connection and tear down the relay (or vice versa).
- The tie-break by pubkey: if the initiator's pubkey > responder's pubkey, the initiator is the initiator. The responder is the responder. The initiator dials; the responder accepts. The responder doesn't dial TCP.
- BUT: the relay is outbound. The responder also dials the relay. So the responder dials relay only. The initiator dials TCP + relay. The first to succeed is the carrier. If TCP succeeds first, the relay pipe is torn down. If the relay succeeds first, the TCP is torn down.
- The race: if both succeed simultaneously, the responder has a relay session and the initiator has a TCP session. The responder's fed-module has a Noise session over relay. The initiator's fed-module has a Noise session over TCP. The two sides' state machines diverge.
- The phase-3 doc is silent on this race. This is a HIGH severity issue.

### F7: WS framing mismatch handling (MEDIUM)
- §6: "one fed record per WS message + retained 4-byte length prefix (redundant but kept)."
- The receiver's parser is fed exactly one record per message. If a WS message has 0 records (empty), 2+ records (concatenated), or the 4-byte length prefix disagrees with the WS message length, the receiver's behavior is unspecified.
- The fix: explicitly define the receiver's behavior on mismatch (close the WS, log, increment a metric).

### F8: DO-per-account hot spot (MEDIUM)
- §5.1: "AccountDO (one per account): SQLite-backed device registry; holds the live WebSocket per online device."
- A busy account (many devices, many concurrent signaling messages) funnels all through one DO. Cloudflare DOs have per-DO limits. The doc doesn't address sharding or load distribution.
- For phase 3, the user count is small (single user, handful of devices). The hot spot is bounded. But for paid tier (many devices per account? teams?), this is a real concern.

### F9: WS hibernation vs signaling latency (MEDIUM)
- §10 Q5 asks about this. The answer: a cold-wake round-trip is ~hundreds of ms in Cloudflare. The 2s per-candidate timeout absorbs this. But for the first signal to an idle account, the user perceives a delay. For the "single login makes subcs connect" UX, this is noticeable.
- The fix: send a periodic keepalive from the device to the AccountDO to keep it warm. Or: accept the latency for v1.

### F10: Rendezvous down degradation (HIGH)
- §5.5: "Absent → phase-2 behavior exactly." This is the absent case. The present-but-down case is not specified.
- If the device's `[rendezvous]` is present but the Worker is down, the control WS fails. The fed-module's behavior: ? Does it fall back to static profiles? Does it retry forever? Does it block the user?
- The doc says "static profiles remain the availability fallback" in §8. But the mechanism is not specified. If the rendezvous-down case hangs the device (e.g., the fed-module waits for the control WS to come up), then a user with only rendezvous-discovered peers is bricked.
- The fix: the fed-module must treat rendezvous-down as a soft failure: continue with static profiles, log a warning, retry in the background.

### F11: Device clock assumptions (LOW)
- The doc doesn't specify whether TTLs are device-clock or server-clock. Server-clock is correct (the RelayDO and AccountDO are server-side, so their TTLs are server-clock). The device's keepalive cadence is timer-based, not clock-based, so a skewed clock doesn't affect it.
- The only device-clock concern: the device's notion of "last_seen" is stamped by the server, so it's server-clock. OK.
- Low severity.

### F12: Pairing UX window — verify-code on first contact (HIGH)
- §4.3: "the rogue device's verify-code will not match anything the operator's real devices display."
- This claim is WRONG. The verify-code is only displayed on the TWO devices being paired (initiator and target). The operator's OTHER devices (not in the pair) don't display a code. So the comparison is between the rogue and ONE existing device. If the rogue is who it claims to be, the codes match.
- The protection is the "loud device-join events" — the operator gets a notification. But if the operator doesn't notice, or if the rogue is enrolled by a compromised account, the operator may not see the join event.
- More specifically: in the cloud-first-contact scenario (no in-person meeting), the operator has only the two devices. The operator looks at A, sees code X. The operator looks at B, sees code X. They match. The operator confirms. The rogue is trusted.
- The phase-3 doc's claim that "the rogue device's verify-code will not match anything the operator's real devices display" is incorrect: the codes WILL match, because the ceremony is between the two devices in the pair.
- The fix: the pairing UX must show the device's identity (name, fingerprint) prominently, and the operator must verify the identity outside the code (e.g., "this is my home server, named X, fingerprint Y"). The loud join event is a backup signal. The doc should make this explicit.

### F13: Rogue device enrollment via stolen WorkOS account (HIGH)
- §8: "Stolen provider account (WorkOS login) | enroll a rogue device → it appears in the registry | Ceremony: rogue device never becomes routable without operator confirming codes on an existing device; join events loud"
- The threat: the attacker has the WorkOS login. The attacker enrolls a rogue device with the attacker's private key. The rogue appears in the registry. The operator (on a real device) gets a loud join event. The operator can then... what? Remove the rogue. The doc implies the operator will notice and remove.
- BUT: the attacker can also REMOVE the operator's real devices (via the same WorkOS login). The doc's "device removal" is a WorkOS-authenticated action. If the attacker has the WorkOS login, the attacker can remove all real devices, leaving only the rogue. The operator's real devices are then offline; the rogue is the only device.
- This is a more severe attack than the doc acknowledges. The fix: device removal must require an additional confirmation (e.g., a confirmation on an existing device, not just the WorkOS login). Or: device removal is gated by a quorum of devices.

### F14: relay_grant issuance — race between connect_request and target online transition (MEDIUM)
- §5.3: "connect_request {to: pubkey} → relayed to the target as connect_offer" and "relay_open {to: pubkey} → relay_grant {relay_url, pipe_id, pipe_token} issued to both sides"
- The flow: A sends connect_request. The rendezvous delivers connect_offer to B. Both sides then race candidates. If both TCP candidates fail, both sides send relay_open. The rendezvous issues relay_grant to both.
- Race: A sends relay_open, but B has gone offline (e.g., closed its control WS). The rendezvous has B in the registry as "online" (stale). The relay_grant is issued. A tries to open the relay pipe; B is not there. The pipe times out. A retries.
- The fix: the rendezvous must check the target's liveness before issuing the relay_grant. Or: the relay_grant is issued only after the target acknowledges.

### F15: AccountDO as a single point of failure (HIGH)
- §5.1: "AccountDO (one per account): SQLite-backed device registry; holds the live WebSocket per online device."
- If the AccountDO crashes or is restarted, all connected devices lose their control WS. The devices must reconnect. The reconnect is straightforward (re-hello with the device token), but the in-flight signaling is lost. A connect_request in flight is dropped.
- The fix: the fed-module's reconnect logic must handle the drop gracefully (retry the connect_request, or surface an error to the user).

### F16: No explicit rate limits on signaling (HIGH)
- §8 doesn't mention per-account/per-device quotas. Phase-2 §5.5 mentions "per-account/per-device quotas + rate limits" but phase-3 §5 doesn't.
- A compromised WorkOS account can flood connect_requests, exhausting the AccountDO's CPU/memory. The DO has limits (128MB memory, 30s CPU), so a flood can cause DoS.
- The fix: enforce per-account/per-device rate limits on signaling ops.

### F17: relay_grant — both sides receiving the same pipe_id (MEDIUM)
- §7: "bridges exactly two authenticated WebSockets (pipe_id + per-side pipe_token, single-use, short TTL)"
- "Issued to both sides" — the doc is ambiguous on whether the pipe_id is the same for both sides. If yes, the relay DO bridges two WS connections with the same pipe_id. The per-side pipe_token is what differentiates them.
- The race: A and B both receive the relay_grant. A opens first. The relay DO accepts A's pipe_token. B opens second. The relay DO accepts B's pipe_token. The pipe is open.
- The risk: what if A opens, the relay DO accepts, then A's token is "used up" (single-use). B opens with the same pipe_id. The relay DO rejects B (token already used). But the doc says "per-side pipe_token, single-use" — each side has its own token, so each is used once.
- This is fine if "single-use" means per-side. The doc is ambiguous. The fix: explicitly state that single-use is per-side, and the relay DO tracks (pipe_id, side) → used.

### F18: Effect ledger interaction with relay reconnect (HIGH)
- §6.1 of phase-2: the in-flight effect is `intent durable, send unconfirmed` when the relay drops. The recovery reconciliation handles crash, not partition. On partition, the effect is `OutcomeUnknown` at the call site. The caller re-invokes with the same effect_id. The serving side's dedup ledger may or may not have the outcome (depending on whether the first send arrived).
- If the first send arrived and the ack was lost, the dedup ledger has the outcome. Re-invoke is safe.
- If the first send did NOT arrive (relay dropped the packet), the dedup ledger has nothing. Re-invoke is a duplicate.
- The phase-2 doc is honest about this: "A re-arriving effect_id with a ledger row returns the RECORDED outcome without re-dispatch — what makes a legitimate origin re-send safe even when the first send actually arrived (lost-ack)." So the dedup is best-effort: it works if the first send arrived, fails if it didn't.
- The phase-3 doc claims "relay-path partitions classify exactly like TCP partitions" — this is true for the classification (OutcomeUnknown), but the underlying at-most-once is best-effort, not exact. The doc should be honest about this.

### F19: Registry delta delivery — at-most-once vs at-least-once (MEDIUM)
- §5.3: "registry_delta (push: device added/removed/online/candidates-changed)" — pushed over the control WS. If the control WS drops mid-delta, the delta is lost. The device reconnects and gets the registry_snapshot (via hello). So the snapshot is the recovery mechanism.
- The race: a device added to the account, a delta is pushed to all online devices. One device's control WS drops before the delta arrives. The device reconnects, gets the snapshot, sees the new device. OK.
- The race: a device added, a delta is pushed. The device's hello completes before the delta is sent. The hello returns the snapshot, which includes the new device. OK.
- The race: a device added, a delta is pushed. The device is added to the DO. The DO pushes the delta to all online devices. One device is in the middle of a hello. The delta is sent after the hello completes. The device gets the snapshot (no new device) and then the delta. OK.
- The risk: a delta is sent, the receiving device is offline. The delta is lost. The device reconnects later, gets the snapshot. OK.
- The risk: a delta is sent, the receiving device's WS drops mid-receive. The delta is lost. The device reconnects, gets the snapshot. OK.
- This is at-least-once with snapshot recovery. Acceptable.

### F20: AccountVerifier seam — provider token handling (MEDIUM)
- §4.1: "AccountVerifier seam in the Worker: verify(provider_token) -> {provider, subject, email}. Implementations: WorkOsVerifier (JWKS), plus a FakeVerifier for tests. Nothing else in the system sees provider tokens."
- The provider token is a WorkOS JWT. The Worker verifies via JWKS. The token is short-lived. Good.
- The risk: the FakeVerifier is for tests. If it leaks to production, the auth is bypassed. The fix: enforce that FakeVerifier is only registered in test environments. The doc should make this explicit.

### F21: "Rotated device key" — key rotation vs device removal (MEDIUM)
- §4.2: "device removal: ... profile entry flips to `revoked_by_account`, non-routable, requires re-pair if re-enrolled."
- The re-pair is required because the existing peers have pinned the old key. If the device is re-enrolled with the same pubkey (private key preserved), the re-pair is a new ceremony. If the device is re-enrolled with a NEW pubkey (key rotation), the re-pair is also a new ceremony.
- The phase-2 doc §5.3: "a legitimate key rotation must be signed by the old key (tombstone chain) or confirmed via an already-verified device / manual re-pairing; any other key change presents as first contact (non-routable + code comparison)."
- The phase-3 doc's "re-pair" path is consistent. The risk: a device that is removed and re-enrolled with the same key — the existing peers' profile entries flip to `revoked_by_account`. The re-pair is required. But the re-pair requires the operator to confirm the verify-code. If the operator doesn't re-pair, the device is non-routable. The UX must make this clear.

### F22: AccountDO storage — encryption at rest (LOW)
- §4.2: "device token: an opaque bearer bound to (account_id, device_pubkey), stored hashed in the DO, revocable, long-lived with rotation."
- The token is stored hashed. Good. But the device_pubkey is stored in plaintext. The registry contains public keys, candidates, last_seen, online. None of this is sensitive. The hashed token is the secret.
- Cloudflare DO storage is encrypted at rest by Cloudflare. The doc doesn't need to specify further.

### F23: WorkOS AuthKit — device authorization flow security (LOW)
- §4.1: "v1 provider: WorkOS AuthKit — first-class OAuth 2.0 Device Authorization Flow (the axis that matters for a CLI + native-app product: login is plain HTTP + 'enter code at url' from any client, zero provider SDK in our binaries)"
- The device authorization flow is a standard OAuth flow. The WorkOS JWT is verified via JWKS. The risk: if the WorkOS account is compromised, the attacker can enroll a rogue device. This is the §8 row "Stolen provider account."
- The doc acknowledges this. The fix: the operator must have a recovery path (e.g., revoke all device tokens, re-enroll all devices). The doc doesn't specify this.

### F24: Device token storage on device (HIGH)
- §10 Q1: "Device-token custody on disk (0600 file next to the device key today) — sufficient for v1, or fold into the credentials vault immediately?"
- The answer in the doc: the question is open. My recommendation: fold into the credentials vault immediately, because the device token is a bearer token that grants signaling-plane access. If the device is compromised (laptop stolen), the token is in the same place as the private key. The credentials vault would protect both.
- The risk: a 0600 file is readable by any process running as the user. A compromised user account can read the token. The credentials vault is subc-core primitive and may have stronger protections (e.g., keychain on macOS).

### F25: AccountDO hot path — control WS upgrade auth (MEDIUM)
- §5.1: "Worker: stateless HTTP + WebSocket upgrade endpoint; JWKS verification on enroll; device-token check on everything else; routes to the account's DO by account_id."
- The control WS upgrade is authenticated by the device token. The device token is sent in the upgrade request (or in a hello message). The Worker checks the token, routes to the AccountDO. The AccountDO establishes the WS.
- The risk: the device token is sent in the upgrade request. If the upgrade is over TLS, the token is encrypted in transit. Good.
- The risk: a replay of the upgrade request would re-establish the WS. The token is "long-lived," so a replay is valid. The replay would re-establish the WS, which is idempotent. OK.
- The fix: use a nonce in the upgrade request to prevent replay. Or: the token is bound to the device's pubkey, so a replay would need the same pubkey.

OK, I have a lot of findings. Let me now organize them by severity and structure the response.

## Inheritance claims verification

Let me explicitly check each:

1. "E2E Noise IK between device static keys" — phase-2 §2.2 confirms. ✓
2. "verify-code ceremony (SHA-256 safety number, order-independent)" — phase-2 §5.3 says "TOFU + out-of-band verification codes (Tailscale/Signal-style safety numbers)." The "order-independent" claim is not in phase-2. The phase-3 doc's own claim. The phase-2 doc says "the code is derived from both endpoints' long-term device static keys (never session/ephemeral material), so it binds the pair, not the conversation." Order-independent: the code is the same regardless of which side computes it first. This is true for a symmetric function (e.g., SHA-256 of sorted pubkeys). The phase-2 doc doesn't explicitly say "order-independent," but the property follows from the design. So the claim is consistent.
3. "default-deny exposure with an enforced verified-gate" — phase-2 §2.3 and §5.3. ✓
4. "exactly-once effect ledger (incarnation epoch + seq high-water + recovery reconciliation)" — phase-2 §6.1. ✓
5. "WAN-proven" — phase-2 §9 phase 1-2. ✓
6. "carrier-blind" — phase-3 doc's own claim. Phase-2 says the WAN is Noise IK; doesn't say "carrier-blind" explicitly. The phase-3 doc extends this by adding a WS carrier. The claim is about the fed-module's state machine being carrier-agnostic. Plausible but not directly supported by phase-2.
7. "reaping unchanged" — phase-2 §6.2. The reaper is the partition classifier. Carrier-agnostic. ✓
8. "verified-gate makes discovered peers non-routable" — phase-2 §2.3 + §5.3. ✓
9. "relay-path partitions classify exactly like TCP partitions" — phase-2 §6.2. The reaper is carrier-agnostic. But the relay idle teardown is a NEW cause of disconnect not considered in phase-2. The classification is the same, but the root cause is lost. PARTIALLY supported.
10. "Noise session resumption = ordinary re-handshake, cheap" — phase-2 §6.1. The effect_id has incarnation epoch per DB creation, not per handshake. Re-handshake preserves incarnation. "Cheap" is a phase-3 claim. Phase-2 doesn't claim 0-RTT. A full IK handshake is ~2 round trips. "Cheap" is relative. PARTIALLY supported.

Now let me structure the response. I want to be adversarial but disciplined. Let me rank the findings by severity:

BLOCKER:
- F12: Verify-code claim is wrong (security)
- F6: Double-win race on simultaneous dial (correctness)
- F18: Effect ledger interaction with relay reconnect (correctness) — actually this is documented in phase-2; it's a known limitation, not a new finding. Let me reconsider. The phase-3 doc claims "relay-path partitions classify exactly like TCP partitions." This is true for the classification, not for the at-most-once guarantee. The doc doesn't claim more. So this is a MEDIUM (clarification needed), not a BLOCKER.

HIGH:
- F1: Account-ID resolution race (correctness)
- F2: Device-token revocation race (security)
- F3: Stolen token + rogue device enrollment (security)
- F4: Replay of signaling messages (security)
- F10: Rendezvous down degradation (operational)
- F13: Rogue device enrollment via stolen WorkOS account (security)
- F15: AccountDO as single point of failure (operational)
- F16: No explicit rate limits on signaling (security/operational)
- F24: Device token storage on device (security)

MEDIUM:
- F5: Candidate poisoning (security) — bounded, acceptable
- F7: WS framing mismatch handling (correctness)
- F8: DO-per-account hot spot (operational)
- F9: WS hibernation vs signaling latency (operational)
- F11: Device clock assumptions (operational)
- F14: relay_grant issuance race (correctness)
- F17: relay_grant — pipe_id same for both sides (correctness)
- F19: Registry delta delivery (correctness) — acceptable
- F20: AccountVerifier seam — FakeVerifier leak (security)
- F21: Key rotation vs device removal (security)
- F23: WorkOS AuthKit device flow (security) — low

LOW:
- F22: AccountDO storage encryption (security)
- F25: Control WS upgrade auth (security)

Let me reconsider the BLOCKER list. The phase-3 doc's central security claim is in §4.3: "the rogue device's verify-code will not match anything the operator's real devices display." This is a factual claim about the ceremony. The claim is wrong: the verify-code is only displayed on the two devices being paired, and if the rogue is one of them, the codes match.

But wait — let me re-read the claim more carefully. "A cloud/account compromise therefore yields metadata + the ability to offer a rogue device for pairing — it cannot make any existing device trust the rogue one, and the rogue device's verify-code will not match anything the operator's real devices display."

The claim is that the rogue's code won't match the real devices' codes. But the ceremony is between the rogue and ONE existing device (the one being asked to pair). The codes WILL match between those two. The claim is that the codes won't match the OTHER real devices. But the OTHER real devices don't display codes during this ceremony.

So the claim is misleading. The protection is NOT that the codes don't match; the protection is that (1) the operator must verify the identity outside the code, and (2) the loud join event.

This is a BLOCKER because the doc's central security claim is wrong. The fix: explicitly state the UX requirement that the operator must verify the device's identity (name, fingerprint) in addition to the code.

OK let me also check F6 (double-win race) more carefully. The phase-3 doc says: "the initiator (pubkey tie-break as phase 2) tries candidates in order." The responder doesn't dial. But the relay is outbound. The responder dials the relay. So:
- Initiator: dials LAN + public + relay.
- Responder: dials relay only.
- The relay is dialed by both. Both arrive at the relay DO. The relay DO bridges them.

The race: initiator's TCP succeeds. Responder's relay succeeds. The responder's fed-module has a Noise session over the relay. The initiator's fed-module has a Noise session over TCP. They both think they're connected. The responder's fed-module sees a new TCP connection from the initiator (the TCP dials arrive at the responder's listening port). The responder's fed-module must decide: accept the TCP or stick with the relay.

The tie-break by pubkey: the initiator's pubkey determines who is the initiator. The initiator dials; the responder accepts. The responder's fed-module, on receiving a TCP connection, should check: do I already have a session with this peer? If yes, reject. If no, accept.

But the responder is the one that opened the relay. The initiator's TCP arrives at the responder's listening port. The responder's fed-module has a session over the relay. The TCP arrival is from the initiator. The responder should reject the TCP (already have a session) and keep the relay. But the responder's pubkey tie-break: the responder is the responder, so it shouldn't be dialing. Wait — the responder's fed-module opened the relay. That's a dial. The responder is dialing the relay, not the peer directly.

OK so:
- Initiator dials peer (TCP) + relay.
- Responder dials relay.
- If initiator's TCP succeeds: responder gets TCP connection. Responder's fed-module accepts, runs Noise IK handshake. Responder's relay is still open (or torn down?).
- If initiator's relay succeeds: initiator's fed-module runs Noise IK over relay. Responder's fed-module runs Noise IK over relay. They meet at the relay.
- If both succeed: initiator's fed-module has TCP + relay. Responder's fed-module has TCP (from initiator's TCP) + relay (from its own dial). Now what? The initiator's fed-module sees two connections. The responder's fed-module sees two connections.

The tie-break: the pubkey-high side is the initiator. The initiator dials. The responder accepts. The responder doesn't dial the peer directly. The responder's relay dial is for the relay, not the peer. So the responder has a relay session. The initiator has a TCP session + a relay session.

The initiator should pick one: first-success-wins. If TCP succeeds first, use TCP, tear down relay. If relay succeeds first, use relay, abort TCP.

The responder: the responder doesn't initiate. The responder's fed-module sees the TCP from the initiator. If the responder already has a relay session, it should reject the TCP (already have a session). If the responder doesn't have a relay session (TCP arrived first), it should accept the TCP and run Noise IK.

This is doable. The phase-3 doc doesn't specify this. It's a correctness gap.

Let me also think about: the responder's relay dial. The responder dials the relay BEFORE the initiator's TCP arrives. The relay is a "long-lived" connection. The responder dials, the relay DO accepts, the responder waits for the initiator to arrive. If the initiator's TCP arrives first, the responder accepts TCP, runs Noise IK. The responder's relay dial is still pending. The responder should tear down the pending relay dial.

Or: the responder only dials the relay if the TCP doesn't arrive within the timeout. The phase-3 doc says "short per-candidate timeout (~2s)." So the responder dials the relay after 2s of no TCP arrival. OK, that's a reasonable policy.

But the doc doesn't specify this. The doc says the responder waits; it doesn't say the responder dials the relay proactively. This is a gap.

Let me also check: the phase-2 doc says "the federation module opens one loopback connection per (peer, remote module)" — so per peer, per remote module. The fed-module has ONE session per peer (not per remote module; the loopback is per remote module, but the network session is per peer). The network session is over ONE carrier. The carrier can be TCP or WS (relay). The fed-module picks one.

So the fed-module's state machine must handle the carrier selection. The phase-3 doc's WsCarrier is a new carrier. The fed-module's state machine must be extended to handle WS. The doc says "carrier-blind" — meaning the state machine doesn't care which carrier. This is a design claim; if true, the state machine is carrier-agnostic. The phase-2 doc doesn't make this claim explicitly.

OK, I think I have enough. Let me now write the response.

Let me also do one more check on the inheritance claims:

- "verified-gate makes discovered peers non-routable" — phase-2 §5.3 says "a cloud-introduced pair is non-routable (deny-all exposure) until the out-of-band verification code is compared and confirmed in the CK app on both ends." So a pair is non-routable until the ceremony. The phase-3 doc's discovered peers are cloud-introduced (the rendezvous is the cloud). So they are non-routable until the ceremony. ✓

- "reaping unchanged" — phase-2 §6.2 says "Per-peer keepalive with a bounded numeric staleness window; on a missed keepalive the module marks the peer's re-exported tools unavailable rather than letting calls hang." And: "the fed-module's keepalive reaper is the AUTHORITATIVE partition classifier. On declaring a peer partitioned it closes that peer's loopback connections." So the reaper does both. The phase-3 doc says the reaping is unchanged. The relay teardown would trigger the reaper. The reaper closes the loopback, delivering GOODBYE/OutcomeUnknown. ✓ (with the caveat that the root cause is lost)

- "relay-path partitions classify exactly like TCP partitions" — the reaper is carrier-agnostic. So a relay teardown is classified as a partition. ✓ (with the caveat about root cause)

- "Noise session resumption = ordinary re-handshake, cheap" — phase-2 doesn't have session resumption; it has effect_id-based dedup. A re-handshake is a new Noise IK session. The "cheap" claim is about the cost. ✓ (with the caveat that "session resumption" is misleading; the dedup is via effect_id, not via Noise session)

- "carrier-blind" — phase-2 doesn't make this claim. The phase-3 doc's own claim. Plausible but not directly supported.

OK let me write the response now.

Actually wait, I should also verify the §5.5 dial policy more carefully. The doc says: "the initiator (pubkey tie-break as phase 2) tries candidates in order: lan → public → relay, with a short per-candidate timeout (~2s) and first-success-wins."

The pubkey tie-break: which pubkey is higher? The doc says "pubkey tie-break as phase 2." Phase 2 doesn't explicitly say which side is the initiator. The phase-2 doc §4.1: "A learns B's exposed catalog and re-registers it into A's local subc." So A is the initiator (the one learning). But the symmetric case: A also learns from B. The doc doesn't say.

The phase-3 doc says "pubkey tie-break" — meaning the pubkey determines who initiates. The higher pubkey initiates, or the lower? The doc doesn't say. This is a gap.

Let me also check: the doc says "the winning transport is remembered per peer and retried first next time, with periodic re-probes for direct upgrade while on relay." So the transport is sticky. If the first connect used relay, subsequent connects try relay first. Periodically, re-probe for direct. This is a transport-upgrade mechanism. Good.

OK I have enough. Let me write the response.

One more thing: the phase-3 doc §5.5 says: "New `[rendezvous]` profile section: `{account: true, control_url, device_token_path}`. Absent → phase-2 behavior exactly (static profiles keep working forever; the WAN test rig never needs an account)."

The absent case is phase-2. The present case: control_url + device_token_path. What if control_url is down? The doc says nothing. The fed-module would try to connect to control_url, fail, retry. Meanwhile, static profiles still work. The fed-module should continue with static profiles while the rendezvous is down.

The risk: if the fed-module BLOCKS on the control WS (e.g., waits for the hello to complete before allowing any dialing), then a rendezvous-down bricks the device. The doc needs to specify that the control WS is non-blocking.

OK, writing the response now.

Let me also think about one more thing: the phase-3 doc §5.3 says "Signaling carries no secrets and no trust decisions: candidates and pubkeys only." This is true. But the connect_request reveals WHO the initiator wants to call. This is metadata. A compromised rendezvous learns the device's call graph. The doc acknowledges this in §8.

But there's a more subtle issue: the connect_request includes the initiator's candidates. This reveals the initiator's network position (LAN, public IP). The rendezvous learns the initiator's network info. The doc says the public candidate is server-observed, so the rendezvous already knows the IP. The LAN candidate is self-reported. So the rendezvous learns the LAN topology. This is a privacy concern. The doc doesn't address it.

For the audit, this is a MEDIUM finding (privacy/metadata).

OK, writing the response.

Actually let me also note: the phase-3 doc says in §4.2 "Day-to-day rendezvous auth uses this token; the provider is out of the loop until the operator re-logins." So the device token is used for auth until the operator re-logins (which would re-issue the token). The "re-logins" is the rotation trigger? The doc doesn't say.

The risk: if the device token is stolen, the attacker has it until the operator re-logins. The operator may not re-login for months. The fix: explicit rotation mechanism (time-based or event-based).

OK, writing the response now. I'll be disciplined about severity.

Let me also revisit F12 (verify-code claim). The claim is: "the rogue device's verify-code will not match anything the operator's real devices display."

Interpretation 1: The codes displayed on the rogue and on the operator's existing device DON'T match. This is FALSE if the rogue is being introduced as a new device, because the ceremony is between the rogue and ONE existing device, and the codes WILL match (both are a function of the two pubkeys).

Interpretation 2: The codes displayed on the rogue and on ALL the operator's real devices DON'T match. But the OTHER real devices don't display codes during this ceremony. So the claim is vacuously true (the "real devices" don't display codes, so the rogue's code "doesn't match" them in the trivial sense).

The doc's claim is misleading. The real protection is: (1) the operator must verify the device's identity outside the code (e.g., "is this my home server?"), and (2) the loud join event.

The risk: a user who relies on the code as the sole verification will trust a rogue that has the matching code.

This is a BLOCKER because the doc's central security claim is misleading, and the UX must be designed carefully to prevent users from trusting a rogue.

The fix: the pairing UX must show the device's identity (name, fingerprint) prominently, and the operator must confirm both the code AND the identity. The loud join event is a backup signal.

OK, writing the response now.

Hmm, I want to double-check the "pubkey tie-break" claim against phase-2.

Phase-2 §4.1: "The federation module on A establishes a Noise session to peer B's federation module."

So A is the one establishing. But the symmetric case (B also establishing to A) is not specified. The phase-2 doc doesn't say "pubkey tie-break." The phase-3 doc claims "pubkey tie-break as phase 2." But phase-2 doesn't have a pubkey tie-break.

Actually, the phase-2 doc may have this in the forwarder or transport layer. Let me search.

Actually, the phase-3 doc says "pubkey tie-break as phase 2" — this is a reference to phase-2's tie-break mechanism. The phase-2 doc doesn't explicitly describe a pubkey tie-break. So the phase-3 doc is asserting that phase-2 has a pubkey tie-break. This is a claim about phase-2 that I should verify.

Let me search the phase-2 doc for "tie-break" or "initiator" or "dialer."

Looking at the phase-2 doc... I don't see a pubkey tie-break. The phase-2 doc §4.1 says "A establishes a Noise session to peer B." This implies A is the initiator, but the symmetric case is not specified.

So the phase-3 doc's claim "pubkey tie-break as phase 2" is asserting a phase-2 property that may not be specified. This is a finding.

The risk: if there is no pubkey tie-break, then both sides may dial simultaneously, leading to the double-win race. The phase-3 doc assumes the tie-break exists.

The fix: the phase-3 doc should either cite the phase-2 tie-break or define it. If phase-2 doesn't have it, the phase-3 doc must add it.

Let me search for "tie-break" in the codebase to be sure.

Actually, I don't have the full codebase here. Let me search the docs.

The phase-2 doc doesn't mention "tie-break" anywhere. So the phase-3 doc's claim "pubkey tie-break as phase 2" is asserting an unspecified property.

This is a finding (F26): the pubkey tie-break mechanism is not specified in phase-2, and the phase-3 doc assumes it. The double-win race is a real consequence.

OK, let me now write the response.

Wait, I should also check the phase-2 doc for the effect_id incarnation semantics. The phase-2 doc §6.1: "The incarnation UUID is minted whenever the origin's send-log db is created (fresh install, db loss, restore-from-backup) and stored IN that db — so a post-loss origin can never re-mint ids that collide with pre-loss effects."

So the incarnation is per-DB-creation. A re-handshake does NOT change the incarnation. The phase-3 doc's "Noise session resumption = ordinary re-handshake" is correct in that the incarnation is preserved.

But the phase-3 doc says "cheap" — a full Noise IK handshake is ~2 round trips + cryptographic operations. Is this "cheap"? In the context of tool calls, yes (the calls are milliseconds to seconds). So "cheap" is acceptable.

OK, writing the response.

Let me also check: the phase-3 doc §5.3 says "connect_request {to: pubkey} → relayed to the target as connect_offer {from: pubkey, candidates} → target answers connect_accept {candidates} — both sides then race candidates."

Wait — "both sides then race candidates." This says BOTH SIDES race candidates. So the responder ALSO dials candidates. The phase-3 doc §5.4 says: "the initiator (pubkey tie-break as phase 2) tries candidates in order: lan → public → relay."

So the initiator tries candidates. The responder... what? The §5.3 says "both sides race candidates" — so the responder also tries candidates. But §5.4 says "the initiator tries candidates." Contradiction.

Actually, re-reading §5.3: "both sides then race candidates (§6)." And §5.4: "On connect_offer/accept, the initiator (pubkey tie-break as phase 2) tries candidates in order."

So §5.3 says both sides race; §5.4 says only the initiator races. This is a contradiction in the doc.

The fix: the doc must specify which side dials. The standard pattern is: the initiator dials, the responder accepts. The "both sides race" in §5.3 may mean both sides have their own candidate sets and try them, but only one side (the initiator) actively dials.

Or: both sides dial (symmetric), and the first to succeed wins. But this leads to the double-win race.

The doc is ambiguous on this. This is a finding.

OK, writing the response now.

Let me also think about: the phase-3 doc §7 says "RelayDO: created per pipe grant; bridges exactly two authenticated WebSockets (pipe_id + per-side pipe_token, single-use, short TTL)."

"created per pipe grant" — so the RelayDO is created when a grant is issued. The grant is issued to both sides. So the RelayDO exists before either side connects. The RelayDO waits for both sides to arrive.

"single-use, short TTL" — single-use per side. The TTL is short.

The race: A and B both receive the grant. A connects first. The RelayDO accepts A's token. B connects second. The RelayDO accepts B's token. The pipe is open. A and B communicate.

If A connects, then the TTL expires before B connects, the RelayDO tears down. A's connection is closed. B's grant is wasted. B retries (gets a new grant).

If A connects, then A disconnects (e.g., A's device crashes), the RelayDO tears down. B's grant is wasted. B retries.

If B connects, then the TTL expires before A connects, the RelayDO tears down. B's connection is closed. A's grant is wasted. A retries.

The risk: the TTL must be long enough for both sides to connect, but short enough to prevent abuse. The doc says "short" without specifying.

The fix: specify the TTL (e.g., 30s) and the retry policy.

OK, writing the response.

One more: the phase-3 doc §5.5 says "Registry-discovered peers materialize as unverified profile peers (name from registry, `verified:false`) — the existing enforced gate makes them non-routable until the ceremony; discovery adds candidates, not trust."

The "existing enforced gate" — this is the phase-2 verified-gate. The phase-2 doc §5.3: "a cloud-introduced pair is non-routable (deny-all exposure) until the out-of-band verification code is compared and confirmed."

So a discovered peer is added to the profile with `verified:false`. The fed-module checks `verified` before allowing any traffic. This is the verified-gate. ✓

But: the profile is local. The registry is on the cloud. The discovered peers are materialised locally. The operator can see them. The operator initiates the ceremony (compare-and-tap). After the ceremony, the local profile entry flips to `verified:true`. The cloud doesn't know.

The risk: a malicious sibling device on the same account can materialise as a discovered peer. The operator sees it. The operator initiates the ceremony. The ceremony is between the operator's real device and the sibling. The codes match (both are a function of the two pubkeys). The operator confirms. The sibling is now verified.

This is the same as the rogue device scenario. The protection is the operator's judgment.

OK, writing the response.

Alright, let me consolidate. I'll write a structured findings list with severity, location, problem, evidence, and fix. Then the answers to the five open questions. Then the overall verdict.

Let me be disciplined about the number of findings. I'll aim for 15-20 high-quality findings, not 30 low-quality ones.

Final list:

BLOCKER (3):
1. F12: Verify-code ceremony claim is misleading (security, §4.3)
2. F6: Double-win race / "both sides race candidates" ambiguity (correctness, §5.3, §5.4)
3. F26: Pubkey tie-break not specified in phase-2 (correctness, §5.4)

HIGH (8):
4. F1: Account-ID resolution race during concurrent enrollment (security/correctness, §4.2)
5. F2: Device-token revocation race vs in-flight signaling (security, §4.2, §7)
6. F4: Replay of signaling messages / single-use enforcement unspecified (security, §5.3, §7)
7. F10: Rendezvous down degradation path undefined (operational, §5.5, §8)
8. F13: Rogue device removal via stolen WorkOS account (security, §4.2, §8)
9. F15: AccountDO as single point of failure for control plane (operational, §5.1)
10. F16: No explicit rate limits on signaling (security/operational, §5)
11. F24: Device token storage on device (security, §10 Q1)

MEDIUM (8):
12. F3: Stolen token + signaling plane abuse (security, §4.2, §8)
13. F5: Candidate poisoning by sibling (security, §5.2, §10 Q2)
14. F7: WS framing mismatch handling unspecified (correctness, §6)
15. F8: DO-per-account hot spot (operational, §5.1)
16. F9: WS hibernation vs signaling latency (operational, §10 Q5)
17. F11: Device clock assumptions (operational, §5)
18. F14: relay_grant issuance vs target liveness (correctness, §5.3)
19. F17: relay_grant pipe_id ambiguity (correctness, §7)
20. F20: AccountVerifier FakeVerifier leak risk (security, §4.1)

LOW (3):
21. F19: Registry delta delivery — at-least-once with snapshot recovery (correctness, §5.3)
22. F22: AccountDO storage encryption (security, §4.2)
23. F23: WorkOS AuthKit device flow residual (security, §4.1)

Inheritance claims verification:
- "E2E Noise IK between device static keys" — ✓ phase-2 §2.2
- "verify-code ceremony (SHA-256 safety number, order-independent)" — phase-2 §5.3 supports TOFU + codes; "order-independent" is a new claim consistent with the design
- "default-deny exposure with an enforced verified-gate" — ✓ phase-2 §2.3 + §5.3
- "exactly-once effect ledger (incarnation epoch + seq high-water + recovery reconciliation)" — ✓ phase-2 §6.1
- "WAN-proven" — ✓ phase-2 §9
- "carrier-blind" — NEW claim, not directly supported by phase-2; plausible
- "reaping unchanged" — ✓ phase-2 §6.2 (with caveat: root cause lost for relay idle)
- "verified-gate makes discovered peers non-routable" — ✓ phase-2 §2.3 + §5.3
- "relay-path partitions classify exactly like TCP partitions" — ✓ phase-2 §6.2 (with caveat: root cause lost)
- "Noise session resumption = ordinary re-handshake, cheap" — phase-2 §6.1 supports incarnation preservation; "session resumption" is misleading (it's effect_id dedup, not Noise resumption); "cheap" is acceptable

Answers to the five open questions:
1. Device-token custody: fold into credentials vault immediately. The device token is a bearer token; a 0600 file is readable by any user process. The credentials vault provides keychain integration (macOS) or similar. Phase-3 v1 should not ship with a plain file.
2. Registry candidates self-reporting: keep as-is for v1. The attack is bounded by Noise key-pinning (the attacker can only DoS the sibling's own dials). For phase 4+, consider validating reachability or using mDNS.
3. Relay pipe lifetime: standing pipe per peer-pair, not per-connection. The reconnect-per-idle-teardown churn is not acceptable at keepalive cadence (the reaper would fire on every idle teardown, causing spurious partitions). A standing pipe is held open by the fed-module with periodic keepalives.
4. connect_offer to unverified: REQUIRED for pairing UX. The pairing ceremony requires the target to receive the offer and display the code. If the target must be verified first, the ceremony can't happen. Keep "allowed, loud" for v1. The loud join event + the verify-code ceremony + the operator's judgment are the protections.
5. WS hibernation vs signaling latency: acceptable to pay cold-wake on first signal. Cloudflare cold-wake is ~hundreds of ms, well within the 2s per-candidate timeout. For the "single login" UX, the user expects <1s, so the cold-wake is on the edge. Mitigate by sending periodic keepalives from the device to keep the DO warm. Or: accept the latency for v1, optimize later.

Overall verdict: GO-WITH-CHANGES. The three BLOCKERs must be fixed before build. The HIGH findings should be addressed in the design or as part of the build plan. The MEDIUM and LOW findings are acceptable for v1 with documented residuals.

Let me now write the final response. I'll keep it focused and disciplined.

One more sanity check: the phase-3 doc says the relay is "zero knowledge" (§7). The relay sees ciphertext between two pinned static keys. The relay learns "traffic volume/timing and the account's device graph — the same metadata the rendezvous already holds."

The relay is created per pipe grant. The relay DO bridges two WebSockets. The relay sees:
- The pipe_id (a random token)
- The volume of traffic (bytes per direction)
- The timing of traffic (when messages flow)
- The account's device graph (which two devices are connected)

The relay does NOT see:
- The tool call contents (Noise ciphertext)
- The device identities (the pipe_id is opaque; the device pubkeys are in the Noise handshake, which the relay forwards as ciphertext)

Wait — the Noise handshake is also forwarded as ciphertext. The relay doesn't see the pubkeys. The relay only knows the pipe_id and the traffic.

But the rendezvous knows the device identities (it issued the grant). So the rendezvous can correlate pipe_id to device identities. The relay itself doesn't know.

The doc says "the relay sees ... the account's device graph — the same metadata the rendezvous already holds." This is correct: the rendezvous already knows the device graph; the relay's knowledge is a subset (only when a pipe is open).

OK, the relay is zero-knowledge for content. Good.

One more: the phase-3 doc §5.3 says "connect_request {to: pubkey} → relayed to the target as connect_offer {from: pubkey, candidates}."

The connect_offer includes the initiator's candidates. The target sees the initiator's candidates. The target uses them to dial back? Or the target just adds them to its own candidate list?

The phase-3 doc §5.4 says "the initiator tries candidates in order: lan → public → relay." So the initiator dials. The target... receives the offer and waits. The target's own candidates are sent in connect_accept. Both sides have each other's candidates.

But the target doesn't dial (per the tie-break). So the target's candidates are for the initiator to try. The target waits for the initiator's connection.

OK, this is consistent. The "both sides race candidates" in §5.3 is misleading — it should say "both sides exchange candidates; the initiator dials."

This is part of the F6 finding.

OK, writing the response now. Let me be concise but thorough.

I realize I should also check: the phase-3 doc §4.2 says "Device removal: fed-cli device remove <name-or-fp> or CK app → DO deletes the record + revokes the token + broadcasts a registry delta."

"broadcasts a registry delta" — the DO pushes the delta to all online devices. The delta says "device X removed." The online devices update their local registry snapshot.

But: a device that is OFFLINE doesn't receive the delta. When it comes online, it gets the snapshot (via hello), which doesn't include the removed device. OK.

The risk: a device is online but its control WS is in the middle of a reconnect. The delta is sent before the reconnect completes. The delta is lost. When the reconnect completes, the device gets the snapshot (no removed device). OK.

The risk: the DO is slow to push the delta. The online devices have stale registries for a window. If a device tries to call the removed device in this window, the call goes to a removed device. The removed device's control WS may still be open (the DO hasn't closed it yet). The removed device receives the call... but the device is "removed" in the DO's view.

This is a race. The fix: the DO must close the removed device's control WS BEFORE pushing the delta. Or: the removed device's control WS checks its own status on every message and refuses if removed.

The doc says "DO deletes the record + revokes the token." The token is revoked. The removed device's control WS is still open (presumably). The removed device can still receive messages (registry deltas, connect_offers). The removed device's fed-module sees the delta and updates its local view. But the device is "removed" — the DO shouldn't be sending it deltas.

Actually, the DO pushes the delta to all OTHER devices (not the removed one). The removed device's control WS is closed by the DO. The other devices receive the delta.

The doc doesn't say the DO closes the removed device's control WS. The doc says "DO deletes the record + revokes the token + broadcasts a registry delta." The control WS closure is implicit.

The risk: if the DO doesn't close the removed device's control WS, the removed device can still send signaling (e.g., accept a connect_request). The accept is processed by the DO. The DO checks the device's status (removed) and rejects.

The fix: explicitly state that the DO closes the removed device's control WS as part of the removal.

This is a finding (F27): device removal — control WS closure not explicit.

OK, writing the response.

Let me also think about: the phase-3 doc §4.2 says the device token is "an opaque bearer bound to (account_id, device_pubkey), stored hashed in the DO, revocable, long-lived with rotation."

"bound to (account_id, device_pubkey)" — the token is bound to the account and the device's pubkey. The token can't be used for a different account or a different device. Good.

But: the token is a bearer token. Anyone with the token can use it. The binding to (account_id, device_pubkey) means the token can only be used for that specific (account, device) pair. It doesn't mean the token is bound to the device's hardware.

The risk: the token is stolen (e.g., from the 0600 file). The attacker uses the token to impersonate the device on the signaling plane. The attacker can't decrypt (no private key), but can DoS and phish.

The fix: bind the token to the device's hardware (e.g., a device attestation). This is not specified in the doc.

This is part of F24.

OK, writing the response.

Actually, I realize I've been thinking too long. Let me write the response now with a clear structure.

I'll structure it as:
1. Inheritance claim verification (separate section)
2. Findings list (BLOCKER, HIGH, MEDIUM, LOW)
3. Answers to the five open questions
4. Overall verdict

Let me keep the findings focused and actionable.

For the audit, I'll be specific about:
- The section reference
- The problem (adversarial, concrete)
- The fix (specific mechanism)

Let me write it now.

Actually, let me also note that the doc claims in §8 that "Stolen device token | impersonate that device to the rendezvous (signaling + registry) | Cannot speak Noise as the device (no private key); revocable server-side."

The claim is correct: the stolen token grants signaling-plane access, not Noise-plane access. The attacker can:
- Register candidates (lie about the device's network position)
- Accept connect_offers (impersonate the device in signaling)
- Reject connect_offers (DoS the device's connectivity)
- Phish the operator (offer a rogue device for pairing)

The attacker CANNOT:
- Speak Noise as the device (no private key)
- Decrypt or sign Noise messages

So the impact is bounded. The doc's claim is honest. Good.

But: the attacker can accept a connect_offer and then... what? The attacker opens the relay pipe. The caller sends the Noise handshake. The handshake fails (wrong key). The caller's fed-module sees a handshake failure. The caller retries. The attacker can keep accepting and failing, causing a DoS.

The fix: rate-limit the number of failed handshakes per device. Or: the caller should detect a handshake failure and surface it to the user.

The doc doesn't address this. This is part of F3.

OK, writing the response.

Let me also check: the phase-3 doc §5.3 says "connect_request {to: pubkey} → relayed to the target as connect_offer {from: pubkey, candidates} → target answers connect_accept {candidates} — both sides then race candidates (§6)."

The "both sides then race candidates" is the double-dial. The doc §5.4 clarifies: "the initiator (pubkey tie-break as phase 2) tries candidates in order." So the initiator dials. The responder... the doc doesn't say.

If the responder doesn't dial, then "both sides race candidates" is wrong. If the responder does dial, then the double-win race is real.

The doc is contradictory. This is a BLOCKER (F6).

OK, writing the response now. Let me keep it disciplined and focused.

Let me finalize the structure:

```
<COUNCIL_MEMBER_RESPONSE>
## Inheritance Claim Verification

[Table of claims and verification status]

## Findings

### BLOCKER

[F12, F6, F26]

### HIGH

[F1, F2, F4, F10, F13, F15, F16, F24]

### MEDIUM

[F3, F5, F7, F8, F9, F11, F14, F17, F20]

### LOW

[F19, F22, F23, F27]

## Answers to the Five Open Questions

[Q1-Q5]

## Overall Verdict

[GO-WITH-CHANGES with must-fix-before-build blockers]
</COUNCIL_MEMBER_RESPONSE>
```

OK, writing it now.

[assistant] 2026-07-07T12:02:55Z

[assistant] 2026-07-07T12:02:57Z

[assistant] 2026-07-07T12:02:59Z

[assistant] 2026-07-07T12:03:02Z

[assistant] 2026-07-07T12:03:04Z

[assistant] 2026-07-07T12:03:07Z

[assistant] 2026-07-07T12:03:09Z

[assistant] 2026-07-07T12:03:11Z

[assistant] 2026-07-07T12:03:14Z

[assistant] 2026-07-07T12:03:17Z

[assistant] 2026-07-07T12:03:19Z

[assistant] 2026-07-07T12:03:22Z

[assistant] 2026-07-07T12:03:25Z

[assistant] 2026-07-07T12:03:27Z

[assistant] 2026-07-07T12:03:30Z
Now I have enough material to do a deep adversarial review. Let me also check the credentials contract for any guidance on token custody, and look for the `keepalive`/`reap` semantics specifics in the actual design (3x reap window) since phase-3 inherits that.

[assistant] 2026-07-07T12:03:34Z

[assistant] 2026-07-07T12:03:37Z

[assistant] 2026-07-07T12:03:41Z
Let me check what the fed-module's actual code/specs say about candidate racing and the "pubkey tie-break" claim, and also about Noise session resumption (whether phase-2 actually supports re-handshake being "cheap").

[assistant] 2026-07-07T12:03:45Z

[assistant] 2026-07-07T12:03:48Z

[assistant] 2026-07-07T12:03:50Z

[assistant] 2026-07-07T12:03:53Z
I have enough context. The phase-2 doc does NOT actually specify a "pubkey tie-break" rule — phase 3 5.4 invents it as if it were inherited. Let me verify the reaper behavior for relay teardown and check whether Noise IK is actually used.

[assistant] 2026-07-07T12:03:56Z

[assistant] 2026-07-07T12:04:01Z
Good — I have what I need. Let me check the phase-2 doc once more for whether it actually supports the "verified-gate makes discovered peers non-routable" claim. The phase-2 design v4 says (5.3, line 171-172): "TOFU pinning: a peer's pubkey, once learned, is pinned locally" and "a cloud-introduced pair is non-routable (deny-all exposure) until the out-of-band verification code is compared and confirmed in the CK app on both ends." So the verified-gate is real. Good.

But there's a subtle issue: the phase-2 doc says the cloud introduces a pair and pinning happens at "first contact" with code comparison. Phase 3 5.5 says: "Registry-discovered peers materialize as **unverified profile peers** (name from registry, `verified:false`) — the existing enforced gate makes them non-routable until the ceremony." So the inheritance holds for the registration step. But the gap is the pairing UX: in phase 2, the user runs `fed-cli verify-code --confirm` once. In phase 3, the `connect_offer` arrives without ANY prior pubkey exchange; the target sees an offer from a brand-new pubkey, and the 4.3 text says "the rogue device's verify-code will not match anything the operator's real devices display" — meaning the **target's first encounter with the offerer's pubkey** is precisely the new-enrollment path. The phase-2 design says "cloud-introduced pairs are non-routable until the out-of-band code is compared." The phase-3 design claims this protection. But the design needs to nail down HOW the operator actually performs the comparison against a cloud-introduced pair — the doc says "CK app will render this as compare-and-tap" — which requires the target to display the code at offer time, not just to display a "you have a new device" notification. Let me check this against phase-2 for completeness.

Actually, the phase-2 doc lines 173-174 say: "Rotation ceremony: a legitimate key rotation must be signed by the old key (tombstone chain) or confirmed via an already-verified device / manual re-pairing; any other key change presents as first contact (non-routable + code comparison). 'Changed key' is never a bare accept/reject prompt." So the ceremony logic is inherited. Good.

Now I have everything I need to write the adversarial review. Let me finalize.

A few more critical checks: I should verify the claims about (1) "verify-code ceremony in pairing UX window" being enforced, (2) Noise session resumption being "ordinary re-handshake, cheap", and (3) the inheritance of "carrier-blind" for keepalive/reaping. Phase 2 6.2 line 213-214: "Per-peer keepalive with a bounded numeric staleness window" and "fed-module's keepalive reaper is the AUTHORITATIVE partition classifier" — but the reaper is described as driven by per-peer keepalive, not by the carrier. So the inheritance claim that "relay-path partitions classify exactly like TCP partitions" is reasonable IF the reaper looks at the Noise session's transport-layer liveness, not at the WS socket itself. The doc does not specify this. This is a finding.

Now let me check the "Noise session resumption = ordinary re-handshake" claim. Phase-2 2.2 says "Noise IK" — but doesn't specify whether session tokens are used for resumption. The phase-3 doc line 204 asserts resumption is "ordinary re-handshake, cheap." But Noise IK itself has no built-in session resumption without an application-level token (the "psk" mode or a separate resumption protocol). An "ordinary re-handshake" means a full XX-style or IK-style key exchange every reconnect. Over WS, that's 1 extra round trip + a DH per reconnect. At keepalive cadence + idle teardown + reconnect, this is not "cheap" — it costs a DH op per reconnect per side. But more importantly, **the effect_id's incarnation epoch assumes a stable Noise session**, not one that resets every idle. An effect sent at incarnation N over a Noise session that gets torn down — what does "session resumption" mean for the effect ledger? Phase 2 6.1 says the **dedup ledger** is on the serving side, keyed by effect_id — so the Noise session lifetime is independent. OK, that's fine. But the **origin-side send-log** state machine in phase 2 6.1 (lines 196-207) requires an "intent fsynced BEFORE the first network write" — and the `network write` is now possibly over a new pipe after a relay idle-teardown. The phase 2 mechanics don't change here — the fed-module just needs to make sure its write actually crossed. But the doc says nothing about how the reaper/recovery reconciles with a fresh pipe carrying a new Noise session for the same logical connection. This is a finding too.

Let me check one more inheritance: "verified-gate makes discovered peers non-routable." Phase 2 5.3 line 172: "a cloud-introduced pair is non-routable (deny-all exposure) until the out-of-band verification code is compared and confirmed in the CK app on both ends." The phrase "on both ends" is the key. In phase 3, when device A enrolls and gets pushed to device B's registry, B sees a registry entry. To become routable, A's pubkey must be compared against B's view of the **same pubkey** (the one the rendezvous presented). B displays a code derived from B's static key + A's static key. But A also needs to display the same code. If A enrolls, the rendezvous pushes A's pubkey to B. B sees "new device from cloud, pubkey X, code Y". A's CK app needs to display "you're pairing with the device whose pubkey is X, code Y". So both display the same code. But the code derives from BOTH static keys. So both sides need to know the OTHER side's static key first. The phase-3 doc 4.2 enrollment binds the pubkey; the registry has the pubkey. So the offer-side has the target's pubkey (from the registry). The target side has the offerer's pubkey (from the connect_offer payload). So both sides can compute the safety number. OK, that works. **But:** the phase-3 doc says "the rogue device's verify-code will not match anything the operator's real devices display" (line 108). This is only true if the verify-code is the SHA-256 safety number from phase 2 — and is computed locally from both pubkeys. If the rendezvous is compromised and offers a rogue device with a rogue pubkey R, then device A computes a code from R and B's static key. B computes a code from R and A's static key. Wait, those are different codes. So both codes are "the same" only if A == B in this computation. Let me re-read the phase-2 ceremony: "the code binds **both endpoints' long-term device static keys**" (line 16 of phase 2 changelog). So if A initiates pairing to B, the code is `SHA256(A_static || B_static)` (or similar). A's display: `SHA256(A_static || B_static)`. B's display: `SHA256(B_static || A_static)`. These must be equal. The phase-2 design says it's "order-independent" — so it's `SHA256(sorted(A_static, B_static))` or some normalization. Good. So a rogue R gets a different code than the legitimate A. The claim holds. But the phase-3 doc text is sloppy about which pubkey the target's display derives from — and there's a subtle issue: if the rendezvous is compromised and alters the pubkey IN the connect_offer, then A might compute a code based on the legitimate B-pubkey (from its registry) while B's display shows a code based on the altered pubkey. They won't match. Good, that's the point. But the code compare happens on B's side based on what B got from the offer. So the inheritance is OK as long as the code is derived from both pubkeys INDEPENDENTLY by each side and compared by the user, NOT shown to the user by the rendezvous. The phase-3 doc doesn't explicitly say this, but the phase-2 ceremony is inherited. This is a minor finding.

One more — the `connect_offer` allows targeting an UNVERIFIED peer (open question 4). This means a target gets connection offers from pubkeys it's never seen. The target's verify-code display will be `SHA256(target_static || new_attacker_pubkey)`. The attacker (claiming to be A) computes `SHA256(attacker_pubkey || target_static)`. **But the attacker can also see the registry — they have an account too — and they know target_static.** So the attacker displays a code. The target displays a code. They won't match unless the attacker IS the legitimate A. The ceremony works. But the target gets spammed with offers from strangers. The "loud" answer is the design intent (line 263: "Current design: allowed, loud"). But the design must require the target to render a code, NOT a "yes/no" prompt. If the user is presented a "Yes, allow" button without a code, the threat model collapses. This is open question 4 itself. I'll address it there.

Now I have enough. Let me also do a quick check on whether the phase-2 doc actually supports the "reaping unchanged" claim. The phase-2 design 6.2 (line 214): "the fed-module's keepalive reaper is the AUTHORITATIVE partition classifier. On declaring a peer partitioned it closes that peer's loopback connections." This reaper is fed by "per-peer keepalive with a bounded numeric staleness window" (line 213). The phase-3 doc says "Keepalive cadence and 3× reap window unchanged." So the inheritance is: the fed-module runs the SAME reaper regardless of carrier. But the reaper's input is the carrier's liveness. For TCP, that's the socket. For WS-via-relay, the carrier is the WS pipe to the RelayDO, AND the peer at the other end. The reaper needs to consider: (a) WS socket to relay dead → drop, (b) relay tells us peer disconnected → drop, (c) keepalive from peer not arriving → drop. (a) and (b) are NOT the same as a TCP keepalive. (c) IS the same. The doc claims "relay-path partitions classify exactly like TCP partitions" — but a relay idle teardown is NOT a peer partition; the peer might be fine. This is a real correctness finding.

Now let me write up the final review. I have a comprehensive view.

Let me also verify the "noise session resumption = ordinary re-handshake, cheap" — checking phase 2 doesn't actually claim this.

[assistant] 2026-07-07T12:04:34Z
Confirmed — "resumption = ordinary re-handshake, cheap" is asserted ONLY in the phase-3 doc, line 204. The phase-2 doc says nothing about Noise resumption semantics; it just says "Noise IK" sessions. So the "cheap" claim is unsupported by phase-2 inheritance. This is a finding to check (it might still be true, but the inheritance is fictitious).

Now I have my full adversarial review. Let me write it up.

<COUNCIL_MEMBER_RESPONSE>
# ADVERSARIAL DESIGN REVIEW — subc Federation Phase 3 (DRAFT v1)

Scope: I read both `docs/subc-federation-phase3-design.md` (DRAFT v1) and `docs/subc-federation-design.md` (v4.1, shipped phase 0-2) end-to-end. Where phase 3 asserts an inherited property, I checked the phase 2 doc for the supporting clause. Inheritance claims that do not survive the check are findings.

Verdict at the bottom: **NO-GO**. The design has shape but ships at least three BLOCKERs and a flock of HIGHs, all of them in the *implementation* of the locked decisions (which is exactly what is in scope).

---

## F1 — BLOCKER: Phase-3 contradicts the phase-2 6.1 effect-ledger mechanics with "Noise session resumption = ordinary re-handshake, cheap"

- **Severity**: BLOCKER
- **Location**:  (Relay), line 203-204
- **Confidence**: HIGH
- **Issue**: The phase-2 doc (v4.1) specifies the at-most-once state machine in 6.1 (lines 196-207): intent fsync BEFORE the first network write; outcome fsync BEFORE the consumer sees the reply; on restart, recovery reconciliation queries the SERVING ledger for every `sent`-without-outcome effect. The effect_id carries a stable incarnation epoch. The reaper closing a peer's loopback connection is the partition classifier (line 214). Phase 3  adds idle teardown of the RelayDO pipe and says "peers reconnect via a fresh grant — Noise session resumption = ordinary re-handshake, cheap." This combines into a correctness gap:
  1. The reaper is fed by per-peer keepalive (phase 2 6.2 line 213). With a relay pipe, the **carrier-level** liveness is two things: (a) the WS to the RelayDO, and (b) the peer at the other end. Idle teardown is (a) only — it is NOT a peer partition. The reaper must NOT classify a relay-idle as a peer partition; today nothing in the doc says how the reaper distinguishes "pipe torn down, peer still alive" from "peer partitioned." The phase-2 reaper has no concept of "torn down pipe, fresh pipe to come."
  2. A "fresh grant" carries a fresh Noise IK handshake — that is a *new* cryptographic session. Phase 2 6.1's recovery reconciliation keys entirely off the (origin_device_pubkey, incarnation_uuid, seq) effect_id and the serving-side dedup ledger. The Noise session is irrelevant to the dedup ledger, but the doc's claim of "cheap" is also wrong: each reconnect is a full DH (X25519) on both sides plus a round trip through the relay. At keepalive cadence + per-idle-teardown reconnect, that is non-trivial for an idle connection that just woke up.
  3. The phase-2 inheritance claim "relay-path partitions classify exactly like TCP partitions" (line 195) does not survive the check: the phase-2 doc's partition classifier looks at per-peer keepalive and closes that peer's loopback connections. It is silent on WS/relay carriers. "Exactly like" is asserted but not inherited.
- **Evidence**: phase-2 6.1 lines 196-207 (effect-ledger mechanics keyed on effect_id, not Noise session lifetime); phase-2 6.2 lines 213-214 (reaper semantics); phase-3  line 194-195 and  line 203-204.
- **Fix**:
  - Define a separate "pipe state" abstraction in the fed-module: a logical peer-connection survives a relay pipe teardown. The reaper fires on the logical connection's keepalive (fed-module-level, not carrier-level). A relay-idle is a TRANSPORT EVENT, not a partition event; it triggers a fresh `relay_open` and a Noise IK re-handshake, but the reaper's clock and the effect ledger's incarnation epoch are unaffected.
  - Move the "cheap" claim out: a full IK re-handshake over a relay is O(1 DH + 1 RTT). It is acceptable, not cheap. State so. If "cheap" is a hard product requirement, add a Noise resumption token persisted across the pipe-idle window (an application-level 0-RTT resumption) — but that is non-trivial design and must be specified.

## F2 — BLOCKER: `connect_offer` allowed to unverified targets breaks the threat-model claim for the pairing UX window

- **Severity**: BLOCKER
- **Location**: 4.3 (F4 locked), 5.3 (signaling ops), open question 4
- **Confidence**: HIGH
- **Issue**: 4.3 claims "A cloud/account compromise therefore yields metadata + the ability to *offer* a rogue device for pairing — it cannot make any existing device trust the rogue one, and the rogue device's verify-code will not match anything the operator's real devices display." 5.3 lists `connect_request → connect_offer` to an arbitrary `to: pubkey` (no `verified:true` precondition). Open question 4 confirms the current design is "allowed, loud."
  The phase-2 ceremony (5.3 line 172 of v4.1) gates first contact with a *code comparison*. The phase-3 doc, however, never says how the CK app surfaces the ceremony to the target for an incoming `connect_offer` from a previously-unknown pubkey. The most common UX failure is "Accept/Deny" buttons instead of a code. With Accept/Deny, the operator clicks Accept → the rogue device becomes routable. The verify-code protection is a property of the **rendering**, not of the protocol. The doc has nothing enforcing that rendering. "Loud" join events (line 110) are noise, not ceremony.
  Compounding: 5.3's `connect_request` is relayed to the target. The target is presented with an offer from a pubkey it has never seen. If the target's CK app renders "Pair with 'unknown device', pubkey 0xAB…? [Accept] [Deny]" — which is the easy implementation — the operator is one tap from trusting the rogue device. The doc has no contract on this rendering.
- **Evidence**: phase-2 5.3 line 172 ("non-routable … until the out-of-band verification code is compared and confirmed in the CK app on both ends"); phase-3 4.3 lines 104-110, 5.3 lines 150-152, open question 4 line 261-263.
- **Fix**:
  - State explicitly that `connect_offer` to an unverified pubkey forces the **SHA-256 safety-number display** on the target, computed independently by both sides from both static keys. The accept gesture is a single button labelled "Codes match"; there is no Accept/Deny without a code.
  - Require the target's accept message to include a *signature* over the offered safety number using the target's static key, and the offerer to verify it; this catches an attacker who rewrites the safety number post-offer.
  - Add an explicit `verified:false` flag on every effect on the discovered peer until the ceremony completes; the fed-module refuses to forward tool calls (not just refuses routing) until verification, not just refuses binding.

## F3 — BLOCKER: Pipe-token model is underspecified in a way that almost certainly allows cross-peer and cross-pipe redemption

- **Severity**: BLOCKER
- **Location**: 5.3 line 153;  line 199-200
- **Confidence**: HIGH
- **Issue**: 5.3: `relay_open {to: pubkey}` returns `relay_grant {relay_url, pipe_id, pipe_token}` "issued to both sides" ( line 200).  "per-side `pipe_token`, single-use, short TTL." Three problems compound:
  1. **Who can consume whose grant?** The doc does not say whether `pipe_token` is bound to a specific device pubkey. If it isn't, device X receives a `relay_grant` for `pipe_id=P`, and device X redeems it to open its end; but Y's grant for the SAME `pipe_id=P` is also a `pipe_token` — if X can also present Y's `pipe_token` to the RelayDO, X can masquerade as Y on the relay. The doc must bind each `pipe_token` to `(pipe_id, device_pubkey)` at mint time and enforce on consume.
  2. **"Single-use"** for the *consume* step is specified, but `pipe_id` is the pipe itself — a WS hub at the RelayDO. The RelayDO accepts TWO consumes per pipe (one per side). What "single-use" means here is that each `pipe_token` is redeemed once. But the doc does not specify: (a) what happens if a side redeems, then disconnects, then tries to re-redeem the same `pipe_token` (probably rejected — good); (b) what happens if the pipe is fully formed and idle-tears down, can the same `pipe_id` be reused with a fresh grant? The doc says "peers reconnect via a fresh grant" but does not say whether the `pipe_id` is a new value or whether the old `pipe_id` is retired and a new one minted.
  3. **RelayDO auth-before-resource** (inherited from phase 2 5.5 line 184) is stated at the high level but the doc does not say: the RelayDO must verify the connecting WebSocket presents a `pipe_token` whose binding matches the WS's authenticated device identity (which is the device token from 4.2). Otherwise an attacker who steals one `pipe_token` and one device token can ride any pipe.
- **Evidence**: phase-3 5.3 line 153,  lines 199-200,  line 220; phase-2 5.5 line 184 ("auth-before-resource (a connection proves device identity before any forwarding state is allocated)").
- **Fix**:
  - Specify `pipe_token = HMAC(pipe_id_key, pipe_id || device_pubkey || exp || nonce)`; the RelayDO looks up `pipe_id`, verifies the HMAC under its `pipe_id_key`, and refuses if `device_pubkey` in the token does not match the WS's authenticated device identity (which itself is bound to a device token presented at WS upgrade).
  - State explicitly: on idle teardown, BOTH sides must present a NEW `pipe_token` to a NEW `pipe_id`. The old `pipe_id` is atomically retired. Cross-pipe continuity is a fed-module concern, not a RelayDO concern.
  - On re-handshake, require the device token to be re-presented (a stale `pipe_token` alone is insufficient).

## F4 — BLOCKER: Rendezvous-down fallback path is asserted but not engineered, and the "candidate-priority dialing" claim depends on it

- **Severity**: BLOCKER
- **Location**: 5.1, 5.4, 5.5
- **Confidence**: HIGH
- **Issue**: 5.5 states "Absent → phase-2 behavior exactly (static profiles keep working forever; the WAN test rig never needs an account)." This is the inheritance claim. But the candidate-priority dialer in 5.4 — lan → public → relay — needs candidates to come from *somewhere*. If the `[rendezvous]` section is present but the rendezvous is unreachable (Worker down, account DO cold-waking, network partition), the candidate list is empty or stale. Phase 2 has no notion of "candidate list from the cloud"; the fed-module's profile has static `addr` lines, and that's what it dials.
  The phase-2 design's partition classifier (line 214) closes the per-peer loopback connection on a missed keepalive. With a rendezvous present-but-down, every per-peer connect goes through the rendezvous. The fed-module's reaper sees the carrier's WS to the rendezvous as alive (it's still up) but signaling responses time out. The reaper's input is the per-peer keepalive to the PEER, not the rendezvous. So a rendezvous-down state is not classified as a partition; it's an indefinite "I have no candidates" state. The design asserts "static profiles remain the availability fallback" but does not specify the code path that says: "if rendezvous is unreachable, dial the static `addr` from the profile" — let alone how the fed-module decides the rendezvous is "down" vs "empty."
  A simpler attack: an attacker who can keep the rendezvous WS open (e.g. by holding a valid device token) but never relay signaling — the static-fallback path is fine, but the candidate-priority path silently no-ops. Worse: if the rendezvous is held open but `connect_request` is dropped on the floor (signaling abuse), the operator gets a hung UX with no diagnostic.
- **Evidence**: phase-2 5.5 line 184 and 6.2 line 214 (reaper is per-peer, not per-rendezvous); phase-3 5.4 line 161-169 (candidate priority), 5.5 line 173-180 (rendezvous section + absent behavior),  line 225-226 ("static profiles remain the availability fallback").
- **Fix**:
  - Specify a **rendezvous-dead timer** on the fed-module: after N seconds of rendezvous-unresponsive, mark rendezvous unavailable, fall back to profile's static `addr` lines as the only candidate. Re-attempt rendezvous on a slow background schedule. State the timer (e.g. 5s; document rationale).
  - Make signaling timeouts explicit and short. `connect_request → connect_offer` is delivered to the rendezvous; if no `connect_offer` reaches the peer within T1, retry once; if no `connect_accept` within T2, fail. T1, T2 must be tuned against WS-hibernation cold-wake latency (open question 5).
  - Add a `rendezvous` status field to the `registry_delta` so the fed-module can distinguish "registry says zero candidates" from "registry unreachable."

## F5 — HIGH: Re-enrollment after `revoked_by_account` is underspecified and creates a TOFU-pinning hole

- **Severity**: HIGH (not BLOCKER because the phase-2 ceremony's safety number still gates it, but the spec is missing a contract)
- **Location**: 4.2 line 95-98
- **Confidence**: HIGH
- **Issue**: Phase-2 5.3 (line 173-174): "Rotation ceremony: a legitimate key rotation must be signed by the old key (tombstone chain) or confirmed via an already-verified device / manual re-pairing; any other key change presents as first contact (non-routable + code comparison)." Phase-3 4.2: "Device removal: … profile entry flips to `revoked_by_account`, non-routable, requires re-pair if re-enrolled."
  The re-enroll case is not specified. Two distinct scenarios:
  1. **Same device, same key** (user lost access, got it back): the device re-enrolls with the SAME pubkey. Should the rendezvous re-bind the existing device record, or reject because the device was removed? Today the doc does not say. If it re-binds, the operator's other devices still have the pubkey pinned, but the device record in the AccountDO is fresh. A compromised cloud can re-bind to a stolen pubkey if it can produce a WorkOS login.
  2. **Same device, new key** (key loss / re-key): the device re-enrolls with a NEW pubkey. This is a key rotation, which phase 2 says must be confirmed via an already-verified device. The phase-3 doc says "requires re-pair" but does not say: (a) does the rendezvous notify other devices of the new key? (b) does the operator have to re-verify the new key on each existing device? (c) what if the new key collides with an existing pubkey? (d) what if the new key is the rogue-device's key (a cloud-introduced substitution)?
  The phase-2 design's "tombstone chain" inheritance is *partially* honored ("requires re-pair") but the cloud-side story is missing: the rendezvous has no protocol to chain an old key to a new key. A rogue device with WorkOS access and a fresh pubkey can replace the old pubkey in the AccountDO and present itself as "re-enrolled" to existing peers, who then see a new pubkey to compare against (which won't match the old one they had pinned).
- **Evidence**: phase-2 5.3 lines 171-174; phase-3 4.2 lines 95-98.
- **Fix**:
  - Specify the re-enroll flow: when a device re-enrolls, the AccountDO must publish a `registry_delta` containing `(device_pubkey_old, device_pubkey_new, tombstone_signed_by=old_or_verified_device)`. Other devices receiving the delta must:
    - Verify the tombstone is signed by the OLD key OR by a currently-verified device (not just a sibling device that is itself unverified).
    - Treat the new key as **first contact** until the operator re-verifies — even if they previously verified the old key. This is what phase-2 says; phase-3 must restate it.
  - For same-key re-enroll after removal: the AccountDO must require a WorkOS-side proof of access (not just a device token — the device token was REVOKED). WorkOS is the only root of identity for enrollment. The new device token binds to the existing pubkey.

## F6 — HIGH: Candidate self-reporting is a SIBLING-DEVICE trust hole

- **Severity**: HIGH
- **Location**: 5.2 line 126-143, open question 2
- **Confidence**: HIGH
- **Issue**: 5.2 lets a device self-report `candidates`, including the `lan` candidate. Open question 2: "any hardening needed against a malicious sibling device on the same account lying about its LAN addr?" The current answer is "it can only redirect its own inbound dials; Noise pins identity regardless."
  The answer is wrong in two ways:
  1. **A sibling CAN redirect the OPERATOR's outbound dials.** If the operator on device A dials the `lan` candidate published by sibling B (which says `192.168.1.34:7841` but really is `attacker.local:9999`), the dial is from A to attacker. The Noise handshake then fails — the attacker doesn't have B's static key. So far so good. But:
  2. **A sibling CAN lie about the operator's `lan` candidate on behalf of OTHER operators.** If the AccountDO trusts self-reported candidates without rate-limiting or sanity-checks, a compromised sibling can flood the registry with many fake candidates across all its peers, and the next dial to any peer that uses the registry as primary source goes to attacker-controlled infra. Noise handshake fails, but the dialer learns `attacker.local` exists, and if the attacker also has B's static key (from a separate compromise), the dialer completes a Noise session with the WRONG peer. The verify-code display on A's side is for the claimed pubkey (B's), but the dial is to the attacker's endpoint, which is using B's real key. The code compares and matches. The operator has no signal that the dial is to attacker infra.
  This is a real, exploitable false-routing attack. The "noise pins identity regardless" rebuttal is wrong because the dial is E2E — the Noise session is with whoever owns the static key at the address it dials. The address is the variable.
- **Evidence**: phase-3 5.2 lines 126-143, 5.4 lines 161-169; phase-2 2.2 (data plane is E2E by design — meaning the dial endpoint IS the security boundary).
- **Fix**:
  - Bind candidates to (account, device_pubkey) and pin: the *first* time a candidate is observed for a device, the operator (on each other device) sees a "first-seen candidate" prompt and must accept before future dials use it. Subsequent candidates of the SAME kind must match the same pattern (e.g. IP/24 for public, IP/octets for LAN) or trigger the same prompt.
  - For the `lan` candidate specifically: dial the local-network BROADCAST first (mDNS or a small LLMNR-style query for the device_pubkey fingerprint) and verify the responding endpoint holds the right static key. Only use a self-reported `lan` candidate if the broadcast confirmation succeeds. The Noise handshake already does this cryptographically; what is missing is binding the candidate to "this is the network address where I've SEEN this key" rather than "the device said this is its address."
  - Server-observed `public` candidates are already stronger (the Worker stamps from observed source IP) but note: the `public` candidate is also self-reportable from the device — the Worker must ALWAYS overwrite, never accept, the `public` from the device payload.

## F7 — HIGH: Device-token revocation vs in-flight signaling is racy

- **Severity**: HIGH
- **Location**: 4.2 line 95-98
- **Confidence**: HIGH
- **Issue**: The phase-2 doc 5.5 (line 184) says the relay does "auth-before-resource (a connection proves device identity before any forwarding state is allocated) and enforces per-account/per-device quotas + rate limits, so an attacker can't amplify or exhaust the relay." Phase-3  (line 222): "Stolen device token | impersonate that device to the rendezvous (signaling + registry) | Cannot speak Noise as the device (no private key); revocable server-side."
  The "revocable server-side" claim is correct, but the doc does not specify the revocation semantics for in-flight signaling. Concretely:
  1. Attacker holds a stolen device token. The legitimate operator revokes the token. Between revoke and the AccountDO propagating the revoke to all in-flight sessions, the attacker can still:
     - Complete a `relay_open` against a victim device (the grant is then issued to BOTH sides, and the legitimate device accepts a relay pipe from the attacker).
     - Re-issue signaling (push a `connect_offer`).
  2. The device token has rotation, but rotation is not specified. If a device token is rotated while a peer holds a stale token, the peer's next `hello` is rejected — but the doc does not say when. If rotation is "operator-initiated via CK app," there is a window where the new token is delivered to the device but the AccountDO has not yet marked the old one revoked; the attacker can race this window.
- **Evidence**: phase-2 5.5 line 184; phase-3 4.2 lines 86-99,  line 222.
- **Fix**:
  - Specify token revocation as: AccountDO maintains a `revoked_token_set`; on every `hello`, the WS is checked against current valid AND against a small "recently revoked" window (e.g. last 5 minutes). On hit, the WS is closed and an out-of-band notification is sent to the legitimate device.
  - Token rotation: rotation produces a NEW token AND atomically invalidates the OLD token. The new token is delivered to the device over the existing authenticated channel; the old token is invalid from that moment. There is no "two valid tokens" window.
  - `relay_open` issued while a token is later revoked: the RelayDO must check token validity on every WS frame, not just at upgrade. If revoked, drop the WS and tear down the pipe.

## F8 — HIGH: Signaling abuse — no nonce, no timestamp, no per-account/per-pubkey rate limit

- **Severity**: HIGH
- **Location**: 5.3
- **Confidence**: HIGH
- **Issue**: 5.3 lists the signaling ops: `hello`, `registry_delta`, `connect_request`, `connect_offer`, `connect_accept`, `relay_open`, `relay_grant`. None carries a nonce or timestamp.  (line 220) says "Signaling abuse: connect_request floods" is not in the threat model — the doc notes only that "wrong addresses" is possible.
  Concrete attacks:
  1. **Replay.** A `connect_request` captured by an attacker (e.g. on-path) is replayed N minutes later. The rendezvous delivers a duplicate `connect_offer` to the target. The target, having already paired with the offerer, sees "connect_offer" and either silently completes the dial (no ceremony check) or shows a duplicate UI. The doc does not say.
  2. **Flood.** A compromised device token (or a stolen WorkOS account) issues `connect_request` to every device in the account repeatedly. The target's CK app gets spammed. The relay gets grinded.
  3. **Cross-account injection.** A compromised cloud can issue `registry_delta` to any account's devices (no per-account or per-device signing). The doc does not specify that registry deltas are signed by the AccountDO. If they are not, a compromised Worker can lie.
- **Evidence**: phase-3 5.3 lines 146-159; phase-2 5.5 line 184 mentions quotas but only for the relay, not the rendezvous.
- **Fix**:
  - Add a `nonce + timestamp + signing_account_id` envelope to every signaling message. The AccountDO signs outbound `registry_delta` and `connect_offer` with an account-binding key (rotated; the device can verify against the WorkOS-issued account token or a separate account pubkey).
  - Rate limit `connect_request` per (account, source device pubkey) AND per (account, target device pubkey). Default: 10/min per source, 30/min per target.
  - `connect_offer` MUST carry the offerer's current `registry_seq` or equivalent freshness tag so the target's fed-module can reject stale replays.

## F9 — MEDIUM: AccountDO is a hot spot and the design doesn't bound it

- **Severity**: MEDIUM (operational, not security)
- **Location**: 5.1
- **Confidence**: HIGH
- **Issue**: "one per account" is the design. For a small user base (paid tier, individual), this is fine. For any team / shared-account topology later, one DO funnels all signaling+registry. Cloudflare DO throughput is per-DO; a single DO is a single-threaded state machine. The design's "via Cloudflare" inheritance is OK for v1 individual users; the doc should say so.
- **Fix**: Document the per-account throughput assumption (signaling ops/sec/account) and the failure mode if exceeded (backpressure on the WS, queue depth, dropped messages). Defer sharding/team-DOs to phase 4+.

## F10 — MEDIUM: WS-hibernation cold-wake will blow signaling timeouts

- **Severity**: MEDIUM
- **Location**: 5.1, 5.4, open question 5
- **Confidence**: HIGH
- **Issue**: 5.1: "hibernation-friendly — WS hibernation keeps idle accounts at ~zero cost." 5.4: "short per-candidate timeout (~2s)." Open question 5: "acceptable to pay a cold-wake round-trip on first signal to an idle account?"
  A Cloudflare DO WS cold-wake is documented (in Cloudflare's own docs) to be tens to hundreds of ms in the best case, seconds in the worst case. A 2s per-candidate timeout will routinely be blown. The 2s number is not justified.
- **Fix**:
  - Bump the signaling timeouts (e.g. T1=connect_request→connect_offer=5s; T2=connect_offer→connect_accept=10s) to absorb cold-wake jitter. Document the choice.
  - Add a "wake-warm" heartbeat from device to AccountDO on application-level events (any control-plane activity) so the DO is warm during normal usage.
  - Probe-and-pin: keep the WS warm when the device is in active use; only let it hibernate after N minutes of zero activity.

## F11 — MEDIUM: `revoked_by_account` re-pair path — pubkey re-enrollment after revocation bypasses the cloud's idea of the operator's intent

- **Severity**: MEDIUM
- **Location**: 4.2, 5.5
- **Confidence**: MEDIUM
- **Issue**: The doc says "Device removal: … profile entry flips to `revoked_by_account`, non-routable, requires re-pair if re-enrolled." This is correct for the same-account peer. But the AccountDO has no record that the operator explicitly authorized the re-enroll. A compromised WorkOS account can re-enroll any pubkey. Phase 2 5.3 (line 174) says the residual is "a cloud-tier user who skips the code comparison is trusting the cloud at introduction time." Phase 3 inherits this. But the re-enroll case is *not* an introduction; it is a re-introduction after removal. The operator who removed the device is no longer presented with a "compare codes" UX on the OTHER devices — they are presented with a "the device is re-enrolled, restore?" prompt. The doc does not specify which.
- **Fix**:
  - On re-enroll, the operator's OTHER devices must go through the FULL code-compare ceremony, exactly as for first contact. Not a restore prompt.
  - The AccountDO must publish a "device was re-enrolled" event that the operator's CK app surfaces LOUDLY, separately from "device was added" (which is a first-contact event).

## F12 — MEDIUM: Device-token custody on disk is exposed to an entire class of local attacks

- **Severity**: MEDIUM
- **Location**: open question 1, 4.2 line 88-91
- **Confidence**: HIGH
- **Issue**: Open question 1: "0600 file next to device key — sufficient for v1, or fold into the credentials vault immediately?" The 0600 file is plaintext-readable by any process running as the same user, and any process with the same UID on a misconfigured host. The fed-module's device key (Noise static) is presumably protected by a `reserved`+`launch_nonce` mechanism, so it can't be read by an arbitrary process — but a device token sitting next to it as plaintext is a strictly lower bar. The doc's threat model says "stolen device token | impersonate that device to the rendezvous" — the bar to steal is the same as the bar to read the device key file, which is the bar to read any file the user can read.
  This is a real issue because the **rendezvous** is the most attractive target — it carries the device graph and is the relay-grant authority. A device token is enough to make the rendezvous do useful work for the attacker (signaling abuse, registry manipulation, `relay_open` grinds).
- **Fix**:
  - For v1: store the device token ENCRYPTED at rest, keyed by a per-host key derived from the device key's static secret (or from a separate derived host-secret). The fed-module decrypts on use; the on-disk form is opaque to other processes.
  - For phase 4 or sooner: fold into the credentials vault (per `docs/cortexkit-credentials-contract.md` — which I have not loaded in detail but is referenced as the vault contract). State the choice in the doc.

## F13 — MEDIUM: WS carrier framing — "one fed record per WS message" is asserted but not enforced

- **Severity**: MEDIUM
- **Location**:  line 187-191
- **Confidence**: HIGH
- **Issue**:  "one fed record per WS message; the 4-byte length prefix is redundant inside a message framing but kept identical so record parsing is carrier-agnostic and golden vectors hold." The 4-byte length prefix is REDUNDANT inside a single-record-per-message framing — but the doc keeps it. The receiver's parser is fed "exactly one record per message" — but on a multi-record or zero-record message, the doc says nothing.
  Concretely:
  1. If a malicious relay (or a buggy fed-module) emits a WS message with TWO records concatenated and the redundant length prefix points to the first one, the receiver reads one record and discards the rest (lossy) or errors (no spec).
  2. If the redundant length prefix disagrees with the WS message length, the receiver has no spec.
  3. The "kept identical so golden vectors hold" rationale is weak: a parser that asserts length-prefix-equals-WS-message-length is stricter than a parser that asserts length-prefix-equals-record-length-only. The doc's spec is ambiguous.
- **Fix**:
  - Specify the receiver's contract: on a WS message of N bytes, expect the 4-byte length prefix to be exactly N-4. If it differs, close the carrier with a framing error. The redundant prefix is then a sanity check, not redundancy.
  - On a multi-record or zero-record message: reject with a framing error. No lossy behavior.

## F14 — MEDIUM: "Carrier-blind keepalive" inheritance — relay teardown is NOT a partition, but the reaper will treat it as one

- **Severity**: MEDIUM
- **Location**:  line 192-195, 
- **Confidence**: HIGH
- **Issue**:  "Keepalive cadence and 3× reap window unchanged; relay-path partitions classify exactly like TCP partitions." The phase-2 6.2 (line 214) reaper "closes that peer's loopback connections" on a missed keepalive. If the fed-module treats a relay pipe teardown as a missed keepalive (because the carrier's WS dropped), it closes the peer's loopback connections — even though the peer's fed-module may be about to open a fresh pipe via a fresh `relay_grant`. The "OutcomeUnknown" storm on the consumer side is the result.
  The phase-2 reaper is per-peer, not per-carrier. The phase-3 doc needs to specify: a relay pipe teardown is a TRANSPORT-LEVEL event, not a PEER-LEVEL event. The reaper must NOT fire on a relay teardown; it must wait for either (a) a fresh pipe to be established within a small budget, or (b) a true keepalive miss to fire.
- **Fix**:
  - Specify a "pipe-torn-down grace period" (e.g. 3× the keepalive cadence) during which the reaper is suspended for the affected peer. If a fresh pipe establishes within the grace, the reaper resets. If not, the reaper fires.
  - This grace must apply to the logical peer, not to the carrier. A subsequent relay-pipe establishment is the same peer; the reaper's "missed keepalive" counter must be reset on any successful round-trip with the peer, regardless of carrier.

## F15 — MEDIUM: Effect-ledger recovery reconciliation across a relay pipe reconnect is unspecified

- **Severity**: MEDIUM
- **Location**:  
- **Confidence**: HIGH
- **Issue**: Phase 2 6.1 (line 204): "intent durable, send unconfirmed | OutcomeUnknown; recovery queries the SERVING ledger for the effect_id: ledger hit → settle with the recorded outcome; miss + peer reachable → provably not executed → not_sent tombstone; miss + peer unreachable → stays unknown until reachable." With a relay pipe that idle-tears-down mid-effect:
  1. The origin fed-module fsynced `intent`, sent the bytes over a relay pipe.
  2. The relay idle-tears-down before the response arrives.
  3. The origin fed-module's local state: intent durable, send unconfirmed, peer "partitioned" (per the reaper bug in F14).
  4. On restart or reconciliation, the origin queries the serving ledger. The serving fed-module received the bytes, dispatched, and stored the outcome. Reconciliation succeeds.
  5. BUT: in step 3, the reaper closed the peer's loopback connection, and the consumer saw "OutcomeUnknown." The reconciliation settles the effect to "outcome" later, but the consumer's call is already terminal. The consumer's correlation between the effect_id returned at accept time and the actual settled outcome is broken.
  This is the "recovery reconciliation interact[s] badly with relay reconnects" issue called out in the prompt.
- **Fix**:
  - The recovery reconciliation MUST publish a `fed.effect_status` update that the consumer (or its harness) can query. The phase-2 doc specifies this query (line 205). The phase-3 doc must restate that the query works across relay pipe teardowns, not just TCP teardowns.
  - The consumer should query `fed.effect_status{effect_id}` on `OutcomeUnknown` and settle. The doc must specify this as the recovery contract for relay-path partitions.

## F16 — MEDIUM: Device clock assumptions — TTLs, last_seen, token expiry

- **Severity**: MEDIUM
- **Location**: 5.2 (last_seen_ms),  (pipe_token TTL), 4.2 (token rotation/long-lived)
- **Confidence**: MEDIUM
- **Issue**: The design uses:
  1. `last_seen_ms` in the registry — server-side stamped by the Worker, so the device's clock is not in the trust path here. Good.
  2. `pipe_token` "short TTL" — the device must consume the grant before the TTL. If the device's clock is skewed forward, the device thinks the token is valid but the RelayDO has expired it; the device's noise handshake fails and the effect is lost-but-marked-unretryable. If the device's clock is skewed backward, the device thinks the token is valid longer than it is; this is safe (server is authoritative).
  3. The device token is "long-lived with rotation" — the device's clock is not in the trust path (server-side expiry). Good.
  4. The device keeps a "candidates" list and may decide to use a candidate based on `last_seen_ms` (e.g. "I saw this peer 5 minutes ago, the candidate is still fresh"). If the device's clock is rolled back, stale candidates look fresh. This is a DoS amplifier: dial a dead endpoint. Not a security issue per se.
  5. The reaper's "missed keepalive" cadence is timed by the device's fed-module clock. If the device's clock is rolled back, the reaper never fires. This is a real correctness bug: a clock rollback extends the apparent liveness of a partitioned peer.
- **Fix**:
  - All server-side timestamps must be server-stamped, not client-stamped. The doc mostly does this; verify `last_seen_ms` is server-stamped (it appears to be, from 5.2 line 142 "stamped server-side by the Worker"). Make this explicit for ALL fields.
  - The reaper should be monotonic-clock based on the device, with an upper bound from server-provided timestamps. If the device's monotonic clock is unchanged but the wall clock jumps backward, the reaper continues to fire normally. State the reaper's clock source.
  - The pipe-token TTL enforcement is server-side (the RelayDO checks `exp`). Make this explicit.

## F17 — LOW: 3× reap window inheritance is asserted but the phase-2 doc doesn't say what it is

- **Severity**: LOW
- **Confidence**: HIGH
- **Issue**: Phase-3  line 194: "Keepalive cadence and 3× reap window unchanged." Phase-2 6.2 line 213-214: "Per-peer keepalive with a bounded numeric staleness window; on a missed keepalive the module marks the peer's re-exported tools unavailable rather than letting calls hang." The "3× reap window" is not stated in the phase-2 doc; it appears to be an internal fed-module constant that the phase-3 doc is now inheriting. The inheritance is a string-level claim, not a spec-level one.
- **Fix**: State the actual keepalive cadence and the actual reap window numbers (e.g. "keepalive 5s, reap window 15s"). Justify them against the relay's idle teardown (so the reaper doesn't fire on relay teardown — F14) and against the recovery reconciliation contract (F15).

## F18 — LOW: `federation_exposure` allow-list is referenced as inherited but the registry does not carry it

- **Severity**: LOW
- **Confidence**: HIGH
- **Issue**: Phase-2 2.3 (line 67): "Per-peer allow-list in the profile, authored via the CK app." Phase-3 5.2 registry entry does NOT include `federation_exposure` — only the public profile does. The fed-module reads the profile to get the per-peer allow-list, applies it after the verify ceremony. This is fine. The doc should be explicit so a reviewer doesn't think the registry is the policy source.
- **Fix**: One sentence in 5.2: "The registry carries discovery metadata only; the per-peer `federation_exposure` allow-list lives in the device's local profile (phase-2) and is applied by the fed-module after the verify ceremony, never read from the cloud."

## F19 — LOW: "Loss is acceptable" is documented, but the relitigation of "managed auth provider" should be made explicit as out-of-scope

- **Severity**: LOW (discipline)
- **Confidence**: HIGH
- **Issue**: The prompt says "managed auth provider at the edge (WorkOS), NOT self-hosted auth" is locked. The doc's threat model (line 221) names WorkOS in the table. The doc should explicitly mark "WorkOS as the v1 provider" as a LOCKED decision so the next reviewer doesn't re-litigate.
- **Fix**: Add a "Locked decisions" subsection at the top of the doc that restates the four locked items (managed auth at edge, account=discovery-only, Cloudflare hosting, relay-NOT-hole-punching, one phase gate). This is purely a discipline fix.

## F20 — LOW: Candidate list poisoning via the cloud is acknowledged but the fix is unspecified

- **Severity**: LOW
- **Confidence**: HIGH
- **Issue**: 5.3 line 156-157: "A malicious rendezvous can deny service or hand out wrong addresses; it cannot impersonate (Noise IK fails against the pinned key) and cannot read tool traffic." The "hand out wrong addresses" is the data-plane DoS — dialer wastes a connect attempt. With candidate-priority lan→public→relay, a wrong `lan` candidate wastes the 2s timeout, then a wrong `public` wastes another 2s, then the relay works. Total dial latency: 4s+ before success. For a noise session, that's a UX cliff.
- **Fix**:
  - Reduce the per-candidate timeout for known-untrusted candidates (e.g. a candidate list is hashed; if the rendezvous's delivered list does not match a previously-validated hash, treat as untrusted and reduce per-candidate timeout to 500ms).
  - Or: a small signed-claims protocol where the rendezvous's candidate list is signed by the AccountDO, and the device validates the signature before dialing.

## F21 — LOW: Static-profile fallback path is asserted to be airtight but not specified

- **Severity**: LOW (becomes a BLOCKER if F4 is fixed and this is the residual)
- **Confidence**: MEDIUM
- **Issue**: 5.5 line 175: "Absent → phase-2 behavior exactly (static profiles keep working forever; the WAN test rig never needs an account)." This is fine. But the doc does not say what happens to a previously-rendezvous-enabled device when the rendezvous is REMOVED (operator deletes `[rendezvous]` from the profile). The previously-discovered registry peers should be removed; previously-verified peers should remain verified (key-pinned). The transition is unspecified.
- **Fix**: One paragraph in 5.5 specifying the transition: removing the `[rendezvous]` section causes the fed-module to (a) close the rendezvous WS, (b) flush unverified registry peers, (c) retain verified peers, (d) re-dial the static `addr` candidates for the verified peers on next connect.

## F22 — LOW: "Device-join events are pushed loudly to all enrolled devices" — but the events are signed by whom?

- **Severity**: LOW
- **Confidence**: HIGH
- **Issue**: 4.3 line 110 and  line 221 say device-join events are "loud." Loud is the wrong word; the events need to be AUTHENTICATED. If the Worker can push a fake "join" event for a rogue pubkey, the loud UX is itself a phishing surface — operator sees "your device A is joining" and taps to confirm. The event must be signed by the AccountDO (or by a key the AccountDO attests to), and the device must verify the signature.
- **Fix**: State that `registry_delta` is signed by the AccountDO with a per-account signing key; devices maintain a public-key pin for the AccountDO established at first login (e.g. derived from the WorkOS-issued account token's JWKS or a separate per-account key distributed at enrollment).

---

## ANSWERS TO THE FIVE OPEN QUESTIONS (

1. **Device-token custody on disk (0600 file next to device key) — sufficient for v1, or fold into the credentials vault immediately?**
   **RECOMMENDATION: neither exactly as stated. For v1, encrypt the device token at rest keyed by a per-host secret derived from the device's Noise static key (e.g. HKDF(static_key, "device-token-wrap")). The fed-module decrypts on use. The on-disk file is opaque to other processes; the encryption is bound to the device, so a stolen file is useless on a different host. Folding into the full credentials vault (per `docs/cortexkit-credentials-contract.md`) is the right end state but adds coupling to a v1 that is otherwise self-contained. Do the host-bound encryption in v1; land the vault integration in a follow-up phase, not in this gate. The risk of plaintext-0600 is a real device-graph-leak surface; the host-bound encryption closes the realistic attack (other-user-process reading) without coupling.**

2. **Registry `candidates` self-reporting — any hardening needed against a malicious sibling device lying about its LAN addr?**
   **RECOMMENDATION: yes. The "noise pins identity regardless" rebuttal in the doc is wrong — Noise pins the *endpoint at the dialed address*, which is exactly what the attacker controls by lying about the address. At minimum: (a) the Worker's `public` candidate MUST always be server-stamped and never accept a client-supplied `public` value (state this explicitly); (b) the `lan` candidate SHOULD be confirmed by the dialer via a local network probe (mDNS/LLMNR query for the device_pubkey fingerprint) before being trusted, OR by a one-time operator confirmation the first time a `lan` candidate is seen for a given device. Without one of these, a compromised sibling can false-route dials. (See F6.)**

3. **Relay pipe lifetime policy: per-connection grants vs a standing pipe per peer-pair — is reconnect-per-idle-teardown churn acceptable at keepalive cadence?**
   **RECOMMENDATION: standing pipe per peer-pair, with idle-teardown on the standing pipe; the grant is the means to OPEN the pipe, and the pipe itself is long-lived. Reconnect-per-idle-teardown is operationally fragile (the fed-module must re-handshake Noise IK + re-establish the carrier on every idle cycle; this churns the AccountDO's WS state and risks reaping false-positives — see F14). Standing pipes with idle teardown AND a grace period on the reaper (F14) is the right shape. The pipe_token's TTL governs the OPEN, not the LIFETIME; once consumed, the pipe lives until idle-teardown or explicit close.**

4. **Should `connect_offer` require the target to be verified before signaling is relayed (quieter unpaired devices) or is offer-to-unverified needed for the pairing UX?**
   **RECOMMENDATION: offer-to-unverified is REQUIRED for the pairing UX, but the target's UX must be HARD-LOCKED to a code-compare display — not an Accept/Deny. Specifically: the target sees a connect_offer from an unverified pubkey, and the CK app renders the SHA-256 safety number derived from BOTH static keys. The single accept gesture is "Codes match." There is no Accept/Deny; the only way to proceed is to compare the codes. If the codes don't match, there is no "Deny" — there is "Codes don't match" with a single "Cancel" button. (See F2.)**

5. **WS hibernation vs signaling latency — acceptable to pay a cold-wake round-trip on first signal to an idle account?**
   **RECOMMENDATION: yes, but tune the signaling timeouts to absorb the cold-wake (T1=connect_request→connect_offer=5s, T2=connect_offer→connect_accept=10s; document the numbers), and probe-and-pin: keep the WS warm on any control-plane activity, only hibernate after N minutes of zero activity. Do not let the 2s per-candidate timeout in 5.4 stand as-is — it will routinely be blown by Cloudflare DO cold-wake, which is tens to hundreds of ms in the best case and seconds in the worst case. (See F10.)**

---

## SUMMARY

**Findings by severity:**
- BLOCKER: 4 (F1 effect-ledger × relay-pipe interaction; F2 unverified-offer UX; F3 pipe-token model underspecified; F4 rendezvous-down fallback unengineered)
- HIGH: 4 (F5 re-enroll + tombstone chain missing; F6 candidate self-reporting as sibling-attack surface; F7 device-token revocation race; F8 signaling abuse — no nonce/timestamp/rate-limit/signing)
- MEDIUM: 8 (F9, F10, F11, F12, F13, F14, F15, F16)
- LOW: 6 (F17, F18, F19, F20, F21, F22)

**Overall risk assessment: HIGH.** The architectural shape is sound and the inheritance from phase 0-2 is mostly honored. But four BLOCKERs each independently prevent a safe ship:
- A claim about "Noise session resumption = cheap" that does not survive inheritance check (F1).
- A claim about "rogue device never becomes routable" that depends on an unspecified UX rendering (F2).
- A claim about "single-use, short TTL" pipe tokens with no specification of who can consume whose grant (F3).
- A claim about "static profiles remain the availability fallback" with no engineering of the rendezvous-down code path (F4).

**Inheritance claims that did NOT survive the check against phase 2 v4.1:**
- F1: "Noise session resumption = ordinary re-handshake, cheap" — phase 2 says nothing about session resumption semantics or cost.
- F1/F14: "relay-path partitions classify exactly like TCP partitions" — phase 2's partition classifier is per-peer, not per-carrier; the equivalence is asserted, not inherited.
- F4: "Absent → phase-2 behavior exactly" — phase 2 has no rendezvous path; "exactly" requires engineering the absent path, which the doc doesn't do.
- F17: "3× reap window unchanged" — the phase 2 doc does not state a "3× reap window" number; this is internal fed-module state being re-asserted as a doc-level spec.

**Inheritance claims that DID survive the check:**
- Noise IK E2E (F1 — phase 2 2.2, 5.3).
- Verify-code ceremony gated first contact (F2 — phase 2 5.3 line 172).
- Exactly-once effect ledger (F1/F15 — phase 2 6.1).
- Per-peer keepalive + reaper partition classifier (F14 — phase 2 6.2).
- Reserved + nonce-bound fed-module (not directly challenged in this review).
- `federation_exposure` default-deny (F18 — phase 2 2.3).

**Overall verdict: NO-GO.**

Must-fix-before-build blockers (cannot ship without these):
1. **F1** — Spec the logical-peer / physical-pipe split: relay teardown is a transport event, not a partition event. Restate the effect-ledger's recovery contract across pipe reconnects. Drop the "cheap" claim or specify a real resumption mechanism.
2. **F2** — Lock the verify-code UX contract: a code-compare display with a single "Codes match" gesture; no Accept/Deny for unverified offers. Add a server-side enforcement that the offerer and target compute the code independently.
3. **F3** — Spec `pipe_token` as `HMAC(pipe_id_key, pipe_id || device_pubkey || exp || nonce)`; bind to device pubkey; require the WS's authenticated device identity to match. On idle teardown, mint a new `pipe_id` AND new tokens.
4. **F4** — Spec a rendezvous-dead timer and the static-profile fallback path explicitly. State the timer. State the signal that triggers it. State the test that proves the fallback works.

If those four are fixed, the design is ready for build with the HIGHs as a follow-up gate.

</COUNCIL_MEMBER_RESPONSE>

