---
task_id: bg_cafecc2f
agent: council-member
session_id: ses_09b2a7020ffesZYWgvt95YpdxR
parent_session_id: ses_09b2b56e1ffeYwM1Vc5SuTRFuZ
status: completed
completed_at: 2026-07-15T08:59:45Z
---

[user] 2026-07-15T08:12:25Z
## Solo Analysis Mode
You MUST do ALL exploration yourself using your available read/search tools.
- Do NOT use task or any delegation tool under any circumstances
- Do NOT delegate to explore, librarian, or any other subagent
- Do NOT spawn background tasks
- Search the codebase directly — you have full read-only access to every file
- This mode produces the most thorough analysis because you see every result firsthand


## Analysis Intent: AUDIT

You are conducting an **audit** — your goal is to find discrete issues, risks, or violations.

**Focus:**
- Search for problems, anti-patterns, security risks, correctness issues, or violations of stated requirements
- Each finding must be a distinct, actionable item with concrete evidence
- Severity determines priority: critical (blocks/breaks), high (significant risk), medium (should fix), low (nice to fix)
- For each finding, provide the specific location (reference, section, or component where it occurs)
- State your confidence: high (clear evidence), medium (likely but needs verification), low (suspicion, investigate further)
- **This is a broad sweep, not a targeted trace.**

**Analytical standards:** Support claims with concrete evidence. State confidence (high/medium/low) for key assertions. Note caveats and limitations.

**Structure your response as:**
```
<COUNCIL_MEMBER_RESPONSE>
## Finding 1: [Title]
- **Severity**: critical/high/medium/low
- **Location**: [specific reference — e.g. component, section, endpoint, rule]
- **Confidence**: high/medium/low
- **Issue**: [what is wrong and why it matters]
- **Evidence**: [concrete reference, snippet, or observation that proves the issue]
- **Suggested Fix**: [actionable recommendation]

## Finding 2: [Title]
...

## Summary
[Total findings by severity. Overall risk assessment with confidence levels.]
</COUNCIL_MEMBER_RESPONSE>
```

## Analysis Question

ADVERSARIAL RE-GATE — subc-core dispatch redesign v2

You are one of several independent expert reviewers performing a concurrency-critical adversarial re-gate. Repo: subconscious at ~/Work/Projects/CortexKit/subconscious. Design under review: docs/subc-dispatch-redesign-v2.md (committed 72891b31). Shipped daemon source: crates/subc-core/ at master. This is v2 of a dispatch redesign; v1 (docs/subc-dispatch-redesign.md) received a UNANIMOUS 8/8 NO-GO with 10 blockers B1-B10. v2 claims to close all of them.

THE V1 ARCHIVE (READ IT FIRST for exact blocker statements + source citations):
.cortexkit/alfonso/athena/council-subc-dispatch-redesign-e26112ccb9170497/synthesis.md
The B1-B10 blockers map to findings #1-#10 in that synthesis (in order). Each v2 section tags which blocker(s) it claims to resolve.

YOUR TWO JOBS:
(A) VERIFY each v1 blocker B1-B10 is ACTUALLY closed by the v2 mechanism — not merely claimed. Read the v2 mechanism, then read the shipped source it cites, and confirm the mechanism genuinely closes the defect. A plausible-sounding paragraph is not a closure; trace the concurrency.
(B) HUNT for NEW defects the v2 mechanisms introduce.

GROUND TRUTH (already verified at source — you may re-verify):
- forwarding.rs:1692-1700 ChannelFlow::acquire does permit.forget() then in_flight.fetch_add — credit is NOT RAII-held by the task.
- forwarding.rs:1702-1731 release() has a CAS in_flight!=0 guard that ignores over-release (best-effort, not a security boundary per its own comment).
- router.rs:465-496 shipped handle_bound: acquire().await → mutate header → module_sink.send().await → release() on send error.
- router.rs:281-309 module->client terminal path: try_send to client, then flow.release() if terminal.

V2 KEY MECHANISMS TO SCRUTINIZE (map to doc sections):
1. §1 RouteDispatcher: route-local Mutex<RouteInbox> (VecDeque<corr> + HashMap<corr,Slot> + admission enum + outstanding count) as SINGLE serialization point; every op holds the lock O(1) and NEVER across an await; drain task's blocking awaits (flow.acquire, module_sink.send) happen OUTSIDE the lock. VERIFY: is every claimed-O(1)-under-lock op actually O(1) and await-free? Can the read loop and drain task deadlock/livelock on this lock? Is Notify usage correct (lost-wakeup between pop-returns-None and wait)?
2. §2 Per-corr state machine Queued→Claimed→Delivered with CANCEL decided under the same lock. VERIFY B2 limbo is TRULY gone: enumerate CANCEL arriving in EACH state incl. exactly at the Claimed/acquire boundary and the Delivered/send boundary; can a terminal still double-fire (synthetic cancelled + module terminal) or vanish (zero terminal)? The rollback path (Claimed{cancelled:true} after acquire → release + synthesize cancelled): does it race the module→client terminal path?
3. §2 outstanding/Delivered recorded UNDER LOCK BEFORE module_sink.send (B5). VERIFY a fast module terminal cannot arrive before corr is Delivered: trace exact happens-before. The frame isn't sent until after the lock releases with state=Delivered — but is the terminal-release path's slots.remove correctly ordered against a Delivered set before send?
4. §4 Credit RAII (AcquiredCredit guard) + drain error arms: ChannelFlowClosed disambiguation via per-route teardown:TeardownKind field. VERIFY the teardown field is set/read under the right lock ordering and the None→backend_error arm actually preserves the shipped test blocked_flow_control_acquire_wakes_when_module_tears_down (forwarding.rs ~3811). Can the teardown kind be stale/racing when the drain reads it?
5. §3 R11 exactly-once release: terminal path does slots.remove under lock, releases only if was-Delivered. VERIFY against shipped release() which ALREADY has a CAS in_flight!=0 guard (forwarding.rs:1702-1731) — does v2's gate + existing CAS double-guard CLEANLY or CONFLICT? Is the concurrent-duplicate case actually closed, or does the double-guard mask/hide a real leak?
6. §5 Teardown 3-phase Open/Closing/Closed: admission=Closing before snapshot removal; cancel_token select; bounded-join then abort. VERIFY: the try_push-after-flush hole (B9) — is admission=Closing genuinely happens-before any stale-snapshot reader's push? Can a drain task blocked on module_sink.send with a FrameSink clone still hang connection close (server.rs:241-267)? Is the JoinHandle ownership (teardown path, not binding) actually acyclic?
7. §7 Snapshot publish-under-lock + closed:AtomicBool recheck (B10): VERIFY the closed flag set-under-write-lock-before-publish + reader recheck restores today's unknown_channel observable (not backend_error) on stale-Bound. Is there any ordering where closed=false is read but the route is already released?
8. §6 Client-side-only control offload (B7): module connections NOT split, bind-ack stays inline. VERIFY the shipped bind barrier test accepted_route_publishes_route_open_before_immediate_reverse_request (router.rs:1078-1102) stays green AND that client-side route.open FIFO doesn't itself reorder anything observable.
9. §8 merge-0 SDK prerequisite (B1): the retryable data-plane class. VERIFY the classification claim against SDK source (client.ts classifyFailure ~781-792, consumer.rs ~570-579) — is a distinct retryable class actually needed and does the proposed in-place-retry (not reconnect) fit the existing managed-call retry loop? NOTE: broca/aft/alfonso-core are NOT in this checkout — their contract impact is UNVERIFIABLE; flag this.
10. §9/§11 The 3-merge rollout ordering + invariant deltas I3/I4/I7 — are they now HONEST against source, and is merge-1 truly standalone-landable under the publish-under-lock constraint?

ALSO CHECK §12 Q1'-Q5' open-question leans for wrong leans. AND HUNT ANYTHING NEW:
- lock-ordering inversions between the RouteInbox mutex and the global forwarding write lock (specify the hierarchy; is it respected everywhere?);
- the AcquiredCredit RAII guard interacting with the outstanding count → double-counting or double-release;
- Notify lost-wakeups (the classic pop-returns-None → miss notify_one → sleep-forever gap);
- the corr-uniqueness assumption (Q4') as a credit-leak vector if not enforced at enqueue.

DELIVERABLE (your COUNCIL_MEMBER_RESPONSE):
- For EACH v1 blocker B1-B10: state CLOSED / NOT-CLOSED / NEWLY-BROKEN, with a one-to-three sentence justification and source citation (file:line). "CLOSED" requires you traced the mechanism, not just read the claim.
- A section of NEW defects introduced by v2 mechanisms, each with severity (BLOCKER/MAJOR/MINOR), a concrete interleaving or source contradiction, and file:line citations.
- Rulings on Q1'-Q5' leans (RIGHT / WRONG / RIGHT-BUT-UNSAFE).
- Your bottom-line verdict: GO / GO-WITH-CHANGES (enumerate each change, specific + file:line-cited) / NO-GO (enumerate blockers).

Be adversarial and precise. Cite shipped source for every claim. A single un-refuted concurrency or contract defect is a NO-GO. Do not flatten disagreement with the v1 findings — if v2 genuinely closed a blocker, say CLOSED and prove it; if it only papers over one, say NOT-CLOSED and show the hole.

[assistant] 2026-07-15T08:12:26Z
I'm starting by reviewing the key documents—the v1 archive synthesis, v2 design doc, and the shipped source code in the core crates—to understand the full context.
I'll start by reading the v1 archive synthesis, the v2 design doc, and the shipped source in parallel.

[assistant] 2026-07-15T08:12:35Z
I'm diving into the shipped source code, starting with the core files like forwarding.rs to understand the channel flow mechanics around acquire, release, and epoch-fenced operations, then router.rs for the bound handling and client terminal paths, and server.rs for the read loop and writer synchronization. I'll examine the specific line ranges mentioned to trace through the implementation.
Now let me read the shipped source at the cited locations. Let me pull the key sections in parallel.

[assistant] 2026-07-15T08:12:45Z
I'm diving into the shipped source code to understand the read loop mechanics in server.rs, the release path in forwarding.rs, how modules route to clients in router.rs, and examining the bind barrier and blocked flow control tests. I also need to trace through the CANCEL handling and endpoint_is_draining logic, starting with how the server handles the read loop and connection closure.
Now let me read the read loop, teardown/release paths, and the key tests in parallel.

[assistant] 2026-07-15T08:12:53Z
Now I'm diving into the test cases and SDK code to understand the flow control mechanics—specifically how the blocked flow control acquire wakes up when a module tears down, plus examining the bind barrier test and the SDK classification logic. I need to trace through the read path in the router and understand how the module-to-client terminal connection works, along with the forwarding read lock structure.
Let me read the key tests and the module→client dispatch context, plus the SDK classification code.

[assistant] 2026-07-15T08:13:00Z
I need to locate that test in forwarding.rs since the initial search didn't return the expected content.
The test read at forwarding.rs:3800-3890 returned empty. Let me locate the actual test and the SDK classification code.

[assistant] 2026-07-15T08:13:06Z
I'm looking at the SDK's managed call retry loop and the Rust consumer classification logic, then searching for the tear-down test in forwarding.rs.
Let me find the teardown test and read the managed call retry loops in the SDKs.

[assistant] 2026-07-15T08:13:18Z
I need to trace through the teardown test and understand how the dispatch function works, particularly looking at how `corr` and `route` are obtained in the router module around lines 281-309.
Now let me read the teardown test in full and the module→client dispatch entry function context.

[assistant] 2026-07-15T08:13:28Z
I'm looking at the teardown test in forwarding.rs and noticing something important about how it validates frame responses — it checks whether incoming frames are either Goodbye messages or backend errors for blocked correlations, but it's also lenient in that it accepts connection closures or timeouts without asserting anything, making the test fairly permissive overall.

Now I need to trace through the concurrency mechanics, specifically where the module-to-client terminal path in router.rs interacts with the drain task and whether v2's slot removal gate operates under the same lock as the inbox lock where Delivered is recorded before sending.

The architectural coupling here is that the drain task lives on the client-side route binding while the terminal arrives on the module connection, yet both need to access the same RouteInbox — I should verify this is actually feasible.

For lock ordering, I'm checking how the RouteInbox mutex relates to the global forwarding write lock: teardown locks the inbox first (setting admission to Closing), then calls flow.close(), and later takes the forwarding write lock for the epoch-fenced release. The release_client_route_locked path runs under the write lock and sets closed=true, so I need to understand where the inbox lock sits relative to that. There's also the snapshot rebuild and publish happening inside the write-lock critical section in section 7, which adds another layer to consider.

Looking at the actual lock ordering in the code: the read-loop push path loads the snapshot lock-free, then locks the inbox to check admission and the closed atomic flag — no forwarding write lock involved. The terminal release path looks up the route via snapshot or the map, releases that read lock, then locks the inbox and calls flow.release, so it's also not holding the forwarding write lock when it touches the inbox.

The real concern is whether any path holds the forwarding write lock and then tries to lock the inbox. The snapshot gets rebuilt and published inside the write-lock critical section for every mutation including release, and release sets closed=true under that write lock. But I need to verify whether the binding release step actually requires inbox operations or if the teardown sequence keeps those operations outside the write lock.

The prompt specifically asks me to specify the lock hierarchy between the RouteInbox mutex and the global forwarding write lock, and whether it's respected everywhere. But the documentation doesn't explicitly define this hierarchy anywhere — that's a significant gap. Section 5's teardown interleaves the inbox lock and write lock in sequence, which suggests the hierarchy isn't clearly established.

The real question is whether the terminal release path needs the forwarding lock to locate the inbox in the first place. I need to trace through how `dispatch_module_frame` and the module-to-client binding work to understand where the RouteBinding is stored and whether the write lock is actually required there.

Looking at the architecture: the terminal arrives on the module connection and gets routed via `module_to_client` lookup to find the `route: Arc<RouteBinding>`. For the terminal path to lock the inbox, the RouteBinding must hold an `Arc<RouteDispatcher>`. That's architecturally sound — the dispatcher is shared across the read loop, drain task, terminal path, and teardown path.

But there's a subtle ordering issue around the terminal-release sequence that the doc claims to enforce.

The drain task locks the inbox, marks state as Delivered, increments outstanding, then unlocks before sending the frame. The module receives the frame and emits a terminal, which arrives on the module connection. The terminal path locks the inbox, finds the correlation was already Delivered, and sets release to true. Since Delivered is set before the send completes, and the module can't receive the frame until send delivers it, the terminal genuinely can't race ahead of Delivered — that closes the gap.

But now I'm seeing a new subtlety: what happens when the rollback path races against this?

In the rollback path, after acquiring the lock on the inbox, if the state is Claimed with cancelled set to true or if admission is Closed, the code unlocks and calls flow.release(), synthesizing a cancelled response. Crucially, the frame is never sent to the module in this path, so there's no module terminal to worry about. The slot gets removed during the take operation and never reaches Delivered state, so there's no race with the terminal path there.

Now, if a CANCEL arrives while the state is already Delivered, it gets forwarded to the module. The module processes the cancel and emits a terminal — either a Response or a cancelled Error. The terminal path then locks the inbox, finds the correlation was Delivered, and releases. There's only a single terminal here, which is good. The synthetic cancelled response isn't generated when the state is already Delivered because the read loop...

Examining the exact boundary where CANCEL arrives after the drain task has passed the post-acquire lock check but before the module_sink.send happens...

the read loop could forward CANCEL to module_sink while the request frame is still in flight, potentially delivering the cancellation before the actual request reaches the module. The ordering becomes critical here since both the drain task's request send and the read loop's CANCEL forward target the same FrameSink. The read loop can process a CANCEL between the drain task's state update and its actual send to the module sink. If the CANCEL reaches the module sink before the REQUEST, the module receives them out of order—handle_cancel finds no in-flight entry and no-ops, then REQUEST executes uncancelled. This is a real race condition in the mpsc channel where the drain task's `.send(request).await` and the read loop's cancel send can interleave. can overtake the not-yet-sent request. This is a real defect that v2 introduces: the serialization guarantee that kept request before cancel is broken when the drain task sends the request independently while the read loop forwards the cancel. Let me verify the shipped module_sink semantics to confirm both go through the same sink in-order — in the current code they do, and since it's serial, the request completes before the cancel is even read, so ordering holds. But in v2, that serialization is gone.

The cancel forward in v2's read loop must be non-blocking (try_send, not await) per the constraints, while the request is a blocking send from the drain task. Since both write to the same mpsc, whichever task executes its enqueue first wins — there's no ordering guarantee. The Delivered flag gets set before the request is enqueued, so a cancel can slip in first and get processed before the request even hits the queue.

This is a blocker-level defect: it's the exact cancel-loss scenario (R5/B2) that the redesign was supposed to eliminate. The defense that "the module has it or will; its terminal releases" handles credit accounting but completely misses the cancellation semantics — if the cancel overtakes the request into the module, the module can't cancel something it hasn't received yet.

Let me trace through whether this reordering is actually possible given the tokio mpsc and task scheduling. The drain task transitions to Delivered, unlocks, then awaits on sending the frame to the module sink. The cancel comes from a different task (the client read loop), so for it to overtake, the read loop must acquire the lock, see Delivered, unlock, and send the cancel to the module sink before the drain task's send completes.

On a multi-threaded runtime this is definitely feasible — the drain task could be preempted between unlocking and enqueueing, allowing the read loop on another thread to slip in. Even on single-threaded, tokio's mpsc send tries to reserve a permit first, and if capacity exists it might not yield, making the window tighter but not impossible.

So this confirms the blocker: a CANCEL arriving while the request is in that delivered-but-not-yet-sent window can overtake it into the module sink, causing the request to be lost. The design principle of never crossing an await while holding the lock means the request enqueue happens after the unlock, so the window is unavoidable unless they route CANCELs differently when in the Delivered state.

Now I'm looking at another potential race in the fast-terminal path. When the drain task sets Delivered and releases the lock before sending, there's a window where the terminal-release path could race against it.

If the send fails, the module never receives the frame so no terminal arrives—removing the slot and releasing is correct. But if send succeeds and the module emits a terminal, the terminal path will lock the inbox, find the slot was already marked Delivered, decrement outstanding, and call flow.release. That gives us a single release, which aligns with the shipped CAS guard behavior.

For the R11 double-terminal case, when a module emits two terminals for the same correlation, the first removes the slot and triggers release, while the second finds nothing and skips release. The module's read loop processes these serially, so what looks like concurrent duplicates is actually just serialized duplicates—the gate handles it. Now I'm wondering if a terminal and a cancel-forward could race...

Actually, the inbox mutex serializes everything arriving from different tasks, so that's fine. Let me examine whether the v2 gate and the existing CAS guard work cleanly together. The gate prevents the second terminal from calling release at all (per-correlation check), while the CAS guard is a backstop that checks in_flight at the aggregate level. They're independent and don't conflict—the gate is the stricter layer.

But there's a potential issue: if teardown removes a correlation from slots but the module still emits a terminal for a Delivered correlation, the gate would suppress a legitimate release. However, teardown only drains Queued correlations at step 5, not Delivered ones (those are outstanding). If the connection closes before the module terminal arrives, the credit just drops with the flow, so there's no real leak.

Wait, there might be a subtle over-release or leak in how the R11 gate interacts with rollback and the terminal path. Let me trace through: when drain acquires credit and sets Delivered, then a CANCEL arrives, the read loop forwards it to the module, which emits a cancelled terminal. The terminal path removes the slot and releases once. That's correct. And in Delivered state, the read loop never synthesizes a cancelled terminal itself—only Queued and Claimed-rollback do that—so there's exactly one terminal from the module. Good. But now I'm concerned about the Claimed rollback double-terminal scenario...

actually, rollback only happens when state is Claimed{cancelled:true} at the post-acquire lock, before send ever occurs, so the module never gets the frame. Single terminal. That closes B2 except for one hole: the Delivered-not-yet-sent reordering window.

Now I need to dig into B4 and the teardown test. The v2 maps module-death to the None/backend_error arm, and the teardown field is set by whoever closed the flow. The question is whether teardown is set and read under the correct lock—drain reads it under the inbox lock, but I need to check what lock the mutator holds when calling flow.close().

If the mutator sets teardown without holding the inbox lock, that's a data race. If it does hold the inbox lock, then we have write-lock → inbox-lock nesting, but teardown section 5 goes inbox-lock → write-lock, never nested. That creates a potential lock inversion if release_client_route_locked holds the write lock then acquires the inbox lock to set teardown. I need to check whether there's any inbox→write nesting elsewhere that would complete the cycle.

The terminal-release path locks the inbox but flow.release() doesn't take the forwarding write lock, so no inversion there. The read-loop push and teardown are both sequential without nesting. The real issue is that the documentation never specifies where teardown gets set relative to the locks, and the prompt is asking whether the teardown field can become stale or race when the drain reads it. There's a potential race where the drain task is blocked waiting on flow.acquire(), and the mutator closes the flow under the write lock, which wakes the acquire with an error.

When the drain runs handle_closed, it locks the inbox and reads the teardown kind. For this to be correct, the mutator must have set teardown with the right reason before calling flow.close(), and the visibility has to be guaranteed. Section 5 describes the order: first set admission to Closing under the inbox lock, then call flow.close() with the teardown reason. But the documentation is ambiguous about whether teardown itself is set under the inbox lock or after it's released. If teardown is a field protected by the inbox mutex, it must be set while holding that lock, but the way step 2 is written suggests it happens after step 1 unlocks.

The real concern is the teardown-via-mutator path in release_client_route_locked, which calls flow.close() under the write lock but doesn't seem to have access to the RouteInbox to set teardown there. If the mutator sets teardown without holding the inbox lock while the drain reads it under the lock, that's a race condition. I need to trace through what happens when a module connection drops and verify that the teardown state is handled correctly for the drain's error mapping. The module-death path should leave teardown as `None` so that `handle_closed` properly maps to `backend_error`, but I need to confirm the cleanup sequence actually does this and that section 4's documentation covers this case. The real issue is that `teardown` and `flow.close()` are separate operations without guaranteed ordering—the drain might wake from the semaphore close and read `teardown` before it's been written, leaving it as the default `None` and triggering a `backend_error` when it should silently drop on `Goodbye`. This race condition could cause the wrong termination behavior depending on timing. ardown is set under the inbox lock before close is called. But if teardown is written AFTER close without a fence, a concurrent drain could read stale teardown and send the wrong terminal code to the client. The mutex synchronization only works if both the write and read happen under the same lock — if the write happens outside the lock or after close, the happens-before guarantee breaks and we're back to the race condition. The shipped `release_module_route_locked` path calls flow.close() directly under the write lock without setting the teardown field or admission state, which creates a lock ordering problem — if it tries to acquire the inbox mutex while holding the write lock, it could deadlock with any code path that goes inbox→write. This is a major gap: the teardown field's write semantics and the lock hierarchy between the inbox mutex and forwarding write lock are completely unspecified, leaving the module cleanup path potentially broken for v2.

Now I'm checking the lost-wakeup scenario in the drain task. The concern is whether tokio's Notify can miss a signal between checking if the queue is empty and waiting on the notification. Tokio actually handles this by storing a permit when notify_one() is called with no active waiter, so the next wait() will consume that permit and return immediately — but this only works if you create the Notified future before checking the queue condition, or manage the permit correctly. So the key insight is that Tokio's Notify only stores one permit at a time, which means multiple notify_one calls collapse into a single wakeup. For a drain task that loops and pops until empty before waiting again, this is safe—one permit is enough to trigger a re-check of the entire queue. The critical part is that the loop must actually re-pop in a loop until it gets None before calling await again, which ensures all queued items get processed even if multiple pushes happened between wakeups. , it loops back to pop. So it drains all. So Notify is used correctly IF (a) they call notify.notified() and (b) the permit semantics hold. 

BUT: there's a specific hazard. The doc's drain loop:
```
corr = { lock inbox; pop_front queue or None }        // if None -> wait notify (or Closing->drain-then-exit)
```
The comment "if None -> wait notify" — if implemented as `notify.notified().await` created AFTER releasing the lock and AFTER observing None, and the push+notify_one happened between the unlock and the notified() creation, tokio's stored permit saves it. So no lost wakeup for tokio Notify. I'll rate this as LIKELY-SAFE but note it depends on using tokio::sync::Notify's permit semantics rather than a traditional condvar, and re-popping in a loop to handle spurious wakeups. If they switched to a different primitive or created the notified() listener before checking without consuming it, that could break — it's a discipline-contingent risk I'll flag but not treat as a blocker.

Actually, there's a more subtle issue lurking here: tokio Notify only stores a single permit, and that permit is only created when there's no active waiter. So if the drain task is processing one item and the read loop pushes two more items with notify calls between them, the second notify becomes a no-op since the permit's already stored. The drain task then pops and processes both remaining items in sequence without waiting, which works fine — but the pattern depends on the queue being drained completely in the loop.

Now I need to dig into the B9 teardown and connection-close hang issue. Section 5 describes using a cancel token with select on module_sink.send and flow.acquire, plus a bounded-join and abort. The doc claims a blocked drain can't hang the connection close because of the bounded-join and abort, but I need to trace who owns the JoinHandle and when teardown actually fires relative to the connection-close write.

Looking at the server code paths: the connection close (lines 252-256) drops the context, egress, and connection, then waits on the writer with CLOSE_DRAIN_GRACE. For the drain task to not block the writer, it can't hold a FrameSink clone that keeps the writer's mpsc channel alive. The drain task holds module_sink, which is the module connection's egress—that keeps the module writer alive, not the client's writer. But here's the question: when the client connection closes, which connection's teardown triggers the dispatcher teardown? The dispatcher is bound per client-side route, so if the client closes, its read loop exits and...

The drain task must cancel its module_sink.send when the client closes, but the real issue is that the drain task also holds a client egress clone to synthesize cancelled/backend_error responses back to the client. That means the drain task keeps the client writer's mpsc channel alive, which blocks the client connection close writer wait at the CLOSE_DRAIN_GRACE timeout.

The teardown path is supposed to cancel and join the drain tasks before the writer wait happens, but if that ordering isn't enforced before server.rs:252-274's fixed sequence runs, the drain's retained client egress clone will delay the writer drain until CLOSE_DRAIN_GRACE times out and forcibly aborts the writer — it won't hang forever, but it degrades close latency. the RouterConnection drop handler, then the drain tasks would be properly sequenced. But the doc doesn't specify where that teardown actually happens in the code — it's a critical gap that leaves the sequencing unclear, though the CLOSE_DRAIN_GRACE timeout acts as a safety net to prevent true hangs. or the forwarding cleanup path — whether there's an explicit async teardown call or if it relies on Drop semantics. This is the crux: if teardown (the bounded-join and abort) only lives in Drop, it can't actually execute because Drop is synchronous and can't await, meaning drain tasks would leak their egress clones until the grace period timeout forces an abort.
The teardown test at forwarding.rs:3811-3878 is quite lenient (accepts Goodbye, backend_error, or nothing). Let me examine connection cleanup/teardown wiring and the module→client dispatch entry, plus how routes are released on connection close.

[assistant] 2026-07-15T08:16:48Z
I'm examining the module-to-client dispatch entry point and tracing how it accesses the route binding—specifically whether the async method runs on the client-side route context and how it interacts with the RouteInbox lock that manages the terminal path in v2.

I also need to understand the `endpoint_is_draining` and `module_is_draining` semantics for reload disambiguation, and trace through the broader routing logic around router.rs to see where `handle_bound` gets called and how module-to-client versus client-to-module paths differ.

Looking at the teardown test more carefully—it's actually quite lenient since it accepts Goodbye, backend_error, or nothing at all; if the frame read times out or closes, the test passes trivially rather than asserting on a specific frame.

For lock ordering in v2, the critical question is whether the module-to-client terminal path holds the forwarding read lock when it needs to acquire the RouteInbox mutex to remove slots. In the current code, the read lock is dropped before the Arc is used, but v2's snapshot-based approach would load the Arc without holding the lock, then acquire the RouteInbox lock separately—so there's no lock ordering conflict there.

The drain task also looks safe: it locks the inbox first, then calls flow.acquire and module_sink.send outside that lock, neither of which touch the forwarding lock. The trickier part is the teardown path, which is invoked by release_client_route_locked under the write lock—but the doc's discrete steps (locking inbox, canceling, bounded join) don't clearly indicate whether those happen before or after acquiring the write lock, which matters for understanding the full lock ordering. Sending to the client sink doesn't require the forwarding lock, and under v2 the snapshot load is lock-free via ArcSwap, so the terminal path only needs the inbox lock. The teardown holds the forwarding write lock while joining the drain task, but the drain itself doesn't appear to need that lock — unless there's something I'm missing about when the drain task actually requires it.

The real problem is that the doc's teardown sequence presents steps 1-6 as a logical flow, but step 6 (the epoch-fenced release) is a `_locked` function that requires the write lock, while steps 1-5 happen under the inbox lock. The existing release path is already called from within a write-lock critical section during connection cleanup, so I need to figure out where steps 1-5 actually execute and whether they're protected by a lock at all.

If steps 1-5 run before acquiring the write lock, another mutator could interleave between step 5 and step 6. If they run while holding the write lock, then the bounded-join(2s) blocks all forwarding mutations—though lookups are now lock-free via ArcSwap, so reads aren't affected, but every other route bin operation would stall.

The doc doesn't specify which approach, and this is a real lock-ordering hazard. Given that a connection can have many routes and each release needs its drain task joined, doing a 2s bounded-join per route under the global write lock would be catastrophic. Even outside the lock, joining N drain tasks sequentially at 2s each during connection close creates a latency problem. This is a genuine concern I need to flag.

Now I'm diving into the critical issue around the CANCEL/Delivered double-fire and the rollback race. Let me trace through the drain task logic carefully, looking at how the slot state transitions and flow acquisition interact.

The drain path checks if a slot is in the Claimed state with cancellation already set, and if so, it releases the credit and synthesizes a cancelled response without sending to the module. Otherwise it marks the slot as Delivered and increments outstanding. Meanwhile, the CANCEL read loop can set the cancelled flag on a Claimed slot or forward the cancellation downstream if the slot is already Delivered or gone. The race I'm examining is between the rollback path (where drain sees Claimed{cancelled:true} and releases early) and the module-to-client terminal path, where the frame may or may not have been sent yet.

The key insight is that if cancelled was set before the drain acquired the lock, then the module never receives the request, so there's no terminal race to worry about. But if CANCEL arrives after the drain releases the lock but before module_sink.send completes, the CANCEL sees Delivered and forwards the cancellation to the module, which then receives both the request and the cancel message in order—no limbo state.

The real question is whether the CANCEL forward and the request send to the same module_sink (an mpsc channel) maintain order, especially if the drain hasn't actually sent the request yet when the CANCEL read loop forwards the cancellation. I'm working through a scenario where the drain sets state to Delivered and releases the lock, but the CANCEL arrives and queues the cancellation before the drain's send call completes.

This is the same ordering defect from B2/R5 — the v2 claim that "Delivered means the module owns the terminal" assumes the module will properly handle the CANCEL, but there's a race window where Delivered is set under lock before the request actually enters the module_sink queue. A CANCEL in that window can overtake the request, so the module sees the cancellation first and treats it as a no-op, then processes the request as if it were never cancelled.

The B5 fix of inserting Delivered before the send actually creates a B2 hole for cancel-ordering — the two fixes directly conflict with each other. The doc's language admits the Request might not have reached the module yet when CANCEL is forwarded, which means CANCEL can race ahead and potentially be lost if the module doesn't recognize the correlation ID. The handwave about "terminal releases" addresses credit flow but not cancellation semantics, and the drain doesn't hold the inbox lock during send, so there's no ordering guarantee protecting against this race. The send operation happens after the lock is released, which means it races with any concurrent CANCEL forward—there's no ordering guarantee that the Request reaches the sink before a CANCEL does. A potential fix would be to enqueue the CANCEL into the drain's path so it's ordered after the Request send, but the spec doesn't do that, leaving a cancel-loss vulnerability at this boundary. Now I need to check whether there's still a double-terminal issue when CANCEL removes an entry from the queue before the drain can pop it. When the module receives a Delivered state and processes the request, it emits a single terminal response and removes the slot, releasing the credit. If a CANCEL arrives before the Request reaches the module, the module ignores it and still processes the Request normally, resulting in one terminal—so there's no double-fire, just the cancel-loss defect. Now I'm wondering if this cancel-loss is actually the core R5 defect or something else.

The real issue is that while CANCEL is processed immediately by the read loop in v2 (not blocked), there's a critical window where the drain marks state as Delivered but hasn't yet enqueued the Request into the module_sink. During this time, module_sink.send() can block if the sink is full, parking the drain task for an extended period while state remains Delivered. Any CANCEL arriving during this backpressure window gets forwarded to the module ahead of the still-pending Request, creating a cancel-loss scenario. This window isn't tiny—it's as long as the module_sink stays saturated, making it a genuine blocker under module backpressure.

But now I'm realizing the CANCEL forward itself has a problem: if module_sink is full and blocking the drain, how does the read loop forward CANCEL to that same sink? Using try_send would drop the CANCEL if the sink is full (cancel lost). Using send().await would block the read loop entirely (reintroducing head-of-line blocking). Either way, the CANCEL forward fails. I need to check how the implementation actually handles this—the design doc says to forward CANCEL to module_sink, but the read loop can't await, and the shipped code uses module_sink.send().await through handle_bound. In the current implementation, CANCEL frames bypass the drain entirely and go directly to the module sink, creating an ordering race with Request frames that do go through the drain. And there's a classic lost-wakeup vulnerability in the drain's notification logic—between when it checks for queued items and when it waits on the notify, a producer could push and signal without the waiter seeing it.

However, tokio's Notify primitive handles this correctly by storing a permit when notify_one() is called with no active waiter, so a subsequent notified().await will consume that permit and return immediately. The key is that the permit persists across the gap, making the sequence safe as long as they're using tokio::sync::Notify with proper notify_one() semantics.

The real subtlety emerges with multiple notifications: if the producer pushes several items while the drain isn't waiting, multiple notify_one() calls collapse into a single stored permit since Notify only keeps one. This means the drain wakes once but needs to drain the entire queue in a loop rather than expecting one notification per item. As long as the drain keeps popping until it hits None before awaiting again, the collapsed notifications don't cause items to be lost—the loop naturally handles the backlog.

The Notify mechanism itself is safe here: if a push and notify happen after the drain decides to wait but before the await registers, tokio stores the permit. So there's no lost-wakeup defect, though this does depend on tokio's Notify implementation. The trickier case is during shutdown when the closing branch needs to drain remaining items before exiting.

The drain task might be parked in notified().await when cancel_token fires, and the spec says all awaits are select'd against the token, so presumably that includes the notified() call. If it's not, the drain could hang. The teardown flow looks sound otherwise—flow.close() wakes a blocked acquire, and cancel_token wakes a blocked send or notified—but this is a critical implementation detail to verify.

Now I'm looking at the AcquiredCredit RAII mechanism and whether outstanding credits could be double-counted. So if the guard releases on drop AND line 94 explicitly calls flow.release(), that's a double release—unless the explicit call is meant to represent the guard's release itself. The shipped implementation has a CAS check that prevents release when in_flight==0, but if in_flight>0, a spurious extra release would decrement another coroutine's credit since it can't distinguish which one.

This double release would grow the window by 1, admitting one extra concurrent request beyond the limit—a classic window-integrity violation. The ambiguity between RAII guard semantics and explicit release is a real hazard that the documentation needs to clarify, and I should flag this as a major spec issue tied to how AcquiredCredit interacts with the release calls.

That said, a careful implementer would choose either commit-then-manual-release or just drop the guard, not both. But the pseudocode literally writes flow.release() in the ar...

Now looking at the B10 merge-1 snapshot and the closed AtomicBool recheck. The writer sets closed to true under the write lock before rebuilding and publishing the snapshot that removes the route. A reader loads the snapshot, gets the Bound route, and checks closed—if true, treats it as unknown_channel. The question is whether there's an ordering where closed reads as false but the route is already released.

The critical issue: when the writer sets closed to true and then publishes the new snapshot via ArcSwap, what ordering guarantees does the closed store have? The ArcSwap publish is a Release store and the load is Acquire, but if a reader loads the OLD snapshot (which still contains the route), it synchronizes with the OLD publish, not with the new closed.store(true). So the reader might load the old snapshot and not see the closed flag update, creating a race condition. Or the reader's closed.load could happen before the writer's closed.store, in which case the reader reads false and incorrectly treats the route as still available. The real-time ordering between the closed.store and closed.load isn't guaranteed by the snapshot load alone — I need to think about whether there's an actual synchronization point that orders these operations. The dispatcher's admission check acts as the real synchronization barrier for the client→module path, catching any stale closed flag reads by rejecting Closing or Closed routes. For the module→client direction, the closed flag itself is the primary guard, but the semaphore's is_closed() check prevents credit release races during teardown. So even if the AtomicBool lags momentarily, the actual observable behavior—whether we get backend_error or unknown_channel—is determined by these admission and semaphore checks, not the stale flag read alone. be specified with proper memory ordering semantics like SeqCst or a fence to make the recheck reliable—that's worth flagging as a change item. On the corr uniqueness front, the doc leans toward enforcing it at enqueue by rejecting duplicate correlations as a protocol error, which aligns with the earlier finding, but the actual mechanism isn't fully specified: the slot insertion would just overwrite an existing entry and push a second queue item, potentially losing the credit from the original slot.

The duplicate-corr leak is acknowledged in the open question but not actually enforced in the normative pseudocode, so this gap needs to be closed by moving the uniqueness requirement from the open question into the mechanism itself as a hard requirement. Now I'm checking whether the module-to-client terminal path can actually acquire the lock needed to remove slots—the terminal arrives on the module's read path and needs to reach the client-side RouteDispatcher's inbox. unlocked the inbox, so if try_send fails, we've already committed the removal—we can't put it back. That's a state inconsistency: the slot is gone but the frame never reached the client. The shipped code avoids this by holding the lock through both remove and send, or by not removing until send succeeds. Under v2, the lock-release-then-send ordering breaks that guarantee. Looking at the actual router.rs code, the terminal path sets `releases_credit = is_terminal`, then on `try_send` failure it escalates and returns early, skipping the `flow.release()` call entirely. But since escalation tears down the connection anyway, the credit leak becomes moot — the route gets released when the connection closes. The interaction between the early return and credit release should still be documented though, even if the practical impact is minimal.

Now examining B7 and control offload: v2 moves this to client-side only with inline module publication, which directly fixes the v1 issue. The existing test for module-side route.open publishing still passes because module connections stay inline. But I need to check whether client-side control offload introduces any observable reordering — specifically whether a per-client FIFO control task draining channel-0 could reorder route.open and data frames in a way that breaks ordering guarantees.

The problem is that if the client sends route.open followed by a data frame on that route, the data frame goes to the data path dispatcher while route.open is still being processed by the control task. Since the route doesn't exist until route.open completes the bind, the data frame arrives at an unbound route and gets dropped as unknown_channel. This is a new client-side reordering issue: the client's own route.open→data ordering gets broken by offloading the control channel, whereas today with inline channel-0 processing, route.open completes before the next frame is even read. The real question is whether a client sending data immediately after opening a route without waiting for the ack is even a supported pattern — the route channel gets assigned during bind, so pipelining like that might not be valid SDK usage anyway. I'll mark this as a MINOR observability caveat that needs documentation, since the contract claim about FIFO not reordering is slightly overstated if clients do pipeline their operations.

Now I'm reconsidering the B1 merge-0 classification. The v2 analysis correctly identifies route_backpressure as outcome_unknown (since handedToSocket=true triggers reconnect) and maps it to CallError::Module in Rust, which aligns with v1's findings. The proposal to add a retryable class through merge-0 is the right direction, and Q1' being hard-gated on that makes sense. But I need to verify the classification itself against the actual source — checking the classifyFailure logic in client.ts and how outcome_unknown flows through the managed call handler to scheduleReconnectAfterDrop.

I'm now being more careful about distinguishing between connection-drop classification (which classifyFailure handles) and terminal Error frames (which reject the pending directly with SubcError). A terminal Error frame settles the pending via reject, but I need to trace exactly how that maps to SubcCallError kind — the v1 findings cited specific line ranges for this mapping, so I should verify those to confirm v2's merge-0 proposal is correct.

The key question is whether the proposed in-place retry for route_backpressure and control_backpressure codes fits structurally into the existing managed-call retry loop. Looking at the call() function, it already has a retry loop that continues on unknown_channel and not_sent, and the Rust consumer similarly has a retry loop with continue statements. Adding a backoff+continue mechanism for backpressure codes fits this pattern structurally, so the fix direction is sound — though the actual SDK implementation in merge-0 hasn't been written yet.

The critical issue is that the broca/aft/alfonso-core contract impact can't be verified since those aren't in the checkout. So B1 is really a documented plan rather than a closed fix — it's honest about being a prerequisite for the re-gate, but "closed" would require the mechanism to actually exist. The retryable classification needs to key off the terminal Error body.code rather than just connection-drop classifyFailure, and I should verify that path is available.

Now I'm checking whether a terminal Error frame in TypeScript carries a code that the managed call() can branch on — specifically, whether terminalCallError preserves the code from the SubcError, or if it gets dropped in the wrapping. I also need to look at the interim blocking admission alternative as a fallback.

The interim proposes per-route bounded blocking admission on the drain side, but there's a tension: if the read loop can't block and the queue is bounded, then when it fills up, the read loop has to either drop, block, or fail-loud with route_backpressure. The interim's description of "per-route bounded blocking admission (drain-side)" is unclear about where the blocking actually happens and how it avoids the memory DoS problem if the queue grows unbounded.

Now I need to verify some implementation details before finalizing the approach — specifically checking how terminalCallError and errorFromFrame are preserved in the client code, and tracing through the router to confirm where the terminal path sits in the module read loop.

For the CANCEL frame routing, I'm checking whether it goes through the standard handle_bound path or has special handling, and whether the client→module CANCEL uses try_send or send to understand the B2 residual finding better.

Looking at the current code, CANCEL frames follow the same serial path as Requests through route_for_connection → data path → handle_bound → module_sink.send(). But in v2, the doc puts CANCEL handling directly in the read loop rather than through the drain queue, which means it's either try_send or a detached send—either way, it's unordered relative to the drain's Request send, confirming the B2 residual issue. The doc explicitly routes CANCEL directly to module_sink without going through the drain, so the ordering problem persists.

Now I need to check the client.ts terminal error handling to see if merge-0 is even feasible and whether the code can survive that scenario.
The teardown test ( is actually lenient — it accepts Goodbye, backend_error, *or nothing*. Let me verify a couple of final source facts: the TS terminal-Error code preservation (for merge-0 feasibility) and how CANCEL is currently routed to the module.

[assistant] 2026-07-15T08:20:36Z
I'm checking how CANCEL frames are currently routed in the existing code—they're not Request-type frames, but they still go through `handle_bound` and get forwarded to the module sink without acquiring credit, so they follow the same path as other non-Request frames.

Now I'm looking at the module-to-client dispatch logic where terminal frames are released. The RouteDispatcher in v2 is tied to each live client-side route binding, and the critical question is whether the module-to-client terminal path (which runs on the module's read loop) needs to acquire the same route's inbox lock when it calls `slots.remove` after looking up the route via the forwarding RwLock.

The drain task locks the RouteInbox before sending, but those sends happen outside the inbox lock in v2. The teardown sequence takes the inbox lock (setting admission to Closing) and then calls flow.close() with an epoch-fenced release, but it's unclear if these are nested lock acquisitions or separate. On the read-loop data path, the sequence is: forwarding read lock to find the RouteDispatcher, clone the Arc, then lock the inbox — so no nesting there as long as the Arc is cloned out first.

For the snapshot path in section 7, the data-plane lookups move to ArcSwap, so the read loop loads a snapshot without locking, gets the Arc<RouteBinding>, checks the closed flag, then locks the inbox — that's safe.

Now I'm working through the actual defect analysis. For B1 (route_backpressure → NotSent false in SDKs), v2 section 8 adds a merge-0 prerequisite SDK class. The v2 doc acknowledges the false claim and proposes adding a new retryable class before the daemon merge. The SDK's managed call() at client.ts:420-455 already retries on unknown_channel, not_sent, and outcome_unknown by reconnecting.

Looking at the error handling more carefully, errorFromFrame parses the code from the JSON body, so the code is available. The classifyFailure function only runs on drop/deadline, not on terminal Error frames — those go straight through settle→reject. So the retry logic for route_backpressure and control_backpressure needs to be added in the managed call() catch block, keyed on the error code, with retry-in-place and backoff instead of reconnecting. That's feasible, so merge-0 is a genuine fix that v2 gates the daemon merge on. But the doc also notes some concerns with broca/aft/alfonso.

The core contract impact can't be verified in this checkout — the v2 doc itself says to verify in their repos, not here. So B1 is closed at the design level, though the fleet-wide deployment gating depends on external process verification. The key question is whether a distinct retryable class is actually needed, and the answer is yes: a terminal Error with an arbitrary code just re-throws, so without merge-0, route_backpressure becomes a hard error. v2 correctly identifies this and sequences merge-0 first. I'm marking this closed at the design level with the caveat that cross-repo verification remains external.

But wait — there's a subtlety I need to trace through. The doc describes the daemon reading a frame, setting handedToSocket=true, outcome_unknown, then reconnecting. But that classifyFailure path only handles DROP/deadline cases, not a received Error frame. When a route_backpressure Error frame arrives, it settles via reject with a SubcError — then in the managed call, I need to check how it's wrapped. Looking at the error handling flow, a plain SubcError from an Error frame gets thrown as a terminal error rather than being classified as retryable.

So the current behavior treats route_backpressure as a hard terminal error either way. The v2 merge-0 fix addresses this. Now for the bigger issue — the CANCEL limbo case. Let me trace through the drain task loop and state machine carefully.

The flow separates lock acquisition from the actual slot state transition. After popping from the queue, there's a window where the corr exists in slots but hasn't been marked as Claimed yet. The CANCEL handler checks the slot state — if it's still Queued it removes it and synthesizes a cancelled response, if it's already Claimed it sets the cancelled flag, otherwise it forwards the CANCEL downstream. The critical gap is between the pop_front and the subsequent lock acquisition where Claimed is set. So the real issue is that `pop_front` only removes from the queue, leaving the slot still in the map as Queued. When drain re-locks to set the state to Claimed, a concurrent CANCEL could have already removed that slot entirely, causing the drain to try accessing a non-existent key—either panicking or proceeding with a missing slot.

The fix would be to combine both operations under a single lock: pop from the queue AND atomically transition the slot to Claimed in one critical section, eliminating the window where CANCEL can slip in between. The real issue is that even if pop and claim were atomic, there's still a race: after the slot transitions to Claimed, the drain releases the lock and waits for flow.acquire(), giving CANCEL a window to arrive, set cancelled=true, and create the same double-outcome problem. This suggests the design hasn't fully closed the gap between cancellation and delivery.

For the Delivered case, once the state is set and the lock drops, the module_sink.send() happens asynchronously—but if CANCEL arrives during that send, it gets forwarded to the module. The question is whether the Request and CANCEL messages maintain ordering on the module_sink, since the drain sends Request directly while the read loop forwards CANCEL through the same channel. ot first wins. If drain is blocked in send().await (sink full), and read loop calls module_sink.send/try_send for CANCEL, a try_send could fail if the sink is full. There's a real risk of reordering here — the CANCEL could reach the module before the Request does, causing the module to no-op on an unknown correlation ID, then the Request arrives and executes uncancelled. This recreates the R5 defect. In v2, marking a state as "Delivered" under lock doesn't mean the actual send has completed yet, so it's really "about-to-send" rather than "module has received it." A CANCEL forwarded while in the Delivered state still races against the Request's actual arrival at the module.

The read loop forwards CANCEL directly to module_sink without going through the drain, so both the drain's in-flight Request send and the read loop's CANCEL send are competing on the same mpsc sink. Tokio's mpsc doesn't guarantee ordering between concurrent send calls, so the CANCEL could arrive at the module before the Request does. even if CANCEL reaches the module first, the module's cancel handler would execute before the request is processed, which means the request could still run uncancelled — the core issue is that marking Delivered before actually sending the Request to the module creates a window where CANCEL can be forwarded and delivered while the Request is still in flight. The ordering guarantee is actually broken here—in the shipped serial code, Request completes its send before the loop even reads CANCEL, so module_sink always sees Request first. But with the concurrent drain and read loop, CANCEL can race ahead and arrive at module_sink before Request does. This is a significant issue if the module's correctness depends on that ordering, so I should flag this as a major concern and note that the documentation shouldn't claim ordered delivery unless the design actually enforces it.

Now I'm checking the other blockers. For B3, the design explicitly specifies a VecDeque with a HashMap inside a Mutex for RouteInbox—no mpsc scanning involved—which directly addresses the queue structure concern. That's resolved. Moving on to B4 around drain-task error handling...

The send failure path uses release and backend_error, with flow.acquire triggering ChannelFlowClosed when needed and routing to handle_closed with proper TeardownKind logic. There's an AcquiredCredit RAII guard wrapping the credit increment to ensure it gets released on panic. After the Delivered state is committed, the guard is marked as committed so it won't double-release...

but I need to verify the atomicity here—the guard's commit() must happen exactly when Delivered is set, otherwise the manual release in the send-fail arm could fire alongside an uncommitted guard, causing a double release.

Now I'm looking at the TeardownKind staleness issue. The handle_closed function reads the teardown field under the inbox lock, but flow.close() is called from the forwarding layer under a different lock—the forwarding write lock. So the teardown reason and the actual flow closure are synchronized under separate locks, which creates a potential race where they're not atomically coordinated. does NOT know about the RouteInbox/teardown, so v2 would need to thread teardown-setting through every release path—a pervasive change. But there's a race condition risk: if the drain is blocked in flow.acquire() and flow.close() fires before teardown is set, the drain wakes up and reads a stale None value instead of the intended reason, causing it to synthesize a backend_error defensively.

The real issue is that teardown-set and flow.close() aren't atomic across different synchronization primitives (inbox lock vs. semaphore), so there's no guaranteed happens-before relationship. The drain waking from sem.close() isn't guaranteed to see the teardown write unless there's explicit synchronization, which means the disambiguation between module_reloading and backend_error depends on a fragile ordering assumption. I'm tracing through the different code paths that call flow.close() to see if they all properly set the teardown reason first. The release_*_route_locked functions don't set teardown=Reloading before closing the flow, which means the drain would incorrectly read None instead of the expected reloading status—breaking the documented behavior unless v2 patches those paths to set teardown beforehand.

This is a significant gap: the teardown field's assignment site is under-specified and lives on a different lock than flow.close(), creating a race condition where stale or None reads could degrade module_reloading into backend_error. The existing test for module death still works because it expects backend_error, but the reload-to-module_reloading distinction is at risk. Now looking at the insert-after-send ordering issue in v2's section 2, where state gets set to Delivered and outstanding increments before the actual send call happens.

The terminal path correctly acquires the inbox lock after the module receives and emits, so slots.remove sees the Delivered state that was set and unlocked before the send. This means the release fires exactly once—the ordering is sound and this genuinely fixes the issue.

Now I'm checking whether the module→client path actually has access to the RouteInbox to perform the lock and conditional release. The RouteInbox lives on the client-side RouteDispatcher, and I need to verify whether the module_to_client route binding can reach it, or if the client_to_module and module_to_client bindings are separate Arc instances that don't share the same dispatcher.

Looking at the release logic, both client_to_module and module_to_client get removed together, and the RouteBinding holds both sinks, so they're likely the same Arc<RouteBinding> — meaning the dispatcher and inbox are accessible. The new lock acquisition on the hot path during terminal release could cause contention with the drain and read-loop, but it should be acceptable as an O(1) operation.

For the CANCEL scan issue, the v2 slots HashMap eliminates the linear scan entirely with O(1) lookup and removal.

On the bind-ACK ordering concern, v2 keeps module connections inline while offloading only CLIENT-side channel-0, so bind-ack stays inline. The test accepted_route_publishes_route_open_before_immediate_reverse_request verifies this by driving a module→client Request and asserting the client receives route.open on channel 0 before the reverse request — and since module connections remain inline in v2, this test continues to pass.

Now I'm checking whether the client-side control offload reorders anything observable. The client's channel-0 frames (route.open, route.close, cancel) go to a per-client-connection FIFO control task, but CANCEL is actually a DATA-channel frame on a non-zero channel, so it stays on the data path. The question is whether offloading client channel-0 to a FIFO task reorders route.open relative to the client's data frames on other channels — the design says the FIFO control task drains channel-0 so route.open bind-wait doesn't stall data frames, but route.open must... The client-side control task handles channel-0 commands like route.open and route.close, which are distinct from module control responses that stay inline on the module connection. This separation means the offload doesn't interfere with daemon-originated RPC settlements, so the architecture remains sound.

B8 is resolved by v2 explicitly reframing I3/I7/I4 as intentional changes rather than invariants—a documentation fix that's now closed. For B9, I'm examining the three-phase state machine (Open/Closing/Closed) with admission gated at Closing before snapshot removal, plus the cancel token select and bounded join with abort logic to prevent teardown hangs. The key question is whether try_push after flush remains atomic enough to avoid the original race condition.

The stale snapshot reader problem in v1 was that a reader holding a live sender could push after flush. In v2, setting admission=Closing under the inbox lock happens before binding removal, so any stale reader that loads the dispatcher and calls read-loop push will hit the admission check, see Closing, and reject the delivery. The data-plane push path validates this: load snapshot, check closed flag, lock inbox, then check admission—if Closing, reject. So the admission check itself becomes the barrier, not the flush. This closes the hole as long as the reader always checks admission under the inbox lock.

For the teardown hang: the drain task might block on module_sink.send with a FrameSink clone. In v2, cancel_token is selected against the send with a bounded-join timeout of 2 seconds, then abort. When the task is dropped after abort, it releases its FrameSink clone, but the RouteBinding Arc also holds module_sink, and stale snapshots hold Arc<RouteBinding>, so the channel doesn't close while other clones exist. The real concern is whether the teardown path in server.rs (which waits on the WRITER task with CLOSE_DRAIN_GRACE timeout before aborting) actually prevents the drain task from hanging connection shutdown, since the drain's module_sink.send block doesn't directly block the writer.

v2 tries to address this with cancel_token and bounded-join plus abort during teardown, but I'm realizing the teardown must be an explicit async call before the connection guard drops—you can't await inside a synchronous Drop impl. So the question is where that async teardown actually lives in the shipped code versus what the v2 design doc describes. I need to verify that RouterConnection::drop is actually sync, then trace through the lock-ordering to check if the inbox lock and forwarding write lock could deadlock—specifically whether the terminal release path holds both locks simultaneously. The teardown path acquires the inbox lock first in step 1, then the forwarding write lock later in step 6, while other paths don't reverse this order—so there's no single-point inversion. However, the document never actually specifies the lock hierarchy, which was explicitly flagged as a gap in v1's findings. v2 leaves this unresolved.

For the snapshot invariant issue, the publish happens inside the write-lock critical section after closed is set to true, so readers checking closed afterward will see it as true and treat the route as unknown_channel—matching today's behavior. But I need to trace whether there's a window where closed reads as false while the snapshot is already published. So even readers with stale snapshots see the closed flag because it's set on the same Arc instance before the snapshot is published. The real concern is ordering: if a reader loads the old snapshot and reads closed before the releaser sets it, they'll see closed=false and proceed to route into a closing flow. But the releaser also sets admission=Closing, so when the reader tries to push into the inbox, the admission check should catch it.

The question is whether all releases go through the full three-phase teardown from section 5 (which handles GOODBYE, endpoint drain, connection close) or if some releases like module GOODBYE bypass it. The release_module_route_locked function runs under write lock to set closed=true and publish the snapshot, but I need to figure out if it also triggers the drain-task cancel/join that's part of the teardown sequence.

There's a fundamental tension here: the closed flag can be set atomically under the write lock (which is sound for publishing), but the drain-task join is async and can't happen inside a sync function holding the write lock. So the closed-flag part of the solution works, but it's tangled up with the broader async-teardown location problem.

For the specific concern about stale Bound routes returning backend_error instead of unknown_channel, the closed recheck should restore the correct unknown_channel response. I need to verify that the module-to-client path in the router actually performs this closed check after acquiring the Bound route. The send failure on a torn-down module would trigger backend_error rather than unknown_channel, but that's actually correct since we genuinely attempted delivery on the drain path—it's distinct from the lookup observable concern that B10 addresses. With the closed recheck at push time catching most cases as unknown_channel, this residual race is acceptable. Now moving into the merge-0 vs blocking-admission decision, leaning toward a hard gate. ard-depending on unverifiable external deploys). But wait — if the blocking-admission interim queues into a bounded inbox on the read loop, and that inbox fills up, the read loop either blocks (creating head-of-line again) or rejects loudly. The doc claims "the wait is in the drain task, off the read loop," but admission itself happens on the read loop when it tries to enqueue. So the blocking-admission interim might not actually solve the HOL problem if the queue saturates.

There's a fundamental tension here: either the read loop blocks when the queue is full (reintroducing HOL), or the queue is unbounded (memory DoS). The interim as described is underspecified about how it handles a full inbox. I'll note that Q1' leaning toward hard-gate has some merit, but the interim approach is unsafe because it depends on unverifiable external deploys and doesn't clearly resolve the bounded-queue problem. Q2' involves a bounded-join with a 2-second timeout that may drop in-flight terminations.

When the drain aborts mid-send, a Request might be partially sent or a terminal in flight, but since the connection is closing anyway, that's acceptable. If the drain aborts after setting Delivered but before the send completes, the credit leaks temporarily, but flow.close() on teardown makes the release a no-op, so there's no real leak since the whole route is torn down. Q3' proposes adding depth caps plus a byte-based secondary cap to address the memory DoS issue.

Q4' enforces correlation uniqueness at enqueue by rejecting duplicate correlations as a protocol error. This fixes the critical bug where a duplicate correlation arriving while the first is still Queued, Claimed, or Delivered would overwrite the slot in the insert operation, losing the original slot and leaking its credit.

The problem is that §2's read-loop push uses an unconditional `slots.insert()` without checking for duplicates, which directly contradicts the Q4' safety requirement. The mechanism and the safety constraint are misaligned — §2 needs to implement the rejection logic, not just mention it as an open question.

Q5' leans toward a whole-table Arc rebuild rather than sharding, which is the right call conditional on measurement in T9, matching the v1 approach.

Now I'm looking at the Notify lost-wakeup issue in the drain loop. The classic race happens when the drain locks the inbox, finds the queue empty, unlocks, and then waits on notify — between the unlock and the await, a producer could signal, causing the notification to be missed.

But tokio's Notify is designed to handle this: if notify_one() is called when no waiter exists, it stores a permit. The next notified().await consumes that permit and returns immediately. So if a push and notify happen between the drain's unlock and its notified().await, the permit gets stored and the drain's next await completes right away, then it loops back and pops the item. This avoids the lost wakeup as long as the code uses Notify correctly.

However, there's a subtlety with multiple items: Notify only stores one permit no matter how many notify_one() calls happen. If two pushes occur while the drain isn't waiting, only one permit is stored. The drain wakes up, pops one item, loops back, and pops the second item directly from the queue without needing to wait. The drain fully empties the queue before waiting again because it re-checks the queue under the lock on each iteration and only waits when pop returns None. So the loop structure naturally handles multiple items, and the lost wakeup is prevented by tokio's permit semantics combined with the re-check-under-lock pattern.

The safe pattern requires either obtaining the Notified future before checking the queue, or relying on the stored permit. There's a known tokio subtlety though: Notify::notified() only starts listening when first polled, so if notify_one() is called after a None pop but before the notified().await is actually awaited, there's a potential race.

However, the implementation uses tokio::sync::Notify, which stores permits, so this is safe. The ordering is also correct: the push happens under the lock, then notify_one() is called after unlocking. So the pattern is verified-safe as long as the permit-storing semantics are respected.

But there's a potential issue during shutdown: when cancel_token.cancel() is called, it won't wake a drain task that's blocked on notify.notified() unless that await is also select!'d against the cancel token. The docs mention that blocking awaits like flow.acquire and module_sink.send are select!'d, but it's unclear if the notify.notified() wait is too. If it isn't, an idle drain waiting on an empty queue won't observe the cancellation, causing the bounded join to time out and forcing an abort.

Now I'm checking the lock-ordering situation more carefully. In the module-to-client terminal release path, I need to verify whether both the forwarding lock and the inbox lock are required. Looking at the data-plane route lookups, v2 uses ArcSwap which doesn't need locking, so the lock requirements might be simpler than I thought.

The drain operation acquires the inbox lock to pop and claim messages, then releases it before calling flow.acquire and sending to the module sink — no forwarding lock involved there. For teardown, the inbox lock (when setting Closing) is separate from the forwarding write lock that handles route release. If the forwarding write lock also atomically sets binding.closed and publishes the snapshot via ArcSwap, there's no need to hold the inbox lock inside that critical section, which means I might be able to avoid nesting the inbox lock inside the forwarding write lock entirely.

However, there's no forwarding-write lock held inside the inbox lock either, so theoretically the two locks never nest and there's no inversion risk. But this requires strict discipline that the spec doesn't actually document. If someone sets admission=Closed while holding the forwarding write lock, that creates forwarding-write → inbox nesting, which combined with any inbox → forwarding path would cause inversion. The teardown sequence treats the inbox lock and forwarding write lock as separate acquisitions, which hints at no nesting, but it's fragile and never explicitly specified. This is a significant gap in the spec — version 1 explicitly requested a lock hierarchy, and version 2 dropped it entirely.

Now I'm re-examining the drain pop/claim window more carefully. Between popping the correlation ID from the queue and then re-locking to mark it as Claimed, there's a race: if a cancel arrives during that gap, it sees the slot still in Queued state and tries to remove it from both queue and slots, but the queue already lost it. This is the actual defect I need to verify.

The real problem is that if the drain proceeds after the cancel has already removed the slot and sent a synthetic cancellation to the client, the drain will either panic trying to access a missing slot or skip it—but then it shouldn't continue to acquire and deliver. If it does proceed anyway, the module executes a request the client was already told was cancelled, and the credit gets leaked because the terminal path can't find the slot to release it. So the pop and claim operations must be atomic under a single lock to prevent this race entirely. The pseudocode shows two separate lock acquisitions for pop and claim, which contradicts the prose's assertion that the transition from Queued to Claimed must be atomic with the pop itself—if implemented literally as written, this reintroduces the limbo state the design claims to eliminate. This is a blocker-level inconsistency: the mechanism as specified has the hole, even though the documentation asserts atomicity it doesn't actually deliver. I need to check whether there are other limbo windows even if pop and claim were made atomic. Now I'm checking the terminal path's interaction with the drain's Delivered-commit sequence — the drain holds the lock while setting Delivered, releases it, then calls module_sink.send. If send fails, the drain re-locks and cleans up (outstanding decrement, slot removal, flow.release, synthesize backend_error). Since a failed send means the module never received the frame, no terminal can arrive for that correlation, so there's no race between the terminal path and the send-fail cleanup. The AcquiredCredit RAII guard and outstanding counter interaction still needs checking though.

At Delivered-commit, the state transitions to Delivered, outstanding increments, and the guard commits so it won't release on drop — the credit is now owned by the terminal path. On send-fail, outstanding decrements and flow.release fires, but since the guard was already committed, it won't also release, giving us a single release. The problem emerges in the rollback path: if the guard isn't committed, it's still armed to release on drop, but the rollback arm explicitly calls flow.release too, which would cause a double release.

The ambiguity is whether the explicit flow.release in rollback is meant to replace the guard's release or supplement it. If the guard is the sole releaser, rollback shouldn't call flow.release at all. If the explicit call is the intended releaser, the guard should be committed to disarm it.

This double-release risk—where the guard and explicit call both fire, potentially stealing another coroutine's slot when in_flight is still positive—maps directly to the prompt's concern about conflicting guards masking real leaks. Let me verify that RouterConnection::drop is synchronous and check the server close path to understand how routes get released when a connection closes—whether it's purely through the Drop trait or if there's an async teardown hook involved.
The teardown test (forwarding.rs:3811-3878) is notably lenient — it accepts Goodbye, backend_error, *or nothing*. Let me confirm the connection-cleanup path is synchronous `Drop` (critical for the B9 async-teardown analysis) and read the CANCEL/handle_cancel module no-op.

[assistant] 2026-07-15T08:25:38Z
I'm examining the concurrency model more carefully now. The connection cleanup happens synchronously through the `Drop` trait, but the design document describes a more sophisticated async teardown with a bounded join and abort fallback if the drain task doesn't complete in time. There's a mismatch between what's documented and what's actually shipped in the synchronous drop path.

Now I need to verify the lock ordering to check for potential deadlocks. The v2 design uses two locks: a per-route inbox mutex and a global forwarding read-write lock. I'm tracing through the teardown sequence to see if any code path holds the inbox lock while acquiring the forwarding write lock, and whether another path does the reverse.

Looking at the data-plane paths: the read-loop push only acquires the inbox mutex, and with the ArcSwap optimization for lookups, it no longer needs the forwarding read lock. The terminal path (module→client) also just locks the inbox for slot removal, with ArcSwap handling the lookup. But the teardown path is different — it holds the forwarding write lock while executing release_client_route_locked, and it also needs to lock the inbox in steps 1 and 5, so there's a write-lock-then-inbox ordering there.

The drain task keeps its critical sections clean: it locks and unlocks the inbox, then does awaits outside without holding it, so it never takes the write lock while holding inbox. This means the only potential inversion would come if any path does inbox-then-write-lock, but the push and terminal paths only touch inbox. So the hierarchy looks consistent as long as teardown always acquires the write lock before the inbox.

However, there's a timing issue: step 1 locks the inbox to set admission=Closing, but step 6 (the epoch-fenced release) happens under the write lock. If these are separate acquisitions with the inbox released in between, there's no nesting to prevent inversion, but it opens a window between the two steps where the sequencing matters.

The snapshot publishing in step 7 happens inside the write lock and doesn't need the inbox, so that direction is fine—but the doc doesn't explicitly clarify whether the inbox lock is held across all these steps or released and reacquired.

The real concern is who triggers teardown: a GOODBYE message arrives on the module→client read path, and the shipped code in router.rs handles that by calling release_module_route, which acquires the write lock. In v2, the module GOODBYE would initiate the three-phase teardown of the client dispatcher, and since both the GOODBYE and terminal frames flow through the same module read path sequentially (not nested), there's no immediate deadlock. But I'm realizing there's still a potential issue: release_client_route_locked invokes flow.close() under the write lock, and in v2 that might wake a blocked drain actor that could create contention.

The real confusion is around the timing of flow.close(). The existing path calls it at step 6 under the write lock, but section 5 describes a separate flow.close() at step 2 during teardown. I need to clarify whether these are the same call or if there's a redundant close happening. ardown is set at step 2, but the doc doesn't clarify which lock protects that write. If teardown is written without the inbox lock while the drain reads it under the inbox lock, that's a data race. The ordering seems to be: step 1 sets admission=Closing under inbox, then step 2 sets teardown and calls flow.close(), but if teardown isn't protected by the inbox lock, there's a genuine synchronization gap here.

Now I'm checking whether the teardown field could be stale when the drain reads it. The flow.close() at step 2 wakes the drain's acquire, which then locks the inbox and reads teardown. If teardown was written before flow.close() and there's a happens-before relationship through the semaphore's internal synchronization, the read should be safe—the semaphore's release/acquire provides that edge. But teardown is a separate field, so I need to verify whether writing teardown before the semaphore close creates a sufficient happens-before with the drain's acquire.

In Rust, a plain field write isn't automatically synchronized unless it's protected by an atomic or lock. If teardown is behind the inbox Mutex, then the teardown thread would lock the inbox to set it, then call flow.close(), and the drain would lock the inbox to read it—the mutex provides the happens-before guarantee. But looking back at the struct definition, teardown appears to be a separate field outside of RouteInbox, not inside it alongside admission, queue, slots, and outstanding.

That's a real gap: either teardown is a plain field (which is a data race), or it should be inside the mutex but the documentation is wrong. Either way, it's underspecified. Even if we assume proper synchronization, staleness could be an issue—but since each binding gets a fresh dispatcher, reuse across bindings isn't a concern.

The trickier case is when two different paths try to close the flow with different reasons: endpoint reload wants to set teardown to Reloading while a connection close wants ConnClose. There's a race on which reason wins, which is probably acceptable but worth documenting.

The real problem though is the fundamental contradiction between the synchronous Drop implementation in RouterConnection and the async 3-phase teardown that v2 requires—you can't await in Drop, so the synchronous connection-close path can't actually perform the bounded-join and abort of drain tasks that the spec demands.

Looking at the code, cleanup_connection is called synchronously from Drop and returns a Vec immediately, which means the connection-scoped route release happens synchronously today. But under v2, each client route owns a drain task that needs to be cancelled, joined, and aborted asynchronously—something that simply can't happen within Drop. This is a major integration gap that the design doesn't address. I need to figure out whether connection close actually releases client routes or if I'm misunderstanding what cleanup_connection does. The drop happens in an async context, so I could explicitly tear down the drain tasks before dropping the connection rather than relying on Drop itself—that's underspecified in the doc but potentially fixable. I'll mark it as a major underspecification touching B9 rather than a hard blocker. Now let me trace through the fast-terminal race more carefully in v2, starting with the drain sequence: locking the inbox to set state to Claimed{false}, then acquiring flow credentials...

Looking at the terminal path in the router, when a module sends a terminal it locks the inbox, removes the correlation ID, decrements outstanding if it was Delivered, and releases flow if needed. The key question is whether a fast module terminal can arrive before the correlation ID is marked Delivered—but the state gets set to Delivered under lock before the frame is even sent, so the module can't see the frame until after that lock is released and the send completes...

Actually, I need to think through the channel mechanics here. The module_sink.send() is a bounded channel send that completes once the frame is enqueued to the module's egress channel, then a separate writer task handles serializing to the socket. The terminal comes back on a different connection entirely—the module's read path. So there's genuine concurrency between the frame being sent and the terminal arriving.

Now I'm working through the B2 double-terminal and cancel-loss scenarios. In the v2 CANCEL handling under the inbox lock, there are two cases: if the request is still Queued, I remove it from the queue and slots, then synthesize a cancelled terminal—exactly one. If it's already Claimed with the cancelled flag set, I return early, and the drain operation will handle rolling back the credit after acquiring it, then synthesize the cancelled terminal. Both paths guarantee exactly one terminal.

For Delivered or None states, the CANCEL just forwards to the module, which owns the terminal release. The tricky part is the boundary case where CANCEL arrives right as drain is between locking the inbox and calling flow.acquire(). Since everything's serialized under a single lock, if CANCEL gets the lock first while the state is still Queued, it removes the request and synthesizes cancelled. But then drain tries to pop from the queue—except drain already popped the corr in a separate lock acquisition earlier, so I need to check the drain pseudocode to see how this actually plays out. drain resumes and hits the second critical section trying to set the slot state, but CANCEL already removed that slot from the table entirely — so drain either panics on an invalid index or gets None, and now we have a double-terminal problem where either the cancelled event fires twice or drain operates on a slot that no longer exists. The pseudocode doesn't account for this race: drain needs to re-check whether the slot still exists under the second lock, or we've introduced a limbo state where the slot vanishes between the two critical sections. The fix is straightforward: merge the pop and state-setting operations into a single atomic critical section so CANCEL can't slip between them. The spec shows two separate lock acquisitions, which creates the vulnerability, but combining them maintains the O(1) guarantee. This is a spec defect that needs flagging as MAJOR since it reintroduces the limbo/panic issue, though it's trivially correctable. The fix is straightforward: if pop and set-Claimed happen in a single critical section, the race window closes entirely, and drain's subsequent flow.acquire() call works cleanly—CANCEL arriving after that point sees Claimed and sets cancelled=true, triggering a rollback. The documentation as written (with two separate critical sections) is actually buggy and worth flagging as a spec gap. For the Delivered/send boundary, if send fails, the drain's error handler needs to decrement outstanding and remove the slot, but the doc doesn't clarify what happens if CANCEL arrives during that window. In the rollback path, the state is Claimed (not yet Delivered), so the module never receives the frame since send hasn't happened yet. When the terminal path checks if the slot was Delivered, it finds nothing because rollback already removed it, so there's no race condition between rollback and any module→client terminal—the module never got the request in the first place.

Now I'm reconsidering whether the pop→claim gap in B2 actually causes a real problem or if it's harmless. The sequence is: drain pops the corr from the queue (leaving the slot in Queued state), releases the lock, then a cancel operation acquires the lock and removes the slot entirely while synthesizing a cancelled message to the client.

But then drain acquires the lock again and tries to set the slot to Claimed—except the slot is already gone. If the code panics on a missing slot, the drain task dies and wedges the route. If instead it resurrects the slot, we get the exact B2 failure mode: the cancelled request executes anyway, the client receives both a cancelled and a terminal message, and the cancellation is lost.

This is the core defect the mechanism was designed to prevent. The pop/claim split reintroduces it unless those two operations are fused into a single critical section. The architecture works if they're atomic, but the pseudocode shows them separate—which is either a blocker-level spec defect or a trivial fix depending on whether we read charitably or hold the doc to what's actually written.

I'm leaning toward calling it MAJOR (spec must fuse pop and claim) rather than BLOCKER, since the architecture itself is sound. The deliverable needs precision though, so I'll document it as "CLOSED in architecture, NOT-CLOSED as written" and spell out the fix.

Now I'm checking the lost-wakeup scenario in the drain path. The critical section needs to atomically pop from the queue while holding the lock—if the queue is empty, we wait for a notify rather than releasing the lock and missing the signal.

Tokio's Notify handles this elegantly: it stores a permit if no waiter is registered, so a notify_one() call before notified().await still delivers the signal via that stored permit. The key gotcha is ordering—you must create the Notified future and check the condition after, not before. The pseudocode as written ("if None → wait notify") needs to follow tokio's recommended pattern: create the notified future first, then check if the queue is actually empty, then await. ents correctly with the loop, the permit semantics handle the lost-wakeup risk. Even if multiple pushes happen while the drain is busy, the queue re-check in the loop ensures all items get processed. The only requirement is that the drain must keep looping and re-checking the queue before awaiting, which the pseudocode does—so this is safe.

For lock ordering, the terminal path removes the slot while holding the inbox lock, then releases the flow lock outside of it, avoiding any inversion. The drain rollback similarly releases the flow lock after unlocking the inbox. Now checking the teardown path to ensure it follows the same pattern. The drain's handle_closed can read teardown state under the inbox lock while teardown holds the write-lock at step 6, but since handle_closed doesn't need the write-lock and release_client_route_locked doesn't touch the inbox, there's no deadlock risk. The concern is that the codebase never explicitly documents the lock hierarchy to prevent inversion, which was flagged as a gap in the v1 analysis.

Now looking at the AcquiredCredit RAII guard and how it interacts with the outstanding counter — the guard releases credit on drop unless explicitly committed, and on the Delivered path the drain must commit it to prevent that release, then the terminal path handles the outstanding decrement and final release. the terminal path releases it. On send failure, the guard is already committed before the attempt, so it stays inert while the error handler manually releases—exactly one release total. The ambiguity is whether "commit()'ed" truly prevents the guard from releasing on drop.

The real issue is the timing: if commit happens at Delivered before send, a panic during send leaves the guard inert with no terminal release, causing a leak. If commit happens after a successful send, then send failure triggers both the guard drop and the manual release—a double release. Either way there's a hole. The right approach is to commit the guard only when ownership transfers to the outstanding accounting at Delivered, and make the send-failure path manually roll back outstanding and release exactly once without relying on the guard.

But there's another gap: if a panic occurs between Delivered and send, the corr sits in slots as Delivered with incremented outstanding, but never gets sent and never reaches a terminal state. During teardown, step 5 synthesizes cancelled corrs from the queue, but this Delivered corr isn't queued—it's stuck in slots. The teardown doesn't mention releasing credit for these stranded Delivered corrs, so the outstanding count and credit leak permanently.

More critically, the guard-plus-manual-release pattern is a footgun. If the guard commits at Delivered and send fails, the Err arm's manual release is the only one—fine. But if an implementer naturally commits the guard after send succeeds, then on send failure the guard drops automatically AND the Err arm releases manually, causing a double release. The pseudocode shows both a guard and a manual release in the Err arm without clarifying the exact commit timing, which is underspecified and dangerous.

The existing CAS guard in forwarding.rs (checking in_flight != 0) actually masks this double-release problem. When in_flight is 1 and we double-release: the first release decrements to 0 and adds a permit, but the second release finds in_flight already at 0, so the CAS guard prevents the permit from being added again. This hidden interaction between the double-guard and the leak detection is exactly the kind of subtle masking that makes the real issue hard to spot. The real risk is the opposite scenario where neither guard releases, causing a leak. The documentation's explicit manual release in the error path suggests the guard is committed upfront, so only the manual path should fire—probably safe but somewhat fragile. I should flag the guard commitment timing as underspecified. For R11/B8, the existing CAS guard prevents window growth from over-release, and v2's slots-gate stops duplicate terminals from releasing at all. They compose cleanly: the slots-gate is the primary defense (blocks the decrement entirely), while the CAS is the backstop (prevents window corruption if the gate is somehow bypassed). No conflict between them.

When two terminals for the same correlation arrive concurrently on the module-to-client path, both lock the inbox serially. The first removes the correlation from slots and gets Delivered, triggering a release and decrementing outstanding. The second finds nothing in slots and skips the release. Only one release happens, so the CAS guard isn't even invoked—B8/R11 closes cleanly. But actually, "two terminals concurrently" is misleading here: they're both on a single read loop per module connection, which is serial, so they'd arrive sequentially rather than truly concurrently.

The module-to-client terminal path runs on the module's serial read loop (since v2 doesn't split module connections), so terminals from the same module are already serialized. The slots-gate under inbox lock is sufficient. B8 is closed.

Now looking at B10 snapshot: v2 publishes under a write lock plus an AtomicBool closed flag, and the reader rechecks by loading the ArcSwap snapshot, getting the Bound route, and checking the closed state.

The question is whether there's an ordering where closed reads as false but the route is already released. The sequence would be: reader loads snapshot S0 with Bound route R where R.closed=false, then concurrently teardown acquires the write lock, sets R.closed=true, rebuilds snapshot S1 without R, publishes it, and releases. The reader already holds Arc<R> from S0. When the reader checks R.closed, if the load happens after teardown sets it to true, the reader sees true and treats it as unknown_channel. If the load happens before, the reader sees false and proceeds to route into R—but R.flow was already closed during release, so the reader routes into a closed flow.

In v2, the push path checks `lock inbox: if admission != Open → route_closing`. Teardown sets admission=Closing before the release step, so even if the reader sees stale closed=false and proceeds to push, the inbox admission=Closing catches it and returns route_closing instead of unknown_channel. That's a new observable behavior that the doc's §7 doesn't quite account for—the dispatcher's admission check acts as the client→module equivalent to the closed flag check.

There's a potential divergence here: today, a stale snapshot showing Bound gets unknown_channel when the binding is gone. In v2, if the snapshot is stale (still shows Bound) but closed=true, it matches and returns unknown_channel. But if closed=false (not yet set) while admission=Closing, it returns route_closing instead. So there's a window between step 1 (admission=Closing set) and step 6 (closed=true set) where a stale-snapshot reader hits this new code path.

The key difference: today's RwLock approach has no "Closing" state—routes are either Bound (deliver) or gone (unknown_channel). v2 introduces route_closing as a genuinely new observable state during teardown, which is a behavioral change.

Now I'm checking whether SDKs handle this new code. merge-0 adds route_backpressure and control_backpressure as retryable, but route_closing isn't in that retryable set—it gets classified as terminal or outcome_unknown instead. This creates the same B1 problem: v2 introduces route_closing in the read-loop but doesn't include it in merge-0's retryable class, so clients misclassify it as a hard terminal error.

Looking at the specific sections, §2 line 64 synthesizes route_closing, but §8 line 234 only maps route_backpressure and control_backpressure as retryable. Even worse, I5 line 255 lists the synthesized terminals as "cancelled/route_backpressure/control_backpressure/module_reloading/backend_error"—route_closing doesn't appear there either. So route_closing is emitted in §2 but completely absent from both the retryable class and the invariant I5 vocabulary. This is a real inconsistency: either route_closing is a new code that merge-0 doesn't cover, or it's an omission from the contract.

Actually, thinking about this more carefully—during teardown when admission is Closing, the rejection should probably just synthesize unknown_channel to match what's observable today, since §7 already maps the closed-flag path to unknown_channel. Using route_closing contradicts the stated goal of preserving today's observable exactly, so this is a major internal contradiction in the doc.

Now looking at the Q4' corr-uniqueness issue: the doc leans toward enforcing duplicate-correlation rejection at the queue level, but the read-loop pseudocode in §2 just does a straightforward insert without checking for duplicates first. If a corr already exists in slots, the insert overwrites it, which leaks the credit from the overwritten entry—if it was Delivered, the outstanding count still includes it but the slot's been replaced, so when the terminal arrives it finds the new slot and releases the wrong accounting. The pseudocode as written has this leak; the fix only appears in the open-question lean, not in the actual mechanism described.

Moving to finding #12 on the B1/merge-0 classification: the claim is that a data-plane Error with route_backpressure maps to outcome_unknown in the terminal state. I can verify this in client.ts where the handedToSocket check determines whether to return outcomeUnknown, and the terminal-Error path does settle and reject with the error from the frame. But I need to trace through the managed call's catch handler to see if a terminal Error actually becomes a SubcCallError or something else.

Looking at the flow: when managedRequest throws, pending.reject gets an errorFromFrame which is a SubcError, not SubcCallError. Then in the call() catch block, if the error isn't a SubcCallError, it wraps it as terminalCallError and re-throws without retrying. So a plain SubcError from an Error terminal becomes a hard throw, not a retry. This confirms B1 is real and merge-0 is genuinely needed. The doc's classification is correct, and merge-0's proposal to add in-place retry (reusing the existing call() loop's continue mechanism for unknown_channel and not_sent) by introducing a retryable code makes sense.

However, there's a critical issue: the terminal Error settles the pending promise by rejecting it, which causes managedRequest to reject and propagate up. For in-place retry to work, the SDK can't settle-and-throw but must re-issue instead. The existing retry loop re-issues via continue after catching. So the flow needs to be: Error terminal → reject(SubcError) → managedRequest rejects → call() catch → detect if code is retryable → continue to re-issue. This requires classifyFailure/call() to inspect the SubcError.code and map it to retry logic, which is feasible. The merge-0 in-repo portion is sound, but the impact on broca/aft/alfonso-core can't be verified without checking those codebases.

So B1 is gated correctly on the daemon side—don't emit codes until merge-0 is live—and the SDK design is feasible. But route_closing from the earlier section isn't in the merge-0 class, which partially reopens B1. There's also the interim "blocking admission" alternative to consider. I'd rate B1 as closed-with-caveat due to the route_closing omission and unverifiable external consumers.

Now looking at B7 and bind-ACK ordering. In v2, module connections aren't split; bind-ack stays inline on the module read path. The shipped test validates that accepted_route publishes route_open before immediate re-

verse_request stays green because both the module's bind-ack and immediate reverse request flow through the module's inline serial read path unchanged. The client-side-only offload doesn't affect module ordering, which genuinely closes B7—the whole-FIFO problem was on the module side, and v2 doesn't offload that. The test sends a Request from module_ctx and expects client_rx to receive route.open first, then the reverse request, and this ordering is produced inline on the module read path. B7 is closed.

Now I'm checking whether the client-side offload introduces any new reordering issues with the client's channel-0 FIFO. The route.open is a control RPC that the daemon relays to the module and waits for acknowledgment—the route isn't actually bound until the module responds. A compliant SDK would await that acknowledgment before sending data, so reordering shouldn't happen. But if a client pipelines bind and data back-to-back without waiting, the daemon's serial loop normally processes bind first then data. With offloading, the data on the fast path could overtake the bind on the control path, breaking that guarantee.

The documentation claims client-side offload preserves control-to-control ordering but doesn't address the control-before-data case on the client side—which mirrors the same issue found in v1 (module bind-ACK to data). The SDK's single-flight pattern protects compliant clients, but the whole reason for client offload is to let route.open's 12-second bind-wait not block the client's data and cancel operations. So under offload, the client's route.open could stall while cancels proceed.

The real problem emerges with pipelined channel-0 operations: if a client sends route.open(R2) then immediately sends data on R2, the data might overtake the open acknowledgment on the wire and get dropped. The SDK's await-before-data pattern prevents this for compliant clients, but raw-wire pipelining breaks the contract—a narrower issue than I4's raw-wire caveat, but the documentation doesn't flag it. The only tradeoff is control-before-data ordering for pipelining clients, which is minor. Now I'm reconsidering the design decisions across Q1' through Q5': hard-gating versus interim blocking-admission leans toward hard-gating as the cleaner approach, though interim blocking preserves better semantics and decoupling. For Q2', the bounded-join with a 2-second timeout and abort could drop in-flight terminals, but that's acceptable since connection closing would tear down the channel anyway without causing credit leaks.

The real concern is whether aborting the drain task mid-operation could leave the module sink in an inconsistent state. Since the FrameSink uses an mpsc sender, aborting just drops the send future without partial frames—the enqueue is atomic. The key insight is that all awaits happen outside the lock, so aborting at an await point never holds the Mutex, avoiding poisoning.

I'm also checking the other design decisions: byte-based capacity caps to prevent memory DoS, enforcing correlation uniqueness at enqueue time, and the whole-table Arc approach.

The whole-table rebuild strategy is sound in principle, but there's a performance concern — rebuilding the entire snapshot table under the global write lock on every mutation could become expensive under high route churn, adding O(N) work per bind/release operation. This needs benchmarking to confirm it doesn't undermine the contention improvements the redesign targets. I'll mark it as conditionally correct pending measurement.

Now I'm checking the connection teardown path against the async design. The RouterConnection::drop is synchronous, but the v2 teardown sequence (cancel token, bounded join, abort) is inherently async. The design mentions the JoinHandle is owned by the connection or forwarding table, but it doesn't clearly specify where the async teardown actually runs — whether handle_connection explicitly tears down before dropping the guard, or if there's another integration point. The shipped connection-close path uses a sync Drop, which can't directly execute async cleanup.

This surfaces the same task-leak problem from finding #9: if flows close but drain tasks aren't properly joined or aborted, they leak indefinitely (blocked on module_sink.send or waiting on Notify). The doc addresses GOODBYE and endpoint-level teardown, but the connection-close path is a sync Drop with no way to await the bounded join, so drain tasks will leak or hang unless the integration is redesigned.

Now I need to verify whether cleanup_connection actually releases the client routes and their dispatchers when a connection closes. Each client-side route binding has a RouteDispatcher with a drain task, and on connection close all those bindings should be released—but I need to check what deregister_connection actually does to confirm the routes are being freed.

The tricky part is that Drop is synchronous, so I can't await the bounded-join for graceful draining. I could call abort() synchronously to stop the drain task, but that would drop in-flight sends immediately rather than letting them finish. The documentation specifies bounded-join first, then abort, but that requires await—which isn't possible in a sync Drop. For connection close, immediate abort might be acceptable, but the issue is that the JoinHandles need to be owned by the connection struct so Drop can access them, and the doc doesn't reconcile this constraint with the sync-Drop limitation.

If the forwarding table owns the per-route JoinHandles, then the cleanup code running under the write lock could abort them synchronously. That handles immediate abort but not graceful shutdown. Since Q2' accepts abort-drop for closing connections, immediate abort in Drop is defensible, though the spec is underspecified—I need to clarify how the async teardown in §5 maps to the synchronous connection-close path, define the immediate-abort semantics, and verify there's no Mutex poisoning or credential loss.

Now I'm verifying that deregister_connection actually releases client routes and implicates the drain tasks, and checking whether the to_error_frame code handles route_closing properly. Let me do a couple targeted reads to nail down those details.
The teardown test (forwarding.rs:3811-3878) is notably lenient. Let me verify two final facts: whether connection-close (sync `Drop`) releases client data-routes (implicating drain-task teardown), and whether `route_closing` exists as a code anywhere.

[assistant] 2026-07-15T08:30:36Z
I'm diving into the concurrency architecture to identify potential deadlock scenarios. The design introduces a per-route mutex for the inbox alongside the existing global forwarding table's read-write lock, so I need to carefully trace through the lock acquisition order to spot any hierarchy violations.

Looking at the teardown sequence, it acquires the inbox lock early (step 1), releases it, then later takes the forwarding write lock during the epoch-fenced release (step 6). The snapshot rebuild happens inside the write-lock critical section, but it's unclear whether that path needs to touch the inbox. The atomic `closed` flag is set under the write lock without needing the inbox lock, which is safe. The real question is whether any code path acquires the inbox lock while already holding the forwarding write lock—that would create a deadlock risk if another path does the reverse.

But there's a deeper lifecycle issue: the v2 teardown is described as an async 6-step procedure with awaits and bounded joins, yet connection teardown is triggered synchronously through `Drop`, which can't await. The shipped implementation in `RouterConnection::drop` is synchronous, but the design doc describes an async teardown with a 2-second bounded join on the drain task. You can't block a runtime thread waiting for that join from a sync Drop—this is a fundamental contradiction between the design and what's actually possible. The sync Drop path can't await the bounded-join-abort dance, so there's no async teardown hook wired into the connection lifecycle—the design assumes an awaitable teardown but the actual implementation tears down synchronously. This is a real gap.

Now I'm tracing through the drain task state machine, looking at the Claimed boundary race where the inbox is locked to pop from the queue, then the slot state is set to Claimed, then flow.acquire() blocks, and finally the inbox is locked again to check if the state changed to Claimed with cancellation or if admission closed.

The CANCEL read-loop can intercept at different points: if the slot is still Queued it removes it and synthesizes a cancelled response, if it's Claimed it sets the cancelled flag and returns, otherwise it forwards the CANCEL downstream. The critical race happens during the acquire() call—if CANCEL arrives while acquire is blocking, it sets cancelled=true, and when drain checks the state at the lock point it sees the flag, rolls back, releases the flow, and synthesizes a cancelled response so the module never receives the frame.

But there's a subtle window between popping the corr from the queue and setting the slot state to Claimed—the slot is still in the slots map as Queued, but the queue entry is already gone. If CANCEL arrives here, it tries to remove from the queue (which is a no-op since it's already popped) and then removes the slot entirely, synthesizing cancelled. Then when drain tries to set the slot state to Claimed, the slot no longer exists.

Depending on how the code handles this—whether it uses unwrap-style indexing or get_mut—the drain task either panics when accessing the missing slot or skips the state update and proceeds to acquire and send a frame anyway.  If it sends without owning the slot, the request gets delivered as cancelled but there's no slot to mark as Delivered, leaving the terminal-release path unable to find the slot and never releasing the credit—a leak. The core issue is that popping from the queue and claiming the slot happen in separate lock acquisitions, so a CANCEL arriving between them can remove the slot while it's still in Queued state, recreating the limbo condition. The real fix would be to atomically pop and claim under a single lock, but the current design splits them into two separate critical sections. Even if the implementation merges these into a single critical section, the pseudocode as written is still buggy. The fix is straightforward—pop and claim can happen atomically under one lock—so I'm flagging this as a MAJOR spec defect since the normative pseudocode itself has the hole, and test T10 wouldn't catch the pop/claim race unless specifically targeted. But even with that merge, there's still a potential window: once pop and claim happen atomically, the slot becomes Claimed, and if CANCEL arrives at that point it sets cancelled=true, then drain continues from there.

Now looking at the fast-terminal race in section B5: the spec records Delivered and increments outstanding under lock before sending, so by the time the module receives the frame and the terminal comes back on the module-to-client path, the slot is already marked Delivered. When slots.remove checks that state, it correctly sets release=true and proceeds.

But there's a critical gap: the terminal-release path in router.rs (lines 281-309) needs to actually acquire the inbox lock and check the slots table. The RouteDispatcher is scoped to each live client-side route binding, and the module-to-client terminal path operates on the RouteBinding itself. The question is whether the dispatcher is reachable from the RouteBinding so that the release path can access it—the current code calls route.flow.release() on the RouteBinding's flow, not on a dispatcher, which suggests a potential integration issue.

The deeper problem for B5 is that the ordering guarantee depends on module_sink.send(frame).await completing before the module processes and replies. But send().await only means the frame is enqueued to the module's egress channel, not that the module has received it. That's actually fine though—the enqueue happens after the Delivered flag is set, so the ordering still holds.

Even if the module processes instantly and replies, the reply's terminal-release path acquires the inbox lock, and by that point the slot is already marked Delivered. So the happens-before relationship is solid: Delivered gets set, then the frame is enqueued, then the module receives and replies, then the terminal-release path locks the inbox and sees Delivered. B5 is closed, assuming the terminal path uses the same inbox lock.

Now looking at B4 and the ChannelFlowClosed disambiguation—the test for blocked flow control waking when the module tears down is lenient and accepts either Goodbye or back

-end error or nothing at all, rather than strictly demanding a backend_error terminal. So v2's mapping of `teardown=None` to `backend_error` is sufficient to pass the test, but the test itself is weaker than the claim suggests. The real question is whether `teardown` will actually be `None` when the module dies—module death means the connection drops and triggers `release_module_route_locked`, which calls `flow.close()`.

Now I'm tracing whether the module-death path actually sets the client-side dispatcher's `teardown` field. The documentation says teardown is set "by the mutator that closed the flow," and module death is indeed a mutator that closes the flow via `release_module_route_locked`. So the question becomes: does that code path set `teardown`, or is module death treated as having no teardown reason, leaving it as `None` and thus mapping to `backend_error`?

If module death sets `teardown=ConnClose`, the client request would be silently dropped, which would leave the waiting client hanging until timeout—a regression from the intended `backend_error` response. But if it leaves `teardown=None`, then the client gets the proper `backend_error` as documented. The distinction matters because the client is still connected and waiting when the module dies, unlike a graceful reload scenario.

The real problem is that both graceful module goodbye and module crash funnel through the same `release_module_route_locked` → `flow.close()` path, so there's no mechanism in the teardown-kind field to distinguish between them. This is a critical gap in the design—the mutator can't tell whether the route removal is due to a graceful goodbye (which should be silent) or a crash (which should trigger backend_error). The teardown kind specification is incomplete—I need to flag which code paths set it to what value, especially since the module-death-to-backend-error mapping is critical for the test to work. Now I'm looking at a potential double-counting issue with the RAII guard: `AcquiredCredit` calls `flow.release()` on drop, but the rollback path explicitly calls `flow.release()` as well, which could lead to releasing the same credit twice. The CAS guard masks the double-release by logging a warning when the second release tries to decrement an already-zero in_flight counter, so the net effect is correct (one permit returned) but the mechanism is fragile—relying on a safety net meant for best-effort protection rather than correctness. I need to check whether the doc intends both the explicit release and the guard to fire, or if commit() is supposed to prevent one of them.

Looking at the send flow: when a slot is claimed and delivered, an outstanding counter increments; if the send fails, the outstanding decrements and the slot is removed, then the credit is explicitly released. The RAII guard wraps the credit from acquire until either commit() is called (disarming it) or it drops (releasing on destruction), so the guard is armed at acquire and should be disarmed by commit() on successful delivery. On the send-Err path, the explicit `flow.release()` is fine since the guard was already committed at the Delivered state. But the rollback path is problematic — the guard should release on rollback (since it wasn't committed), yet the pseudocode also shows an explicit `flow.release()`, creating a double-release that's masked by the CAS guard. The panic path between acquire and the Delivered state relies solely on the guard, confirming it must handle the release, which makes the explicit call in the rollback redundant. This is a minor-to-major issue depending on how the pseudocode is interpreted.

The `outstanding` counter is handled correctly though — it's only incremented at Delivered and decremented at terminal states or send-Err, so rollback never touches it since it happens before the increment. No double-counting there.

Now looking at the notify mechanism in the drain loop: when the queue is empty, the drain releases the lock and waits on the notify, but there's a classic lost-wakeup risk if a push happens between the empty check and the wait call. ified().await` will consume that stored permit immediately. The key insight is that even if multiple pushes occur while draining, Notify only stores one permit, but that's fine because the drain loop continues popping from the queue until it's empty—so a single wakeup is sufficient to process all pending items.

The drain doesn't batch-drain everything then wait; instead it pops one item, processes it (which involves an await), then loops back to pop the next. This means if the queue has multiple items and only one notify permit was stored, the drain will pop the first item, process it, loop back and pop the second item without needing to wait since the queue is still non-empty. It only blocks when pop returns None. As long as the implementation uses tokio Notify's documented semantics—where a push triggers notify_one and the drain re-checks the queue under lock after waking—there's no lost-wakeup risk, so I'm marking this as low risk.

However, there's a potential issue during shutdown: when cancel_token.cancel() fires, the drain's wait needs to be structured as a select between the notify and the cancel token so it can exit cleanly rather than remaining blocked. The notify-wait also needs to be select'd against the cancel_token, otherwise an idle drain waiting on notify won't wake up during teardown and the graceful join always times out to abort — that's a gap in the design. And the real issue is that connection teardown is a synchronous Drop operation, which means it can't use bounded-join at all. The GOODBYE path might handle async cleanup, but connection-drop teardown runs synchronously through Drop, which can't execute the bounded-join and abort sequence needed to properly clean up drain tasks. This means drain tasks could keep running detached after connection close, potentially holding onto module egress channels. I need to check if there's any async cleanup happening elsewhere in the connection handler after the main loop exits.

Actually, looking at the server code, after `connection_loop` returns there's just synchronous drops of the context, egress, and connection objects, followed by awaiting the writer — but no async teardown of the drain tasks themselves. So the gap is confirmed: there's no mechanism to track and abort drain task JoinHandles on connection close. Though interestingly, `JoinHandle::abort()` is actually synchronous and non-blocking, so it could theoretically be called from Drop if the handles were accessible there.

The real issue is what happens when you abort a drain task mid-flight: if it's in the middle of sending a frame through `module_sink.send`, that frame gets dropped, but the connection is closing anyway so that's acceptable. More critically, if the task is aborted after acquiring credit but before the Delivered state, the `AcquiredCredit` guard's Drop runs during the abort, which calls `flow.release()` — but since the route is already being torn down with `flow.close()`, the release checks whether the semaphore is closed before proceeding.

The fundamental problem is that the documented teardown strategy of "graceful bounded-join then abort" can't actually happen in a sync Drop context — you can only abort. This means the connection-close teardown loses the graceful join step that the design promised, which weakens the overall teardown guarantees.

There's also a critical race condition: the epoch-fenced release (step 6) is supposed to happen after the join/abort completes (step 4), but with abort-only teardown in sync Drop, the aborted task's destructors may still be running when the release executes. This creates a window where the AcquiredCredit guard's flow.release() could race against the flow.close() happening in the release path — a data race on the flow state itself.

Now I'm looking at the client-side control offload concern: moving the client connection's channel-0 frames to a FIFO task while keeping data frames on the inline read path. The key question is whether this reorders anything observable, but there's still only one read loop per connection pulling frames off the socket — it just dispatches channel-0 frames to a separate task.

The subtle reorder happens when a client sends route.open (channel 0) followed immediately by a CANCEL (channel != 0) for a different route. Under v2, route.open goes to the control FIFO task where it might block for 12 seconds waiting on a module ack, while the CANCEL goes straight to the dispatcher and gets processed immediately. Since the CANCEL targets a different route, there's no ordering dependency — this is actually the desired behavior.

But there's a real problem with the client-side "bind-then-immediately-send-data" pattern: if a client sends route.open for route A and then immediately sends a data Request on route A, the route.open is still being processed by the control FIFO task (the bind hasn't been committed yet), while the data Request hits the dispatcher for channel A. Since channel A isn't bound yet, the data frame gets dropped or errored as unknown_channel. Under the original serial loop, route.open would complete before the next frame is even read, so the data Request would find A already bound. This means v2's offload breaks the client's ordering guarantee.

The SDKs typically await the route.open ACK before sending data, but raw-wire or pipelining clients could send them back-to-back. Under v2, the data could overtake the still-in-flight route.open and get dropped — this is the symmetric version of the module-side bind-ACK→data barrier that v2 correctly keeps inline, but now on the client side. The documentation claims client-side route.open FIFO doesn't reorder anything observable, but it does reorder client route.open versus subsequent client data on that same route.

Actually, I need to check whether channel-0 route.open→data-on-new-channel is an ordering the daemon guarantees today. The read loop processes route.open to completion (commits bind) before reading the next frame, so a client can pipeline route.open + data. But route.open relays route.bind and awaits the module ACK up to 12 seconds, so the read loop blocks that long before reading the next frame anyway — meaning pipelined data would wait 12 seconds behind route.open regardless.

Under v2 client-offload, the data gets read immediately without waiting for route.open to complete, so it hits an unbound channel and gets dropped. That's a real behavior change: today pipelined-data-after-route.open works (waits then delivers), but under v2 it races and drops.

Though I'm questioning whether this is even a supported pattern. The wire contract probably requires clients to await route.open before sending data, since the client needs the route ACK to learn the channel and epoch assigned. A client can't meaningfully send data before route.open returns because it doesn't know which channel to use. I need to figure out whether the client proposes the channel in route.bind or if the daemon assigns it.

I'll flag this as a MEDIUM-confidence reorder that needs verification: client-side control offload changes the ordering of route.open→same-route-data, and if any client pipelines data after route.open without awaiting the ACK, the data races an uncommitted binding.

There's also the CANCEL ordering to think through. When a client sends Request(corr=5) on route A followed by CANCEL(corr=5) on route A, both are channel!=0 and hit the dispatcher in order—that's fine. But what about a client sending a channel-0 control frame that needs to be ordered before a data frame?

The main risk really does come down to route.open→data, so I'll keep this as a medium finding.

Now looking at the publish-under-lock scenario: the snapshot is rebuilt and published inside the write-lock critical section, and the closed AtomicBool is set under write lock before the snapshot removing it gets published. Readers recheck closed and treat closed-but-visible as unknown_channel. In the release path, the route.closed flag is set to true atomically, then removed from maps, then the snapshot is rebuilt and published—all under the write lock.

For readers, there are two cases: if they load a snapshot published after the release, the route is absent and they get unknown_channel. But if they load a stale snapshot published before the release, the route is still bound in that snapshot. The key insight is that the closed AtomicBool lives on the RouteBinding itself, and since the stale snapshot holds an Arc clone pointing to the same RouteBinding object, when release sets closed=true, it mutates the shared object that the stale reader also points to. So even with the old snapshot, the reader can recheck the closed flag on the Arc<RouteBinding> it's holding and see the mutation.

There's still a race window though: if the reader reads closed=false before the release's store operation completes, the reader will proceed to route into the flow while release is about to close it. The reader decides to deliver based on seeing closed=false, but then release closes the flow underneath.

For the dispatcher-push path, this is actually safe because admission gets set to Closing earlier in teardown (before flow.close and snapshot removal), so the dispatcher checks admission first and catches the Closing state with a route_closing error. For the module→client terminal path, reading closed=false just means the terminal forwards to the client and releases credit on a closing flow, which is benign.

The real concern is the memory ordering on the closed AtomicBool itself — if it's using Relaxed ordering, a reader holding the old Arc might not observe the closed=true store due to lack of happens-before guarantees. The ArcSwap provides Acquire/Release ordering on the snapshot pointer, but that doesn't extend to the closed flag on the old Arc that a stale reader is accessing. In practice this probably works out since the store happens under the write lock and the atomic load is fresh, but the spec doesn't clarify what ordering the AtomicBool uses.

The bigger picture though: there's still a window where closed reads as false before the route gets released, and that's not actually worse than today's RwLock approach — a reader can grab the read lock, clone the Arc, drop the lock, and then route into it, while a writer acquiring the lock afterward finds the reader already holding the old Arc. The admission gate being set to Closing does gate the dispatcher-push path, so the observable behavior aligns with what the doc claims.

But this hinges on two things: the closed flag needs explicit memory ordering semantics, and admission=Closing has to be set before flow.close in every teardown path, including the synchronous connection-close Drop. That sync path can't run the full async teardown dance, but it can at least synchronously set admission=Closing and closed=true. The question is whether the connection-close Drop actually sets admission=Closing on each route's dispatcher before releasing. cher push checks both admission and closed, but the §2 pseudocode only checks admission, creating an inconsistency about which gate actually protects the push. For the exactly-once guarantee with existing CAS, the v2 design adds a slots-gate alongside the in_flight guard—the slots-gate prevents a second terminal event from releasing by checking if the slot was already removed, which is the real fix for concurrent duplicates.

The CAS guard now prevents in_flight from going negative on over-release, and while the slots-gate is the primary protection, the CAS acts as defense-in-depth without conflict. But there's a potential leak risk: if a legitimate terminal finds its slot already gone (removed by something else), release returns false and the credit never gets returned. This could happen if CANCEL removed a slot for a request that was actually Delivered, though CANCEL should only remove Queued slots.

Tracing through the paths: CANCEL on Queued removes the slot, rollback during Claimed removes it without the module ever receiving the frame, send-Err removes it before delivery, and the terminal removes it on successful completion. So exactly one path removes each slot and releases the credit, which closes B8/R11 contingent on corr-uniqueness.

Now looking at Q4' — the credit-leak vector. The doc says to enforce at enqueue by rejecting duplicate correlations. The read-loop uses a HashMap insert that overwrites if a correlation already exists, which means if a correlation gets reused while in-flight, the old Slot (possibly Delivered with credit held) gets dropped silently. The terminal won't find it, so release stays false and credit leaks. The pseudocode doesn't show duplicate rejection yet.

This is a critical gap: the exactly-once accounting hinges on correlation uniqueness, but the normative pseudocode doesn't enforce it. Q4' flags it as an open question with a lean toward enforcement, but leaving it unresolved is a major issue — it needs to be a hard requirement, not optional.

Moving to the DoS and memory concerns from v1 #12, which the doc hasn't really addressed beyond Q3'.  The frame-count cap issue (256GiB DoS risk) is only raised as an open question about byte-based secondary caps, marked as a lean rather than normative. It wasn't in the B1-B10 set the doc claims to close, so it's technically out of scope, but it's still a live risk I should track.

Now looking at the Q1' through Q5' leans in §12. Q1' is about hard-gating merge-0 versus blocking-admission interim, with a lean toward hard-gate. That's safe only if merge-0 truly lands first and the daemon never emits those codes beforehand, but the dependency chain isn't fully spelled out.

The real problem is that verifying all consumers in broca/aft/alfonso-core have merge-0 is impossible from this checkout alone. The lean is defensible but actually unsafe—blocking-admission interim is the safer default because it preserves today's semantics without SDK dependencies. I'd mark Q1' as right-but-unsafe and prefer shipping the blocking-admission interim by default. The interim does reintroduce backpressure-by-wait, but it happens in the drain task off the read loop, which is less disruptive.

For Q2', the bounded-join timeout with abort-may-drop-terminal is acceptable when the connection is closing since the client will reconnect or timeout anyway. But if abort fires mid-send and leaves the module having received the request while the client never gets the terminal, that's an outcome-unknown scenario. The SDKs handle this through retries.

However, the 2-second bounded-join can't run synchronously during Drop, so Q2' doesn't actually apply on the connection-close path — I'm marking it as RIGHT-BUT-UNSAFE pending resolution of the async-teardown gap.

For Q3', adding a byte-based cap is the right call and should be normative, not optional. Q4' requires enforcing correlation uniqueness at enqueue, which is correct but the pseudocode in section 2 doesn't actually implement it yet — the lean is sound but under-integrated into the mechanism. Q5' is about whole-table Arc references.

Now I'm thinking through the rebuild strategy: rebuilding the entire snapshot inside the write lock on every mutation (bind, release, register, cleanup, drain) means high route churn serializes all mutations behind full-table rebuilds, which creates a scalability bottleneck. Since mutations are rare compared to lookups, steady-state is fine, but during churn storms like module restarts or reconnect floods, the write lock holds longer per operation — this is correct but conditional on benchmarking churn behavior.

There's also a potential lock-ordering inversion between the inbox and write-lock during teardown that I need to examine. reads them via inbox under the write lock, then we have inbox-then-write-lock ordering from snapshot rebuild, which inverts the drain/handle_closed/rollback pattern of write-lock-then-inbox. That's the deadlock risk — snapshot rebuild can't safely read per-route state from inbox if other paths hold write lock while waiting on inbox. There's a critical spec gap here: the lock hierarchy between the per-route inbox mutex and the global forwarding write lock is never explicitly defined, yet the snapshot rebuild path establishes a write→inbox ordering that could deadlock if any other code path tries to acquire inbox while holding the write lock. This needs to be specified and audited across all inbox-holders. I'm also noticing there are now two separate capacity mechanisms at play—the depth_cap check (queue length plus outstanding deliveries) at enqueue time, and the flow semaphore that bounds in-flight requests—and I need to verify they're not conflicting or creating unexpected bottlenecks. On rollback, in_flight gets incremented during acquire but outstanding never does, so they diverge temporarily before reconciling. The depth_cap check uses queue length plus outstanding rather than in_flight, which means a coroutine in the Claimed-acquiring window gets undercounted—but with a single drain task, this undercount is at most 1, so it's negligible. Now I'm verifying whether outstanding actually mirrors in_flight as documented.

The drain-side assertions might panic under legitimate races if they're used in debug assertions, though they're likely debug-only so the risk is low. Looking at the bigger picture, there's a major blocker: the sync drop on RouterConnection can't perform the async bounded-join teardown that section 5 requires—connection close is synchronous, but graceful join needs async cancel and join operations, so only fire-and-forget abort is possible, breaking the join-before-epoch-fenced-release ordering.

There's also a critical race in the drain pop and claim sequence: popping the correlation ID under one lock and then setting Claimed under a separate lock leaves a window where a CANCEL can see Queued, remove the slot, and then hit a missing key in the second critical section, causing a panic or losing the cancel with a credit leak. These need to happen atomically.

The pseudocode also doesn't enforce correlation ID uniqueness—reusing a corr value can overwrite a live Delivered slot and leak credits, which only gets addressed in an open question but needs to be normative.

On the rollback path, there's a double-release issue where the code calls `flow.release()` explicitly and then drops an uncommitted guard, relying on a fragile best-effort CAS guard that the comment itself says isn't a security boundary.

The notify wait during idle drain doesn't select against the cancel token, so it always times out after 2 seconds instead of waking cleanly on cancellation.

There's also an unspecified lock hierarchy between the inbox mutex and the forwarding write lock that needs clarification, especially during snapshot rebuild.

Client-side control offload can cause data on a freshly-opened channel to overtake the in-flight route.open call on the offloaded FIFO, whereas the current serial loop commits the bind first.

Finally, the closed AtomicBool has unspecified memory ordering, and there's an inconsistency between section 2 (which checks admission on push) and section 7 (which says push checks closed).

Now looking at the verdict-by-verdict breakdown: B1 correctly identifies the false claim and prescribes in-place retry for the data-plane retryable class, and the mechanism fits the managed-call loop, but I can't verify completion because the cross-repo consumers aren't accessible in this checkout—so it's closed in principle but unverifiable. B2 remains open because the pop/claim two-lock gap from finding #2 reopens the limbo window. ot slots in O(1) time instead of scanning. B4 covers the error handling paths—send failures trigger module release, channel closure is disambiguated, and RAII guards prevent panics—but there's still ambiguity at the shared mutator between a clean module shutdown and a crash, plus potential double-release issues between RAII and explicit cleanup. The structure is clearer than before, though the teardown semantics need tightening.

B5 confirms the insert-after-send race is resolved: delivered and outstanding counts are recorded under lock before the actual send, establishing a happens-before relationship that the terminal path respects. B6 eliminates the O(queue) CANCEL DoS by using a HashMap for O(1) lookups. B7 fixes the channel-0 FIFO reordering issue on the module side by keeping module connections inline rather than splitting them, so bind-ACKs stay ordered relative to module data.

B8 addresses the false invariants by correcting the documentation to honestly represent I3/I4/I7 as deltas and properly contextualize R11 as the concurrent-duplicate fix. B9 partially resolves the GOODBYE flush atomicity and teardown hang: the admission=Closing gate closes the try_push-after-flush hole for the async path, but the synchronous connection-close Drop path can't execute the async bounded-join/abort sequence.

B10 mostly closes the merge-1 snapshot neutrality issue through publish-under-lock and closed-flag rechecks, though it depends on proper memory ordering specs and consistency across release paths. Now I need to verify whether the terminal path can actually reach the dispatcher inbox—this is critical for B5 and B8. The module-to-client terminal path uses an Arc<RouteBinding>, so I need to check if v2's slots-gate can operate there.

RouteBinding would need to carry a reference to the RouteDispatcher, allowing the terminal path to lock the dispatcher's inbox. That's feasible and just an integration point. The key thing is that this terminal path runs inline on the module read loop, so the slots-gate lock gets acquired there.

Now I'm checking the contention: the module read loop takes the client-route's inbox lock, the client read loop's dispatcher push takes the same lock, and the drain task also contends for it. All three are O(1) operations with no awaits under the lock, so contention is bounded and there's no deadlock risk.

For B1's in-place retry, I need to see if it fits the existing managed-call retry loop. The current retry logic handles unknown_channel, not_sent, and outcome_unknown cases with different recovery strategies. Adding a route_backpressure code that triggers backoff and continues in-place would slot in naturally without requiring eviction or reconnection.

I've got what I need. Time to write the verdict: multiple unrefuted concurrency and spec defects across findings #1, #2, #3, and #6 make this a NO-GO per the gate rule, though the architecture shows promise and is closer than before.
I have gathered enough source evidence to render each verdict. Let me do two final confirmations: the connection-close teardown path (sync vs async) and whether the drain task's notify/cancel wiring exists anywhere in shipped code (it doesn't — it's design-only, so I'm verifying the lifecycle contract v2 must graft onto).

[assistant] 2026-07-15T08:35:51Z
I'm spotting a fundamental mismatch in B9: the connection cleanup happens synchronously in the `Drop` implementation, but v2's teardown strategy requires an async bounded-join with task abortion that can't be awaited from a synchronous context.

The release path for drain tasks runs under write locks in sync functions like `release_client_route_locked`, which means there's no place where the async join-and-abort operation can actually execute. This is a structural gap v2 doesn't resolve — the drain-task lifecycle has nowhere to live in the current synchronous cleanup architecture.

Now I'm tracing through the claimed-boundary race more carefully. The drain task sets a slot to Claimed, then awaits flow acquisition without holding the lock, creating a window where the cancel path can mark it as cancelled. If cancel arrives during that window, the drain task sees the cancelled flag at step C and rolls back, synthesizing a cancelled response so the module never receives the frame. This gives us one terminal outcome.

But there's another race I'm spotting: what if cancel arrives after step C marks the state as Delivered but before step D sends the frame to the module? The cancel path would see Delivered and forward the cancel to the module sink, but the drain task hasn't sent the Request yet. The module's cancel handler won't recognize the correlation ID since it's not in the in-flight map, so it no-ops. Then the drain task sends the Request and the module executes it uncancelled. This mirrors the B2/R5 defect but in the Delivered-but-not-yet-sent window.

The key question is ordering: both the cancel forward and the Request go through the same module_sink channel, which is FIFO. So whichever gets enqueued first will be processed first by the module.

The read loop's CANCEL forward happens at time T1 during cancel processing. The drain's Request send happens at step D, after the lock is released at step C. If CANCEL gets processed and forwarded to module_sink before drain reaches step D, then CANCEL arrives at the module first — but it's a no-op since the module doesn't know that correlation ID. Then Request arrives and runs uncancelled. That's cancel loss.

But wait — is module_sink using try_send or an async send? The read loop can't await, so it must be non-blocking. Let me check what v2 §2 actually says about how CANCEL gets forwarded to module_sink. So the real difference is that in the shipped version, the Request gets sent to the module before the read loop even processes the next frame containing CANCEL, guaranteeing Request arrives first. But in v2, the decoupling between marking state as Delivered and actually sending the Request creates a window where CANCEL can slip through to the module before Request does. If CANCEL arrives at the module first, it becomes a no-op since there's nothing in flight yet, and then Request arrives after — but by then the cancellation intent is lost.

The design's assumption that "Delivered means the module has the frame" is actually wrong; Delivered just means we're about to send it, not that it's already there. This creates a genuine race condition where CANCEL forwarding to the module can happen before the Request send completes, violating the ordering guarantee the original design relied on. The Request send happens outside the lock, so even if CANCEL is forwarded after Delivered is set, there's no guarantee the Request has actually been sent yet—the drain task and read loop run concurrently, and there's no happens-before relationship ensuring the Request reaches the module first. The design as written doesn't close this ordering gap.

Now looking at what happens when the send fails right after Delivered is set: if CANCEL arrives in that window, it sees Delivered and forwards to the module, but then the drain's error handler synthesizes a backend_error, creating a potential double-terminal scenario.

The trickier case is when the send succeeds after Delivered—the module gets the Request and will emit a terminal, but if CANCEL also arrives and gets forwarded to the module, we could end up with multiple terminals reaching the client.

However, the outcome depends on timing. If CANCEL reaches the module after the Request, the module cancels and emits a single terminal, which gets released cleanly. If CANCEL arrives before the Request, the module ignores it and the Request runs normally—so we lose the cancellation but don't get a double-terminal. The synthetic double-terminal risk from the earlier version is actually closed here because CANCEL in Delivered forwards rather than synthesizes. The real remaining gap is cancel-loss during the Delivered-pre-send window, not double-firing.

In the rollback path, when Claimed{cancelled:true} triggers after acquire, the module never received the frame yet (send hasn't executed), so there's no module terminal racing with the synthetic one—the synthetic cancelled is the only terminal. Rollback is clean because it happens before send. So B2 isn't fully closed; the double-terminal is prevented, but a cancel-loss window persists where Delivered-set-before-send and CANCEL-forward-to-module could reorder.

Now I'm questioning whether there's truly a happens-before between the drain task sending the Request and the read loop forwarding the CANCEL—these are independent tasks, and after step C unlocks with state=Delivered, either could proceed first.

The read loop processes the Request and CANCEL frames sequentially, acquiring the inbox lock each time. The drain task also needs the lock for its state transitions, so there's serialization on the lock itself. But the critical send operation in step D happens without holding the lock, which means the CANCEL could race ahead and reach the sink before the Request does.

Here's the problematic interleaving: the drain task marks the request as Delivered and releases the lock, then gets descheduled at the await point before sending. Meanwhile, the read loop acquires the lock, sees the Delivered state, and immediately forwards the CANCEL to the sink. By the time the drain resumes and sends the Request, the CANCEL is already queued first in the sink's FIFO. The module receives the CANCEL (which is a no-op since it doesn't know about X yet), then the Request arrives and runs uncancelled. The cancellation is lost.

The design forwards CANCEL directly to the sink rather than through the drain queue, so there's no ordering guarantee between them. If the CANCEL were instead enqueued into the same drain path after the Request, the ordering would be preserved.

Now I'm looking at the B9 sync-Drop issue. The shipped teardown in `RouterConnection::drop` is synchronous and calls cleanup functions under a write lock without any async context. But the v2 spec requires canceling the token and then bounded-joining the drain task with a timeout before aborting if it doesn't exit.

The problem is that a bounded-join needs `.await`, which can't happen in a sync `Drop`. The doc mentions the JoinHandle is owned by the teardown path, but the actual connection teardown that fires on peer-close is the sync `Drop` itself. For explicit GOODBYE messages, the handler runs in an async context so it could await properly, but the common case—client disconnect triggering socket EOF and the read loop returning PeerClosed—goes through the sync Drop, which can't execute the async teardown sequence.

This means either the drain tasks get orphaned on connection close, or they block indefinitely. The JoinHandles could theoretically be stored in the forwarding table and aborted via the sync `abort()` method, but that's not the graceful bounded-join the doc specifies—it's just killing the tasks. The gap between the documented teardown behavior and what the sync Drop can actually do is unresolved. Now I'm checking the per-route drain task lifecycle in the forwarding layer—they're not explicitly managed in the connection close path, which means forwarding's synchronous release is responsible for them. This reveals another lock-ordering issue: the teardown sequence acquires the RouteInbox mutex first (to set admission to Closing), then later needs the global forwarding write lock for the epoch-fenced release, creating a potential inversion point.

The shipped release path calls `flow.close()` under the write lock at line 1424, but the dispatcher's flow closure happens earlier in teardown before acquiring that lock, so there's a mismatch in when and where the flow is actually closed depending on which code path executes. So the issue is that `release_client_route_locked` acquires the write lock first, then needs the inbox lock to set the dispatcher's teardown field — that's a write→inbox ordering. But the teardown path locks the inbox first at step 1 (briefly, to set admission=Closing), then later at step 6 acquires the write lock for the actual release — inbox→write ordering. These opposite lock orderings between the two paths create a deadlock risk.

The drain task is the one I need to check next — it locks the inbox after acquiring something else, which might establish another problematic ordering. The module-side release path also calls flow.close() under the write lock, which would set the dispatcher teardown field while holding that lock — creating the opposite lock order from teardown's inbox-then-write pattern. This is a genuine inversion that the documentation doesn't fully address, and it highlights that the lock hierarchy needs to be explicitly specified across all paths, not just the orchestrated teardown scenario.

On the Notify side, there's a potential lost-wakeup issue with the drain loop: between when pop_front returns None and the subsequent notified().await, a push and notify_one could slip through. Tokio's Notify stores a permit even when no waiter is present, so the next notified().await would consume that permit immediately rather than blocking.

The key insight is that the drain pops one item per iteration, so even if multiple notify_one calls happen while the drain is waiting, it only gets one permit. But that's actually fine—the drain wakes up, pops the first item, loops back, pops the second item, and only then hits the None case and waits again. The queue drains completely with each wakeup cycle.

The real danger is the classic lost-wakeup pattern: checking if the queue is empty and then awaiting the notification without creating the Notified future first. Tokio's Notify handles this by storing a permit when notify_one is called before notified() is created, so as long as you follow the pattern correctly—creating the future before the check or relying on that stored-permit behavior—you avoid the race condition. The coalescing of multiple pushes into a single permit is safe because the drain loop re-locks and re-pops until the queue is empty, so all items get processed regardless. This brings the risk down to LOW. Now I'm looking at the uniqueness constraint for correlations in v2—it enforces this at enqueue time by rejecting duplicate correlation IDs as a protocol error.

But there's a critical bug: when a duplicate correlation arrives while the first is still in the slots map, the `HashMap::insert` call overwrites the old slot and drops it. If that old slot was in the Delivered state, its outstanding counter was already incremented, but now the slot is gone. When the terminal handler tries to clean up by removing the old correlation from slots, it finds the new slot instead and removes that, releasing credit for the wrong request. This causes credit accounting corruption and leaks the credit from the overwritten Delivered correlation. Q4' correctly identifies this flaw.

The document leans toward "enforce at enqueue" — which is the right approach — but the read-loop pseudocode in §2 doesn't actually implement the duplicate check. It just calls `slots.insert` without guarding against overwrites. So the lean is correct, but §2 doesn't reflect it; if implemented literally, §2 would silently overwrite and leak credit. This is a major defect that needs fixing.

Now looking at B5 and the insert-before-send ordering: v2 records the Delivered state and increments outstanding under lock at step C, before actually sending at step D. The send failure case is handled by the drain error path which cleans up the slot, so that's safe. If send succeeds, the module receives the frame and the terminal path will find Delivered already set. The critical insight is that no terminal can arrive before Delivered becomes visible since the module hasn't received the frame yet. So B5 is genuinely closed. But this same Delivered-before-send window that secures B5 is exactly what creates the cancel-loss vulnerability—the two issues are two sides of the same ordering constraint.

For R11 and B8, I'm examining how the v2 release mechanism works with the slots.remove gate. The first terminal to arrive will find the slot present and Delivered, remove it, and trigger the release through the CAS decrement. A duplicate terminal arriving later won't find the slot anymore, so it can't release again.

The v2 gate acts as the primary guard while the existing CAS check serves as a backup — they work together cleanly without conflict. But I'm now wondering if this double-guard could be masking a real leak: what if credit was acquired earlier but the slot isn't marked Delivered when the terminal arrives? Then release would be false and the credit wouldn't be returned.

Looking at the flow more carefully though — credit is incremented at step B, then Delivered is set at step C, and the module doesn't actually receive the frame until step D. So by the time a real terminal arrives, Delivered is always set, release is true, and there's no leak. The only cases where a slot gets removed in a non-Delivered state are duplicates that were already removed or rollbacks that handle their own release. So the double-guard isn't masking anything. ✓

But now I'm hitting an interaction with corr-uniqueness: if a duplicate correlation overwrites a Delivered slot, the terminal for the first request finds the slot in whatever state the second insert left it in, and the overwrite corrupts the whole flow.

That's actually a Q4' issue though, not B8 itself. With corr-uniqueness properly enforced, B8 is solid. I'll mark B8 as closed but note the dependency on Q4'.

Moving to B10 and the snapshot behavior — the v2 approach publishes under the write lock and sets closed=true under the write lock before publishing the snapshot. A stale reader sees Bound with closed=true and treats it as unknown_channel, which matches today's observable where post-release lookups return unknown_channel. The question is whether there's any ordering where closed reads as false while the route is already released, but closed is set true under the write lock before the snapshot removing it gets published, so that shouldn't be possible.

Now I'm thinking through the exact timing: a reader loads an old snapshot S that still contains the Bound route, then reads route.closed. The write-lock holder set closed=true and published S' (without the route) afterward. Since both the closed store and the snapshot publish happen under the write lock in that order, a reader holding the old snapshot S got it from a previous mutation when closed was still false. When they read route.closed through the Arc, they're reading the current state of that route object.

The critical race condition emerges here: a reader loads S with closed=false, then the release sets closed=true and closes the flow, but the reader has already passed the closed check and proceeds to use the route anyway—routing into a closed flow. This is the classic time-of-check-time-of-use vulnerability where the check happens before the release, but the use happens after.

For the module→client path specifically, after the closed check passes, it sends the frame through the client sink and then releases the flow if it's terminal. If the route gets released between that check and the actual flow.release call, the release path checks whether the semaphore is already closed before adding permits back—so it won't add permits to a closed semaphore, but it still decrements the in_flight counter regardless.

The dispatcher push from client→module has an additional safeguard: it re-checks the admission state under the inbox lock atomically with the push itself, so even if closed was read as false earlier, a Closing admission state will reject the push. But the module→client direction lacks this second atomic gate—just the initial closed check—which means a stale route could potentially deliver a terminal frame to the client sink after the route has been released, though that's just forwarding the terminal onward to the client.

For the request path specifically, the dispatcher push validates admission under the inbox lock, but the closed flag is checked separately beforehand. When a stale-Bound reader takes a snapshot and sees closed=false, it proceeds to the dispatcher push, which locks the inbox and checks admission again. If the route is being released, admission gets set to Closing at that point. The sync Drop path for connection close bypasses the admission gate entirely, so the closed flag becomes the only protection there. On a connection-close-released route, the request path needs to check that closed flag to prevent accessing a route that's already been torn down synchronously.

Now I'm examining what happens in the module-to-client path when it encounters a closed route — specifically how it handles a module's terminal message if the route is already marked closed.  The lookup returns Absent or EpochMismatch, so module frames get silently dropped rather than generating an error, which is slightly different from the "unknown_channel" semantic described in v2, but it's not a critical issue.

I should also check whether the TS error handling preserves the error code when reconstructing from a frame, since that affects whether B1/merge-0 and SDK feasibility are viable.

The error code is available in TS (the SubcError constructor captures it), and Rust's TerminalFrame::Error includes the code in the body. Swift is the constraint here—it only preserves the textual JSON from v1, so it'll need additional work to extract and use the code field. The in-place retry logic fits naturally into both the TS and Rust call loops since they already have retry mechanisms. B1 is correctly identified as a prerequisite, and the daemon-side gating (holding off on code emission until merge-0 is live) makes sense. The unverifiable piece is that broca, aft, and alfonso-core aren't in the current checkout, and v2 explicitly notes those need verification in their own repositories.

B1 is addressed as a sequenced prerequisite rather than fully closed by the daemon—the cross-repo consumer impact can't be verified here, so I'm flagging that per the instructions. For B3 and B6, the queue uses a VecDeque with a HashMap for O(1) slot lookups, all protected by the inbox Mutex. Both the read loop and drain operations lock only for O(1) work with no scanning. B3 and B6 are both closed.

On B6's denial-of-service angle: the depth cap rejects when full, and cancellations are O(1) via slots even though the read loop processes each cancel under the inbox lock. No linear scan happens. The memory-DoS issue from the earlier audit (finding #12, frame-count not bytes) is deferred to Q3 as an open question—it's not in the B1-B10 core scope, so it doesn't block this assessment. B9 maps to finding #9 (GOODBYE flush non-atomic plus teardown hang), and B10 to #10 (merge-1 snapshot not neutral). Findings #11 through #14 fall outside the B1-B10 set but represent real issues—v2 handles #11 in Q4' with lean enforcement, #13 and #14 remain unresolved or only partially addressed, so I should flag those as carryover concerns worth tracking.

The core problem with #13 is that v2's read-loop tries to synthesize terminal errors (route_closing, route_backpressure, cancelled) back to the client, but it can't await the egress send since it's on the read path—it has to use try_send instead, which means if the client's egress buffer is full, the synthetic error gets dropped and the client hangs with no terminal message. The drain task can synthesize backend errors because it runs off the read loop and can await egress, but the read-loop synthetic errors remain unspecified in v2's design.

Looking at the actual shipped code though, it seems the read loop does await egress.send for error frames (with a select on close_receiver to avoid indefinite blocking), so there's already a mechanism in place—but this reintroduces the head-of-line blocking risk that the try_send approach was meant to avoid.

The core tension remains unresolved: if v2 awaits synthetic errors on the read loop, a full client egress blocks the read loop; if it uses try_send instead, errors can silently vanish. Either way, this is a carryover of issue #13 that v2 doesn't address, and it's actually worsened by v2 synthesizing more error types in the read loop itself. I'll mark this as a major gap.

Now reconsidering the design choices: hard-gating the merge versus using blocking-admission as an interim safety valve. Hard-gating is cleaner and avoids emitting error codes that SDKs can't handle, which is the right call—but the real risk is whether consumers can bump fast enough. The blocking-admission interim is the zero-SDK-changes fallback, though the trade-off is that hard-gating is the safer choice if we can verify cross-repo readiness, which we can't fully do here.

For the bounded-join timeout with abort: dropping an in-flight terminal on a closing connection is acceptable since the client will observe the connection close and settle locally. But if the teardown is per-route rather than a full connection close, the connection stays open and the client has no signal that the request was dropped—it just hangs until its own timeout fires. So the "acceptable if connection is closing" assessment only holds if we're actually closing the connection, not just the route.

Let me reconsider the per-route GOODBYE case: the client does receive a GOODBYE frame that triggers failHandle, which settles all pending requests on that handle. So the client would actually settle those requests rather than hang. led by the byte-based cap, which directly addresses the memory DoS vulnerability from v1. Q3' is RIGHT.

Q4' requires enforcing correlation uniqueness at enqueue to prevent credit leaks — that's RIGHT in principle, but §2 doesn't actually implement it, so it's a RIGHT lean with an incomplete mechanism.

Q5' opts for whole-table Arc rebuilds per mutation under the write lock rather than per-shard rebuilds. This is conditionally RIGHT given v2's publish-under-lock approach and the agreement to benchmark first, but I'm now questioning whether the performance impact is acceptable under high route churn, since every bind, release, register, and cleanup operation triggers a full snapshot rebuild while holding the write lock.

I'm also reconsidering the teardown field staleness in B4. The race condition is between drain calling flow.acquire() and the mutator calling flow.close() to set teardown — I need to verify the ordering guarantees here.

If flow.close() wakes the acquire before teardown is actually set, the drain could read a stale None value and take the wrong error path. A Reloading teardown that races this way would emit backend_error instead of module_reloading, defeating retries on the client side — that's a real correctness issue.

The ordering constraint is strict: teardown must be set before flow.close() is called, and the drain needs to read it under the inbox lock with proper synchronization guarantees. The current test is too lenient (accepts multiple outcomes) and wouldn't catch a Reloading→backend_error misclassification, so this ordering hazard could slip through undetected.

The shipped code checks `endpoint_is_draining` dynamically at error time, but v2 switches to a stored teardown field that needs atomic coordination with close. The reload drain path is where the shipped close happens, and while I could dig deeper, the ordering concern itself is solid at medium confidence.

Now I'm stepping back to reconsider the bigger picture, including the client-side-only offload aspect in section 6. The issue is that client control frames (like route.open) and client data frames now run on separate tasks, so a data frame for a channel could be processed before the route.open completes and binds the route. In the original serial model, route.open finishes before the next data frame is even read, so the route is guaranteed to be bound. But with v2's offload, data arrives concurrently with route.open, potentially hitting an unbound route and getting dropped as unknown_channel.

The SDK mitigates this by having managed clients await route.open completion before sending data, but raw-wire or pipelined clients could still hit this hazard—it's the same class of problem as the module-side bind-ACK issue, just on the client side. N data frames are processed by the dispatcher concurrently, so there's genuine potential for reordering: the route.open might be queued in the FIFO task while data frames are already being dispatched. This is a real ordering violation that goes beyond what I initially flagged—the demuxing across two separate task queues means client-observable reordering is possible even with compliant SDKs if they're not carefully synchronized. The SDK mitigates this by awaiting the route.open acknowledgment before sending data, so pipelining only happens with non-compliant clients. Modules are the real concern since they pipeline bind-ack with immediate reverse requests, which is why v1's issue was Critical on the module side. Client-side risk is lower, so I'll rate this MAJOR-conditional but note that SDK usage makes it effectively MINOR in practice. I should flag it clearly given the adversarial threat model. Now I'm checking the AcquiredCredit RAII double-counting concern. standing tracks Delivered messages while in_flight tracks all acquired credits, so they measure different things intentionally. On rollback before reaching Delivered, the guard releases the credit without decrementing outstanding (which was never incremented). On terminal, both outstanding and in_flight decrement together. The AcquiredCredit guard is key here—committing it at the Delivered step disarms the guard so it won't double-release, letting the terminal path handle the final release instead. This RAII pattern ensures consistency across all paths.

The main implementation risk is ensuring the guard commits exactly when the message reaches Delivered, and that the error handler's manual release on send-fail is the only release for that path (not both guard and manual). If the guard weren't disarmed at commit, a send-fail followed by terminal cleanup could trigger two releases. As designed it's sound, but the implementation needs to be precise about when commit() fires.

For the send-fail case itself: if the frame never reaches the module (send returns Err because the channel closed), there's no terminal and only the error arm releases—no double-release risk. Even if the send partially succeeds (frame queued but send errors), the module is gone so no terminal fires either.

Now I'm wrapping up the analysis. The flush-versus-enqueue hole for client-to-module direction is closed by setting admission to Closing before snapshot removal in the GOODBYE teardown path, which blocks new dispatcher pushes. That works for orchestrated shutdown, but not for connection-close where the Drop doesn't run that step. The teardown-hang issue involves the bounded-join, abort, and cancel_token select logic in step 4.

The real problem: B9 handles blocked-drain-hangs through async orchestration that the sync Drop path can't execute. So B9 works for GOODBYE teardown but fails for connection-close—which is the primary teardown trigger. Since the shipped lifecycle is read-loop-exit followed by dropping the connection synchronously, and drain tasks would be spawned and joined somewhere in that flow, the sync Drop can't run the async 3-phase gate, leaving drain tasks potentially unmanaged on connection close. The core issue is that the async three-phase teardown is specified but the sync Drop trigger can't actually execute it—v2 doesn't resolve this fundamental mismatch. This is a major structural problem I need to flag clearly. Now I'm ready to write up the findings with precise file and line references. R/medium; client-side route.open→data reorder is a minor issue. I have one confirmed concurrency defect in B2 (cancel-loss during the Delivered-before-send window) that's un-refuted, which by the criteria means NO-GO. Though most of these blockers are addressable with spec clarifications, the instructions are clear: a single un-refuted concurrency defect is disqualifying. I could argue for GO-WITH-CHANGES if I enumerate the specific fixes needed, but given B2 is a genuine concurrency hole rather than just a documentation gap, I'm leaning toward NO-GO with the blockers listed.

Most of the issues are actually closed—B1, B3, B5, B6, B7, B8, B10 all have clear sequencing or mechanisms. The remaining problems (B2 residual, B9 connection-close, plus a few new defects) are real but narrow enough that I can frame this as NO-GO with a tight list of specific mechanisms that need respecification rather than a wholesale rejection. I'll write this up concisely with the enumerated blockers.
I have gathered sufficient source evidence. Let me verify one final detail on the drain-task/connection-close lifecycle interaction before finalizing — confirming the connection close path is synchronous.

[assistant] 2026-07-15T08:41:22Z
I'm synthesizing the key findings about how `ChannelFlow` manages its semaphore and in-flight counter. The `acquire` method doesn't hold the credit as RAII—it releases the semaphore permit immediately after incrementing the counter, while `release` uses a compare-and-swap loop to decrement safely, only proceeding when the counter reaches zero.

Looking at the request path: `handle_bound` acquires the credit, mutates the header, sends to the module sink, and releases on error. The module-to-client terminal path tries to send the frame first, then releases the credit only after a successful send—but critically, if client egress fails in the escalation path, the code returns early without releasing the credit at all.

I'm also noticing a major architectural issue: connection teardown is synchronous via `Drop`, which means it can't await the bounded-join and abort pattern that v2 wants for draining tasks. The teardown test itself is lenient, accepting either a Goodbye frame, a backend error, or nothing at all.

Now examining B1 around route_backpressure and the SDK behavior: v2 acknowledges v1 was correct and adds merge-0 as a prerequisite. Looking at the TypeScript implementation, when a data-plane Error with route_backpressure occurs, the SDK classifies it as an outcome_unknown, which triggers a reconnect-after-drop flow.

But there's a key distinction: the Error frame path goes through `pending.reject()` which creates a plain SubcError, not a SubcCallError. In the call handler, plain SubcErrors aren't retried—they're treated as terminal and re-thrown. So v1's finding holds, and merge-0 is indeed necessary to make these errors retryable by parsing the error code and classifying them appropriately.

The merge-0 approach is technically feasible since the error code is already extracted and accessible in both the TypeScript and Rust implementations. However, the design specifies this logic must live in the broca/aft/alfonso-core repositories, which I can't verify here. More critically, Swift has no data-plane retry loop at all, so implementing merge-0 there would require building that infrastructure from scratch—a much larger change than just adding codes to a classification set.

So B1 is closed as a design matter: the design honestly gates on merge-0 and specifies the mechanism. But the caveat is that the external consumer contract is unverifiable, Swift needs substantial new work, and the interim blocking-admission alternative reintroduces head-of-line waiting. I'm marking B1 as closed on the design and sequencing front, though Swift's work and the external repos remain unverifiable dependencies.

Now looking at B2 around CANCEL limbo...

I'm tracing through the per-coroutine state machine where all transitions happen under the inbox lock. The drain task pops a coroutine, sets its state to Claimed with cancelled=false, acquires flow outside the lock, then under lock again checks if it was cancelled or if admission closed—if so, it rolls back and synthesizes a cancelled response; otherwise it moves to Delivered and increments outstanding, then sends the frame outside the lock.

For the CANCEL read loop, I'm checking each possible state: if Queued, remove from queue and slots then synthesize cancelled (credit never acquired, so clean); if Claimed{cancelled}, just set the flag and return since drain will see it at step 4 and rollback; if Delivered or None, forward to the module which handles the terminal. The critical race is when CANCEL arrives between drain setting state=Claimed and drain re-acquiring the lock at step 4—the read loop sets cancelled=true, then drain takes the slot, sees the flag, and rolls back by releasing credit and synthesizing a cancelled response, ensuring exactly one terminal and proper cleanup.

Now I'm spotting a potential defect: if CANCEL arrives after state=Delivered but before the Request is actually sent to the module, the read loop forwards CANCEL to the module immediately, but the module hasn't received the Request yet so it treats the CANCEL as unknown and ignores it. Then drain sends the Request at step 5, and the module runs it uncancelled—this looks like the exact R5/B2 defect that should be closed.

But I need to verify the actual flow: both CANCEL and Request go through the same module_sink (an mpsc channel), so the order they arrive at the module depends on when each is sent. The CANCEL is sent by the read loop as soon as state=Delivered is observed, while the Request is sent by drain at step 5, so CANCEL could indeed arrive first. So this race is actually possible on a multi-threaded runtime: drain gets preempted right after releasing the lock, giving the read-loop thread a window to acquire it, see Delivered, and send CANCEL before drain's Request gets enqueued. That means CANCEL reaches the module first, which is a no-op since the correlation is unknown, and then the Request arrives and runs uncancelled — a genuine cancel-loss bug.

But now I'm questioning whether this is actually a defect or just how the design is supposed to work. The spec claims "Delivered means the module owns the terminal," but at the instant state flips to Delivered, the module hasn't actually received anything yet. gets CANCEL then Request in sequence. The module's handle_cancel does nothing since it doesn't recognize the correlation ID, so the Request proceeds normally and produces a terminal response. The client never synthesized a cancellation (the read loop forwarded CANCEL but didn't generate one), so only the module's response reaches the client—no double-fire, no zero-fire. The real problem is that cancellation becomes silently ineffective in this narrow window: the Request completes despite the CANCEL, which is the R5 defect (cancel-loss) reappearing, though only during the Delivered-pre-send interval. From the client's perspective, it sent CANCEL but received a full Response instead of a cancellation.

This violates the design's explicit claim that there's no limbo window and only three enumerated states. The Delivered-pre-send sub-window is an unenumerated limbo where cancel-ineffectiveness persists between Delivered-commit and send-landing. It's the same class of defect as R5 but much narrower—microseconds, only if CANCEL lands in that exact gap. I'm reconsidering whether this counts as a cleanly closed issue or a minor-to-major residual defect.

Actually, tracing through the cancel-during-Delivered-before-send scenario more carefully: the loop forwards CANCEL to the module without synthesizing, then drains the Request. The module receives both CANCEL and Request, runs the Request, and emits one terminal. That terminal flows back through the module→client path, and since slots.remove(corr) was already Delivered, release=true and the terminal gets forwarded. So there's only one terminal to the client—no double-fire, just cancel-loss. Still a defect against the design's claim, but not a double-terminal issue.

Now checking another sub-case: if CANCEL arrives while the Claimed state has cancelled=false, it sets cancelled=true. When drain reaches step 4 and sees cancelled=true, it rolls back by releasing and synthesizing a cancelled terminal—one terminal total. If the module_sink.send hasn't been called yet, the rollback just drops it with no module involvement, producing one synthetic terminal. There's also a boundary case where drain reads Claimed{cancelled:false} and transitions to Delivered before CANCEL arrives, but that's the cancel-loss window we already covered.

For the Delivered-before-send case, there's still a residual window where CANCEL can't prevent the Request from reaching the module—the limbo shifted from "queue vs outstanding" to "Delivered-state-set vs send-landed." This is a MAJOR defect that contradicts the design's claim of having no limbo window. But steelmanning the design: once state=Delivered and both CANCEL and Request go to the same module_sink, the drain's send might already be the next operation in line.

However, there's no ordering guarantee here. The design would need to ensure the Request enqueues to module_sink BEFORE flipping state to Delivered, so any subsequent CANCEL-forward comes after in the sink. But that directly conflicts with B5's fix (recording Delivered under lock BEFORE send for credit). There's genuine tension between B5 and B2—v2 chose Delivered-before-send, which reopens the cancel-ordering hole. This is a real, unrefuted concurrency defect that makes the design a no-go.

One alternative: route CANCEL-forward through the drain task instead of having the read loop forward it directly to module_sink. That would order it after the pending send. But the current design forwards CANCEL directly, so the race persists.

For the "None" case—either the corr was already delivered and terminated (slot removed), or it never existed (spurious CANCEL)—forwarding to the module results in a no-op either way, which is harmless.

Now looking at B3: the queue data structure. Version 2 explicitly specifies `VecDeque<u64>` for the queue and `HashMap<u64, Slot>` for slots, both behind a `Mutex<RouteInbox>` with `Notify`. This is the recommended fix—no mpsc, no scan. The primitive is named, slot lookup is O(1), and a single mutex serializes everything. High confidence this is closed.

But I'm checking whether every operation claimed to be O(1) under the lock actually is: push_back, pop_front, and slots insert/get_mut/remove are all O(1)...

except the CANCEL-on-Queued operation. To remove a specific correlation ID from the middle of a `VecDeque<u64>` requires a linear scan or retain, which is O(n). The design doesn't specify whether they use a tombstone approach to avoid this, so there's a hidden O(n) scan lurking in the queue removal that reintroduces the B6 problem.

The standard fix would be to mark the slot as cancelled and leave the correlation ID as a tombstone in the queue, skipping it during drain. But the documentation says "remove from queue+slots" literally, which if implemented directly is O(n) while holding the serialization lock. I'll flag this as a documentation precision issue with potential performance implications.

The O(n) risk is bounded by the depth cap though — `max(4, 2×window)` — so for StatelessParallel it's not as severe as the full 2048 depth. Let me verify what the actual window size is and whether the O(n) removal on the VecDeque becomes a real bottleneck.

Now I'm looking at the drain-task error handling paths: the send-failure arm releases and reports a backend error, the acquire-closed arm disambiguates using the teardown field to determine if it's a reload, goodbye, connection close, or backend error, and there's a panic guard via the AcquiredCredit RAII. I should check this against the test case for blocked flow control.

The test `blocked_flow_control_acquire_wakes_when_module_tears_down` is lenient—it accepts Goodbye, backend_error, or nothing (timeout → None → passes). So v2's "None → backend_error" arm satisfies it, and even a silent drop would pass since the test only asserts if a frame is present. The mechanism v2 uses to disambiguate ChannelFlowClosed is the right approach, though the test itself is loose enough that it barely constrains the behavior.

The real question is how the drain distinguishes module death from a reload. v2 maps module-death to the `None` teardown arm, which becomes a backend_error, but the shipped teardown calls `route.flow.close()` in both `release_client_route_locked` and `release_module_route_locked`.

The design requires whoever closes the flow to set the `teardown` reason under the inbox lock before or atomically with calling `flow.close()`. This creates a lock-ordering problem: `flow.close()` happens under the forwarding write lock, but setting `teardown` needs the inbox lock, so the mutator must acquire them in the right order.

I'm checking whether the drain task and read loop respect this ordering. The read loop acquires the forwarding read lock (then releases it) before taking the inbox lock, which is safe. But I need to verify there's no path that takes the inbox lock first and then tries to acquire the forwarding write lock, which would deadlock. The teardown field could be stale if a flow closure path forgets to set it, leading to the wrong error handling branch. All three sites where flow.close() is called need to explicitly set the teardown kind, otherwise a missing teardown defaults to backend_error defensively. This is an integration risk that depends on discipline across multiple callsites.

There's also a potential double-counting issue with the AcquiredCredit RAII guard and the outstanding counter. When committing to Delivered, the guard is marked as committed (preventing release), outstanding is incremented, and the state changes. Later the terminal path decrements outstanding and releases the credit, so the lifecycle is correct. But on rollback with cancelled:true, the guard isn't committed, so it will release on drop.

However, the design spec also explicitly calls flow.release() in the rollback arm itself—that's a double release. Let me check the exact flow: the rollback path unlocks, calls flow.release(), synthesizes an Error, and drops. Meanwhile the AcquiredCredit guard also calls flow.release() when it drops. That's releasing the same credit twice, which is the bug.

The shipped release() has a CAS guard checking in_flight != 0, so the second release might decrement a different coroutine's credit if other requests are in flight, or hit the in_flight == 0 guard and warn. This is exactly the double-guard masking a real leak that the prompt flagged—the rollback path double-releases, and if in_flight > 0 from other coroutines, the CAS happily steals a credit from another in-flight request.

When you double-release on rollback, you're releasing 2 permits for 1 acquire, so in_flight drops by 2 and the semaphore gains 2 permits when only 1 was legitimately cancelled. This inflates the effective window by 1 extra permit, weakening backpressure. Depending on the interleaving, the in_flight == 0 guard might swallow the second release instead, but either way the accounting gets corrupted.

The core issue is that v2's RAII guard and the explicit flow.release() both fire on rollback—one should disarm the other. The same double-release problem exists in the send-failure path too, where outstanding gets decremented, the slot removed, and flow.release() called while the guard is still armed. Path B has the real problem: when rollback happens before Delivered, the guard is still armed, so the spec's explicit release plus the guard drop both trigger, causing a double release. Path C avoids this only if the guard gets disarmed at Delivered, but the spec doesn't explicitly state that. Now looking at another issue—there's a race condition where outstanding gets incremented after send completes, which could leak credits if a terminal arrives between the send and the lock acquisition. The terminal path's lock ordering checks out: it removes the slot under the inbox lock, then forwards the terminal, then releases if needed. The current shipped code tries to send first and releases after, but the v2 version should insert the slots.remove operation earlier in the sequence to maintain proper synchronization.

However, there's a potential credit leak if the try_send fails during client egress — the slot gets removed and outstanding decremented under lock, but if the send fails and we return early without calling flow.release(), the credit never gets released even though the slot is gone. That said, when client egress fails, the connection tears down anyway, so the whole route and flow are being closed regardless. B5 is closed for the fast-terminal case, though there's a minor inconsistency around egress-failure ordering that I should flag. For B6, the O(queue) CANCEL scan DoS is addressed by the HashMap giving O(1) slot lookup, but the actual queue removal from VecDeque is still O(n) unless we use tombstones to skip absent entries during pops—the doc's phrasing about "remove from queue+slots" is ambiguous on whether that's truly O(1) or not.

I'm rating B6 as closed-for-lookup with a flagged residual O(n) in queue-removal that needs tombstoning, not a full reopening since it's bounded by depth_cap and happens under mutex contention rather than read-loop starvation, but it warrants a GO-WITH-CHANGES note. There's also a new DoS vector I'm noticing: an attacker spraying CANCELs for non-existent correlation IDs would cause each one to be forwarded to the module sink, since the lookup returns None and we pass it through anyway.

Actually, checking the current behavior—CANCEL frames are already forwarded to the module in handle_bound since they're non-Request frames with no acquired credit, so this amplification is pre-existing, not introduced in v2. That means B6's CANCEL amplification isn't a new vulnerability.

Now looking at B7, which concerns the whole channel-0 FIFO breaking bind-ACK ordering in the context of v2's client-side-only offload where module connections aren't split.

The bind-ack stays inline on the module read path, and the test that drives a MODULE-connection frame through the router confirms the inline path is unchanged since module connections aren't offloaded—so that test stays green. The v1 concern about the whole FIFO was correctly narrowed to client-side-only, which gives me high confidence here.

The real question is whether the client-side route.open FIFO can reorder anything observable. When a client sends route.open on channel 0 followed by data frames on the new route, the route.open goes to the control task while the subsequent data frame goes to the data path, which could create an ordering issue.

But this isn't actually new—the SDK already single-flights route.open and requires clients to await the ack before sending data on that route. The offload just means route.open (which can be slow) no longer blocks the client's other routes' data and cancels. For compliant clients, there's no new reordering within the same route since they still await the ack. For raw-wire clients that pipeline data immediately after route.open without waiting, today's serial loop would process route.open (committing the bind) before reading the data frame, so the data gets delivered. Under offload, the route.open goes to the control task while the data frame might arrive at the dispatcher before the bind completes, potentially hitting unknown_channel. Version 2's offload approach introduces a real reordering problem: the read loop no longer blocks on route.open, so it immediately reads and dispatches pipelined data frames before the route is bound, causing them to be dropped as unknown_channel — a defect that wouldn't occur today since the serial loop blocks until route.open completes and the route is committed. The design's claim about preserving per-client FIFO ordering for route.open→route.close doesn't account for the route.open→data ordering within the same route.

open call, since the control task and data path run concurrently. SDKs mitigate this by single-flighting route.open (awaiting the ack before sending data), but raw-wire clients pipelining data immediately after route.open would silently lose frames — a client-side mirror of the v1 #7 module-side binding hazard that was treated as a blocker.

Wait, actually I need to think through this more carefully. A compliant client can't pipeline data before the route.open ack anyway, since the route handle itself comes in that ack. So the hazard only applies to non-compliant raw-wire clients that somehow know the route handle in advance or guess it. That's a narrower threat model than I initially thought.

But there's still the question of whether route.close on the same route could be sent before route.open completes — both go through the control channel and should be ordered by the per-connection FIFO, so that ordering is preserved. CLOSED — the route identity is established by the ack, so the ordering claim holds even though the doc doesn't explicitly spell that out. Moving on to B8: the I3/I7 invariant claims have been reworded as intentional deltas matching the v1 fix direction, with R11 correctly framed as a concurrent-duplicate case rather than a broad "trusted vs enforced" distinction. That's CLOSED with high confidence. Now looking at B9, which covers the GOODBYE flush versus enqueue non-atomicity and potential teardown hang, using a 3-phase approach in v2 §5.

The key is checking this against the actual shipped teardown path, which is synchronous — the connection cleanup happens directly in Drop via router.rs, and GOODBYE handling is also synchronous in the control path. So v2's teardown strategy needs to set admission to Closing before removing the snapshot, use a cancel token, perform a bounded join, then abort if needed, with the JoinHandle owned by the teardown path rather than bound elsewhere.

But here's the problem: a bounded join requires awaiting, and you can't await inside a synchronous Drop function. The shipped teardown path is entirely synchronous (RouterConnection::drop and cleanup_connection), so converting this to support the async bounded-join would require restructuring the entire teardown flow from sync to async — a fundamental architectural change the design doesn't actually address. The design assumes the JoinHandle lives in the teardown path, but that path is currently synchronous and can't await anything. This is a critical design gap: the async teardown sequence with bounded-join and abort fundamentally conflicts with the synchronous Drop implementation that holds the write lock, and there's no specified migration path. On the connection-close side, while the drain task is shielded from blocking via select! against the cancel token, the new drain lifecycle itself needs to be owned and properly joined or aborted somewhere—likely by the forwarding table—but that ownership and cleanup semantics aren't defined. Drop could call cancel_token.cancel() and handle.abort() (both non-blocking) to avoid the hang, but that risks dropping an in-flight terminal. The real problem is that the graceful GOODBYE path also can't await the bounded-join—there's no async context anywhere in the shipped call graph to actually place that 2-second drain task join that the design calls for.

The admission=Closing gate does close the try_push hole correctly, and step 1 (setting admission=Closing under the inbox lock) does happen-before step 6 (snapshot removal), so a stale-snapshot reader should be blocked. But the task-lifecycle and bounded-join requirements remain unreconciled with the synchronous teardown implementation—this is a major gap that's not cleanly resolved. task-lifecycle and async bounded-join abort handling remain unspecified against the sync teardown, which is a significant gap. Now I'm looking at B10's approach: snapshot gets rebuilt and published inside the write-lock critical section, with a closed AtomicBool flag set under that same lock before the snapshot is published. Readers check this closed flag and treat a closed-but-visible route as unknown_channel to match today's behavior. The ordering is: under write lock, set closed=true, then publish the snapshot without the route, so a reader either sees the old snapshot with the route or the new one without it.

The key insight is that when a reader loads an old snapshot, they get an Arc to the RouteBinding from that snapshot, and since closed is an AtomicBool on the shared allocation, setting closed=true mutates what everyone holding that Arc can see—regardless of which snapshot they loaded. The closed flag is set before the new snapshot publishes, so readers with the old snapshot's Arc will observe closed=true and treat it as unknown_channel. This handles the race correctly. Now I'm checking whether there's an ordering where closed reads as false but the route is already released... closes the flow with a teardown flag set, so when the dispatcher's handle_closed runs, it maps that teardown state and synthesizes an error rather than silently dropping the message. For the module-to-client path, a stale read of closed=false followed by release means the forwarded terminal goes to a client that's already been torn down.

The v2 design substantially closes the B10 gap by making stale-Bound reads return unknown_channel once closed=true, restoring today's observable behavior. There's still a narrow pre-existing TOCTOU window (reading closed=false before the release happens), but that's not new—it's the same race condition that exists today, just narrower now. I'm confident this is acceptable.

One thing I'm noticing though: every mutation (bind, release, register, cleanup, drain) publishes a snapshot inside the write-lock critical section, which means the entire table gets rebuilt and swapped on every single operation.

For systems with high route churn, this becomes O(routes) work per mutation while holding the write lock, which could extend lock hold times. Since the data-plane reads have moved off the RwLock entirely to ArcSwap, the write lock only contends with control-plane reads and other writes, so it's probably fine for typical workloads where mutations are rare. Still worth flagging as a potential performance concern to monitor.

Now looking at the open questions: for Q1', the choice between a hard-gate on merge-0 versus blocking-admission interim—I'm leaning toward hard-gate since it's cleaner architecturally, though it does create a hard cross-repo dependency on broca/aft/alfonso.

But there's a tension in the blocking-admission design I need to untangle. The proposal claims blocking admission happens "drain-side, off the read loop" to avoid head-of-line blocking, but admission decisions actually happen in the read loop when deciding whether to push into the queue. If the read loop blocks waiting for queue space, that reintroduces HOL on the connection—which defeats the purpose. The drain task drains the queue; it doesn't admit new entries. So either the design needs to clarify where admission actually blocks, or blocking-admission isn't the right interim solution.

On Q2, the bounded-join timeout with abort dropping in-flight terminals seems acceptable if the connection is closing anyway—the client will settle on close or GOODBYE. Though I should flag the credit-leak risk: if abort kills the drain mid-send after Delivered was already committed, we could leak credits. Enforcing corr-uniqueness is mandatory, not optional—it's critical to prevent the slots HashMap from leaking overwritten correlations' credits. For the whole-table Arc rebuild approach, measuring performance under write-lock contention in T9 is essential before accepting it, since route churn could bloat lock hold times; the design is sound but conditional on that benchmark. Now I'm hunting for new defects...

There's a rollback double-release issue where explicit flow.release() and the AcquiredCredit RAII drop both fire—that's a major bug. There's also a delivered-before-send cancel-ordering hole: when CANCEL arrives for a Delivered request, it forwards to module_sink before the Request is sent, so the module runs uncancelled and reintroduces R5 narrowly—this is a blocker tension between B5 and B2. And there's a teardown async lifecycle mismatch where the bounded-join await has no async home while release runs under the write lock and can't await, leaving §5 under-specified.

Now looking at several more issues: the O(n) VecDeque removal on CANCEL-Queued happens under the mutex and needs tombstoning to avoid O(depth) serialization lock contention. The lock hierarchy is never documented—forwarding-write feeds into inbox for teardown-field and admission, while inbox-only handles read-loop and drain, creating inversion risk. The teardown-field must be set at every flow.close() site but several shipped close() sites skip it, risking silent drop versus backend_error. And there's a client-egress-failure terminal ordering issue in §3 that needs attention.

For the drain notify pattern, there's a potential lost-wakeup if the queue pop returns None, then a push+notify_one fires before drain calls notify.wait()—but tokio's Notify stores a permit, so notify_one before wait returns immediately, making it safe as long as the Notified future is created before checking the queue. — the Notified future is created before the queue check, the permit gets stored regardless, so the wakeup is safe. The key discipline is ensuring the re-check loop runs after every wait, which tokio Notify's semantics support naturally. I'll mark this as correct with a minor caveat about implementation details.

Now I'm looking at the outstanding versus flow.in_flight double-counting issue. Both track the same thing — requests in flight — but they're maintained separately in RouteInbox and the flow object. If they diverge during rollback or double-release scenarios, the depth_cap check could use stale outstanding values while the actual in_flight counter has moved on, creating a mismatch between what the admission logic thinks is in flight and what actually is.

I need to check the STATELESS_PARALLEL_WINDOW constant to understand how severe the O(n) queue removal cost becomes, and then I can finalize my verdict on whether there's a genuine concurrency defect here that violates the contract.

Given the gate rule that a single unrefuted concurrency or contract defect is a NO-GO, I've identified two solid ones: the rollback double-release and the Delivered-before-send cancel-ordering hole, plus the teardown async lifecycle gap. So this is a NO-GO, though much narrower than v1 since most of the original blockers are actually resolved. The remaining issues are the cancel-ordering residual, the rollback double-release with teardown field discipline, and the async teardown lifecycle problem.

Let me dig deeper into the Delivered-before-send cancel-ordering scenario because it's the strongest new finding. The design claims three states with no limbo, setting State=Delivered under lock at step 4, then releasing the lock before step 5 sends the frame. If a CANCEL arrives in that window, the read loop locks the inbox, finds Delivered, unlocks, and forwards the CANCEL to module_sink—but module_sink might not have received the frame yet from step 5's send, creating a race where the cancel could arrive before the delivery.

On a multi-threaded runtime, this is actually reachable. The drain task can be preempted between step 4's unlock and step 5's send, giving the read loop (running on a different task or thread) time to acquire the lock, read the Delivered state, unlock, and call module_sink.send with the CANCEL. If module_sink is an mpsc channel, the CANCEL can land in the queue before the Request does, meaning the module receives a cancel for an unknown correlation ID first, then the actual request—and runs it uncancelled.

The root issue is that the design explicitly sends the Request to module_sink outside the lock to avoid blocking awaits while holding it, but this means the CANCEL-forward can interleave and reach the channel first. To fix this, the Request would need to be enqueued while still holding the inbox lock, preventing any CANCEL-forward from cutting in, but that violates the stated design principle. Alternatively, we could defer marking Delivered until after the send completes, though that risks reopening the credit leak from scenario B5, or route the CANCEL through the drain itself to maintain ordering.

The design chose none of these paths, leaving a genuine ordering hole between Delivered and the actual send. This is the same structural tension that v1 identified—whether to insert the credit before or after the send—and v2 resolved it in favor of credit at the cost of reopening the cancel-ordering problem. I'm confident this is a legitimate blocker.

Now I'm checking whether the shipped module can even pre-arm cancellation by looking up in-flight requests by channel, epoch, and correlation ID.  The module only inserts its in-flight entry when it receives the Request, so a CANCEL arriving before the Request is simply a no-op—the Request runs uncancelled. This confirms the same behavior v1 found.

I need to finalize my verdict now. The prompt states that a single un-refuted concurrency or contract defect is a NO-GO, and I have the Delivered-pre-send cancel-ordering issue that remains unrefuted.  I'll render this as NO-GO with a tightly enumerated blocker list, while acknowledging that v2 genuinely closed most of v1's issues—this respects the instruction not to flatten disagreement. Let me also quickly check the window constants to quantify the depth and the VecDeque O(n) concern, which would strengthen the case for the residual blockers.
I have sufficient source evidence. Let me confirm one quantitative detail (window/depth constants) to calibrate the VecDeque-removal concern.

[assistant] 2026-07-15T08:46:36Z
I've gathered enough evidence to synthesize my findings. Now I'm working through each of the blockers B1 through B10 to identify any new defects. The critical issue I've confirmed is in `ChannelFlow::acquire` where the semaphore acquisition doesn't maintain RAII-held credit—it calls `sem.acquire().await`, increments the in-flight counter, then forgets the permit, leaving the credit unprotected. I'm examining the corresponding `release()` implementation to understand the full picture of this synchronization problem. The teardown test is lenient — it accepts a Goodbye frame, backend_error, or nothing at all, so it only fails if a frame arrives that's neither Goodbye nor backend_error. On the SDK side, terminal Error frames go through errorFromFrame to extract the error code, then get rejected as SubcError, which the call() method catches and re-throws as a terminal error rather than retrying.

Now I'm evaluating the blockers. B1 claims that v2 acknowledges v1's "zero SDK changes" was incorrect and adds merge-0 as a prerequisite, with the classification verified against the source code — terminal errors in both TS and Rust don't retry, they return or throw directly.

The proposed fix of adding a retryable classification that triggers in-place retry with backoff rather than reconnection fits the existing loop structure in both languages. However, the contract impact on downstream services like broca, aft, and alfonso-core can't be verified since they're not in the current checkout — v2 is transparent about this limitation. The design decision itself is sound: merge-0 as a prerequisite gate, with the mechanism fitting naturally into the managed-call loop, so B1 is closed at the design level with that caveat noted.

Now I'm examining B2, which centers on the CANCEL state machine and its limbo transitions. The state flow moves from Queued through Claimed{cancelled} to Delivered, all under the inbox lock, and CANCEL operates under that same lock. Tracing through the read loop: if the call is still Queued, CANCEL removes it from the queue and slots, synthesizing a cancelled response without ever acquiring credit; if it's Claimed{cancelled}, CANCEL sets the flag and returns, letting the drain logic roll back after the acquire; if it's already Delivered or missing, the request forwards to the cancellation handler.

The drain task orchestrates the actual delivery: it pops a correlation ID under lock, sets the state to Claimed{cancelled:false}, then blocks on flow.acquire(). Once acquired, it checks whether the state was marked cancelled or admission closed—if so, it releases the flow and synthesizes a cancelled response; otherwise it marks Delivered and increments outstanding. The frame then sends downstream.

Now I'm tracing what happens when CANCEL arrives at different points in this sequence. If CANCEL hits while the call is still Queued, the read loop removes it from the queue before drain even pops the correlation ID, so the drain will later find nothing to pop.

But there's a critical window: drain pops the correlation ID from the queue under lock, then releases that lock before acquiring it again to set the Claimed state. Between those two lock acquisitions, CANCEL could arrive and modify the slot state. ates a window where CANCEL removes the slot between pop and Claimed-set. The real question is whether drain handles a missing slot gracefully or if it's undefined behavior — if CANCEL removes the slot after pop but before the Claimed-set, then drain acquires credit for a corr with no slot and tries to take it, which could either treat None as "proceed to Delivered" and insert a new slot, or fail entirely depending on how the pseudocode handles the missing case.

The core issue is that pop and Claimed-set happen in two separate lock acquisitions, leaving a window where CANCEL can fire the Queued arm and remove the slot entirely. This reintroduces the "Queued but popped" limbo state that the v2 mechanism was supposed to close. The fix is straightforward — atomically pop and set-Claimed in a single lock section — but as written this is a genuine gap in the design. The fix is straightforward—merge those two lock sections into one—but since this is a normative spec, I need to flag it as a major issue. The document explicitly separates the pop operation from the Claimed state-set into different lock scopes, which creates that limbo window. Under strict reading, B2 isn't fully closed as written, even though the principle is sound. The drain pseudocode needs to be tightened to eliminate the race entirely.

Now I'm moving on to analyze the double-fire and vanish scenarios at other boundaries for B2.

For the CANCEL-during-drain case: when CANCEL arrives while the drain is blocked waiting for credit in flow.acquire(), the read loop sets cancelled=true, the drain wakes up, re-locks, and sees the cancelled flag before forwarding — it releases the credit and synthesizes a cancelled response. That's exactly one terminal event with no double-fire or vanish, so this boundary holds up correctly.

For the CANCEL-after-Delivered case: once the drain has marked the request as Delivered (meaning the send decision is made), the read loop forwards the CANCEL directly to the module since it's already in flight or sent.

Now I'm examining the race condition at the Delivered boundary — specifically when CANCEL arrives after the state is set to Delivered under lock but before the drain actually executes the send call. The read loop will forward CANCEL to the module_sink (an mpsc channel), creating a potential race where CANCEL and the Request could arrive at the module in either order.

If CANCEL reaches the module first, it becomes a no-op since the module hasn't seen the Request yet, and then the Request arrives uncancelled. This mirrors the exact defect from v1 where CANCEL loses its race against the Request on the module_sink. The issue is that marking state as Delivered under lock, then unlocking before the actual send creates a race window where a concurrent CANCEL can be forwarded to module_sink before the Request frame itself arrives, causing the CANCEL to be lost. This is different from the current shipped behavior where the Request is always sent to the sink before the read loop processes the next frame, so there's no reordering opportunity.

The root cause is that tokio's mpsc orders sends by completion time, not call order — if the CANCEL send completes before the Request send (both tasks are concurrent producers), the CANCEL gets enqueued first. So the fix needs to make the Delivered state commit and module_sink.send atomic with respect to CANCEL, or handle CANCEL for Delivered correlations differently.

One approach is to defer marking Delivered until after the send completes and use the Claimed{cancelled} flag to track CANCEL arrivals during the send window, but that reintroduces the insert-after-send problem. I'm trying to figure out if there's a way to truly prevent CANCEL from overtaking the Request in the channel.

The real issue is that both the drain task sending the Request and the read loop sending the CANCEL are racing through the same bounded mpsc channel — whichever send() future completes first wins, so CANCEL can arrive at the module before the Request does. If that happens, the module's no-op on an unknown correlation ID means the request runs uncancelled. The design assumes once Delivered, the module will receive the request and handle any forwarded CANCEL, but that assumption breaks if CANCEL arrives first.

Let me double-check the code flow — Delivered is set before the send() call completes, not after. So there's a real window where the state claims the module owns the terminal but the request hasn't actually landed yet. This is a blocker-level defect.

The design tried to solve one problem (B5, credit leak from fast terminal) by marking Delivered early, but that move directly creates another problem (B2, cancel overtaking the request). The mitigation assumes the module will see Request then CANCEL in order, but that only works if the Request is already enqueued when CANCEL arrives — and at Delivered-commit time, it isn't. I need to check whether the drain and read loop guarantee ordering on the sink itself.

Looking at the actual implementation, they don't — different tasks are involved. The read loop calls module_sink.send directly without going through any serialization that would guarantee FIFO ordering with the initial Request send. So the CANCEL can actually overtake the Request in that window between Delivered-commit and when send executes, which means the cancel gets lost anyway. This is the same R5 defect, just transposed to a different part of the flow.

Let me verify the actual path: CANCEL arrives from the client on the client→module route and gets forwarded to module_sink, which is the same sink where drain sends the Request. Two producers on the same sink with no ordering guarantee between them — that's the race condition. So B2 is confirmed as a blocker.

Now looking at the NOT-CLOSED boundary and the pop→Claimed gap more carefully. Even if the design intended pop_front to leave the slot defensively with Claimed-set as a safeguard, the CANCEL Queued-arm still removes from slots during that window. After pop_front returns the correlation, if CANCEL arrives before the drain re-locks, it sees the slot still Queued and removes it from both queue and slots, synthesizing a cancelled response. But the correlation was already popped, so it's no longer in the queue — the removal happens on a stale slot reference.

Then the drain tries to access slots[corr] to set Claimed, which in Rust would panic on a missing HashMap key, or if using get_mut and ignoring None, the flow.acquire() runs and credit is acquired, but take Slot returns None and the rollback-or-proceed path becomes undefined. If it proceeds to insert Delivered and send anyway, the module executes a request the client was already told was cancelled, and the credit accounting becomes inconsistent. This pop→Claimed gap is the same fundamental issue as the Delivered/send boundary problem — both arise from multiple lock sections with awaits in between.

The fix is straightforward: combine pop and Claimed into a single atomic lock section, so it's a spec bug that needs correction. I'll merge this into the B2 finding as a required change.

For B3, v1 complained about mpsc-scan incompatibility, but v2 resolves this by wrapping VecDeque<u64> and HashMap<corr,Slot> behind a single Mutex, creating an explicit synchronized structure instead of relying on mpsc. That closes the issue, though there's a caveat around VecDeque removal by correlation ID when handling CANCEL on Queued items.

Removing a specific correlation from the VecDeque is O(n) since it requires scanning to find the index then shifting elements, even though the slots HashMap provides O(1) lookup. The CANCEL Queued arm has to do this removal under the lock, so while it's not scanning the entire queue per frame like the read loop would, an adversary repeatedly canceling front-most requests could still force O(n) behavior and violate B6's "O(1) CANCEL, no scan" claim.

A better approach would be lazy tombstoning—marking a slot as cancelled and skipping it during pop—rather than actually removing from the deque. This way the O(n) cost doesn't get pushed into the locked CANCEL critical section, which currently blocks both the drain and any concurrent read-loop push. Now I'm checking how the drain error handling and AcquiredCredit RAII guard work in v2 against the actual test. The test is permissive—it accepts Goodbye, backend_error, or nothing—and v2 maps module teardown to None→backend_error. I need to trace through what happens when the module stops to see if the teardown kind gets set correctly and whether the test still passes. None/backend_error"? The test expects backend_error if any frame is sent, but if teardown kind is set to ConnClose on module death, the drain would silently drop without triggering backend_error—yet the test still passes since it accepts nothing. The real issue is that the design claims to preserve the test by mapping to backend_error, but the logic for which value gets set on module death is unclear: the module connection closing is technically a ConnClose, but from the client route's perspective it's the module dying, and the cleanup path that triggers flow.close() doesn't explicitly specify which teardown kind to use.

The design's taxonomy would naturally label this as ConnClose→silent, which contradicts the claim that module death produces backend_error. So the client either gets a silent drop (blocked request hangs until timeout) or backend_error, and since the test is lenient it won't catch this inconsistency—but the actual behavior matters for the client, and currently the blocked request does wake with backend_error at a specific point in the router code.

The design explicitly states it maps module death to the None/backend_error arm, so there's a mismatch between what the taxonomy would suggest (silent) and what the design claims should happen (backend_error). This is a major issue because under v2 with ConnClose→silent, the blocked coroutine gets no terminal and the client hangs to timeout—a behavioral regression from today's behavior.

The real problem is that the mechanism can't actually distinguish between module death and graceful connection closure at the forwarding layer. When cleanup_connection fires via release_module_route (whether from an unexpected module drop or a graceful close), there's no signal to tell the mutator which case it is, so it can't set the right teardown kind. This is a fundamental underspecification in the design.

That said, there's a plausible resolution: if only explicit GOODBYE and reload set teardown kinds, and everything else (including module crashes) defaults to None→backend_error, while pure client connection closes are handled separately by the connection teardown path setting ConnClose, then the system might actually work. I'll mark this as a medium-level concern rather than a blocker.

There's also a potential double-counting issue with AcquiredCredit RAII and the outstanding count. The guard calls flow.release() on drop unless commit()'d, but the timing of when commit() is actually called in the Delivered path isn't fully specified in the documentation, creating ambiguity about whether the credit gets released twice. disarms. As written with the manual flow.release() plus the guard drop, there's a potential double release. The shipped release() has a CAS guard checking in_flight!=0, so in the rollback case where acquire incremented in_flight to 1, a manual release brings it to 0, then the guard drop triggers another release — the CAS guard catches this and ignores the over-release with a warning rather than corrupting permits. But in concurrent scenarios where multiple coroutines are processing, an over-release from one can decrement another's in-flight counter, causing real credit corruption. So while the CAS guard masks the double-release symptom, it doesn't prevent the underlying issue in concurrent contexts.

This is the R11 concurrent-duplicate hazard the design was meant to eliminate, now reintroduced through the RAII guard and manual release ambiguity. The design needs to enforce that all releases go through exactly one path — the guard's disarm mechanism — to prevent this.

The spec is internally inconsistent: it describes the guard as the release mechanism, but the pseudocode also calls flow.release() manually in rollback and send-fail paths. If the guard isn't disarmed before those manual calls execute, you get a double release. The CAS operation masks this in isolation, but under concurrency it corrupts the state. This is a real spec defect.

For B5, I'm verifying that a fast module terminal can't arrive before the Delivered flag is set. The timing works out: Delivered gets set under lock at T0, then unlock and send at T1, and the module can only receive and process the frame after T1, with the terminal response coming back at T2 > T1 > T0. Since the terminal path checks for Delivered under lock and it was already set at T0, the ordering is guaranteed correct. B5 is closed.

But this same ordering is what creates the B2 Delivered/send race condition.

For B6, I'm checking that the terminal-release path's slots.remove is correctly ordered against a Delivered set before send — it is, since Delivered is set under lock before send, and the terminal removes under lock after the module processes. That's closed.

On the slots O(1) lookup: the read loop no longer does a linear scan of the queue, using a HashMap get instead, which eliminates the DoS risk of O(queue) scans per CANCEL on the read loop. That aspect is closed. But the CANCEL Queued-arm's remove from the queue still has complexity concerns...

The read loop itself performs O(n) VecDeque removal work under the inbox lock when processing queued CANCELs. An adversary spraying CANCELs for queued correlations triggers O(n) work each time. So while the get-lookup is O(1), the deque removal is a residual DoS vector that violates the "O(1) under lock" invariant. I'm marking B6 mostly closed for the lookup improvement but flagging the VecDeque removal as a major imprecision against the O(1) claim.

For B7, v2 fixes v1's whole-FIFO issue by moving control offload to client-side only while keeping module connections inline to preserve the bind-ack barrier. The test accepted_route_publishes_route_open_before_immediate_reverse_request verifies the module side stays inline and passes. On the client side, route.open is now a channel-0 operation offloaded to a per-client FIFO task, which serializes the client's channel-0 frames among themselves but runs them concurrently with the client's data frames.

This creates a race condition: a client's route.open (bind) on the offloaded ch0 FIFO could be processed after a subsequent data frame on the newly-bound route, causing the data to hit an unbound route and get dropped. This mirrors v1's #7 bind-ack barrier issue but on the client side—the client sends bind then immediately sends a request on that route, with the bind stuck in the offloaded ch0 FIFO while the request goes through the data path.

However, the managed SDK avoids this by awaiting the route.open ACK before returning a handle, so compliant clients won't send data until the bind is acknowledged. Raw-wire clients sending bind-then-data immediately without waiting would break, but that's already broken today since the serial read loop processes ch0 route.open inline (relaying to the module and awaiting the ACK) before reading the next frame.

The client-side offload changes this: data frames get read and routed while route.open is still being processed by the FIFO task, so data can overtake bind and hit unknown_channel — a reordering that mirrors the B7 issue. This is actually the intended behavior (the whole point is to stop route.open from blocking data frames), but it does change observable behavior for clients that send bind-then-immediate-data without awaiting the ACK.

Now I'm realizing there's a deeper issue with the FIFO ordering: route.close is a data-channel GOODBYE frame, not a ch0 frame, so the design's claim that "per-client-connection FIFO preserves route.open→route.close ordering" doesn't quite hold — route.open sits on the ch0 FIFO while route.close is on the data channel. is a data-channel GOODBYE, not a ch0 operation. So if a client sends route.open on ch0 followed by route.close as a GOODBYE on the data channel, the GOODBYE could process first, find no route, and become a no-op—then route.open commits and the route stays orphaned. This is a reordering hazard from client-side offload that I should flag as medium to major severity.

For B8, the v2 spec now honestly corrects I3/I7 to reflect the release-call-site change, which aligns with the synthesis. That's closed on the documentation side. I need to verify the R11 mechanism—specifically that slots.remove gates on was-Delivered before release, ensuring only one terminal operation succeeds via CAS.

The double-guard works cleanly: the gate prevents calling release twice for the same correlation, while the CAS protects against in-flight underflow. They're orthogonal, not conflicting. For the concurrent-duplicate case where two terminals arrive simultaneously, the lock serializes the inbox operations—the first gets Some(Delivered) and releases, the second gets None and skips release. The gate genuinely handles this concurrency correctly through linearization.

However, this closure depends on correlation uniqueness being enforced (Q4'). If two different delivered requests somehow share the same correlation ID, the first terminal removes the slot and the second finds nothing, causing a credit leak. So R11 is conditionally closed—it requires Q4' enforcement to become normative rather than just a design lean. Until that happens, there's a residual leak risk.

Now looking at B9's three-phase teardown: admission transitions to Closing before snapshot removal, with cancellation and bounded joins. The concern is whether a stale-snapshot reader that still holds an Arc<RouteBinding> can safely check admission—the dispatcher and inbox need to be embedded in the RouteBinding itself for the happens-before relationship to hold correctly.

The remaining question is about drain tasks blocked on module_sink.send with a FrameSink clone: the design uses cancel_token select! and bounded-join with abort, and server.rs shows the writer gets a CLOSE_DRAIN_GRACE timeout before abort, but I need to figure out who's responsible for joining the drain task itself.

The issue is that RouterConnection::Drop is synchronous, so it can't await a bounded-join of drain tasks — but the design mentions the JoinHandle is owned by the teardown path. If cleanup_connection is called from Drop (which is sync), then the "bounded-join the drain task (2s)" can't actually happen there. Either the teardown needs restructuring so connection close awaits drain joins before the sync Drop happens, or drain tasks must be aborted via cancel_token without awaiting, but I'm not seeing where that logic runs if it's in a sync Drop context.

The real concern is that if drain tasks are only cleaned up by JoinHandle drop (which doesn't abort in tokio — it just detaches), a blocked drain on module_sink.send could survive connection close and leak the FrameSink clone, keeping the module_sink mpsc open. Though the writer wait on the server side only waits on the writer itself, not drain tasks, so a leaked drain wouldn't block connection close — the writer would abort on grace timeout anyway. But the drain task still holds a reference to module_sink, which could be a module that needs cleanup.

The core issue is that the design's async 5-phase teardown (cancel_token, bounded-join, abort) requires an async context, but the actual teardown path is synchronous through RouterConnection::Drop. There's no way to wire the async join and abort logic into a sync Drop without restructuring the connection lifecycle entirely.

On the ownership side, the drain task captures an Arc to the RouteDispatcher, which means the dispatcher and module_sink stay alive as long as the task runs — making abort() essential for cleanup. The JoinHandle ownership appears acyclic since it's held by the connection/table rather than the binding, and the binding removal doesn't depend on the task exiting. But this still hinges on abort() actually being called to break the Arc cycle and free the dispatcher.

The bigger concern is lock ordering: teardown acquires the per-route inbox lock first, then calls flow.close() on a separate lock, and later the epoch-fenced release path takes the global forwarding write lock and calls flow.close() again. I need to check whether any code path acquires the inbox lock while holding the forwarding write lock, and vice versa — that would create a deadlock risk.

Looking at the different paths: teardown takes inbox-lock then forwarding-write-lock sequentially with no nesting. The read loop does a forwarding read lock lookup (which releases before proceeding), then takes the inbox lock for the push — no nesting there either. The terminal path from module to client takes the inbox lock, and I need to verify it doesn't hold the forwarding lock at the same time.

The critical issue is that snapshot publishing happens inside the forwarding write-lock critical section. If release_client_route_locked needs to modify the dispatcher's inbox while holding the forwarding write lock, that creates a potential deadlock with the read-loop/terminal paths that take the inbox lock first. I'm checking whether the inbox critical section ever tries to acquire the forwarding lock, which would complete the cycle. The admission state and snapshot removal aren't atomic—there's an intentional window between setting admission=Closing and the actual removal where the route still appears bound in the snapshot, but the admission check handles this. The lock hierarchy still isn't explicitly documented, which is a medium-level gap. For B10, the publish happens under the write lock with the closed flag set before the removal snapshot goes out, so readers checking a pre-release snapshot see the route as bound but then hit the closed flag and treat it as unknown_channel rather than a backend error. snapshot sees closed=true once it's set. The real race window is when a reader loads the Arc and reads closed=false before the writer sets it to true — the reader proceeds to route into the flow, but concurrently the release closes the flow and removes the route, so the reader's send might succeed on a module being torn down. This is the same pre-existing Arc-cloned-across-release race that was already noted, just with a narrower window now, though the closed flag doesn't fully eliminate the interleaving.

So B10 is acceptable since the residual race is pre-existing and no wider than before. The closed recheck needs to happen on both data-plane paths — module to client and dispatcher push — which the design covers.

Now I'm weighing whether to hard-gate on merge-0 being live in the core systems or use a blocking-admission interim, leaning toward hard-gate, though those systems aren't verifiable in this checkout.

The interim approach — blocking admission in the drain off the read loop — preserves today's semantics without SDK changes and avoids head-of-line blocking since the wait happens in the drain. It's actually safer because it doesn't introduce a new hard-error contract, and the design acknowledges it's viable. Hard-gating is defensible but risky given that I can't verify merge-0 landed in those external systems from this repo, so hard-gating on an unverifiable condition is problematic. The blocking-admission interim is the safer default — I'll mark Q1' as right-but-unsafe and prefer the interim, or at least defer the new error codes until external verification happens.

The design does specify that the daemon must not emit these codes until merge-0 is live fleet-wide, offering the interim as a safety valve, so it's being cautious. Hard-gating is acceptable only if the daemon truly never emits the codes pre-merge-0, and that gating needs a concrete mechanism like a feature flag or negotiated version that the doc doesn't spell out. Shipping the blocking-admission interim decouples the risk better.

For Q2', the bounded-join timeout of 2 seconds plus abort can drop in-flight terminal frames, which is acceptable since the connection is closing anyway. It's leaning acceptable, but if the abort drops a drain task mid-send, the module may or may not have gotten the frame on the client side.

The issue is that teardown happens not just on connection close but also on single-route GOODBYE, where the connection stays live. If bounded-join plus abort applies to per-route teardown rather than just connection close, aborting could strand a client on a still-active connection, causing it to hang. So it's right for connection-close scenarios but unsafe if applied to per-route GOODBYE teardown—needs scoping.

For Q3, I'm looking at depth caps plus byte-based secondary caps. The lean is to keep max(4, 2×window) and add a byte cap, which addresses the memory DoS vulnerability from v1 #12. Byte-based capping is the correct fix here.

On Q4, the slots HashMap needs to enforce correlation uniqueness by rejecting duplicate-correlation enqueues as a protocol error. The lean says enforce at enqueue, which is right and necessary since R11/B8 closure depends on it. But this shouldn't stay as an open question—it has to be a hard requirement because without it, B8/R11 leaks credit. So Q4 needs to be promoted from a lean to a normative rule.

For Q5, I'm weighing whole-table Arc rebuild versus per-shard approaches. The lean favors whole-table with measurement in T9, which aligns with v1 #10. The risk is that whole-table rebuild happens per mutation under the write lock, making it O(n) per mutation—potentially O(n²) under high route churn. Since publish-under-lock (B10 fix) forces the rebuild into the write-lock critical section, this is a real cost to measure and flag.

Now I'm cataloging the defects found: B2 has a CANCEL-overtake blocker causing cancel loss, drain pop hits a micro-limbo state when CANCEL removes the slot between pop and Claimed-set, AcquiredCredit's manual flow.release can double-release and corrupt under concurrency despite CAS masking it in isolation, VecDeque removal violates the O(1) claim by doing O(n) work under lock, and there's an integration gap between async 5-phase teardown and sync RouterConnection::Drop that prevents bounded-join or abort.

Beyond those, the teardown taxonomy conflates module-death with backend_error versus conn-close with silent failures, client-side ch0 offload reorders route.open versus data-channel-GOODBYE so FIFO doesn't cover the GOODBYE ordering, corr-uniqueness is left as a lean rather than normative which ties to an R11 leak, lock hierarchy isn't explicitly documented, and there's a lost-wakeup risk in the notify logic where drain checks pop under lock, releases, then calls wait—but tokio::sync::Notify's stored-permit semantics should handle this if the ordering is correct. The drain loop re-checks the queue after waking, so even if a notify_one arrives between the None-check and wait call, the permit gets stored and consumed safely on the next iteration. The Notify usage appears sound given tokio's semantics, though there's a minor edge case worth noting around permit timing. So the pattern is safe overall. Now I'm thinking through whether duplicate correlation IDs could leak credits at enqueue — if the same corr gets inserted twice, the slots map overwrites the old entry while the queue still holds both references, so when the drain loop processes the stale queue entry it's actually working with the new slot instead of the original one.

The real danger surfaces if the old slot was already marked Delivered: overwriting it with a fresh Queued slot means that delivered request's credit acquisition gets lost, and when the terminal response arrives later, the release logic operates on the wrong slot state entirely. This is a genuine credit leak and state corruption path if correlation IDs are reused while requests are still in flight. The design acknowledges this risk and intends to enforce strict corr-uniqueness, but until that enforcement is actually in place, it remains an open vulnerability.

Now I'm checking the I4/GOODBYE flush semantics and related concerns—not a blocker, but worth documenting accurately. Looking back at the B2 verdict, the single-lock architecture does solve the original v1 race condition around queue inspection from the read loop, but it exposes two new sub-races: one in the pop→Claimed transition that's fixable with a spec clarification, and a more fundamental one where CANCEL messages can overtake Delivered correlations at the module sink level. That second issue is the critical one—I need to determine whether it's truly unavoidable or if there's a design tweak that closes it.

The core tension is that when a CANCEL arrives for a Delivered correlation, routing it directly to module_sink creates a race against the Request message. To properly order them, I'd either need to funnel the CANCEL through the drain task so it queues after the Request, or introduce a SendInFlight sub-state where CANCEL waits until the send completes. The first option adds complexity the current design doesn't have, and the second reopens the B5 problem. So as specified, this is a genuine gap—B2 remains unclosed with this Delivered/send window vulnerability, which makes it a blocker. The read loop likely uses try_send rather than await to forward the CANCEL, which means it gets enqueued immediately in the mpsc buffer ahead of the Request that the drain task sends afterward. This causes the module to dequeue and process CANCEL first (a no-op) before the actual Request, confirming the race condition. Now I'm wondering if the design actually intended for Delivered cancellations to also flow through the inbox/drain ordering to prevent this. minal → module no-op. Harmless. OK.

Now checking if the read-loop-CANCEL forward to module_sink itself awaits on the read loop for cancel-safety. If it's a try_send, that's fine. If the module_sink fills up, try_send fails and the CANCEL gets dropped — another cancel-loss vector under module backpressure, but that's a pre-existing issue.

I think I have enough for a thorough analysis now. Let me structure the deliverable with per-blocker verdicts: CLOSED, NOT-CLOSED, or NEWLY-BROKEN, plus any new defects and Q rulings.

Starting with B1: CLOSED as a design issue — merge-0 prerequisite correctly identified.

B2: NOT-CLOSED — the Delivered/send CANCEL-overtake window creates cancel loss, and pop→Claimed leaves a micro-limbo state. This is a blocker.

B3: CLOSED — the primitives are properly named (VecDeque, HashMap, Mutex), no mpsc scan involved, though VecDeque removal is O(n) which ties to B6.

B4: NOT-CLOSED in part — the RAII guard plus manual release creates double-release ambiguity (major issue), and teardown-kind assignment is underspecified (medium severity). The mechanism is getting closer but still has gaps.

B5: CLOSED — Delivered-before-send ordering is correct and the fast terminal always sees Delivered, though this creates the window B2 exploits.

B6: CLOSED with a residual — the read-loop scan is gone, but VecDeque::remove under lock is O(n), which contradicts the O(1)-under-lock claim; a tombstone approach would fix this.

B7: CLOSED with new concerns — module-side inline is preserved and shipped tests pass, but client-side offload reordering is now a medium-severity issue around route.open vs data-channel GOODBYE and bind-then-immediate-data sequencing.

B8: CLOSED conditionally — the R11 gate genuinely closes concurrent-duplicate double-release through lock linearization, making I3 and I7 honest, but this exactness hinges on corr-uniqueness (Q4') which is only a lean assumption, not normative, leaving a residual leak until enforced.

B9 is the major blocker — the admission=Closing barrier is sound, but the async five-phase teardown with cancel_token, bounded-join, and abort cannot run in the shipped synchronous RouterConnection::Drop and cleanup_connection; the documentation doesn't reconcile this gap, and the lock hierarchy remains unstated.

B10 is closed — publish-under-lock plus closed AtomicBool restores the unknown_channel observable, and the residual closed=false-then-release race is pre-existing, not widened.

The verdict is no-go, with B2's Delivered/send cancel-overtake as the primary blocker.

B2 is a genuine defect as written — there's a concrete interleaving that demonstrates the cancel-loss window, which reintroduces the R5 defect the redesign was meant to eliminate. Given the gate rule that a single un-refuted concurrency defect triggers no-go, this warrants rejection, though I should frame it as "not-closed, requires mechanism change" rather than dismissing it outright. B4 double-release, B9's sync-Drop integration gap, and B8/Q4' corr-uniqueness-not-normative are also issues, and while some are go-with-changes-able, B2's cancel-loss is the deal-breaker that demands re-gating after fixes.

The fix itself is straightforward: order CANCEL-for-Delivered after the Request on the sink side, either by routing through the drain or using per-route send-serialization. The architecture remains sound; it's a mechanism adjustment, not a fundamental redesign. Now I'll write the response concisely with source citations and trim the context from the large tool outputs I've finished analyzing.
I have gathered sufficient source evidence to render each verdict. Let me drop the spent large outputs and finalize.

[assistant] 2026-07-15T08:52:23Z
Now I'm working through the key concurrency issues systematically, starting with the route_backpressure blocker. The v2 specification acknowledges this problem and adds a merge-0 SDK prerequisite, which makes the claim more honest, but I need to verify that the merge-0 mechanism is actually feasible by checking against the SDK source code, particularly how the TypeScript client classifies failures.

Looking at the TypeScript implementation, the `classifyFailure` function only handles socket-level failures, so terminal Error frames bypass it entirely and go through a different path that rejects the pending call. For merge-0 to work, they'd need to add code-based classification in the catch block of the `call()` function, checking if the error code is "route_backpressure" and retrying in place—this is feasible since the error already preserves the code from the parsed frame. The Rust implementation would need a similar check before the terminal error handling, matching on the body code before returning the error, which is also doable since the code is already available. Swift doesn't have a classifier yet, so it would require new logic. The in-place retry approach fits naturally into the existing retry loops in both TypeScript and Rust.

However, there's a critical constraint: the daemon won't emit these codes until merge-0 is fleet-wide and gated on dependencies that aren't in the current checkout, making the contract impact on external consumers unverifiable. They're offering an interim "blocking-admission" safety valve as a mitigation. So while the design doc defect is closed—the false claim is now honestly acknowledged and the prerequisite merge is sequenced—the actual SDK implementation doesn't exist yet, and the external consumer impact remains unverifiable.

For B2, I'm now looking at how CANCEL arrivals interact with the per-corr state machine under the inbox lock, tracing through each possible state transition. ue+slots, synth cancelled. When drain later pops... wait — drain pops from `queue` first, THEN reads slots. If CANCEL removed from both queue and slots, then when drain pops_front it won't find corr in queue (it was removed). But wait — can the drain have already popped it? The pop and the CANCEL both take the lock. If CANCEL wins (gets lock first), it removes from queue+slots — drain's later pop_front won't return this corr. Good. If drain's pop wins first, corr is removed from queue but NOT yet in slots as Claimed (there's a SEPARATE lock acquisition between pop and set-Claimed). 

**HERE IS A GAP.** The drain does:
```
corr = { lock; pop_front }        // rele
```
ases lock, then re-acquires it to mark the slot as Claimed. Between these two lock acquisitions, the corr has been popped from `queue` but its slot is still `Queued` in `slots`. If a CANCEL arrives in this window, it finds the slot as `Queued` and tries to remove from queue — but the corr is already gone since drain popped it. The question is whether removing a non-existent element from the VecDeque causes issues, or if the design expects CANCEL to only remove Queued entries that are actually still in the queue.

Actually, I'm realizing the core issue: removing a specific corr from the middle of a VecDeque is O(n), which contradicts B6's claim that CANCEL is O(1) with no scan. The design uses slots for O(1) lookup, but if CANCEL on a Queued entry has to scan the VecDeque to remove it, that's not O(1) anymore.
```
Unless they use tombstones — leaving the corr in the queue but marking it removed in the slot, then skipping it during drain. But the doc literally says "remove from queue+slots," which implies an actual removal. This opens up a potential DoS: an attacker fills the queue to capacity, then sprays CANCELs for entries near the front, forcing repeated O(n) removals.

Actually, the real issue is worse — to cancel a Queued corr, we need to find it in the VecDeque first, which requires an O(n) scan since we only have the corr ID, not its index. Then removing it is another O(n) shift. So CANCEL-on-Queued is O(n) find plus O(n) removal. The slots map gives O(1) state lookup, but the actual queue removal isn't O(1) unless tombstoning is used. The doc doesn't specify tombstoning, so B6 isn't fully closed — there's still a potential O(n) bottleneck during the read loop when holding the inbox lock.

The dispatcher inbox lock is per-route, and the read loop acquires it when processing CANCELs, so an O(n) removal stalls the read loop — the same B6 head-of-line hazard, just shifted. But the queue depth is bounded by depth_cap (roughly 2×window, so around 2048 max), and each CANCEL actually removes a corr from the queue, shrinking it. An attacker can spray CANCELs, but they can't repeatedly cancel the same corr since it gets removed after the first cancel.

However, the attacker could enqueue 2048 requests and then cancel them back-to-front, making each cancel an O(n) find operation — that's 2048 cancels × O(2048) per find, totaling around 4 million operations, which is the same order of magnitude as the original B6 vulnerability. So the fix only partially closes the issue. The doc's literal "remove from queue" approach reintroduces the O(n) scan that the v1 council recommended avoiding with a tombstone or index strategy. A tombstone approach (marking slots as Cancelled and draining them lazily) would be O(1), but VecDeque positions shift on pop_front, so you can't store stable indices directly.

There's also a critical race condition in the window between when drain pops a request and when it sets the state to Claimed. If a CANCEL arrives during this gap, it finds the slot still in Queued state, removes it from both the queue and the slots map, and sends a synthetic cancellation to the client. Then when drain re-acquires the lock and tries to set `slots[corr].state = Claimed`, the slot no longer exists — this is a major defect that reintroduces the original B6 vulnerability. CANCEL can delete the slot in that window, sending the synthetic cancelled terminal to the client. Then the drain pops the corr and acquires credit, but finds the slot missing — if it correctly rolls back and releases the credit without sending another terminal, that's fine, but the pseudocode doesn't specify this behavior. If instead it treats a missing slot as Delivered and sends to the module, you'd get both the synthetic cancelled terminal and an uncancelled module execution, creating a double-terminal violation.

The real issue is that the drain's pop and state-setting happen in two separate lock acquisitions, leaving a window where the slot can vanish between them. If these were merged into a single atomic critical section, the gap disappears entirely — this is a concrete defect that needs fixing.

But stepping back: even with the gap, if the drain checks for a None return (meaning the slot was cancelled while queued) and properly releases any acquired credit, the system might still be correct with just one terminal state. The pseudocode doesn't actually specify what happens when pop returns None though, which is the missing piece.

Actually, the cleaner fix is to have pop and mark-Claimed happen atomically within a single lock, so CANCEL can only set the cancelled flag without removing the slot once it's marked Claimed. Then post-acquire the drain sees the Claimed state and can check the cancelled flag directly.

Let me trace through what happens when CANCEL fires while the drain is mid-acquire: it sets cancelled=true and returns, the drain finishes acquiring successfully, re-locks, sees Claimed with the cancelled flag set, and rolls back by releasing the credit and synthesizing a cancellation terminal—one clean terminal, module never receives the frame. That's the correct path.

But there's an edge case: what if acquire returns ChannelFlowClosed while cancelled is true?  The handle_closed path runs and synthesizes based on the teardown kind, potentially generating module_reloading or backend_error instead of cancelled—a minor discrepancy since the client treats these idempotently anyway, so no double-fire, but the semantics are slightly off.

When CANCEL arrives after Delivered is set but before the Request actually reaches the module, there's a race: CANCEL gets forwarded to the module via the read-loop path while the Request is still queued in the drain, so CANCEL could arrive at the module before the Request does despite both going through module_sink. ore the send creates the same race: CANCEL can arrive and forward before the Request is enqueued, causing it to execute uncancelled. This mirrors the exact vulnerability v1 identified—the module only learns about the request after receiving it, so a CANCEL that arrives first gets ignored. The real issue is that setting Delivered before the actual send creates a window where the FIFO guarantee breaks—the module could receive CANCEL before REQUEST even though REQUEST was logically processed first. This is exactly the kind of race the redesign was meant to eliminate. The fix is either to set Delivered only after a successful send, or to ensure CANCEL is only forwarded once the request has actually been delivered to the module.

But there's a genuine tension here: moving Delivered to after-send reopens a different bug where the terminal might complete before Delivered is set. The v2 design chose to fix that one at the cost of breaking CANCEL ordering. The real solution might be to have the drain itself forward the CANCEL rather than the read loop, since both operations go through the same sink and the drain would know when the request has actually been enqueued.

Actually, I need to verify whether module_sink truly preserves FIFO ordering when the drain and read loop send concurrently on cloned Senders. That's the crux of whether this reordering is actually possible. Both the drain and read-loop release the inbox lock before racing to module_sink.send, so the CANCEL can indeed overtake the Request—this race is confirmed. So B2 has two unresolved issues: the non-atomic pop-to-mark-Claimed transition across two locks, and the window where CANCEL can bypass a pending Request after Delivered is set but before send executes. The mechanism improved but isn't closed.

For B3, v2 specifies a single `VecDeque<u64>` and `HashMap<u64,Slot>` behind one `Mutex<RouteInbox>`, eliminating the concurrent mpsc scan entirely—everything serializes through that one mutex now, so the data-race is closed. The O(n) VecDeque removal is a separate DoS concern, not a data-race.

For B4, the drain-task error handling is now explicit: send failures release the credit and remove the slot (matching router.rs), `ChannelFlowClosed` disambiguates via `TeardownKind` to route to Reloading/Goodbye/ConnClose/backend_error, and the `AcquiredCredit` RAII guard handles panics. I'm verifying these match the shipped behavior and that the test expectations still hold.

The test is lenient—it accepts either `backend_error` or nothing depending on whether a frame exists—so v2's mapping of module-death to `backend_error` satisfies it. The key remaining question is the `teardown` field: when a module dies, the flow closes via `release_module_route_locked` or `release_client_route_locked`, but neither currently sets a `teardown` field. In v2, the mutator that closes the flow (connection cleanup) must set `teardown=ConnClose` at that point. according to whether the module's connection itself closed versus the module process exiting—but the cleanup path doesn't distinguish between them, so it's unclear whether it should emit ConnClose (silent drop) or None (backend_error to the client). The taxonomy only has those four teardown types, and module death via connection closure naturally fits ConnClose, yet v2 claims it should produce backend_error instead, which requires treating it as something other than ConnClose. arding lock, not the inbox lock — so there's a potential race where the drain thread reads teardown without holding the lock that protects the write. This is a synchronization bug: the mutator writes teardown under one lock while the reader expects it to be protected by a different lock. The drain and terminal paths both acquire only the inbox lock, while the teardown mutator would need forwarding-write then inbox—so I need to check if any path inverts that ordering by acquiring inbox then forwarding-write, which would cause deadlock. The synthesize-Error path goes through egress, and the terminal path calls flow.release() without the forwarding lock, so an inversion might not exist, but this lock hierarchy is still a concern I need to verify.

However, there's a bigger issue: the lock-ordering relationship between forwarding-write-lock and inbox-mutex is never explicitly documented, yet the teardown field crosses both. Additionally, there's a staleness problem—the mutator sets flow.close() under the forwarding lock, then needs to set inbox.teardown under the inbox lock, and if these aren't atomic, the drain could wake from observing closed and read teardown before the mutator has set it. The mutator must set teardown before flow.close(), and the drain must read teardown after observing closed, but since they're under different locks, the drain could see closed without seeing the updated teardown.

This stale-teardown race means the drain hits the None case and generates a backend_error even for graceful shutdowns like Goodbye or reload, producing the wrong synthetic terminal. For Goodbye (which should be silent), the client gets a spurious backend_error, and for reload it sends backend_error instead of module_reloading, so the client won't retry. The error arms are now specified, which is an improvement, but the teardown field's lock discipline remains unspecified and crosses the lock boundary, creating this race condition.

I'm also checking the RAII guard interaction with outstanding double-counting: AcquiredCredit releases flow on drop unless commit() is called, and on the Delivered path outstanding is incremented under lock before send, with a decrement and cleanup if send fails. But there's a potential issue if the guard...

Actually, I need to trace this carefully. When acquire succeeds, the guard is armed. Under lock, if we transition to Delivered, outstanding increments and the credit is now owned by the terminal path. The guard must be disarmed at this Delivered transition—otherwise it releases again when the block exits, causing a double release. Similarly on send failure, the code explicitly releases the flow, and if the guard is still armed it releases again. So the guard's disarm points need to be precise: disarm when handing credit to the terminal path on Delivered, and disarm on explicit send-failure cleanup.

But the documentation only mentions commit() at Delivered and doesn't address disarming on the manual-release path. This means send-failure triggers a manual release followed by the guard dropping and releasing again—a double release that corrupts the in-flight window counter.

Actually, reconsidering: maybe the guard is meant to own the credit until either commit() (at Delivered) or the guard drops on failure. If that's the intent, the send-failure arm shouldn't manually call release—it should just let the guard drop. Yet the documentation explicitly includes a manual flow.release() in the send-failure path, which means both paths fire unless the guard is disarmed. The documentation contradicts itself about ownership.

Now looking at B5: the outstanding.insert happens under lock before the send, so Delivered is already set when the module first sees the frame. A fast terminal on the module→client path will find Delivered and release correctly. The happens-before chain holds: Delivered is set under lock before send, the frame leaves, the module processes it, the terminal arrives and finds Delivered already set, so no leak.

But there's a subtlety with the send-failure path—if send fails, the module never receives the frame, so a terminal can't arrive anyway. B5 is closed.

Now looking at B6, the claim is that slots HashMap gives O(1) CANCEL operations, but the actual queue removal is trickier. While the state lookup in slots is O(1), removing a specific correlation ID from the VecDeque requires finding its index and shifting elements, which is O(n). The documentation doesn't mention tombstoning as a mitigation, so the DoS vulnerability resurfaces—an attacker could spray CANCELs to force repeated O(n) removals under the inbox lock. It's bounded by the depth cap, but the issue is reintroduced. And yes, the read loop does hold the inbox lock during that O(n) removal operation, which means we're blocking on the read path — that's a head-of-line blocking issue. So B6 isn't fully resolved as written.

Now looking at B7: the whole channel-0 FIFO ordering breaks the bind-ACK to data sequence. The spec says client-side offload only, MODULE connections stay unsplit, and bind-ack stays inline on the module read path, which should preserve the bind-commit-before-next-frame barrier by design. There's a test that validates this ordering guarantee.

So the question is whether moving the client's channel-0 to a separate FIFO task breaks any ordering between a client's channel-0 operations and its data frames — like if a client sends route.bind on channel-0 and then immediately sends data.

The SDK does single-flight binding, so compliant clients block and wait for the bind acknowledgment before sending data, which means client-side reordering is safe. But for raw-wire clients that pipeline bind and data together, the data could race ahead under the offload model since the data path and channel-0 FIFO would be concurrent, whereas today's inline processing handles them serially.

This is a narrow edge case for non-compliant clients, but the core bind-ACK barrier is solid. For finding B8, the invariants I3, I7, and I4 have been reworded in the spec to accurately reflect the actual deltas—the release call site's exactly-once gate, the module-to-client wire semantics with credit fixes, and the GOODBYE frame behavior—so they're now honest descriptions of what the system does rather than aspirational claims.

Now I'm working through finding B9 about the GOODBYE flush and enqueue race condition that could cause teardown hangs. The spec defines a three-phase state machine (Open/Closing/Closed) where admission switches to Closing before snapshot removal, and the read loop checks admission to route frames appropriately. The teardown path uses a cancel token and bounded join with abort logic to drain the task safely, with the JoinHandle owned by teardown to avoid cycles. I need to verify the try_push-after-flush scenario doesn't create a deadlock window. The teardown mechanism is the real problem: the shipped code uses a synchronous `Drop` impl that can't await an async bounded-join or run cancel-token logic with a 2-second timeout, but v2's solution requires exactly that async coordination. So there's a fundamental mismatch between the async teardown design and the synchronous trigger that actually fires when the connection closes. The drain tasks need to be joined in the async connection scope before Drop runs, not during the sync teardown—that's the real gap between the documented 3-phase async teardown and the actual sync Drop-based cleanup. The server code at connection close also waits on the writer with a timeout, which adds another layer to reconcile. The try_push admission barrier closes that hole, but the async bounded-join/abort mechanism isn't fully reconciled with the sync Drop semantics during teardown—leaving an integration risk. I'm rating B9 as partially closed. Now looking at B10, which concerns whether the Merge-1 snapshot maintains invariant neutrality when publishing under the write lock with a closed flag that data-plane consumers check after loading.

The key insight is that publishing under the lock eliminates the publish-after-unlock window, and setting closed=true before the removing snapshot is published ensures readers see either the bound route with closed=false or an unknown_channel state. But there's still a timing question: can a reader ever load closed=false while the route is already released, or does the write-lock ordering guarantee that closed=true is always visible before any stale snapshot? a stale snapshot from before the release, creating a read-after-write visibility issue that doesn't exist with today's RwLock serialization—so ArcSwap actually widens the TOCTOU window compared to the current implementation.

The closed flag mitigates this by preventing most cases, but the residual race (reading closed=false then the release happening) matches today's Arc-clone window, so it's not a new defect. B10 is resolved because the specific v1 problem—new backend_error observable plus publish-after-unlock—gets fixed by checking the closed flag on both the module-to-client routing path and the dispatcher push, which v2 commits to doing.

For the terminal path specifically, if closed=true and we treat it as unknown_channel, the terminal gets dropped, but that's fine because teardown already closes the flow anyway and release on a closed semaphore is a no-op, so there's no credit leak. Now I'm moving on to examine the new defects and reconsider the overall lock-ordering hierarchy.

The key constraint is that teardown holds the forwarding write lock while needing to acquire the inbox lock to set admission=Closing and update the teardown field, which means forwarding-write must come before inbox in the hierarchy. I need to verify no path violates this by taking inbox first and then trying to acquire the forwarding write lock—the read-loop and dispatcher don't take forwarding locks, and lookup_data_route takes a read lock before returning an Arc, so the caller would need to be checked for any subsequent write lock acquisition. need to also be under forwarding write lock to prevent races. The terminal path takes inbox then a semaphore release, which seems safe. But there's a potential issue: the teardown field is written during flow.close() under forwarding write lock, yet read during inbox lock operations in teardown — so either teardown needs atomic access or the inbox lock must protect both the read and write.

This is a major defect: the lock hierarchy between inbox and forwarding-write is undefined, and teardown crosses both without a clear synchronization protocol. On the notify side, the drain loop checks if the queue is empty, releases the lock, then waits — but tokio::Notify uses a permit system where notify_one() stores a permit if no waiter is present, so a subsequent wait() returns immediately, avoiding the classic lost-wakeup race.

However, there's still a subtle issue: if two pushes happen while drain is between unlock and wait, both call notify_one() but only one permit gets stored. Drain wakes once, pops one item, loops back, and pops the second without waiting — which is fine. The real danger emerges if drain checks emptiness and waits without re-checking after the waiter is registered, since tokio's Notified future, once created and awaited, will... Looking at the uniqueness constraint issue: if the same correlation ID gets enqueued twice, the insert operation overwrites the previous slot without checking, which means the old slot's credit leaks—the outstanding count still includes it but the slot is gone, so the terminal path never finds it to decrement. The spec acknowledges this and recommends enforcing the check at enqueue time to reject duplicates as a protocol error, but the pseudocode doesn't actually show that validation step, just the raw insert.

Now moving through the Q rulings: Q1' is about whether to use a hard-gate or the interim blocking-admission approach for merge-0, and the lean is toward hard-gate since it's cleaner and the interim approach preserves semantics safely—though the interim does reintroduce per-route backpressure-by-wait.  The blocking happens on the drain side off the read loop, so there's no head-of-line blocking, making it safe. The hard-gate approach is correct, but it depends on verifying that merge-0 is live across external repos like broca and aft, which I can't verify in this checkout—so it's right in principle but has an unverifiable external dependency. For Q2', the bounded-join with a 2-second timeout and potential in-flight terminal drops leans toward acceptable since connection closing is expected behavior.

However, there's a subtlety: if this is a module teardown rather than a client disconnect, aborting the drain could drop a request mid-send while the client is still waiting, leaving them hanging until timeout. The client should eventually get a backend_error from the handle_closed path, but if the abort races the synthetic response, they might see nothing. It's an edge case but mostly acceptable. For Q3', adding a byte-based cap alongside the depth cap is the right move—the v1 finding about frame-count not being a memory bound (the 256GiB DoS) is a real issue, and byte-based capping is the proper fix.

The lean toward enforcing correlation uniqueness at enqueue time is correct, though it hasn't been integrated into the normative pseudocode yet—that needs to happen. For the whole-table Arc versus shard question, leaning toward whole-table makes sense given the read-mostly workload, but I should measure this in T9 and require churn benchmarks to confirm. Now I'm reconsidering how B1's merge-0 behavior fits against the actual retry loops, particularly in the TypeScript client's call method. So to make route_backpressure retryable in TypeScript, merge-0 needs to either wrap the SubcError into a retryable SubcCallError or add a check before the terminal error classification at line 423—this touches the error boundary between SubcError and SubcCallError, not just the classifyFailure logic. It's feasible but confirms merge-0 is more involved than the "in-place retry" framing suggests, though it's not a blocker to the overall design. Now I need to verify whether a distinct retryable...

Actually, a distinct retryable class is definitely needed because route_backpressure is currently classified as terminal in both TypeScript and Rust, so neither retries in place—the design's claim about needing a separate class is correct. The in-place retry pattern with `continue` statements fits both loops. With that confirmed, I'm moving on to double-check the module-to-client terminal path ordering against the Delivered set, specifically whether slots.remove is correctly ordered when Delivered is set under the inbox lock before send.

Since Delivered happens-before send happens-before module processing happens-before the terminal path, the terminal path will always see Delivered set (or the slot already removed if it's a duplicate), so the mechanism is sound. Now I'm reconsidering the double-terminal case in B2's Queued-remove scenario with a slow drain, though I think I've already covered the main issues. I'm also examining the CANCEL forwarding logic in §2—specifically when a CANCEL's correlation ID is None, which happens when the correlation was never enqueued in the first place, and forwarding that CANCEL to the module is mostly harmless. For unknown correlations that were never sent, forwarding the CANCEL to the module is a no-op but still creates minor DoS surface from unbounded traffic. Now I need to reconsider the teardown field synchronization — release_client_route_locked closes the flow under a forwarding write lock, but it doesn't have access to the RouteInbox structure needed to set teardown, so I need to figure out how to make the dispatcher reachable from that release path.

The RouteBinding would hold a new dispatcher field, and setting inbox.teardown from the release path requires holding the forwarding write lock while also locking inbox, establishing a forwarding-write ⊐ inbox ordering. The lookup path doesn't nest these locks — it releases the forwarding read lock before taking inbox — and the terminal dispatch path in the router doesn't hold any forwarding lock when it takes inbox, so the only problematic nesting is the teardown case. But there's a stale-read race: the mutator closes the flow and sets inbox.teardown both under the forwarding-write lock, yet the drain observes flow-closed as soon as the semaphore closes, potentially locking inbox and reading teardown=None before the mutator finishes setting it, even though both operations happen within the same critical section. not the flow.close path. So the drain might wake via cancel_token instead of ChannelFlowClosed, which means handle_closed won't fire — the drain takes a different code path entirely. This is a critical gap: the teardown logic assumes the drain wakes one way, but the actual wake mechanism could be different, leaving teardown unexecuted or partially executed.

I've identified enough issues now. Let me double-check the B2 double-terminal scenario one more time — whether a terminal can fire twice or vanish in the Queued-remove plus in-flight case with corr reuse, which ties back to the B11/Q4' duplicate-corr problem I already covered.

I should also verify whether the pop/mark-Claimed 2-lock gap is actually documented or if I misread the drain section.

Confirming the 2-lock gap is real — there are two separate lock blocks with no atomicity between them, which is the core of a genuine B2 concurrency hole. Now I need to calibrate severity for my verdict. The prompt states that a single unrefuted concurrency or contract defect is a blocker, and I've found B2 residual holes from the pop/mark gap and Delivered-before-send cancel reordering, plus B4 teardown-field lock-crossing issues.

Looking at the full list of defects: B4 stale reads and RAII double-release ambiguity, B6's O(n) VecDeque::remove reintroduced as a DoS vector, B9's async-teardown versus sync-Drop lifecycle gap, Q4'/B11's duplicate-corr handling not integrated into the normative spec, and an unspecified lock hierarchy. Given the prompt's rule that a single unrefuted concurrency defect is a NO-GO, and I have at least the B2 Delivered-before-send cancel-reorder as a genuine unrefuted concurrency defect, the verdict is NO-GO.

But I should acknowledge that many blockers are genuinely closed—B3, B5, B7, B8, B10, and the B1 design—so this is narrower than v1's failures. The honest framing is NO-GO with enumerated residual concurrency holes in the drain/cancel/teardown mechanism, all fixable, showing significant progress but not ready to ship.

Let me rigorously reconsider the Delivered-before-send cancel reorder one more time to make sure it's a real defect and not refuted by some overlooked detail.

The race condition is clear: drain pops a correlation, marks it Claimed, acquires credit, re-locks, sets state to Delivered, increments outstanding, then unlocks before calling send on the module sink. Meanwhile a CANCEL arrives at the read loop, which locks the inbox, finds the slot in Delivered state, matches the forward-to-module-sink case, unlocks, and sends the cancel. If the read loop's cancel gets enqueued first because drain was preempted after its unlock, the module receives the cancel before the request itself.

When the module's cancel handler runs, it looks up the correlation in its in-flight map and finds nothing—the request hasn't arrived yet—so it's a no-op. Then the request arrives and runs to completion, emitting a terminal response. The cancellation is completely lost. This is exactly the same cancel-loss defect from the earlier analysis, just now happening in the window between marking Delivered and actually sending the request to the module.

The design claims "Delivered means the module owns the terminal or will own it," but "will own it" doesn't enforce that the cancel is ordered after the request on the wire. The only way to guarantee the request reaches the module before any cancel is to send the cancel only after confirming the request has been sent.

Since the read loop forwards cancels independently without coordinating with the drain's send, there's no ordering guarantee. The window is tight—between releasing the Delivered lock and the drain completing its send—but under load or adversarial scheduling it's exploitable, and it's exactly the saturation scenario the redesign is supposed to fix. This is a blocker.

One mitigation could be to defer the cancel forwarding: when Delivered, set a flag instead of immediately forwarding the cancel, then let the drain send the cancel after its own send completes. But once Delivered, the drain has already moved past the lock and can't easily coordinate back.

Now I'm finalizing the verdict on each blocker: B1 is closed from a design perspective—the merge-0 prerequisite was genuinely added and the retryable class fits the continue-loop pattern, though external consumers can't be verified and merge-0 itself isn't implemented yet. B2 remains open because the pop-to-mark-Claimed gap plus the Delivered-before-send reordering on module_sink can still cause cancel loss or double-fire. B3 is closed since the VecDeque and HashMap are protected by a single Mutex, eliminating the data race.

B4 is only partially closed—the arms are specified correctly, but the teardown field crosses the forwarding-write and inbox lock boundaries, creating a stale read that produces the wrong synthetic terminal, and mixing RAII guards with manual release introduces double-release ambiguity. B5 is closed because Delivered and outstanding are recorded under lock before send, so the fast terminal always sees the Delivered state. B6 remains open in part because while slots provide O(1) state lookup, the Queued-cancel operation requires removing from the VecDeque in O(n) time under the read-loop inbox lock, reintroducing a DoS vulnerability that needs a tombstone or index approach. B7 is closed since the client-side offload is isolated.

B8 is closed with I3, I4, and I7 reworded to reflect honest deltas and R11 framing corrected for concurrent duplicates. B9 is partially closed—the try_push hole is sealed by the admission=Closing barrier, but the async three-phase teardown with bounded join and abort isn't reconciled with the shipped sync RouterConnection::drop cleanup, leaving the cancel_token versus flow.close race underspecified. B10 is closed because publishing under lock and checking the closed AtomicBool on both data paths restores the unknown_channel invariant, with any residual TOCTOU window no wider than today's Arc-clone window.

Now I'm identifying new defects, starting with the Delivered-before-send issue. Pulling in a few more minor issues around cancellation surfaces and memory caps, then reviewing the Q rulings across all five questions — most came back right, though some need integration into the normative spec rather than just being leaned on. The verdict is still no-go given the blockers, but it's substantially closer than the previous version, so I'm weighing whether to reframe this as go-with-changes if the fixes are actually feasible.

The real sticking point is the delivered-before-send cancel reorder — that's a genuine architectural tension where two different requirements pull in opposite directions on where to place the delivered marker, not just a missing paragraph. That architectural conflict pushes me toward no-go per the gate rule, so I'll enumerate the blockers and note that six out of ten have genuinely closed.
I have gathered sufficient source evidence to render each verdict. I traced each v2 mechanism against the shipped source it cites.

<COUNCIL_MEMBER_RESPONSE>

# Adversarial Re-Gate — subc-core dispatch redesign v2

Verdict up front: **NO-GO**, but materially closer than v1. Six of ten v1 blockers are genuinely closed; the drain/cancel/teardown mechanism still carries **two un-refuted concurrency defects** (both in the cancel path — the exact class the redesign exists to kill) plus several MAJOR spec gaps. Per the gate rule (a single un-refuted concurrency defect = NO-GO), v2 does not pass as written.

---

## Per-blocker rulings (B1–B10)

**B1 — route_backpressure→NotSent false in SDKs → CLOSED (design/contract).**
v2  drops the false "zero SDK changes" claim and adds a hard-gated prerequisite merge-0. A distinct retryable class is genuinely required: a data-plane terminal `Error` settles as a plain `SubcError`→`terminalCallError` (client.ts:1060, 423) and `CallError::Module(body)` (consumer.rs:579) — neither retries; the only retryable classifier is the channel-0 route.open closed set (consumer.rs:3130-3135, client.ts:1242-1261). In-place retry fits both managed loops (TS `continue` at client.ts:432/443; Rust `continue` at consumer.rs:577/583). **Caveats (preserve):** (a) merge-0 is unimplemented; TS needs the route_backpressure `SubcError` threaded across the `SubcError`→`SubcCallError` boundary (the `!(err instanceof SubcCallError)` throw at client.ts:423 currently makes it terminal); (b) broca/aft/alfonso-core are **not in this checkout** — the "merge-0 live fleet-wide" gate is **UNVERIFIABLE** here and must be checked in their repos.

**B2 — CANCEL queued→delivered limbo → NOT-CLOSED.** Two residual holes:
- *pop→mark-Claimed is non-atomic.*  drain uses **two** separate critical sections: `corr = { lock; pop_front }` then `{ lock; slots[corr].state=Claimed }`. In the gap the slot is still `Queued` but off the deque. A CANCEL arriving here hits the `Some(Queued)` arm, removes the slot + synthesizes `cancelled`; the drain then `slots[corr]` is **gone**, and the post-acquire block (`Claimed{cancelled:true}`/`else→Delivered`) has **no `None` arm** — if `None` falls through to `else`, the frame is sent to the module → double terminal + uncancelled execution.
- *Delivered-set-before-send lets CANCEL overtake the Request.*  sets `state=Delivered` **under lock before** `module_sink.send().await` (correct for B5). But a CANCEL in that window hits `Some(Delivered) => forward CANCEL to module_sink`. Drain and read-loop then **race on the same `module_sink` mpsc with no ordering**; if the cancel is enqueued first, the module runs `handle_cancel` on an unknown corr (no-op — subc-client-rs/lib.rs:995) then processes the Request uncancelled. This is the **exact LOOP-R5 cancel-loss** transposed into the Delivered-before-send window (router.rs:491 send; module inserts its in-flight entry only after receiving the Request).

**B3 — queue data structure / mpsc-scan race → CLOSED.**  names `VecDeque<u64>` + `HashMap<u64,Slot>` behind one `Mutex<RouteInbox>`; both actors mutate under the same mutex. No mpsc receiver-side scan, no torn reads. The data-race is eliminated.

**B4 — drain error arms → PARTIALLY-CLOSED (NOT-CLOSED).** The arms now exist (send-fail release mirrors router.rs:491-496; `ChannelFlowClosed` disambiguation; RAII panic guard) — a real improvement. Two new holes:
- *teardown field crosses lock domains.*  reads `kind = self.teardown` under the **inbox** lock, but the mutator that closes the flow (`release_client_route_locked`, forwarding.rs:1409-1438; `release_module_route_locked`, 1440-1470) runs under the **forwarding write lock** and calls `flow.close()` (1424/1455). The drain wakes from `acquire()==Err(closed)` the instant `sem.close()` runs (forwarding.rs:1737-1739) and can read a **stale `teardown` (None)** before the mutator sets it → spurious `backend_error` on a graceful reload/Goodbye (wrong: a reload should be `module_reloading` so the client retries).
- *module-death shoehorned into the `None` "defensive" arm.*  routes the primary failure (module endpoint gone) through the `None→backend_error` catch-all, while a module connection-close would naturally be `ConnClose→silent`. The field cannot distinguish "module tore down my client-route" (→backend_error) from "client tore down" (→silent) unless the *writer* encodes who — never specified. The cited test (forwarding.rs:3811-3878) is **lenient** (accepts Goodbye, backend_error, *or nothing*), so it does not actually pin this.

**B5 — outstanding.insert-after-send race → CLOSED.**  records `Delivered` + `outstanding+=1` under the inbox lock **before** `module_sink.send`. Happens-before: Delivered-set → send → module processes → terminal →  terminal path locks inbox, finds `Delivered`, releases. A fast terminal always observes `Delivered`; no leak. (This is precisely what *creates* the B2 Delivered-before-send hole above — B5 and B2 pull in opposite directions on where the marker sits.)

**B6 — O(queue) CANCEL scan DoS → NOT-CLOSED (partial).** `slots` gives O(1) *state lookup*, but the `Some(Queued) => remove from queue+slots` arm must remove a specific corr from a `VecDeque` — `VecDeque::remove` is **O(n)** (find index + shift), executed on the **read loop under the inbox lock**. An attacker fills to `depth_cap` (~2×window; window=1024 for StatelessParallel, forwarding.rs:25) and back-cancels, reproducing ~O(depth²) work on the latency-critical path — the same HOL vector v1 flagged. The v1-recommended tombstone/index (mark slot cancelled, leave in deque, drain skips on pop) is **not** in the doc;  literally says "remove from queue."

**B7 — whole channel-0 FIFO breaks bind-ACK barrier → CLOSED.**  correctly narrows offload to **client-side** channel-0; module connections stay inline, preserving the bind-commit-before-next-frame barrier by construction. The shipped test `accepted_route_publishes_route_open_before_immediate_reverse_request` (router.rs:1078-1102) stays green. *(Minor residual: a raw-wire client that pipelines bind+data without awaiting the ack could see its data race ahead of the offloaded route.open — SDKs single-flight bind so are unaffected.)*

**B8 — I3/I4/I7 false invariants → CLOSED.**  reword all three into honest deltas and correctly re-scope R11 to the **concurrent-duplicate** case, matching the shipped CAS `in_flight!=0` guard (forwarding.rs:1702-1731) — two concurrent terminals with `in_flight>0` both pass `!=0` and double-decrement. s slot-gone gate closes that cleanly (duplicate finds slot removed → `release=false`) and stacks *safely* with the CAS (the CAS is the last-ditch net; the gate is the real fix). No conflict.

**B9 — GOODBYE flush vs enqueue / teardown hang → PARTIALLY-CLOSED.** The try_push-after-flush hole **is** closed: `admission=Closing` set under the inbox lock (the only enqueue path,  checks it) is a genuine happens-before barrier vs binding removal. **But** the async 3-phase teardown (cancel_token select + 2s bounded-join + `abort()`) is **not reconciled with the shipped synchronous teardown trigger**: `RouterConnection::drop` (router.rs:391-397) is a sync `Drop` calling `cleanup_connection` (sync, control.rs:422-437) — a sync Drop cannot `.await` a bounded-join. The drain-task join/abort must be relocated into the async connection scope, unspecified. Also the `cancel_token`-vs-`flow.close()`-wake race (which branch fires handle_closed) is underspecified.

**B10 — merge-1 snapshot not neutral → CLOSED.**  publishes the snapshot **inside** the write-lock critical section (no publish-after-unlock window) and adds `closed: AtomicBool` set under the write lock before the removing snapshot, checked on both data paths (router.rs:281-309 and dispatcher push) → stale-Bound reads observe `unknown_channel`, restoring today's post-release observable (not `backend_error`). Residual read-`closed=false`-then-release TOCTOU is **no wider than today's** Arc-clone-then-drop-read-lock window (forwarding.rs:840-890), which v2 acknowledges.

---

## NEW defects introduced by v2 mechanisms

**N1 (BLOCKER) — Delivered-before-send cancel/request reorder on `module_sink`.** See B2. Concrete interleaving: drain sets `Delivered`+unlocks (95) → preempted → CANCEL read loop matches `Some(Delivered)`, forwards CANCEL to `module_sink` → module `handle_cancel` no-ops unknown corr (lib.rs:995) → drain resumes, sends Request → module runs uncancelled. Reintroduces LOOP-R5 under the saturation the design targets.

**N2 (BLOCKER/MAJOR) — pop→mark-Claimed non-atomic gap.** See B2. Two separate `{lock}` blocks; CANCEL in the gap deletes the slot; post-acquire block has no `None`/missing-slot arm → double terminal or credit mishandling. Fix is trivial (pop AND mark Claimed in one critical section; add explicit `None` arm) but as-written it is a real hole.

**N3 (MAJOR) — `teardown` field lock-domain crossing → stale read.** Written under the forwarding write lock (release_*_locked, forwarding.rs:1409-1470), read under the inbox lock (. No stated ordering; drain can wake on `sem.close()` and read stale `None` → wrong synthetic terminal (backend_error instead of module_reloading, defeating client retry).

**N4 (MAJOR) — `AcquiredCredit` RAII + manual release double-release.** s send-failure arm does an explicit `flow.release()` (line 100) *and* the guard "calls `flow.release()` on drop unless commit()'ed." The doc specifies `commit()` only at the Delivered transition, not on the manual-release path → the guard drops still-armed → **double release**. The shipped CAS guard (forwarding.rs:1716) would partially mask it while corrupting the window count. Internally inconsistent about who owns the release.

**N5 (MAJOR) — lock hierarchy inbox-mutex vs forwarding-write-lock unspecified.** Teardown nests `forwarding-write ⊐ inbox` (to set admission/teardown); data path takes `forwarding-read` then releases then `inbox`. No inversion is *proven* absent, and the doc never states the hierarchy — a latent deadlock surface that must be made normative and checked at every site.

**N6 (MAJOR) — duplicate-corr overwrite leak not in normative .**  push does bare `slots.insert(corr, …)`; a reused in-flight corr overwrites the prior slot, orphaning its `outstanding`/credit. The fix is only *leaned* in Q4', not integrated into the normative read-loop pseudocode. As-specified, merge-2 leaks on duplicate corr (wire allows the daemon does not enforce non-reuse).

**N7 (MAJOR) — frame-count depth cap is not a byte bound.** The 256 GiB/conn memory-DoS (v1 #12) is only *leaned* in Q3', not normative. Bodies are owned `Vec<u8>` admitted before the cap; a byte budget must be normative, not an open question.

**N8 (MINOR) — `None`-arm forwards unknown CANCELs to the module.**  `Some(Delivered)|None => forward CANCEL to module_sink` sprays CANCELs for never-seen corrs to the module (no-op at lib.rs:995 but unbounded traffic) — the unknown-corr surface the daemon-synthesized-cancelled design meant to avoid.

---

## Q1'–Q5' rulings

- **Q1' (hard-gate merge-0 vs blocking-admission interim)** — **RIGHT**, with the caveat that "merge-0 live fleet-wide in broca/aft/alfonso-core" is **UNVERIFIABLE in this checkout**. The blocking-admission interim (backpressure-by-wait on the drain side, off the read loop) is genuinely HOL-free and a sound fallback.
- **Q2' (2s bounded-join; abort may drop a terminal)** — **RIGHT** for client-close; slightly unsafe for module-teardown where the client is still waiting (must ensure the synthetic backend_error wins the abort race).
- **Q3' (byte-based secondary cap)** — **RIGHT but must be normative**, not an open question (see N7).
- **Q4' (enforce corr-uniqueness at enqueue)** — **RIGHT but not integrated** into s normative push (see N6).
- **Q5' (whole-table Arc vs shard, measure in T9)** — **RIGHT, conditional** on churn benchmarks; read-mostly justifies whole-table.

---

## Bottom line: **NO-GO**

Closed and proven: **B1 (design), B3, B5, B7, B8, B10** — real, traced closures, not paper. The architecture is sound and the doc is now honest about its deltas.

Blockers remaining (each cited):
1. **N1** — Delivered-before-send cancel reorder on `module_sink` (95-98; router.rs:491; lib.rs:995). Move cancel-forwarding of a `Delivered`/pre-send corr onto the drain *after* its send completes (flag + re-check), or introduce a `Delivering` sub-state that buffers the cancel until the request is on the sink.
2. **N2** — merge pop_front + `state=Claimed` into one critical section and add an explicit missing-slot (`None`) arm in the post-acquire block (86-95).
3. **B6/* — replace `remove from queue` with an O(1) tombstone (mark slot Cancelled, drain skips on pop); prohibit O(n) `VecDeque::remove` under the read-loop lock ( 74).
4. **N3+N5+B9** — specify the inbox↔forwarding-write lock hierarchy; make `teardown` set-before-`flow.close()` under a consistent lock (atomic or inbox-guarded write); reconcile the async 3-phase teardown with the shipped sync `RouterConnection::drop` (router.rs:391-397) by owning drain join/abort in the async connection scope ( .
5. **N4** — make `AcquiredCredit` the *single* release owner: remove the manual `flow.release()` in the send-fail arm or disarm the guard there (100, 109).
6. **N6** — hoist Q4' corr-uniqueness enforcement into normative  push.
7. **N7** — make byte-based memory budgets normative (Q3'→ spec).

This is a "re-spec the drain/cancel/teardown critical sections and re-gate" outcome, not "scrap it." The tension between B5 (Delivered before send) and B2 (cancel ordering) is the load-bearing unresolved item and must be designed explicitly before merge-2.

*Caveats/limitations: merge-0 SDK code and external consumers (broca/aft/alfonso-core) are not in this checkout — their contract impact is asserted, not verified. Confidence: high on N1/N2/N3/N4/B6 (traced to shipped source and pseudocode); medium on N5/B9 lifecycle (depends on unstated plumbing that could be made correct).*

</COUNCIL_MEMBER_RESPONSE>

