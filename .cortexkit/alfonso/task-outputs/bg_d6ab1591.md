---
task_id: bg_d6ab1591
agent: council-member
session_id: ses_0ce3c58d3ffef94mgFtHu8xwkb
parent_session_id: ses_0ce3e0e75ffed3gUYBr4lZyfSt
status: completed
completed_at: 2026-07-05T10:24:20Z
---

[user] 2026-07-05T10:12:14Z
## Solo Analysis Mode
You MUST do ALL exploration yourself using your available read/search tools.
- Do NOT use task or any delegation tool under any circumstances
- Do NOT delegate to explore, librarian, or any other subagent
- Do NOT spawn background tasks
- Search the codebase directly — you have full read-only access to every file
- This mode produces the most thorough analysis because you see every result firsthand


## Analysis Intent: AUDIT

You are conducting an **audit** — your goal is to find discrete issues, risks, or violations.

**Focus:**
- Search for problems, anti-patterns, security risks, correctness issues, or violations of stated requirements
- Each finding must be a distinct, actionable item with concrete evidence
- Severity determines priority: critical (blocks/breaks), high (significant risk), medium (should fix), low (nice to fix)
- For each finding, provide the specific location (reference, section, or component where it occurs)
- State your confidence: high (clear evidence), medium (likely but needs verification), low (suspicion, investigate further)
- **This is a broad sweep, not a targeted trace.**

**Analytical standards:** Support claims with concrete evidence. State confidence (high/medium/low) for key assertions. Note caveats and limitations.

**Structure your response as:**
```
<COUNCIL_MEMBER_RESPONSE>
## Finding 1: [Title]
- **Severity**: critical/high/medium/low
- **Location**: [specific reference — e.g. component, section, endpoint, rule]
- **Confidence**: high/medium/low
- **Issue**: [what is wrong and why it matters]
- **Evidence**: [concrete reference, snippet, or observation that proves the issue]
- **Suggested Fix**: [actionable recommendation]

## Finding 2: [Title]
...

## Summary
[Total findings by severity. Overall risk assessment with confidence levels.]
</COUNCIL_MEMBER_RESPONSE>
```

## Analysis Question

You are an adversarial reviewer performing a v3 RE-GATE of the subc federation design. Your job is to VERIFY that v3's deltas actually close the prior v2 re-gate findings, and to HUNT NEW gaps introduced by the deltas themselves. Be skeptical, precise, and source-grounded. Do NOT rubber-stamp.

## READ FIRST (in full)
The design doc: `docs/subc-federation-design.md` (v3). Read the whole file. Pay special attention to §2.6 (two subc-core primitives P1/P2), §5.3 (TOFU), §5.4 (identity split), §6.1 (at-most-once mechanics), §6.2 (partition/liveness).

## SOURCE GROUNDING (already verified by the orchestrator — you may re-open any file to confirm; cite file:line where you rely on source)
Key subc-core facts relevant to the primitives:

1. **Concurrency / flow-control coupling (critical for P1).**
   - `Registry::register_with_control_ops` (crates/subc-core/src/registry.rs:65-89): rejects duplicate `module_id` (line 74-75); stores `control_ops` taken from the HELLO; bumps `generation`.
   - control.rs:584 — `control_ops` are computed from the HELLO's advertised ops (`effective_module_control_ops(hello.control_ops)`), i.e. control_ops are a HELLO-time property.
   - control.rs:619-625 — at registration, `concurrency = manifest_concurrency(&registration.manifest)` is read from the manifest and passed into `forwarding.register_module_connection(...)`.
   - forwarding.rs:271-308 — `register_module_connection` takes `concurrency: Concurrency`, and (line 280-282) EVICTS any prior endpoint for that connection_id, then (line 284) bumps `next_generation`, creating a NEW `ModuleEndpointId { connection_id, generation }`. All existing route bindings key on the OLD `ModuleEndpointId` (forwarding.rs:41-46, 55). So re-registering a connection orphans its existing route bindings.
   - forwarding.rs:18-22 — the per-channel request-credit WINDOW is derived from concurrency: `DEFAULT_MODULE_MANAGED_WINDOW = 32` (ModuleManaged), `STATELESS_PARALLEL_WINDOW = 1024` (StatelessParallel). So concurrency in the manifest DIRECTLY sets the flow-control window. control.rs:1852-1862 `manifest_concurrency` — only `ToolProvider` carries explicit concurrency; other roles fall back to ModuleManaged (32).
   - IMPLICATION TO SCRUTINIZE: P1 `catalog.update` proposes to REPLACE a module's manifest IN PLACE without re-registering the connection. But concurrency AND control_ops are captured at register time from the manifest/HELLO. Does P1 change them? If the replacement manifest changes concurrency, does the live flow-control window change, and is that safe on in-flight routes with outstanding credits? If it does NOT change them, is the catalog now inconsistent with the flow window? Can catalog.update change advertised control_ops (which the health prober reads)? Is there any atomic transition that guarantees a route is NEVER bound to a tool the registry no longer knows?

2. **Reserved-nonce squatting protection (critical for P2).**
   - supervise.rs:344-395 — `SupervisorHandle` holds `reserved_nonces: HashMap<String,String>` (EXACT module_id → nonce) and `spawn_nonces`. `reserved_hello_authorized(module_id, presented)` (line 384-395): if there is NO reserved_nonces entry for the EXACT module_id, returns `true` (unconditionally authorized). Only an exact-id match triggers nonce checking.
   - IMPLICATION: P2 wants to extend this from exact ids to PREFIXES (e.g. reserve `fed:`). The gate today is a HashMap exact lookup returning `true` on miss. Prefix matching is a real semantics change. SCRUTINIZE: collision semantics between an exact reserved id and a reserved prefix (does `fed:` prefix-reserve also cover an exact id `fed`? what about `fedx:`?); what stops the fed-module ITSELF from registering `fed:<peerA>:tool` when policy meant a different module owns peerA; how is "connection owned by that module's attested process" KEYED, given the fed-module opens N per-peer loopback connections all from ONE attested process (one launch nonce) — is ownership keyed by nonce, by pid, by connection? Can a co-resident local key-holder open a connection and present the fed-module's launch nonce (is the nonce a shared secret visible to other local processes)?

3. **At-most-once client taxonomy (critical for §6.1).**
   - crates/subc-client-rs/src/consumer.rs:581-593 — `CallError` variants are EXACTLY: `NotSent` (request not accepted by writer path / route.open failed before data send), `OutcomeUnknown` (accepted by writer path but no terminal response observed), `Module(ErrorBody)` (handler error frame), `SubscriptionBackpressure`. Real-daemon tests (crates/subc-client-rs/tests/real_daemon.rs:294-303, 421-422, 524-525, 801) assert: accepted mid-call → OutcomeUnknown; OutcomeUnknown is NEVER auto-retried; bounded target absence → NotSent; route-gone after accept → OutcomeUnknown.
   - IMPLICATION: §6.1 says the fed-module reports `accepted` to the local consumer only after `intent` is durable, and an ambiguous WAN outcome surfaces as `OutcomeUnknown`. SCRUTINIZE: is "accepted-after-intent-durable" compatible with the existing NotSent/OutcomeUnknown taxonomy at the ORIGIN consumer (which is a plain subc client that only knows these 4 variants)? Once the fed-module has reported `accepted`/written the body, the origin client can ONLY see OutcomeUnknown on ambiguity — is that the intended and safe classification, and does anything the origin consumer does with OutcomeUnknown (never retry) compose correctly with the fed-module's own dedup/re-send? Also: llm-runner (the cited intent-log precedent) is NOT in this repo — treat that precedent as unverified/external.

4. Client taxonomy has no separate "durably-recorded-remote-intent" state; the fed-module must MAP its richer internal state machine onto these 4 variants for the origin consumer.

## PRIORITY FOCUS AREAS (address each explicitly)
1. **P1 catalog.update**: safe interaction with in-flight routes; the flow-control window when the replacement changes concurrency; route bindings to REMOVED tools; the catalog generation/staleness model; the health prober's advertised control_ops (can catalog.update change control_ops?). Is the daemon-side state transition atomic enough to NEVER leave a route bound to a tool the registry no longer knows?
2. **P2 prefix reservation**: collision semantics between exact reserved ids and prefixes; what stops the fed-module registering an id under a DIFFERENT module's reserved prefix; interaction with the connection-ownership check across the fed-module's N per-peer connections (all owned by one attested process — how is ownership keyed? is the nonce a shared secret?).
3. **§6.1 mechanics**: is the serving-side dedup ledger retention window soundly definable? What happens when the ledger row is evicted but the origin legitimately re-sends? Cross-restart durability of the monotonic seq in effect_id (a reset seq after origin db loss would collide with prior effect_ids)? Is reporting accepted-after-intent-durable compatible with the existing NotSent/OutcomeUnknown taxonomy at the ORIGIN consumer?
4. **Identity split §5.4**: is the profile-authored local BindIdentity sufficient for AFT-class providers that validate harness against an allowlist? (A `fed:<peer>` harness marker would be rejected by AFT today if AFT allowlists harness values — does the design need a harness-registration story? Check whether AFT/providers validate harness/BindIdentity against an allowlist.)
5. **TOFU §5.3**: first-contact window (TOFU trusts the first key blindly — what if the cloud is malicious at FIRST contact, before any pin exists?); key rotation legitimate-vs-attack disambiguation (how does a user tell a legit key rotation from an attack-substitution? both present as "changed key"); what the verification code binds EXACTLY (does it bind the session, the directory entry, both endpoints' long-term keys?).

Also sanity-check ANYTHING else that strikes you as unsound (e.g. §6.2 partition classification, §6.4 ClientHello device-identity transport addition, §6.5 cross-version negotiation, the one-connection-per-peer multi-registration claim vs forwarding.rs eviction behavior, relay DoS).

## OUTPUT REQUIREMENTS
- For EACH finding: give it a short title, a VERDICT of **BLOCKER** / **SHOULD-FIX** / **NOTE**, the evidence (cite file:line where you rely on source), WHY it matters, and a CONCRETE resolution proposal.
- Explicitly state, per v2-re-gate delta (P1, P2, §6.1, §5.4, §5.3), whether the v3 delta ACTUALLY CLOSES the prior finding or leaves residue.
- Separate "the delta closes the old finding" from "the delta introduces a NEW gap."
- END with an overall verdict: **GO** / **GO-WITH-CHANGES** / **NO-GO** for proceeding to phase 0 (building the two subc-core primitives P1 and P2), with a one-paragraph justification. If GO-WITH-CHANGES, list the must-fix-before-phase-0 items vs the can-fix-during items.
- Be concrete. Prefer "this specific line/mechanism is unsound because X; fix by Y" over generalities. Distinguish genuine BLOCKERs from nitpicks — calibrate severity honestly.

[assistant] 2026-07-05T10:12:14Z
**Reading design documentation**

I need to read the design document fully, which might be around 2000 lines. I should also open specific code files related to the project. It would be useful to create an outline of the document, and maybe I'll consider reading the entire thing for thoroughness. Using grep for section searches could help too. Starting by checking the file length and the headings seems like a good plan!

[assistant] 2026-07-05T10:12:27Z

[assistant] 2026-07-05T10:12:35Z

[assistant] 2026-07-05T10:12:42Z

[assistant] 2026-07-05T10:12:49Z

[assistant] 2026-07-05T10:12:56Z

[assistant] 2026-07-05T10:12:58Z
**Searching for identity validation**

I need to read around the BindIdentity usage in the subc-mcp main section, possibly looking into allowlists. It seems like class providers may not be from subc-mcp. I should search the project for harness and identity validation, and check if there are subc-control or providers related to this. Using grep to find "identity.harness" lines in subc-mcp main could help me locate relevant matches and understand the context better.

[assistant] 2026-07-05T10:13:03Z
**Exploring identity creation**

I need to gather more information on identity creation and how to harness overlay. It seems important to dive into the historical context, particularly around the year 1666. I’m curious about how that time might relate to my current understanding. I'll also look for any validation or studies related to harness overlay to ensure I'm approaching this correctly. I want to get this right!

[assistant] 2026-07-05T10:13:07Z

[assistant] 2026-07-05T10:13:11Z

[assistant] 2026-07-05T10:13:13Z
**Evaluating config options**

I'm looking at a harness-specific configuration overlay instead of an allowlist. I wonder if I need to consider AFT-specific providers and check if there's a subc-aft available. It might make sense to search and list relevant crates to see what options I have. This could help me understand better how to proceed. I really want to make sure I'm thorough in this process to get it right!

[assistant] 2026-07-05T10:13:17Z
**Exploring AFT class providers**

I'm considering if AFT class providers relate to AFT tools in the subc-core fake-aft-stub. I think searching for "opencode" in the harness allowlist could be useful, particularly in looking at tool providers tests. I’ll definitely want to search through the harness in subc-core or examine other crates as well. There seems to be a bit to unpack, but it looks promising!

[assistant] 2026-07-05T10:13:21Z

[assistant] 2026-07-05T10:13:27Z

[assistant] 2026-07-05T10:13:29Z
**Exploring validation issues**

I’m looking into the idea that providers validate against an allowlist like opencode and others. That seems good, but I think there might be a design issue here. I'm noticing there might not be any general validation code in the repository aside from comments. I plan to search for "config_divergence" in the provider. Plus, I’ll look up terms like “opaque config_divergence” and “mcp:” semantics. I might also check out fake-aft-stub or subc-control.

[assistant] 2026-07-05T10:13:34Z

[assistant] 2026-07-05T10:13:37Z
**Investigating validation code**

I’m noticing that Fake AFT doesn’t validate the harness; it just rejects the attachment through the environment. However, there’s a comment in subc-mcp that indicates the providers do some validation. Now, I wonder if I should look for the actual validation code in another location? Maybe searching for "mcp:" and checking around the call arguments could lead me to what I need. It’s a bit puzzling, but I’ll keep digging!

[assistant] 2026-07-05T10:13:41Z
**Verifying code validation**

I’m considering that the provider validation code might not be in the repository, but the comment could serve as evidence. I should verify if there’s an allowlist based solely on the comment. I think it would be helpful to find tests related to the default harness prefix. By reviewing those tests, I might get a better understanding of the expected semantics. It feels like a methodical approach!

[assistant] 2026-07-05T10:13:45Z

[assistant] 2026-07-05T10:13:50Z
**Investigating route bindings**

I think I need to inspect the control handle for route_open and the requirements for binding identity. I’m considering whether the BindIdentity might be passed unchanged, which could lead to rejection if there's a mismatch in requirements. This could be important, especially regarding any identity splits. I should search around how route binding is built and see if that sheds some light on the issue.

[assistant] 2026-07-05T10:13:54Z
**Evaluating provider route validation**

I need to consider if I should provide route.bind validation and whether searching through fake-aft or subc-mcp around attach_session is necessary—though it might not matter much. I think I have to inspect the documentation for primitives P1/P2, since there could be unresolved gaps. The design doc only outlines a few details. I'll need to evaluate if it addresses prior findings and if atomicity specifics are missing. I'll compare this with the current code to identify any issues.
**Analyzing concurrency and control operations**

I see the current code captures concurrency and control operations at HELLO time, and the design states that `catalog.update` replaces the manifest's catalog in place while updating the registry; existing route bindings remain untouched. However, it's unclear if concurrency or control operations are immutable or updated. If they're updated, the flow-control windows for existing routes might stay outdated, while new routes could use updated windows if forwarding.modules_by_id is refreshed. This could lead to mixed semantics across routes, raising concerns.
**Clarifying concurrency governance**

I'm realizing that concurrency governs channel flow on a per-route basis rather than globally for the module in serial contexts. It seems the flow window is specific to the channel route rather than an overarching one. I need to consider if `Serial` could imply that the window for these routes is set to a singular state. I should search for `window_for` to get a clearer picture of how these mechanics are structured.

[assistant] 2026-07-05T10:14:09Z

[assistant] 2026-07-05T10:14:11Z
**Investigating launch nonce**

I'm looking into the current spawn attestation and the connection file, specifically focusing on the launch nonce passage. I'll search for the launch nonce in the "hello" module and the connection file. It's important to determine if the nonce is a shared secret visible to other local processes. So, I need to figure out where the launch nonce is stored or passed. I'll search for "launch_nonce" to gather more information.

[assistant] 2026-07-05T10:14:33Z
**Checking launch nonce details**

I need to read the subc-mcp lines around 1307 to figure out how the launch nonce is injected. It’s essential to see if it’s visible in the environment. I think this could help shed light on the processes involved. I want to make sure I’m getting the right information, so diving into these lines seems like a good next step. Let’s explore this further!

[assistant] 2026-07-05T10:14:42Z
**Investigating nonce visibility**

I’m wondering if the environmental injection might be accessible to other local processes. The question is about whether a co-resident local key-holder can open a connection and present the fed-module's launch nonce. I’ll need to inspect how the nonce is transported or injected. It seems like I should search for subc_protocol environment constants, specifically looking into whether there's an environment variable for the launch nonce. It's an interesting problem to tackle!

[assistant] 2026-07-05T10:14:47Z

[assistant] 2026-07-05T10:14:59Z

[assistant] 2026-07-05T10:15:15Z
**Clarifying module registration**

I’m trying to make sense of the different sections. So, §2.5 mentions one loopback connection per peer, along with a HELLO-registering process for the peer's namespaced catalog. This makes me think it’s one module per peer, which would merge the catalogs exposed by each peer. But then, §2.6 indicates that the federation module registers these catalogs as `fed:<peer-pubkey-fingerprint>:<module>`, suggesting that there might be multiple IDs under a single prefix. It’s a bit confusing!

[assistant] 2026-07-05T10:15:36Z
**Evaluating phase 0 progress**

I’m considering whether the trust anchor might act as a blocker if we're only building P1/P2 during phase 0. The verdict on building primitives is important because if we handle the primitives first, we might resolve TOFU issues later. The user is asking about a GO/NO-GO decision on proceeding with phase 0 for P1/P2. I need to decide if there are blockers that must be fixed before we begin. It looks like I might need to consider whether specifications for P1/P2 are adequate before moving forward.
**Analyzing identity split for AFT-class providers**

I'm considering that moving forward could be a GO-WITH-CHANGES with some must-fix items. The user is asking if the local BindIdentity is sufficient for AFT-class providers that validate against an allowlist. From subc-mcp commentary, it seems that these providers validate against opencode|pi|runner|mcp:<client> and auto-prefix with shim. However, since the `fed:<peer>` harness marker isn't in the allowed list, providers would likely reject it. This could be a concrete blocker, so the design may need a new registration strategy or utilize existing allowed harness families.
**Evaluating allowlist conditions**

It seems that the allowlist may only include items with the format `mcp:<client>`, but I’m wondering if it could also accept anything with the mcp prefix. There might be some flexibility there, which could affect how we view the overall functionality. I’ll have to explore this further to see if any adjustments are needed to accommodate different harness markers. Understanding the limits of the allowlist will be crucial for our next steps.
**Analyzing device key design**

I'm looking at whether the design indicates that the code is derived from both devices' keys. It binds device keys but not session or peer identity names, which might allow the cloud to swap directory entries. This could lead to users detecting key substitutions if they know the device involved. However, the current transport does not include stable peer identifiers. It looks like device identity is necessary, so it seems this design may need further development in future phases.
**Examining protocol design**

I’m wondering if the old finding needs to be noted since the spec alone may not close the issue. The user wants me to sanity-check things, so I might look into whether cross-version negotiation uses an unknown ExecutionMode variant. Although, this might not be necessary unless it leads to unsoundness. I should also inspect how unknown role handling works in the current protocol deserialization. It seems like I need to clarify the one-connection-per-peer multi-registration claim versus forwarding eviction as well.
**Evaluating client taxonomy tests**

I've found that OutcomeUnknown isn't auto-retrying automatically for test cases 294-303. That means legitimate re-sends come from a fed-module durable log, especially during reconnects or crashes. This can lead to arbitrary delays unless the protocol implements caps. The at-most-once guarantee isn't reliable after garbage collection (GC). I’ll need to add a resend expiry with effect_id tracking for better control. There are potential blockers since core mechanics may not be sound, which raises concerns about crash safety and database resets.
**Addressing taxonomy mapping**

I need to note that version 3 closes the taxonomy mapping based on an old finding: mapping accepted-after-intent-durable to the origin OutcomeUnknown aligns with the current client taxonomy. However, I need to clarify that the local consumer should never encounter a new intermediate state. Instead of creating a separate finding, I could summarize it. The user is interested in how this impacts previous findings, so I may need to incorporate that information into the summary without labeling it as a finding.
**Evaluating registry generation**

I've got 67 and 73 from my read, which is good! I need to verify the current registry generation and its staleness model. It seems like P1 might need catalog generation, and I’m wondering if there's a global registry generation that could lead to issues. From doc 185, I see that signed catalog and policy generations are module-level. 

I think I should note that registry generation isn’t tied to catalog updates, but that’s not built yet. I'll check if I need to look at querying the tool manifest after a route opens. Lastly, I might need to ensure that existing route bindings remain untouched to prevent compatibility issues.
**Investigating catalog updates**

I need to check if `catalog.update` as a generic channel-0 operation must be authorized through `control_ops`. The baseline for `control_ops` only includes `route.bind` and `route.status`. If I introduce a new operation, it might need to re-advertise with a HELLO for `control_ops`, which connects to the mutability aspect. 

I need to determine whether this new operation requires a baseline operation or a new control operation and look into `MODULE_BASELINE_CONTROL_OPS`. If `catalog.update` is directed at the module level, maybe it won't be part of the baseline? Checking for additional control operation gating feels necessary. I’ll also investigate `effective_module_control_ops` to see if channel-0 requests require an advertised control operation.

[assistant] 2026-07-05T10:17:19Z

[assistant] 2026-07-05T10:17:21Z

[assistant] 2026-07-05T10:17:26Z

[assistant] 2026-07-05T10:17:28Z

[assistant] 2026-07-05T10:17:30Z

[assistant] 2026-07-05T10:17:52Z

[assistant] 2026-07-05T10:17:55Z

[assistant] 2026-07-05T10:17:59Z
**Analyzing request handling**

I'm thinking about how routes request frames interact with modules. The module receives data from TCP/socket after the consumer writes have been accepted, but the local accepted boundary is set earlier, leading to complications. There's no handshake to address backpressure, which means the design can't fully resolve older findings. It seems to require a new per-request ACK or a more detailed client state. Maybe I need to look into server routing to verify accepted statuses. I'm curious if the client library can interpret terminal errors effectively!
**Evaluating auto-retry semantics**

I'm realizing that the auto-retry semantics are a bit problematic because the client doesn't automatically retry with the module, which leaves some scenarios lost but unretryable. As a result, the earlier findings on section 6.1 aren't fully resolved because we can't change the client taxonomy or transport to include per-request acceptance ACK. This is crucial for the fed-module to ensure proper visibility of outcomes. I see a couple of significant blockers: acceptance boundary mismatch and retention/sequence issues. I should clarify these points effectively.
**Examining request patterns**

I’m contemplating if there's a local pattern at play where the consumer uses a per-call route and the provider sends a route.bind acknowledgment only after being ready. However, the durability of intent comes after the request body arrives, which is after the writer has been accepted. This seems to create an insurmountable issue. It's clear v3 doesn’t fully resolve the earlier findings. I need to check if there's any way for the provider to reject the route.bind before requests.
**Evaluating findings on protocol design**

I'm analyzing some findings regarding client semantics. For Finding 5, I see that the existing accepted boundary can't be adjusted without changing the protocol or using an explicit wrapper. In Finding 6, even if the boundary is fixed, issues with ledger retention persist. I’m considering whether to combine these findings or keep them separate for clarity. 

Also, I’m thinking about how to ensure a durable logging process before any actions are taken. If there's a crash before logging, the origin client would see an "OutcomeUnknown," and manual retries could pose some risks.
**Analyzing route.open errors**

I need to take a closer look at the route.open errors related to "NotSent" tests. There's a potential gap in line 177, which states that `NotSent`-class resends are safe, even if the first send reached the fed-module but the local writer closed afterward. In that case, the classify_failure could lead to an OutcomeUnknown rather than a NotSent. This discrepancy raises questions about client behavior. I should also investigate P1 control operations and how health prober interacts with control_ops in the registry to ensure clarity in documentation.
**Inspecting routing and module_id integrity**

I’m considering the implications if an update removes the tool provider from no-routable roles. The handle_route_open function relies on registry checks for target roles, but it seems live module connections could still function properly with existing live routes. I need to examine the module_id mapping from lines 697-712 closely, as some route queries depend on module_id_by_endpoint. If the P1 update changes the manifest module_id, that could cause issues, but documentation suggests the module_id should remain fixed. I also want to check for any potential race conditions with separate locks.
**Clarifying output structure**

I see the user provided a structure, but the output requirements have changed. Now, each finding needs a short title, a VERDICT of BLOCKER/SHOULD-FIX/NOTE, evidence, WHY, and a resolution proposal. It’s probably best to keep everything organized within the required tags.

The original template mentioned severity, but the user clearly prioritized the latest request. So, I should decide whether to include both severity and verdict or just stick with the latest user instructions. To be safe, I’ll focus on using the verdict only for clarity.

[assistant] 2026-07-05T10:18:46Z
**Evaluating findings**

I’m thinking about the final number of findings, possibly six. They might include that P1 cannot selectively tear down vanished-tool routes and lacks atomic target tracking. Also, there's a missing admissibility contract for concurrency and control operations. P2's prefix reservation and ownership semantics are under-specified, and the bearer nonce isn’t connection-owned. Additionally, §6.1's accepted-after-durable aspect clashes with the current client accepted boundary, and its dedup horizon and effect_id durability seem unsound.
**Inspecting provider validation**

I'm looking to see if there's explicit provider harness validation in the documentation, checking for more direct statements than comments. Maybe I'll search for terms like `opencode|pi|runner|mcp:<client>`, which might just lead me to code comments. While I have medium confidence that tests confirm the auto-prefix, I’m unsure about validation. I think I could state that "the code comment in subc-mcp documents the current provider contract," but I'm still considering it. I might also need to check related documents or architecture notes regarding route.bind rejections and config divergence.

[assistant] 2026-07-05T10:19:14Z

[assistant] 2026-07-05T10:19:27Z
**Inspecting module protocols**

I’m looking into ownership mapping, and it seems that's not present. Next, I want to check the module named "hello" to see its type in the control_ops along with the launch_nonce. This is about searching through the lines to understand the behavior and configuration more clearly. I need to ensure everything is functioning as expected. It feels like there could be important details to uncover here!

[assistant] 2026-07-05T10:19:31Z

[assistant] 2026-07-05T10:19:35Z
**Analyzing process interaction**

This comment points out that if a process is down or restarting, it can't register as a reserved module. I wonder if a different process could inject environment variables to gain access, but it depends on secrecy policies. I could simplify it by saying that authorization relies on bearer-nonce possession without tracking separate connections. Confidence in semantics is high, but I need to consider if file ownership can limit access. It might not be necessary to explore the nonce for environmental processes right now. I may need to reference specific lines later.
**Evaluating module connections**

I could cite the specific lines from `subc-client-rs/src/consumer.rs:2206-2216`, since they show the same environment-based consumer identity when the module acts as a client. However, it may not be necessary. The design indicates that the fed-module opens multiple loopback connections from one attested process, using the same launch nonce for every HELLO. I need to check if the module can open multiple provider connections. The connection ownership check might simply re-evaluate the launch nonce in the HELLO against the reserved prefix owner's module.
**Considering module registration**

I’m realizing that because reserved prefixes let a module register IDs that aren’t its own, a direct lookup by claimed module ID won’t work properly. The daemon could store a module owner name, like a fed-module ID paired with a nonce. This way, any HELLO under the reserved prefix must present that fed-module’s nonce, regardless of the claimed module ID, which might be an effective solution. It seems that a persistent connection tag might not be necessary for the initial HELLO, as long as the nonce remains secret.
**Evaluating the delta in P1**

It seems like P1 isn't closed after all, because the delta creates a new gap. It's promising non-disruptive removal, but it lacks enough state to manage that properly. I'm feeling confident about this assessment. Also, there's a finding that the mutable field contract in P1 is missing. This could be a critical issue that I need to further investigate to address any potential problems.
**Defining effect_id parameters**

I’m considering making the effect_id collision-resistant and self-contained by using a combination of details like origin_device_pubkey, durable installation epoch, and either a random or monotonic sequence. I also need to clearly define a replay TTL/lease for each effect, along with a server garbage collection contract. It’s important to specify what happens after expiry, which should be to avoid redispatch and handle it with "OutcomeUnknown/manual reconciliation." My confidence in this approach is high.
**Considering device identity**

I’m thinking about the trust anchor that manual pairing represents, and how it leads to partial closure. It might be good to note that the `ClientHello` message reveals the device identity. But since the prompt is asking for a sanity check, I wonder if I need to include that in my summary. However, they explicitly asked about the device identity addition. Maybe it’s worth mentioning after all!
**Ensuring evidence accessibility**

I want to make sure that evidence line numbers are accessible, particularly from doc lines 193-195. That's a good start. I’m thinking I might need to use ctx_reduce to drop large outputs, but maybe that’s not necessary since we have many tags. However, I should consider dropping some if the context limit is an issue. With around 91 tool outputs, it might help to drop some older large ones after extraction.

[assistant] 2026-07-05T10:20:53Z

[assistant] 2026-07-05T10:20:58Z

[assistant] 2026-07-05T10:21:13Z

[assistant] 2026-07-05T10:21:16Z

[assistant] 2026-07-05T10:21:18Z

[assistant] 2026-07-05T10:21:21Z

[assistant] 2026-07-05T10:21:23Z
**Clarifying route cache functionality**

It's clear now that the route cache works per provider and identity, not just by tool. This insight will help update my findings for point 1, making it more comprehensive. I also need to note that a "GOODBYE" for a vanished tool could unintentionally disrupt live calls to other tools sharing the same provider route. This realization highlights the importance of maintaining route integrity to avoid affecting unaffected tools. If we keep the route, it allows continued access until the provider rejects it. Hence, a selective route "GOODBYE" isn't feasible without distinct per-tool routes.
**Reviewing finding 1 and route semantics**

I need to adjust finding 1, and I'm considering inspecting the separation between control and data planes, but I might not need to. I checked if `route.bind` ties to the ToolProvider’s tool name, and it doesn’t. Providers might have a single route for calls if the consumer opts not to cache, but currently, caching routes is by provider and identity. There might be new routes if caching isn’t used. P1 can handle provider-wide changes atomically, but not individual tool changes. If there are remote catalog changes at the provider level, it can impact older findings.
**Evaluating concurrency changes**

I should consider how concurrency might be affected if the tool set within a provider changes, even if the concurrency field stays the same. I’m not sure if a change in tool provider concurrency directly depends on the remote module’s tool set. I also need to examine route bindings related to removed tools and control operations. It's worth checking whether `ProviderRole::ManagementSurface` or InternalService has fixed concurrency because that could restrict updates. Lastly, I might want to look into whether `register_with_control_ops` influences registry generation, as that will impact updates.
**Analyzing delta summary**

I’m reflecting on the delta summary regarding P1 and P2. For P1 (`catalog.update`), the old v2 issue with in-flight calls is only partly handled. While v3 drops disconnects effectively, it doesn't fully close the gap because it lacks tool-specific routing and atomicity for mutable fields. There are new gaps as well, making per-tool GOODBYE impossible and creating concurrency risks. 

For P2, the prefix reservation doesn't solve the old exact-id squatting issue, even though v3 is moving in the right direction but misses out on specific rules for prefix conflicts and owner-binding semantics.
**Clarifying verdict process**

I’m considering that "GO-WITH-CHANGES" usually means that I can continue once those necessary fixes are applied, rather than stopping altogether. It seems like the user is asking for the current status or decision, suggesting they want clarity on how things are moving forward. I’ll need to ensure they understand what needs to be done and what the timeline looks like, so I can provide a clear update.
**Considering finding closure**

I'm thinking about how to merge the verdict with severity; perhaps using labels like BLOCKER=critical/high isn’t necessary. It seems good to include confidence here. I want to make sure each finding clearly notes both “Old finding closure” and “New gap.” I could outline it with bullets for clarity: 

- Old v2 finding: Partially closes / Not closed / Closed
- New gap: …

This approach would satisfy the need for explicit per delta separation.
**Clarifying documentation requirements**

I need to verify the cross-version document and check the lines for closed enums. I already have the manifest and doc lines noted. It might be good to mention that "unknown fields tolerated" doesn't address unknown enum tags because they fail to decode. I should also inspect if unknown operations are excluded from exposure. While it’s not strictly necessary, I want to ensure that the final response remains concise yet thorough given the complexity.
**Organizing precise citations**

I need to gather precise citations as evidence for my findings. 

For Finding 1, I have several references, including the lines from the "subc-federation-design" document that discuss promises for replacing catalogs and removing tools. There are also references from various Rust files discussing route binding and identity management without naming tools directly. 

For Finding 2, I see repeated citations from the design document and other control and forwarding files. I might be able to cite some of these more compactly if needed.
**Gathering evidence for summary**

I’m looking into evidence related to the subc-federation design and its components. I think it's important to mention device identity based on some key sections in the documentation. Including a final note, like "Acknowledged but still open dependencies," can clarify the broader sweep. I'll summarize findings with bullet points, particularly highlighting the need for a transport change in the ClientHello structure. Doing this helps ensure everything checks out correctly.
**Considering citation formatting**

I need to be mindful about how I format citations. It seems like using a format such as `docs/subc-federation-design.md:72-73` would work well. But I’m also thinking there’s no need to include line numbers in bullet points—keeping it simpler seems better. Such details are crucial for clarity, and I want to make sure everything aligns properly. Let's ensure this format works effectively!
<COUNCIL_MEMBER_RESPONSE>
## Finding 1: P1 promises per-tool route behavior that the current wire model cannot express
- **Verdict**: BLOCKER
- **Location**: `docs/subc-federation-design.md:72-73,111`; `crates/subc-protocol/src/lib.rs:76-87`; `crates/subc-protocol/src/session.rs:45-53`; `crates/subc-protocol/src/manifest.rs:40-44`; `crates/subc-client-rs/src/consumer.rs:1665-1699`; `crates/subc-core/src/forwarding.rs:49-60,400-459`
- **Confidence**: high
- **Old v2 delta closure**: **Not closed**
- **New gap introduced**: v3 assumes subc can target “routes to tools that vanish,” but routes are provider-scoped, not tool-scoped.
- **Evidence**: P1 explicitly says removed tools get route-GOODBYE and cites AFT-style runtime tool-surface changes. But `RouteTarget::ToolProvider` carries only `module_id`, `route.bind` carries only that target, the client route cache is keyed by provider+identity (not tool name), and `RouteBinding` stores no tool/op identity.
- **Why it matters**: one ToolProvider route can serve many tools. If one tool disappears, subc cannot identify “the routes for that tool” without a protocol change. A provider-wide GOODBYE would also kill unrelated still-valid tools, defeating P1’s non-disruptive goal.
- **Concrete resolution**: narrow P1’s safe scope to additive/provider-level changes only, or explicitly accept provider-wide route teardown on ToolProvider catalog deltas, or redesign routing so binds are tool-specific (bigger protocol change).

## Finding 2: P1 does not define what manifest fields are mutable, but core behavior captures some at HELLO time
- **Verdict**: BLOCKER
- **Location**: `docs/subc-federation-design.md:72-73`; `crates/subc-core/src/control.rs:584-625,1248-1266,1752-1769,1852-1862`; `crates/subc-core/src/forwarding.rs:18-22,453-454,1277-1282`; `crates/subc-core/src/supervise.rs:1023-1045`
- **Confidence**: high
- **Old v2 delta closure**: **Not closed**
- **New gap introduced**: v3 says “replace manifest’s provided catalog in place,” but does not say whether concurrency/control ops may change.
- **Evidence**: `control_ops` are derived from HELLO and stored in the registry; health probing consults that stored set. Concurrency is read from the manifest at registration and used to size each route’s flow-control window.
- **Why it matters**: if `catalog.update` changes concurrency, old routes keep old windows while future routes may get different ones; if it changes advertised control ops, registry/prober state becomes stale or split-brain.
- **Concrete resolution**: make P1 reject any update that changes `module_id`, routable role shape, concurrency, or `control_ops`; or define a drain/re-register path for those fields instead of pretending they are in-place mutable.

## Finding 3: P2 names the right problem, but the authorization semantics are still underspecified
- **Verdict**: BLOCKER
- **Location**: `docs/subc-federation-design.md:67,73,110`; `crates/subc-core/src/supervise.rs:347-354,380-395,2023-2033`; `crates/subc-protocol/src/lib.rs:126-139`; `crates/subc-client-rs/src/lib.rs:521-532`; `crates/subc-core/src/forwarding.rs:168-174,280-282`; `crates/subc-core/src/registry.rs:38-44`
- **Confidence**: high
- **Old v2 delta closure**: **Not closed**
- **New gap introduced**: v3 says prefix registration is allowed only on a connection “owned by that module’s attested process,” but current machinery proves only bearer-nonce possession, and exact-vs-prefix collisions are unspecified.
- **Evidence**: today `reserved_hello_authorized` is exact-id lookup and returns `true` on miss. Spawn attestation is a single process nonce injected via env and echoed in HELLO. I found no daemon-side connection-owner field. Also the doc says both “one loopback connection per peer” and `fed:<peer>:<module>`, while same-connection re-registration currently evicts the old endpoint.
- **Why it matters**: the design does not answer precedence (`fed` vs `fed:`), delimiter rules (`fed:` vs `fedx:`), or how multiple fed-module connections are proven to be from the same attested instance. The one-connection-per-peer representation is also inconsistent with per-module namespaced ids unless clarified.
- **Concrete resolution**: specify delimiter-aware longest-match prefix rules; tag a connection with an owner identity after successful reserved/prefix HELLO; key future prefix authorization off that tag; and choose one topology explicitly: one synthetic module per peer, or one loopback connection per exported remote module.

## Finding 4: 6.1’s “accepted only after intent is durable” is incompatible with the current origin client boundary
- **Verdict**: BLOCKER
- **Location**: `docs/subc-federation-design.md:175-177`; `crates/subc-client-rs/src/consumer.rs:581-585,1174-1187,2219-2224`; `crates/subc-client-rs/tests/real_daemon.rs:294-303`
- **Confidence**: high
- **Old v2 delta closure**: **Not closed**
- **New gap introduced**: v3 relies on an acceptance point the plain subc client does not have.
- **Evidence**: the design says the fed-module reports accepted only after durable intent. But the current client classifies `OutcomeUnknown` once the writer path accepted the request, and `NotSent` only if the writer path/route-open failed before that. That boundary occurs before provider-level fsync. Tests confirm `OutcomeUnknown` is not auto-retried.
- **Why it matters**: if the writer accepts, then the fed-module crashes before durably recording intent, the origin still gets `OutcomeUnknown` and will not auto-retry — the exact lost-but-unretryable case v3 says it fixes.
- **Concrete resolution**: either add a protocol-level per-request accept ACK after durable intent, or move federation’s durable-send semantics into a federation-aware client API/wrapper instead of mapping them onto the current 4-variant client contract.

## Finding 5: 6.1 still lacks a sound replay-retention and effect-id durability contract
- **Verdict**: BLOCKER
- **Location**: `docs/subc-federation-design.md:175-179`; `crates/subc-client-rs/tests/real_daemon.rs:294-303`
- **Confidence**: high
- **Old v2 delta closure**: **Not closed**
- **New gap introduced**: the v3 ledger is bounded, but the resend horizon and id uniqueness rules are not.
- **Evidence**: the design requires server dedup retention “≥ the origin’s max legitimate re-send horizon,” but never defines that horizon. It also defines `effect_id = (origin_device_pubkey, monotonic_seq)` with no installation epoch or collision story if the origin DB is lost/reset while the device key persists.
- **Why it matters**: after GC, a legitimate replay can duplicate a remote mutation; after origin state loss, a fresh call can collide with an old `effect_id` and receive stale deduped outcome.
- **Concrete resolution**: define an explicit replay lease/TTL and server GC contract; specify post-expiry behavior as “do not redispatch, surface ambiguity”; and strengthen `effect_id` with a durable installation epoch or high-entropy component.

## Finding 6: 5.4 fixes the deputy split in principle, but the example harness value conflicts with current provider vocabulary
- **Verdict**: SHOULD-FIX
- **Location**: `docs/subc-federation-design.md:157-159`; `crates/subc-protocol/src/lib.rs:42-48`; `crates/subc-mcp/src/main.rs:1149-1156,3880-3890`
- **Confidence**: medium
- **Old v2 delta closure**: **Partially closed**
- **New gap introduced**: `fed:<peer>` is not obviously a currently-valid harness family.
- **Evidence**: the design proposes a local BindIdentity with a `fed:<peer>` harness marker. `BindIdentity.harness` is just a string, but the MCP shim documents the current provider contract as `opencode|pi|runner|mcp:<client>` and only auto-prefixes bare tokens. A `fed:...` value would pass through unchanged.
- **Why it matters**: AFT-class providers may reject or misclassify federated binds even though the confused-deputy issue itself is fixed directionally by making BindIdentity local-authored.
- **Concrete resolution**: settle a provider-compatible harness namespace before rollout, or extend the provider allowlist/registration story as part of federation.

## Finding 7: 5.3 mitigates post-pin silent replacement, but first-contact and rotation are still underspecified
- **Verdict**: SHOULD-FIX
- **Location**: `docs/subc-federation-design.md:151-153`
- **Confidence**: high
- **Old v2 delta closure**: **Partially closed**
- **New gap introduced**: the mitigation is prompt-based, not yet a complete trust-state machine.
- **Evidence**: v3 adds TOFU pinning plus an out-of-band verification code derived from both devices’ keys, and changed keys become loud re-verification events. But the doc does not say first-contact traffic/exposure is blocked pending verification, and it gives no legit-rotation ceremony.
- **Why it matters**: a malicious cloud can still win the first contact if the pair is usable before verification; later, “changed key” looks the same for attack and legitimate reprovision.
- **Concrete resolution**: make cloud-introduced peers unverified/non-routable until code comparison (or explicit user override), define a rotation ceremony, and specify exactly what the verification code hashes.

## Finding 8: 6.5 needs a raw-manifest filtering story before typed decode
- **Verdict**: NOTE
- **Location**: `docs/subc-federation-design.md:193-195`; `crates/subc-protocol/src/manifest.rs:36-37`
- **Confidence**: high
- **Old v2 delta closure**: n/a
- **New gap introduced**: the design says unknown roles/ops are excluded from exposure, but current typed manifests reject unknown role tags at deserialize time.
- **Evidence**: `ProviderRole` is a closed enum; unknown role tags fail serde decode.
- **Why it matters**: older peers cannot “exclude unknown roles from exposure” unless negotiation/filtering happens on raw data before normal manifest decode.
- **Concrete resolution**: specify that federation handshakes exchange raw capability docs (or a translated compatibility form) first, then decode only the filtered subset.

## Summary
- **Counts**: 5 BLOCKER, 2 SHOULD-FIX, 1 NOTE.
- **Delta status**:
  - **P1**: **not closed**.
  - **P2**: **not closed**.
  - **6.1**: **not closed**.
  - **5.4**: **partially closed**.
  - **5.3**: **partially closed**.
- **Overall verdict**: **NO-GO**.
- **Why**: the v3 doc correctly identifies the v2 failure modes, but the actual subc-core/client semantics still defeat three key claims: P1 assumes tool-granular route control that the wire does not have; P2 assumes prefix/owner semantics that current spawn-attestation state does not define; and 6.1 assumes an origin “accepted” boundary that the plain subc client does not expose. Those are design-level blockers, not implementation nits.
- **Must-fix before phase 0**: (1) narrow or redesign P1 so its scope matches provider-scoped routing, (2) freeze or explicitly re-register HELLO-captured fields under P1, (3) specify P2 prefix precedence, delimiter rules, connection ownership, and topology.
- **Can fix during later phases**: 6.1 replay lease/effect-id durability, 5.4 harness vocabulary rollout, 5.3 first-contact/rotation UX+state machine, 6.5 raw-manifest compatibility filtering. Also note that 6.4 is still an acknowledged transport dependency: the current `ClientHello` carries only nonce+role (`crates/subc-transport/src/auth.rs:25-28`), while the design correctly says device identity must be added (`docs/subc-federation-design.md:190-192`).
</COUNCIL_MEMBER_RESPONSE>

