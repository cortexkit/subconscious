---
task_id: bg_d7a707f4
agent: council-member
session_id: ses_09b6049f3ffe78gep5JPV22pwJ
parent_session_id: ses_09b61906bffeZMC8Uc32l89Pqb
status: completed
completed_at: 2026-07-15T07:35:20Z
---

[user] 2026-07-15T07:13:36Z
## Solo Analysis Mode
You MUST do ALL exploration yourself using your available read/search tools.
- Do NOT use task or any delegation tool under any circumstances
- Do NOT delegate to explore, librarian, or any other subagent
- Do NOT spawn background tasks
- Search the codebase directly — you have full read-only access to every file
- This mode produces the most thorough analysis because you see every result firsthand


## Analysis Intent: AUDIT

You are conducting an **audit** — your goal is to find discrete issues, risks, or violations.

**Focus:**
- Search for problems, anti-patterns, security risks, correctness issues, or violations of stated requirements
- Each finding must be a distinct, actionable item with concrete evidence
- Severity determines priority: critical (blocks/breaks), high (significant risk), medium (should fix), low (nice to fix)
- For each finding, provide the specific location (reference, section, or component where it occurs)
- State your confidence: high (clear evidence), medium (likely but needs verification), low (suspicion, investigate further)
- **This is a broad sweep, not a targeted trace.**

**Analytical standards:** Support claims with concrete evidence. State confidence (high/medium/low) for key assertions. Note caveats and limitations.

**Structure your response as:**
```
<COUNCIL_MEMBER_RESPONSE>
## Finding 1: [Title]
- **Severity**: critical/high/medium/low
- **Location**: [specific reference — e.g. component, section, endpoint, rule]
- **Confidence**: high/medium/low
- **Issue**: [what is wrong and why it matters]
- **Evidence**: [concrete reference, snippet, or observation that proves the issue]
- **Suggested Fix**: [actionable recommendation]

## Finding 2: [Title]
...

## Summary
[Total findings by severity. Overall risk assessment with confidence levels.]
</COUNCIL_MEMBER_RESPONSE>
```

## Analysis Question

You are a member of an ADVERSARIAL design-review council gating a concurrency-critical redesign of the `subc-core` daemon BEFORE any implementation. Your job is to HUNT for correctness defects, false invariant claims, and broken consumer contracts — not to praise the design. Assume the design is guilty until proven innocent. Every claim you make about SHIPPED behavior MUST cite file:line from the repo. The design is gated; a single un-refuted concurrency defect is a NO-GO.

## Repo & artifacts
Repo: `~/Work/Projects/CortexKit/subconscious` (subc-core Rust daemon). Design doc under review: `docs/subc-dispatch-redesign.md` (committed f3185c89). Read it in full first. Then verify its source claims against master.

## What subc is
A zero-deserialization loopback frame router. 21-byte envelope; the daemon never parses frame bodies. Per-route flow-control credits: Serial window=1, ModuleManaged=32, StatelessParallel=1024. Routes are epoch-fenced. Frames route client→module (Requests, credit-gated) and module→client (terminals release credit).

## Shipped behavior — VERIFIED anchors (re-verify, then extend)
- Per-connection read loop routes each frame TO COMPLETION before the next read: `crates/subc-core/src/server.rs:357-400` (`connection_loop`), routing awaited at `server.rs:370-374`.
- Routing a client Request awaits `route.flow.acquire().await` inline: `crates/subc-core/src/router.rs:465` (`ForwardBackend::handle_bound`), then awaits `route.module_sink.send(frame).await` inline: `router.rs:491`. On send failure after acquire, credit released: `router.rs:494-496`.
- Channel-0 control runs inline in the read path: `router.rs:207-218` → `handle_control_frame(ctx, frame).await` at `router.rs:214`.
- Module→client terminal path is non-blocking `try_send` + `route.flow.release()`: `router.rs:281-310` (release at `router.rs:307-309`, gated by `is_terminal_frame` at `router.rs:281`/`501-506`).
- Every data-frame lookup takes one process-wide `std::sync::RwLock` via `read_inner()`: `crates/subc-core/src/forwarding.rs:846` (`lookup_data_route`).
- `RouterError::to_error_frame` — the `RouteError{code,message}` variant emits a canonical JSON ErrorBody with an ARBITRARY code and needs only channel/epoch/corr from the envelope (no body parse): `router.rs:582-633`. This is the vocabulary the daemon-synthesized `cancelled` / `route_backpressure` / `control_backpressure` terminals would reuse.
- Consequence (verified): a saturated route blocks the whole connection INCLUDING the CANCEL that would relieve it (CANCEL sits unread behind a blocked `acquire`) — cancellation is structurally defeated on saturated serial routes.

## The redesign (summary — read the doc for the authoritative version)
1. Read loop never awaits route work: read, classify, hand off (sync, non-blocking).
2. Per-route bounded dispatch queue + one drain task each owning credit-acquire + module-send (moves B1 `flow.acquire` + B2 `module_sink.send` off the read loop).
3. CANCEL inspects the route's dispatch queue: still-queued Request → remove it + DAEMON synthesizes terminal `Error{code:"cancelled"}` for that corr (no credit was acquired, so no release; module never sees it). Already-delivered/unknown → forward CANCEL as today (module emits the cancelled terminal, which releases credit).
4. GOODBYE for a route: flush queue (drop queued frames), then today's epoch-fenced release + relay. Flush must precede binding release.
5. Per-connection FIFO control task (channel-0 offload) — a slow route.open stalls only later control commands, never data/CANCEL.
6. Queue overflow (Request) = fail-loud retryable `Error{route_backpressure}` for that corr (maps to SDK NotSent). Non-Request frames (CANCEL/GOODBYE) are never dropped for capacity — they inspect/flush the queue on the read loop (O(queue) scan, no await). Per-connection aggregate cap 4096 frames → protocol-error close.
7. Per-route `outstanding: HashSet<corr>` (insert on delivery to module, remove-once on terminal); release fires only if `outstanding.remove(corr)` returned true — makes credit release exactly-once, retiring the R11 duplicate-terminal double-release defect.
8. ArcSwap snapshot-published forwarding table: data-plane reads lock-free; mutations stay serialized under the existing write lock, then publish a fresh snapshot.
9. Two-merge rollout: merge-1 snapshot forwarding (claimed invariant-neutral standalone); merge-2 dispatch queues + control offload + R11 rider.

## YOUR HUNT (work each independently, cite source, then give a verdict)

1. MISSED INTERLEAVES. Enumerate concurrency windows and for each: prove safe against a specific existing semantic, or flag it. Cover at minimum: bind-commit vs snapshot-publish vs first-frame arrival; GOODBYE flush vs a concurrent enqueue into the same route queue; CANCEL racing the queued→delivered boundary in BOTH orders (CANCEL wins / Request-delivery wins); ANY window where BOTH the daemon-synthesized `cancelled` terminal AND a module terminal fire for the SAME corr (prove impossible or exhibit it); drain-task panic/leak; connection-close vs drain-task shutdown ordering; late enqueue after the queue sender is dropped/closed.

2. CREDIT ACCOUNTING — exactly-once acquire AND release across ALL exit paths: (a) delivered + module terminal; (b) queued + CANCEL-synthesized-cancelled; (c) queue flushed on GOODBYE; (d) connection death mid-flight (Request in queue, Request delivered-not-terminated); (e) module death mid-flight; (f) drain-task panic. For EACH, state whether a credit was acquired and whether it is released exactly once, and check the `outstanding` set insert/remove pairing. Find any leak or double-release.

3. SECTION 4 INVARIANTS — attack each claim against source. Especially: I3 (epoch-fenced release + escalation semantics "byte-identical, release paths untouched") — is that TRUE given the drain task now owns acquire and the outstanding-set now gates release? I6 (BufReader cancel-safety preserved) — does the new sync hand-off truly keep the "only cancel is connection close" property? I7 (module→client direction unchanged) — does the outstanding-set gate change this path's behavior on duplicate/late terminals?

4. BACKPRESSURE CONTRACT SHIFT. Previously-blocking sends (socket TCP backpressure) become fast retryable `route_backpressure` errors. Does any shipped SDK/consumer contract break? Look at the TS/Rust/Swift SDK error classifiers and retry logic, and the daemon-consuming patterns (broca, aft, alfonso-core if present in-repo). Is the NotSent mapping HONEST in every overflow case — i.e., is it guaranteed the Request never reached the module when `route_backpressure` is synthesized? Could a consumer that relied on implicit backpressure (bounded in-flight via blocking) now over-issue and get surprised?

5. ORDERING. Per-route Request FIFO across the queue — preserved? Per-connection control FIFO sufficiency: is route.open→route.close ordering actually safe when control is a single FIFO task but data frames are on separate per-route drain tasks (can a data frame for a route be processed before that route's route.open control command commits, or after its route.close)? Any CROSS-route or control-vs-data ordering the OLD single serial loop implicitly provided that a consumer could depend on, which the new multi-task design breaks?

6. DAEMON-SYNTHESIZED `cancelled` TERMINAL. Verify `RouterError::to_error_frame` (router.rs:582-633) genuinely supports emitting `code:"cancelled"` with zero body deserialization and correct channel/epoch/corr. Then: is a LATE `Error{cancelled}` harmless against the ACTUAL cancel-handling paths in all three SDK clients (find them)? Specifically, if the daemon synthesizes `cancelled` for a queued Request, but a race delivered it, could the module ALSO emit a terminal → does any SDK mishandle a second terminal for a settled corr?

7. SNAPSHOT STALE-READ WINDOWS. Enumerate every window where a data-frame reader loads a stale ArcSwap snapshot. For each, map it to an ALREADY-existing semantic (pre-commit → Absent drop; post-release → channel-gone drop). Flag ANY new observable state the old RwLock-serialized lookup could not produce (e.g., a reader seeing a route Bound that the write side already released, enqueueing into a queue whose drain task is mid-shutdown).

8. DoS MATH. Queue depth max(4, 2×window) (Serial=4, ModuleManaged=64, StatelessParallel=2048); per-connection aggregate cap 4096; O(queue) CANCEL scan on the READ LOOP per CANCEL. Can an adversarial client make read-loop work superlinear or expensive — e.g., fill queues to max then spray CANCELs to force repeated O(queue) scans, all on the latency-critical read loop? Quantify worst-case read-loop work per malicious frame.

9. ROLLOUT. Is merge-1 (snapshot-published forwarding) TRULY invariant-neutral as a standalone landing, given the read loop still awaits routing at that point? Does moving lookups from RwLock-read to ArcSwap-load, WITHOUT the dispatch queues, introduce any window (e.g. a lookup seeing a snapshot slightly staler than a same-thread control mutation would have)?

10. OPEN QUESTIONS Q1–Q5 (doc §8). For each lean, say whether it is RIGHT or WRONG and why. Q1 fail-loud route_backpressure vs per-route pause-set; Q2 daemon-synthesized cancelled vs forward-both+SDK tombstones; Q3 whole channel-0 FIFO vs route.open-only offload; Q4 R11 rider now vs defer; Q5 whole-table Arc swap vs per-shard.

## OUTPUT FORMAT
Produce PER-ISSUE findings with: a severity (BLOCKER / MAJOR / MINOR / OK), the specific interleave or contract, a source citation (file:line) for any shipped-behavior claim, and a concrete fix if it is a defect. Then a one-line member verdict: GO / GO-WITH-CHANGES (list the specific changes) / NO-GO (list the blockers). Be concrete and adversarial. Do NOT restate the design back; spend your tokens on finding what breaks. If you cannot verify a source claim, say so explicitly rather than assuming.

[assistant] 2026-07-15T07:13:37Z
I'm going to read through the design doc and cross-reference it with the actual source code to verify the claims being made.
I'll start by reading the design doc in full, then verify its claims against the actual source.

[assistant] 2026-07-15T07:13:49Z
I'm checking the design doc against the actual commit hash mentioned in the question—there's a discrepancy between what the doc references and what was actually committed. Now I need to trace through the key components: the connection loop in server.rs, the route lookup logic in forwarding.rs, how flow control acquire and release work, and the error classification implementations across the different SDKs.

[assistant] 2026-07-15T07:13:57Z
I'm examining the flow control implementation, particularly the credit acquire and release mechanisms, the teardown path, and how the module sink handles things. I should also look at the SDK error classifiers and trace through the cancel handling logic, plus review the forwarding release paths and the FlowControl struct itself.
Now let me examine the flow-control implementation, the release/teardown paths, and the CANCEL handling.

[assistant] 2026-07-15T07:14:04Z
I need to dig into the flow control release mechanism, how in-flight accounting works, and the ChannelFlow struct implementation. I'm also tracing through the SDK's cancel-handling logic across all three clients and examining the release_module_route and GOODBYE paths to understand the full lifecycle.
Let me examine the flow-control release path, the `ChannelFlow` struct, and the SDK cancel-handling paths.

[assistant] 2026-07-15T07:14:12Z
I need to understand how the SDK clients process incoming terminals and manage correlations—specifically what happens when a terminal arrives for an already-settled correlation, and how the response dispatch, error classification, and retry mechanisms work across the different SDKs.
Now let me examine how the SDKs handle incoming terminals (and duplicate/late terminals for a settled corr), plus the error classifiers/retry logic.

[assistant] 2026-07-15T07:14:21Z
I need to dig into how the TypeScript client classifies error codes from the daemon—specifically ones like `route_backpressure` and `cancelled`—and understand how it handles terminal error frames for in-flight requests. I'm also looking at the retry logic and the NotSent contract, particularly how retryable codes get classified when a Request returns an Error terminal.

[assistant] 2026-07-15T07:14:29Z
I'm tracing through how the TypeScript client handles incoming error frames from the server and determines which ones are retryable. I need to understand the flow from the read loop that dispatches error frames, through to the managed call logic that classifies error codes and decides whether to retry, particularly looking at how terminal error responses map to the NotSent state.

[assistant] 2026-07-15T07:14:38Z
The grep search came up empty for `route_backpressure` and `control_backpressure` across the SDKs, which contradicts the design's claim that these codes would integrate into existing error classification paths. I need to trace through the TypeScript client to understand how error frames get routed to pending requests and what triggers the retry logic in the managed `call()` method.

Looking at the flow, when the daemon sends an `Error` frame with `route_backpressure`, the TS SDK classifies it as a `terminal` kind rather than `not_sent`, and retry logic only fires for `not_sent` errors. This means `route_backpressure` would be treated as non-retryable, which directly contradicts what the design claims about it mapping to the existing NotSent contract and riding the error-classification paths.
The grep is revealing: **no SDK references `route_backpressure` or `control_backpressure` at all**. Let me verify how an incoming Error frame terminal is classified in the managed call path (the retry decision at line 435).

[assistant] 2026-07-15T07:14:48Z
I need to trace how an incoming Error frame with a route_backpressure code gets routed to a pending request and what SubcCallError classification it receives. The managedRequest function should show me how this error gets categorized in the managed call path.
The design claims `route_backpressure` "maps to the existing NotSent contract" and "rides existing error-classification paths." But no SDK references it. Let me verify exactly how an incoming Error-frame terminal settles a managed request — what `kind` it gets.

[assistant] 2026-07-15T07:14:59Z

[assistant] 2026-07-15T07:15:02Z
I'm tracing through how an Error frame gets dispatched and resolves a pending request—specifically, when the readLoop receives a terminal Error frame, I need to find where it gets converted into a rejection rather than a successful resolution.
Now let me find where an incoming Error-frame terminal settles a pending request — this determines what `kind` a `route_backpressure` Error gets.

[assistant] 2026-07-15T07:15:14Z

[assistant] 2026-07-15T07:15:17Z
I'm tracing how Error frames get handled differently from write/deadline failures. When an Error frame arrives, it bypasses the `classifyFailure` logic and instead goes through `errorFromFrame` to determine the error type. Let me look at what `errorFromFrame` actually does.

[assistant] 2026-07-15T07:15:23Z

[assistant] 2026-07-15T07:15:26Z
I'm tracing through how error frames get classified when they arrive. When a `route_backpressure` error comes in, it gets dispatched as a `FrameType.Error`, which calls `errorFromFrame` to create a plain `SubcError` with that code rather than wrapping it in a `SubcCallError`. This rejection then propagates through the promise chain that was set up in `sendManaged`.

Once it reaches `managedRequest`, the catch handler checks if it's a `SubcCallError` — it's not, so it wraps the error through `terminalCallError`, which creates a `SubcCallError` with kind `"terminal"` and extracts the original code. Back in `call()`, the retry logic checks for specific conditions like `unknown_channel` or `not_sent`, but since this error has kind `"terminal"` and code `route_backpressure`, it doesn't match any retry condition and just throws the error to the caller. This means the managed call doesn't actually retry on `route_backpressure` — it treats it as a terminal failure, which contradicts what the design documentation claims about it being retryable.

The design says the SDK classifier needs to be updated to add `route_backpressure` to the retryable set, but that's an additive change that contradicts the stated goal of zero SDK modifications. More fundamentally, as the code currently ships, a daemon-synthesized error frame becomes a terminal error that never gets retried and never maps to the `not_sent` contract the design describes. The core issue is that the SDK doesn't automatically map `route_backpressure` to `not_sent` even though the request genuinely never reached the module—it gets rejected at admission. This is a consumer-contract defect that violates the design's claim of zero SDK changes and transparent retry behavior, so I'm rating it MAJOR against the audit criteria.

The shift from implicit backpressure (old behavior, transparent) to explicit terminal errors (new behavior, fail-loud) means consumers relying on managed retry now face unexpected hard failures instead of automatic retries under load.

Now I need to verify the Rust SDK's classifier behavior and check for over-issue concerns, plus confirm how all three SDKs handle daemon-synthesized `cancelled` messages for already-settled correlations. Looking at the TypeScript SDK specifically, when a late `Error{cancelled}` arrives for a correlation that's already settled, the dispatch logic checks for pending waiters and finds none, then harmlessly logs and drops the duplicate terminal frame. So even if both the daemon and module send terminal signals for the same correlation, the first one settles it and the second is safely discarded.

But there's a key distinction I need to clarify: if the daemon synthesizes `cancelled` for a queued Request that never reached the module, then the module never emits a terminal at all—there's only one terminal signal. The double-terminal race condition only occurs when the Request actually gets delivered to the module AND the daemon simultaneously synthesizes cancelled. I should examine that specific race scenario more carefully in the CANCEL section.

I also need to dig into the credit-release path, the R11 `outstanding` set, and verify the I3 claim, plus confirm how the daemon constructs the `Error{code:"cancelled"}` frame via the RouterError mechanism.

Now I'm hitting a critical concurrency issue: the I3 claim states that epoch-fenced release and escalation semantics are byte-identical with release paths untouched, but section 3.7 introduces an `outstanding.remove(corr)` gate that gates the release path itself. That's a direct contradiction—the release path is no longer untouched; it now only fires if that removal succeeds.

The real problem emerges when I trace through the R11 rider interaction with the existing over-release guard in forwarding.rs. If a duplicate terminal arrives, release() gets called twice, and the second call could decrement in_flight erroneously if other requests are still in flight, causing the aggregate counter to exceed the window. The outstanding set should prevent this, but the inconsistency between what I3 claims and what 3.7 actually does needs to be resolved.

Now I'm realizing the concurrency issue: the outstanding set gets inserted on the drain task (client-side route) when delivery happens to the module, but removal happens on the module's connection read loop—two different tasks accessing the same HashSet without synchronization. The design claims confinement to the route's own state with no global lock, but that's only true if both operations happen on the same task.

There's a critical race condition here: the drain task acquires credit and sends the frame to the module, then inserts the correlation ID into outstanding. But if the module processes the frame instantly and emits a terminal that arrives back on the module→client path before the insert completes, the remove call will fail and the credit never gets released—a permanent leak. This is a classic time-of-check-time-of-use bug where the insert happens after the send, giving the terminal a window to race ahead.

The safe ordering would be to insert before sending to the module, which creates a happens-before relationship ensuring remove always finds the entry. But the design description in §3.7 says "inserted on delivery to module by the drain task," and the pseudo-code in §3.2 shows the acquire and send but doesn't clarify where the insert actually occurs in that sequence.

If an implementer reads "on delivery" as after the send completes, that creates a race condition where the module could emit a terminal and trigger removal before the insert happens — a real credit leak. This is a major design ambiguity that must be pinned down: the insert has to happen before the send call, not after.

There's also the concurrency of the set itself — the drain task inserts per client route while the module-to-client path removes, so these operations are genuinely concurrent and need proper synchronization like a Mutex or DashMap, not just hand-waving about "route's own state."

Now looking at the I3 escalation path: when try_send fails on a terminal frame, the code returns early without releasing credit, and the ordering between remove and try_send is unclear — if remove fires before try_send fails, the correlation is gone but the credit release logic depends on remove returning true, creating another potential leak.

The escalation closes the connection anyway so the credit becomes moot, but this interaction with the early return contradicts the claim that release paths are untouched. I need to document this.

Next I'm examining the connection-close versus drain-task shutdown interaction, particularly around the GOODBYE flush and concurrent enqueue operations, then tracing how routes are actually released during teardown today.

I'm verifying the outstanding set concurrency model — confirming that inserts happen in the client drain task while removes happen in the module connection's read loop, making these cross-task operations. Then I'll look at the Rust SDK's error classification and how it maps daemon error codes, plus the endpoint drain and release paths.
This is a critical finding on the SDK classification path. Let me verify the Rust SDK classifier and dispatch, plus the teardown/release paths and the R11 `outstanding` set interaction with the module→client credit release.

[assistant] 2026-07-15T07:16:35Z
I'm realizing the critical distinction: when the Rust SDK receives an Error frame marked as terminal, it treats it as a module-level error rather than a retryable condition—so a `route_backpressure` Error frame synthesized by the daemon would be classified as `CallError::Module`, not `NotSent`. The TypeScript SDK follows a similar pattern with how it processes incoming Error frames.

This directly contradicts the design document's claim that new retryable codes would map to the existing NotSent contract through SDK classifiers. There's actually no such classifier hook for arbitrary Error-frame codes in the request path—both SDKs classify incoming Error frames as terminal/module errors regardless of the specific code, which means they won't be retried.

The design acknowledges that `route_backpressure` needs to "join the retryable set in both SDK classifiers" through additive config (line 233), but this directly conflicts with the stated goal of "zero SDK changes required" (line 52) and the claim that the NotSent mapping needs "nothing required" (line 232). The document is internally inconsistent about whether SDK changes are actually needed.

The core issue is that the existing SDKs have no mechanism to inspect an inbound data-channel Error frame's code and reclassify it as retryable — the only code-based retry classification (`isRetryableRouteOpenCode`) exists only on the route.open control path, not on the request terminal path. So "additive config" alone won't work; the data-frame error dispatch logic would need to be modified to check the error code against a retryable set and remap the kind to not_sent. I need to verify whether the TS SDK actually has any such code path that I'm missing.

Checking the TS flow: an inbound Error frame goes through dispatch → errorFromFrame → SubcError → reject → managedRequest catch → terminalCallError with kind "terminal", and only `err.code === "unknown_channel"` gets special retry treatment. `route_backpressure` falls through as a terminal error without retry. Now I need to check the Rust consumer's retry logic to see if it handles `CallError::Module` with specific codes differently, and also verify whether there's a head-of-line ordering issue if the daemon sends a synthesized `cancelled` terminal while a real terminal races through egress.

Looking at the module→client path in the router, the `outstanding` set would be per-route and managed by the drain task on delivery, but this is a separate code path from the drain task itself. The release currently calls `route.flow.release()` directly, but under the redesign it needs to check `outstanding.remove(corr)` instead. The key question is whether the module-side `RouteBinding` (looked up via `module_to_client`) is the same binding object where credit release happens, or if it's the client-side binding (`client_to_module`).

Since `ChannelFlow` is shared between the client and module route halves as an `Arc`, the question becomes whether the `outstanding` HashSet would also be shared across both halves or kept separate per binding.  The real concurrency issue is that the insert happens on the client-side drain task after delivery completes, while the remove happens on the module→client path — two different execution contexts that need synchronized access to the same set, which means the set itself needs to be wrapped in a Mutex to prevent races and ensure memory visibility.

Now I need to check the RouteBinding struct and how the flow Arc is shared between the module→client route binding and the client-side route to understand if they're actually operating on the same synchronized state.
Critical SDK finding confirmed. Now let me verify the `RouteBinding` structure (whether `flow` and any `outstanding` set is shared between client/module halves), the teardown/release paths, and the Rust consumer retry logic.

[assistant] 2026-07-15T07:17:16Z
I'm realizing that `RouteBinding.flow` is an `Arc<ChannelFlow>` shared between the client and module sides, and the module→client terminal path releases credit through `route.flow.release()`. If I implement R11 with an `outstanding` HashSet per route, it would need to be shared between the drain task that inserts entries on delivery and the module→client terminal path that removes them—which means I'd need another lock protecting that mutable set. Now let me think through the Rust implications of this approach.

I need to verify that the client→module binding (using `client_to_module` lookup) and the module→client binding (using `module_to_client` lookup) actually share the same `Arc<ChannelFlow>` for credit accounting to work correctly. Let me trace through the bind commit path that creates both bindings, and also check whether the consumer's call retry loop retries on Module error codes and whether the `is_terminal_frame` check properly gates the `outstanding` set removal—though the module→client path releases credit on any terminal frame regardless.

The real issue is that the `outstanding` set is supposed to be per-route, but it's unclear whether the module→client terminal path (which uses the `module_to_client` binding) can actually reach this set, since the drain task and terminal path are different tasks operating on the same set and need synchronization. I should also examine how connection close teardown and endpoint draining work.
Let me verify the shared-flow invariant (both route halves must share the same `Arc<ChannelFlow>`), the bind-commit path, and the Rust consumer's retry loop.

[assistant] 2026-07-15T07:17:33Z
Looking at how the Rust consumer handles Module errors in its retry loop to see if Error frames trigger retries.
Let me find the Rust consumer's main `call` retry loop to see how a `Module(ErrorBody)` terminal is handled — is it ever retried, or always surfaced as terminal?

[assistant] 2026-07-15T07:17:39Z

[assistant] 2026-07-15T07:17:43Z
I'm tracing through the data-plane request path to confirm that Module errors don't trigger retries—the retryable_route_open_code logic only applies to route.open calls on channel 0, while data-plane requests hit an Error terminal that converts directly to CallError::Module without retry logic. Let me verify this by checking the into_call_result conversion.

[assistant] 2026-07-15T07:17:50Z
When a `route_backpressure` Error terminal arrives in the Rust consumer, it's treated as a `CallError::Module` — a terminal module error rather than a `NotSent` error — which means it won't be retried in-place.

The TypeScript implementation follows the same pattern: the error gets classified as a `terminalCallError` and thrown without retry. So the design's claim that `route_backpressure` maps to the existing `NotSent` contract and joins the retryable set is actually false against the shipped code — there's no code path that classifies an arbitrary Error-frame terminal code as retryable.

The only retryable terminal classifications that actually exist are `unknown_channel` (both clients) and `is_retryable_route_open_code` (only on the route.open path for a closed set of four codes, excluding `route_backpressure`). A `route_backpressure` Error frame arriving as a terminal for a data-plane request gets classified as a permanent application error and surfaced to the caller without retry, directly contradicting the design's claims. This is a broken consumer contract — the design says "zero SDK changes required" but SDK changes are actually necessary for the retryable semantics to work, which is at least a major defect.

I'm also checking what happens when the daemon synthesizes a `cancelled` error for a queued request — specifically what the client that issued the CANCEL does with it. lock it too to remove items, creating a critical section where both operations serialize. That's the synchronization point that prevents the double-terminal race — but the design needs to be explicit about this locking strategy to avoid the scenario where CANCEL reads a stale queue snapshot while the drain task is mid-delivery. So the CANCEL gets lost in that narrow window where the drain task has already dequeued the Request but is blocked waiting for flow credit—the module hasn't received it yet, so forwarding CANCEL to the module is a no-op (unknown correlation ID), and then the drain task eventually acquires credit and delivers the Request anyway. This is the exact race condition the design was supposed to prevent. module. B runs. CANCEL(B) was lost.

The dispatch queue actually creates a third state that the design doesn't account for: a request that's been dequeued but is still waiting at `flow.acquire().await` in the drain task, not yet delivered to the module. CANCEL messages for requests in this state are lost because they're no longer in the queue (so no synthetic terminal fires) and haven't reached the module yet (so forwarding the CANCEL does nothing). This is a genuine concurrency defect—the binary "queued vs delivered" model is incomplete.

On a Serial route with window=1, this gap is exactly the R5 scenario the redesign was supposed to fix. The drain task pops the request first, then waits for credit, then sends to the module. Between the pop and the send, there's an unprotected window where cancellation fails. The design's definition of "still queued" doesn't include requests held by the drain task pre-delivery, so this state falls through the cracks entirely. This could be a blocker because it defeats the primary goal of the redesign. The real fix would require the queue and credit system to work together—keeping the request visible in the queue while acquiring the credit, then removing it only when actually sending to the module. But the current design with recv-then-acquire doesn't implement this coordination, so the lost-cancel window persists as specified.

In the reverse scenario where CANCEL finds and removes the request from the queue before the drain task pops it, there's no double-terminal issue since only one operation can succeed with a shared lock—either the scan removes it or the drain pops it, not both.

The real danger emerges if CANCEL targets a request that's already been delivered to the module and is being processed. The read-loop won't find it in the queue, so it forwards the CANCEL, the module emits a cancelled terminal, and the credit gets released—that's the expected single-terminal case. The double-terminal only happens if the daemon synthesizes cancelled for something that also got delivered, but with exclusive remove-or-pop semantics under a shared lock, that shouldn't occur unless the ArcSwap snapshot staleness allows a data frame to be enqueued after the scan.

Now I need to check how the outstanding-set interacts with the credit acquire/release cycle. The design tracks delivered requests in a HashSet, removing them once a terminal arrives, and only fires the release if that removal succeeds. The acquire happens in the drain task, but the release happens in the—

wait, that's in the module-to-client terminal path, which is a different task. So the outstanding set needs to be shared between them with proper synchronization. The critical timing issue is whether the insert into outstanding happens before or after the send completes—if it's before, then any arriving terminal will find the entry and remove it correctly, but if it's after, a fast module could trigger the terminal path before the insert happens, causing the remove to fail and the release to never fire.

The design intent says insertion happens "on delivery to module" but doesn't specify the exact ordering relative to the send call. This has to be insert-before-send to avoid the leak, which is a major constraint that needs to be explicit. And the claim about release paths being untouched is wrong—the release is currently unconditional at that location, gated only by whether it's a terminal frame.

The redesign changes this to gate release on whether `outstanding.remove(corr)` succeeds, which is a real modification to the release path. The design document even acknowledges this in section 3.7, so calling it "byte-identical" and "untouched" is contradictory and false. The invariant claim needs to be corrected, and more importantly, this new gating mechanism reintroduces the insert-ordering hazard I flagged earlier. I3 has a major issue here—both the false invariant claim and the ordering vulnerability it creates.

Now looking at I7's claim that "Module→client direction unchanged." The redesign actually adds the `outstanding.remove(corr)` gate directly into that path (the terminal release in router.rs), so that's also false. On the duplicate terminals issue: today a second terminal triggers `route.flow.release()` again, causing a double-release. R11 has a guard against this at forwarding.rs where it checks if `in_flight == 0` and returns early to prevent over-release, so the existing mechanism already protects against that scenario.

But wait — the guard only prevents `available_permits` from exceeding the window when `in_flight` is already zero. Consider the actual case: window is 32, 5 requests in flight. A duplicate terminal for correlation X releases once (in_flight drops to 4, permits increase by 1). The same duplicate terminal releases again (in_flight drops to 3, permits increase by 1) — but only 4 distinct requests are actually in flight now. So `in_flight` undercounts and we've added an extra permit, effectively growing the window to 33. The guard only catches the degenerate case where everything is idle. So R11 is a real defect when `in_flight > 0`, and the outstanding-set fix with remove-once correctly prevents it.

But here's the critical issue for the audit: I7's claim that the path's release semantics remain "unchanged" is false — the semantics DO change with this fix. It's not a defect per se since the change is beneficial, but it's a FALSE INVARIANT CLAIM that can't be accepted at face value. There's also a subtle concurrency risk: the outstanding set lives per-route on the shared `Arc<RouteBinding>`. Both the drain task on the client→module path and the terminal on the module→client path reference the same binding's outstanding set, so that set needs mutex protection. Assuming it's properly specified, I'd flag I7 as a false claim — MINOR to MAJOR severity.

Now I need to examine the GOODBYE flush versus concurrent enqueue interaction.

The design claims queue flush must precede binding release so no frame can enqueue after flush, both happening under the route-teardown path in the drain task's shutdown. But the actual concurrency is messier: enqueue happens on the read loop, GOODBYE flush also runs on the read loop, yet the drain task is concurrently consuming from the same queue. That's three actors contending on one route's queue — the read loop as producer, the drain task as consumer, and the release path — and the thinking cuts off mid-analysis of whether GOODBYE flush actually runs where the design claims. doesn't acquire credit until after popping from the queue, so dropped frames don't need credit release. But there's a contradiction in the design: the spec says flush happens on the read loop in section 3.5, yet section 3.6 describes it as part of the drain task's shutdown path — I need to clarify whether flush is actually on the read loop or triggered during drain task shutdown.

The real issue is that if flush happens in the drain task shutdown while enqueue still runs on the read loop, a data frame could slip in after the drain task starts flushing but before the sender is dropped, creating a race condition. The safe approach is to move flush to the read loop (producer side) to prevent this window. I'm also tracking how connection-close and drain-task shutdown interact, especially when a late frame tries to enqueue after the sender is already closed — that would fail with a channel-gone drop.

Now I'm examining the snapshot staleness scenario more carefully: a data frame reads a snapshot showing the route as Bound, but by the time it tries to enqueue, the write side has already released and dropped the queue sender. The try_push fails and maps to a channel-gone drop, which is observable today. The design assumes try_push on a closed bounded mpsc sender returns Err, which is correct. But there's still a window where the snapshot shows Bound while the receiver is already gone. The single read loop on the client side ensures frames are processed in order, so GOODBYE is handled after any pending requests, preventing late enqueues. The real concern shifts to the module→client direction, where the module's read loop might send a terminal frame after the route has already been released on the client side.

Now I'm thinking through the control task versus per-route drain ordering. When route.open commits, it spawns the drain task and publishes the snapshot, but if a data frame arrives before that commit completes, the snapshot still shows the route as absent or reserved, which gets dropped as existing behavior. The new issue is that control and drain are now separate tasks, so there's potential for ordering mismatches between when the route is considered open and when data frames start flowing through. The managedRequest path is safe since it awaits the route.open response before sending the request, but raw clients that pipeline could break — this is a real ordering regression where the old serial loop guaranteed control commits happened before data frames, but the redesign breaks that guarantee. It's flagged as minor-to-major severity depending on whether any in-repo consumers actually pipeline. Now I need to think through route.close as well.

For route.close, both the open and close operations go to the control FIFO task so they're ordered, but data frames for that route live on the read loop, which means a data frame could theoretically be processed after route.close commits and the route is released — the snapshot would show it as absent and drop it. The design relies on the client awaiting these operations, which should prevent issues. I'm also looking at potential DoS vectors: with a StatelessParallel queue depth of 2048 and a per-connection cap of 4096, an adversary could fill the queue and then spray CANCEL messages, each triggering an O(2048) scan on the read loop — so N CANCELs would create N × 2048 work.

Each CANCEL frame is only 21 bytes, giving the attacker roughly 2048× amplification in scan steps per frame. With two routes each holding 2048 items, a single CANCEL could scan up to 2048 entries, and sustained CANCEL spam would force the read loop to perform 2048 comparisons for every 21-byte frame, blocking all other data and CANCEL processing on that connection — which directly undermines the read-loop responsiveness the design was meant to protect.

GOODBYE frames trigger the same O(queue) flush scan, and since the design treats non-Request frames as never dropped for capacity and processes them on the read loop, an adversary can send unlimited GOODBYE/CANCEL frames, each incurring O(queue) cost. The fix would be to index the queue by correlation ID so CANCEL becomes O(1) removal, or cap the scan cost per frame. This is a major issue — a self-inflicted read-loop DoS that contradicts the core goal.

The scan really is O(queue) because removing a queued Request by correlation ID requires scanning the FIFO without a secondary index, and the design explicitly acknowledges this O(queue) scan with no await.

On a StatelessParallel route with 2048 depth, this becomes a real concern, so a correlation-index is strongly recommended. The cancel-safety property holds because the redesign moves routing to a sync, non-blocking try_push operation, eliminating any new await point that could be cancelled mid-read — the only cancellation point remains connection close.

However, the critical dependency is that try_push must genuinely be non-blocking with a bounded channel; if the control queue or per-route queue ever awaits on a full channel, both cancel-safety and read-loop non-blocking guarantees break.

Now I'm mapping out the credit accounting exit paths systematically. When a request is delivered and the module reaches terminal, we acquire once in drain and release once when outstanding.remove fires — that's clean if insert-before-send ordering holds. For queued requests that get synthetically cancelled, there's no acquire since they never left the queue, so no release needed. But the tricky third state is when a request has been popped and is parked waiting on acquire in drain — if cancel arrives here, the module sees no-op, but then when the request eventually delivers, the module terminal tries to release a credit that was never acquired.

The real problem emerges in the R5 scenario: a long-running request A is in flight at the module while B is pipelined behind it. When the client cancels A, the read loop scans the queue but A isn't there anymore since it's already been delivered to the module — so the cancel gets forwarded downstream instead of being handled locally.

The fix works for this case: the module receives the cancel, terminates A, releases the credit, and B's acquire finally returns. But there's a narrower gap: if the client cancels B instead (the queued request parked on acquire), that cancel still gets lost because B never reaches the module to receive it. This is less common but still a real defect, so I'm marking it as MAJOR rather than BLOCKER since the primary R5 case is resolved.

The core issue is nondeterministic—whether a cancel works depends on an unobservable race condition: has the drain already popped B off the queue? If B is still queued, the cancel works correctly. If B is already popped and parked on acquire, the cancel gets forwarded to the module but the module ignores it, and B runs anyway when credit becomes available. That's the broken case.

Now looking at what happens when the queue flushes on GOODBYE: queued requests never get acquired, so there's no release to worry about. But a request that's already popped and parked on acquire during GOODBYE is still stuck in the drain task. The acquire call returns an error when the flow closes, so no permit is taken and nothing needs to be released—the key is ensuring teardown actually closes the flow to unblock any parked acquire calls. I should verify that the release path closes the ChannelFlow, though the drain-task panic backstop provides some protection here. For connection death mid-flight, when a request has acquired credit but hasn't terminated yet, the route release needs to properly drop that credit accounting when the flow closes.

When the binding Arc is dropped, the semaphore goes with it—no leak, just freed memory. Module death mid-flight works similarly: the endpoint drain releases routes and abandons in-flight credits with the binding. The drain-task panic guard is the critical piece—if it fails to fire, a live binding with a dead drain creates a silent wedge, which the design acknowledges as a known risk mitigated by a drop-guard that mirrors the broca coordinator-actor pattern.

The panic backstop is described as a pattern rather than formally proven. A panic after acquiring a credit but before sending it leaks that credit since the Request never gets delivered and no terminal ever fires. However, if panic triggers a full route teardown (dropping binding and flow), the leak becomes moot because the flow is gone—the client's Request is lost with no terminal, so it times out with an unknown outcome. That's acceptable degradation. Now looking at Finding 9 with the merge-1 snapshot forwarding running standalone while the read loop still awaits routing.

Switching from RwLock-read to ArcSwap-load for lookups is invariant-neutral. Today, control mutations take the write lock and data lookups on the same connection's read loop take the read lock, keeping them serialized. With ArcSwap, a lookup might load a snapshot published slightly before a mutation that a read-lock would have caught, but since control still runs inline on the read loop in merge-1, the route commit and publish happen immediately before the next frame's lookup loads the fresh snapshot—preserving read-your-writes ordering for that connection. Cross-connection behavior is where things get interesting.

For a module's terminal lookup versus a client's bind across different read loops, the shared RwLock never guaranteed ordering beyond the mutation's atomicity itself. ArcSwap preserves that the commit is visible after publish, but introduces a bounded staleness window: the mutation applies to canonical state under the write lock, then the snapshot publishes. Between apply and publish, a concurrent reader might load the old snapshot and see pre-mutation state, whereas RwLock would block that reader until the write lock released and they'd see post-mutation.

This staleness is observable in specific scenarios. For bind operations, a reader sees Absent slightly longer and might drop a data frame that RwLock would have routed, but on merge-1 the reader for the newly-bound client route is the same connection that issued the bind inline, so it can't observe the gap. For module-to-client delivery, a module terminal arriving as the client route binds could load a snapshot without the new binding, but that's contradictory since the binding's existence implies the module already knew the route. For release, a reader loading a pre-release snapshot sees Bound and tries to deliver to a route the write-side just released — RwLock would serialize and drop it, but ArcSwap could deliver one frame into a route that was just released.

When that frame reaches client_sink.try_send, the sink might still be alive since release doesn't instantly kill it, so the frame could be delivered to the client after release. This could be observable as a frame arriving on a channel the client already closed via GOODBYE, but the client-side dispatch drops terminals with no waiter or epoch mismatch, so it's benign. The design's consistency argument covers pre-commit and post-release states, but the apply-to-publish staleness is the subtle case. On merge-1 without queues, "channel-gone" via sender-close doesn't exist yet since there's no queue sender; the frame goes straight to module_sink or client_sink via the old await path, creating a stale—

Now with the bound read on merge-1, a frame delivered into module_sink of a route being released means the module gets a frame for a route it's tearing down. The existing code already handles this through module-side epoch checks, so it's likely benign, but it is a new interleave that the RwLock serialization used to prevent. I'm flagging this as MINOR: merge-1 isn't strictly invariant-neutral because it introduces a bounded stale-Bound-after-release read window. Whether it's actually observable depends on sink liveness and epoch fencing, so I should recommend an explicit argument and test rather than just treating it as mechanical. Now I'm moving through the open questions verdicts to give quick leans on each one, starting with finding 6 about daemon-synth cancellation and the to_error path.

Looking at how _frame supports arbitrary error codes passed through from the router, with zero body deserialization confirmed—it builds an ErrorBody from the code and message while passing the envelope channel, epoch, and correlation ID. That's mechanically supported. For a late cancelled error on an already-settled correlation, the TypeScript side finds no pending entry and either calls late() if it's in lateResponses or logs a dropped terminal frame. In Rust, I need to verify that settle_pending for an unknown key just no-ops, and that a second terminal for a settled correlation is guarded by the pending map lookup so it doesn't double-settle.

The real concern is credit double-release on the daemon side, which the outstanding-set addresses. So this is mechanically OK as long as the SDK's double-terminal handling is safe. I should verify Rust settle_pending idempotency and confirm that an unknown correlation's terminal is safely dropped, then check whether the release path properly closes the flow on teardown.
The SDK classification finding is significant. Let me verify a few more critical paths: Rust `settle_pending` idempotency (double-terminal safety), whether teardown closes the flow (parked-acquire case), and the Swift terminal/error handling.

[assistant] 2026-07-15T07:21:18Z
I need to examine how `release_module_route_locked` and `release_client_route` handle the `flow.close()` call during teardown, especially for the scenario where a drain task gets parked waiting on an acquire operation.

[assistant] 2026-07-15T07:21:23Z
So when a route is released, calling `route.flow.close()` closes the semaphore, which causes any parked `acquire().await` to return an error. This means the drain task won't hang if the route gets torn down while it's waiting to acquire the semaphore. But I need to think through what happens to the frame that was in flight when the drain task gets unblocked by that error.

There's a critical race condition here with CANCEL. The drain task pops a request from the queue and then parks at `acquire().await`, but meanwhile CANCEL arrives on the read loop and scans the queue. Since the request was already popped, CANCEL doesn't find it in the queue and forwards it to the module per the rule. But the request hasn't actually been delivered to the module yet—it's still parked waiting for flow capacity. So the module receives the CANCEL for a request it doesn't know about yet, drops it as unknown, and then when the request finally gets delivered, the cancellation is lost.

The design's binary rule (in queue → synthesize, not in queue → forward) doesn't account for this intermediate state where a frame is popped but not yet delivered. This is a real defect that the design claims to prevent.

Now I'm considering whether this could produce a double terminal instead. If CANCEL arrives while the frame is still in the queue, it gets removed and a synthesized error terminal is created for that correlation.

But here's the race condition: the drain task might have already popped that same frame via recv() just before CANCEL scans the queue. These operations should be mutually exclusive if queue removal is atomic, but the read loop and drain task are separate threads accessing the queue concurrently. The design assumes CANCEL can scan and remove from the queue, but a standard tokio mpsc channel doesn't support that kind of scan-and-remove operation from the producer side.

So the design would need a custom queue implementation—something like Arc<Mutex<VecDeque>> with a Notify primitive—to let the read loop inspect and remove while the drain task pops. Even with that synchronization, though, the fundamental window remains: once drain pops a Request under the lock and releases it to await the acquire operation, that frame vanishes from the deque before it's delivered, and CANCEL can't find it anymore. The fix would require the drain task to handle the Request delivery before releasing the lock, but that's not what the current design does.

The real solution involves either recording in-flight frames in a "processing" set that CANCEL can still see, acquiring credit before removing from the queue, or checking a cancelled-set after acquire and dropping the frame if it was already cancelled—but that last approach creates a double-terminal risk if CANCEL synthesizes a cancellation while the frame is parked and drain delivers it anyway. Section 3.3 glosses over this popped-but-undelivered state entirely, which is a critical gap.

Now examining the double-terminal scenario in the "already delivered" branch: if CANCEL scans the queue and doesn't find the request, it forwards CANCEL to the module, which receives it for an already-delivered request, cancels it, and emits a terminal—single terminal, credit released. But if the request is parked at acquire (popped but not yet delivered), CANCEL won't find it in the queue, forwards to the module which no-ops, and then drain delivers the request anyway—no double terminal, just a lost cancellation. The design claims to prevent the case where daemon synthesizes cancelled for a queued request AND the module emits a terminal, which would require the request to be both removed from the queue by CANCEL and delivered to the module, but if the queue is mutex-guarded and CANCEL removes it under lock before drain pops it, drain won't find it.

The key insight is that removal must be atomic and the "synthesize" decision and queue-removal must happen under the same lock hold—if CANCEL removes under lock, drain can't also get it, making the double-terminal impossible. The real vulnerability is the lost-cancel window when the request is parked at acquire.

Now checking the GOODBYE-flush against concurrent-enqueue: since both happen on the same read loop for a given connection, they're serialized for that client→module route. But the DRAIN task pops concurrently, so GOODBYE's flush races against drain's pop and deliver operations.

If drain has already popped a frame and is waiting at acquire or mid-send when GOODBYE flushes, the flow.close() will unblock acquire with an error, causing drain to drop the frame. But if drain already passed acquire and is sending to the module, that frame gets delivered before the GOODBYE relay arrives at the module.

The module then processes the frame and emits a terminal, but since the route is gone on the daemon side, the terminal gets dropped as unroutable. The client already settled locally on GOODBYE, so this is probably acceptable. The credit for that frame is technically "leaked" since the binding's flow was closed and won't release it, but since the entire ChannelFlow is discarded anyway, it doesn't matter in practice.

Now checking the drain-task panic and abort-guard behavior... The dead receiver case is distinct from backpressure—when the drain task panics, the binding stays live but the route becomes wedged, and the design acknowledges this needs an abort-guard to release it, though that's currently unimplemented. Now I'm checking whether the epoch-fenced release semantics still hold given that the outstanding-set now gates the release path.

The credit release in the terminal frame forwarding is being modified by R11 to gate it on the outstanding set, so the claim that "release paths untouched" is actually contradicted by section 3.7 which explicitly changes how credit release works. The epoch-fence binding-release might be separate and untouched, but the wording is ambiguous and the credit-release path is definitely modified—that's an internal inconsistency worth flagging.

For I7's claim that "Module→client direction unchanged," that's also false because 3.7 adds the outstanding-set gate directly to the module→client terminal forwarding path where release happens. The set is inserted when the terminal is delivered to the module and removed once on the terminal, which is exactly on this path. This matters because a late or duplicate terminal will now behave differently than before—the unchanged claim doesn't hold.

With the new set in place, a duplicate terminal will return false from outstanding.remove and skip the release entirely, leaving it forwarded but credit-inert as intended. But there's a deeper issue: what if the first terminal for a correlation was never inserted into the outstanding set in the first place? Insertion happens at delivery (one per Request), removal at terminal, so they should pair up. But consider a Request that gets delivered and then—

the outstanding set is per-route, shared between the drain task that inserts and the module's connection read loop that removes, so it needs synchronization. The real concern is ordering: can a terminal try to remove a correlation before the drain task has even inserted it?

If the insert happens after module_sink.send() returns, there's a race window where a fast module processes the request, emits a terminal, and calls outstanding.remove(corr) before the drain task's insert(corr) executes — remove returns false, no credit gets released, and the correlation leaks forever in the set while the credit stays consumed.

The fix is to insert the correlation into outstanding strictly before calling module_sink.send(), so the terminal's remove always finds it. The design's wording "inserted on delivery to module by the drain task" is ambiguous and doesn't guarantee this ordering, which means a naive implementation will leak credit for fast modules. This is a major ordering defect that needs to be explicitly fixed by ensuring the insert happens-before the send, and while a Mutex-protected HashSet provides the synchronization guarantee, the design itself must be clear about this constraint.

Now I'm checking the backpressure contract — the design claims route_backpressure maps to the existing NotSent contract and that new retryable codes integrate into the existing error-classification paths. I'm verifying this across the TypeScript implementation to see how error frames are classified.

Tracing through the code: when an Error frame with route_backpressure arrives, it gets classified as a SubcError (not SubcCallError), which means managedRequest routes it to terminalCallError with kind "terminal" rather than "not_sent". This means route_backpressure errors are classified as terminal and won't be retried — contradicting the design's claim that it maps to the NotSent contract.

Looking at the Rust implementation, Error frames become CallError::Module, and the retry loop only handles unknown_channel and route-related cases, so route_backpressure would surface as a terminal error to the caller. This breaks the retryable NotSent contract the design promises and requires an SDK change, which contradicts the "nothing required" claim. _backpressure and cancelled to the data-path retryable set. But this raises a deeper question: is this a correctness defect or just a missing SDK change? The design explicitly claims zero SDK changes are needed and that it uses existing paths, which I've now shown is false. The consumer would hit a hard terminal error instead of getting an automatic retry. While that might be acceptable for a fail-loud retryable where the caller can manually retry, the stated contract—that it "maps to NotSent" and "requires nothing"—is demonstrably broken. This is a major finding around false contract claims and broken consumer guarantees, potentially a blocker given how the gate treats unrefuted defects, though this is a contract violation rather than a concurrency issue.

I'm rating this MAJOR since it undermines the design's core selling point. Now I need to check what happens with a late `cancelled` terminal—specifically whether it's harmless if the request has already settled. The dispatch logic checks whether the key is still pending; if it's not, the frame either goes to lateResponses or gets dropped entirely, which means the caller never sees the error.

Both TypeScript and Rust handle a second terminal for an already-settled correlation idempotently—TypeScript's settle() uses a single-winner guard, and Rust removes the entry from the pending map on first settlement, so a duplicate terminal just returns harmlessly. Swift likely follows the same pattern. So even if the parked-at-acquire lost-cancel window did produce a second terminal, the SDKs would drop it safely.

But the real issue remains: the lost cancel means the request runs uncancelled, which is a genuine semantic regression from the design's T3 claim. Now I need to verify Swift's behavior, check the error classification, work through the DoS math and snapshot windows, and examine the control-plane FIFO ordering concern—specifically whether data frames can be processed before route.open commits, since the redesign offloads channel-0 control to a per-connection control task while data frames are looked up via snapshot on the read loop.

The critical problem is that a client can send route.open followed immediately by a Request on the new channel, and in the old design both run serially on the read loop so the Request always sees the bound route. But in the new design, route.open goes to the control queue while the Request hits the read loop snapshot lookup potentially before the bind is committed, causing the Request to be dropped or error with unknown_channel. This breaks a working client pattern of pipelining a Request without awaiting the route.open response—though the design assumes clients await each control response and SDKs single-flight route.open, so I need to verify whether any SDK actually does pipeline like that.

Looking at the consumer SDK, route.open returns a handle after the RouteOpen response is installed, and managedRequest uses a cached handle. The TypeScript client's cachedRouteHandle also awaits the open. So SDKs do await, but the old daemon allowed pipelining without awaiting due to the serial loop guarantee. A hand-written client or future consumer could rely on that ordering contract, which the design explicitly acknowledges as an accepted assumption. This is a real narrowing of the ordering guarantee—I'll flag it as MEDIUM/MAJOR.

Now I need to verify whether the SDKs actually await before finalizing the rating. I'm also checking the control-path FIFO for route.open and route.close on the same channel—both are ordered there. But route.close sends a GOODBYE frame, and the design routes GOODBYE on the read loop (the data path, not control) according to section 3.3. Looking at the router code, GOODBYE for a route is handled as a data-channel frame on the module data path.

So route.open lives on the control task while GOODBYE lives on the read loop—different tasks entirely. Could GOODBYE process before the route.open that creates it? Only if the client sends open then immediately goodbye without awaiting, which is pathological and wouldn't matter anyway since the route wouldn't exist yet. The real ordering concern is between route.open (control task) committing the bind and spawning the drain task versus a data Request (read loop) trying to load the snapshot—if the snapshot hasn't published yet, the request gets Absent and drops, creating a window where things could go wrong.

Now I need to verify the SDK await behavior and Swift implementation, check the DoS implications, and validate the merge-1 standalone claim.
The SDK classification path is a major finding. Let me verify the Swift SDK's terminal/error handling, the SDK await-before-data behavior, and confirm the retryable-code sets are control-plane-only.

[assistant] 2026-07-15T07:23:37Z
I'm noticing the Swift SDK doesn't actually reference `route_backpressure` or `cancelled` in retry logic—the "cancelled" mentions in the UI code are just color mappings. I need to trace how Swift handles terminal errors on incoming requests and whether any retry mechanism kicks in, plus verify that `isRetryableRouteOpenCode` is only applied at the route level.

Looking at the JavaScript side, when an Error frame comes through in `dispatch`, it rejects with a plain `SubcError` rather than a `SubcCallError`. That error then gets caught in `managedRequest` and converted to a terminal `SubcCallError`. In the `call()` method, the retry logic checks for specific error codes like `unknown_channel` or error kinds like `not_sent` and `outcome_unknown`, but `route_backpressure` doesn't match any of those conditions, so it falls through without retrying.

The design documentation claims that `route_backpressure` "rides the existing error-classification paths" and that SDKs need "nothing required" to handle it as retryable. But that's not accurate — there's no data-request error-code classifier in any SDK at all. The only retryable classifier is `isRetryableRouteOpenCode`, which only applies to the control path and doesn't include `route_backpressure`. So in practice, `route_backpressure` surfaces as a terminal, non-retryable error to callers, not as a NotSent error like the design claims. case. The SDK maps route_backpressure to a terminal error instead of NotSent, so consumers treat transient overflow as permanent failure and won't retry — actually worse than the old blocking behavior. This is the core backpressure-contract-shift defect. And there's a secondary risk: clients that previously relied on implicit TCP backpressure to throttle pipelined requests could now over-issue since the error handling changed.

Let me trace through the concurrency-window issues more carefully — specifically the GOODBYE flush timing against concurrent enqueues and the double-terminal window scenario.

The design specifies that CANCEL inspects the queue: if the target request is still queued, it removes it and the daemon synthesizes a cancelled response; if already delivered, it forwards the CANCEL downstream. The assumption is these are mutually exclusive because the read loop owns the queue as the single source of truth for queued versus delivered state.

But there's a race condition here — the CANCEL inspection in the read loop and the DRAIN task's dequeue-and-deliver operation run concurrently on different tasks, so while the read loop is scanning for a particular correlation ID, the drain task could be simultaneously dequeuing and delivering that same request.

This creates a classic time-of-check-to-time-of-use window: the read loop scans and finds the request still in the queue, decides to synthesize a cancelled terminal, but between that scan and the removal, the drain task has already popped the request and is delivering it to the module. The result is both a daemon-synthesized cancelled terminal and a module-emitted terminal for the same correlation ID — exactly the scenario the design claims is impossible.

For the design to be safe, the scan-and-remove operation must be atomic with respect to the drain task's pop operation.  This requires both operations to contend on a shared mutex protecting the queue structure. However, a standard tokio mpsc channel doesn't support inspecting or conditionally removing arbitrary elements — you can only pop from the front. So the dispatch queue would need to be a Mutex-wrapped VecDeque that both the drain task and the read-loop CANCEL handler lock when accessing it, but the design doesn't specify this synchronization and hand-waves the performance implications of an O(queue) scan.

Even with a shared mutex in place, there's still a race window: the drain task pops the request under the lock, releases it, then awaits on flow acquisition and the module sink send. Meanwhile, the CANCEL handler locks the queue, doesn't find the request (already popped), and forwards the CANCEL to the module. Now CANCEL and the in-flight Request race to reach the module, creating the double-terminal problem. ial path through the drain task, which would restore ordering guarantees. If CANCEL bypasses that and goes directly to the module while the Request is still parked on flow.acquire(), the module receives CANCEL first, drops it as unknown, then processes the Request uncancelled — reintroducing the exact cancellation-loss bug the redesign was meant to prevent.

Now I need to verify how the outstanding set interacts with the shared flow and the module→client terminal path, which currently executes in the route_for_connection context.

The outstanding HashSet is shared mutable state between the client-side drain task (which inserts) and the module-side read loop (which removes), so it needs synchronization. But there's a critical ordering hazard: the drain task sends the Request to the module, then inserts the correlation ID into outstanding, while the module could process and emit a terminal response so quickly that it arrives and removes the correlation ID before the insert even completes. If remove executes before insert, the release gets skipped. If insert happens after send, the credit leaks because the terminal's remove call won't find the correlation in the outstanding set. The acquire ordering is correct—it happens before send—but insert must also precede send to be safe. However, there's a contradiction in the spec: I2 claims the release path is now "enforced" by the outstanding set check, while I3 insists the "release paths [are] untouched," but gating release behind outstanding.remove() clearly modifies the release path, so both statements can't be true.

The module-to-client terminal forwarding at router.rs:307-309 is itself a release path that's being changed, making I3's "byte-identical" claim false for the credit-release portion. The spec conflates two different meanings of "release"—credit-release (which is changing) versus route-release/teardown (which may be unchanged)—and this ambiguity is worth flagging as a precision issue in the gate checklist.

Now looking at the drain-task panic scenario: if the drain task panics after acquiring credit but before sending, that credit gets stuck because the abort-guard only releases the route via teardown, not the acquired-but-unsent credit itself. The design accepts this as a tradeoff—a single panic kills the entire route—but it means credit can leak in this failure path.

For ordering between route.open and data frames: data frames can only be dispatched once lookup_data_route returns Bound, which requires the bind commit to publish the snapshot first. Since bind commit happens in the control task during route.open, the control task naturally orders before any data frame dispatch.

The protocol itself enforces this ordering — clients await the route.open response before sending data on the new channel, so by the time data arrives at the daemon, the snapshot is already published. This hinges on the assumption that all SDKs actually await control responses before depending on them, which the Rust consumer does by installing the route handle only after the RouteOpen response resolves.

Now I'm wondering about route.close ordering when a GOODBYE message arrives while data frames are still being drained by the route's drain task. The design says GOODBYE runs under the route-teardown path in the drain task's shutdown, but I need to figure out whether GOODBYE arrives on the control channel or on the route's data channel itself.

Looking at the current implementation, route GOODBYE is actually handled in the data path—it comes in as a non-Request frame on the route's own data channel and gets processed by the read loop. This means the GOODBYE flush happens on the read loop while the drain task is concurrently popping from the same queue, creating the same TOCTOU race condition as CANCEL. I need the shared-lock atomicity to handle this safely.

Now I'm examining the snapshot stale-read issue. The design assumes that when a data frame loads a pre-release snapshot, it can't enqueue into a queue being flushed because the flush-then-release ordering plus the queue-sender drop prevents late enqueues. But if the queue is actually a shared Mutex<VecDeque> (which it needs to be for CANCEL scanning), there's no "sender closed" semantics—enqueue is just a lock and push operation.

This reveals a fundamental inconsistency: CANCEL and GOODBYE require a scannable, removable shared structure like Mutex<VecDeque>, but the overflow and late-enqueue-fail semantics assume an mpsc channel model with try_push and sender-closed behavior. These two sets of operations are mutually exclusive under their respective implementations, which is a critical design coherence defect. The queue abstraction is under-specified and can't cleanly satisfy both requirements.

A Mutex<VecDeque> with Notify could handle everything, but then "sender closed" becomes a closed flag checked under the lock, and any stale-snapshot enqueue-after-release must check that flag atomically with the flush. The consistency argument in the design doesn't actually hold for this required structure.

Now I'm working through the DoS math for CANCEL. With a StatelessParallel queue capped at 2048 and an aggregate connection limit of 4096, an adversary could fill the queue and then spray CANCELs for non-existent correlations, forcing O(queue) scans on each one.

Each CANCEL frame is only 21 bytes, so an attacker can pack many into a single TCP segment, but each one triggers a scan of up to 2048 entries on the latency-critical read loop. That's a serious per-frame amplification—21 bytes in, thousands of comparisons out—and it blocks all other channels' data on that connection while the read loop is busy.

The old design handled CANCEL in O(1) time, so this is a major regression. The fix is straightforward: index the queue by correlation ID using a HashMap so CANCEL lookups become O(1) instead of O(queue). The current design explicitly specifies an O(queue) scan, which is a self-inflicted DoS vulnerability.

GOODBYE has the same O(queue) flush problem, but it only happens once per route teardown, so it's far less exploitable than CANCEL, which can be repeated indefinitely without changing state. Let me also double-check that the error frame generation for "cancelled" works correctly with an empty body. ct for a client-facing terminal. The synthesized cancelled needs to go to the client via their egress with their channel/epoch/corr, and since the queued frame preserves the client's original values, the daemon can read those off correctly. Now I'm checking whether SDKs handle receiving a second terminal for an already-settled correlation ID — the TypeScript implementation uses object identity to guard against duplicate settlement, so a second terminal just gets logged as a dropped frame with no waiter and doesn't cause issues.

The real concern is correlation ID reuse: if the client's allocator recycles a corr before a late second terminal arrives, that terminal could settle the wrong request. But TypeScript uses monotonically increasing allocation that never wraps until u64 max, so within a single connection there's no reuse risk. Rust does the same with monotonic corr IDs, and Swift likely follows the same pattern, so the late double-terminal scenario is harmless across all SDKs.

The actual danger shifts to the daemon side—specifically credit double-release when the outstanding-set doesn't maintain proper ordering. That's where the real risk lives, not in SDK handling. For the QUEUED case though, no credit was acquired initially, so if the race condition delivers it and the daemon synthesizes another...

Actually, let me trace this through: the drain task acquires credit and inserts the correlation into outstanding, the module runs it and emits a terminal, which removes the correlation and releases the credit. That's balanced—one acquire, one release. The daemon also synthesizes a cancelled terminal, but that doesn't touch credit. So credit stays balanced even with two terminals reaching the client; it just drops whichever one arrives second. The credit defect only surfaces if the daemon thinks no credit was acquired while the drain task actually did acquire it, but even then the module's terminal release keeps things balanced.

The real vulnerability is the insert-after-send ordering leak. And thinking through the happy path: if CANCEL wins the race and removes the item from the queue before drain pops it, no acquire ever happens, no release is needed, and outstanding never sees it—perfectly balanced. But if the item is already delivered and CANCEL gets forwarded to the module, the module cancels and emits a terminal that removes the correlation and releases the credit.

The actual hazard is when CANCEL reaches the module before the item does—the module drops the CANCEL, then later runs the item uncancelled. That's not a credit leak but a lost-CANCEL defect worth tracking.

Now checking the merge-1 invariant with the ArcSwap change: I'm moving the route lookup from an RwLock read to an ArcSwap load, which breaks the serialization between control mutations and data lookups. The risk is a lookup seeing a stale snapshot compared to what a same-thread control mutation would have observed.

The design says apply-then-publish under the write lock, but I need to verify whether publish happens inside or after the lock releases. If it's inside, readers never see a snapshot older than the last committed write. If it's after, there's a window where a mutation has been applied but the snapshot hasn't been published yet, and a concurrent lookup could see the pre-mutation state.

Even with publish inside the lock, a reader who loaded the Arc before the publish completes will see the old snapshot—that's just latency. The real concern is whether a reader could see a route marked as Bound in a stale snapshot when the canonical state has already released it. In that case, the reader would try to enqueue into a route being torn down, but the existing error handling for module_sink send failures should catch this. The flow.close() call during release_client_route_locked is where this gets tricky with a reader holding a stale Bound snapshot—calling route.flow.acquire() on a closed semaphore returns ChannelFlowClosed, which maps to a backend error frame sent to the client. In the old RwLock model, the reader would've seen the route already gone and silently dropped it as Absent. So merge-1 introduces a behavior change: instead of a silent drop, we're now sending an error frame to the client when acting on a stale Bound route—which contradicts the "invariant-neutral" claim.

The window is narrow and the magnitude small, but it's a genuinely new observable state the old lock-serialized lookup couldn't produce. The design's consistency argument in §3.8 only covers the pre-commit and post-release-with-queue-sender-drop cases; it doesn't address the post-release-flow-closed scenario that merge-1 creates. This directly answers questions #7 and #9—merge-1 isn't strictly invariant-neutral. Now I'm reconsidering whether the old model could've produced flow-closed in some other path.

Actually, the old model could produce this too. The lookup and handle_bound aren't atomic—lookup grabs the read lock, returns a cloned Arc<RouteBinding>, releases the lock, and then handle_bound calls flow.acquire. Between those steps, a concurrent release could close the flow, so even the old model allows a reader with a cloned Arc to call acquire on a just-closed flow and get ChannelFlowClosed. The "stale Bound → flow closed" observable already exists today; merge-1 doesn't introduce it, just widens the window where it can occur. Now I'm checking whether the module's read loop and the client drain task operate on the same route's outstanding set. The module connection runs on its own task, while the client drain happens on the client's connection task, so both are accessing a shared per-route structure—that requires synchronization via a per-route Mutex or DashMap. The design claims "no global lock" confinement, which is technically true, but it glosses over the fact that a per-route mutex is still needed and will be contended between these two tasks. It's not a real defect, just imprecise framing about what "confined" means here.

I'm now verifying the module-to-client terminal path and then moving on to check the Swift error handling and whether there's an existing retryable-data-error classifier I might have overlooked.
The SDK classification gap is the sharpest finding. Let me confirm the Swift request-terminal path and rule out any existing data-request retryable-code classifier.

[assistant] 2026-07-15T07:26:24Z
The Swift SDK lacks retry logic entirely and `SubcError` only carries a message without error codes, so I need to examine how the `remoteError` method surfaces these errors. Now I'm looking at the daemon-consuming patterns across broca, aft, and alfonso-core to understand their backpressure handling and verify whether the outstanding set is per-route and how it interacts with the R11 mechanism.

I'm also checking the abort-guard pattern for the drain task, the module-side ingress through handle_bind, and crucially the GOODBYE handling on the client route path since the design's flush-then-release depends on it working correctly.

[assistant] 2026-07-15T07:26:38Z
I'm working through the concurrency behavior when a route is released via GOODBYE while the drain task is waiting at `flow.acquire().await` — specifically, what happens to the frame that was about to be acquired when `flow.close()` gets called and returns `ChannelFlowClosed`.

There's also a critical race condition with daemon-synthesized `cancelled` terminals: when CANCEL arrives, the read loop scans the queue to find and remove the still-queued Request, but the drain task is concurrently calling `queue.recv()` on that same queue. Both paths could be accessing the same frame simultaneously, which creates a boundary race between queued and delivered states.

The design glosses over this by saying the queue supports "inspecting and removing" operations, but a standard mpsc channel doesn't allow random-access removal — you'd need a shared `Mutex<VecDeque>` instead. That shared lock between the read loop and drain task creates a window where CANCEL could remove a correlation from the deque while the drain task is popping it, and without atomic check-and-remove semantics, both the synthesized `cancelled` terminal and the module's actual terminal could fire for the same correlation. This is exactly the "both terminals" race the hunt is trying to prove impossible or exhibit, and the design's mutual exclusivity claim depends on atomicity that isn't actually guaranteed. There's a narrower race condition I'm seeing: the drain task pops a message under lock but then gets blocked waiting for flow credit, leaving the message no longer in the queue but not yet delivered to the module. If CANCEL arrives during this window, it won't find the message in the queue, forwards the cancellation to the module, and the module ignores it as an unknown correlation—then when the message finally gets delivered, the cancellation is lost. This is the exact "CANCEL overtakes" failure the design was meant to prevent, just shifted to a different point in the pipeline, and I can see it confirmed in the no-op handler for unknown correlations.

On the GOODBYE flush side, both the read loop's enqueue and GOODBYE handling happen on the same per-connection read loop, so they're serialized for the client—each frame processes one at a time.

But here's the issue: when a module-side GOODBYE or endpoint drain triggers a route release via `release_client_route` or `release_module_endpoint_routes`, that runs on a different task (the module connection's read loop or supervisor). Now that module-side release races with the client-side enqueue into the same route.

The design claims the flush-then-release plus queue-sender-drop handles this — late enqueues fail because the sender's closed, mapping to a channel-gone drop. But there's a race window: the client read loop snapshots the route as Bound and enqueues, while concurrently the module-side release drops the queue sender. If the enqueue happens after the sender drops, try_send fails and the frame gets dropped (fine). If it happens before but the frame's still queued when flush runs, it gets flushed and dropped anyway. Either way the frame is dropped and credit was never acquired. But what about a frame the drain task already pulled and acquired credit for, mid-send, when the module-side release closes the flow?

Since flow.close() is called during release and the terminal will never arrive (the module route is gone), the credit gets leaked — but the flow itself is dropped anyway once both route halves are removed and the drain task releases its Arc reference, so the leak is bounded to the route's lifetime. The outstanding set entries remain on forced teardown but the state is dropped, which is acceptable. I think I have enough to write a strong adversarial review now.

Let me verify the connection aggregate cap math and check whether the queue-inspect versus drain-pop atomicity is properly specified — that's the real blocker, along with the CANCEL-loss window when popping while parked on acquire. I also need to confirm I3's claim about release paths being untouched, because the R11 rider actually adds an outstanding-set check before calling release, which does modify the release path and contradicts what I3 said.

Looking closer, I3 is probably referring to the route-teardown release (the epoch-fenced `release_client_route`/`release_module_route`), which is separate from the credit `flow.release()` that R11 gates. So there's some ambiguity in the wording, but I'll flag that as a minor issue. Now I need to confirm whether the outstanding-set is per-route and who's responsible for inserting and removing entries.

The drain task inserts into the outstanding set when delivering to the module, and the module→client path removes entries once they're terminal — but that removal happens on the module's read loop, which is a different task entirely. This means the outstanding set is shared between two concurrent tasks, so it needs proper synchronization. There's also a potential race condition: the module could process and emit a terminal response that arrives on the read loop before the drain task has even inserted the correlation ID into the outstanding set. The drain task needs to insert into the outstanding set atomically with or before sending to the module sink. The per-route set needs synchronization with a Mutex, which contradicts the "no lock" cost claim in the design. And the insert-before-send ordering is actually a hard correctness requirement that isn't documented. More critically, the credit acquire happens before send while release only fires on a successful remove from the outstanding set — if the terminal arrives before the insert, the remove fails and release never runs, leaving the in_flight counter incremented with no permit returned, causing a permanent window shrink.

Now I'm organizing the findings to finalize the response. I've got the queue-inspect atomicity issue with the CANCEL-loss window and the design shift from mpsc recv to O(queue) scan that creates the vulnerability.

The second blocker is the outstanding-set insert-before-send ordering problem across tasks — if the terminal arrives before the insert completes, the remove fails and the permit gets leaked, causing a permanent credit shrink. The set is shared across tasks so it needs synchronization per route.

The third major issue is that route_backpressure isn't classified in the SDK contract at all. Error frames become terminals in the client implementations (TypeScript, Rust, Swift) without retry logic, but the design claims it maps to existing error paths — that's not how it's actually shipped, so SDK changes are required. The retryable classification for route open codes is also incomplete.

The fourth problem involves the flow.acquire() parked state during shutdown. Dropping the queue sender alone won't wake a task waiting in acquire — only calling flow.close() does that. If the release ordering doesn't close the flow, the drain task stays parked and leaks until the flow is explicitly closed, contradicting the design's assumption that dropping the sender is sufficient.

The fifth issue is a race condition where both the request delivery and a daemon-synthesized cancellation can reach the client, creating a double-terminal for an already-settled correlation. TypeScript's dispatch drops the second one, but this needs verification across all SDKs. The real issue is that the client gets told a request was cancelled but the module actually executes it anyway — a semantic violation even though the SDK handles the double terminal harmlessly. MINOR 6 is about ambiguous wording in the spec around whether epoch-fenced route teardown and credit release are the same thing. For the snapshot stale-read case, the merge-1 scenario mostly maps to existing error states, though standalone it would need the read loop to keep awaiting routing while lookups switch from RwLock to ArcSwap.

The stale-read window is low risk because control mutations and data lookups are already separate code paths, and the consumers that need read-your-writes guarantees stay on the lock by design. On the same connection, control is inline in the read loop so mutations and lookups serialize naturally, with the ArcSwap publish happening under write lock before control returns — meaning the next data frame sees the published snapshot. Cross-connection reads could see staler snapshots, but cross-connection ordering was never guaranteed anyway.

Now I'm looking at a DoS vector: an attacker could fill the queue then spray CANCEL frames, where each CANCEL triggers an O(queue) scan on the read loop. With StatelessParallel allowing up to 2048 queued entries, a single CANCEL could scan all 2048 entries, and if an attacker sends N cancels that's N×2048 work all on the latency-critical read loop — roughly 2048× amplification since each 21-byte CANCEL frame triggers a 2048-element scan. This is a real DoS risk the design introduces, and the mitigation would be to index the queue by correlation ID using a HashMap instead of scanning linearly.

I'm also reviewing some open questions about the design: the fail-loud route_backpressure approach is wrong as specified because SDKs don't classify it, which is a major issue requiring SDK work the design doesn't account for; daemon-synthesized cancellations are reasonable but hit a blocker around the timing window; the whole channel-0 FIFO ordering is correct from route.open to route.close; and the R11 rider has an insert-before-send hazard that's a blocker.

The whole-table Arc swap approach is right for read-mostly workloads. Given blockers 1 and 2 plus the SDK contract issue, this is a no-go. Time to write this up.
I have gathered strong evidence across the daemon and all three SDKs. Let me verify two final points: whether the outstanding-set remove runs on a different task than the insert (cross-task hazard), and the terminal-path release site.

[assistant] 2026-07-15T07:28:17Z
I've traced through the key code paths and verified the critical sections: the read loop completes fully in server.rs, handle_bound manages credit acquisition and sending inline with proper release on failure, control frames are handled directly, and the terminal path uses try_send with release semantics.

Now I'm examining the central claim about route_backpressure classification—the design asserts it's retryable and maps to the existing NotSent contract, but looking at the TS client implementation, incoming Error frames are rejected directly without the retryable classification logic that the design claims should apply. path error frames, so `route_backpressure` becomes a `CallError::Module` that's not retryable either—it surfaces as a terminal module error to the caller.

Swift has no retry or NotSent classification at all, so an Error terminal just throws directly. Searching across all three SDKs for `route_backpressure` or `control_backpressure` returns nothing—these codes have no landing spot in any shipped client. The design claims "zero SDK changes required" and that backpressure "maps to the existing NotSent contract," but both assertions are demonstrably false against the actual implementations.

The real problem is that overflow now surfaces as a non-retryable terminal error in TypeScript and Rust, and a plain error in Swift—breaking the consumer contract by turning what should be transient into a hard failure. This is actually a regression: today, TCP backpressure keeps the request queued until it goes through, so the caller never sees a spurious error; post-redesign, queue overflow triggers an immediate terminal or Module error. Even if NotSent were reclassified, the claim that "the request never reached the module" only holds if the daemon actually drops the frame at admission.

There's also a major race condition with daemon-synthesized `cancelled` errors. The design says CANCEL checks the queue—if queued, remove it and synthesize an error; if delivered or unknown, forward the CANCEL and let the module emit the terminal. But when CANCEL arrives right at the queued-to-delivered boundary, the drain task and the CANCEL handler are separate, creating a window where both could emit terminal errors. The real issue is that tokio's standard mpsc channel doesn't support scanning or selectively removing elements—once a frame is received by the drain task, it's gone from the queue's perspective. So the design assumption that CANCEL can inspect and remove items from the dispatch queue doesn't actually work with a standard bounded channel; you'd need a custom queue implementation to support that O(queue) scan and arbitrary removal.

If we use a shared Mutex<VecDeque> instead, the scenario becomes clearer: the drain task locks, pops the correlation, unlocks, then awaits flow acquisition and sends to the sink. Meanwhile CANCEL scans the queue, doesn't find the already-popped item, and forwards the cancellation to the module—resulting in just one terminal emission. But if CANCEL finds the item still in the queue and removes it, it synthesizes a daemon error locally instead.

The critical gap here is that the design never explicitly specifies the queue structure or locking strategy needed to make inspect-remove and pop mutually exclusive. Without that atomic guarantee, the safety proof collapses—and a plain mpsc channel won't provide it. This is the core unproven concurrency assumption I need to flag as a major blocker.

Now I'm tracing through a double-terminal scenario: the drain task pops a request X and blocks waiting for credit, while a CANCEL arrives for that same X. The read loop scans the queue, doesn't find X (it's already popped), and forwards the cancel to the module. But X hasn't actually been delivered yet—it's still parked at the acquire point. The module drops the unknown cancel, then later when credit frees, drain delivers X and it completes normally instead of being cancelled. This exposes a race where cancellation can be lost if it arrives between pop and delivery.

The real issue is that the design assumes a binary state—either a request is queued or it's been delivered—but there's actually a third state in between: popped from the queue but not yet sent to the module. The CANCEL handler misclassifies this limbo state as "already delivered" and forwards the cancel to a module that's never seen the request, causing the cancellation to be silently dropped.

For a Serial route with a single window, this is especially critical—when a long-running request A blocks request B, and B gets popped and parked waiting for A's credit, that's exactly when CANCEL(B) gets lost. This is a blocker-level defect because the entire redesign's core promise is that cancellation works under saturation, and this window reintroduces the exact failure it was supposed to prevent.

The fix is to have the drain task register the request in an "outstanding" or "delivering" set before popping it from the queue, so the CANCEL handler can still find it in that intermediate state.

But there's another contradiction emerging: the design claims release paths remain untouched, yet the new outstanding-set gate and the shift of acquire to the drain task both modify the release path. These two statements can't both be true. The "byte-identical" claim is false because the gate was added, which constitutes a change to the release path — calling this MAJOR because a false "untouched" invariant in a gate checklist is exactly what needs catching to avoid skipping review of modified code. I'm also identifying a cross-task synchronization issue: the outstanding-set gets inserted by the drain task during client→module delivery and removed by the terminal path on module→client delivery, but the insert/remove ordering can invert, creating a potential race condition. credit never released. The drain task calls `module_sink.send(frame).await`, so the insert must happen-before that send is observable to the module—if I insert before sending, the module can't receive the frame before the insert completes, making it safe. The spec says "inserted on delivery to module" which is ambiguous; if read as "after send returns," it creates a race condition and leaks credit. The design must explicitly specify insert-before-send, with the acquire happening before send and the insert between them. This is a major spec ambiguity that could cause credit leaks if implemented naively.

Now looking at the GOODBYE flush versus concurrent enqueue problem: the spec says queue flush must precede binding release so no frame can enqueue after flush. For client-initiated GOODBYE, both the flush and the enqueue happen on the same client read loop, so they're serialized—no concurrency issue there. But module-initiated release creates a different scenario that needs careful analysis.

When the module side releases a route (either via GOODBYE at the router or through endpoint drain), that runs on the module's read loop concurrently with the client's read loop still trying to enqueue data frames. The snapshot design means the client can load a stale view showing the route as Bound even after the module has released it and dropped the queue sender. So a late enqueue attempt hits a closed sender, which the design handles by dropping the frame. The key question is whether this causes a credit leak: the Request that fails to enqueue never acquired credit in the first place since credit acquisition happens in the drain task, which is already gone. So no credit leak there. But I need to trace through what happens to the client Request when it fails to enqueue.

Currently, a client Request to an absent route gets an UnknownChannel error frame, and a stale epoch gets an EpochMismatch error. Under the redesign, a Request that loads a stale-Bound snapshot and fails to enqueue on a closed sender would silently drop instead of generating an error frame — that's a behavior change for the request that raced the release.

The key difference: today, the lookup happens under the write lock's serialization, so if the release committed before the lookup, the client gets an error frame (UnknownChannel or backend error). If the lookup happens before the release, the request forwards and blocks on acquire, then the release closes the flow, causing acquire to return ChannelFlowClosed, which triggers a backend error frame back to the client. Either way, today a request racing release always gets a terminal error frame. Under the redesign, that request would hang until timeout instead. The issue is that for module-initiated GOODBYE (when the module dies or drains), the client hasn't settled locally yet—it still has pending Requests waiting for responses. Today those get either a GOODBYE relay to the client or error frames, but under the redesign, the queued Requests flush silently while the route-gone GOODBYE still gets relayed, so the client receives the GOODBYE which fails all pending handles anyway—meaning the client does get notified, just through a different path than before. So if the client's egress buffer fills up, the GOODBYE relay gets dropped as best-effort, leaving queued Requests without a terminal message or GOODBYE — the client would hang waiting for a response. It's a real issue but a narrow window, so I'm marking it MEDIUM. Now I'm looking at a major finding around CANCEL and GOODBYE operations doing an O(queue) scan on the read loop, which could be a denial-of-service vector depending on queue depth.

An adversarial client can exploit this by filling the queue to its limit (2048 for StatelessParallel routes, 4096 aggregate across routes), then spraying CANCEL frames for non-existent correlations. Each CANCEL forces a full queue scan to determine the correlation isn't there before forwarding it, and since CANCELs are cheap 21-byte frames that can be pipelined, an attacker can generate massive read-loop overhead — potentially O(4096) work per malicious frame — all serialized ahead of legitimate traffic on that connection.

This is a critical self-inflicted denial-of-service on the exact latency-critical path the redesign was meant to protect. The fix is to index the queue by correlation ID so CANCEL lookups become O(1) instead of O(queue), or alternatively rate-limit how many CANCEL scans are allowed per byte sent.

The same corr-indexed structure solves Finding B's atomic inspect-remove requirement, so the solution converges: the per-route queue needs to be correlation-indexed with a lock shared between the read loop and drain task.

Now I'm looking at a control ordering issue — the single per-connection control FIFO processes route.open and route.close commands, while data frames are handled on separate per-route drain tasks. This creates a potential race where a data frame could be delivered to the module before its route.open binding is committed, or after route.close has already torn it down.

For the before-open case: a conforming client can't send data frames until it receives the route.open response, which is when the binding gets published and the client learns the channel/epoch. A malicious client sending data on a guessed channel would just get dropped since the binding doesn't exist yet — no cross-task hazard. For the after-close case: route.close runs on the control task and triggers a flush that drops the drain sender, but a data frame that captured a pre-close snapshot could still be enqueuing, which Finding E already covers.

Now I'm thinking about cross-route ordering. The old serial loop processed all frames in socket order across channels, so a consumer could implicitly rely on frame A on channel 1 being processed before frame B on channel 2 if A was submitted first. The new design with independent drain tasks breaks that guarantee — cross-channel order isn't preserved anymore. The design doc claims cross-route ordering was never guaranteed, but that's not quite accurate: today the serial read loop does process frames in socket order across routes as an emergent property, even if it was never explicitly promised. So a consumer that pipelined writes on different routes expecting that ordering would currently see it hold.

Post-redesign, those frames end up on separate drain tasks and can reorder, especially if they target the same module. This is a medium-level concern because while the design asserts it was never guaranteed, there's no evidence that no consumer relies on this emergent behavior. There's also the control-versus-data ordering issue: channel-0 control frames and data frames currently share the serial loop, so they maintain a specific interleaving that could change with the new architecture. The snapshot forwarding in merge-1 introduces a potential consistency gap: the data path switches to loading snapshots via ArcSwap, which can be staler than control-plane mutations on the lock, yet the read loop still awaits routing handles. This creates a window where a data frame's route lookup might observe an older snapshot than a concurrent control operation would see, though the impact depends on whether clients properly await control responses before sending dependent data frames. The key insight is that on a single connection, the sequence of operations—reading the route.open frame, committing the bind under the write lock, publishing the snapshot, then reading the next data frame—preserves ordering through ArcSwap's acquire/release semantics. Cross-connection, the client can't send data until it receives the route.open response, which gates the timing. So merge-1 neutrality should hold as long as ArcSwap uses standard release ordering on publish and acquire on lookups.

There's a subtle edge case though: when a binding is released, the flow is closed and a new snapshot published without it, but readers holding the old snapshot still have an Arc to the now-closed RouteBinding. When they try to acquire the flow, it returns ChannelFlowClosed and triggers the same error handling as today—the mechanism is preserved under merge-1.

Now I'm spotting a medium-severity issue with the drain-task panic backstop. The spec says a panicking drain task must release the route via abort-guard, but if the panic happens after acquiring credit but before the request reaches the module sink, that credit gets leaked—the flow closure doesn't actually release outstanding credits, just blocks new acquires.

The real problem is what happens to the client's pending request: if the backstop only tears down the route internally without relaying a GOODBYE message back to the client, the client's request hangs indefinitely waiting for a response that never comes.

Looking at the findings, Q1's fail-loud approach for route backpressure is actually wrong given the earlier analysis—it only works if SDKs treat it as retryable, but they don't. The pause-set alternative or blocking that route's frames would be better since it preserves the current contract of not generating spurious errors. So Q1 needs to be reversed unless SDKs get updated first.

Q2 on daemon-synthesized cancellations is theoretically sound, but there's a gap in how queued versus delivered messages are classified. SDKs treat cancelled as a terminal module error, which makes sense for cancellations (the caller wanted it), though Swift and TypeScript surface it as a generic error rather than a typed cancelled outcome—minor issue.

Q3 about the whole channel-0 FIFO is acceptable; offloading to a slow route could stall later control operations, but the lean approach is fine given the ordering constraints from earlier findings.

Q4 on the R11 rider shows it's not actually free—it changes the release path, which contradicts an earlier invariant, and introduces complications.

For Q5, the whole-table Arc swap with clone-on-write per mutation is low-frequency and acceptable, but cloning affected maps for a table with many routes becomes O(routes) per mutation, which could spike under reconnect storms. I'll measure this in testing.

Now I'm looking at Finding K about BufReader cancel-safety. The sync hand-off is supposed to preserve only connection-close cancellation, but the new read loop chains read, classify, and route_frame operations synchronously with non-blocking try_push, and the admission path has some gaps I need to examine. The control queue enqueue must use try_send rather than await to avoid blocking the read loop and introducing a cancellation point — the design already specifies control_backpressure overflow handling, so this needs to be made explicit. Now I'm checking whether a double terminal fire is actually possible: the daemon could synthesize a cancelled event while the module emits a delivered event for the same correlation ID, which would require both branches to execute.

The key insight is that the CANCEL handler and drain task operate on a single shared queue structure with atomic removal semantics — whichever one removes the item first wins, making the branches mutually exclusive. If CANCEL finds and removes the item, the drain never pops it and no module terminal fires. If drain pops it first, CANCEL doesn't find it and only the module terminal (or lost event per Finding B) occurs. So atomic single-owner removal prevents the double-terminal scenario entirely.

However, this impossibility claim hinges on an unproven assumption about the queue's atomicity — without that guarantee, a double-terminal could theoretically happen. If it did occur, the outstanding-set in R11 would make the second release credit-inert since only one remove succeeds, but the client would still receive two terminals for a single correlation. The dispatch settlement logic guards against this through object-identity checks and a "no waiter" drop condition, and the Rust consumer also ensures removal happens only once.

So SDKs are actually safe even if a double terminal somehow fired — the second one gets dropped as already settled. That's a meaningful point in favor of the design for this scenario. Now I'm checking hunt 6: whether to_error_frame can support a "cancelled" code with zero body parsing, and I'm verifying how late Error messages with that code are handled across the SDKs.

TypeScript drops the terminal frame if the correlation is already settled or unknown, Rust's settle_pending just no-ops on a missing key, and Swift likely behaves similarly though I haven't fully traced its dispatch logic. So a late or duplicate cancelled terminal is harmless — hunt 6's SDK-tolerance question is good to go. The real defect is Finding B, which is the lost cancel itself.

Now I'm walking through the credit accounting summary by exit path: for delivered plus module terminal, we acquire once during drain and release once at terminal, gated by outstanding.remove, which is correct.

For queued frames with a synthetic cancel, there's no acquire since the frame never gets popped, and no release needed — the accounting balances. But Finding B shows up here: if the frame was popped and parked at the acquire point, the acquire might have succeeded if credit was available, then the cancel gets forwarded to the module which drops it, the request delivers normally, and the module emits a terminal that triggers release — balanced accounting but the cancellation is lost. If credit wasn't available and the frame stayed parked, the acquire never happened, the cancel still gets forwarded, and later the acquire-deliver-terminal-release sequence completes — again balanced but the cancel is defeated.

For queued frames flushed on GOODBYE, they never acquire so no release is needed.  When a popped-parked frame hits the flush, the acquire might be held; flow.close() wakes the parked acquire with an error, so no send happens. In the redesign the drain task owns the acquire, and when flow closes, acquire returns an error — meaning no credit was actually acquired, so there's nothing to release. But if acquire had succeeded before the flush, the frame is mid-send to the module, the send might complete and the module receives it, then the route gets torn down with no terminal coming back through the now-removed route — that's a credit leak, though the flow is already dropped so the module can't use it.

When the client gets the GOODBYE relay it's acceptable but this popped-parked mid-send corner case needs explicit handling. For connection death mid-flight, teardown releases all routes, flows close, and drain tasks abort — queued requests never acquire credit and get dropped, delivered-but-unterminated requests hold credit but the route's gone so it doesn't matter since the client is dead anyway. For module death, the endpoint drain closes flows and relays route-gone GOODBYE to clients, settling them; drain tasks for those routes tear down, queued client requests flush, and the client settles via GOODBYE.

Now I'm looking at drain-task panics and the double-release scenario. There's a guard at the ChannelFlow level that catches over-release — when in_flight hits zero, a duplicate terminal that tries to release again gets caught by the guard which warns and ignores the second decrement.

But the real issue emerges when multiple correlations are in flight: if in_flight is 2 (two distinct correlations A and B), and the module sends duplicate terminals for A while B is still running, the first terminal for A decrements in_flight from 2 to 1 and adds permits, but the second terminal for A would only be caught if in_flight had already hit zero — which it hasn't because B is still in flight.  This means available gets over-credited by 1 relative to actual outstanding work. The outstanding-set per-correlation fixes this because the second terminal for A returns false on removal, preventing the spurious release. So R11 is a legitimate defect for windowed routes with high parallelism, and the outstanding-set approach has real value, though Findings C and D still stand regarding the changes it introduces to the release path.

Now I'm wrapping up the audit. The verdict is NO-GO due to blockers: Finding A shows the SDK's route_backpressure isn't classified, breaking the consumer contract, and Finding B reveals CANCEL gets lost in the window between popping and acquiring, defeating the core goal. There are also major issues C, D, and F that need fixing. I'm pulling together concise per-issue findings with proper citations and trimming down the large tool outputs I've extracted, keeping only the key evidence I've already identified.
I have gathered sufficient evidence across the daemon and all three SDKs. Let me reduce spent search outputs and finalize.

[assistant] 2026-07-15T07:32:01Z
Now I'm pulling together the most critical defects I've uncovered, particularly around concurrency and contract violations. The first major issue is that the backpressure contract claim is fundamentally broken across all SDKs—the documentation asserts that `route_backpressure` maps to the existing NotSent contract, but this turns out to be false everywhere, which is a blocking problem.

In TypeScript, when an Error frame arrives, it gets converted to a plain `SubcError` rather than a `SubcCallError`, which then becomes a terminal error that gets thrown immediately instead of being retried like a proper NotSent would be. The Rust implementation has the same flaw—it classifies backpressure errors as module errors that only retry on unknown channel codes, so everything else fails hard rather than being retried as the design claims.

Swift doesn't even have retry logic at all, and the only actual retryable-code classifier that exists (`isRetryableRouteOpenCode`) is narrowly scoped to channel-0 route.open calls only, not data-plane requests. This means the documentation's claim that "zero SDK changes required" is false—without SDK changes landing first, `route_backpressure` becomes a hard failure, which directly contradicts the merge plan and blocks this unless SDK work is added as a prerequisite.

The bigger issue is that replacing the implicit bounded in-flight from the blocking read loop with fail-loud overflow creates a behavioral regression. Since SDKs don't classify `route_backpressure` as retryable, consumers relying on natural TCP backpressure now get hard terminal errors under load instead of graceful flow control, and with Serial routes having only a queue depth of 4, this is trivially easy to trigger for existing consumers like broca, aft, and alfonso that never had to handle backpressure before.

There's also a critical race condition in the exactly-once terminal guarantee: when CANCEL scans the queue and finds a request still queued, it removes it and synthesizes a `cancelled` terminal to release the credit. But if that same request frame is concurrently being dequeued by the drain task mid-send to the module, the queue no longer contains it, creating a window where both the daemon and module could emit terminal events for the same request.

The real issue is that the design assumes the dispatch queue supports both out-of-band removal by the read loop and FIFO draining by the drain task simultaneously — but a tokio mpsc can't be scanned by the sender side. This means the queue needs to be something like a shared Mutex-wrapped VecDeque instead, and every scan and drain operation must coordinate carefully to avoid this race.

The concurrency primitive for the dispatch queue is fundamentally unspecified, and the requirement to inspect it from the read loop while the drain task owns it rules out mpsc entirely. Whatever locking mechanism gets chosen becomes a new contention point on the read loop that the design doesn't acknowledge.

Even if both a synthetic `cancelled` and a module terminal fire, the SDK dispatch is idempotent, which mitigates the issue but confirms the subtlety. The critical issue is that the `outstanding` set's insert and remove operations happen on different connection tasks — one during delivery by the drain task, the other during the module's read loop — so they need their own per-route lock for synchronization, even though the design doc suggests confinement to a single route's state. This is underspecified but necessary. Now I'm realizing that I3's claim about release paths being untouched and byte-identical is actually false, which is a major problem. eally the issue: the release path *is* modified by the gate, contradicting the stated invariant.

Now I'm looking at a potential credit leak when a client connection dies mid-flight. The request gets delivered to the module and acquires credit, but when the connection drops, the teardown closes the route's semaphore without decrementing the in-flight counter or releasing credit. The binding gets dropped entirely, so when the module's terminal response arrives later, it can't find the route and triggers an Absent drop instead of properly releasing the credit.

The redesign still has this problem if teardown drops the flow, but the real hazard emerges when the drain task is mid-acquire and close() fires simultaneously. The acquire call fails with ChannelFlowClosed, which in the old code propagates as an error frame with no credit release needed since the acquire never succeeded. The new design puts the drain task in charge, and the spec requires an abort-guard that must...

Actually, wait — if acquire failed, there's no credit to release anyway, and calling release on a closed semaphore is a no-op. The deeper issue is whether the abort-guard can correctly distinguish between "panicked after acquiring credit" (needs release) versus "panicked before acquire" (nothing to release). The design mentions mirroring broca's drop-guard but doesn't specify that the guard tracks whether the current frame actually acquired its credit, so a blind release could over-release if the panic happened before acquire succeeded.

Now I'm seeing a second major issue: the drain-task panic credit-accounting isn't proven to be exactly-once, and the design defers this to an unspecified guard mechanism. There's also a denial-of-service vulnerability where the read loop performs an O(queue) scan during CANCEL operations — an adversary could open a StatelessParallel route with a deep queue and fill it with requests to trigger expensive scans. the per-connection cap only limits queue size, not the computational cost of scanning for each CANCEL frame—an attacker can send thousands of cheap CANCELs, each forcing O(2048) comparisons on the read loop, creating an unbounded DoS. This needs a correlation-to-position index for O(1) removal instead of the linear scan. Then there's a cross-task ordering hazard: control frames go to a per-connection task while data frames go to per-route tasks, so a data frame could arrive and be processed before its corresponding route.open has committed, or after route.close. The old serial loop processed route.open inline before reading the next frame, so even a client that pipelined data immediately after open (without awaiting) would have the open committed first — but the new design breaks this guarantee, dropping data from clients that don't round-trip. This is a semantic regression: the old contract was "route.open strictly-happens-before subsequent frames," while the new one requires the client to actually wait for the response.

On the merge-1 invariant side, snapshot forwarding is mostly invariant-neutral, though there's a caveat: the read loop still awaits routing at merge-1, and moving the route lookup from the read lock to an ArcSwap load means control-plane mutations serialize under the write lock before publishing. Within a single connection's serial read loop, a control mutation and subsequent data lookup happen on the same task, so...

Actually, wait — at merge-1 the read loop still processes control inline, so when a route opens and commits under the write lock, the publish happens synchronously inside the control frame handler before returning. The next data lookup on that same task then loads a snapshot published at or after the commit, so read-your-writes is preserved. The only staleness risk is cross-connection — a module frame on another connection's task seeing a slightly stale snapshot — but that's acceptable.

The key constraint is that publish must happen synchronously within the same write-lock critical section as the mutation itself. If publish gets deferred or batched, read-your-writes on the same task breaks and a lookup could incorrectly see the pre-bind Absent state. I need to flag this as an explicit requirement.

Now I'm working through the open questions, starting with the fail-loud versus pause-set backpressure strategy. Q4's rider approach is cheap but oversells itself—finding 5 invalidates the "untouched" claim for I3 and finding 6 leaves the panic path under-specified, so the lean verdict is defensible even if the docs are misleading. Q5 favors whole-table Arc swapping since the read-mostly pattern justifies it. Now I need to verify whether `to_error_frame` actually handles `cancelled` correctly with the right channel, epoch, and correlation IDs by checking the router implementation.

The error frame construction does support arbitrary codes and envelopes without parsing the body, so the daemon can synthesize `cancelled` and `route_backpressure` frames directly—finding 6 confirms the vocabulary exists. The real issue is how consumers classify these frames. For `route_backpressure` on queue overflow, the request gets rejected at admission and never enqueued, so marking it NotSent is semantically honest since the request genuinely never reached the module.

The problem isn't the honesty of the classification—it's that SDKs don't actually map the code to NotSent. The mapping logic exists in principle but remains unimplemented across all SDKs. Now I'm checking whether `route_backpressure` could be classified through some code-based retry path I've missed. The only code-based retry in the call flow is for `unknown_channel`, but everything else with `kind: terminal` throws immediately, and the error wrapping converts frames to SubcError rather than SubcCallError, which means the terminal classification path doesn't trigger the retry logic.

So the hard throw is confirmed—no retry happens. This solidifies the first finding. The design's core claim of "zero SDK changes" is demonstrably false, and shipping without SDK support would turn transient saturation into hard failures for every consumer. Now I need to determine whether this is a true blocker for the design gate or just an implementation sequencing issue.

Looking at the design document itself—lines 52-54 and the consumer contract in section 5—it explicitly states that zero SDK changes are required and that route_backpressure should ride existing error-classification paths. That claim is provably false against the actual shipped code across all three SDKs. A false invariant about consumer contracts is exactly what this gate exists to catch, so this is a blocker.

The verdict is NO-GO. The blockers are the unimplemented route_backpressure NotSent mapping across all SDKs, the O(queue) CANCEL scan DoS vulnerability on the read loop, and the underspecified dispatch-queue primitive that the scan depends on. A GO-WITH-CHANGES path exists if those three issues are addressed.

The gate instruction is clear: a single unrefuted concurrency defect triggers NO-GO. I'm seeing multiple—the DoS scan, ordering narrowing, panic credit accounting—plus the broken consumer contract. That's the final call.
Based on full verification of the design doc against shipped source across the daemon and all three SDKs, here are my adversarial findings.

<COUNCIL_MEMBER_RESPONSE>
## Finding 1: `route_backpressure`/`cancelled` NotSent mapping is UNIMPLEMENTED in every SDK — "zero SDK changes" is a false consumer-contract claim
- **Severity**: BLOCKER
- **Location**: doc 3.5:154-157, 230-233, Goal 5 (lines 52-54), I8:226-228 vs. all three SDK terminal classifiers
- **Confidence**: high
- **Issue**: The design's load-bearing consumer claim — `route_backpressure` "rides the existing error-classification paths," "maps to the existing NotSent contract," "Nothing required" from SDKs — is FALSE against shipped source. A synthesized `Error{route_backpressure}` terminal is classified as a **hard terminal failure**, not a retryable NotSent, in every client. Under saturation the consumer gets a thrown application error where today it gets transparent TCP backpressure.
- **Evidence**:
  - **TS**: incoming Error settles as `pending.reject(this.errorFromFrame(frame))` (client.ts:1057-1058); `errorFromFrame` returns a plain `SubcError`, not `SubcCallError` (client.ts:1116-1126). `managedRequest` wraps a non-`SubcCallError` as `terminalCallError` → kind `"terminal"` (client.ts:739-742,1155-1158). `call()` retries only `err.code === "unknown_channel"` (client.ts:427) or `kind === "not_sent"` (client.ts:435); a `terminal` error is re-thrown at client.ts:450. No path maps `route_backpressure` to retry.
  - **Rust**: Error terminal → `PendingTerminal::Error` → `CallError::Module(body)` (consumer.rs:2877-2884, 394, 579). The managed loop retries only `body.code == "unknown_channel"` (consumer.rs:570-578); everything else `return Err(CallError::Module(body))` (consumer.rs:579). Not NotSent, not retried.
  - **Swift**: no retry/classifier logic exists at all (grep for retry/notSent/backpressure = 0 matches); an Error terminal becomes a bare `SubcError` (Client.swift:671-674).
  - The ONLY retryable-code classifier is `isRetryableRouteOpenCode` / `is_retryable_route_open_code`, a CLOSED set `{unknown_module, module_reloading, target_unavailable, module_timeout}` gated to channel-0 route.open only (client.ts:1252-1259; doc-cited byte-identical to consumer.rs:3130-3135). It never touches data-plane request terminals.
- **Suggested Fix**: This is not "additive config." Add a PREREQUISITE SDK merge (merge-0) that (a) parses data-plane Error `code`, (b) adds `route_backpressure` to a data-request retryable set mapping to not_sent/retry-in-place in TS+Rust+Swift, and (c) ships before merge-2. Until then merge-2 converts transient saturation into hard consumer errors. Correct the doc: Goal 5 /  / I8 are false as written.

## Finding 2: Removal of implicit backpressure lets consumers over-issue into hard failures
- **Severity**: MAJOR
- **Location**: doc 3.5:144-157; server.rs:172-178 (shipped serial-loop backpressure)
- **Confidence**: high
- **Issue**: Today the blocking read loop is the flow-control governor: a client pipelining faster than credits free simply TCP-backpressures (memory #7022, backpressure-by-serialization). The redesign replaces this with fail-loud overflow at depth `max(4, 2×window)` — Serial=4 (doc 3.5:149), trivially overrun. Combined with Finding 1, a consumer that today relied on implicit bounded in-flight now over-issues and receives hard terminal errors instead of natural throttling.
- **Evidence**: shipped implicit bound at server.rs:177-178 ("intentionally keeping inbound dispatch serial: each routed frame is awaited before reading the next"); redesign removes it (doc 34-37) with no consumer-side rate limiter added.
- **Suggested Fix**: Either land SDK retry-with-backoff for `route_backpressure` (Finding 1) or reconsider Q1's per-route pause-set, which preserves old semantics without SDK changes.

## Finding 3: The dispatch-queue primitive is underspecified — "CANCEL inspects the queue" is incompatible with an mpsc drained by the drain task
- **Severity**: MAJOR
- **Location**: doc 3.2:89-95, 3.3:117-123, 3.5:158-160
- **Confidence**: high
- **Issue**: 3.3 requires the READ LOOP to scan/remove-by-corr from the per-route queue while 3.2's `drain_task` concurrently `recv()`s FIFO from the same queue. A tokio mpsc receiver is single-consumer and NOT scannable from the sender side — so the queue cannot be an mpsc. It must be a shared `Mutex<VecDeque>` (or equivalent), meaning every CANCEL scan and every drain `recv` contends a per-route lock. The doc's "O(queue) scan, no await" (3.5:160) hides that this scan now takes a lock shared with the drain task — a new read-loop contention point the design never accounts for, and the exact HOL-class hazard the redesign set out to kill.
- **Evidence**: 3.2:91 `queue.recv()` (mpsc semantics) vs 3.3:117 "CANCEL inspects the route's dispatch queue" and 3.5:159 "executed by the read loop against the queue structure itself" — mutually exclusive for a tokio mpsc.
- **Suggested Fix**: Specify the concurrency primitive explicitly (shared `Mutex<VecDeque>` + a `corr→node` index for O(1) removal) and prove the read-loop lock hold is bounded and non-awaiting.

## Finding 4: O(queue) CANCEL scan on the read loop is a self-inflicted DoS / HOL vector
- **Severity**: MAJOR
- **Location**: doc 3.5:158-160, 3.3:117
- **Confidence**: high
- **Issue**: Adversary opens a StatelessParallel route (queue depth 2048, 3.5:150), fills it to 2048 Requests (within the 4096 per-connection cap, 3.5:162), then sprays cheap 21-byte CANCELs for non-existent corrs. Each CANCEL forces an O(2048) scan **on the latency-critical read loop**, finds nothing, forwards to module; the loop cannot read the next frame until the scan completes. The per-connection cap bounds queue SIZE but not (scan cost × CANCEL rate).
- **Evidence**: worst-case read-loop work = O(queue_depth) per CANCEL ≈ 2048 comparisons/frame; N CANCELs ⇒ O(N·2048) read-loop ops, unbounded in N — reintroducing the cross-channel HOL the design claims to eliminate (Goal 1, lines 42-44).
- **Suggested Fix**: Back the queue with a `corr→node` index so CANCEL removal/lookup is O(1); never linear-scan on the read loop.

## Finding 5: I3 "release paths untouched / byte-identical" is self-contradicting with the 3.7 R11 rider
- **Severity**: MAJOR
- **Location**: doc I3:219, 3.2:97-101 vs 3.7:178-188; router.rs:307-309
- **Confidence**: high
- **Issue**: I3 asserts release paths are "byte-identical (release paths untouched)," but 3.7 inserts an `outstanding.remove(corr)` gate *before* `route.flow.release()` — release now fires conditionally (3.7:183 "release fires only if outstanding.remove(corr) returned true"). The shipped release site is unconditional at router.rs:307-309 (`if releases_credit { route.flow.release(); }`). The rider is by definition a change to that path. The exactly-once proof for the CANCEL case (3.3:125) *depends on* this modification, so calling it "untouched" both misstates the invariant and hides the rider's real surface.
- **Evidence**: shipped release is unconditional (router.rs:307-309); 3.7 makes it conditional on a new per-route set. Additionally the set's insert (drain task, client connection) and remove (module→client path, router.rs:307 — a DIFFERENT connection's task) run on two tasks, so "no global lock" (3.7:184) still requires a per-route lock shared across connections — unspecified.
- **Suggested Fix**: Reword I3 to "release call site gains an exactly-once gate; epoch-fence and escalation semantics unchanged." Specify the `outstanding` set's lock and prove insert-before-any-possible-terminal ordering (delivery→insert must happen-before the module's terminal can reach the remove).

## Finding 6: Drain-task panic credit accounting is unproven exactly-once
- **Severity**: MAJOR
- **Location**: doc 3.6:171-174 (panic backstop, "abort-guard mirroring broca")
- **Confidence**: medium
- **Issue**: The drain task now owns `flow.acquire().await` then `module_sink.send().await` (3.2:93). A panic/abort can occur (a) before acquire — nothing to release; (b) after acquire, before the outstanding-insert — one credit acquired, in_flight=1, MUST release and there is no corr in the set; (c) after insert. The design's guard is hand-waved as "must release the route" without specifying that it must know whether the *current* frame acquired its credit and whether it was inserted. A blind release over-releases in case (a) (masked only by the `in_flight==0` guard logging "over-release ignored," forwarding.rs:1705-1714); a non-release in case (b) leaks a credit while the binding may be recreated — the exact silent wedge 3.6 claims to kill.
- **Evidence**: acquire returns `Err(ChannelFlowClosed)` when the sem is closed (forwarding.rs:1692-1693); release no-ops on a closed sem (forwarding.rs:1723) and on in_flight==0 (forwarding.rs:1705-1714). The interleave between acquire-success and set-insert is a genuine unguarded window the doc does not close.
- **Suggested Fix**: Make acquire+insert a single critical step (insert corr into `outstanding` *before* awaiting send, immediately after acquire returns Ok) and have the abort-guard release exactly the credits whose corrs are still in `outstanding` for that route — not a blanket "release the route."

## Finding 7: New task topology narrows the shipped "route.open happens-before subsequent same-connection frames" guarantee
- **Severity**: MAJOR
- **Location**: doc 3.4:132-140, 3.8:205-209
- **Confidence**: high
- **Issue**: Today the single serial loop processes channel-0 route.open INLINE and to completion before the next read (server.rs:357-375, router.rs:214), so even a client that pipelines a data frame immediately after route.open (without awaiting the response — legal on the wire) has the bind committed before its data frame is read. Under the redesign, route.open goes to a separate control FIFO task while the data frame hits the per-route drain path; the snapshot lookup sees Absent (3.8:208-209) and the Request is dropped/errored. The design leans entirely on "SDKs single-flight route.open and await responses" (3.4:136) — true for the shipped SDKs (Rust publishes the handle before resolving the waiter, consumer.rs:2854-2867), but it is a *narrowing* of shipped wire behavior for any non-SDK/future consumer that pipelines.
- **Evidence**: shipped inline control at router.rs:207-218; SDK await-reliance acknowledged at 3.4:136. Old guarantee = unconditional same-connection ordering; new guarantee = conditional on client round-trip.
- **Suggested Fix**: State the narrowed contract explicitly in I1/3.4 ("data frames on a channel are only guaranteed routable after the client observes that channel's route.open Response"); add a regression test for pipelined-open-then-data to document the intended drop.

## Finding 8: Double-terminal for one corr — daemon vocabulary and SDK tolerance both hold (refutes the client-side risk)
- **Severity**: OK
- **Location**: doc 3.3:117-126,  hunt; router.rs:582-633; client.ts:1103-1110; consumer.rs:1902-1906
- **Confidence**: high
- **Issue/verdict**: `RouterError::to_error_frame` genuinely supports an arbitrary `code` (`cancelled`, `route_backpressure`) with envelope-only channel/epoch/corr and a canonical JSON body, zero body parse — VERIFIED at router.rs:602-608 → error_frame at router.rs:617-632. If a race causes BOTH a synthetic `cancelled` and a module terminal for one corr, both SDKs tolerate the second: TS `settle` is object-identity-idempotent and the second terminal hits "dropped terminal frame with no waiter" (client.ts:1078-1091); Rust `settle_pending` removes-then-checks and a second terminal finds no entry and returns (consumer.rs:1903-1906). Credit side stays exactly-once via the `outstanding` gate (Finding 5). This portion of I5/I7 holds.
- **Suggested Fix**: none — keep as an explicit "second terminal is harmless" test assertion (T3/T7).

## Finding 9: Merge-1 snapshot forwarding is invariant-neutral ONLY if publish is synchronous inside the mutation's write-lock section
- **Severity**: MINOR
- **Location**: doc 267, 3.8:198-211
- **Confidence**: medium
- **Issue**: At merge-1 control still runs inline (B3 not yet moved), so a route.open commit and a subsequent same-task data lookup are ordered on one task. Read-your-writes is preserved because the shipped write path commits under the write lock (release/bind mutate `client_to_module`/`module_to_client` under `write_inner`, e.g. forwarding.rs:1420-1461). If the ArcSwap publish is done synchronously in that same critical section, a same-task lookup after the commit loads a snapshot ≥ commit and sees the bind — neutral. If publish is deferred/batched, a same-task lookup could load a pre-bind snapshot and wrongly Absent-drop a Request the old RwLock would have routed — a NEW observable state.
- **Evidence**: shipped RwLock read at forwarding.rs:846; 3.8:199-201 says "apply to canonical state, then publish" — the ordering is stated but the "publish before releasing the write lock" requirement is not made explicit.
- **Suggested Fix**: Make it a hard requirement: publish the new snapshot *while still holding the write lock*, before returning from the mutation. Add a merge-1 test that binds then immediately routes on the same task.

## Finding 10: Open-question leans
- **Severity**: MINOR
- **Location**: doc 
- **Confidence**: medium
- **Q1** (fail-loud vs pause-set): lean fail-loud is **WRONG as sequenced** — given Finding 1, fail-loud produces hard consumer errors until SDK support lands; the pause-set preserves old semantics with zero SDK changes. At minimum, gate fail-loud behind the SDK merge.
- **Q2** (daemon-synthesized cancelled): lean yes is **RIGHT** (Finding 8 confirms SDK idempotency), but only once the queue primitive supports O(1) scannable removal (Finding 3/4).
- **Q3** (whole channel-0 FIFO): **RIGHT** relative to the route.open-only alternative, but neither restores the pipelined-open ordering (Finding 7) — accept with the contract note.
- **Q4** (R11 rider now): defensible on cost, but **the doc framing is wrong** — it is not "release paths untouched" (Finding 5), and the panic path is under-specified (Finding 6). Land only with those closed.
- **Q5** (whole-table Arc swap): **RIGHT** — read-mostly justifies it; per-shard only if T9 shows publish-hot.

## Summary
- **BLOCKER (1)**: `route_backpressure` NotSent mapping is unimplemented in TS, Rust, and Swift — the design's "zero SDK changes / rides existing classification" contract is false against source; merge-2 without a prerequisite SDK merge turns transient saturation into hard application errors.
- **MAJOR (6)**: over-issue regression (F2); dispatch-queue primitive underspecified & incompatible with mpsc drain (F3); O(queue) CANCEL-scan DoS/HOL on the read loop (F4); I3 "release untouched" self-contradiction + cross-task outstanding-set locking (F5); unproven drain-task-panic credit accounting (F6); narrowed route.open happens-before contract (F7).
- **MINOR (2)** / **OK (1)**: merge-1 publish-ordering requirement (F9); open-question corrections (F10); double-terminal tolerance verified safe (F8).

Overall risk: HIGH, confidence high on the blocker and on F3/F4/F5 (all cited to shipped source). Per the gate rule (a single un-refuted concurrency/contract defect is NO-GO), multiple stand un-refuted.

**MEMBER VERDICT: NO-GO** — blockers: (1) SDK `route_backpressure`→NotSent mapping does not exist in any of the three shipped clients ("zero SDK changes" false; add a prerequisite SDK merge); (2) dispatch-queue primitive underspecified and its read-loop CANCEL scan is an O(queue) DoS/HOL vector (F3+F4; require O(1) corr-indexed removal); (3) I3 "release paths untouched" is false and the drain-task-panic credit accounting is unproven exactly-once (F5+F6; specify acquire+insert atomicity and the abort-guard's per-corr release). Resolve those three and F7's contract note and this becomes GO-WITH-CHANGES.
</COUNCIL_MEMBER_RESPONSE>

