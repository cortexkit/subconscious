# Decision Log — Subconscious PRD

Canonical memory + audit trail. Every decision, change, and override recorded as the conversation unfolds.

## 2026-06-17

- **Intent:** Create (no prior PRD; BMad install fresh today; all artifact folders empty).
- **Lineage:** subc PRD derives from the Takım PRD (`~/Work/Projects/Takim/PRD.md`, the source product doc). `subconscious-design-handoff.md` spawned from Takım. subc = Takım Gateway at single-machine scale, built with seams to grow.
- **Stakes:** Launch-grade. AFT + MC are already in the wild with real users; v1 changes live infrastructure beneath them.
- **v1 scope (confirmed):** subc is a machine-wide daemon supervising AFT as a managed module. Success line = move AFT from an OC/Pi-managed binary (one per harness instance, NDJSON bridge) to a subc-managed binary reached via `harness ⟷ subc ⟷ aft`.
- **Three connection modes (Ufuk):**
  1. Classic MCP — all MCP-capable harnesses, less surface, no hoisting.
  2. Thin deep-plugins over a non-MCP protocol to subc — preserves AFT hoisting (built-in read/write/edit/bash/grep replacement), OC/Pi.
  3. MITM proxy for non-pluggable harnesses (Codex, Claude Code) — subc injects/handles tools on its end; also how MC reaches those harnesses. **Later.**
  - **v1 = modes 1 & 2.** Mode 3 is later.
- **Problems v1 attacks:** (A) every harness instance spawns its own `aft` (dup process/index/memory) → one subc-managed `aft`; (C) AFT/MC are OC+Pi only → broad harness coverage via mode 1. Hot-update (swap `aft` without dropping sessions) rides the daemon-owns-connection model.
- **Out of v1 (motivating but later):** MC dreamer headless via an LLM Runner/Loop module; the LLM-proxy / cache-codec plane. Problem B (dreamer needs OC/Pi held open at schedule time) is the reason for the later phase.
- **Open question carried to architecture:** hoisting gradient across modes — mode 2 preserves full hoisting; mode 1 (MCP) can only add tools, not replace built-ins (degree of built-in replacement depends on whether the harness lets you disable its own tools).
- **Working relationship:** Ufuk wants to be in the loop on every design decision (strong scalable-systems/architecture background). Collaborative design, no solo authoring of architecture.
- **Peers:** AFT dev agent registered as peer `AFT` (session `ses_313660571ffeZTsf4koSJwk50Q`).

- **A vs C headline (resolved):** Ufuk — "both A and C at the same time." A and C are co-equal v1 drivers, not sequenced. Framing committed: one daemon resolves both; lifecycle coupling falls out for free.
- **Section spine (confirmed):** Problem → Vision → Goals & Success Metrics → Users & Beneficiaries → Features & FRs → NFRs → Out of Scope (v1) → Open Questions/Forks.
- **Problem section: committed.**

- **Vision: committed.** Tone approved; plural "hosts CortexKit's modules" capability framing confirmed (v1 ships AFT as the first module). Takım-kernel paragraph kept.

- **Q2 coverage target (resolved):** Ufuk — "1 MCP harness working with the new structure is already success." v1-done = subc's MCP facade verified against ONE harness beyond OC/Pi. No named set required.
- **Dedup unit (Ufuk override → single process):** NOT one aft per root. Ufuk wants **one aft process total** for the machine, multi-root via Rust threads, "to reduce memory greatly." Later: extract the embedding pipeline out of AFT into its own machine-global module (LMStudio-style).
- **Consequence flagged (open, needs Ufuk confirm + AFT-Alfonso coordination):** single-process-total REQUIRES the ProjectActor/ProjectRuntime extraction from AFT's single-root AppContext — which AFT-Alfonso scoped as POST-v1 cleanliness, not a v1 blocker. So this decision pulls AFT's biggest internal refactor INTO v1. Sequencing fork under discussion: (i) v1 = transport-swap + cross-instance dedup (per-root, fast/safe), then a memory phase = single-process + embedding-module together; vs (ii) v1 = transport-swap + single-process now (ProjectActor in v1), embedding-module later. Embedding vectors are the dominant RSS (170–360MB), so the embedding-extraction lever interacts with the single-process lever.

- **AFT command contract delivered:** `~/Work/Projects/CortexKit/aft/.alfonso/plans/aft-module-command-contract.md` (~67 commands). Protocol constraints for architecture phase: dual-scope identity (session_id + project_root), async PUSH frames, subc→AFT→bash lifecycle nesting. (Detail in memory.)
- **Disk: confirmed shared, not duplicated.** index/ and semantic/ are flat project-keyed (not harness-namespaced) → zero cross-harness disk duplication. Saving is RAM + process count + LSP children + watcher only.
- **Correctness win to add to PRD:** embedding flip/flop — one shared project-keyed `semantic.bin`; OC and Pi with different embedding backends each rebuild the other's index in place (wasted re-embed CPU/thrash). subc owning the per-root embedding queue fixes it. Goes next to the RAM/process metric as a correctness benefit.
- **Fork crux → AFT sizing requested:** asked AFT-Alfonso to size/risk the ProjectActor refactor (single-root AppContext → multi-root-in-one-process). That sizing decides sequence (i) vs (ii) for single-process-in-v1. Pending.

- **Goals & Success Metrics: committed (fork-independent parts).** Dedup unit + exact resource figure parameterized pending AFT ProjectActor sizing + the 2×OC+1×Pi measurement. Coverage bar = one MCP harness beyond OC/Pi. Flip/flop correctness win added next to the RAM metric.
- **Deferred to Open Questions:** whether multiplexing lands in v1 (the AFT #117 head-of-line-block fix depends on it); the exact added-latency target (needs a baseline).

- **ProjectActor sizing (AFT-Alfonso, from source):** Path A (ProjectActor — one AppContext per root on its own actor thread, main loop = router) is contained/MEDIUM; the 44/58 single-root handlers don't change. Path B (shared multi-root AppContext) rejected as a rewrite (82 RefCells, LspManager must run on dispatch thread).
- **RAM truth (premise correction):** one process total does NOT cut the dominant RAM — 170–360MB/proc is per-root semantic vectors, which don't shrink with process count. Single-process saves process overhead + shared singletons (embedding model loaded once = the real lever). The actual big-RAM lever is the embedding-pipeline extraction into its own machine-global module (Ufuk's "later" module) + vector streaming/mmap.
- **AFT recommendation (Alfonso concurs, pending Ufuk):** v1 = transport swap (per-root over socket); single-process-multi-root = fast-follow (ProjectActor + singleton hoist); gate single-process-in-v1 on an RSS decomposition measurement (singleton fraction vs per-root-data fraction).
- **Action:** RSS decomposition requested from AFT (solo single-process breakdown now; cross-instance 2×OC+1×Pi folds into Ufuk's timing). **Open for Ufuk:** (1) concur with v1=transport-swap + single-process-as-measured-fast-follow; (2) whether the embedding-module extraction moves up the roadmap since it's the real RAM lever.

- **PRIORITY CORRECTION (Ufuk, "very very crucial"):** RAM/dedup is NOT the main problem — "not even a big problem." The MAIN driver is removing SERIAL tool execution so NON-MUTATING tools run in PARALLEL, mutating serialized. This reorders the Problem (concurrency = headline #1; coverage #2; duplication demoted to #3 secondary; lifecycle #4) and the Goals (concurrency = core goal #1; dedup demoted to secondary goal #7).
- **Source-verified (not inferred):** AFT dispatch is strictly serial — `crates/aft/src/main.rs` main loop (lines ~134–225) receives one line, calls `dispatch(req,&ctx)` synchronously, writes the response, then reads the next. A dedicated stdin-reader thread buffers lines into a channel, but execution is one-at-a-time on the main thread. This is empirically the #117 head-of-line stall.
- **Key architectural consequence:** subc's multiplexed transport is NECESSARY but NOT SUFFICIENT. Transport mux alone relocates the serial queue behind the socket; AFT must ALSO execute non-mutating commands concurrently (per-root reader-writer: non-mutating handlers on a thread pool against the already-Arc-shared indexes; mutating handlers take a write barrier). So v1's heart = AFT concurrency refactor + subc mux. The single-process/ProjectActor play is now secondary and measurement-gated.
- **Sent to AFT-Alfonso (crux question):** what does it take for AFT to run non-mutating commands concurrently per-root? Specifically: do the non-mutating handlers touch any RefCell mutable state, or are they already clean against the Arc structures? This sizes the central v1 work and supersedes (composes with) the ProjectActor sizing.
- **Problem + Goals/Metrics: rewritten** around concurrency-first. Metrics now lead with the concurrency proof (slow search + N quick reads don't queue) and mutation-correctness-under-concurrency.

- **Confirmed (Ufuk):** priority right (concurrency #1, coverage #2, RAM secondary); v1 center = AFT concurrency refactor + subc mux, accepted as the central work.
- **Working-mode shift (Ufuk):** drop BMad section rigidity. North star = design subc-core/protocol/seams so every foreseeable module integrates without a big core change. PRD + log kept as background record; work proceeds as collaborative architecture.

- **CK app + mgmt plane (Ufuk):** the CortexKit app (one client for the whole suite) arrives near-term. MC's dashboard today reaches around modules (reads MC db + OpenCode db + local config directly). subc inverts this: the CK app sees/edits everything THROUGH subc over the mgmt plane, never locally. Topologies: subc on multiple machines; subc-on-server + CK-app-on-laptop (remote config control + remote dashboard "without local db"). → mgmt plane promoted far-future → NEAR-TERM.
- **Role model evolved → planes × provider/consumer.** The dashboard surfaced that my flat 5-role list under-weighted the mgmt plane. New frame: roles = per-plane provider/consumer pairs (tool / proxy / mgmt) + cross-cutting services. Closure becomes structural: only a genuinely new PLANE forces core change.
- **New design rule:** everything CK-app-facing is subc-served from the module (config = module-owns-schema + subc-owns-store+remote-transport; data = module mgmt-surface). Local files / harness-db direct reads are eliminated.
- **New seam:** mgmt-plane auth (CK-app/human → subc, remote TLS), distinct from module→provider auth.
- **Open fork (MC domain):** the dashboard's session/message data currently comes from OpenCode's db — needs a harness-agnostic, subc-served owner. Needs MC-Alfonso (not yet a registered peer).

- **Started the canonical design doc:** `docs/subc-core-architecture.md` (living). Operationalizes the handoff into the module-integration contract. First draft covers: thin-core ownership, the 3 planes, the HELLO + manifest registration vocabulary (provider/consumer roles, scheduled-tasks, bindings), core services, the closure argument (every foreseeable module as a manifest), open forks.
- **Key design decisions embedded in the draft (for Ufuk review):**
  - **`concurrency` is a declared per-module field** (`serial` | `module_managed` | `stateless_parallel`). subc honors the declaration; correctness stays in the module. v1 concurrency = subc mux + AFT declaring `module_managed`. `mutates` is observability-only metadata; subc never acts on it (thin-core preserved).
  - **Config is a binding, not a file** (`source: subc_mediated`): module owns schema, subc owns store + remote-edit transport → enables remote config control from the CK app.
  - **Closure is structural** (planes closed), not enumerated; only a new plane forces core change.

- **#1 fork RESOLVED (MC-Alfonso audit):** the dashboard data splits cleanly on an ownership boundary. MC serves context.db only (~99% of compartment view); the harness owns raw transcript + session/worktree metadata + per-step token usage. Model = TWO mgmt-plane providers (MC + per-harness session-store), composed by the CK app; no module proxies another's data. The seam already exists (MC's get_session_messages OC-SQL vs Pi-JSONL switch). MITM harnesses later: subc-the-proxy can be the session-store. → harness is a tool-plane consumer AND a mgmt-plane provider. Closure holds (harness-session-store is a management_surface — no new role-kind). Doc updated.
- **MC cleanup owed (logged, not now):** move compartment start/end timestamps into context.db at publish → removes MC's last opencode.db read.
- **Config constraint (MC):** config is two-tier (user overridden by project); {env:}/{file:} expansion allowed only at user tier (project denied, secret-exfil safety). subc stores tiers as data (never flattened), module merges + expands at use-time on its own machine (secrets never reach the CK app). bindings.config updated with tiers + per-tier expansion. Confirm sent to MC.
- **Transform shape confirmed (proxy plane, later):** normalized→normalized, prepends m[0]/m[1], no LLM calls, needs prefix-evicted signal, declares frozen-floor. Live wire contracts deferred until proxy-plane work; MC will pull from source.

- **Config model confirmed by MC (against loader source) + two precision invariants:** (1) tier provenance must survive — subc keeps user/project as distinct origin-tagged raw documents, never pre-merged/flattened; (2) subc does ZERO semantic preprocessing — the project-tier trust policy is broader than {env:}/{file:} (strips privilege-escalation + sqlite.* fields, drops inherited embedding key on endpoint redirect), all module-owned; (3) stored/transported form is always the raw token, never expanded for display/validation/round-trip. Generalized as subc's rule: **store and route without interpreting** (config-plane splice-without-parse). Doc + memory updated.

- **Concurrency consistency (Ufuk):** edit(A)/read(A) intra-batch ordering is AFT's problem, NOT subc's. → subc's concurrency contract = DELIVERY SEMANTICS ONLY (per-channel FIFO delivery + concurrent in-flight + cancel + flow-control windows). ALL execution consistency, incl. intra-batch same-resource ordering, is module-owned. The (a)/(b) fork is dropped — never subc's.
- **AFT migration-design status (honest):** AFT has NOT given a committed migration design. What exists = Path A SIZING (cross-root actor shape: per-root AppContext instance on its own actor thread, main loop = router). The within-root concurrent executor (reader-writer delivering non-mutating-parallel, the v1 headline) is STILL the open crux question (do non-mutating handlers touch RefCell?) — unanswered since first asked. Nuance: Path A as sized is single-threaded-per-actor → cross-root parallel only; within-root parallelism needs the reader-writer INSIDE each actor. "Actor model sized" ≠ "concurrency migration designed."
- **Decision:** do NOT lock subc's concurrency/delivery contract until AFT's migration design lands (the two must interlock). Sent AFT a sharpened ask: (1) confirm the two-leg split [transport+router vs within-root executor], (2) the RefCell-touch answer, (3) sequencing (both legs in v1, or executor as fast-follow). Concurrency doc section held pending this.

- **AFT answered the concurrency crux (source-verified):** two-leg split confirmed & separable. Leg 1 = transport+router (contained, cross-root parallel, serial-per-root). Leg 2 = within-root reader-writer executor (the headline). RefCell answer: non-mutating handlers touch RefCell EXTENSIVELY (query substrate is RefCell<Option<T>>, !Sync; navigation/semantic/outline lazy-mutate on the read path) → Leg 2 = interior-mutability surgery: (a) index structs → Arc+RwLock / Arc<snapshot> (CallGraphStore SQLite → read-only pool/snapshot), (b) move lazy-build/open/model-load/generation-revalidation out of the read path. Sequencing: Leg-1-then-Leg-2, BOTH in v1.
- **Interlock LOCKED:** subc builds its full contract day one (concurrent in-flight + cancel + per-channel FIFO delivery + flow-control windows); AFT accepts concurrency from Leg 1 (serial internal FIFO) and runs concurrent after Leg 2 — same wire contract throughout. subc does NOT gate on Leg 2 timing. → subc's concurrency design is UNBLOCKED.
- **#117 split by leg (sets the metric):** subc-mux liveness-from-cache kills passive-poll-behind-scan immediately; heavy-scan-blocks-quick-reads-within-root only falls with Leg 2 (in v1). Headline concurrency metric = Leg 2 deliverable.
- **Doc: wrote §6 Concurrency & the delivery contract** (delivery semantics, the two-leg module side, leg attribution, Leg 2 substrate cost). Closure→§7, forks→§8. AppContext substrate-conversion inventory accepted from AFT (durable artifact, pending).

- **AFT confirmed #117 split + cancel + inventory.** Nuance folded into doc §6.3: Leg-1 stale-status caveat — subc liveness is always answerable (subc supervises the process); module-originated status (index/code-health) is push-refreshed and can be briefly stale during a long Leg-1 serial scan (actor can't emit until it yields); liveness never stale; Leg 2 closes the window. Acceptable for Leg 1.
- **Cancel mechanism:** cooperative interruption-poll in AFT's long loops (callgraph cold-build, embedding runs) — AFT Leg 2 substrate work.
- **AFT substrate inventory:** deferred to AFT v0.39.4 (AFT note #279); will land at `~/Work/Projects/CortexKit/aft/.alfonso/plans/aft-appcontext-substrate-inventory.md`. Smart note set to fold it into doc §6.4 when the file exists.
- **CONCURRENCY: fully locked** (contract + two-leg plan + #117 attribution + cancel + caveat). The only remaining concurrency-related input is the cosmetic substrate inventory (tightens Leg 2 notes, not the contract).

- **Wire envelope LOCKED** (doc §4.8): 17-byte little-endian header [len u32@0 · ver u8@4 · type u8@5 · flags u8@6 · channel u16@7 · corr u64@9], body spliced unparsed. Little-endian; len = body bytes after header. CANCEL/PING/PONG/GOODBYE pure-header.
- **Versioning resolved:** HELLO negotiation (primary; subc speaks the superset, negotiates down — module skew is normal) + per-frame ver (self-describing). **Frozen-prefix invariant locked:** len(u32@0) + ver(u8@4) fixed meaning/position forever → any reader reads 5 bytes → version → header length → splice. Corollary: len stays u32.
- **Two extension tiers:** small (new type values / reserved flag bits) = no bump; structural (new/resized fields) = bump ver. Transport separately swappable (h2 later) without touching envelope versioning.
- **Micro-decision settled:** KEEP per-frame ver — it is the extensibility anchor (resolves the earlier "drop ver to reclaim a byte?" question).
- **Federation validated:** inter-subc = a bridge MODULE (client + provider re-exposing a remote subc's capabilities locally); zero core/envelope change; reuses remote-transport + manifest + vault seams; distributed shared-state rides the storage-backend-swap (SQLite→Postgres) seam. The Takım growth path = add connector modules + swap storage backend.

- **Party-mode review of the v1 epics (Winston/John/Amelia, independent subagents).** Convergent findings to fold into the story breakdown:
  1. **Story zero — freeze the subc⟷AFT contract before Epic 1:** the envelope is locked (§4.8) but the manifest + command set + the wire spec the fake-AFT stub conforms to must be frozen first (Winston + Amelia).
  2. **Epic 1 = one epic, multiple stories along test-seam boundaries** (Amelia's 1.1 wire codec → 1.2 socket+router → 1.3 supervision → 1.4 HELLO/manifest → 1.5 dual-scope identity → 1.6 PUSH → 1.7 e2e real-AFT). The **fake-AFT stub** is the seam that decouples Epic 1 from AFT Leg 1 — only 1.7 depends on Leg 1.
  3. **Epic 1 done-bar: prove mux-readiness** (two channels interleave frames on the wire) even though execution is serial (Winston) — so Epic 2 doesn't discover a non-mux-ready envelope too late.
  4. **Value sequencing (John):** cross-session/cross-call concurrency is subc-owned and lands in Epic 2 **regardless of AFT Leg 2**; only **within-root** parallelism is Leg-2-gated — name them separately. Surface the flip/flop dedup bugfix as **felt value** rather than burying it; every epic needs a real demo, not "it still works."
  5. **Pull daemon-absent fallback forward into Epic 1** as a dogfood safety net (Winston); keep hot-swap-under-load in Epic 4.

- **Party-mode round 2 (cross-talk) — CONVERGED resolution:**
  1. **Epic sequence unchanged** — 4 epics in order (spine → concurrency → coverage → resilience). No re-sequence (all three agree). The fake-AFT stub reconciles build-order with felt-value-early: concurrency is demoable against the stub inside Epic 1's timeframe, so the two goals stop competing.
  2. **Story Zero (hard precondition):** freeze + co-sign with AFT the full wire+manifest+command contract the stub conforms to (envelope already locked §4.8). The stub strategy is only as sound as this contract.
  3. **Epic 1 stories along test-seam boundaries** (Amelia): 1.0 daemon-absent fallback (connect-time) · 1.1 wire codec · 1.2 socket+splice router (AC: two channels interleave frames, execution serial = mux-ready, per Winston) · 1.3 supervision + fake-AFT stub · 1.4 HELLO/manifest · 1.5 dual-scope identity · 1.6 PUSH · 1.7 e2e real-AFT (ONLY story gated on AFT Leg 1).
  4. **Concurrency split (John, conceded by all):** cross-session concurrency is subc-owned, demoable in Epic 2 WITHOUT Leg 2 (Amelia's test: stub delay(channel A)=500ms, assert channel B quick read returns first). Within-root parallelism is Leg-2-gated — kept as a lexically separate AC so nobody claims the Leg-2 metric off the cross-session green.
  5. **Fallback split:** daemon-absent → Epic 1 (story 1.0, connect-time branch, trivial); daemon-death-mid-session → stays Epic 4 (recovery protocol, hard).
  6. **Demo-bar rule (John, non-negotiable):** every epic closes on a sentence a user would say. Surface the embedding-dedup bugfix as its OWN named demoable story ("two harnesses stop rebuilding the same repo's index"), not scattered plumbing.
  7. **OPEN scope question surfaced for Ufuk:** dedup (FR3 one-shared-AFT-per-root + NFR10 flip/flop) may belong at Epic 1's CLOSE rather than Epic 3 — because subc owning per-root AFT inherently dedups mode-2 instances (OC+Pi), giving Epic 1 a real user sentence. That would make Epic 3 purely the mode-1 MCP-facade coverage. Decision pending.
- **Mechanism note:** round-2 used real session continuation (background_prompt to the same sessions) — agents referenced and refined their OWN prior turns ("the bar I asked for last round", "tighten my seam rather than break it"), which BMAD's stateless re-spawn+summary cannot do.

- **Advanced-elicitation pass (inversion + assumption-audit, parallel subagents) — ALL findings applied** as a "v1 Hardening" section in epics.md. Convergent (both methods) gaps: stub fidelity (→ Spike A + 1.5 recorded-trace + 1.1 dual conformance), cross-session leak (→ 1.10 isolation + 1.1 per-command scope table), ProjectRootId edge cases (→ 1.7 property tests), no latency gate (→ 1.11), Leg-1/2 schedule risk (→ 1.1 DoD split). Plus subc-owned contract ACs: FIFO delivery order (2.1), cancel race/idempotency (2.3), hoisting fidelity (1.9), module-death (1.5), version floor (1.6), PUSH misroute (1.8), flow-control starvation (2.4), hot-swap state+manifest drift (4.1), fallback two-writer (4.3). Two spikes added: A (stub-fidelity, before Epic 2, highest priority); B (MCP-facade transform trace, before Epic 3).
- **Workflow correction (Ufuk):** ALF (Alfonso subsystem) design insights → session NOTES (deliverable-then-discardable), not subc memories. Memories 6502/6503 archived; consolidated into note #281 (cognitive-primitive library: meetings + typed analysis-methods, entropy via model+temperature, auto-context via a context resolver). Ufuk is in design mode using BMAD's structure as a reference to build a 10x version in ALF.
- **Next:** dispatch the hardened epics to Athena council (athena-junior) for an independent synthesized opinion — specifically what the inversion + assumption passes MISSED.

- **Athena council review (7 models, ~30min, strong agreement) — findings the prior passes MISSED:**
  - CRITICAL: no story for the **TS mode-2 plugin client** (the harness-facing surface is invisible; blocks 1.9). CRITICAL: **daemon bootstrap / discovery / singleton enforcement** unstoried (FR3 unenforceable without it).
  - Corrections to MY hardening: **Spike A must move INTO Story Zero** (it produces the freeze, not validates it after 8 stories depend on it); the **dual-conformance suite re-introduces a Leg-1 dependency into Story Zero** (self-contradiction — Story Zero is meant to be pre-Leg-1); **Story-Zero 67-command freeze gate contradicts the versioned-envelope extensibility** → trim to a ~8–10 command e2e subset.
  - SCOPE CUT (the angle no prior pass took): **Epic 4 zero-drop hot-swap + session-state survival is over-engineered for v1** → drain-to-quiescence suffices; defer zero-pause to v1.1.
  - New definitions needed: **channel lifecycle / GOODBYE / teardown** (channels allocated, never closed → leaks); **subc-protocol crate** has no repo/build/versioning home; **UDS tenancy** (per-user vs machine-wide) + socket permissions undecided.
  - Reframe: within-root headline is 100% Leg-2-gated; cross-session (subc-owned) is a DIFFERENT problem than the PRD's stated pain — re-baseline the headline honestly or gate Epic 2 on Leg 2 sizing.
  - Archive: `.alfonso/athena/council-subc-v1-epic-plan-audit-f9d10efdc8b90b37`. Pending Ufuk's call on disposition (esp. scope cuts + headline reframe + tenancy/crate decisions).

- **Council disposition applied (Ufuk decisions):** A — added Story 1.0 (daemon bootstrap/discovery/singleton, per-user) + Story 1.8b (TS mode-2 plugin client). B — Epic 4 scope-cut to drain-to-quiescence (zero-pause hot-swap → v1.1). C — per-user daemon, per-user socket path + race-free bind as the conflict guard, no fixed TCP port, perms 0600. D (Alfonso's call, Ufuk delegated) — v1 headline framed in 3 honest tiers: contract + cross-session ship in v1; within-root parallelism (the actual stated pain) is Leg-2-gated → AFT Leg 2 is the true critical path; do NOT gate Epic 2 on Leg 2. Mechanical fixes folded: Spike A into Story Zero (recorded trace from today's NDJSON AFT resolves the dual-conformance/Leg-1 contradiction), freeze gate trimmed to ~8–10 command subset, channel/GOODBYE lifecycle in 1.6, crate repo home, Spike B before Epic 2, 2.5 status-cache protocol, 1.8 PUSH flow control. All in epics.md "Council review — applied changes".

- **Epics & Stories workflow COMPLETE** (all 4 steps). `epics.md` final: 4 epics, ~23 stories (incl. council additions 1.0, 1.8b), all 19 FRs covered, 3 review layers applied (roundtable + inversion/assumption + Athena council). Implementation-ready. Tracked non-blocking follow-ups: (1) merge addendum ACs (Hardening + Council sections) into parent stories at the story-file stage; (2) coordinate AFT Leg 1/Leg 2 timing (Leg 2 = felt-headline critical path); (3) deliver ALF cognitive-primitive design (notes #281/#282) to Alfonso. `on_complete` resolved empty — no terminal hook.

- **PROCESS TOPOLOGY RESOLVED (authoritative — Ufuk direct via AFT session):** SINGLE AFT PROCESS total; AFT = a singleton module (NOT per-project-pool). Brief whipsaw (A per-project-pool → B single-process) settled directly. Reasons: operability (one PID/log) + upgrade atomicity (process = upgrade unit, all roots one version; per-root independent hot-update is a MIS-FEATURE I had wrongly listed as a benefit). NOT RAM (embedding extraction covers RAM). Consequences: subc routes all aft traffic to one pid, AFT self-demuxes by project_root (internal ProjectActor map); subc still resolves ProjectRootId for lease/scheduler/other-modules, not for AFT routing; per-project-pool cardinality NOT built (all modules singletons); crash blast-radius (one panic→all roots) accepted (auto-restart + panic-hardening). AFT Leg 1 = in-process ProjectActor extraction (real but bounded daemon-prep move), Leg 2 = RefCell→Arc unchanged. Architecture doc §2.1 added; memory 6426 updated; AFT replied. **Note for stories:** manifest needs no cardinality field (all singletons); Story 1.4 router routes by module-kind not per-root instance; Story 1.0 supervises AFT as a singleton (no pool spawner).

- **HARNESS CARDINALITY RESOLVED (Ufuk via AFT, Option A):** the single AFT process serves ALL ROOTS *and* ALL HARNESSES (OpenCode + Pi + MCP) — NOT one-AFT-per-harness. Reason: Ufuk runs multiple harnesses concurrently on the SAME git-root; per-harness processes would re-create the exact duplication the migration kills (2 watchers / 2 tsserver / 2 callgraph / 2 Tier-2 loops / semantic flip-flop for one root). Under A, all harness sessions on a root share ONE ProjectActor (one watcher/index/LSP/callgraph/semantic); sessions are cheap multiplexed clients. Refines (not reverses) single-AFT-process.
- **ROUTING MODEL — the (root,harness,session) triple does NOT change the 17-byte envelope (I corrected AFT's "envelope requirement" framing; envelope stays locked/co-signed):** the envelope `channel` (u16) is the per-frame route handle; the triple binds to a channel ONCE at session-attach. Per-frame = channel+corr+opaque-body. PUSH return = AFT echoes originating channel -> subc maps channel->connection (subc never parses the triple per-frame; thin-core preserved). TWO distinct channel-0 handshakes now named: (1) MODULE REGISTRATION (aft->subc, manifest) = what Story 1.6 built; (2) SESSION ATTACH (harness session->subc, carries the triple, allocates a route channel bound to (connection,triple)) = new, data-plane story, where the triple lives. Proposed binding-propagation (pending AFT confirm): on attach subc parses triple (channel-0, allowed), resolves canonical ProjectRootId (1.7), binds channel->(connection,root) for lease+scheduler+PUSH-return, and RELAYS an attach event to aft so aft demuxes by (root,harness) keyed by channel; GOODBYE releases both. Alternative (triple-in-every-body) rejected as repetitive. Folds into Story Zero contract as the session-attach schema.
- **AFT inventory v2 = Story Zero AC5 input:** `~/Work/Projects/CortexKit/aft/.alfonso/plans/aft-appcontext-substrate-inventory.md` (Oracle-validated: Leg-2 bigger than one field but bounded; callgraph_store downgraded barrier->wrap). CAVEAT: the storage-scope split (project-artifacts root-keyed/shared vs session-state (harness,session)-keyed) is NEW design not yet Oracle-reviewed — subc holds dependent contract work until AFT validates it.

- **PUSH FAN-OUT OWNERSHIP (Alfonso's call, AFT confirmed attach-relay first):** AFT owns root->[channels] fan-out (emits one PUSH per target channel); subc stays a PURE channel->connection 1:1 router (no "broadcast" semantic). Rationale: (1) thin-core single routing rule; (2) coherence — bash-completion + bash_watch PUSH-target decisions (remembered session->channel + channel-gone->queue-for-replay) are irreducibly AFT's, so keep ALL PUSH-target policy in one owner rather than fragment status-fan-out to subc with different channel-gone semantics; (3) zero new state for AFT (root->[channels] == its ProjectActor session set / refcount); (4) marginal bandwidth (N-1 extra small frames per coalesced status event on local UDS). subc CAN do it (has root->[connections] via the lease binding) — chosen against on boundary grounds.
- **BIDIRECTIONAL CHANNEL-0 RELAY + Story 1.8 PUSH contract:** ATTACH-relay (subc->aft: channel bound to (root,harness,session)) + DETACH-relay (subc->aft: channel gone on GOODBYE/connection-drop) — detach-relay is the AUTHORITATIVE trigger for AFT's per-frame-type channel-gone policy (queue-for-replay for completion/watch; drop/coalesce for status). subc additionally best-effort drops a PUSH handed to a dead connection (emit-before-detach-relay race) and surfaces a channel-gone outcome on that send; AFT's detach-relay + cross-restart replay is the durable net. **Story 1.8 PUSH contract = subc routes PUSH by channel->connection, returns channel-gone if absent; AFT selects channel(s) + owns replay.** AFT ensure-or-create-ProjectActor confirmed: attach warms cold root (first) / binds to warm actor (subsequent); detach decrements session refcount; zero->idle-evict. Attach-relay eliminates AFT's session_id-in-every-body (net win).

- **PUSH contract LOCKED (AFT confirmed + 2 refinements):** (1) channel-gone = TWO converging signals — proactive DETACH-relay (authoritative) + reactive per-send channel-gone outcome (race backstop); both feed AFT's one per-frame-type policy. (2) Replay queue is disk/DB-backed keyed by (harness,session) — survives detach AND ProjectActor idle-evict AND process-restart (AFT's existing bash-task persistence/replay machinery); subc just delivers replayed PUSH on the new channel after re-attach. Nothing changes subc-side. Story 1.8 PUSH contract final.

- **STORAGE-SPLIT Oracle-validated (AFT, conditional GO) → config-cardinality model + new session-attach outcome:** AFT's "artifacts are pure functions of files" was FALSE for search (depends on max-file-size + ignore rules) and semantic (fingerprint = backend/model/base_url/dim → the flip/flop bug). Resolved three-tier config model: **RootConfig** (artifact-shaping + executable-choosing keys: search max-size, semantic backend/model/base_url, LSP server set + binary paths, diagnostic cache cap) = canonical per-root, reconciled across harnesses; **demand overlay** (feature booleans search/semantic/callgraph/inspect) = ref-counted UNION (build if ANY attached session wants it, expose tool surface per session — NOT first-attach-wins); **SessionConfig** (restrict_to_project_root, url-fetch, formatter/checker, bash, backups, filters) = per (harness,session). RootConfig reconciliation is AFT-internal; subc config plane UNAFFECTED (subc stores/serves the two-tier docs; AFT computes RootConfig from them).
- **NEW session-attach OUTCOME + flow (subc-side):** session-attach can now be REJECTED with `config_divergence` (a later harness computing a divergent RootConfig for a warm root → reject with an actionable diff). Mechanism = the existing channel-0 ERROR {code,message} frame (now the unified error schema). **Flow refinement:** session-attach is REQUEST/RESPONSE THROUGH AFT (not subc-local-ack) — subc parses triple + resolves ProjectRootId, relays attach (triple + harness RootConfig keys, forwarded uninterpreted) to AFT; AFT ensure-or-creates ProjectActor + reconciles RootConfig → ACCEPT (subc binds route channel + attach-ack) or config_divergence (subc returns ERROR, binds nothing, no session added). subc's channel binding is GATED on AFT accept (upgrades earlier "binds-then-relays" → "relays, awaits, binds-on-accept"). Structurally kills the semantic flip/flop (NFR10). Future data-plane (session-attach) story; no current code change. AFT inventory v3 "Config cardinality & reconciliation".

- **THIN-CORE LINE on config relay (AFT corrected my slip):** subc does NOT pre-select "RootConfig-relevant keys" (that = interpreting config = denting thin-core). subc forwards config WHOLE + OPAQUE + PROVENANCE-PRESERVING (un-merged user-tier + project-tier docs, tier labels intact); AFT does ALL semantic work (merge + trust-boundary + partition + reconcile + expand). subc's only obligation: deliver un-merged provenance-tagged tiers (the enforcement inputs for the trust boundary — project config can't override user-only keys lsp.*/api_key_env). The merge+trust-boundary logic moves from AFT's TS plugin config.ts into AFT-core (AFT plugin-thinning). Module-side enforcement OK for first-party AFT (user-privilege); subc-side enforcement only if untrusted modules are ever admitted (future, not v1). AFT also single-flights per-root ProjectActor creation → "first attach establishes canonical RootConfig, later attaches reconcile" is race-free regardless of arrival order (AFT-internal; subc unaffected).

- **AFT↔subc BOUNDARY DESIGN COMPLETE (both sides, pending implementation).** Config-relay thread closed. Final division: subc transports un-merged provenance-tagged config tiers (user+project separately, labels intact, either may be absent); AFT layers defaults<user<project (project blocked from user-only security set lsp.*/api_key_env/semantic.api_key_env) → merge → partition → reconcile (union demand, reject-on-divergence) → expand. Trust-boundary escalation path on record: module-enforced while first_party; moves to subc (holds provenance) when trust_tier drops to untrusted — out of v1 scope. Whole AFT↔subc contract summary: 17-byte envelope unchanged throughout; (root,harness,session) triple + opaque config ride the channel-0 session-attach; session-attach is a vetoed request/response through AFT (relay→reconcile→bind-on-accept, config_divergence reject via channel-0 ERROR); PUSH fan-out + durable replay owned by AFT, subc routes channel→connection; single AFT process (singleton), AFT self-demuxes by (root,harness); thin-core intact (subc never parses non-channel-0 bodies, never partitions config). subc will re-engage AFT at the session-attach data-plane story for wire-level details.

- **PER-MODE FIFO CONTRACT (tightened, Ufuk-prompted) + #9 design decision (council bg_e230ea87, 8 models strong):** concurrency is PER-MODULE via manifest `concurrency` (FR16), with explicit ordering semantics now pinned: `serial` = one in-flight, strict order; `module_managed` = concurrent in-flight ACROSS channels, per-channel FIFO submission preserved WITHIN a channel (module schedules internally) = AFT's mode; `stateless_parallel` = fully parallel, no ordering (opts out of FIFO). **#9 design = A3+B2:** replace `dyn Backend` with closed `enum Backend` (data-plane; channel-0 ControlHandler stays a dedicated field), `async fn handle(ctx: RouteCtx, frame) -> Result<(), RouterError>`, all output via a per-connection bounded `mpsc` (`FrameSink`) drained by one writer task. **#9 SCOPE (Ufuk: 'go as initially planned'):** lock the policy-AGNOSTIC shape (enum + FrameSink + async + writer-task + read/write split); dispatch stays SERIAL now (only real backends = channel-0 Control [inherently serial] + test Echo; ForwardBackend is Epic 2). The mode-aware dispatcher (spawn-per-request + per-channel FIFO queue + flow-control Semaphore + CANCEL + inflight map) lands in Epic 2 WITH ForwardBackend (its first real consumer) — not built against a test echo now. Shape does NOT bake in serial-forever; writer-task/sink built ONCE, Epic 2 swaps only the dispatch within it (no double rewrite). Council caveats (per-channel FIFO, CANCEL permit leak) become real only with ForwardBackend → deferred with it. Fallback if plugin extensibility ever needed: A1 (#[async_trait]) + same FrameSink (axis-A-agnostic).

- **PATH-IDENTITY ALIGNMENT (AFT P0 / subc P5 cutover) — shared-code proposal:** subc + AFT ProjectRootId canonicalization MUST NOT diverge or frames misroute at cutover (session attaches as root X in subc, AFT resolves Y → wrong ProjectActor). subc's identity.rs TODAY = `std::fs::canonicalize` (realpath) on the given path: absolute/normalized/symlinks-resolved, /var→/private/var implicit (symlink), worktrees distinct by distinct canonical PATH (no git parse, no RepoId), non-existent → typed reject (no fallback). subc does NOT do (a) workspace-root detection (walk-up to lockfile/git-root) or (b) case-folding — these are the BIGGER divergence risks (if AFT walks-up and subc doesn't, roots differ). **Alfonso's structural counter to AFT's "shared spec + test vectors": put the canonicalization pipeline in SHARED CODE (subc-protocol module or a `subc-paths` crate AFT path-deps) → byte-identity BY CONSTRUCTION, not reconciliation; RepoId + AFT-only concerns layer on top; test vectors become a regression guard on the shared code.** Eliminates the divergence class (preferred over policing it). OPEN rules to pin in the spec: workspace-root walk (who/where — harness sends resolved root, or shared walk-up?); case-folding on case-insensitive FS; non-existent policy for a transiently-missing root. NEXT: Alfonso writes the canonicalization spec + initial vectors (seeded from subc's behavior) for AFT review; identity moves to the shared crate AFTER the in-flight reshape/cleanup mason (bg_82e12ad3) merges (avoids lib.rs conflict). AFT P0 (path-identity) just starting → builds against the shared code/spec.

- **PATH-IDENTITY shared-crate design CONVERGED (AFT):** shared primitive = **`cortexkit-paths`** — PUBLISHED to crates.io, dependency-light (pure path: NO serde/transport/git), cortexkit-NEUTRAL name (AFT ships standalone as `agent-file-tools` on crates.io, can't path-dep an unpublished/transport-heavy crate; subc-protocol disqualified as home). Lives as a workspace member in the subc monorepo (or its own repo — minor, pending Ufuk); **publish-to-crates.io is a Ufuk-gated release step**, coordinated when AFT's P0 release needs it (dev git-dep meanwhile). LAYERING: walk-up (cwd→workspace root, nearest git/lockfile) = HARNESS-owned, shared in @cortexkit/aft-bridge (so OC+Pi can't diverge); canonicalize = shared Rust `cortexkit-paths` (subc+AFT) + a TS mirror in aft-bridge. **Alfonso refinement:** subc/AFT RE-canonicalize the received root authoritatively (cheap realpath at attach), so the TS realpath is belt-and-suspenders — load-bearing cross-language parity is WALK-UP (both plugins reach the same dir), NOT pixel-perfect TS realpath. Case: no explicit fold (rely on realpath stored-case). Non-existent: ProjectRootId rejects; AFT's operation-target/create-file existing-ancestors fallback layers on CanonicalPath AFT-side. RepoId: AFT-side, NOT shared (needs git).
- **LEASE GRANULARITY DECISION (Alfonso, AFT-asked):** subc per-project lease is **per-PATH (per ProjectRootId), NOT per-repo.** Worktrees are distinct working states (diff branches/changes) with distinct per-path artifacts (RootConfig per-root + AFT ProjectActor per-path) → separate leases + separate dreaming is correct, not contention (Ufuk runs masons in worktrees; each is its own project). Per-repo lease would wrongly serialize analysis across different working states. Nothing subc coordinates is per-repo (lease/scheduler/config per-path; git object store read-not-leased; embedding = machine-global singleton). RepoId→shared only if a real per-repo coordinated resource ever emerges (not v1).

- **PATH-IDENTITY spec CONVERGED with AFT (1 delta folded):** DELTA 1 = Windows verbatim `\\?\` prefix MUST normalize to non-verbatim IN the shared crate (`\\?\C:\foo`→`C:\foo`, `\\?\UNC\server\share`→`\\server\share`) — `std::fs::canonicalize` returns verbatim on Windows and every AFT consumer + harness walk-up use non-verbatim, so leaving it would break by-construction parity; reference impl = AFT's `windows_non_verbatim_path` (resolver.rs/position.rs); crate Windows-correct from v0 (AFT needs it standalone P0). #2 NonExistent-reject confirmed (AFT's create-file normalize fallback stays AFT-side on CanonicalPath). #9 case-insensitive stored-case provisionally confirmed (AFT adds live vector at crate-land). Spec (docs/cortexkit-paths-canonicalization-spec.md) updated with the Windows rule + vectors 10/11. NEXT: create `cortexkit-paths` crate (move ProjectRootId+IdentityError+canonicalization from subc-core/identity.rs; keep SessionId+RequestIdentity in subc-core), launched in PARALLEL with the 1.0 bootstrap mason (only lib.rs pub-block overlaps; resolve at merge). AFT git-deps it for P0 dev; publish to crates.io stays Ufuk-gated.

- **DATA-PLANE FORWARDING design (Oracle-reviewed bg_8c592eb1, gpt-5.5 xhigh) — the keystone that unblocks AFT Leg 1:** client REQUEST on route-channel C → ForwardBackend enqueues to the module's FrameSink + RETURNS (does not await) → module's RESPONSE/STREAM/PUSH arrives on the module connection's reader → demuxed by (module_connection_id, C) → client's FrameSink. CORRECTION: dynamic route state lives in a NEW `ForwardingTable` (shared Arc+Mutex), NOT Registry (keep: manifests/liveness) or Router.backends (keep: static/test only). Bidirectional map: (client_connection_id, C)→module endpoint + (module_connection_id, C)→client FrameSink, KEYED WITH MODULE CONNECTION GENERATION (restart can't reuse stale bindings). Channel reserve-before-attach-relay, commit-on-module-accept, release-on-reject/ack-fail. Reuse the existing one-reader-per-connection loop (do NOT add a 2nd reader); module channel-0 attach/detach responses go to a pending-relay waiter table, not normal control handling. Cleanup owned by ForwardingTable: client GOODBYE/drop→best-effort detach-relay + remove bindings; module drop→remove endpoint + all routes for that generation; NO auto-rebind across restart (clients reattach). SCOPE: "serial submission + async forwarding" — concurrent-in-flight is unavoidable+fine ("serial"=serial read/submission order, NOT serial request lifetime); CANCEL + per-channel flow-control windows SAFELY DEFERRED to Epic 2 (CANCEL may pass through syntactically, no cancellation promise yet). FIFO preserved by serial-read + ForwardBackend awaiting send + single module writer (reordering only if per-frame send tasks / unordered try_send). Channel-0 ControlHandler must GROW to dispatch async/vetoed session-attach vs the existing sync module-registration HELLO; don't conflate module GOODBYE with session detach. Effort: Medium (1-2d coherent slice). PLAN: spike (happy-path attach→request→response demux vs fake-aft-stub) FIRST, then expand (failure modes + multi-client + FIFO-stress + generation/detach robustness + config_divergence).

- **ATTACH config wire-shape = ORDERED LABELED TIER LIST (agreed both Alfonsos, AFT's call as consumer; pending final co-sign at AFT's shim):** `config: [ {tier:"user", doc:<opaque>}, {tier:"project", doc:<opaque>} ]` on BOTH AttachRequest and AttachRelay; precedence = LIST ORDER (later wins). Chosen over a two-field {user,project} struct because (1) precedence becomes a WIRE property not AFT's fold code, and (2) a future third tier (machine/enterprise, or post-embedding-extraction daemon-defaults) is a list-insert, NOT a protocol break + re-co-sign. subc forwards the list WHOLE+OPAQUE, never merges/partitions/relabels (preserves memory 6515 thin-core). The spike currently uses a single opaque `serde_json::Value` PLACEHOLDER — replace with the labeled list at the subc-protocol PROMOTION step (after hardening lands). SECURITY INVARIANT (load-bearing for AFT's trust boundary): the tier LABEL is stamped from SOURCE (which config file PATH was read), NEVER derived from doc content — a malicious project doc must not be able to relabel itself "user" (the whole "project can't override lsp.*/api_key_env" boundary depends on it). subc-side enforcement: preserve labels exactly as received, reject any later frame that tries to rewrite a bound route's tiers. OPEN (AFT picks at co-sign): ENFORCEMENT POINT = Option A (lean) harness-side stamp — mode-2 plugin/aft-bridge reads user-path→"user", project-path→"project", subc forwards opaque + label-preserving, subc stays config-path-agnostic (thin-core), trust chain all first-party; vs Option B subc reads the two files itself from a known scheme (shorter trust chain but subc gains config-location awareness, dents thin-core). Alfonso(subc) leans A.

- **ATTACH config ENFORCEMENT POINT = OPTION A, LOCKED both sides (was open):** per-harness plugin/aft-bridge stamps each tier by source path; subc forwards opaque + label-preserving + config-path-AGNOSTIC. Option A is FORCED not merely preferred — under one-AFT-serving-many-harnesses the "user" tier doc is PER-HARNESS (OpenCode user-config path != Pi user-config path ~/.pi/agent/aft.jsonc), so Option B (subc stamps) would require subc to embed + keep-in-sync every harness's config-path scheme = the thin-core dent + a drift risk (subc's "user path" diverging from the plugin's). Plugin-thinning consequence: the plugin STOPS merging (today config.ts read+merge+trust-strip) → becomes read+label-by-source+forward; merge+trust-boundary+partition+reconcile all move to AFT-core, which then receives PER-HARNESS raw tiers — exactly the input RootConfig cross-harness reconciliation + reject-on-divergence needs. subc commitments restated: preserve labels exactly, never rewrite in transit, reject any frame relabeling a bound route's tiers.

- **ATTACH config contract CO-SIGNED + LOCKED (both Alfonsos, 2026-06; shapes in subc-protocol::session @ 3ade87e):** all three field decisions confirmed by AFT (the consumer/parser): (1) doc=VERBATIM TEXT — consequence ON RECORD: AFT-core (Rust) gains a JSONC parser (plugin-thinning moves parse+merge from TS plugin into Rust core); (2) tier=open String, unknown=>least-privileged; (3) source KEPT, but AFT commits to using it for format-inference+diagnostics ONLY, NEVER to re-derive/validate the tier label (source=metadata, label=trust authority, kept separate — else harness config-path schemes leak into AFT-core). Vec<ConfigTier> 0/1/2 entries, precedence=order, AttachAck-accept / config_divergence-veto. AFT depends on subc_protocol::session at its shim via local path-dep. CROSS-PROJECT GATE SURFACED: AFT P0 (identity rewire) is implementation-DONE + committed (Rust+TS+tests green) but its FINALIZE/release waits on the cortexkit-paths crates.io PUBLISH, which is Ufuk-gated — flagged to Ufuk.

- **cortexkit-paths PUBLISH PREP (Ufuk approved "prep now, Ufuk runs publish"):** Cargo.toml set to version=0.1.0, license="MIT" (matches AFT crates), removed publish=false, added keywords/categories. `cargo publish --dry-run` PASSES (packages 5 files, 15.1KiB). OPEN for Ufuk before `cargo publish`: (1) repository URL — this repo has NO git remote yet, so `repository` is left as a TODO comment (publish works without it, just a warning); (2) optional LICENSE-MIT text file (needs copyright holder line; SPDX `license="MIT"` field already satisfies crates.io). Ufuk runs `cargo publish -p cortexkit-paths` with his crates.io creds. PUBLISH DE-RISKED: AFT ran NATIVE-Windows-11 verification (not just cross-compile) — Rust cortexkit-paths cfg(windows) vectors 3/3 PASS (verbatim \\?\C:\ strip, verbatim UNC strip, lowercase-drive uppercase) + all 8 cross-platform vectors green on Windows; AFT's TS aft-bridge mirror (normalizeWindowsRoot + projectRootKeyHash) 9/9 PASS on native Node — lowercase/uppercase drive → SAME canonical path AND same 16-hex hash, confirming TS↔Rust agreement + TS-internal self-consistency on Windows. No fix needed to the shared crate. AFT swaps its path-dep → published version once live; AFT P0 release rides validated Windows code.

- **CANCEL (Story 2.3) design = DUMB-FORWARD (Oracle-reviewed bg_cd286aa5, gpt-5.5 xhigh):** subc forwards the pure-header `Cancel` frame on the target request's ROUTE CHANNEL (existing client_route demux, frame-type-agnostic — NO subc-core change, like 2.2); the MODULE owns abort-safety + emits the TERMINAL frame for that corr (`Error{code:"cancelled"}` for an aborted non-mutating read, or a normal `Response` if a mutation completed). subc tracks NOTHING per-corr and SYNTHESIZES NOTHING — "resolves the corr as cancelled" = delivers the module's terminal frame (preserves §6.1: subc never orders execution / reasons about resources / decides semantic outcomes). Per-corr tracking would break thin-core. AC wording tightened accordingly. STUB+TEST work only: stub tracks in-flight by `(channel,corr)` (corr reuse across channels is valid), models a cancellable delayed request (Cancel→abort→one `Error{cancelled}`) AND a non-cancellable request (ignores Cancel, completes with `Response` = mutation mid-commit). CRITICAL stub invariant: CLAIM THE TERMINAL UNDER A LOCK (remove/mark the (channel,corr) entry before sending the terminal) so a cancel-vs-completion race yields EXACTLY ONE terminal. Tests: cancel-before-response, cancel-after-response (idempotent no-op), double-cancel, non-cancellable-completion, unknown-channel-cancel; assert stub OBSERVED the Cancel (proves forwarding, not hidden subc synthesis); assert subc-core src UNCHANGED. RACE verdicts: cancel-vs-response benign best-effort (subc no guard); corr-reuse is the client's contract (monotonic/no-reuse per route channel) not subc's; double/stale cancel = module no-op; cancel-on-unknown-channel keeps `unknown_channel` ERROR (no special-case). SCOPE LINE: flow-control-window interaction (CANCEL bypasses the window, must NOT consume a request credit, must NOT free the original's credit until the module terminal) = Story 2.4, NOT 2.3. Effort: Short.

- **FLOW-CONTROL WINDOW (Story 2.4) — Oracle-reviewed (bg_0504814e, gpt-5.5 xhigh); PENDING Ufuk decision on scope:** VERDICT: 2.4 is NOT proof-only (unlike 2.2/2.3). The existing bounded-mpsc (CONNECTION_EGRESS_BUFFER=64) + serial read loop bound TRANSPORT buffering, NOT per-channel un-acked REQUESTS — §6.1 explicitly lists "bounded un-acked in-flight requests per channel" as part of the locked contract. It's the FIRST story adding real new subc-core PRODUCTION mechanism. THIN-CORE FIT: a minimal per-channel CREDIT COUNTER fits IF subc classifies ONLY the envelope `type` byte (Request=+1; Response|Error|StreamEnd=−1 release; Push/StreamData/Cancel=no release) — never parsing bodies, deciding mutation-safety, synthesizing outcomes, or matching corrs. RECONCILE w/ 2.3: the 2.3 "no terminals/dumb-forward" decision is SCOPED TO CANCELLATION SEMANTICS (subc doesn't resolve/synthesize cancel outcomes); 2.4 recognizes terminal frame KINDS only to RETURN TRANSPORT CREDITS — that's the line, acceptable use of the wire envelope subc already reads. VALUE: prevents one channel monopolizing the shared module connection with unlimited outstanding work (bounded mpsc bounds queued frames, not in-flight-awaiting-terminal). HONEST LIMITATION: minimal design does NOT give full same-socket cross-channel fairness (one FIFO client stream — a saturated channel can still HoL later frames on the same socket); full per-channel fairness = a larger scheduler story (client-side credits / separate connections / per-channel queues), NOT 2.4. DESIGN: Arc<ChannelFlow> created in ForwardingTable::commit_route, shared by ModuleRoute+ClientRoute; credit acquired in ForwardBackend::handle BEFORE forwarding a Request (await if at window; release immediately if send fails); released in the module-demux path on terminal type (guard underflow — warn, never over-release); CANCEL bypasses (no credit, doesn't free original's credit). Window MUST be < global module queue size. Effort: Short. ALFONSO(subc) PROPOSED REFINEMENT (pending Ufuk): tie window SIZE to the manifest `concurrency` declaration — serial→1 (subc then actually enforces declared seriality: never 2 in-flight to a serial module, folding in §6.1's "honor declared concurrency"/FR16), module_managed→N (default e.g. 32), stateless_parallel→high cap (OOM guard only) — makes the window MEANINGFUL vs an arbitrary constant.

- **LIVENESS/STATUS-FROM-CACHE (Story 2.5) — Oracle-reviewed (bg_f4d933c9, gpt-5.5 xhigh) + Alfonso AFK-adaptation:** MECHANISM: module pushes OPAQUE latest-wins status; subc caches it per **(ModuleEndpointId{generation}, route_channel)** — NOT per root (subc never owns root fan-out; AFT emits one status per channel, same as PUSH). Passive polls answered LOCALLY, never forwarded: LIVENESS from subc's OWN state (route's module endpoint still active+registered → live; never stale), STATUS from the cache (verbatim opaque bytes; may be briefly stale; `status_unavailable` on miss). THIN-CORE LINE: clean iff subc reads only wire header/type/channel + stores opaque bytes + returns them unchanged (it may KNOW "this frame is a status-cache update" as a protocol routing fact, like it knows Request/Push/Cancel/terminal/channel-0); crosses the line if subc parses status JSON, keys by status content, merges root status, or uses status for scheduling. LIVENESS def: process Running + registration active (NO heartbeat — that'd reintroduce module responsiveness into the passive path). **AFK ADAPTATION (Alfonso, deviates from Oracle's literal "new FrameType kinds"):** Oracle proposed header-visible StatusUpdate/StatusPoll/LivenessPoll frame TYPES — but that means adding variants to the LOCKED, AFT-co-signed FrameType enum, which I will NOT do during AFK. INSTEAD use CHANNEL-0 CONTROL RPCs with body discriminators (the attach pattern, subc parses the channel-0 control ENVELOPE but treats the status PAYLOAD as opaque): module→subc = channel-0 PUSH body `StatusUpdate{route_channel, status:String(opaque)}` (cached, not forwarded); client→subc = channel-0 REQUEST body `{op:"status"|"liveness", route_channel}` → subc answers locally (StatusReply{status}/Error{status_unavailable} or LivenessReply{live}). NO envelope change; shapes subc-core-LOCAL (like AttachRequest pre-promotion). State: a `StatusCache` in/beside ForwardingTable, evicted on the SAME teardown paths as routes (cleanup_connection: client detach, attach reject, module disconnect/restart). DEFER: durable status across restart, root reconciliation, real AFT status taxonomy, heartbeat/readiness, TTL, AND promotion+AFT-co-sign of the wire shapes (status poll/update become a real contract only when AFT wires the module side — flagged for Ufuk/AFT review, NOT locked during AFK). Effort: Short proof slice. Liveness for the proof = route endpoint active in ForwardingTable+Registry (full Supervisor-state threading = deferred Medium hardening).

- **TS Mode-2 plugin client — HOME + SCOPE (Stories 1.8b/1.2) — Ufuk decided 2026-06:** HOME = **subconscious monorepo** (NOT commons), on the **OWNERSHIP principle**: subc's owner (this project/Alfonso) must hold authority over the subc-client lib — changes to it ARE subc-contract changes and belong under subc's umbrella, not a future commons agent's. Bonus: co-located with `subc-protocol` (the Rust wire source of truth) so the Rust+TS wire mirrors change in ONE repo/PR (drift killed by co-location + a TS↔Rust e2e against the fake-AFT stub — stronger than static vectors). Package = `@cortexkit/subc-client` (GREENFIELD — commons today has ONLY `crates/cortexkit-paths`; the planned `@cortexkit/aft-bridge` was never built, nothing to refactor). **SCOPE = GENERIC MODULE-NEUTRAL CORE ONLY:** walk-up→ProjectRootId, UDS discovery + daemon-absent fallback (1.2), 17-byte envelope codec, channel-0 session-attach (triple + opaque config tiers), channel mux, corr correlation, PUSH routing, CANCEL, liveness/status poll (2.5), reconnect. subc ships the core + makes adapters EASY; the per-module **ADAPTERS** (which config files to read+stamp as tiers, module id/manifest, module-specific PUSH interpretation) are **AFT's and MC's duty in THEIR repos**, NOT subc's. AFT = consumer #1, MC = consumer #2 → designed module-neutral from the start. CORRECTNESS INVARIANT: MC and AFT MUST compute byte-identical ProjectRootId or single-AFT-per-root routing breaks → the walk-up is a routing-correctness invariant, shared by construction. Source in subconscious (private ok), PUBLISHES to npm (source-location ⊥ publish-target, like cortexkit-paths: commons source → crates.io). Polyglot monorepo: add a TS package + TS CI lane (incl. the TS↔Rust e2e booting subc + fake-AFT stub). LOGISTICS DEFAULT (proceeding unless Ufuk vetoes): the TS **canonicalization parity mirror** lives in **commons next to cortexkit-paths** (shared identity primitive, co-located Rust+TS, drift-protected by the existing Windows/cross-platform vectors); subc-client DEPENDS on it — identity stays shared, wire client stays subc-owned. Pending: NPM_TOKEN on subconscious at first publish.
- **SPAWN-MODE SIGNAL (subc→AFT) — subc-proposed 2026-06, pending AFT co-sign of the env name:** subc's Supervisor signals "you're running under subc" via a SINGLE env var `SUBC_SOCKET=<uds-path>` set at spawn. Presence = subc mode (AFT picks shared-storage + multi-root ProjectActor demux); value = the endpoint AFT connects OUT to, then sends HELLO/manifest unchanged. Absence = standalone (per-harness storage, NDJSON). ONE branch point → every subc-mode path ships DORMANT in standalone by construction (satisfies AFT's dormancy requirement; AFT P2 storage-scope activates only when `SUBC_SOCKET` present). No separate boolean flag, no implicit default — one signal carries mode+endpoint. Pre-HELLO bootstrap; HELLO unchanged. fd-passing a pre-connected socket = possible later robustness optimization, NOT v1 (connect-to-path simpler + cross-platform). CONTEXT — coexist-vs-freeze (Ufuk raised; AFT framed): the freeze-vs-coexist choice reduces to ONE engineering property — can AFT's multi-root ProjectActor model SUBSUME standalone as its N=1 case via ONE codepath? Yes → coexist nearly free, no release freeze, ships dormant-or-subsuming (P0/P1 discipline). No (process-global statics LAST_EMITTED_STATUS_BAR/SEMANTIC_REFRESH_ACCOUNTING can't move per-actor) → freeze justified. AFT's P3 ProjectActor-extraction spike decides empirically. subc's OWN migration already chose SUBSUME (daemon-absent fallback 1.2/4.3 = standalone as the degenerate case of one subc-client codepath) — worked example + strong prior. Alfonso(subc) engineering rec to Ufuk: subsume-unless-the-spike-says-otherwise. FINAL freeze-vs-coexist = Ufuk's product call (PENDING).
- **Epic 3 MCP = SEPARATE RUST BIN `subc-mcp` (Ufuk corrected my TS-shim proposal) 2026-06:** MCP is its OWN Rust binary in the subconscious monorepo that handles EVERYTHING MCP-related, connecting to subc as a participant — NOT a per-harness TS shim on the TS subc-client. WHY (Ufuk's instinct, agreed): a Rust bin links `subc-protocol` NATIVELY → ZERO wire-mirror drift for the MCP path (the TS-client drift concern simply doesn't exist here); ALL MCP logic centralized in one codebase (not scattered/duplicated per harness); clean quarantine — subc-core stays 100% MCP-agnostic / body-opaque (the surviving half of my proposal). CORRECTS my earlier (chat-only) claim that "Mode 1 collapses onto the TS subc-client" — WRONG: Mode 1 is its own Rust track. The two modes are PARALLEL/DIFFERENT/INDEPENDENT: Mode 1 = `subc-mcp` Rust bin (native subc-protocol, for external MCP harnesses); Mode 2 = `@cortexkit/subc-client` TS (in-harness OC/Pi plugins). Both provable vs the fake-AFT stub; buildable IN PARALLEL (no file overlap — one TS package, one Rust crate/bin). subc-mcp is a CONSUMER (attaches sessions, forwards tools/call to AFT) — cross-instance dedup free (all roads → one subc → one AFT). OPEN SUB-QUESTIONS (pending Ufuk, architecture-first): (1) lifecycle/transport — stdio per-harness (harness spawns thin subc-mcp → connects to shared subc daemon; max MCP compat) vs subc-supervised long-running HTTP/SSE service; (2) formal module (registers manifest) vs pure session-client gateway; (3) Rust `subc-client` crate extracted now vs inlined in subc-mcp until a 2nd Rust client (e.g. InterSUBC). Spike B (MCP↔envelope transform trace) STILL applies — now a Rust hand-trace defining subc-mcp's transform layer before build.
- **SPAWN-MODE SIGNAL — connect-failure path CO-SIGNED (subc+AFT) 2026-06** (refines the SUBC_SOCKET entry above): (1) **BIND-BEFORE-SPAWN GUARANTEE** — subc binds + is accepting BEFORE the Supervisor spawns AFT with SUBC_SOCKET set (already the order: bootstrap binds/listens → Supervisor.spawn()). So SUBC_SOCKET-present ⟹ socket-listening in correct operation; the spawn-before-listening race is eliminated by construction. (2) **FAIL-LOUD INVARIANT** — SUBC_SOCKET present-but-UNCONNECTABLE = broken invariant (only subc sets it, only after binding) → AFT FAILS LOUD, NEVER a silent standalone downgrade (that would be the split-brain the daemon exists to kill + the 4.3 two-writer hazard). (3) **TOPOLOGY OWNERSHIP** — the HARNESS subc-client owns ALL topology decisions; AFT-spawned-by-subc NEVER decides topology. daemon-ABSENT (no live subc at the discovery path) → harness-client falls back to in-process standalone AFT (1.2), spawned WITHOUT SUBC_SOCKET. daemon-DIED-mid-session → harness-client reconciles+falls back (4.3, no two-writer). "Silent fallback to standalone" exists but is the harness-client reacting to subc-ABSENCE, never AFT reacting to a dead SUBC_SOCKET. (4) **STALE-ENV CLOSURE** — SUBC_SOCKET meaningful ONLY on the direct subc→AFT spawn: set explicitly+fresh by subc's Supervisor, explicitly CLEARED by the harness-client's standalone-spawn path; never relied on via ambient inheritance. (5) **RETRY** — not needed for the race given bind-before-spawn; a SHORT bounded connect-retry (~100–500ms) is acceptable cheap insurance against a transient accept hiccup THEN fail-loud — never a silent downgrade. AFT owns retry count; co-signed invariant = present-but-unconnectable ⟹ fail-loud, never silent-standalone. AFT P3 ProjectActor spike RUNNING; early static-inventory grep (~3–4 per-root statics, rest correct-global) leans subsume.
- **SUBC_SOCKET-clearing OWNERSHIP (refines stale-env closure #4) — subc 2026-06:** the standalone-spawn env hygiene (CLEAR SUBC_SOCKET + strip subc-mode signals from the child) lives in the GENERIC subc-client CORE's daemon-absent fallback helper (Decision 1), NOT per-module adapters. Adapters supply only the module-specific spawn (AFT binary+args / MC binary+args); the core GUARANTEES the clearing — so the stale-env / split-brain closure is STRUCTURAL for every module (MC inherits it; no adapter can forget it and reopen the hole). Footgun eliminated programmatically, not by per-adapter discipline. Transitional: a plugin that spawns standalone TODAY pre-subc-client builds it locally; durable home = the core helper. AFT adopted the topology division verbatim (AFT-spawned-by-subc NEVER decides topology; harness-client owns all fallback).
- **TRANSPORT: DROP UDS FULLY → TCP-loopback + KEY (Ufuk decided 2026-06, "no brainer") — CLEAN CUTOVER:** Windows killed UDS — tokio's `UnixStream`/`UnixListener` are cfg(unix)-only (don't compile on Windows), Node uses named pipes (not AF_UNIX) for Windows IPC, AF_UNIX on Windows has no 0600. Rather than a UDS-on-Unix / named-pipe-on-Windows split, subc uses ONE uniform transport everywhere: **loopback TCP (127.0.0.1/::1) + a shared KEY.** Replaces UDS-0600's OS-enforced owner-only access with: loopback-bind (no off-machine reach) + a random key only the owner can read (key in a per-user FILE with 0600/user-ACL — the only remaining platform-specific bit, and writing an owner-only file is trivial cross-platform, unlike an AF_UNIX socket). Clients present the key on connect (HELLO for the envelope transport; bearer token for the MCP HTTP endpoint); subc verifies CONSTANT-TIME, rejects+closes on bad/missing. DISCOVERY = a per-user **CONNECTION FILE `{port, key, …}`** at a well-known path (auto-written by subc = the "config" clients read); lean EPHEMERAL port (bind :0, OS picks free) to KILL the per-user port-conflict concern Ufuk raised earlier; fixed-configured-port is the alternative. RESOLVES MCP **2a**: sMCP exposes localhost HTTP + bearer token (consistent model; note TWO auth boundaries — harness→sMCP token AND sMCP→subc key — likely distinct secrets). REVISES: Story 1.0 bootstrap (UDS-file-bind + connect-first PING/PONG + 0600-socket → TCP-port + connection-file + key); §4.10 spawn model (discovery = read the connection file; mode still = explicit invocation, never probe); drops the SUBC_SOCKET env var entirely. CLEAN CUTOVER — rip out ALL `UnixListener`/`UnixStream`/0600-socket/XDG-socket-path code, no coexistence. FOUNDATIONAL → lands BEFORE subc-mcp / TS-client build on top. PLAN: Oracle adversarial pass on key-lifecycle + singleton-over-TCP races → mason cutover. PENDING Ufuk: ephemeral-port+connection-file vs fixed-port.
- **PORT MODEL RESOLVED (Ufuk 2026-06): FIXED CONFIGURABLE PORT** (not ephemeral) — rationale: a static known port is easier to reason about + diagnose (a gateway on a stable port shows up in `lsof`/`netstat`, stays constant in logs, is directly probeable). subc binds a DEFAULT port, overridable via daemon-config; on bind-conflict it **FAILS LOUD** with a clear "port in use → set the port in config" message — NEVER silently reselects another port (silent reselect would destroy the diagnosability that motivated fixed-port AND reopen the stale/ambiguous-port problem). The CONNECTION FILE remains the client discovery source (`{port, key}`); fixed-port just pins its `port` field to a stable human-known value — clients still read the file for the KEY (random per subc start). Multi-user-on-one-machine + occupied-port both resolved by config-override. SECURITY NOTE: a fixed well-known port makes the **key-leak-to-reused-port hazard (Oracle #2) MORE salient** (a known port is likelier to be grabbed by an unrelated process after subc dies) → the server-proves-it-is-subc-BEFORE-client-sends-key handshake is even more load-bearing. Folds into the running Oracle review (bg_c54d0bb3, which evaluates both branches).
- **TCP+KEY TRANSPORT — Oracle security/singleton design (bg_c54d0bb3, gpt-5.5 xhigh), ADOPTED:** (1) **SINGLETON primitive = a per-user ATOMIC START LOCK** (`O_CREAT|O_EXCL` lockfile / lock dir), NOT the TCP port (the port is no longer the contended resource). Keep the connect-first→lock→re-probe→reclaim-stale shape. (2) **CONNECTION FILE atomically published** — write `{schema, endpoints, key, daemon_id, pid(advisory), daemon_ver}` to a same-dir temp with owner-only perms ALREADY applied, then atomic-rename; readers never see a partial file. (3) **LIVENESS = authenticated daemon PROOF, not "port connects"** — a stale file's port may be reused by an unrelated process; live only if the endpoint returns a valid subc server-proof for that file's key+daemon_id (connect-refused/timeout/invalid-proof/wrong-daemon_id/HTTP-garbage = stale/foreign). (4) **HANDSHAKE ORDER prevents key leak (THE sharp risk):** client sends a NON-SECRET probe nonce → subc replies `daemon_id + server_nonce + ver + HMAC(key,"subc-server-v1"||client_nonce||server_nonce||daemon_id)`; client VERIFIES that proof, THEN sends `HMAC(key,"subc-client-v1"||…)`. The RAW KEY is NEVER transmitted (only HMACs); a reused-port stranger can't forge the server-proof → client aborts → no leak. Domain-separated + nonce'd (anti-replay). **PRE-ENVELOPE transport handshake — the locked envelope is NOT touched** (also avoids an unauth 64 MiB alloc). (5) **KEY ROTATES per daemon restart** — clients discard cached `{port,key,daemon_id}` on connection loss, re-read file, re-validate server-proof, re-auth/reattach; old keys never accepted. (6) **AUTH BEFORE ROUTING** — gate every TCP connection in `server::handle_connection` BEFORE the router sees frames (session-attach clients use channel-0 too, not just module HELLO). (7) **TWO SECRETS** — `subc.key` authenticates sMCP→subc; `subc-mcp` issues its OWN bearer token for harness→sMCP (sharing would let an MCP-token leak become full subc impersonation). DoS v1: small pre-auth parser (not the 64 MiB body path), 1–2 s auth deadline, global unauth-connection cap, constant-time HMAC, FAIL STARTUP if owner-only file creation is impossible. **SIGNAL: keep our model** (mode via explicit INVOCATION, discovery via well-known connection-file path, custom path via argv) — NOT Oracle's `SUBC_CONNECTION_FILE` env var (avoids reintroducing ambient env / the stale-env footgun); ADOPT Oracle's ordering: bind→accept→atomic-publish→THEN spawn children. AFFECTS: the auth handshake is implemented by EVERY client (AFT module shim, sMCP, TS subc-client core) — centralized in the subc-client core so adapters inherit it; needs an **AFT co-sign of the handshake spec** (envelope unchanged, but a new pre-envelope auth prelude). **PORT: Oracle leaned EPHEMERAL (simplicity); Ufuk chose FIXED (diagnosability) — HOLDING fixed:** Oracle's design is port-INDEPENDENT (singleton = lockfile) + its handshake neutralizes the reused-port risk, so fixed is safe; cost = fail-loud-on-conflict + config-override (accepted). Effort: Medium (~1–2 days incl. 76-test migration UDS→TCP-loopback+handshake).
- **TCP+KEY TRANSPORT CONTRACT — AFT CO-SIGN COMPLETE (2026-06):** AFT fully co-signed the transport+auth+invocation contract (§4.10/§4.11, commit 629a1e4) after working the attack cases (reused-port stranger can't forge server_proof → client aborts pre-disclosure; server_proof can't replay as client_auth — domain prefixes differ; both-nonces-in-both-HMACs binds the exchange; daemon_id check; loopback+owner-only-key = UDS-0600 parity). LOCKED: server-proves-first ordering · HMAC-SHA256 domain-separated (`subc-server-v1`/`subc-client-v1`) · raw-key-never-on-wire · constant-time both verifies · CSPRNG ≥128-bit nonces · loopback-only (never 0.0.0.0) · atomic+0600 connection file · key-rotation+reconnect (4.3) · separate sMCP bearer token. **INVOCATION SPELLING CONFIRMED (AFT-verified against its real arg dispatch):** subc spawns `<aft-binary> --subc <connection-file-path>` (two space-separated argv tokens; AFT also accepts `--subc=<path>` defensively, but the canonical/spawned form is two-token). AFT's main() is a hand-rolled matcher (`--version` → known subcommands `migrate-storage`/`warmup` → else DEFAULT module request-loop); `--subc` isn't a subcommand so it falls through to the default mode by construction, where AFT branches on `--subc` presence (present ⇒ subc-mode TCP; absent ⇒ standalone NDJSON, untouched). Semantically a GLOBAL FLAG on the default module invocation — NOT a subcommand (`aft daemon`/`aft connect` would mis-name AFT as the daemon; it's the module). AFT implements its `--subc` parse + Rust auth shim WHEN the subc cutover lands, gated on Ufuk's P3-timing (coexist-vs-freeze). Contract locked until then.
- **TCP+KEY TRANSPORT CUTOVER — DONE (merged 2a7b818, 2026-06):** UDS fully ripped (git grep clean: no unixlistener/unixstream/subc.sock/SUBC_SOCKET on master); real transport now loopback TCP + key auth on the Oracle-designed handshake (auth-core f0666d1 reused untouched). 87 tests (76 behaviors preserved + 11 new). Auth gate in `server::handle_connection`: unauth-semaphore permit (cap 32) → `authenticate_server` (2 s deadline) BEFORE `router.begin_connection()` — proven a pre-auth frame never reaches the router. Singleton = `create_new` start-lock (connect-first→lock→re-probe→stale-removal→bind→atomic-publish). Liveness = authenticated probe (not port-accepts). 5 real security/singleton tests (foreign-reused-port reclaim, stale-unbound reclaim, bind-conflict fail-loud, second-singleton already-running, key-rotation old-file-fails-auth). Supervisor passes `--subc <connection-file-path>`; stub parses it + TCP-connects + authenticates. Shared test helper `tests/common/mod.rs`. **`DEFAULT_SUBC_PORT=8757`.** Reopened + closed Story 1.0's bootstrap. **TWO FOLLOW-UPS (non-blocking):** (1) the port override is wired via a `SUBC_PORT` env var as a STOPGAP — there's no daemon-config-file layer yet; when the config plane lands, the port override should move to daemon config (env var is temporary, not the intended UX — Ufuk dislikes magic env vars). (2) Windows owner-only ACL on the connection file is a `// follow-up` marker — needs native Windows verification (AFT/CI own it, same as cortexkit-paths).
- **COEXIST-VS-FREEZE — Ufuk steer (2026-06):** Ufuk comfortable with EITHER; leans "let AFT's P3 spike decide" and PRE-APPROVES a freeze/big-bang as fallback since the whole migration is ~2–3 days. RESOLUTION = spike-decides: per-root statics relocate cleanly → COEXIST (AFT keeps shipping, subc-mode dormant — design + Alfonso preference); can't → FREEZE acceptable (Ufuk-approved 2–3 day big-bang). Either way: MOVE. Pinged AFT for the P3 verdict to lock the path; relayed that subc transport cutover is DONE (merged 2a7b818, 87 tests, UDS gone) → AFT unblocked to implement `--subc` + the auth shim against the built §4.10/§4.11 (fake-aft-stub is the in-tree reference client) regardless of path.
- **COEXIST — LOCKED (AFT P3 verdict, 2026-06):** AFT's P3 ProjectActor spike DONE + merged + self-verified: relocated the hard per-root statics (SEMANTIC_REFRESH circuit-breaker trio + consecutive-failure count, LAST_EMITTED_STATUS_BAR status-bar dedup, retry ATTEMPTS map, SEMANTIC_REFRESH_ACCOUNTING) off process-globals → AppContext fields; independence test = the subsumption proof (two AppContexts in one process, trip A's breaker → B stays closed/zero failures; old static would've opened it globally); standalone byte-identical (13/13), orphan-clean. iq-10 Oracle: NO coupling breaks "standalone = N=1", NO blocker; resequenced REGISTRY-FIRST (1-entry RuntimeRegistry boundary → migrate state through it); real risks = 3 process SERVICES (request-root identity, shared stdout/progress sink, signal/shutdown aggregation), all tractable. → COEXIST: AFT keeps shipping, subc-mode DORMANT behind `--subc`, no freeze. AFT order: P3-01 registry → P3-02 per-root migration → THEN `--subc`/auth attach against real subc. Stub→real integration test agreed at the attach phase (fake-aft-stub = in-tree reference client).
- **MCP GATEWAY (subc-mcp) — Q2 RESOLVED (Ufuk): GATEWAY/CONSUMER + multi-host vision.** subc-mcp connects as a CONSUMER/gateway (attaches sessions, forwards tools/call), NOT a provider-module (hosts no tools; registry stays real-providers-only). UFUK'S EXPANSION (design-with-in-mind): AFT won't be the only tool host — subc-mcp is the SINGLE ENTRY POINT aggregating MULTIPLE tool hosts, offering what no one does today: cross-provider namespace DEDUP on tool-name clashes, granular per-tool ENABLE/DISABLE, central APP-MANAGED config (CK app connects to subc to manage all tools). v1 = AFT-only gateway, but manifest/Registry + gateway design must NOT preclude multi-host. THIN-CORE INTERPRETATION (Alfonso — Ufuk to flag if wrong): the tool→owner CATALOG (owning-module, deduped name, enabled-state) is CONTROL-PLANE METADATA (extends subc's manifest Registry; queried by gateway + app); DATA-PLANE routing stays CHANNEL-BASED/OPAQUE (gateway resolves tool→owner, attaches the owner's channel, forwards opaque — subc never parses a tool call). OPEN FUTURE FORK (NOT v1): catalog + dedup/enable-disable enforcement in subc-core (central/app-manageable/shared) vs the gateway — resolve when multi-host is built (v1 AFT-only doesn't force it); likely an Oracle design pass like the transport.
- **Q3 RESOLVED (Ufuk): EXTRACT a shared transport/auth crate now.** AFT CONFIRMED it DEPENDS on the crate (does NOT reimplement) — crypto handshake on the security path; 3 copies = footgun (one drifts on constant-time/HMAC-input-order/domain-prefix/nonce-length → silent auth bypass or wire-incompat under attack); same cortexkit-paths argument (byte-identical-BY-CONSTRUCTION beats careful re-reading of §4.11). FACTORING: a **`subc-transport`** crate holds BOTH halves — handshake (compute_proof + ClientHello/ServerProof/ClientAuth + authenticate_client/authenticate_server + pre-envelope framing) + connection_file (ConnectionInfo + read/write_atomic + generate_key/daemon_id) — extracted FROM subc-core's current auth.rs/connection_file.rs. subc-core (server) + subc-mcp (client) use it via workspace path; AFT (client) uses the PUBLISHED version. **HOME = subconscious workspace** (subc owns its wire contract — consistent with Decision-1's TS-client ownership call), **PUBLISHED to crates.io** (source-home ⊥ publish-target, like cortexkit-paths: commons-source → crates.io). AFT's 3 CONSTRAINTS accepted: (1) PUBLISHED to crates.io — release CI / Windows-MSVC / Linux-cross resolve it, not a path-dep; (2) LEAN + MSVC-CLEAN — hmac/sha2/getrandom/subtle, all pure-Rust / no-C-build-script / MSVC-clean / minimal bin-size; (3) handshake change = WIRE-BREAKING semver MAJOR → coordinated AFT+subc bump, never silent skew. Higher-level CLIENT SDK (attach/mux/reconnect) stays subc-side (subc-mcp inline for now; AFT doesn't need it). NEEDS: crates.io publish pipeline on subconscious (mirror commons' per-crate-tag release.yml + CARGO_REGISTRY_TOKEN). OPEN: asked AFT whether subc-protocol's current PATH-dep resolves in their RELEASE CI cross-platform — if not, publish subc-protocol to crates.io alongside subc-transport (same pipeline). Flag-for-Ufuk: home = subconscious-owned+published (alt = commons); pending his nod (consistent w/ Decision 1 so proceeding unless redirected).
- **Q4 RESOLVED (Ufuk): HARDEN 2.5 liveness to REAL Supervisor process-state NOW.** Thread Supervisor `ModuleState` (running / crashed / restarting / failed) into the liveness answer so "alive" reflects the actual OS process + supervision state, not just route-endpoint-active. Subc-ONLY (no AFT dependency), small. Touches status.rs / forwarding.rs (liveness response) + supervise.rs (expose ModuleState to the status path) — does NOT touch auth.rs/connection_file.rs, so it can sequence cleanly around the subc-transport extraction. The STATUS half stays opaque + AFT-co-design-later; only the LIVENESS half gains real process-state now.
- **PUBLISH-BOTH CONFIRMED (AFT, 2026-06) — corrects the path-dep premise:** AFT has ZERO subc dependency today (manifest-checked: no subc-protocol path-dep exists — the integration is entirely future-phase, lands at the cutover attach after P3-01/P3-02). AFT publishes to crates.io as `agent-file-tools`, so EVERY dep MUST be crates.io-resolvable — a path-only dep literally cannot publish (their one internal path-dep `aft-tokenizer` carries `path + version` so the published build uses the version). So published-pinned is REQUIRED. RESOLUTION: publish BOTH `subc-protocol` (flip `publish = false` → publishable) AND `subc-transport` to crates.io via a new subconscious publish pipeline mirroring commons' per-crate-tag release.yml (+ CARGO_REGISTRY_TOKEN). Source stays subconscious-workspace (subc-owned); AFT consumes PUBLISHED pinned versions (their proven cortexkit-paths = "0.1" pattern — already cross-platform-validated in AFT's Windows-MSVC/Linux-cross/macOS CI). AFT wires both at the cutover attach; Alfonso(subc) pings with first published versions to pin.