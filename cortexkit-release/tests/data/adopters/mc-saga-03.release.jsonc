{
  // adopter case: mc-saga-03
  "version": 1,
  "trains": [{
    "id": "mc-load-flake",
    "intended_commit": "mc03",
    "signing_profile": "none",
    "phases": [{
      "id": "load-gate",
      "type": "gates_local",
      "params": {
        "command": "sh",
        "args": ["-c", "if test -e .mc-saga-03-retry; then printf recovered; else : > .mc-saga-03-retry; printf transient >&2; exit 75; fi"],
        "retry_budget": 1,
        "load_class": "shared-host-contention"
      }
    }]
  }]
}
