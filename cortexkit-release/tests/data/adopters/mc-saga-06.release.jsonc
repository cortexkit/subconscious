{
  // adopter case: mc-saga-06; tests replace the runtime-minted sibling facts.
  "version": 1,
  "trains": [{
    "id": "mc-environment-drift",
    "intended_commit": "mc06",
    "signing_profile": "none",
    "phases": [{
      "id": "sibling-drift-precheck",
      "type": "precheck-sibling-drift",
      "params": {"siblings": [{
        "name": "mc-api",
        "path": "__SIBLING_PATH__",
        "expected_ref": "__EXPECTED_REF__",
        "clean": true
      }]}
    }]
  }]
}
