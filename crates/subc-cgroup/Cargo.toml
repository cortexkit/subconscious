[package]
name = "subc-cgroup"
version = "0.1.3"
edition = "2021"
license = "MIT"
publish = true
repository = "https://github.com/cortexkit/subconscious"
description = "Linux cgroup placement for supervised subc module children."

[dependencies]
tokio = { version = "1", features = ["macros", "process", "rt-multi-thread", "time"] }

[lints.rust]
unsafe_code = "deny"

[dev-dependencies]
subc-test-support = { path = "../subc-test-support" }
