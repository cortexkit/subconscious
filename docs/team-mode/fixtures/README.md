# Gate-evidence fixture mirror

`room1-contract-samples.mirror.json` is a GENERATED COPY of the
normative CKCRED fixture set, committed here so single-repo evidence
gathers (which cannot cross repository roots) can read it in-package
during contract gate rounds.

Source of truth: cortexkit-account
`workers/account/test/fixtures/room1-contract-samples.json` at the
commit named in the room notice accompanying each mirror refresh. On
any divergence the cortexkit-account copy wins; this mirror is
evidence, not authority.

Current mirror source: cortexkit-account @ c015c03 (v7 fixture set,
9/9 verification checks green).
